# B′ draft — changelog (one line per revision)

- **2026-08-01 · `draft-v1.md` created** (paper-writer, C2W4). Tier-i audit paper drafted from
  `bprime-rivals` (spine) + `bprime-theory` + `bprime-fb4-gate` + `bprime-fb1-recon` + `bprime-c6` +
  `harness-debt` + `PREREG-Bprime.md`. Main text §1–§7 + Appendices A–M. **NUMBER-FREEZE GATE OPEN:**
  every rival-arm number carries a `⟦F3⟧` provisional marker pending `bprime-rivals-f3`'s before/after
  table; CLU-column, theorem, protocol-validation, byte-ledger and attribution numbers are frozen.
  `bprime-c6` §5 lifted verbatim into §4.6.1 **minus its final tier-ii clause**, per the binding edit.
  Figures specified (Appendix K), not rendered. No LaTeX build requested.
- **2026-08-01 · `draft-v2.md`** (paper-writer, C2W4 v2 pass). **NUMBER-FREEZE GATE CLOSED** against
  `bprime-rivals-f3`: all 47 `⟦F3⟧` markers replaced with full-grid values, draft-state banner deleted.
  Every rival number now quoted at **pooled n = 9** per the Hub ruling; rescue verdicts re-stated at n = 9
  (RESCUED `deltanet`/`gdn`/`gdn2`; NOT RESCUED `ttt_mlp`; UNSTABLE `ttt_linear`), all three-seed rescue
  verdicts withdrawn. Headline survives and strengthens (−0.2592 … −0.4602, every margin ≥ 4.4 SE below
  zero) with the restricted rescued-arms form added. §2.6 rewritten as *"the standard was met"* + the
  three closing measurements + the initialisation-redraw disclosure + the selection-rule finding;
  contribution 1 gains the two protocol findings; §4.1.1 re-tabled at n = 9 with both code paths; §4.3's
  weak-inversion re-expressed on components available at n = 9; §4.5/App. H frontier column re-stated as
  "no quotable margin" rather than a three-seed verdict; B.5 gains the gate's power requirement; §6 gains
  L2a and L4a and rewrites L2/L4/L11; App. A.1 rewritten + A.1b added (first pass); App. I.1a–I.1d added
  (before/after, the second pass's own scorecard, the un-re-aggregated columns, the grid audit);
  App. J/L/M extended. Figures still specified, not rendered; no LaTeX build.
- **2026-08-01 · `draft-v2.md` RENAMED `draft-r2.md`** (C2W5 Hub, no content change). Head ruling
  A18.5 (2026-08-01) governs: B′ revisions use the **r-convention** (`r2`, `r3`, …), never "v2" — the
  "v2" form collides with the V2 short. Supersedes the `[C2W4-CLOSE]` ruling 5 (`draft-vN.md`) for B′.
  The post-referee revision will be `draft-r3.md`.

- **2026-08-01 · `draft-r3.md`** (paper-writer, C2W5 — the POST-REFEREE revision of `draft-r2.md`).
  **Referee must-fixes, all five closed:** **MF-1** the CLU's audit-table row printed ✅ RESCUED against a
  blank it reads *below* — now ⛔ **NOT RESCUED** (full −0.5261 ± 0.0863 vs blank −0.4221, lift −0.104),
  stated as a finding consistent with the thesis in §4.1.1/§4.2/§6 L2/B.5/App. L 1a, with §4.2's arm-count
  qualifier extended to our own arm; **MF-2** the two objects the audit is about are now specified — new
  **Appendix N** (task families: construction rule, query law, metric, chance, why three were struck) and
  **Appendix O** (rival arms: implemented equations, state formulas, the iso-state head-width derivation),
  plus **Appendix P** (the store under audit) for referee N-3; **MF-3** CM-27(c)'s two mandatory caveats
  added to §2.5 in our own voice — the anchor-vs-family objection (unresolved, carried to L1) and the
  coverage clause (two of three families 0/3 write-admissible, endpoint loss 0.246–0.381 vs tolerance
  0.05, escalation moves ≤ 0.005), scoped so it cannot be read against §4.1.1's query-level coverage;
  **MF-4** §2.2's n = 3 frontier rescue verdict replaced by the n = 9 form (0 of 15 cells clear the gate);
  **MF-5** pre-registration documents committed to supplementary material with a dated five-row table in
  the App. I preamble, and "previously published" → "the closed form stated in our pre-registration" at
  all three sites (§3.1, App. C, App. L 4; §4.6 → "our own earlier estimate", §5.4's internal-process
  clause cut per N-2).
  **Should-fixes:** SF-1 direction rule stated once in B.5 and wired into §2.2/§4.1.1/§6 L2; SF-2 the
  `b → (d_head, F2, rows, table)` mapping in B.2 + §4.1.1 + I.1c(e) (**and R4's rule: never quote a single
  TTT byte figure as *the* n = 9 value**); SF-3 −0.0789 now **±0.0620, n = 3, per-seed spread** at every
  use; SF-4a in-sentence `n = 3` labels on the 5× and held-out stress ranges + SF-4b the optional-stopping
  defence; SF-5 "seven independent groups" → the seven named near-misses; SF-6 both `d/s` figures now name
  the atom-width ruler; SF-7 discharged by the rider (App. H re-pointed); SF-9 (a) arXiv:2605.17590 **cut**
  (unverifiable — in no artifact), (b) GDN-2 → **Hatamizadeh et al., 2026**, (c) Guo et al. **(ICML 2020)**;
  SF-10 folded (below). SF-8 (figure renders) is not the writer's. **Nice:** N-2, N-3, N-4 (App. H CLU
  ±0.0139), N-6 ("this implementation class") applied; **N-1 (glyph strip) deferred to the LaTeX pass per
  Hub ruling.**
  **Rider 1 — the uniform n = 9 fold:** App. I.1c rewritten from `n9_full_columns` as the **full column
  set at a uniform n = 9** across four selection columns (launder · dividend · same-keys null · blank ·
  the three +0 B readers · paired `full − null`/`full − blank` · per-seed ledger), the first pass's
  three-seed columns retained as history in I.1c(f); the mixed-n framing and its "honest and temporary"
  labels retired at §4 preamble, §4.1, §4.1.1, App. J and App. M. ⭐ **CLAIM CHANGES from the fold, flagged
  per the pre-commitment: (1) §4.3's weak-inversion is now quoted as a DIVIDEND at n = 9 instead of
  argued from two components — DeltaNet +0.1515 ± 0.0600, GDN +0.5960 ± 0.0933, GDN-2 +0.6824 ± 0.0756 —
  which SHRINKS the gated arms' magnitudes by 42 %/22 % against the first pass's +1.02/+0.88 (both printed,
  before/after) while keeping the sign on 5 of 5 and adding DeltaNet as positive beyond 2 SE; (2) new
  first-class rival-side finding — BOTH TTT arms read below their own same-keys null (−0.2063 ± 0.1016,
  −0.1995 ± 0.0665) while all three delta arms read above it; (3) §4.1.1's coverage line is now all nine
  seeds (0.455–0.825), a wider spread than the registered three.** No rescue verdict and no headline
  changed sign.
  **Rider 2 — the deltanet frontier row:** App. H split into **H.1** (nine seeds, current code path, three
  arms × five head widths, **0 of 15 rescued, best lift +0.0694 ± 0.0491 — a LABELLED NULL**, byte-frontier
  column, never a dividend family) and **H.2** (the first pass's three-seed rows as history); §4.5 and
  App. J/K/M re-stated accordingly, with `ttt_mlp`/`gdn` declared NOT-RUN there and the `d ≤ 8`
  not-table-lossless caveat carried.
  **Citations:** cite-check applied in full (GDN-2 authors; Feng **paraphrase kept** + the "by the same
  logic"/datasets-not-memories scope clause; the **EDA (Li et al., 2026) concurrency clause** in §5.1;
  SDM "our own convention" clause at the 0.967 ratio); MUNKEY still cited **0×**, and §4.7's
  deletion-by-design sentence drops **"(oral)"** per ERRATA **E2a**. **Never-quote sweep: 0 violations**
  (see the report). Figures still specified, not rendered; no LaTeX build. 1697 → 2231 lines.
- **2026-08-01 · `draft-r4.md`** (paper-writer, C2W5 — the **mechanical fold** of the three artifacts that
  landed after `r3` froze at 16:12). Per-item disposition:
  **(1) The CLU column at n = 9 (`bprime-referee-closures`, a re-aggregation of banked per-seed cells — no
  CLU cell re-measured).** ⭐ **Claim change, flagged per the F3 pre-commitment:** the n = 3 "reads *below*
  its own blank store (−0.5261 vs −0.4221, lift −0.104)" framing is **replaced by measurement** —
  **NOT RESCUED at n = 9, lift −0.0465 ± 0.0406 (|t| = 1.14) ⇒ "statistically indistinguishable from an
  empty store", point estimate on the wrong side of zero**. Magnitudes moved (all improve, no sign moves):
  `full` −0.5261 → **−0.4370 ± 0.0417**; `+0 B`/raw margin −0.3180 ± 0.0804 (≈4 SE) → **−0.2897 ± 0.0328
  (8.8 SE)**; dividend −0.0789 ± 0.0620 → **−0.0561 ± 0.0315 (1.78 SE)** ⇒ ⛔ **every "the launder beats
  the store" softened to "reads no worse than"**; launder **−0.3810 ± 0.0345**, blank **−0.3906 ± 0.0124**,
  null **−0.6512 ± 0.0383**, `full − null` **+0.2141 ± 0.0443**. The "(its own table is already raw)"
  parenthetical becomes a **measured** statement (raw ≡ +0 B, float-identical 9/9 seeds). Sites: abstract,
  §1.1, §4 preamble, §4.1, §4.1.1 (row + three rewritten paragraphs), §4.2, §4.3, §6 L2/L2a/L11, §7, B.5,
  **new A.1e**, I.1c(a)/(b)/(f), J, L 1/1a, M, K. **The `n`-asymmetry is CLOSED** (the frontier curve stays
  banked at n = 3 and is labelled).
  **(2) The Mamba-2 (SSD) arm (`bprime-mamba2-arm`).** Headline **0-of-5 → 0-of-6**, range
  **−0.2563 … −0.4602** (min SE multiple unchanged at 4.43); row into §4.1, §4.1.1, §4.3's
  apples-to-apples + metric-native tables, §4.4 ledger, §5.1 (new state-space paragraph), **new A.1f**,
  **new O.2b** (equations, three asserted-equal implementations, sizing, four declared deviations, the
  block-level ablation), **new App H.1b** (frontier: NOT RESCUED at 5/5 head widths **and** in 3/3
  selections ⇒ the column goes **0 of 15 → 0 of 20**); §5.2's "SSM family named but not measured"
  **RETIRES**; §6 L5 moves Mamba-2 from reasoned to measured (reasoned = SDM, Titans); §2.6 gains the
  "0 of 45 → 1 of 54" grid-selection correction, the interior-optimum evidence and the block ablation.
  Both honest qualifiers carried verbatim: best `full` = **a tie with gdn2, not a win**; the +0 B margin
  **+0.0047 ± 0.0519 = a tie with zero** (and it flips sign under held-out selection).
  **(3) Selection-stability (Hub ruling, extends A17.4).** New rule stated in §2.2 and B.5 and applied
  everywhere: verdicts quoted only where **stable across all three registered selection rules** ⇒
  ✅ `gdn`, `mamba2` RESCUED · ⛔ `ttt_mlp` NOT · ⚠ `ttt_linear` **INIT-UNSTABLE** (relabelled from
  "UNSTABLE") · ⚠ **`deltanet`, `gdn2` SELECTION-DEPENDENT** (+0.077 ± 0.045, +0.669 ± 0.339 under
  held-out). No flattering margin is quoted against a selection-dependent arm. §4.2's restricted form now
  rests on **{GDN, Mamba-2}**; §6 L2's comparative half is carried by **two** arms; L4a, I.1a, I.1c, I.1d
  updated.
  **(4) Closure cracks.** **R2** CLU byte ledger labelled **modal (8 of 9)** at every site — 5456 B/100 B/
  54.56×, seed 8 5472 B/120 B/45.60× (six items admitted) — §4.1, §4.1.1, §4.4 (×2), §6 L6, A.1, A.1e,
  A.2, I.1c(e), P.4, Fig 2; **R3** §4.6's audited cell **3.72 → `d/s_fit = 3.59`**, ruler named and
  `sep/s_fit = 3.71` explicitly excluded (§4.6 table, §4.6 prose, §4.6.1, Fig 3); **R4** the `0.814` row's
  **±0.40 → ±0.31** (2·grad_se); **R5** Fig 1's mixed-`n` caption language **deleted** (uniform n = 9).
  **(5) r3 reconciliations ruled.** N186 causal framing **kept, softened** to the n = 9 form ("a candidate
  mechanism for why the written store reads no better than blank", §2.5 + §4.1.1); the 2605.17590 cut
  **ratified** (unchanged); Poliak **and** the Mamba-2 citation facts marked **⟦CITE2⟧** (§1, §2.4, §5.1,
  §5.3, A.1f, editorial item 6) pending `bprime-cite-check-2`.
  **(6) Figures.** App K gains a **render-status block** (Figs 1–5 rendered, PNG+PDF, 48-entry provenance
  table); Fig 1 spec = seven bars, uniform n = 9, NOT-RESCUED / INIT-UNSTABLE / SELECTION-DEPENDENT
  hatching — ⚠ **the current render carries six bars and a Mamba-2 re-render is requested** (report); Fig 2
  gains the modal-ledger caps + the SSD bar; Fig 3 names `d/s_fit = 3.59`; Fig 5 gains the two write loads
  and the 0-of-20 rival null.
  **Also:** new negatives **L 21–24** (seed-dependent CLU ledger / selection-dependence / the frontier null
  at 20 cells / the SSD tie); App N.1 gains a blank-store cross-section reconciliation (−0.4221 at 3 seeds
  vs −0.3906 ± 0.0124 at 9); App J rewritten (Mamba-2 out of NOT-RUN, Mamba-1/3 + four SSD-specific
  NOT-RUNs in). **A18.1 + never-quote sweep: 0 violations** (see the report). Figures rendered earlier this
  wave, not re-rendered here; no LaTeX build. 2231 → 2621 lines.
- **2026-08-02 · `draft-r5.md`** (paper-writer, C2W5 — the **citation-layer** fold of `bprime-cite-check-2`
  and `bprime-cite-check-3`. ⛔ **No measured number moved, was added, or was removed anywhere in the
  draft.**) Per-item disposition:
  **(1) §5.3 MIS-ATTRIBUTION FIXED (the one live error).** The quotes *"30–80× larger than B-trees"* and
  *"4 orders-of-magnitude more time to build"* were attributed to SOSD/Kipf et al. 2019; they are
  **Chesetti & Pandey, ACDA 2025 (SIAM) pp. 101–114, arXiv:2407.00590v2 §6.6**. The bullet is rewritten on
  cite-check-3 §3.3's base and now carries: the correct attribution with the version pin; the subject
  caveat (**RadixSpline and RMI**, with PGM **4× smaller** in the same paragraph); the sparsified-baseline
  caveat (**every 256th key**); that paper's own "roughly a tie" headline; and ⛔ **SOSD's opposite size
  verdict** (RMI 3 %, RS < 1 % vs B-tree 16 %; *"learned models indeed often outperform state-of-the-art
  implementations"*). The quotes are now cited for the **tone of the accounting**, never as a verdict
  against learned structures.
  **(2) All eight `CITE2` markers removed** (§1 ×2, §2.4, §5.1 ×2, §5.3, A.1f, editorial item 6).
  **Poliak** verified incl. "6 of 10" (preprint ≡ published text): §5.3's verb corrected **"beat" →
  "significantly outperform"**, "6 of 10" → "six of the ten". **Mamba-2** cited at the PMLR locus
  (**Dao & Gu, ICML 2024, PMLR 235:10041–10071**) at §1 and §5.1; A.1f's ⚠ citation-provenance row
  becomes a ✅ discharge row carrying the author-order trap (Mamba-2 = Dao & Gu), the triple-sourced
  `d_head × d_state` convention, and the honest residual (**the Mamba-2 paper body was never read**).
  **(3) GDN equation restriction LIFTED.** §5.1 now **quotes** *"a simple gated update rule,
  `S_t = α_t S_{t−1} + v_t k_tᵀ`, which uniformly decays all key-value associations…"* from the ICLR 2025
  camera-ready, with authors named (Yang, Kautz & Hatamizadeh) and ⚠ **their own hedge carried** —
  *"up to specific parameterization"* — plus the note that Mamba-2's shipped parameterisation is a per-head
  `P × N` state and that our arm's identity claim rests on Appendix O.2b, not on that sentence.
  **(4) §1 cite upgrades + the safe/unsafe claim split.** Mitzenmacher = **NeurIPS 2018** (quoted from the
  published §3.1), Kipf et al. = **non-archival NeurIPS 2019 ML-for-Systems workshop**, the companion study
  = **Marcus et al., PVLDB 14(1):1–13** (⛔ different first author; the two entries are never collapsed).
  ⛔ The unsafe blanket *"compare … at matched space (Mitzenmacher; Kipf)"* is replaced by the split:
  matched space is a **condition on the verdict** in the Bloom-filter line, and **a reported column** in
  index benchmarking. §5.2's survey blockquote is given the same split ("matched or explicitly accounted
  space") — ⚠ **flagged to the Hub as the one edit inside a negotiated sentence.**
  **(5) NEW Appendix Q — verified bibliography entries:** the seven verified BibTeX entries (Mitzenmacher ·
  Kipf/SOSD · Marcus · Chesetti & Pandey · Poliak · Dao & Gu · Yang et al.), each with its caveat note;
  **Q.2** records what is deliberately **not** printed (the unverified **Belinkov** corroboration entry,
  which this draft does not cite; Poliak's ar5iv-sourced Table 2 decimals). ⛔ Never-copy recorded twice:
  Semantic Scholar labels Poliak's venue *SemEval* — **\*SEM ≠ SemEval**, the ACL Anthology record governs.
  **(6) Editorial item 6 rewritten as DISCHARGED**, plus **new item 6b** naming every residual
  single-sourced/unverified citation (all qualitative; no number is quoted from any of them).
  **(7) Sweeps printed in the report:** claims-matrix **v2.11 §0.9 / CM-30** (incl. the §A20.2 read-protocol
  form and the `null*` = 0.00117 oracle-selected-upper-bound clause) + carried-forward A18.1 and r4
  patterns — **48 patterns, 0 real violations**; all 13 flagged clusters adjudicated in the report. One
  clarity fix arose from the sweep: App L 19's "0 of 15" now points at the 0-of-20 column of record.
  Figures unchanged and not re-rendered; no LaTeX build. 2621 → 2824 lines.
- **2026-08-18 · `draft-r6.md`** (paper-writer, `bprime-r6-evidence-fold` — the **evidence fold** of the
  three C2W8/C2W10 pieces the Shorts Advisor commissioned (charter Add. 8, Head ruling Q6 "update first,
  condense later"). ⛔ **No existing number, verdict, thesis sentence or headline finding (i)–(iii) moved
  anywhere**; every edit is additive, `draft-r5.md` stays the frozen citation-clean reference, and the
  referee pass runs on this file. 2825 → 3218 lines.
  **What was folded (approved wordings verbatim, riders attached):**
  **(1) The no-daylight row (N276, C2W8)** — matrix §0.12's approved form quoted verbatim at both sites;
  tier-i thesis at a third substrate (Split-CIFAR-10 census rig, `d = 12`, 3 arms × 3 seeds).
  **(2) The two criterion-4 tripwires (N294 INSECTS · N295 Metro, C2W10)** — matrix §0.13's approved form
  + the mandatory framing rider *"a FALLBACK BEING RETIRED, not a venue crisis"*, folded as FB4-class
  protocol evidence. **(3) The 24-h label embargo (N296, C2W10)** — the approved methodology form
  (+10.9 % / −0.3 %, asymmetric in the direction of firing; seasonal-naive(`t−24 h`) degenerate, the
  non-degenerate naive `t−168 h`), with the registry-precision deltas (+10.85 % / −0.34 %) beside it in
  the appendix.
  **Diff summary (every touched section, with the registry source per change):**
  · **§1.2** — new closing pointer paragraph (the three folded pieces, each labelled no-benchmark /
  no-memory-cell) [N276, N294/N295, N296];
  · **§2.5** — new closing block: the admissibility discipline run on real streaming venues, approved
  §0.13 wording blockquoted, framing rider, design-rule tie-in (recency window / 24-lag clock), the
  baseline-only scope sentence [N294, N295];
  · **§4.8 (NEW)** — the third-substrate row, §0.12 approved wording blockquoted, registered-term
  decoding, three riders in-text (matched-items-not-bytes with the 2,364×/4,728×/3.147× asymmetry ·
  metric-native-ceiling-expected with the attributability content · component-build-not-verdict) [N276];
  · **§6 L1** — one sentence: the bar now measured on designed AND real venues [N294/N295];
  · **§6 L11** — one sentence: the folded runs are baseline-only, no memory arm on real data, value legs
  NOT-RUN [N294/N295, N276];
  · **Appendix A.5 (NEW)** — flag provenance for the N276 run (commit `0f057a0`, JAX 0.9.0, seeds 0–2,
  full census knobs, instrument label "pass-3, pre-hardening", the width-flag quoted with its N290
  instrument caveat, byte ratios, launder audit-in-code) [N276, N290];
  · **Appendix A.6 (NEW)** — flag provenance for the two gate spokes + embargo (HEAD `7fcef50`, scratch
  venvs, protocols, arms — SAM-kNN port validation, ARF byte caveat 9,542,925 B = 14.35× = byte-matched
  to nothing, registered k = 5, the NINE Metro references named, shuffle-null construction, ±10 SD clip)
  [N294, N295, N296];
  · **Appendix J** — the old "any real-data leg" bullet split into five: real-data VALUE legs = DECLARED
  NOT-RUN for want of an admissible venue, never a null · no third tabular venue shopped · the
  `out-of-control` stream has no data source (declared NOT-RUN) · `rls_ff` diverged (declared NOT-RUN) ·
  language-modelling unchanged [N294/N295, N300, N296];
  · **Appendix L 25–28 (NEW)** — INSECTS fire + anti-hobbling inversion (raw-only would have passed the
  venue) [N294] · Metro triple fire incl. shuffle-helps [N295] · our own leak-free-prequential assertion
  refuted by mechanism + seasonal-naive degeneracy [N296] · the third-substrate null kept as a null
  [N276];
  · **Appendix M** — one sentence on the folded runs' seed disciplines [N276, N294–N296];
  · **Appendix R (NEW, R.1–R.3)** — the full protocol-evidence appendix: R.1 per-arm A3a/A3b, the
  registered rule quoted from code, attribution numbers, raw-read example, instrument label [N276]; R.2
  the two firings with all mandatory caveats (ARF byte caveat · "at ARF's shoulder, never beats ARF" ·
  port-required/one-line-estimates-void · registered k = 5 = 306.76 at the L = 14,894 budget, the k = 10
  ladder labelled anti-hobbling and 300.09 nowhere · NINE references · no per-band INSECTS number · the
  shuffle-is-not-a-drift-free-source limit · value surface = declared NOT-RUN, never a null · the
  fifth/sixth-firing count · the proposed-not-adopted shuffle practice labelled as such) [N294, N295];
  R.3 the embargo mechanism, fix, 45-arm delta highlights, verdict-unchanged note, scope rider verbatim
  (mechanism protocol-general, magnitudes not) [N296];
  · **Open editorial items 9–11 (NEW)** — ⟦N…⟧ markers are internal provenance stripped at circulation;
  the fold's new citations (Souza, Losing, Gomes/ARF, river, UCI Metro, Webb 2016) are UNVERIFIED, none
  in Appendix Q, cite-check owed; no new figure.
  **Permission/authority flags for the Hub (task rule 3):** no fold decision relies on a v2.16- or
  v2.17-added clause for *permission* — the wording sources are §0.12 (filed at v2.15, itself PROPOSED)
  and §0.13 (Hub-authored at `[C2W10-CLOSE]`), and the *permission* is the commissioning task + N276's
  disposition (§A32.1 "the no-daylight row folds into B′'s evidence base at the next revision") + §0.13's
  own routing ("routed to B′ as protocol evidence"). ⚠ One tension flagged, not resolved: §0.12's generic
  scope line "no paper number" vs the Head-sequenced fold that quotes §0.12's own approved numbers —
  treated as superseded by the later, specific commission; Hub to confirm at review. ⚠ The approved
  §0.13 embargo form's rounded +10.9 %/−0.3 % and the registry's +10.85 %/−0.34 % both appear (form in
  the anchor sentences, precision in R.3/L27) — both trace to N296.
  **Never-quote sweep (per-file over `draft-r6.md` only — never a directory-level grep; positive
  controls returned hits):** §0.9–§0.14 + task riders — 0 violations. Zero-hit patterns: `300.09` ·
  "eight/8 references" · "river ships" · `S_eff` · `4.95e-63` · `n_never_read`/`frac_never_read` · "arm B
  never" · "not separable by any read" · "φ is the binding constraint" · `any_basin` ·
  `best_is_also_lowest_drift` · K5/organizer-swap/UNASKABLE/`0.2719`/`0.7254`/compositional (rule 5:
  no toy-compositional claim added) · `13.9×` · pooled-ρ / `ρ(U` · INSECTS band-4 (`R(4)`/`A(4)`/91.25) ·
  "the completed gate"/`gate_pass` · undifferentiated "strong φ" (0 hits; the differentiated form used).
  Context-checked hits, all compliant: "verified to 1e-9" ×2 = r5's own self-negating guard; "criterion
  4" ×2 = the paper's pre-existing metric-nativeness criterion (the program's venue criterion is never
  named "criterion 4" in the draft); `0.272…` = a pre-existing rival fit-grid value; `out-of-control` ×2
  = declared-NOT-RUN sites only; ARF always adjacent to the 14.35×/byte-matched-to-nothing caveat;
  Metro references always **nine**; the CLU-budget arm always the registered k = 5 = 306.76. Approved
  wordings verified character-identical to the matrix (emphasis/blockquote markup aside), each present
  at exactly its two intended sites; framing rider present ×3.
  **Not done, by rule:** no abstract/§1.1-contribution change, no §7 change, no figure, no LaTeX build,
  no re-render; zero files touched outside `draft-r6.md` + this entry.

- **2026-08-18 · `draft-r7.md`** (paper-writer, `bprime-r7-referee-closures` — the **referee-closure
  revision** of `draft-r6.md`, commissioned by the Shorts Advisor, Addendum 12, the Q6 sequence's
  referee-closure step). ⛔ **Zero new measurements**; ⛔ **no measured number, verdict, thesis sentence or
  headline finding (i)–(iii) moved**; `draft-r6.md` is byte-unchanged (md5 `bc441dc2…` before and after)
  and remains the frozen reference the referee pass ran on. 3218 → 3658 lines (+508 / −68).
  **MUST-FIXES.**
  · **MF-1 CLOSED (Figure 1 now carries the Mamba-2 bar).** Re-rendered from the **banked** artifacts at a
  uniform `n = 9` — seven bars (six rival arms + the CLU), each bar's gate status *derived* from the
  banked selection columns, renderer asserting bar-count = 7 and `n = 9` before drawing; new files
  `figures/fig1_headline_raw_margin_r7.{png,pdf}` + a 14-entry provenance record
  (`outputs/bprime-r7-referee-closures/figure1_provenance_r7.json`); the r6 six-bar render is left in
  place, untouched. Draft sites: **Appendix K render-status block** (the "one re-render is outstanding"
  disclaimer **deleted**), **Appendix K Figure 1 spec** (target state → rendered state + a bar-by-bar
  table + the zero-new-measurement statement), **§1.1 headline-figure paragraph**, **editorial item 4**
  (rewritten as discharged; Figures 3/5 caption checks and Figure 2's SSD bar remain open),
  **editorial item 11**.
  · **MF-2 CLOSED (six fold-introduced citations verified, folded).** **Appendix Q.1** now carries
  **thirteen sources / fourteen BibTeX records** — the six new sources pasted **verbatim with their
  caveat `note` fields** (Souza et al. 2020 · Losing et al. 2016 · Gomes et al. 2017 · Montiel et al.
  2021 + the `river` 0.25.0 software `@misc` · Hogue 2019 UCI · Webb et al. 2016), heading and preamble
  updated (why `river` gets two records); **Q.2** gains two verified-but-not-printed entries (the 2019 ARF
  correction, unread and inert while no ARF number is quoted; the `ltm_size = 0.4` provenance).
  **Editorial item 10 rewritten as DISCHARGED** with the three usage qualifiers and the two declared
  residuals (ARF body text unread; DMKD publisher pages SSO-blocked).
  · **MF-3 CLOSED (the N224 `s`/subtraction clause).** The `α‖q‖²` subtraction convention is now stated
  wherever an `s`/`d/s` statement is made, `s = 0.40` is **flagged for a check and not discharged in
  general**, and the correction's direction (`s` smaller, `d/s` larger ⇒ suppression *stronger*, printed
  numbers conservative) travels with it. Sites: **§4.6** (new rider paragraph), **§4.6.1** (ruler +
  convention pointer under the lifted paragraph), **§6 L9(ii)** ("discharges it" → the flagged form),
  **Appendix J** (new declared NOT-RUN, never a null, direction pre-stated), **abstract** and **§7**
  (pointer travelling with the `exp(−½(d/s)²)` law), **§3.3** (its `d/s` labelled a *designed* width, not
  a fitted one, so the two rulers cannot be confused).
  · **MF-4 ROUTED, NOT CLOSED HERE (no draft text).** The §0.12 "no paper number" vs Head-commissioned-fold
  permission tension is a **Hub/Head matrix-record item**: the ruling that the commission supersedes
  §0.12 for exactly the N276/N294–N296 material should be written into the matrix (dated clause; §0.12's
  vehicle v2.15 is PROPOSED and v2.16/v2.17 confirmation is owed). ⛔ Nothing in this revision relies on a
  v2.16/v2.17 clause for permission; the draft's handling is unchanged from r6.
  **SHOULD-FIXES — all ten closed.** · **SF-1** "statistically indistinguishable from — **or below** — a
  small table's", with the analogy pointed at the dividend/launder comparison (−0.0561 ± 0.0315, 1.78 SE):
  §1.2(i), §4.8 heading + opening paragraph. · **SF-2** the bare ordinal enumerated ("a third in this
  paper's own counting: after this audit's two designed families") and the substrate named *real-image*:
  §1.2(i), §4.8 heading/body/closing sentence. · **SF-3** the sixth projected-control handicap wired into
  **§2.4** (0.614; **+0.6139 ± 0.1386**, pointer to I.1c(b)). · **SF-4** "three independent estimates" →
  "three estimates by **two independent methods**" (§4.6; L9(ii) harmonised). · **SF-5** the **§A20.5
  substrate-scope sentence** added verbatim, in the paper's own voice, as the first of §1.2's now-three
  hard boundaries. · **SF-6** the seeds-3–8 power addition **located and its status stated plainly** in the
  Appendix I preamble: it lives in the run record's flag-provenance seeds row, **not** in any of the five
  dated documents (document 4 registers seeds 0–2) ⇒ **no dated pre-registration is claimed** for it, and
  the fallback defence is printed instead. · **SF-7** the "foreordained headline" rebuttal assembled in one
  place — a six-bullet itemisation of what could have come out differently and did not (**Appendix N.1**,
  immediately after the quotable construction sentence) with a pointer paragraph in **§4.2**. · **SF-8** a
  decode sentence adjacent to §2.5's approved blockquote (*frontier* = within the pre-registered
  admissibility margin; INSECTS 1.90 points below ARF-100, 0.10 inside the threshold, *"at ARF's shoulder",
  never "it beats ARF"*; Metro fires outright by 4–25 %) — ⛔ the approved wording itself untouched.
  · **SF-9 (HEAD-APPROVED)** the r6 evidence folded forward: the **abstract** gains the real-image
  replication at the **2,364×** handicap with its matched-items/component-build riders in-sentence;
  **§1.1** gains the label-embargo finding as contribution 1's third protocol finding (with its
  one-stream/mechanism-general rider) and a **new contribution 7** for the venue-admissibility discipline
  (approved §0.13 wording verbatim + the *frontier* decode + the fallback-being-retired and baseline-only
  riders); **§7** gains a closing paragraph carrying all three folded pieces with their riders.
  · **SF-10** internal-note voice struck from the **§4.1** NOT-RUN table note (`never "a preprint"`) and,
  same class, from **§R.2.1** ("the spoke's own"); **editorial item 8** now names the sites rather than
  deferring generically.
  **CITE-CHECK RECONCILIATION 1–3 + 5.** · **(1)** the **Webb** clause replaced with the cite-check's
  recommended wording verbatim — framework theirs (Eq. 6), the **total-variation instantiation ours**,
  their own case study uses Hellinger, TV meets their stated metric criteria (§R.2.2). · **(2)** the
  **MOA-vs-`river`** ARF clause added at both sites (§R.2.1, A.6): *`river` ARF ≠ MOA ARF*; ours is `river`
  0.25.0's `ARFClassifier`, theirs MOA; **ensemble size 100 = MOA's default, our inference**. Flag F1 also
  applied: "the published state-of-the-art ensemble" → "the venue's **best published method**", scoped to
  the six methods those authors ran. · **(3)** "a permitted 3,000" relabelled: a **3,000-exemplar STM cap**
  from the authors' **reference-implementation default `ltm_size = 0.4`**; the ICDM paper publishes
  `k`/`L_min`/`L_max` only (§R.2.1). · **(5)** editorial item 10 rewritten as discharged (above).
  **NICE.** · **N-2** the mechanics-substrate reading split: **INSECTS's half is our registered ruling**,
  **Metro's is our own inference** (§2.5, Appendix R.2). · **N-3** "criterion 4" given its parenthetical
  at first main-text use (§4.3), with the paper-internal object named. · **N-4** one family-wise-error
  disclosure: gates are **per-cell, no multiplicity correction applied or claimed** (§2.2). · **N-5** the
  `σ_q/spacing 0.334 → 0.210` arrow decoded (PCA reference → fitted contrastive encoder, `d = 12`, 3/3
  seeds) with the direction of safety stated (R.1). · **N-6** the R.3 delta convention stated:
  `(embargoed − leaky)/embargoed`, positive ⇒ plain prequential flattered that arm. · **N-7** the
  matrix-approved **N190 companion clause** carried in §1.2 ("a store that organizes well and then reads
  like a cheap table scores dividend ≈ 0 by design — an inference-cost win, not a failure").
  ⚠ **N-1 DEFERRED to the Hub, deliberately and not silently:** whether §0.12's approved form subsumes
  N276's "pass 1's 1,253× stream-launder caveat travels unchanged", and how the 832 B *cue-side* table
  should be decoded beside the *stream* reads, are ruling questions on an approved wording — no clause was
  invented for either, and the r6 text stands unchanged at both sites.
  **Never-quote sweep (per-file over `draft-r7.md` only; positive controls returned hits: `54.56` 14 ·
  `306.76` 3 · `14.35` 2 · `0.2897` 13 · `no-daylight` 4 · `2,364` 6 · "ARF's shoulder" 3).** Zero-hit
  patterns: `3.6 SE` · `CLU-former` · `K5` · `UNASKABLE` · `0.2719` · `0.7254` · `300.09` · `S_eff` ·
  `4.95e-63` · `n_never_read` · `any_basin` · "the completed gate" · `gate_pass` · `91.25` · "principled
  forgetting" · "we alone delete" · `ICML 2026` · `0.3019` · `AttentionPsi` · "Def. 1" · "0 of 5 rival" ·
  `compositional` · `organizer` · "de Souza et al." (as a citation form) · `Pfharinger` (as a name field) ·
  a "-016-" Webb DOI. Context-adjudicated hits, all compliant: `1089` ×1 = the self-negating erratum site ·
  *"it beats ARF"* ×1 = inside the mandatory **never**-form · "river ships **NO** SAM-kNN" and *never "de
  Souza et al."* = the verbatim never-copy warnings inside the new Q.1 notes. Approved wordings verified
  present and character-faithful after the pass (§0.12 no-daylight ×2 · §0.13 admissibility ×3, the third
  being the new contribution 7 · framing rider ×4 · embargo form ×1 · N190 companion ×1 · §A20.5 sentence
  ×1); every mandatory rider count is unchanged or higher than r6's, none lower.
  **Not done, by rule:** no new measurement of any kind; no restructuring beyond the listed findings; no
  LaTeX build (none requested); Figures 2–5 not re-rendered; nothing from the missing-experiment list
  entered (that list is a separate Head decision); `draft-r6.md`, `draft-r5.md` and all earlier revisions
  untouched.
