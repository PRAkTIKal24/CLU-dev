# F5 arXiv note — revision log (one line per revision)

- 2026-07-10 · wave-14 · physics-theorist · **v0.5 CORRIGENDUM-2 — Prop-9's FDT noise is Newtonian-only (BLOCKING, now cleared)** (task `f5-corrigendum-2`; defect from `v2-symmetry-deepdive` §7bis R8 / §10 X6, independently re-derived and *strengthened* here). §11 stated $\sigma_i^\star=\sqrt{M_{{\rm eff},i}T\gamma(2-\gamma)}$ as **the** exact discrete-FDT noise, as a class-level statement, and asserted the corrected relativistic chain is "$O(\varepsilon^2)$-biased." **Both false.** New §6.2 ("Thermal budget") replaces them with two propositions: **Prop. `prop:gsmooth` (Gaussian smoothing)** — if the *last* sub-step of a discrete chain is $p\mapsto D+\sigma\odot\xi$ with $\sigma_i>0$ constant and $\xi$ independent, then **every** invariant momentum marginal is a Gaussian convolution, $|\hat\mu_p(t)|\le e^{-\frac12t^\top\Sigma t}$ (proof: one line; uses *nothing* about $D$, hence holds for any damping, linear or not); and **Prop. `prop:relnogo` (relativistic Gibbs no-go)** — since $H$ is separable, $\pi_p\propto e^{-T(p)/T}$ is potential-free and, in relativistic mode, Maxwell–Jüttner, whose characteristic function decays as $C|t|^{-3/2}e^{-m_0c|t|}$ (exponential, not Gaussian), violating the bound for $|t|\gtrsim2m_0c/\sigma^2$. Hence **no $\sigma$ — no per-mode $\sigma_i$, no full $\Sigma\succ0$ — gives the coded relativistic Langevin a Gibbs invariant**, for *any* $V_\theta$ and *any* $\gamma$. The Newtonian case is exactly the boundary ($\sigma^\star$ saturates the bound), so the corrected statement **contains** the old one and is strictly stronger. Two further new results: **(i) the control parameter is $d\Theta=dT/(m_0c^2)$, not $\Theta$** — the coded $T(p)$ shares one square root over all $d$ coordinates, so $\mathrm{Var}_{\rm MJ}/(M_{\rm eff}T)=1+(d+2)\Theta/2\to(d+1)\Theta$ and $\mathrm{KL}=\tfrac{1}{16}d(d+2)(d+3)\Theta^2$ (exactly $K_2(1/\Theta)/K_1(1/\Theta)$ at $d{=}1$); a 1-D scan badly understates a high-$d$ unit, and "raise $c$" means $c\gtrsim\sqrt{dT/m_0}$. **(ii) An exact, cheap repair (latent mass):** $\sqrt{\cdot}$ is a Bernstein function, so Maxwell–Jüttner is a Gaussian scale mixture with one shared latent scale, $p\mid s\sim\mathcal N(0,M/2s)$, $s\mid p\sim\mathrm{InverseGaussian}(\tfrac{c^2}{2TT(p)},\tfrac{c^2}{2T^2})$ — *the correct relativistic thermostat is the coded Gaussian one with a randomized inertia equal to the relativistic mass $m_0\gamma_{\rm Lorentz}$*. It is the state-dependent-$\Sigma$ escape the smoothing proposition leaves open, costs one inverse-Gaussian draw per step, and dominates an exact MJ refresh (keeps momentum persistence). §11 limitation bullet re-scoped; new check row (e$'$) (15 rows); (e) re-labelled "Newtonian". Verification: `.claude/scratch/f5-corrigendum-2/{mj_theory,mj_dim,nogo_chain,fixes}.py` — free-particle-on-torus KS tests (coded $p$ Gaussian $p{=}0.97$; MJ rejected $p{=}0$), closed-form characteristic function vs numerical FT to $1.4\times10^{-14}$, $d$-amplification on the chain ($-0.111\to-0.633$, $d{=}1\to64$, Newtonian control $\le10^{-3}$), latent-mass fix (KS $D{=}0.0013$, bias $-0.727\to+0.0011$), and the $d{=}1$ deep-dive table reproduced independently to $\le2\times10^{-6}$. tectonic build clean (f5-note.pdf, **13 pp**, 0 unresolved refs). **F5's arXiv push is unblocked on this axis.**
- 2026-07-09 · wave-12 · physics-theorist · **v0.4 CORRIGENDUM — kinetic-isotropy error (BLOCKING, now cleared)** (task `f5-corrigendum`; proof/sites from `v2-symmetry-deepdive` §4.1/§10-R3). §3.2 asserted, unhedged, that a non-isotropic $M$ makes an $SO(2)$ channel "a pseudo-Goldstone one with $\mu^2\propto$ the mass splitting." **False**, and contradicted by our own data (`v2-full-runs` item 5: $\mu^2_{\rm ang}\sim10^{-15}$, angular $n_{1/2}=\infty$, write-freeze $0$). Replaced by **Prop. `prop:kinblind` (kinetic-spurion blindness)**: $W=M^{-1/2}KM^{-1/2}$ is a congruence, so by Sylvester's law of inertia $\operatorname{rank}W=\operatorname{rank}K$ and every flat direction keeps $\mu^2=0$ **exactly** for any invertible $M$; the vacuum manifold, the channel count, and the $\gamma>0$ latch are intact, and the latched physical direction is $\ker K$ itself (unrotated). Anisotropy perturbs only the **Noether current**: bounded, non-secular ($H$ conserved + $V$ coercive ⇒ compact orbit), and on the vacuum ring exactly $Q=\sqrt{2E}F(\vartheta)$, amplitude $\sqrt{2E}r_\ast(\sqrt{M_{\max}}-\sqrt{M_{\min}})$, period half a revolution. Abstract clause re-scoped (isotropy binds the *current*, not the register); check (g) re-scoped and its "2.6" **retracted as a drift number** (it is a running-supremum excursion, not reproducible across two algebraically identical $\nabla V$ spellings); new check row (g$'$) added (14 rows; legacy count of 13 preserved in the provenance line); decay constant named $F:=\sqrt{M_{\rm eff}}\,r_\ast$ (not the orbit radius $r_\ast$, which is the vacuum radius/condensate); one clarifying clause added to §8 (isotropy ⇒ equivariance of the *map*; the latch survives without it). tectonic build clean (f5-note.pdf, 11 pp, 0 unresolved refs). Verification: `.claude/scratch/f5-corrigendum/{verify,verify2,verify3}.py`. **F5's arXiv push is unblocked on this axis.**
- 2026-07-09 · wave-11 · paper-writer · **v0.3 count fix** (task v2-revision-3, SF2-1 from `v2-referee-2.md`). Provenance appendix (strip-on-arXiv block): "all 14 checks reproduce F5 App-N" → "all **13** checks (a)–(m) reproduce F5 App-N" — the verification table has 13 rows (a…m); the v0.2 abstract fix ("All twelve"→"All results") had left this second, inconsistent count in place. Closes the free reviewer snag if the strip is ever missed. No other change; the exact-map "∼5×" misprediction (Prop. metric-bifurcation; check (k), γ=0.1, ε=0.1, deepest ratio 0.19) is UNCHANGED and is now the explicitly-attributed source of the V2 short's exact-map clause (MF2-A). tectonic build clean (f5-note.pdf, 164 KiB). Remaining pre-arXiv items: title/authors (Head) + the arXiv id for the shorts' citation string.

- 2026-07-08 · wave-8 · paper-writer · **v0.2 referee-revision** (task v2-revision, MF-5). Stripped all 5 `\todohead` [TODO-HEAD] margin notes (title, Q1 Goldstone, Q3 anharmonic-%, Q5 check-(c), Cor-3) + removed the `\todohead` command definition and stale header comment. Q3: dropped the "few-to-fifteen-percent" parenthetical (§11), replaced with neutral "measured in companion evidence-side work". Q5: row (c) given a neutral "supplementary check used by companion work" line. Cor-3 footnote (Head DEC 2026-07-08): CUT the "appears in at least one public instantiation, where it is inert by construction" de-anon clause; footnote reduced to the pure class fact (mean-spectrum penalty degenerate → use max-statistic). N-2: abstract "All twelve results"→"All results" (13 checks a–m; count mismatch removed). tectonic build clean (f5-note.pdf, 168 KiB, no TODO boxes render).
- 2026-07-07 · wave-6 · paper-writer · **v0.1 LaTeX conversion** of `.claude/outputs/f5-arxiv-note.md` (physics-theorist distillation of F5 v1.1). Single-column article class; builds clean with tectonic (f5-note.pdf, 0 overfull). All 12 scoped results + all corollaries retained. Corollary-grade material (Cor 2 singular-value pairing, Cor 3 mean-spectrum degeneracy + its practitioner footnote) moved to Appendix (Head policy 2026-07-07). Theorist open questions 1/3/5 carried verbatim as [TODO-HEAD] margin notes (Minami–Hidaka ref; anharmonic-% figure; squeeze/boost check (c)); Cor-3-footnote retention flagged as [TODO-HEAD] too. Title/authors placeholders (3 titles in cover note). Provenance appendix marked "strip on arXiv". No editorial calls made. Awaiting Head resolution of the [TODO-HEAD] items before arXiv.
