# V5-short changelog (one line per revision)

- v0.1 (w16, paper-writer): first draft. Thesis = the (μ,γ,T) budget cube led by CM-16b (V-curve, one damping-optimum curve across ~11 decades in μ², generalizing to emergent 3/3 seeds); CM-16a (T=0 latch) scoped designed-only with N46 owned in §3.4; friction-preserves/temperature-erases (§3.2, coset-diffusion law + sign flip); the 107.77±4.78× refrigerator vault (§3.3, NOT 13.9×); designed-symmetry-precondition main-text sentence. Erosion-as-restoration → App E per venue-follow-up §3 ship rules (b/c novel, a/d cite-substrate; honest framing preserved verbatim). Coleman/MW + fdt+Newtonian scope → App F. T_φ shredder → App G (future work, no promissory in main text). draft.md canonical; draft.tex source (unbuilt — no TeX toolchain); figs copied from v5-gate + t-lever. Headline fig = the V-curve (fig1_vcurve). Every caption carries the fdt+Newtonian flag; every n₁/₂ carries Δ=0.5 and ℓ_θ/Δ. F5 cited as (Anonymous, 2026); no cross-short citations. Matrix-consistent with V2 (shared numbers/scope; V2 App-J holds the same t-lever/v5-gate material as appendix, V5 leads with it).

- v0.2 (2026-08-18, `v5-rebuild`, paper-writer): **GROUND-UP REBUILD, not a revision** (Add.18 §2, on Head rulings Q1/Add.2 (V5 = budget cube + post-w12 R1 estate), Q11/Add.8 (V5 OWNS the V-curve/critical-damping law), Add.20 (PALM **SHORT track, hard 4 pp main text**)). **New contribution arc, one arc, three contributions:** (1) the (μ,γ,T) budget with the **damping optimum as headline** — one V-curve, argmin `0.902±0.003×γ_crit`, slopes `−1.0020`/`+1.116`, 3/3 emergent seeds, ≈11 decades in μ² (CM-16b; `t-lever-forgetting` §4 + `v5-gate` §3.4) — plus friction-preserves/temperature-erases (`D_θ=εT(2−γ)/(2F²γ)`, 1.0068±0.0219 over 25 cells; sign flip 10/10; 3.77±0.23×) and the vault **107.77±4.78×** (D̂-estimator, pre-registered; scalar control 13.28±0.12×; ⛔ never ~14× as the vault); (2) **NEW — the R1 deletion estate**: CM-25(f) full-scope exact deletion verbatim (0.29×–1.71× lattice capacity, AUC 0.5000±0.0000 on six statistics, byte-equal 1.0000, three stated conditions), the **mandatory Blelloch–Golovin (FOCS'07) attribution verbatim at every exact-deletion site** (N118), CM-23(y) trilemma with the compute-adaptive-read corner as the quotable one, `R₅₀` (CM-25(g)/N128) with N129's `γ_read=0` oscillatory caveat, and **N108's required sentence "the store stops answering before it stops leaking"** with the no-distinguishability-per-se denial; N112 overflow scope clause, N127 capacity-qualifier removal, N131 citation fence (Guo **§3 Eq. (1)**, ⛔ no "Def. 1/2", ⛔ no Ginart-A.5 cost sentence — M1 treated as not-available), N163/N168 "we alone delete" ban discharged by leading with the flat-table trivial substitute; (3) **NEW — the shipped three-state lifecycle** in §0.13's approved wording verbatim (PROTECTED ⇄ ACTIVE → TRASH, 7/7 legs, kill-conditions before the verbs, L4 UNEXERCISED / 0 refusals) with **both mandatory riders** (demotion = re-exposure to designed decay, never the trash region; trash criterion keyed on `read_hits`, never depth), **zero VALUE/benchmark numbers, the value surface labelled a DECLARED NOT-RUN (never a null)**, substrate labelled a mechanics instrument, never a claim venue. **Blast-radius sweep (N149/N150) executed:** the pseudo-Goldstone tilt refuted IN SIGN on a learned store and the `τ_max = Γ/2α` ceiling now travel with every lifetime/tilt sentence (main text §3.2 tail + K.3); the ε-notation collision (integrator step vs tilt) is stated explicitly. **Standing obligations discharged:** §A20.5 substrate-scope sentence once in the paper's voice (§1); the score sentence "external benchmarks won on their own headline metric = ZERO" (§3.3); Q5/Q9 scale-as-scope-choice line with named future experiments and no C3 number (§5); C-9 negatives distributed to Appendix J (15 entries); App-E erosion ship rules preserved verbatim as Appendix H (b/c novel single-sourced, a/d cite-the-substrate) with a new designed-vacuum-only rider; anonymization note extended per PALM (*"any supplementary or linked material as well, including code"*). **Page budget — MEASURED, not estimated: main text = exactly 4 pp** (`article`, 10pt, 1in margins, single column, headline figure included; 2,617 words) — verified by compiling main-text-only through `tectonic` (`.claude/scratch/v5-rebuild/maincount.{tex,pdf}`, 4 pages); full document with appendices = 17 pp. **Main-text-vs-appendix cut map:** MAIN = abstract · §1 (nomenclature+discipline, contributions, verification/evidence labels, substrate-scope sentence) · §3 setup preamble · §3.1 V-curve + Fig. 1 · §3.2 sign flip + vault + designed-symmetry-precondition rider · §3.3 deletion (CM-25(f) + BG attribution + trilemma corner + R₅₀ + N108) · §3.4 lifecycle · §4 related work (compressed) · §5 limitations + scale + horizon. APPENDIX = A flag provenance A.0–A.9 (incl. the banked full Setup) · B diffusion law/sign flip/latch/knob table · C emergent arm + T\* + instrument warning + two-observables caveat + C.7 (banked precondition subsection) · D vault in full + Fig. D.1 · E deletion estate in full (placement rule, theorems-as-instantiations + attribution, exactness/landing, load sweep + overflow, leakage curve, trilemma + gate, citation fence) · F lifecycle legs + UNEXERCISED discussion + owned reconciliations · G Coleman/Mermin–Wagner + fdt/Newtonian scope · H erosion-as-restoration · I T_φ horizon · J negatives ledger · K banked main-text material (K.1 nomenclature/discipline in full, K.2 related work in full, K.3 the learned-store refutations in full). **Sweep:** per-file, positive-controlled never-quote sweep over the finished draft vs matrix §0.1–§0.14 + charter §4 + inventory §4(c) — **0 forbidden hits, instrument LIVE** (`.claude/scratch/v5-rebuild/sweep.py`). **Build:** `draft.md` canonical; `draft.tex` generated from it and **BUILT** with `tectonic` (17 pp, no errors; hbox warnings only) — this supersedes v0.1's "no TeX toolchain" note. Title/authors remain placeholders; theory note cited as (Anonymous, 2026); no cross-short citation anywhere (C-8).

- v0.3 (2026-08-19, `v5-revision-1`, paper-writer): **the referee-closure revision** — all 14 MUST-FIX, 12 SHOULD-FIX, 6 NICE of `v5-referee-v02` and the 7 reconciliation items of `v5-scope-scout`, with **zero new measurements** (every figure is a replot of banked JSON; no number changed, added, rounded or smoothed). **Headline figure replaced** by the collapse `n₁/₂/n₁/₂^min` vs `γ/γ_crit` (5 designed radial + 3 emergent coset curves + the flat corner; log-μ² colourbar carrying the eleven-decade span; MF-5/N-4), the pseudo-Goldstone panel demoted to App C (Fig. C.3), and every internal source-report label stripped from every figure (`Cor-13`, `F5 Cor-13`, `T5`, `T6`, `CM-16`, `Fig 1/2/4`; MF-5/SF-4). **Instrument honesty:** the Jacobian-vs-Hessian span note (MF-1, §2.1 + C.3) and the Jacobian-vs-rollout disagreement `233.6/653.3/249.0` vs `190/370/150`, `1.23–1.77×`, `19–43%` (MF-3, §2.1 + C.3 + Limitations (iv)); asymptotic-window qualifiers restored on both arms with the fit windows stated (MF-4/SF-6, §2.1 + B.6 + C.3); the two flatness numbers labelled by grid and the grid minimum `7.6e-5–2.0e-4` printed (SF-1/N-6, C.2). **Wording:** "three regimes of one curve, evaluated at two values of μ, not two laws" replaces "one curve at three values of μ" (MF-2, CM-16b); contribution (2) reframed as the **composition** claim (SF-8/SF-9/G1); §3.4 lifecycle moved to App F.0 verbatim with one contributions sentence retained. **Corrections:** D.1's vault ratio is `[γ_eff(2−γ)]/[γ(2−γ_eff)] = 13.88×` (MF-8); the 2503.21536-as-CD sentence deleted at both sites and replaced by Fischer & Igel **2010, 2011** + Nijkamp 2020, with Toledo-Marín described accurately as hierarchical feature learning (MF-10); the k-regime scope clause pasted in V2 v0.8's printed form (MF-11); the stale "pending a targeted follow-up" marker replaced by the executed two-instrument search with its coverage statement (MF-12); Guo = **§2, Eq. (1), the ε-form** at all three sites (R1); the history-independence lineage rewritten (Snyder 1977 / Micciancio = oblivious ≠ canonical / WHI-SHI = Naor & Teague; R2); Mo gains "**at least**" at both sites (R3); conformal symplecticity → McLachlan & Perlmutter 2001, HLW with year for leapfrog/stability only (R4); GMOR and Coleman/Mermin–Wagner cited (R5); MUNKEY cited venue-free with the corrected Average-Gap phrasing (R6/SF-10); Titans cited (R7). **Controls and scope:** the FIRED TTL-flag laundering control wired into §2.3 and a new E.5(vi) table (`0.983` vs `1.000` exact; `0.559` vs `0.996` at σ_obs=0.1; MF-9); the packing numbers `61/64`, per-offered `0.9531`, refuse-and-relocate `43/64` printed in E.3 (SF-2); the `|c|`-distribution clause on every retention number (SF-3, §2.3 + E.5); abstract riders — vault designed-only/3 seeds, deletion's three conditions + recency exclusion + dim-3/capacity-8–64 scale (MF-13); the falsifier statement for the V-curve (SF-11). **Mechanics:** §4 rewritten with the venue-native brief in a contrast-not-competition register (SF-7); **References section added** — 58 verified records (MF-6); **all cross-references fixed** — nine `§3.5` danglers eliminated, sections renumbered so the markdown and the compiled PDF agree (Results = §2, Related work = §3, Limitations = §4), Limitations (iv) restored (MF-7); the five orphan appendix PNGs embedded as Figures B.1–B.3, C.1, C.2 (SF-5); project-identifying paths neutralized in App A/E, hashes kept (SF-12); A.0 made self-contained on the 2×2 reduction and "companion" removed from the theory-note reference, which is now non-load-bearing (MF-14). Style pass per the Head's 2026-08-19 directive (ABT openings, macro-to-micro, sparse bold, brevity) applied to every sentence touched; matrix-approved wordings, mandatory riders and scope qualifiers left verbatim. **Page budget — MEASURED: main text = exactly 4 pp** (generic `article`, 10pt, 1in margins, single column, headline figure included; 2,241 main-text words) via `tectonic` on a main-text-only build; full document 25 pp. ⚠ The venue template is the **NeurIPS 2026 style**, not installed here — the 4 pp is a generic-article approximation with a thin margin and must be re-measured in the venue class before freeze. Referee move-menu applied in order: §3.4 → App F, the attribution block reduced to its one-clause form in-line (full block verbatim in E.2), the trilemma reduced to its compute-adaptive-read corner (full statement in E.6), the gated-stiffness `R₅₀` numbers moved to E.6. The do-not-cut list held: N108's sentence, CM-25(f) verbatim, the BG attribution, the score sentence, the §A20.5 substrate sentence, the designed-symmetry precondition and the `fdt`+Newtonian fine print are all present. **Sweep:** per-file, positive-controlled over `draft.md` **and** `draft.tex` — 0 non-benign forbidden hits (3 flagged, all inspected: two literature-description uses of "certified removal" for Guo, one "independent of chain length" that now carries the k-regime clause immediately after); semantic hermeticity `companion`/`sibling`/`program`/`our other` = **0**.

- v0.4 (2026-08-19, `v5-final-pass` stage 1, paper-writer): **the validation fold** — the five items of Add.34 / `v5-vcurve-validation`, verbatim, **zero numbers invented, changed, rounded up or smoothed, zero new measurements**. **(1) The MF-3 caveat is retired and replaced by its measured truth (§2.1 "Two instruments, one law", App C.3.1).** The V-curve **survives the rollout instrument**: argmin `0.9001 ± 0.0052` (rollout envelope-rate, I-R3) vs `0.9032 ± 0.0027` (Jacobian, I-J) in `γ_crit` units, slopes `−0.983 ± 0.013` / `+1.117 ± 0.010` vs `−1.0020 ± 0.0003` / `+1.1182 ± 0.0107` on identical windows, decay **rates** agreeing to `0.995 ± 0.003` in the asymptotic window [report §0, §3.1, §3.2]. ⛔ The banked **"19–43 %" is deleted at both its sites** (§2.1 and App C.3) and replaced by the **constant-in-γ amplitude offset on the first-crossing instrument**: `n_jac/n_R1 = 1.3307 / 1.7981 / 1.8204`, CV `2.2–3.7 %` over 21–25 γ-points, `d log(ratio)/d log γ ∈ [−0.0034, +0.0001]` vs a `0.10` falsification threshold [report §3.2]. The four instruments (I-J / I-R1 / I-R2 / I-R3) are defined and tabulated in C.3.1 [report §2, §3.1], with the standing rule **"never quote a rollout `n₁/₂` from a first- or last-crossing threshold below `γ_crit`"** and the full-window s44 miss (`0.9216`, `0.0004` below band) stated with its cause [report §3.2, L3]. **(2) The instrument-floor problem gets the model-side replacement number (§2.1, App C.3).** The ring-profile floor `1.7e-12` keeps its v0.3 instrument note beside the span sentence, and the designed `μ→0` corner now carries a rollout bound needing no spectral instrument: `max|θ(n) − δ| = 9.400e-13` rad over 5 seeds × 48 γ × 3 amplitudes × 32 000 steps ⇒ `Γ ≤ 7.309e-17`/step ⇒ **`n₁/₂ ≥ 9.483e15` steps**, with the matched Hessian `μ²_soft` values [report §3.4]. **(3) The falsifier sentence answers the linearization objection in both directions (§2.1, C.3.1).** Finite amplitude: argmin `0.897–0.917 γ_crit` and slopes to `≤1.9 %` at `δ = 0.5` rad, a 10× write, every write relaxing home (`|θ_final| ≤ 6.5e-10`) [report §3.3]. Identity: below `γ_crit`, `|λ|² = 1−γ` exactly, two emergent seeds `2.7×` apart in `μ²` both printing `n₁/₂ = 692.5` at `γ = 0.002` vs `2ln2/0.002 = 693.1` ⇒ the falsifiable content is the argmin and the `+1` branch [report §3.6]. The failed early-window clause (`0.52–0.85` vs band `[0.65, 1.35]`, amplitude-independent ⇒ mode mixing, not anharmonicity) ships as a ⚠ in C.3.1 and as negative 19 [report §3.3]. Collapse spreads (`1.15 %` median I-J, `5.67 %` I-R3; `y_min 1.50–1.66` vs `2√2 ln2 = 1.961`; `x_min ≈ 0.90` vs `0.707`) and the **2.7× `μ²` scope caveat** enter C.3.1 and Limitations (iv) [report §3.5, L2]. **(4) ME-3 enters §2.2 compact + a new App D.7 in full.** Transferring laws: refrigerator `0.9998 ± 0.0019` of prediction over 24 field cells, `7.955×` at `γ_φ = 0.5` vs `7.942` predicted, coupled bath rejected at `0.2235` [report §5]; `D̂ ∝ γ_eff^−2` at `1.016–1.103` per `γ_φ`, **law-referenced vault `106.1 ± 5.0×`** beside designed `107.77 ± 4.78×` [report §6]. ⛔ **The CONTRAST number stays designed-only**: emergent `23.39 ± 10.06` vs `8.03 ± 0.80` from the identical estimator on designed, with the named failure mode — the **scalar control arm** delocalises (`D̂/D_flat = 1.18–5.23`) while the field arm sits on the law — so `8.11 ± 0.37` is quoted as designed and only the *direction* transfers [report §7]. New emergent-only positive: **the hole confines**, hop fraction `5.5 / 43.0 / 2.4 %` outside → `0.0000` on 3/3 inside, scalar control still hopping `0.73 / 10.2 / 0.26 %` [report §8]. Riders travelling with every ME-3 sentence: 3 seeds · `T ∈ {4e-3, 8e-3}`, both `> T*` · ⛔ **no emergent `σ_θ` ratio anywhere** (G3's own control returns `0.4586 ± 0.1181` where it must return `1.000`; stated as a void comparison, negative 16) · the **`θ=π`-is-not-a-vacuum confound** (`|∇V| = 0.042/0.105/0.041` vs `0.0000`) attached to every emergent FPT number, with the same-site table (`> 1379×`, `35.5×`, `> 1290×`, censoring `86.7/0/93.4 %`) and the `0.27×` scalar-control impossibility printed as the confound's signature [report §9]. Estimator rebuild + its cross-validation on designed (`112.58 ± 1.09×` vs banked `107.77 ± 4.78×`; `14.16 ± 1.38×` vs `13.28 ± 0.12×`; `8.03 ± 0.80` vs `8.11 ± 0.37`) in D.7 preamble and A.10 [report §4, §1.2]. **MF-13(a) softened accordingly**: the abstract and contribution (1) now say the vault's *laws* are measured on both arms and the *contrast* is designed-only. **(5) Apparatus:** new **A.10** flag-provenance block (commit `7fcef50`, clean tree, no tracked file touched; pre-registered; `fdt`; retie; ε=0.05; float64; γ-grid `geomspace(0.002,0.5,48)`; δ ∈ {0.05,0.2,0.5} rad; JAX 0.9.0; ≈73 min) [report §1, §1.1]; two banked figures assembled, **Figure C.2** (rollout overlay, `figC_rollout.png`) and **Figure D.2** (emergent vault, `figD_emergent_vault.png`), App-C figures renumbered to document order (old C.2→C.3, old C.3→C.4); **six new negatives (15–20)** in App J, the previous 15 renumbered to 21; Limitations (iv) rewritten and the future-work list loses "a rollout-validated V-curve" (now run) and gains the collapse-widening and both-arms-bounded re-measurement. **Sweep:** per-file, positive-controlled, extended with six v0.4 never-quote patterns (the withdrawn rate-disagreement wording · the superseded `233.6/653.3/249.0` cell · the stale "unrun" marker · the emergent `σ_θ` ratio · the emergent contrast number as a result · the measured/measured ratio as a vault) — **0 new hits; the same 3 benign v0.3 hits only** (two literature descriptions of Guo's *certified removal*, one "independent of chain length" immediately followed by its k-regime clause). **Build:** `draft.tex` regenerated from `draft.md`, `tectonic`, 31 pp, no errors. **Page budget — MEASURED, and the criterion is now met in the derived build, not here:** canonical main text = **5 pp / 2,682 words** in the generic `article` class (was 4 pp / 2,241 at v0.3); the canonical is the archive and is deliberately not cut, and the 4 pp limit is met in `submission/` (stage 2).
