# V2 short — revision log (one line per revision)

- 2026-08-19 · C3 one-by-one pass · paper-writer · **SUBMISSION r9 — the Head's six-instruction style pass (restructure · appendix cut · figure promotion · brevity · de-bold · total)** (task `v2-style-pass`; Advisor Add.32 + the Head's 2026-08-19 directives). ⛔ Canonical `../draft.{md,tex}` and `supplementary-theory-note.{tex,pdf}` **byte-untouched**; only `submission/submission.tex`, `submission.pdf` and `BUILD-NOTE.md` (new §9) changed. **NO NUMBER CHANGED, ADDED, ROUNDED OR SMOOTHED.** **(1) Restructure:** §1 Intro → **§2 Related work** → §3 Setup/two-axes (fine print intact) → §4 Results → §5 Discussion; the retirements appendix **dissolved** into §2 as compressed sentences (approved narrow-claim wording verbatim, CM-21 discharged in main text), K.5's bounding prior art folded into §2 ¶2. **(2) Appendix cut to plot/table sections only:** KEPT and merged as **A Supplementary results** (loan curve + recovery ladder; per-step compute), **B Prominent negatives** (11-row table complete, split into two `tabular` blocks so it breaks across pages), **C The mode-mass budget verified + GMOR proper**; DROPPED to canonical-only: the $SO(2)$ primer (Add.11 exception overridden by the Head's tightened rule; ⚠ the camera-ready acknowledgment obligation stands), erosion study, isotropization, exceptional-point signatures, $T>0$ coset diffusion, loan-appendix G.3 reach rungs, and — **under instruction 6's overflow rule** — the kick-amplitude probe. **Riders relocated, never dropped:** the FDT/kinetic-mode flag box → §3 next to fine print (a) (verbatim bar three repositioning words); the chain-length scope clause → §4.3 verbatim; the charge-oscillation caution → Appendix B under the isotropization row; the damping-corollary consequences (i)/(ii) → Appendix C.2; the kick-probe anharmonicity result inlined in the §5 scope box. **(3) Figure promotion:** the anchored-laws/retention plot is now main-text **Figure 2**, placed with the anchor-cure result; main text carries two figures. **(4) Brevity (Add.30) + (5) de-bold:** ABT openings on the abstract, §1 and each results subsection; **`\textbf` in main text = 0** (nine `\paragraph{}` structural headers remain, which the instruction exempts); appendix table headers keep structural bold. **The full measured menu fired:** item 1 (§budget verification demoted to the GMOR appendix, −261 w), item 2 (related-work ¶1 compressed), item 3 (limitations → scope box). **(6) Total: 20 pp → 11 pp** — main **5.72** · references **1.02** (33→28 entries; 5 orphaned by the appendix cut, `\footnotesize`) · appendices **4.23** (`\footnotesize`), measured from PDF bounding boxes. ⚠ **Two criteria MISSED and the reason is measured, not stylistic: main is 5.72 pp against ≤4 pp, and the total is 11 pp against the 8–9 pp target (hard ceiling 12, met).** The main text grew 0.30 pp *net* because the pass was ordered to ADD to it (+0.27 promoted figure, +0.36 retirements, +0.13 FDT box, +0.09 chain-length clause) against −0.39 (menu item 1), −0.16 (`\footnotesize` protected boxes) and −0.04 of prose compression: of 3,549 main-text words, ≈1,300 are protected-verbatim, ≈520 are riders relocated INTO it by this pass, ≈175 are captions. A costed menu of the seven remaining demotions — each of which contradicts one of this pass's own binding instructions or Charter C-6 — is in BUILD-NOTE §9.7 for the Head. **Checks re-run and printed:** two-way numeric check (direction (i) 2 exceptions, both `p{}` column widths; direction (ii) 8 tokens, all section numbers/registry tokens/appendix-resident — no content number left the submission via the main text); final sweep ALL CLEAR on every zero-hit pattern, `N<digits>` 0, `V-curve` 0, `critical-damping`/`V-shape` appendix-only, `CHLU` ×2 (continuity sentence + reference entry); positive controls fired; build 0 errors / 0 undefined refs / 0 overfull boxes; PDF metadata still fully scrubbed. → next: Head reviews `submission.pdf` and rules on BUILD-NOTE §9.7.

- 2026-08-19 · C3 one-by-one pass · paper-writer · **SUBMISSION BUILD (derived; canonical v0.8 byte-untouched)** (task `v2-submission-build`; Head ruling / Advisor Add.29+Add.30). Created `submission/` = `submission.tex` + `submission.pdf` + `supplementary-theory-note.{tex,pdf}` + `BUILD-NOTE.md` + neutral-named `figs/`. Stripped ALL internal apparatus (flag-provenance Appendix B, Appendix M in full, venue/draft-status headers, cross-ref-convention note, commit hashes, source-report and checkpoint names, registry tokens N*/CM-*/SF-*, the A.5 HTML sign-off comment); appendix triage to the Head's visual/measured-results criterion with the three ruled exceptions honoured (primer KEPT per Add.11; negatives KEPT as an 11-row compact table; CM-21 retirements KEPT compressed to ~half a page); appendices renumbered C→B…L→K with every cross-reference converted to `\ref`; parentheses purge (182 net removed — 69 stripped as editorial/provenance, 57 mandatory C-5/C-6/statistical qualifiers CONVERTED TO PROSE, none lost); PJ style pass on connective prose only, approved wordings and riders verbatim. Anonymization per Add.2 + Add.28 Option B (`Anonymous Author(s)`, metadata scrubbed, theory note cited as "Anonymous (2026), provided in the supplementary material", anonymized F5 supplementary compiled; f5-note sources byte-untouched). Built with the only NeurIPS-family style file obtainable locally (NeurIPS-2025 ML4PS geometry, 5.5in x 9in, notice box suppressed) — NeurReps/NeurIPS-2026 templates unobtainable (403). ⚠ **0 errors / 0 undefined refs / 0 overfull boxes, 20 pp total, but MAIN TEXT = 5.4 pp against the 4-pp limit**: the Add.25 lean (GMOR-proper -> one sentence + pointer) was applied and measured to buy exactly ONE typeset line; the residual ~1.42 pp is a Head-level demotion decision, costed in the build note. NO NUMBER CHANGED — every numeric token in the artifact traces to the canonical.

- 2026-08-18 · C3 one-by-one pass · paper-writer · **v0.8 — the referee closures: bibliography, the five blockers, SF-1…SF-8, and the honest page verdict** (task `v2-revision-7`; sources `v2-referee-v07.md` (MF-1…MF-5, SF-1…SF-8, N-1…N-4), `v2-cite-check.md` (26 verified records + BibTeX + Part-2 novelty confirm + its 6-item downstream reconciliation list, owned here), `venue-follow-up.md` §3). **No new experiments; no number changed, added, rounded or smoothed.**
  **MF-2 (bibliography) — CLOSED.** New **References** section in both files (after §5, before the appendix block; typeset in the `.tex` as a hanging-indent `\section*{References}` block, **excluded from the main-text page budget** per the venue rule). **33 entries**, every one from the cite-check's verified primary records, preprints labelled as such. **Rusch & Mishra 2021a (coRNN, ICLR) / 2021b (UnICORNN, ICML PMLR 139) disambiguated at both §4 sites** and at App L.5(iii) and M§4 (5 sites total). In-prose author-year added where the draft cited a system name only: **EDEN → Karuvally, Lertsaroj, Sejnowski & Siegelmann 2025** (×4), **Titans → Behrouz, Zhong & Mirrokni 2025** (×4). ⚠ The prose remains author-year (no `\cite` macros): converting to `\citep` is a one-pass job once the venue style file is chosen, and is noted as such.
  **Cite-check reconciliation list (this spoke was named owner) — all 5 draft-side items CLOSED.** **U1 (highest priority):** Mo's theorem now reads "**at least** $\dim(G/\mathcal H)$ zero Lyapunov exponents" at all five sites (§3.2, App A scope note (iv), M§3.2, and the two §4/M§4 sites that were already correct); the lower-bound reason (time translation, further symmetries, conservation laws, finite-time numerics) is stated in **M§3.2**, the main text carries the qualifier plus a pointer (main-text budget). **U13:** landscape distortion re-attributed to **Fischer & Igel 2010** (ICANN) at all three sites; **F&I 2011 retained, correctly, for the bound on the *bias of the CD gradient*** (M§4). **U14:** conformal symplecticity re-attributed to **McLachlan & Perlmutter (2001)**; **HLW now cited with its year (2006)** and only for leapfrog/Störmer–Verlet stability ($h<2$); **Bhatt, Floyd & Moore (2016)** added in M§4 as the integrator-side treatment of second-order conformal-symplectic *schemes*. **U-usage fixes:** Kong exponent scoped ("one-hot and binary index codings; $1.17\pm0.02$ for their 2D coding") and the unverified "address enters as a bias" softened to "through a dedicated index channel" (L.2); Ramsauer's identity conditioned ("*coincides with* … exactly so for a single head with identity projections and $\beta=1/\sqrt D$", L.5(i)); UnICORNN's "symplectic-Euler" (body-level only in the source) → "structure-preserving (symplectic)" at all three sites.
  **MF-5 (stale scout marker) — CLOSED as a WIRING fix; zero "pending scout" strings remain (verified, both files).** All five markers (§3.4, App C preamble, App G N5, M§3.5, M§5) replaced by the ruled verdicts: **(a)** printed as a sharp instance on a known substrate (Fischer & Igel 2010/2011; Nijkamp et al. 2020), explicitly *not* the discovery that CD is biased; **(b)+(c)** stated as novel **with the coverage statement in print** (databases, venues and journals searched, Aug 2026) and the mandatory *"absence over the surfaces listed, not proof of none"* clause naming what was **not** searched. **(c) carries its scope clause** against the documented $k$-vs-mixing-time regime result (**Decelle, Furtlehner & Seoane 2021; Agoritsas et al. 2023**). ⚠ **Deviation from the cite-check's suggested wording, deliberate:** it proposed asserting that both chain lengths "sit on the same side of the model's mixing time" — we do not measure the mixing time, so the printed clause instead says the sweep *does not resolve* where either sits and that the finding is "frequency-decisive **across the two chain lengths we run**", never "chain length is irrelevant". Also printed: **Toledo-Marín, Maiti, Fox & Melko 2025** as the nearest RBM symmetry-breaking neighbour, with its own attribution (hierarchical feature learning, **not** CD) — which is why it is not a precedent for (a).
  **MF-3 (μ²-orders collision) — CLOSED.** One reconciling *Instrument note* in **App K.4**: the eleven-order span's designed endpoint is the smallest $\mu^2$ the **Jacobian-derived** $\gamma$-grid probe of B.8 resolves on that curve, **not** the **Hessian** $\mu^2\le2.4\times10^{-15}$ of §3.3/B.1 that sets the $13$–$14$-order designed-vs-emergent gap; both numbers unchanged.
  **MF-4 (hermeticity) — CLOSED, and the sweep is now SEMANTIC.** "the paid-access companions" → "future work beyond this paper" (H.3); "the lattice-scale companions" → "deferred to future work at lattice scale" (M§3.4); "this program's models" → the learned-store battery of Appendix G (A.6 table); "claims this program does not make" → "this paper" (App L). Beyond the referee's list, the **companion/program/sibling class** sweep found and cleared three more: App K's "standalone companion", M§1's "companion note … evidence-side companion" (→ "separate note … evidence-side counterpart"), App D's "A companion falsifiable" (→ "A second falsifiable"). **Result: `companion`/`sibling`/`our other`/`this program`/`the program` = 0 hits in both files.** Also removed one internal-process leak the class sweep surfaced: App C's "provisional **per the Head**" → "a pending editorial decision".
  **MF-1 (page budget) — the menu is APPLIED IN FULL; the target is NOT met, and that is the wave's honest finding.** Applied: (a) **§4 ¶2 demoted to 3 sentences + App L pointer** (¶2 $280\to216$ words, §4 overall $525\to471$; App L already carries the material in full); (b) **abstract trimmed** 249→230 words (the (i)/(ii)/(iii) parentheticals: the $\approx3.2\times$, the $1.012$–$1.029$ range, the $1.33\times10^{-15}$ — all survive in §3 with their fine print, and dropping the machine-precision figure from the abstract also removes a CM-15 fine-print exposure); (c) **§1's duplicated App-M/App-A pointer sentence compressed**. ⛔ §2's fine print (a)–(c) NOT thinned (each is still ≥1 sentence; (c) grew per SF-3). **Measured page estimate (10pt/0.9in `article` approximation, `tectonic`): main text = 5 pp + 4 lines ≈ 5.1 pp — unchanged from v0.7** (main text $3419\to3413$ words; references now start on p. 6 and are excluded from the count). **Why the count did not fall: the sanctioned demotions are worth ≈190 words, and this same revision's mandatory additions cost ≈185** (SF-3's fence, the MF-5 scope clause, U1's qualifier, SF-8's clause, and MF-2's in-prose author-year expansions, which lengthen the main text by construction). **Reaching ≤4 pp needs ≈850–1,100 further words (25–32 % of the main text) — i.e. demoting a Head-listed lead result or the headline figure — which is explicitly NOT done here** (C-10: no self-pruning). A costed per-section menu is in `.claude/outputs/v2-revision-7.md` for the Hub/Head. Final pruning waits for the venue template in any case.
  **SF-1…SF-8 — all closed.** **SF-1:** the abstract's cross-experiment splice is split — "$\approx15\times$ better **at short horizons**, then diverges past a measured $\approx700$-step crossing" (the eval-400 `minus-the-physics` number no longer shares a clause with the `fit-gap-anatomy` horizon curve; App H's boxed non-comparability caveat unchanged). **SF-2:** abstract "$\approx1\%$" → "$\le3\%$ (median-consistent with its reported $1.013$)" — no silent round-down of the $1.029$ worst case. **SF-3:** §2(c) and M§2.1 re-fenced on the *tilt-as-a-designed-lifetime-dial construction*, with the self-broken MLP named as an **observed instance of the cell, not a dial** — the taxonomy contradiction is gone, the N149/N150 fence is intact. **SF-4:** Appendix M's preamble now states the $\mathcal H$/$H$ convention (long form writes $H$; condensed text and App A write $\mathcal H$; the Hamiltonian is always written with its arguments). **SF-5:** the cross-reference convention now covers **Appendices A–L**, and A.5's "(§2, taxonomy row 1)" points at "the taxonomy table … row 1 of Appendix M §2.1". **SF-6:** the three internal branch names in B.3/B.6/B.9 → bare commit hashes ("code @ `b41410f`/`9bc2cf7`/`647c54e`"); base commits and the B.9 rebase disclosure retained. **SF-7:** **N51 added to Appendix G** (raw $n_{1/2}$ exponents are not designed-vs-emergent discriminating; $\ell_\theta/\Delta$ boundary layer; never quote $n_{1/2}$ without $\Delta$ and $\ell_\theta/\Delta$), cross-pointed to K.6. **SF-8:** one clause in §5 and M§5 names groups beyond abelian $SO(2)$ ($T^2$; $SO(3)\to SO(2)$) as a **direction on which this paper offers no evidence** — zero evidence claims, per G7.
  **N-1/N-2/N-4 — applied.** N-1: M§3.2's "(Figure 2, seed error bars)" → "(Figure 2 $=$ main-text Figure 1 …)" and "Headline = Figure 2, displayed in the main text as Figure 1". N-2: A.2 opens with the typicality clause (an invariant potential may present a critical point at the origin **and** a degenerate ring; the argument is applied at the vacuum the trained model occupies). N-4: the primer's acknowledgement line is now the double-blind-safe form (non-identifying attribution; acknowledgement placed in the camera-ready). **N-3 untouched by design** — the A.5 S1 sentence and its inline sign-off flag are **byte-identical** (verified).
  **Two editorial changes beyond the literal commission, flagged for the Hub (both revertible in one pass).** **(1) "published" → "recently posted" for Mo (2026)** in the abstract, §1, contributions (2) and the §3.2 header, and "his **published** median" → "his **reported** median" (3 sites incl. the Fig. 1 caption): the cite-check verified arXiv:2605.03338 is a **preprint, not peer-reviewed**, and "a published ML law" is a factual misstatement a NeurReps reviewer will check. Iqbal et al. 2026 (also a preprint) carries no "published" wording anywhere, so no change was needed there. **(2) The self-cite possessive removed** — "**Our** reference unit is the CLU …" → "**The reference unit here** is the CLU (Causal Learning Unit), **introduced as CHLU in Jawahar & Pierini (2026)**" (the mandatory continuity sentence is unchanged and intact ×2): the cite-check flagged "our reference unit … introduced in <our names>" as a de-facto identification under double-blind. The remaining anonymity decision (whether to anonymise the self-citation itself) is the Hub's/Head's.
  **Build.** `tectonic -X compile draft.tex`: **0 errors, 0 undefined references, 0 overfull/underfull-critical boxes** (`draft.pdf` **32 pp**, 1.08 MiB, 6 figures). Three build defects fixed in passing: the A.6 notation table and M§2.1/M§3.3 tables converted from `c` to `p{}` columns (the three overfull hboxes, incl. the 331 pt one, are gone) and the missing `\label{fig:mo_own}` added to the Fig. 2b environment (that reference was undefined in the v0.7 build too).
  **Never-quote sweep (per-file, positive-controlled).** Files: `draft.md`, `draft.tex` (each swept end-to-end). **ZERO HITS (both):** CLU-former · certified · unlearning · exact deletion / "the item is gone" · "exact discrete FDT" · "samples Gibbs" · 0.384 · 16.28 · CAFE · C-MAPSS · N-CMAPSS · HEPA · CAMELS · bpc · S_eff · z_hole · 0.99985 · 54.56 · 306.76 · 300.09 · deltanet · ttt_mlp · MUNKEY · 0.4545 · 13.9 · memory vault · 107.77 · compositional · unaskable · 19× · Guo · Ginart · Sekhari · Track A · waitlist · **pending scout / pending a literature scout** · **paid-access** · **companion / sibling / "our other" / "this program"**. **HITS, context-checked and compliant:** "energy units" ×2 = the refusal statements under the legacy noise default; "wormhole" ×1 = App H.3's own $\kappa=0$ certificate; "2.6" ×22/23 = `2.6e-2`/`2.6e-6`/`2.6e-13` grid and probe values, never the retracted mass-drift number; "98.3" ×1 = the unescaped-dot false positive on `0.98732513581811`; "critical damping" ×1 and "V-curve" ×4 = App K.4/F.2 only (0 in abstract/§1/§3, Q11 intact). **Positive controls fired:** GMOR 25/23 · N46 11/11 · N51 1/1 · "introduced as CHLU" 2/2 · Rusch 8/8 · provenance 11/10. **md↔tex sync verified by token counts on all 18 edited-string classes (identical in both files).** → next: `v2-referee` on v0.8, and a Head ruling on the residual ≈1.15 pp.

- 2026-08-18 · C3 one-by-one pass · paper-writer · **v0.7 — the Q11 re-centering + the colleague primer as opening appendix + the stale-delta fold + NeurReps-EA venue class** (task `v2-revision-6`; authorities: shorts charter **Add.8 Q5/Q7/Q9/Q11**, **Add.9** (physics-theorist audit R1–R6/S1/T1–T6), **Add.10 F3** (lexical false friends), **Add.11 §2** (Option-3 appendix placement, Head's "opening section" shape), **Add.16**; registry **N46 · N51 · N149/N150 · N6 · N22**; `claims_matrix` **CM-15 · CM-16a/CM-16b (split, never "CM-16") · CM-17 · CM-21 · CM-22(b)** and inventory **§0.1/§0.4**, §2(a)–(d)). **No new experiments; no number changed, added or rounded.**
  **1 — Q11 re-centering (Add.8 Q11).** Abstract rewritten: the critical-damping/V-curve law is OUT of the abstract and out of the contribution list; the abstract's old "critical-damping crossover" is re-worded as the **exceptional-point crossover** $\varepsilon\mu\approx\gamma/2$ (a different object: the over/underdamped boundary, not the $\gamma$-sweep minimum). Contributions now lead with mode-mass budget · Mo head-to-head (**Fig. 1, headline, unchanged as the Mo figure**) · GMOR-proper · realization taxonomy · designed-vs-emergent + price-of-physics. The retention minimum is **demoted to a supporting result, new Appendix F.2** ("the damping corollary of the budget"), written as a corollary of the two bands with no "optimum" framing anywhere in the abstract; §2's budget paragraph and §3.1 now point to F.2; App K.4's heading re-pointed from "§3.1's critical-damping minimum" to "the budget's damping corollary (Appendix F.2)". ⛔ C-8 hermetic: no reference of any kind to any other short.
  **2 — The colleague primer enters as the OPENING appendix (Add.11; Head's shape).** New **Appendix A — "An $SO(2)$ symmetry-breaking primer for machine-learning readers"**; all downstream appendices relettered A→B … J→K (mechanical, script-verified: 127 md / 88 tex reference sites) and two new appendices added (L, M — below). Adapted from `.claude/colleague/main.tex` (⛔ **byte-untouched**, md5 `ad2a090fc66ee21f46c8f6943aae0eb5` before and after). Applied from the accepted audit: **symbol renames α→`s`, J→`X`, ω→`θ₀`, H→`𝓗`** (grep-verified: every residual α/J/ω/H in the appendix is either the notation-map table naming the *rejected* letter, an "Appendix J" cross-reference, or `\mathcal H`); American spelling; **T1** (`𝓗 ⊂ G` as a sub*group*), **T2** ("one NG mode per broken generator; the NG fields parametrize the coset $G/\mathcal H$, whose dimension equals the number of broken generators"), **T3** (group elements = the rotations, algebra = span{X}), **T5** (the stationarity condition $\nabla V(R(s)q^*)=0\ \forall s$ is displayed, and Eq. (A.3) is evaluated at $s=0$), **T4** (new A.4: bare vs mass-whitened Hessian; the zero mode survives whitening exactly by Sylvester, massive eigenvalues do not — any quantitative extension must whiten), and the channel-vs-unit clarification ("two-dimensional latent space" = the channel plane of a dim-4 unit). **Boxed scope note at the appendix head discharges R1–R4 in one place**: (i) N46/CM-16a designed-exact-$SO(2)$-only rider with the emergent numbers; (ii) $T=0$ scope + the $D_\theta$ diffusion law and its `fdt`+Newtonian flag (CM-16b, App K); (iii) the γ>0 latch clause (flat = neutrality, storage needs damping; theory-note latch theorem); (iv) the Mo (2026) citation, pedagogy-not-contribution, plus sufficient-not-necessary. **§2 (the merged taxonomy section) remains the document's controlling SSB definition (R6)** and the primer defers to it explicitly and twice. ⚠ **FLAG — S1 reword awaits the colleague's sign-off via the Head:** A.5 now reads "does not ***force*** a zero spectral mass … the orbit argument degenerates ($Xq^*=0$) and gives no information about the eigenvalue", with the $V=r^4$ counterexample; the sentence carries an inline `<!-- FLAG … AWAITS THE CO-AUTHOR'S SIGN-OFF -->` comment in `draft.md`. Authorship line added ("adapted from a co-author's tutorial note; acknowledgement placement pending").
  **3 — The stale-delta fold.** **(a) N149/N150 blast radius (v0.6's biggest known gap).** Every tilt/ε/lifetime sentence checked. New scope clause **(c) in §2** ("the explicitly-broken cell is claimed for designed geometries only"), a rider on §5's horizon sentence ("registers as pseudo-Goldstone modes of a chosen half-life — a **designed-geometry** statement"), a rider in App A.7, and a **full new negatives entry in Appendix G** carrying the learned-store sign refutation ($\lambda_{\min}$ **+0.0994 → −8.2846** envelope, **+0.3291 → −1.1980** depth, monotone in the breaking coefficient, two implementations, every family; mechanism measured: tilt-vacuum residual **0.140–0.343** vs. random-orientation baseline **1/dim = 0.167**, atom-centre spread **1.19–1.95×** the designed radius; what survives is the single-atom geometry, unit-tested to 5%) **and the ceiling** (registered damped-mode floor $\tau=2/\Gamma=5.0$ CONFIRMED at **4.23–6.58**; above-knee slope 0 confirmed; the $1/\epsilon$ branch unreachable because confinement floors the soft mode at $2\alpha$ ⇒ **$\tau_{\max}=\Gamma/2\alpha=4.0$**; $\lambda_{\rm tan}=2\alpha\lVert c\rVert/\rho\approx2\alpha=0.100$ predicted before running, measured 0.0994). ⚠ Two reading rules printed with it: that battery's $\epsilon$ is an **explicit-breaking magnitude, never this paper's integrator step $\varepsilon$**; and no sentence here may be read as a lifetime-dial claim on a learned store. New **flag-provenance row B.9** (branch `647c54e` off `1ce02e4`, disclosed rebase/re-emit, JAX 0.9.0, α=0.05, ε-grid over four decades, **seeds {0,1,2} on race cells and seed 0 only on the ε-dial sweep, labelled as such**, T=0/no Langevin/no wake–sleep). **(b) CM-21 completeness:** all four retired positioning claims are now explicitly retired *in the text* — new §4 paragraph + new **Appendix L** ("Positioning and prior art (full), and four claims we retire") carrying the HiPPO-LegS challenge **with the honest "we have no measurement that answers it"**, the mandatory **Kong, Brewer & Lai (Nat. Commun. 2024)** citation, the NTM/DNC warning (Csordás & Schmidhuber 2019 key/value entanglement), the transformer-competitor retirement (with Jelassi et al. ICML 2024), and the **approved replacement wording** used verbatim except that the "actively deletable" mode is **omitted** (V2 has no deletion evidence and CM-22(o)/(q) restrict deletion sentences — flagged to the Hub). Also folded: CM-22(b)'s UnICORNN occupancy + an explicit **extrapolation fence** in §5 (no off-distribution stability claim in a trained, input-driven slot), EDEN narrowing, Titans as the nearest published neighbour, Ramsauer (App L). **(c) CAFE quote-restrictions:** zero occurrences (sweep below). **(d) Full §0.1–§0.14 sweep printed below.**
  **4 — Scale line (Add.8 Q5/Q9).** New §5 "Scope choices" (i): scale is **out of scope by choice**, no result at larger dimension/width/units/accelerator, none to be inferred; plus a **named future-work list** (dim ≳64 and N≥8 units where mode mixing can break the single-block reduction; a wider trained-model head-to-head; the anchored recipe at longer training/width; the control wrapper; the (μ,γ,T) cube; a learned-store re-test respecting the N149/N150 fence). ⛔ No C3-era number; no implication that a scale result exists. Also added under the same paragraph: the **score sentence** (§0.1 — "no external benchmark is won on its own headline metric anywhere in this paper"), flagged to the Hub as an addition beyond the literal commission.
  **5 — The §A20.5 substrate-scope sentence**, once, in the paper's own voice, in §5: *"these laws govern the store; end-to-end performance additionally depends on the encoder, measured separately, with its parameter and state-byte budget ledgered."*
  **6 — Venue-class header → NeurReps extended abstract** (4 pp main text excl. references+appendices; appendices unlimited; non-archival; double-blind; per-venue fields re-verified before submission). **Main-text condensation by relocation, not by cutting (C-10):** the pre-condensation main text is retained **verbatim and in full as Appendix M** ("The long-form main text, retained in full"), and the main text was rewritten to the budget: **8,789 → 3,420 words**, one figure in main text (the Mo headline), tables and the second/third figures moved to Appendix M. ⚠ **Measured result: main text = 5 pp, not 4** (build below); closing the last page requires demoting a Head-listed lead or the venue's own denser template — flagged for the Hub, not decided here. Editorial: the `\documentclass` moved 11pt→10pt and margins 1in→0.9in to approximate a workshop template; internal registry tokens (`CM-1`, `CM-4`) removed from the paper's prose; a cross-reference convention note added at the head of the appendix block (appendix §N.M numbering = Appendix M's long-form numbering).
  **Never-quote sweep (per-file, positive-controlled).** Files swept: `draft.md`, `draft.tex`, `CHANGELOG.md`. **ZERO HITS:** CLU-former · "certified"/certified removal · "unlearning" · deletion-by-construction / exact-deletion / "the item is gone" · "exact discrete FDT" / "samples Gibbs" / "temperatures in energy units" · the `d=1` defect table (0.384/16.28) · τ∝√M · mass-as-storage · frozen-mass wording · "ε is the manifold-payload lifetime dial" / "lifetime ∝ 1/ε" · CAFE / C-MAPSS / N-CMAPSS / HEPA / CAMELS · bpc and any scale number · gym/rival vocabulary (organizer swap, `S_eff`, `z_hole`, 0.99985, 0.272, 54.56, 5456) · n=3 rescue verdicts and rival margins (306.76 / 300.09 / deltanet / ttt_mlp / gdn2) · `1/11` · MUNKEY · 0.4545 / "98.3% ballistic" · 13.9× / "memory vault" / 107.77 (drafts; the CHANGELOG's historical entries mention them only as *verified-absent* records). **HITS, all context-checked and compliant:** "2.6" ×4 = `2.6e-13/2.6e-2/2.6e-6` physical quantities, never the retracted mass-drift number; "≈4× longer" ×3 = the retirement statements themselves (App I title line, App I "why the 4× is retired", and App M §3.3's referee-cleared sentence with the per-step caveat in the same clause); "extrapolat" ×2 = kick-amplitude extrapolation in App E, not off-distribution extrapolation; "competitor to the transformer" ×1 and "no retention-guarantee analogue" ×1 = App L's explicit retirements; "has no equilibrium" ×1 = App G's refusal to assert it. **Positive controls fired** (GMOR 30/26, N46 12/12, N149 9/6, "introduced as CHLU" 2/2, substrate sentence 2/2, "Mo (2026)" 5/5 in md/tex). Add.10 **F3** respected: V2's friction-field composition/placement negatives (N12–N15, App G) untouched and unrenamed.
  **Build.** `tectonic -X compile draft.tex` clean: **0 errors, 0 undefined references, 1 overfull hbox**, `draft.pdf` **30 pp** (main text pp. 1–5; appendix block opens on p. 6 with the primer), 6 figures. Both `draft.md` (canonical) and `draft.tex` regenerated in sync; Appendix M in the `.tex` is generated from the canonical markdown, so the long form carries the v0.7 edits too. → next: `v2-referee` on v0.7 (pre-committed, Add.16).

- 2026-07-19 · wave-15 · paper-writer · **v0.6 CM-16 split + CM-17 App-F qualifier** (task `v2-revision-5`; sources `v5-gate` §3/§2 (CM-16a/CM-16b split), `f5-corrigendum-2` "Downstream" (CM-17/Prop-9′), claims_matrix v1.9). **Correctness pass, no new experiments, no main-text widening.** **Item 1 — CM-16 SPLIT applied in App J** (now titled "…designed and emergent"): added a lead "The split" paragraph (lead with (b), the general result); **J.1** scoped to the designed arm + emergent failure (N46: coset is a middle-of-spectrum massive mode $1.7$–$4.9\times$ softer than stiffest, $1-|\lambda_{\rm coset}|\approx10^{-3}$ vs designed $\le1.1$e-15 ~12 orders; written $\delta$ relaxes completely $\le2.1$e-3; capacity ≈1–1.6 bits, evidence, 3 seeds); **J.4** adds the emergent V-curve generalization (argmin $0.902\pm0.003\times\gamma_{\rm crit}$, slopes $-1.0020$/$+1.116$, 3/3 seeds; $T>0$ sign flip 10/10 above $T^\star\approx3$e-3 — labeled evidence); **J.6** rewritten (no longer "emergent untested": the arm is tested; the instrument caveat that raw $T$/$\gamma$ exponents do NOT discriminate designed-vs-emergent — matched designed control gives the same shallow slopes via $\ell_\theta/\Delta$; "never quote $n_{1/2}$ without $\Delta$ and $\ell_\theta/\Delta$"). **N46 added to App F** negatives; **§5 limitations** "emergent untested/top open risk" → both-arms-tested + designed-only-register; **§3.4** forward-pointer to N46/J.1-J.4. New **A.8** flag-provenance (`v5-gate`, `d6f8bac`, fdt, retied, newtonian_learned, $\Delta=0.5$, 3 emergent + 1 designed control). **Item 2 — App F σ* qualifier (CM-17/Prop-9′):** the class-level FDT-noise caveat now carries the kinetic-mode qualifier — σ* exact only in Newtonian modes; relativistic O-step ⇒ Gaussian-smoothed marginal ≠ Maxwell–Jüttner ⇒ no σ gives Gibbs (Prop-9′, char.-function); **failure is in the sampler not the thermodynamics** ($\pi_q$ relativity-insensitive, unit's equilibrium well-defined) — explicitly refuses "relativistic CLUs have no equilibrium"; V2 units are `newtonian_learned` ⇒ scope clause not a self-correction. Newtonian-mode qualifier also propagated to the §2.1 fdt fine print and App-J mandatory flag (CM-16 flag now "fdt AND Newtonian"). **Item 3:** `13.9×`/vault/`107.77×` verified absent program-wide — nothing to retract. Both draft.md + draft.tex synced; tectonic build clean, **0 undefined refs**, draft.pdf **26 pp** / 972 KiB, 6 figures (no new figures — no new results). → `v2-referee-4` (clean pass).

- 2026-07-09 · wave-13 · paper-writer · **v0.5 taxonomy overlay + GMOR-proper + corrigendum fold-in** (task `v2-revision-4`; sources `v2-symmetry-deepdive` §1/§2/§3/§4.1, `f5-corrigendum`, `f1-gmor-condensate`, `t-lever-forgetting`; CM-15/CM-16 verbatim). **(1) Symmetry-realization taxonomy adopted in full as a FRAMING OVERLAY** (abstract two-axis clause; §1 "Framing: two axes, not one"; new **§2.1** with the three-realization table); §3 mode-mass budget untouched as the quantitative spine. All four obligations met in §2.1: (i) SSB defined for a finite-dim deterministic system (classical/tree-level Goldstone theorem only — minimiser not G-invariant ⇒ zero Hessian eigenvalue; no thermodynamic limit, no ħ); (ii) Coleman/Mermin–Wagner stated honestly — latch ∞ is a **T=0** statement, at T>0 the coset diffuses with D_θ=εT(2−γ)/(2F²γ) (now an asset: the law is measured, App J) with the **mandatory `langevin_noise="fdt"` flag inline**; (iii) explicit "what we do not import" ¶ — no loops, no chiral logs, no LEC running, no anomalies/WZW; (iv) erosion relabel. **(2) §3.5 retitled "Symmetry restoration under training, and the anchored recipe that prevents it"** — r* is the order parameter; CD drives NG→Wigner–Weyl (degenerate μ² pair ≈1.0 at the collapsed vacuum, Schur, `v2-full-runs` Finding 0, previously unnamed); anchor holds the condensate up; arc sentence added ("architecture breaks / objective restores / anchor keeps broken"); "no thermodynamic phase transition" caveat. **(3) S5 modulus/Mo differentiator** — new §4 ¶ "Sufficient, not necessary": latch is a **modulus** of V_θ (Sylvester ⇒ μ²=0 under any inertia), broken-iso battery is a **measured non-equivariant latching map** (E^T_eq=0.0016–0.108); Mo's theorem stated as *sufficiency* (no necessity attributed to him). **(4+6) New Appendix I — GMOR proper** (CM-15): μ²F²=δΣ to **1.33e-15 abs** on 8 trained ckpts × 10 δ; Σ measured 3 ways (geom/HF exact 0.0; FD 9.2e-11); angular tilt provably cannot see Σ (r* fixed to 2.22e-15, measures only μ²F²=δn²) while linear spurion runs Σ +16.1…+210.1%; LEC ratio **0.99999607**, d(ratio)/dx=−1.0504; **δ=0.3 excluded from the NLO claim** (x=0.68); **precision fine print inline** (absolute machine precision at every δ; relative ≤2.7e-14 only for δ≥1e-2; small-δ residual = ε/δ autodiff-Hessian floor, NOT the law — "2.2e-16 relative" explicitly forbidden). Nomenclature adopted: **F=√(M_ch)·r*** = decay constant, **Σ=r*** = condensate/order parameter (§3.1 ¶ added). Fig I1 (`fig_gmor_condensate.png`) copied into figs/. **(5) Corrigendum's three flagged sites closed:** `draft.md:107` charge "**drift** scales linearly" → "**bounded oscillation**, amplitude √(2E)r*(√M_max−√M_min) **on the vacuum orbit**, off-orbit only boundedness (|Q|≤sup|q|sup|p|)"; measured 5.4/1.6/0.082e-2 relabeled **amplitudes**; **"≈0.7‖[M,X]‖" deleted** (A/‖[M,X]‖ is not constant); `draft.md:141,155` "price of an **equivariant channel**" → "**equivariant write current**" (both sites; grep-clean, 0 occurrences remain); corrected design rule stated in §3.4 + App C + N4 ("anisotropic channel still latches with infinite half-life; tie masses for a θ-independent write current and a degenerate pNG multiplet at dim(G/H)≥2, NOT to save the register; detect kinetic breaking via the charge law — Hessian and latch are provably blind"). **Retracted "2.6" never quoted** (verified 0 occurrences); App C gains the reproducibility caution (off-orbit chaotic-rosette running-suprema are window- and float-order-dependent). App C restructured C.1–C.4. **New Appendix J — the T=0 face of the budget cube** (CM-16, placement marked pending Head call): |λ_flat|−1 ≤1.7e-14 ∀γ∈[0.002,0.5]; drift ≤4.9e-12 rad/200k steps (30/30); D-law 1.0068±0.0219 over 25 cells; sign flip 10/10 (+0.955±0.042, 3.77±0.23×); **unification** — one damping-optimum curve at two μ, flat mode = μ→0 corner where γ_crit→0 (supersedes any "two laws" reading); ℓ_θ thermal-persistence-length qualifier; fdt flag + one-architecture risk stated. **Provenance A.6/A.7 added** (f1-gmor-condensate `9bc2cf7`, spurion defaults 0 ⇒ bit-compatible; t-lever-forgetting fdt/re-tied/f64). §5 limitations + horizon updated (tree-level-only scope; (μ,γ,T) cube as horizon). Both draft.md + draft.tex synced; tectonic build clean, **0 undefined refs**, draft.pdf 24 pp / 956 KiB, 6 figures. One pre-existing cosmetic overfull hbox remains in App A.3 (long `agent/...@b41410f` path token; untouched by this pass). → `v2-referee-3` clean pass.

- 2026-07-09 · wave-11 · paper-writer · **v0.4 number-hygiene fixes** (task v2-revision-3; source `v2-referee-2.md` MF2-A/MF2-B + SF2-1/2; CM-4 misprediction canonical). No experiments; wording/label only. **MF2-A ("5×"):** abstract + §3.2 "mispredicts by up to 5×" → "≈3.2× at the deepest trained-model breaking (meas/pred 0.309±0.012 @ δ=4; the 14-δ sweep goes no deeper underdamped)" + explicit exact-map attribution "continues to ≈5× further underdamped (theory note, Prop. metric-bifurcation / check (k), γ=0.1)" + a hard non-transfer sentence ("the ≈3.2× is the trained-model number; the ≈5× is exact-map only"). Matches table (0.309) / Fig 2 caption (0.31) / Fig 2b (0.30) and CM-4's w10 canonical. **MF2-B (15×/92% splice):** (i) §3.4 table header "CLU (symplectic)"→"CLU (γ=0 conservative)"; (ii) new two-part comparability caveat in §3.4 **and** a boxed non-comparability caveat atop App G.2 — App-G ladder is a separate wake-only experiment (`fit-gap-anatomy`, seeds 0–2, `9a13455`), its MSE scale + twin (0.0047) NOT cross-comparable with §3.4 `minus-the-physics` (twin 0.0128, seeds 42–44, `b41410f`); only within-table gaps meaningful; §3.4's ≈15× and App-G's implicit ≈44× are the same qualitative statement on two scales; (iii) abstract + §3.4 + App G.2: "92% of it"→"92% of the **absolute** contraction-forbidden fit gap … still trails the twin by **≈4.6×** in ratio, but bounded by construction". **Deviation from referee text (flagged):** referee wrote "~5× by ratio"; used **≈4.6×** (0.0216/0.0047=4.596) — rounding 4.6→5 violates the no-round-up rule and would have re-introduced a second "5×" into the same abstract MF2-A had just disambiguated. **SF2-2:** abstract "≈57–69"→"≈56–69" (LEM 56.4, not rounded up). Both draft.md + draft.tex synced; tectonic build clean (draft.pdf 588 KiB, 5 figures, 0 errors). Pruning (SF2-3) deferred to the dedicated pass. → submission-ready modulo pruning + F5 arXiv id.

- 2026-07-09 · wave-9 · paper-writer · **v0.3 SF-1/2/3 fold-in** (task v2-revision-2; source `v2-referee-experiments.md`; CM-4/CM-6 amended wordings). Closed all three `[pending: v2-referee-experiments]` tokens. **SF-1 (§3.2/Fig 2b):** wired Mo's OWN λ̂(T=128) predictor — corr(log pred,log meas)=0.9995 overdamped (meas/pred 0.86–1.03), fails ballistic past EP (0.30 @ δ=4 vs exact-gap 0.31); predictor-substitution closed; **corrected 44% attribution** (−15.6% max deep-overdamped gap·T<0.1; −44.5% max at near-EP row δ=0.17, gap·T≈3.1); added Fig 2b overlay (`sf1_mo_estimator_overlay.png`). **SF-2 (§3.3):** retired "≈4× longer" as a compute claim per CM-4 amendment — lead qualitative triad; added honest per-step line (CLU Verlet h64 ≈6.2× LSTM/3.1× LEM wall, 14–15× FLOPs, not width-matched confound stated) + retention-per-compute inversion (23.5×/14.6× wall); new Appendix H (full SF-2 table). **SF-3 (§3.5+App B.7):** upgraded "cure holds the vacuum"→"laws hold under the cure at ≈20× the erosion horizon" — anchored λ=100 @3000ep (3 seeds): r*=0.917±0.007, flat μ²≈1e-15, GMOR 1.0000±1.5e-12 over 4.6 decades, slope −0.956+floor 27.03, EP φ=0 below/0.5165 above (bit-identical to 150ep); added Fig B1 (`sf3_anchored3000_laws.png`). Abstract + contributions 3/5 synced. A.5 flag-provenance rows (SF-1/2/3) into App A. Both draft.md + draft.tex synced; tectonic build clean (draft.pdf 596 KiB, 5 figures). → v2-referee-2 (w10) clean-pass verdict.

- 2026-07-08 · wave-8 · paper-writer · **v0.2 referee-revision** (task v2-revision, against v2-referee punch-list). MF-1: deleted §1 audit-confession paragraph; App F "audit negatives" reframed (N17/N18 legacy numbers dropped → neutral class-caveat line; N22 kept as class scope limitation, §1 back-ref removed); App F header "audit negatives"→"class-level design caveats". MF-2: wired measured fit-vs-horizon crossing (≈700 steps, twin diverges 196@5000 vs CLU bounded 0.20–0.23) + recovery ladder (+γ 92%, γ_φ −24%) into §3.4; new Appendix G (full loan curve + ladder + reach certificates); abstract crossing clause added; CM-1 guard applied (no lowest-plateau claim). MF-3: §3.4↔App D reconciled — "consistent +12–28%" → +12–15% canonical-kick envelope + distinct kick→0 probe +5 to +29% (1.052/1.125/1.288), stated as different probes on same checkpoints; App D μ² cross-comparison caveat; §3.3 mention synced. MF-4: embedded Fig 1 (GMOR+C2), Fig 2 (Mo, headline), Fig 3 (retention overlay) via \includegraphics (figs/). SF-4: evidence-first framing ¶ before §3.1. SF-5: reach-pricing "separately-measured". SF-6: "solved"→"a constitutive problem with an exact solvable core and quantified anharmonic deviations". N-1: abstract "subsumes"→"contains…as its overdamped limit". N-3: §4 "contributes"→"reports". SF-1/2/3 left as [pending: v2-referee-experiments] slots (Mo estimator overlay, per-step FLOP ratio, GMOR-on-anchored-3000ep). Both draft.md + draft.tex synced; tectonic build clean (draft.pdf, 395 KiB, 3 figures). → back to paper-referee.
- 2026-07-07 · wave-6 · paper-writer · **v0.1 initial draft** (draft.md canonical + draft.tex). Full main text (§1–5) + Appendices A–F, per task v2-short-draft.md. Results order per charter: budget-verified → Mo head-to-head (headline Fig 2) → baseline collapse (CM-4) → designed-vs-emergent + price-of-physics (CM-1) → anchored-training recipe. Physics-audit paragraph (C-1) drafted in §1. CLU naming debut continuity sentence in §1. Evidence traced to v2-full-runs, v2-prefreeze-baselines, minus-the-physics, sleep-erosion-study, anchor-robustness, mo-deep-read, di-bernardo-skim, f5-arxiv-note. Erosion novelty marked pending scout confirm. Working titles (3) proposed in output report. Awaiting Hub C-1 sign-off + paper-referee pass.
