# papers/v6-short — CHANGELOG

## v0.1 — **the V6 condensation** (`v6-condensation`, paper-writer, 2026-08-18)

**What this is.** `papers/v6-short/draft.md`, created by condensing `papers/bprime/draft-r7.md` (the refereed +
closure lineage) for the ratified **TAE venue class** (≤ 8 pp main text excluding references and appendices,
appendices unlimited, double-blind, non-archival). ⛔ **`papers/bprime/*` was not edited** — `draft-r7.md` md5 **`a269a85fe41babb7b6c8f4468dfb5384`**, mtime
2026-08-18 21:11 (i.e. the r7 closure pass's own write, unchanged by this pass); the only writes here are
`papers/v6-short/draft.md` and this file. ⛔ **Zero new claims and zero new numbers:** every quantity in the draft traces to `draft-r7.md` (and
through r7's Appendix A to the flag-provenance tables and registries). ⛔ **No figure was rendered**; Figure 1 is
r7's re-render (seven bars, uniform `n = 9`) and Figures 2–5 are r7's, referenced through Appendix K.
**DIAL DECLARATION (echoed): none — condensation pass; no new measurement; no laundering control applies.**

### ⚠ Length: the acceptance criterion is NOT met, and this is the pass's main finding
- **Measured:** main text (title → §7) = **8,948 prose words + 557 words of table cells (9,505 total)**;
  appendices = **≈ 15.5 k words**. Estimated main-text pages, stating the assumption rather than pseudo-verifying
  a build (no LaTeX toolchain was used and no `.tex` was requested): **≈ 12.8 pp at 800 prose-words/page**, with
  table cells costed at ~55 % of prose density and 0.35 pp for Figure 1 (**≈ 14.6 pp at 700 wpp; ≈ 11.4 pp at
  900 wpp**). r7's main text is ~22.4 k words, so this is a **2.4× condensation** — but the target was ~4×.
- **Why, measured rather than asserted.** The mandated main-text set alone — Add.12 items 1–7, each with **every
  rider adjacent** (task rule 1) and every generalising claim carrying its scale qualifier in-sentence
  (Charter C-5) — already consumes **≈ 6,200 words** *before* §3, §4.3–§4.5, §5 and §7 exist: abstract 498 · §1
  906 · §2 2,635 (of which the R.2/R.3 venue-and-embargo block with its riders is 994 + 103) · §4.1 692 · §4.2
  592 · §4.6 537. At 800 wpp that is **≈ 8.9 pp**. ⇒ **items 1–7 with adjacent riders do not fit 8 pp**; the
  8-pp criterion and the rider-adjacency rule are in direct conflict and only the Head can resolve which yields.
- **Four compression passes were run and are measurable in this file's history:** re-drafting at ~55 % length
  per section returned only −4 % each time, because the incompressible mass is riders, numbers and qualifiers,
  not connective prose. The fourth pass therefore switched strategy to *moving* blocks out (Appendix S), which
  is what the appendix-maximalism rule requires anyway.

### The cut map (what stayed, what moved, what is long-only)
**MAIN TEXT (Add.12 items 1–8 — all present).**
1. *The protocol as the contribution* — §2.1 five-column table + rescue gate + direction rule · §2.2
   learned-initial-state rule (with our own measured 5200 B/5376 B cost) · §2.3 +0 B substitute audit ·
   §2.4 same-keys null and blank-store control appear in the column table; ⇒ item 1 complete.
2. *The self-implicating headline* — §4.1 audit table (all seven arms, uniform `n = 9`) with the CLU row NOT
   RESCUED, §4.2 headline box (0 of 6 **and** not the CLU, −0.2897 ± 0.0328 at 8.8 SE); ⇒ item 2 complete.
3. *Both protocol findings* — the wrong-control/manufactured-dividends result (§2.3, §4.3) and the gate-power +
   selection-split findings (§2.1, §2.5); ⇒ item 3 complete.
4. *R.2 venue firings + R.3 label embargo* — §2.4, **with every rider adjacent**: ARF 14.35× / byte-matched to
   nothing · nine references · registered `k = 5` = 306.76 · band-4 (terminal persistence band) exclusion ·
   `river` ARF ≠ MOA ARF with "ARF-100 is our inference" · "at ARF's shoulder", never "beats ARF" ·
   fallback-being-retired framing · shuffle-is-not-a-drift-free-source · embargo delta convention
   `(embargoed − leaky)/embargoed` · baseline-only / no-memory-cell / NOT-RUN-not-null; ⇒ item 4 complete.
5. *The §4.8 real-image substrate row at the 2,364× handicap* — **stayed in main text as §4.6**, with all three
   riders (matched-items-not-bytes · expected ceiling vs attributable absence of daylight · component-build,
   not a verdict) and the approved N276 wording verbatim; ⇒ item 5 complete, and ⛔ it did **not** move.
6. *The §A20.5 substrate-scope sentence* — §1 "Scope", boundary (a), verbatim; ⇒ item 6 complete.
7. *The three hostile-quote defenses* — (a) the "expected to win" rebuttal, assembled in one place at §4.2 with
   the itemised version at Appendix N.1; (b) the "indistinguishable from an empty store" framing with its
   N190-companion clause (*"an inference-cost win, not a failure"*) at §1 Scope and §4.1; (c) the thinness answer
   at §6 L1 (two real-venue firings + the real-image substrate); ⇒ item 7 complete.
8. *Byte-floor identity + `exp(−½(d/s)²)` suppression* — §3 (T1 and Prop T1.4 as boxed statements, compact per
   the task's "at least in compact form") and §4.5 (the design-identity paragraph kept whole).

**MOVED TO APPENDICES (nothing deleted).**
- r7 §2.6 tuning detail → §2.5 stub + **App A.1 / App I.1** (grid, per-seed selections, before/after).
- r7 §3.3 Prop D2a with its drop map → **App E.1**; r7 §3.4 Theorem T3 with its measurements → **App D**.
- r7 §4.1's protocol-status table + NOT-RUN reasons → **App J**; the audit table's byte columns → **App B.2**.
- r7 §4.4's full ledger table → **App B.2**; r7 §4.5's byte-frontier column → **App H** (main text keeps only
  the gate consequence: 0 of 20 cells clear the gate ⇒ no margin quotable in either direction).
- r7 §4.6's per-radius sweep and three width fits → **App F**; r7 §4.3's per-arm metric-nativeness table →
  **App O.3**; r7 §5.3's quote-scope apparatus (SOSD/Chesetti, Poliak, Feng) → **App Q**.
- Main-text paragraphs displaced by the final pass are reproduced **verbatim** in **App S.1–S.7** (§3
  verification prose · §4.3 falsifier/ledger prose · §4.4 deletion · §4.5 coupling measurement · §5 family
  survey · §5 ancestry · §6 L3–L10) so a pointer always resolves to the full statement, never to a summary.
- All negatives → **App L (29 entries)**, condensed but complete (C-9/Q4): the negatives ledger did not
  disappear and no entry was dropped, including the two venue firings, the embargo refutation and the
  no-daylight null.

**DROPPED TO LONG-ONLY (present in `draft-r7.md`, absent here; each with its reason).**
- r7 App I.1a–I.1d's **full per-arm before/after grid tables** → v6 App I carries the scorecard *summary* with
  every load-bearing number quoted (4–35× re-draw effect, 0/45 and 1/54 counts, 12/45 and 26/45 selections).
  Reason: the tables are re-derivable from the summary plus A.1, and they are pure feedstock at this venue.
- r7 App Q.1's **fourteen BibTeX records** → v6 App Q carries the *quote scopes and never-copy warnings* only.
  Reason: the reference list is assembled at the typesetting pass; the caveats are what a writer must not lose.
- r7 App H.1/H.1b/H.2's **numeric frontier rows** → v6 App H carries the verdict, the extremum
  (+0.0694 ± 0.0491) and the lossy-launder caveat. Reason: the column is a labelled null with no quotable margin.
- r7 App N.2–N.4's per-family paragraphs are condensed; N.1 (including the *"expected to win"* itemisation) is
  carried **in full** because it is a hostile-quote defense.
- r7 §5.4's ε-certified-removal citation paragraph → one clause in §5. Reason: no number rests on it.

### ⚠ `bprime-dividend-family-2` did NOT land before this freeze
Only `.claude/outputs/bprime-dividend-family-2/PREREG.md` exists (no report). Per task rule 6 the draft ships
**without it** and pre-claims nothing. **Fold point, named for a later revision:** §6 L1 (the thinness answer)
and §2.4's design rule (*"the answer is provably not in the table"*) — a second surviving dividend family would
be folded there with its own riders and prereg status **whatever its outcome**, and §4.2's "one surviving
family" scope qualifier plus §1's boundary (b) would move with it.

### Never-quote sweep (per file, positive-controlled, over the finished draft; matrix §0.8–§0.14 + §C3.1)
**Positive controls, all returning hits** (so a zero elsewhere means absence, not a broken sweep): `54.56` (10) ·
`306.76` (5) · `14.35` (3) · `0.2897` (7) · `NO DAYLIGHT` (1) · `2,364` (7) · `ARF's shoulder` (2) · `4.4 SE`
(4) · `reads no worse than` (2) · `fallback being retired` (4) · `17.11` (2) · `0 of 6` (4) · `modal, 8 of 9`
(3) · `expected to win` (2) · `inference-cost win` (1) · `φ-bytes ledgered` (1).
**Zero-hit list (all 0):** `3.6 SE` · `CLU-former` · `cluformer` · `unaskable` · `K5` · `organizer swap` ·
`compositional` · `0.2719` · `0.7254` · `300.09` · `S_eff` · `4.95e-63` · `any_basin` · `the completed gate` ·
`91.25` · `principled forgetting` · `we alone delete` · `ICML 2026` · `0.3019` · `Def. 1` · `0 of 5` ·
`certified unlearning` · `prior mismatch` · `needs > 500 M` · `1089` · `13.9` · `CSF3` · `river ships SAM-kNN` ·
`out-of-control result`.
**Context-adjudicated hits, all compliant:** *"it beats ARF"* ×2 — both **inside the mandatory never-form**
(§2.4 and App R.2.1); *"one-line baseline"* ×2 — both inside the **void-warning** form; *"verified to 1e-9 in
all 28 cells"* ×2 — one is our own **self-flagged erratum** (quoted as wrong), one is the ⛔-never instruction in
App A.1; `43.33` ×3 — always as the superseded value beside the corrected 52.00.

### Compliance notes
- Charter **C-1 (post-reversal)**: no defensive audit-confession paragraph anywhere; J&P 2026 cited only for the
  primitive's introduction, with the continuity sentence *"the CLU, introduced as CHLU in Jawahar & Pierini
  (2026)"* in the abstract and §1. **C-2**: §3/Appendices C–E labelled *verification*, §4 labelled *evidence*,
  with the meeting point (§4.5) reporting the discrepancy. **C-5**: every generalising claim carries `d_in = 5`,
  5–6 items, ~10-token streams, CPU, `n = 9` in-sentence. **C-6**: gate fine print (2 SE, no multiplicity
  correction, per-cell reporting) sits next to the claim. **C-8 hermetic**: no other short of this program is
  referenced anywhere; the folded evidence is described as this program's own other rigs, in third person.
  **C-10**: nothing pruned — displaced text is in App S verbatim.
- **Modal-value rule** applied at every appearance of the CLU ratio (54.56× modal 8 of 9 / 45.60× on seed 8);
  **"reads no worse than"** discipline applied to the 1.78 SE dividend; **genuine-win score sentence** present
  (§1 Scope: *no external benchmark is claimed as won on its own headline metric*) and repeated in App R's
  standing scope.
- Placeholders per house policy: `[WORKING TITLE: …]`, `[AUTHORS PLACEHOLDER]`; venue-class header carried;
  desk-reject checklist items listed as editorial item 1 (metadata scrub, no acknowledgments/funding).

### Open items for the Hub/Head (also in `.claude/outputs/v6-condensation.md`)
1. **The page-limit ruling** (blocking): items 1–7 with adjacent riders are ~8.9 pp on their own. Options,
   ranked, with measured savings — (a) footnote-convention for riders (adjacency preserved, ~0 pp saved in a
   single column); (b) move the §2.4 venue-rider block to App R.2 leaving the approved sentence + framing
   (−430 words ≈ 0.6 pp, touches item 4); (c) move §4.6's riders to App R.1 (−300 ≈ 0.4 pp, touches item 5);
   (d) cut §1's contribution list to four items and the abstract to 300 words (−400 ≈ 0.55 pp, touches nothing
   mandated); (e) drop §3 to the two boxed statements only (−250, item 8 permits compact form); (f) drop §5 to
   the positioning box (−330). Even (b)–(f) together land ≈ 10.5 pp: **8 pp requires a Head ruling on which
   riders may travel in appendices with their numbers.**
2. **Title** is a working title with two candidates in play; **naming debut** (which short debuts "CLU") is a
   standing Head call.
3. **Figures 3 and 5 captions** are inherited unchecked from r7's own open editorial item 4, and Figure 2's
   target render includes the SSD bar.
