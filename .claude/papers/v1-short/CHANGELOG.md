# V1-short — revision log (one line per revision)

- 2026-07-19 · v0.4 · w15 revision (paper-writer) — closes the three MUST-FIXes that dropped V1 from weak-accept to borderline (v1-referee-2); nothing that worked in v0.3 disturbed (F1–F7, Payoff-B fine print, CM-3/CM-8 discipline all intact). **MF-A (router conflation) disentangled — decision ≠ transport, stated once and sharply (§3.2.1 new lead ¶):** §3's det-J=0 object renamed everywhere from "router" to **"state-replacing map" (untrained analytic `no_physics_router`)**; §4.2's `router_mlp` is the *learned decision head* that routes *through* the wormhole's own det-J=1 edge (`v1-router-baseline` L17) and inherits its receipt — "a learned gate bolted onto a certified channel is a consumer of the receipt, not a counterexample." Abstract, contributions 3, §3.2/§3.2.1 table+prose, §5, App C.1/C.1.b/B.2/D all relabelled; "reaches the same targets more cheaply" (a §4.2 FLOPs number imported onto §3's analytic map) deleted. **MF-B (squeeze "collapse" = ζ-grid artifact) restated as a PRICING LAW (preferred option; NO run) —** the squeeze *does* beat C_T by its instantaneous displacement p₀sinh ζ/M₀, priced at energy e^{2ζ}: reach beyond L is exponentially priced in rapidity (verified bracket predicts ζ=2.0105 for d=4.0, ζ=2.6441 for d=5.0 — analytic, not new runs; arithmetic reproduced), wormhole reach is flat-priced (fixed ledger). "cannot beat the box"/"collapses past the box" removed from abstract, contributions 2, §2, §3.2 heading/table/caption, §5; table 0's re-annotated "priced out of swept ζ≤2.0," not "cannot reach." **MF-C (§4.1↔§4.3 C-7 contradiction) — 400-ep budget labelled inline at both sites:** §4.1 point 2 now owns that its escalatable-accuracy headroom is a 400-ep (not-yet-converged) property and at convergence (§4.3 C.4.a, 2000 ep) accuracy *matches* full-budget (payoff = rationing, not accuracy); contribution 4 scoped; A.5 cross-section note gains the epochs axis and the false "no two numbers…contradictory" sentence deleted. **CM-14 folded:** §5 design-rule 3 + App F.2 gain the Newtonian scope clause on the FDT σ* exactness (σ* = proposal-tuning scale post-MH, not a correctness condition; relativistic momentum is Maxwell–Jüttner so MH secures stationarity there) — written as a scope clause, not a retraction (V1's units are Newtonian). Both draft.md (canonical) and draft.tex updated. PDF rebuilds clean (tectonic, 0 undefined refs, only hbox spacing warnings). MF-1 (theory note un-arXiv'd) untouched per task = Head critical path. Deferred/not-mine (flagged to Hub): MF-D (n=8 scope on contributions 6), MF-E (dangling N21/N27), and referee SHOULD-fixes S1–S13. → Hub review (w15).
- 2026-07-09 · v0.3 · w12 revision (paper-writer) — closes the v1-referee SHOULD-FIX punch-list F1–F7; MF-1 (theory note live) remains a Head/critical-path item, untouched. **F3 (the crux) discharged via PAYOFF A, not the referee's soften-option (b):** §3.2.1 NEW — "the receipt cashed out," the router's `det J = 0` measured by autodiff (was "undefined by construction") erases the latched Goldstone charge (std(Q_out)=0, all 16 probes bit-identical) where the wormhole transports it by exact pᵀXΔ (std(Q_out)=std(Q_in)=0.0803, err ≤1.2e-7, reconstruction 2.2e-8); replicates dim{2,4}×seed{0,7}. Definitional→measured. Honest fine print carried inline per CM-7 v1.6: volume alone ≠ latch receipt (det-J=1 random shift scrambles Q, out-spread 3.48×) and the router-beats-gate §4.2 boundary restated *inside* the payoff paragraph. **F1:** headline figure replaced — `fig1_certificate.png` = (a) reach with arms vertically offset + per-arm det-J receipt column (the three coincident 1.0 arms are now individually visible) + (b) the latch payoff scatter (slope-1 transport vs slope-0 erasure), the one plot where wormhole≠router is *visible*. **F2:** `fig2_regime_map.png` = 3 panels, new (c) THE NOISE WALL (gate vs Hopfield across σ∈{0,0.3,0.6} × kv{32,64,96}) — the foregrounded dominant negative is now plotted, not buried in App C.4.c. **F5:** BIBO-coercive-exit caveat moved into §3.1 main text next to the wormhole claim, now *measured* (b=5.0 has ΔH=0 FREE, energy-only test admits, escapes with r*∝T; growth 1.91 vs 1.000 screened) → new App B.2 + Fig 4 (`fig4_bibo.png`), carrying PAYOFF B's caveat verbatim ("the receipt buys BIBO, not the jump; wormhole_blind ≡ no_physics_router exactly"; screen is oracle too). **F4:** §5 MH-kernel wall-of-text → 4 numbered design-rule sentences in main text; full derivation (π-reversibility L1=0.0095, FDT σ*, T_eff annealing, D=½s² coset erosion, CM-3 deflation, specified experiment) → NEW **Appendix F**, fully written per C-10. **F6:** the 4.8× named as **kv16** and its decay stated (1.57× @kv24, 1.14× @kv32; surviving invariant = "never pays full price for less accuracy at any level"). **F7:** Fig-1 title de-leaked (internal "§7.1" gone; caption states the observed edge is d≈3.2, the bracket, not L=2.5). Also: contributions 2→3 split (new "the receipt has a measured consequence"), N-c framing sentence owning the boundary/negative contribution shape, App A.5 (payoff/BIBO provenance, commit `27f232f`, JAX 0.9.0, γ=0.02 BIBO rationale for C-7 cross-section), App C.1 router det-J + C.1.b latch cloud table, App D + 3 payoff fine-print negatives. Deferred per task: N-a (Fig 3 → appendix at the pruning pass). PDF rebuilds clean (tectonic, 0 undefined refs, 2 sub-3pt overfull hboxes). → Hub review (w12).
- 2026-07-09 · v0.2 · w9 revision (paper-writer). §4.3 + App C.4: replaced the `[pending: regime-remap-complete]` slot with CM-8 SETTLED verbatim (full grid, 198 jobs) — under-training artifact (n=8); clean/correlated reversal kv≤64 (Δ+0.02); ρ=0.9 = Hopfield-fragility widening; epoch-budget wall not capacity (kv96@4000, kv128 tie); kv32 over-trains; **THE NOISE WALL as a first-class negative** (0/6 close, gate 0.36 vs Hop 0.71 @σ=0.6 despite fidelity 1.0). Added Fig 2/3 (fig_regime_map.png, fig_frontier_clean.png) + App C.4.a/b/c full tables + App-D N2 (SETTLED) & N2b (noise wall). §5: filled the `[optional slot: thread9-mh-kernel]` with the certified-Markov-kernel design-rule flourish + CM-14 (squeeze-MH HMC-family under stated conditions; governed cascade = Metropolis-within-annealing, not-Gibbs; certified retry erodes latch D=½s²; design rules γ=0/mixture/coset-projection; per-model-T CM-3 deflation) — experiments = future work, no CLU runs claimed. Added Roberts&Tweedie 1996 ref + MALA/Mermin-Wagner prior-art. App-A.2: added minus-the-physics Part B provenance for the Hopfield 0.18→0.88 transfer (Q2). CM-12 already adopted (no hedge to remove). PDF rebuilds clean (tectonic, 0 undefined refs). → v1-referee (w10).
- 2026-07-08 · v0.1 · Initial draft (paper-writer, w8). ML4PS position/theory paper on "paid access." Leads with pillar-4 certificate stack (CM-12) verified on designed testbed; 3 honest learned-memory pillars (CM-2 gate, CM-7 edge, CM-8 cost). C-1 REVERSED (no audit ¶). CM-8 accuracy = marked slot [pending: regime-remap-complete]; thread9-mh-kernel = optional slot in §5. draft.md + draft.tex (builds clean with tectonic, headline Fig 1 = paid_access_reach.png). Appendix-maximal (A–E). Awaiting w9 v1-referee.
