# Advisor Handover — C3 (external Advisor continuity doc)

> **Living document for the ADVISOR thread only, Campaign 3.** Each fresh Advisor reads THIS file first and continues from the latest Running Log entry. Append-only dated log at the bottom; edit stale sections in place; strike, never delete. ⛔ The C2-era `advisor-handover.md` is CLOSED and ARCHIVED — read it only when C2/toy/shorts history is needed.
>
> **Keep this file LIGHT** (the Head's 2026-08-12 ruling): the C2 doc grew too dense to hand over. State goes in §2's few lines; log entries stay short; evidence lives in `.claude/outputs/*` and the charter's addenda, pointed at rather than restated. ⛔ Do not regress to toy-era framing anywhere in this doc.

## 1. Role & read order

- **Roles unchanged:** Research Head (user) directs, final authority, numbered decision lists; Pratik Jawahar authors/edits the repo live. The Advisor owns `.claude/advisor-head-c3-charter.md` (the binding C3 ledger), adjudicates wave gates **from raw artifacts** (⛔ standing self-discipline: FOUR C2-era Advisor errata all came from relaying numbers out of my own text instead of re-deriving — every number handed to anyone gets re-derived from the artifact first), files one short addendum per wave, drafts each wave-lead's spawn. Wave leads scope; the Advisor never scopes task files directly.
- **Read order for a fresh Advisor:** (1) this file, latest entry; (2) `advisor-head-c3-charter.md` IN FULL (it is short; addenda included); (3) `advisor-head-intervention.md` §5/§6/§8 (still in force); (4) the live `[C3Wn]` entries in `.claude/c3-handover.md`; (5) when adjudicating: the wave's `.claude/outputs/*` raw artifacts. Archive (C2 charter + old advisor handover + old hub log) only on demand.

## 2. Current state (edit in place — ⭐ updated 2026-08-13, handover flush)

- **C3W1 is DEEP IN FLIGHT and healthy.** Venues ruled and adopted (C3 charter Add.1): Track A (enwik8/WT-103/PG-19-internal) · ⭐⭐ **CAMELS-US = criterion 4's FIRST measured survivor, adopted Track B primary** · **N-CMAPSS = application venue** (Head ruling, HEPA relevance; 3 permanent guardrails — the 7.988 reference row in every table is a CLAIMS obligation living in the matrix, not just code). Budget convention ruled: **total state bytes AS DEPLOYED** (auxiliary caches incl. conv COUNTED — the GDN-2 escalation ruling); budget is a **ceiling** (2,097,152 B interim), digit binds at the ladder prereg; ⛔ never pre-register solver outputs. **G-B geometry RATIFIED** (8192-atom stores in 3 of 12 layers; placement pre-registered as a design choice; per-store-layer calibration ruled; every TTT number carries the anti-hobbling normalisation sentence).
- **CSF3:** ⭐ **run 3 SUBMITTED and IN FLIGHT** (run 2 + `erosion_partition=True`; the leak-closure ablation; TTT arm SET with the fix; D5; the `preregistered_continuation` exemption — `PREREG_CONT=…` required on every submission/re-resume). ⭐⭐ **Runs 1-vs-2 ADJUDICATED → C3 charter Add.2:** ψ fix = **TIE** (n=2 pairs, permanent — Head ruled run-1 seed 2 a NOT-RUN forever and **run 1 is never reported directly**); the deeper shared result: **the store is CONTENT-INERT at scale** (live-vs-blank ≤ 0.0013 bpc 5/5; self-probe at chance 5/5) while a params-matched GRU beats the CLU 0.014–0.024 and echo ≈ CLU. ⛔ The 0.04-vs-none margin never-quote is **PENDING an explicit Head yes** (Add.2 item 2). TTT NaN on all runs-1/2 legs ⇒ the two-sided control first exists in run 3.
- **Owed to the Advisor:** run-3 adjudication when it lands (baseline interpretation = Add.2) · **the ladder prereg AMENDMENT** (one review: G-B placement argument + per-store-layer calibration + ceiling-as-ceiling + the GDN-2 total-including-conv re-shrink + the allocation-audit rider) **before any ladder arm trains** · the phase-1 **store-liveness diagnostic** design (now the ladder's decisive first measurement per Add.2 item 3) · rival-arm reviews as they land · **A8+ matrix fold content-verification** before any paper-writer touches `claims_matrix.md` (`c2w11-claims-matrix-fold.md` updated 2026-08-13 16:01 — NOT yet verified by me).
- **Owed by others:** the third rival (sliding-window attention) spoke · the CAMELS + N-CMAPSS production loaders (queued behind the ladder start; daymet only; CAFE embargo absolute) · §A26 items 1/3/4/5/8 (C2 housekeeping, my Head queue) · worktree cleanup (wt2/wt3 hold already-merged rival branches).
- **Git (verified at flush):** `main = clu-dev = 3c1dbaa` (G-B + exemption merged at `591d954`; GDN-2 rival `4a523ca`; Mamba-2 rival `3c1dbaa`) · ⛔ `origin` frozen `40c2f31` · worktrees: `CHLU-merge` (main), `CHLU-wt2`/`CHLU-wt3` (merged rival branches, cleanup owed) · the shared checkout still sits on the fully-merged pilot branch (harmless) · one unmerged branch: `c3-rival-ladder-prereg` (its deliverables are `.claude`-side; check whether the branch holds pending amendment code before retiring).

## 3. Standing rulings a fresh Advisor must not re-litigate (C3 set)

- **The pivot is ruled (C2 Add.16):** claim venues are REAL DATA on CSF3 only; the toy substrate is a smoke instrument; tier ii lives as in-block write-swap ablations; tier iii's system swap is the primary control.
- **Venue direction (Head):** physical AI / world models — long-horizon multivariate time series first (HEPA-adjacent), video later; Track A (enwik8/WT-103/PG-19) is the ready spine. ⛔ **Criterion 4 is MEASURED per venue before adoption** (six confirmations; never argue it, run the exemplar-store tripwire).
- **Credits change scheduling, never controls** (A18.4 lineage): prereg, ≥3 seeds, swaps, dyn-eval column, kill-first, NOT-RUNs-never-nulls, byte ledgers — all stand at scale.
- **A rollout must beat persistence at n = 1** before any rollout claim (`rollout_diag.py` is the instrument).
- **Head owns timelines, venues, authorship** — do not track deadlines here.
- The full C2-era never-quote corpus lives in the registries and the archived charters; the curator keeps registries current per wave — quote nothing C2-era without checking `claims_matrix.md`.

## 4. Running Log (newest first)

### 2026-08-13 — Advisor thread 6 continued (C3W1 venue/budget/geometry rulings → C3 Add.1 · runs 1-vs-2 ADJUDICATED → C3 Add.2 · CAMELS survives criterion 4, first ever · N-CMAPSS = application venue · G-B ratified · run 3 in flight · rivals phase 2 already landing) — **NEXT ADVISOR STARTS HERE**

**Session arc (same thread as the C3-open entry below; flushed at the Head's request).** (1) **Venue cycle complete → C3 Add.1:** the scout's brief reviewed (its finding: the 26–47 M grid is EMPTY of modern rivals — we train all five arms; matched-state-bytes is a DECISION spanning 63×); Track A adopted; ⭐⭐ **the CAMELS-US tripwire SURVIVED criterion 4 — the first venue ever** (Advisor re-verified `VERDICT.json` from raw: E 0.2559 vs S 0.758, no fire vs 14 references, no fire at 536× budget; mechanism = unobserved accumulated storage) → **adopted Track B primary**; **N-CMAPSS adopted as an APPLICATION venue on the Head's ruling** (company/HEPA relevance) with three permanent guardrails, and ⭐ guardrail (a) ruled a **claims obligation** — folded into the extended A8 matrix spoke, never code-only. C-MAPSS-as-super-long-horizon advised against on four grounds (label structure, no native horizon, similarity-methods prior, CAFE embargo) and the Head accepted N-CMAPSS as the vehicle instead. (2) **The budget crisis resolved cleanly:** the ruled ≈2 MB rested on a counting error (single-store fp32 vs layer-summed bf16); ruled: **run 3 exempt by pre-registration** (fingerprint-verified one-flag identity — never a re-ruling under pressure), **convention = total as-deployed** (later extended: auxiliary conv caches COUNT — GDN-2 re-shrinks; audit all rivals by walking the official implementation's allocated buffers), **ceiling digit binds at the ladder prereg** (and the prereg amendment rule: never pre-register solver outputs — integer geometry makes exact targets unrealizable). (3) **G-B ratified** (the config shrink is structurally impossible — the w23 floor binds; 3-of-12 full-size stores beats 12 starved ones at identical bytes/compute) with its two riders; the Head's atoms-vs-φ-emitted-parameters and RAM questions answered on the record (atoms = the persistent substrate φ writes into; the bind is fairness, not hardware; compute is the real atom tax). **Rivals funded as phase 2 in parallel — and Mamba-2 + GDN-2 have ALREADY MERGED** (reviews owed). (4) ⭐⭐ **Runs 1-vs-2 adjudicated from raw → C3 Add.2** (config identity verified — exactly `psi_payload_residual` differs): **ψ = TIE at n=2 pairs** (permanent: the Head ruled run-1 seed 2 NOT-RUN forever, **run 1 never reported directly**); the outranking shared result: **the store is CONTENT-INERT at scale** (live=blank ≤ 0.0013, 5/5; self-probe at chance 5/5; params-matched GRU beats the CLU every leg; echo ≈ CLU) — the toy null-with-mechanism reproduced at 28.5 M with ψ controlled for, converging with C2W11's oracle datum and C3W1's G4 on the flat-curve disjunction ⇒ **the phase-1 store-liveness diagnostic is the ladder's decisive first measurement**. ⛔ The 0.04-vs-none never-quote awaits an explicit Head yes. TTT NaN throughout runs 1–2 ⇒ the byte-honest two-sided control first exists in run 3, now flying.

**▶ NEXT ADVISOR — ordered. START HERE.** (1) **The ladder prereg amendment comes to you before any ladder arm trains** — one review covering: the G-B 3-of-12 placement argument · per-store-layer calibration · ceiling-as-ceiling with deployed values of record · GDN-2's total-including-conv re-shrink · the allocation-audit rider · ⭐ **and the phase-1 store-liveness diagnostic as the first rung** (design it against Add.2 item 3: it must separate "carries nothing" from "cannot be addressed" at scale — the C2W11 oracle-addressing control is the toy-side template). (2) **Adjudicate run 3 when it lands** (leak ablation, 2-vs-3, against `PREREG-LeakAblation.md`; baseline interpretation = Add.2; expectation registered: no bpc harm; a >2 SE regression re-prices Add.9 §A28.1 upward). (3) **Review the two landed rival arms** (Mamba-2 `3c1dbaa`, GDN-2 `4a523ca`) — provenance-pinned configs, allocation-derived state, byte ledger entry; and the third (sliding attention) when it lands. (4) **Verify the A8+ matrix fold by content** before any paper-writer touches the matrix. (5) Collect the outstanding Head yes on Add.2 item 2's never-quote; §A26 items 1/3/4/5/8 remain the long-tail queue. (6) Standing discipline: re-derive every decision-grade number from raw before relaying — this thread's streak is intact (zero errata across two campaigns' worth of adjudications this week); the §A28.3→§A26.3 citation-slip class is yours to watch (7th recurrence logged by the C2 close).

---

### 2026-08-12 — Advisor thread 6 (C3 OPENED: the pivot ratified 5/5 + venue amendment · C2 closed at Add.16 · light doc set created · C3W1 spawn handed to the Head) — ~~**NEXT ADVISOR STARTS HERE**~~ **[SUPERSEDED by the 2026-08-13 entry above]**

**Session arc (continues the same thread as the C2 doc's final entry — full detail there):** C2W11 adjudicated from raw → C2 Add.15 (tier-ii unaskable at `d_addr = 4`; store exonerated at 0.8621 under oracle addressing; blocker = the launch head); the Head ratified Add.15's queue, then ruled the **Campaign-3 pivot** in the same session: abandon the toy as a claim venue, run full CLU on real data on CSF3 (credits unconstrained, per-job 2×A100/4-day), venues per charter §3 with the Head's spatio-temporal/long-horizon-TS direction amendment, two scoping tasks funded, run-3 sequencing set, controls unchanged. Advisor filed C2 Add.16 (campaign close), wrote the C3 charter + this doc + `c3-handover.md`, closed the old advisor handover with a pointer, and handed the Head the C3W1 spawn block. **Zero Advisor errata this session** — every number re-derived from raw before being written anywhere.

**▶ NEXT ADVISOR — ordered.** (1) When the C3W1 lead reports its task files: check the scout's venue scorecard measures criterion 4 (never argues it), the harness task's mechanical precondition (pilot-ttt merged) held, and the C2 debts are actually discharged (contents, not existence). (2) **Adjudicate CSF3 runs 1-vs-2** (the ψ ablation) as soon as the Head hands over artifacts — no run number is a scale verdict on its own; the abort criterion remains suspended. (3) Run 3 lands → adjudicate the leak ablation against `PREREG-LeakAblation.md`. (4) File short addenda in the C3 charter; keep every doc light — that is a Head ruling, not a preference.

---
*Maintained by the external Advisor. Update protocol as in the header.*
