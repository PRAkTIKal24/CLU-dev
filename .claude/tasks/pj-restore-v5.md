# pj-restore-v5 — paper-writer — BOUNDED RESTORATION of V5 `pj_sub.tex` (figures + riders + evidence apparatus; the Head's prose is otherwise untouched)

**Commissioned by the Shorts Advisor (charter: `.claude/advisor-head-shorts-charter.md`, Addendum 59, 2026-08-22), on the Head's ruling this session: *"add back in the missing information in pj_sub without rewriting the rest… strictly follow the tone and academic brevity plus accuracy that currently exists in pj_sub.tex… add back the actual figures everywhere making sure no available figure is banked unnecessarily… don't worry about page limits."*** Read `.claude/AGENT_PROTOCOL.md`, then this file, then the four inputs below. Output report: `.claude/outputs/pj-restore-v5.md`.

**⭐ THE EDIT-BAR ON `pj_sub.tex` IS LIFTED FOR THIS PASS ONLY, in this scope only** (Head ruling 2026-08-22; Add.53's bar otherwise stands). You edit exactly ONE content file: `.claude/NIPSsubmission/v5-palm/pj_sub.tex` (+ rebuild artifacts). ⛔ Everything else in the folder — `submission.tex`, `submission.pdf`, `BUILD-NOTE.md`, `figs/*`, the style file, the obsolete `pj_sub_buildcopy.*` set — is byte-untouched, and you write nothing outside this folder except your build note and report.

**DIAL DECLARATION: none — editorial restoration; ⛔ zero new measurements, zero new numbers. Every number you add must be copied from `submission.tex` (or the named output file) with its ancestor line cited in the build note.**

## Inputs (read in this order)
1. This task.
2. `.claude/outputs/pj-fidelity-v5-r2.md` — the authoritative loss/drift inventory (§A.1 dropped-numbers list · §A.2 A2-1…A2-14 · §A.3 · §B.1 · §B.2 Tier-1 #1–16 · §B.4 figure/table mapping).
3. `.claude/outputs/pj-referee-v5-r2.md` — MF-1…8, SF-1…9, N-1…4.
4. `.claude/NIPSsubmission/v5-palm/submission.tex` — the restoration source (it contains verbatim-correct versions of nearly everything lost). `pj_sub.tex` — the object. The folder's `BUILD-NOTE.md` §7 records each figure's home.

## The three rules of this pass
1. **ADDITIVE by default.** The Head's prose is not rewritten, reordered, or compressed. The ONLY non-additive edits are the enumerated corrections in Part C.
2. **Verbatim where verbatim is owed.** Approved wordings, mandatory riders, scope qualifiers and never-quote-adjacent forms restore **VERBATIM from `submission.tex`** (Add.30 boundary). Connective prose you write matches `pj_sub`'s register: academic brevity, plain terms, ⛔ **no intensifiers** — do not imitate the file's "strictly/precisely/completely" layer; the added text carries none.
3. **Nothing invented.** No ancestor → STOP on that item and report it.

## Part A — FIGURES AND TABLES (the Head's explicit instruction; MF-1/MF-2 class)
**A1. Restore ALL 11 figures — every PNG in `figs/` is available and none may stay banked:** replace the 3 placeholder boxes (tex l.102/206/247) with their real figures (`fig1_damping_optimum.png` · `figB_dlaw.png` · `figC_lambda_coset.png` per the captions already in place), and wire the remaining 8 (`fig2_vault` · `fig2_two_instruments` · `figB_signflip` · `figB_massive_vs_flat` · `figC2_vault_emergent` · `figC_register_capacity` · `figA1_damping_optimum_full` · `figC_Tstar`) to the homes the base and `BUILD-NOTE.md` §7 give them — main text only where the figure is the clearest presentation of a main-text result (Fig 1 and the vault contrast are the referee's two MUST-restores there), the rest grouped in the appendix with the result each evidences. Captions restore from the base **verbatim** (Fig 1's already is). ⚠ `figC_Tstar` evidences the T* crossover — its claim returns with Part B item B6, so the figure has a home; if any figure genuinely has no surviving claim after Part B, it still ships to the appendix with its base caption (the Head's no-banking instruction outranks narrative economy this pass) — note it in the build note.
**A2. Restore the 4 missing tables** (fidelity §B.4): both App-C tables (emergent refrigerator; confinement/hop fractions — these carry Part B's restored control arms), the App-B instrument-gap table (+ the mean±sd row of the surviving App-B table), and the base's second negatives table content (rides Part B item B3).
**A3. Fix the two truncated tables** (referee MF-2; overfull 177.5 pt / 152.6 pt): restructure — split into stacked rows or rotate — ⛔ never `\small`-shrink, zero content change.

## Part B — ADDITIVE restorations (consolidated; per-item detail in the reports)
**B1. The deletion story's guards (highest priority):** the encoder-exclusion scope, all three base sentences at their base sites (§A20.5 form · *"a store-level guarantee only — the frozen encoder and any residue of past writes in a learned landscape are separate channels"* · *"the store deleting an item is not the system forgetting it"*) — Tier-1 #1; the explicit certified denial (base L79 *"we claim no certified (ε,δ) unlearning"* and L51 *"We make none of those claims"*) — B.1(a); the three deletion conditions + recency exclusion **beside the claim in §3.3** and in the abstract, with "priority/" restored to the eviction clause, plus the store's scale (dim 3, capacity 8–64, no learning) in the abstract and §3.3 — Tier-1 #14/B.1(b); the score sentence (base L79/L356) + the flat-table trivial-substitute control + the σ_obs modelling-choice admission + the 0.672 substitute control — Tier-1 #3/#4/#9.
**B2. The rider set on the retention/vault results:** Δ=0.5 rad + **ℓ_θ/Δ<0.05 in-line at the 3.77× site** (the paper promises this rule three times and delivers it nowhere — referee MF-5); the 86.97±2.94× first-passage counterpart + the estimator-name sentence at BOTH 107.77× sites (MF-6b); the 11-decade probe-floor rider (*"…eleven orders is one curve on one instrument"*, MF-6a — also resolves the internal 10⁻¹⁵-vs-1.7×10⁻¹² contradiction); the App-B threshold-instrument rider (Tier-1 #7); *"and never the vault"* on 13.88× (A2-10); the 7.942-vs-8.11 reconciliation parenthetical (SF-8); *"the direction, not the number, transfers"* + the 8.11±0.37-is-designed sentence; the designed-symmetry precondition in §3.2 with T*≈3×10⁻³, the λ_coset values and the designed ≤1.1×10⁻¹⁵ (Tier-1 #6); "6 cells" on the law-referenced emergent vault; N108's placement-scope and exact-adversary riders (Tier-1 #8); the fdt/Newtonian fine print beside §3.2's T>0 claims (Tier-1 #12).
**B3. The negatives estate:** restore the 16 missing App-E rows from the base (which makes the existing *"every negative result…"* sentence TRUE — verify and say so); this includes the four that guard surviving claims (score-sentence row · threshold-instruments row · the micro-explanation-Wrong row, which pairs with correction C(g) · the placement-algorithm-is-theirs row · the amplitude-decay/distinguishability row).
**B4. Prior art and citations:** restore App D's prior-art paragraph using the base's own text (the scout-corrected lineage: Snyder '77 canonical · Micciancio '97 oblivious · Naor–Teague '01 WHI/SHI · Blelloch–Golovin '07) + the no-priority clause (*"we claim no priority… the fix-up cascade is theirs"*) + the fourth Blelloch–Golovin attribution site — B.1(d)/SF-1; de-orphan the 10 orphaned references by restoring each entry's base sentence where its claim survives (incl. Jude's drift-bridge sentence and Wang B.'s *"the store deleting an item is not the system forgetting it"* companion, which B1 restores); Sekhari returns to the Guo citation (rides correction C(a)).
**B5. Positioning and scope:** the seam sentence closing §2 (*"Our result sits in that seam, one level down…"* — SF-7); the Titans one-liner (*"Titans learns its gate; the same slot in our store is a budget in (μ,γ,T) whose setting is computed rather than trained"* — SF-5); the abstract's two venue assets — the TTL laundering negative and the explicit conditions (SF-3); the R₅₀ differentiator's numbers + TTL comparator (1.146→0.752 · 0.75–0.77 · 1.52× — Tier-1 #10); the Limitations additions (the deletion store's own scale clause — referee MF-7; limitation (iv); *"No task-level payoff is claimed"*; *"deletion at 10³-item stores"* in named-next); the anonymization note (supplementary/code — Tier-1 #16); the compute-adaptive-read dial named with the trilemma; App C and App D status headers (verification/evidence per base); the theory-note handling per the base (the 2×2 reduction statement — SF-4).
**B6. Precision restorations on correct numbers (A2-items, additive):** *"equal γ_eff"* on the scalar control (A2-3) · the rollout arm's scope (3 emergent seeds, δ-range — A2-5) · *"a large share of sampled"* (A2-7) · the amortized-experiment-unrun admission (A2-11) · the T* crossover claim with its base numbers (supports figC_Tstar) · the confinement claim's two control arms (no-hole 5.5/43.0/2.4% · scalar 0.73/10.2/0.26% — Tier-1 #5, lands with table A2).

## Part C — ENUMERATED corrections (the ONLY non-additive edits; each printed before→after in the build note)
(a) l.80 the Guo sentence → the base's corrected form **verbatim** (§2 Eq. (1), an ε condition with an unnumbered (ε,δ) relaxation immediately after; Sekhari restored to the cite) — the current form inverts it. (b) l.83 `(Mo, 2026)` → `(arXiv:2605.03338)` (Add.45/51 — the token never appears in prose). (c) l.188 *"strictly aligning with"* → *"against"* + the base's 0.90-ratio/discrete-map explanation; §3.1 *"strictly minimized at"* → *"minimized at"* (referee MF-4 — the current sentences are checkably false). (d) the J&P reference entry: delete *"[Reference redacted for double-blind review.]"* → the base's full third-person entry (referee MF-3). (e) l.291 *"Emprical"* → *"Empirical"*. (f) vocabulary accuracy, one word each: *"a variance of 0.35%"* → *"a difference of 0.35%"* (A2-1) · *"nearly 55%"* → *"up to 55%"* (A2-2) · *"exponential factors"* → the base's phrasing (A2-8) · *"is confirmed"* → *"measured"* (A2-9) · *"minimum spacing"* restored for *"boundary"* (A2-6) · delete *"remarkable"* (MF-8). (g) status tags: §3.1 heading's *"Verification"* → the base's **evidence** labelling for the two-instrument result (A2-12); Fig 2 caption *"validation of verification"* → *"verification"* (A2-13). (h) the four App-D sentences the referee lists as meaning-destroying garbles (MF-8) → the base's plain sentences, **verbatim**, listed individually in the build note.

## After editing (all mandatory)
1. Rebuild: `/Library/TeX/texbin/pdflatex -interaction=nonstopmode pj_sub.tex` ×2, inside the folder; 0 errors, 0 undefined references, **0 overfull boxes > 10 pt on tables**; report page count and split.
2. **Two-way numeric check** printed in the build note (every added token → ancestor; every base ancestor → edit-map entry).
3. **Per-file sweeps, positive-controlled**: the fidelity §A.4 zero-hit list re-run (must stay zero — incl. `13.9`, `right-to-be-forgotten`, `memory provenance`) · "certified" per-occurrence table re-printed (denial present, no affirmative form) · author-token rule (`\bMo\b` bibliography-only) · the honest-scope sentence still exactly once.
4. **`BUILD-NOTE-R3.md` in the folder = deliverable #1**: every edit mapped to its worklist item and base ancestor line; the Part-C before→after table; the figure placement map (11/11 with homes); the md5 of the final `pj_sub.tex`; anything not executed, stated honestly with why.
5. Report → `.claude/outputs/pj-restore-v5.md`.

## Acceptance criteria
- 11/11 figures shipped (zero banked), 8/8 tables present, no truncation; every Part-B item present or honestly reported blocked; Part-C applied exactly as enumerated; ⛔ zero edits outside the enumerated set + additive insertions.
- Verbatim riders diff-identical to base; zero new numbers without ancestors; sweeps clean; build clean; App E's completeness sentence true after restoration.
- `submission.tex` and all other folder files byte-untouched; no writes outside the folder except the report.
