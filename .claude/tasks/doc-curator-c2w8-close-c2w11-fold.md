# doc-curator-c2w8-close-c2w11-fold — the C2W8 close fold, two Advisor errata, and the five-session slip

**Campaign 2, wave 11. Agent:** doc-curator. ⛔ **NO WORKTREE, NO BRANCH, NO tracked-code edit.**
Writes `.claude/outputs/doc-curator-c2w8-close-c2w11-fold.md` and edits the transfer/registry
documents named below.
**Budget:** ≈ 0.5 day. **Ungated — runs in parallel with the wave.**

**Binding documents, read first:** charter **ADDENDUM 11 (§A31–§A32) and ADDENDUM 12 (§A33–§A34) IN
FULL** · the `[C2W8-PASS3-SPINE]` (2026-08-10) and `[C2W8-PASS3]` (2026-08-09) §10 entries ·
`.claude/outputs/c2w8p3-{gate-addr,phi-geometry,capture-strong-phi}.md` ·
`.claude/outputs/c2w11/PREREG-C2W11.md`.

## ⭐ DIAL DECLARATION (protocol §7) — echo before your first edit
- **Dial:** none — **documentation integrity.** ⛔ No new claim, no re-scoring, no verdict.
- **Falsifies:** nothing. ⛔ **You never edit the charter or a filed prereg** — flag, never edit.
- ⛔ Depth is not feature importance (§A23.5 ACTIVE). ⛔ Wells are never named semantically.

## THE FOLD (each item names its source; none may be silently skipped)

### 1. ⛔⛔ The two Advisor errata on Addendum 10 (§A31.1–§A31.2, ratified) — propagate the NEVER-QUOTES
- **Erratum 1:** `covered` / `n_never_read` are **LAUNCH-POINT** statistics, computed on `q₀` against
  the codebook in `clu_system._read_diagnostics` ⇒ **store-invariant by construction**. The
  "58/62/62 unassigned, digit-identical" sentence is **RETIRED with the telemetry that produced it**,
  and so is pass 2's *"arm B never addresses"*. ⭐ **The correct, now-standing sentence:** *the store
  can address and still loses to a zero-parameter read of its own keys* (arm A A1 = 0.50/0.42/0.52,
  arm B 0.93/0.98/0.88; both fail on the launder-margin leg).
- **Erratum 2:** ⛔ **§A29.5's mechanism is RETRACTED.** *"σ_q ≈ spacing ⇒ the items are not separable
  by ANY read"* and every quotation of it are **NEVER-QUOTE**. `median_nn_task1` is the NN spacing of
  a **~200-key sizing set** while the store holds **16 items**; against the true population
  σ_q/spacing = **0.32** (MNIST) and **0.19–0.37** (CIFAR) — queries are **~3× CLOSER** to their own
  key than to a neighbour, the **opposite** of the stated mechanism. ⭐ **The failure is real; its
  cause is OPEN and registered (§A32.5).** Mechanical consequence to record: monitor #3's 0.000
  refusal rate was **arithmetic**, not a finding.

### 2. Never-quote additions from C2W8 pass 3
⛔ *"φ is the binding constraint on the CL rig"* (§A29.5) — does **not** survive; the measured relation
is the **inversion** (§A31.4: task-strong φ is the address-**worst** arm beyond 2 SE).
⛔ Any A1 boolean quoted **without `margin_in_SE`** (`randconv` failed by ONE read on 2/3 seeds).
⛔ Any census number quoted before the gate-hardening items land (§A32.3).
⛔ `theta_att` / `P` quoted without `n_non_capturing` beside it.
⛔ "strong φ" used as **one undifferentiated notion** — §A31.4 forbids it program-wide.

### 3. New registry entries (negatives + doctrine), each dated with provenance
- ⭐ **The A31.4 inversion:** on the census, **task accuracy and address geometry are ANTI-correlated**
  (`simclr − randconv` A1 = −0.1406 ± 0.0508, 0/3; `simclr − pca` = −0.1276 ± 0.0589, 0/3), and an
  **unfitted `randconv`** buys the geometry for free at 0 fit steps. ⇒ **the address block is its own
  head; cheap conv-class address geometry is a legitimate default.** ⚠ Carry the qualifier: the cue is
  arm-dependent (`cue_σ/codebook_spacing` 0.927/0.875/**0.710**, a 30 % spread) — it partially
  confounds *"randconv is best"* but does **not** rescue `simclr`.
- ⭐⭐ **The gate rewarded D2a, measured at design level:** ρ(A1, G-DRIFT) = **−0.967**,
  ρ(A1, settle↔launder agreement) = **+0.933** across 9 cells. ⇒ **drift → 0 is NEVER a target**, and
  a gate needs a **two-sided** drift leg or a floor.
- ⭐ **Branch (b) NO DAYLIGHT** (0/9 cells, 3/3 arms): the store and an **832-byte table** are
  statistically indistinguishable on held-out stream reads — **the tier-i thesis at a third substrate**
  (gym → LM block → CL census). ⛔ Per §A32.1 it **folds into B′'s evidence base at the next revision**;
  record the pointer, do not draft.
- **`d = 16` is a declared NOT-RUN**, measured inert at a **fully honoured** 131 072-atom budget ⇒
  **the constraint is REACH, not capacity**; the fix routes to **C2W9**.
- **ONE DEFECT, THREE CONSEQUENCES:** the 200-key sizing spacing vs the 16-item codebook spacing
  produced §A29.5's wrong root cause, monitor #3's never-refusing gate, and pass 3's arm-dependent
  cue. **Record them as one defect, not three.**

### 4. ⭐⭐ THE K6 CROSS-REFERENCE SLIP — FIFTH CONSECUTIVE SESSION, NOW ASSIGNED
Record that **K6 is owned by `c2w11-substrate-and-kills`** (`PREREG-C2W11.md` §4). K6 = §A28.3's
one-line precondition: **the fraction of queries whose asserted set is already exactly right**,
computable **BEFORE any reader is fitted**. Reference fractions: **2/2560 · 3/1280 · 0/2560** (C2W5)
vs **~18 %** (C2W7). Its standing form: *a fitted reader destroys signal only in proportion to that
fraction.* **Cross-reference it correctly everywhere it appears and close the slip.**

### 5. Retired instruments and re-labellings (mechanical, no verdict moves)
- ⛔ **`S_eff ∈ [8, 16]` is RETIRED.** `S_eff = K·F/W` with `W ≤ N_a` ⇒ the band's lower half is
  **unreachable by construction**. The instrument is **direct wells-visited `W/N_a`, two-sided
  labels**; **"COLLAPSED" is reserved for CONCENTRATION**, under-usage is labelled under-usage.
  Retroactive: iteration 1's "S_eff = 16.0 in band" = *"all 32 wells visited"*; C2W7's 16.77 =
  *"30.6/32 visited"*; C2W5's 34.1/51.2/36.6 **is** concentration and its COLLAPSED label **survives**.
- **`best_is_also_lowest_drift`** is retired as too weak (False on all three arms, decided by ties of
  0.004, while the design-level correlation is −0.967).
- **The legal rescale is FULL-STATE co-scaling, address AND payload** — address-only rescaling is
  **not a symmetry**; the payload channel is absolute.

### 6. The transfer documents, per their own protocols
`HEP_primers.md` · the research ledger `philosophy-synthesis.md` (⟲ dated addendum) ·
`.claude/negative_results.md` · `.claude/claims_matrix.md`. ⚠ **`claims_matrix.md` §3 and the
paper-writer agent brief** carry the **PRE-REVERSAL** form of Positioning Charter C-1 — the Head
reversed it 2026-07-07 (*"no defensive audit paragraph in any paper"*) and ruled the reversal
**GOVERNS** (§A18.2). **Verify it was fixed at the C2W4 fold; if not, fix it now** — it mis-fires every
future paper-writer spawn.

## Acceptance (mechanical)
1. Every item 1–6 addressed, each with a `done: true|false` and its source. ⛔ Anything not landed is
   `false` with its reason, never omitted.
2. The two errata's **never-quotes propagated to every document that carries the retired sentence** —
   list the files you swept and the hits, and **positive-control the sweep** (⚠ a directory-level
   search over `.claude/` returns "no matches" spuriously; sweep **per file**).
3. The K6 slip is **closed with a named owner**.
4. ⛔ **No verdict moves and no number is re-scored.** Corrections are labelled reporting-side; the
   charter and every filed prereg are **flagged, never edited**.
5. Reconciliation list in the first 10 lines.
