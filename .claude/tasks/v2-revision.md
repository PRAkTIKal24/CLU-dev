# Task: v2-revision — apply the referee report to the V2 short + F5 note (w8)

- **Agent:** `paper-writer` · **Output:** report to `.claude/outputs/v2-revision.md`; edit `.claude/papers/v2-short/` + `.claude/papers/f5-note/` in place (bump CHANGELOG).
- **Read first:** protocol · `.claude/outputs/v2-referee.md` (THE punch-list — MF-1…5, SF-1…6, NICE) · Positioning Charter (C-1 REVERSED — no audit ¶) · `.claude/claims_matrix.md` (post-w7: CM-1 filled, CM-12 pillar-4-passed; note V2 uses CM-1/CM-4, not the V1 rows) · evidence for the two folds: `fit-gap-anatomy.md` (loan ≈700 + recovery ladder) + `anchor-robustness.md` (already wired per referee — just confirm numbers).

## MUST-FIX (all block submission)
1. **MF-1 — DELETE the §1 audit-confession paragraph** (draft.md L23) + its orphaned cross-refs: reframe App F "audit negatives (physics-audit paragraph, §1)" (N17/N18 are legacy-mechanism confessions → drop or demote to a neutral "class-level design caveats, see theory note" line with NO legacy numbers; N22 saddle-blindness is a genuine scope limitation — KEEP but reframe as a class limitation, drop the §1 back-ref). Keep §2 L44 "150 epochs" forward-ref (not audit framing).
2. **MF-2 — WIRE the measured fit-vs-horizon crossing** (§3.4 L109 currently says "unmeasured"): the loan is called ≈700 steps (twin leads 1–2 orders to ~500, crosses CLU ~700, diverges 196@5000 vs CLU bounded 0.20–0.23); + the recovery ladder (+γ recovers 92% with BIBO/latch/μ² preserved; γ_φ −24% wrong-tool). **Guard (CM-1):** do NOT claim CLU has the lowest plateau (broken-vol 0.14, LSTM 0.13 < CLU 0.22) — the asset is boundedness-by-construction.
3. **MF-3 — RECONCILE §3.4 ↔ App D emergent-lifetime numbers** (the genuine contradiction). §3.4 displays 277/257/303 vs 247/227/263 = +12–15%, calls it "consistent +12–28%"; App D per-seed = +12.5/+28.8/**+5.2%** with different μ². Pick ONE coherent bias range that *includes* the +5% anharmonic seed, make displayed numbers reproduce the endpoints, and state whether §3.4 (v2-full-runs) and App D (v2-prefreeze item 4) are different probes on the same checkpoints (they are — say so). Drop "consistent."
4. **MF-4 — EMBED Fig 1 (GMOR+C2), Fig 2 (Mo head-to-head, the headline), Fig 3 (baseline retention overlay).** PNG assets: `.claude/outputs/v2-full-runs/` + `.claude/outputs/v2-prefreeze-baselines/{figA_retention_overlay.png,figB_lifetime_summary.png}`. Wire `\includegraphics`. Fig 4/5 may stay text+table.
5. **MF-5 (F5 note) — STRIP all 5 `\todohead` margin notes** (they render in the PDF); resolve Q3 anharmonic-% by dropping the parenthetical or citing the short's §3.4. **Cor-3 footnote — DECIDED (Head, 2026-07-08):** keep the corollary as neutral class theory; **CUT the "appears in at least one public instantiation, where it is inert by construction" clause** (the de-anon vector). The pure class fact ("a mean-spectrum penalty is degenerate for the whole class") stays in the Cor body.

## SHOULD-FIX (apply unless it costs a new experiment)
- **SF-4** ML-first reorder: lead §3 with Mo + baseline collapse (evidence), demote GMOR-exactness verification behind them (it's *labeled* verification). At minimum an evidence-first framing paragraph before §3.1.
- **SF-6** §5 "solved constitutive problem" → "a constitutive problem with an exact solvable core and quantified anharmonic deviations."
- **SF-5** reach-pricing "separately-proven" → "separately measured (secondary, aggregate-only at single-unit scale)."
- **N-1** abstract "subsumes" → "contains … as its overdamped limit." **N-3** §4 "contributes" → "reports" (erosion novelty still pending Jul-11).
- **SF-1 (Mo estimator) + SF-2 (FLOP conversion) + SF-3 (GMOR on anchored ckpt):** these may need small runs — DO NOT fabricate. If the data exists in `v2-full-runs`, wire it; else leave a marked `[pending: <slug>]` slot and flag it in your report (the Hub tasks the analyst separately — `v2-referee-experiments`).

**Acceptance:** both PDFs rebuild (tectonic); every MUST-FIX closed or (for MF-5 Cor-3) parked-pending-DEC; a diff-summary in your report mapping each MF/SF to what changed. The draft goes back to `paper-referee` for a re-review pass after this.
