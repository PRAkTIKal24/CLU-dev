# Task: cmapss-fd002-004-fetch — get FD002/FD003/FD004 onto the CAFE path, and fix the label question (experiment-engineer)

- **Agent:** `experiment-engineer` · **Output:** `.claude/outputs/cmapss-fd002-004-fetch.md`
- **Read first:** protocol · **`.claude/outputs/clu-cafe-integration.md`** (§"Data provenance" + the C-MAPSS label caveat + the fit-order footgun — you are extending *your own* prior work; reuse `chlu/eval/cafe_model.py` and `scripts/cafe/run_clu_cafe.py` unchanged where possible) · the handover's WAVE-17 REVIEW (the four-protocol table; the App. G peer table).
- **Git:** branch off local `main` (now at `8068d4f`, suite **354 green**, `cafe` extra declared). CAFE checkout stays **outside** the repo.
- **Scope note:** this is the *data + measurement* pass. It does **NOT** implement horizon-conditioning or the 198K head — that is `clu-horizon-encoder`, which waits on `scout-hepa-predictor`. Keep them separate so the FD002/004 numbers are a clean pre-horizon baseline.

## Why FD002/FD004 specifically
Under HEPA's **matched-head** protocol (paper App. G, Table 5), **MOMENT (0.70) beats HEPA (0.58) on C-MAPSS-2**, as do Moirai (0.66) and TFM-2.5 (0.60) — FD002 is where HEPA's single-latent JEPA *loses* to broad-pretrained foundation models. FD002/FD004 are the **6-operating-condition, multi-fault-mode** sets: **multi-regime = multi-basin**, which is exactly CLU's structural claim and exactly what the theorist marks as needing multi-band/multi-basin structure (C1/C4 `fatal` for Event). So these cells are the most mechanism-diagnostic in the benchmark.

## Items
1. **Fetch + convert FD002/FD003/FD004** by the same route that worked for FD001 (CAFE's own `scripts/download_all.py` C-MAPSS path is **dead** — `ti.arc.nasa.gov/c/6/` and `data.nasa.gov/download/ff5v-kuh6/` both 404; you used the HF fallback). Sanity-check each against the canonical row/unit counts and **state them** (FD001 was 20631 train/100 units, 13096 test/100 units). If the HF mirror lacks a variant, say so rather than substituting.
2. ⭐ **Settle the labelling question, because it may be worth more than the numbers.** You found `cmapss.py` sets `e=1` for all windows and `t = max_cycle − i` = cycles-remaining-*in-recording*, ignoring the true-RUL file — so a test unit with true RUL 112 is labelled `t=0`. **Quantify the damage:** (a) how far off are CAFE's test labels from true RUL, distributionally, per FD00x? (b) **does the h-AUROC ranking change if you use true RUL?** Run CLU both ways on FD001 and report both numbers. This decides whether *any* CAFE C-MAPSS number is externally comparable, and it is a finding the Head is taking to the CAFE team — **make it precise and reproducible, with the exact file/line and a minimal repro.**
3. **Run CLU on FD002/003/004** at the settings that worked for FD001 (`relax_budget=1.6`; both the all-features and `basin_coords`-only arms), 3 seeds, same probe. **Report the raw-statistics baseline alongside every cell** — the 56-d raw-stats reference beat every CLU arm on FD001 (0.7486 vs 0.7230) and must travel with the numbers.
4. **PRE-REGISTER before running** (`.claude/outputs/cmapss-fd002-004-fetch/PREREG.md`). The Hub's committed prediction, from the theorist's collapse taxonomy + your own measured single-basin result: **CLU will do BADLY on FD002/FD004 too — multi-regime data needs multi-basin structure, and you measured that CLU's learned potential is effectively single-basin** (at γT=64 the cross-sample spread of the settled point goes to exactly 0.000 and the probe goes singular). Record the prediction, then report whether it landed. **A confirmed failure here is a real result** — it converts "CLU underperforms" into "CLU underperforms *for the predicted structural reason*", which is what makes the anti-collapse/horizon work a diagnosis rather than a fishing expedition.
5. **Re-measure the relaxation-budget curve on FD002** (the multi-regime set). On FD001 the useful window was a narrow band of *incomplete* relaxation with an optimum near γT≈1.6. **If FD002 is genuinely multi-basin in the data, does the optimum move, or does the single-basin collapse persist?** This is the cheapest direct probe of the multi-basin hypothesis we have.

6. ⭐ **Build and validate the BOUNDED-vs-INFORMATIVE rollout diagnostic** (added 2026-07-20; small, but it de-risks the next experiment). The program's next planned experiment is **CLU's iterated rollout vs HEPA's direct horizon-conditioned prediction**, whose claim is *graceful degradation at long Δt*. There is a trap: **you already measured that CLU's potential is effectively single-basin — at γT=64 every window settles to the same point, cross-sample spread exactly 0.000.** A rollout that collapses to a fixed point is **perfectly "stable" and completely uninformative**, and would read as a win if we only tracked error or boundedness. We must be able to tell those apart *before* we run that experiment, not after.
   - Roll CLU's latent forward over a range of horizons and record, **as a function of rollout length**: (i) prediction error, (ii) **cross-sample spread of the predicted state** (the collapse indicator — the same statistic that hit exactly 0.000), (iii) whether the state stays bounded.
   - **Report the curve, and the rollout length at which spread falls below a stated threshold.** That length is the honest ceiling on any long-horizon claim we can make.
   - Do this on **FD001 and FD002** so we can see whether a genuinely multi-regime dataset delays or prevents the collapse.
   - **State the diagnostic as a reusable definition** (name it, define the threshold, note its failure modes) so the rollout task can adopt it verbatim rather than re-deriving it. **If CLU's rollout collapses early, say so plainly — that is a load-bearing negative result that would reshape the rollout experiment, and it is much cheaper to learn now than after building the HEPA comparison.**

## Acceptance
FD002/003/004 fetched, converted, row/unit-count-verified, and runnable through the existing CAFE path · a quantified answer on cycles-remaining-vs-true-RUL labelling **with CLU h-AUROC measured both ways on FD001** · CLU numbers on all four FD00x with the raw-stats reference beside each · PREREG written before the runs and its outcome reported honestly either way · the FD002 relaxation-budget curve · **the bounded-vs-informative rollout diagnostic defined, measured on FD001+FD002, with the collapse length stated** · suite green · **no changes to the horizon/head path** (that is a separate task). Flag anything that should become a CAFE-team bug report, phrased so the Head can forward it verbatim.
