# Task: clu-cafe-integration — register CLU in cafe-bench, lead with fault-onset vs HEPA (experiment-engineer)

- **Agent:** `experiment-engineer` · **Output:** `.claude/outputs/clu-cafe-integration.md`
- **Read first:** protocol · **`.claude/reference/CAFE_README.md`** (benchmark spec) · **`.claude/outputs/scout-cafe-hepa.md`** (the `Model.encode()` interface + HEPA method — READ THIS FIRST; if not yet delivered, wait for it) · `chlu/eval/clu_scorer.py` (our existing CLU→score bridge — reuse the training/encoding logic) · the handover's 2026-07-20 CAFE-pivot + valley-aware-scoring + anti-collapse entries.
- **Git:** branch off local `main`. **You may `git clone git@github.com:Forgis-Labs/CAFE.git` (Head-authorized; SSH creds on the machine — Hub confirmed).** Keep the CAFE checkout OUTSIDE the repo (e.g. `~/cafe-bench` or `.claude/scratch/`), do NOT vendor it into `chlu/`.
- **Goal:** make CLU a registered `cafe-bench` model and get the **first real CLU-vs-HEPA number on Event Prediction (C-MAPSS FD001)**, whose HEPA h-AUROC is **0.918**.

## Items
1. **Implement the CAFE `Model` interface for CLU** (per the scout's extracted contract — `Model.encode()` etc.). CLU encodes a time-series window/episode into features the CAFE Evaluator consumes. Reuse `clu_scorer.py`'s CLU training (the HCD/EBM objective) and expose the encoding as the CLU latent + physics features (energy, relaxation residual, basin-membership). One-line registration per CAFE's convention.
2. **Lead task = Event Prediction / fault onset (h-AUROC).** Wire CLU to FD001–004 (C-MAPSS) via cafe-bench. **The framing that fits CLU:** a fault onset is the system **approaching basin-exit / instability** — encode "distance-to-instability" features (energy trend, `‖∇V‖`, causal-cap proximity, the governor's braking signal) as the event signal, not raw fit. Run **FD001 first** as the smoke → report CLU h-AUROC vs HEPA's 0.918. Do NOT tune to win yet — get a clean first number on the real harness.
3. **Anomaly encoding = valley-aware (the CLU-specific lever, cross-links `valley-aware-scoring`):** for CAFE anomaly (VUS-PR, TSB-AD-M etc.), the encoding should surface the **basin-membership / EBM** signal (relaxation residual + `V(q*)` at the settled point + valley-conditioned prediction error — mask the transient, score near `‖∇V‖≈0`). DeepSVDD/LSTM-AE are the native CAFE EBM/recon baselines to beat/match.
4. **Baselines come free:** CAFE already registers HEPA, MOMENT, UniTS, DeepSVDD, LSTM-AE, DeepHit, CoxPH — **do NOT rebuild them.** Our job is to register CLU and read the leaderboard. (This supersedes the earlier "add parametric baselines to chlu/eval" idea — CAFE provides the peers.)
5. **CSF3-ready:** the CAFE sweeps run on A100 via `cafe-bench run-all`; provide a `scripts/csf3/` job wrapper (mirror `job_gpu_eval.sh`, `-n 1 -c 8`, logs/, fetch-once for datasets). Smoke locally on FD001 first (Head's standing rule: never spend CSF to find a local bug).
6. **Honesty (CM-3 / the positioning philosophy):** report the first number as-is. CLU will likely trail HEPA on raw h-AUROC pre-tuning — that's expected and fine; the point is a clean, comparable, real-harness result to iterate from. The tuning + valley-aware + anti-collapse work is what moves it.

## Acceptance
CLU registered as a `cafe-bench` model (interface implemented, reuses `clu_scorer` training); a first FD001 Event-Prediction **h-AUROC vs HEPA 0.918** reported (local smoke, then CSF-ready); the basin-exit event encoding + valley-aware anomaly encoding wired; CAFE checkout kept out of `chlu/`; suite green; a CSF3 job wrapper for the CAFE sweep. **Report the CLU-vs-HEPA gap plainly and name the top-3 levers to close it** (training length, valley-aware scoring, anti-collapse reg, encode-feature choice).
