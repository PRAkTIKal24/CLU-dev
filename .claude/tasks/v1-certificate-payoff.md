# Task: v1-certificate-payoff — turn "beats the router" from definitional into MEASURED (w11; the one addition that lifts V1 above weak-accept)

- **Agent:** `experiment-engineer` · **Output:** `.claude/outputs/v1-certificate-payoff.md`
- **Read first:** protocol · `.claude/outputs/v1-referee.md` **F3** (the sharpest attack: the wormhole "beats a no-physics router" only by carrying a det J=1 label of *no demonstrated downstream consequence*) + missing-exp #1 · `paid-access-experiments.md` (the machinery: `WormholeChannels`, the router arm, §7.2 latch-transit, §7.3 certificates — **CHECK FIRST whether a router-latch or router-exit failure was already logged**; if so this is a wiring/plotting note, not a new run) · `paid-access-theory.md` §7.4 (coercive-exit BIBO) · claims matrix CM-12/CM-7.
- **Why:** every V1 result on a *learned/testbed* system is currently a boundary or a negative; the certificate stack's value (BIBO/latch preservation) is *asserted*, never *shown as a failure the router actually incurs*. One arm where the certificate-free router demonstrably violates a guarantee the wormhole preserves converts the definitional win into a measured one — the referee's single highest-leverage addition. **Still on the designed testbed (oracle placement) — this does NOT claim a real-data win; it demonstrates the certificate's downstream consequence.**
- **Git:** branch `agent/experiment-engineer/v1-certificate-payoff`; worktree if any concurrent code task.

## Items (pick the cleanest 1–2 that the machinery already supports)
1. **Router erases the latch, wormhole transports it.** Run the §7.2 latch-transit test with a **no-physics-router jump** as an added arm: the router routes to the target but (having no det J=1 / no p⊤XΔ accounting) scrambles the stored Goldstone charge Q, where the matched wormhole transports it by the exact p⊤XΔ (and the coset-tangent channel preserves it, ΔQ=0). Deliverable: ΔQ per arm — wormhole exact/zero, router uncontrolled (mirror the existing random-shift baseline that erases Q).
2. **Router exit blows up BIBO, wormhole exit stays bounded.** Place a channel exit in a **non-coercive region** (Deep/Conv-style, or outside the coercive sub-level set): the certificate-free router jump → trajectory escapes (r*→∞); the wormhole, constrained to coercive exits by its certificate, stays bounded. Deliverable: r*/BIBO per arm. (This directly exercises paid-access-theory §7.4 issue-7 — currently only stated as a caveat.)
3. Certificates re-verified on all arms (det J, ledger) so the comparison is apples-to-apples.

## Acceptance
A table/figure the V1 short can embed showing **at least one guarantee the wormhole provably preserves and the no-physics router provably violates** (latch transport OR BIBO-bounded exit), with the certificates measured. If the machinery already logged it → deliver the extraction + plot. If neither is cheaply reachable with existing code → report that plainly (C-9) so V1 falls back to the referee's option (b): soften §3.2 to "reaches with a receipt the router cannot supply" and state the receipt's value is argued, not measured. Flag-provenance per §5.
