# bprime-mamba2-arm — the Mamba-2 rival arm for the B′ audit (Head-funded, ruling 5, 2026-08-01)

**Campaign 2, wave C2W5. Agent:** experiment-engineer. **Worktree: takes the FIRST slot freed by
`bprime-referee-closures` or `plan-pass-vectorise` (≤3 staggered held).** Branch
`bprime-mamba2-arm`. Writes `.claude/outputs/bprime-mamba2-arm.md` + artifacts.

**Why now (the Head's rationale, verbatim in substance):** the audit's authority is coverage, the
rival rig is warm, and an engineer is live; reopening the rig at the polish pass is the expensive
path to the same table row. This closes the referee's missing-experiment 5 (its read: if only one
§8.3 closure is funded, fund Mamba-2 — the survey names SSMs and none is measured).

**Read first:** `.claude/AGENT_PROTOCOL.md`; `.claude/outputs/bprime-rivals.md` +
`bprime-rivals-f3.md` (the rig, the protocol, the F3 tuning standard you inherit);
`chlu/experiments/exp_bprime_rivals.py` (the harness — extend, do not fork);
`.claude/outputs/pilot-placement-probe/n9_full_columns.json` + the §1.1 aggregation pattern (your
columns aggregate through the same shipped `audit_table` rule); `draft-r2.md` §5.2 (the survey
sentence your row backs); charter §A14.2 · §A16.1 · §A18.1.

## ⭐ DIAL DECLARATION (protocol §7, C2 form) — echo before your first result
- **Dial:** none — tier-i audit coverage (a rival row, not a CLU claim).
- **Control:** the full B′ column set IS the control structure: launder (P5 projected AND the raw
  +0 B table — the pre-registered R4/R5 distinction), blank store, same-keys null, rescue gate.
- **Falsifies:** nothing of ours; the row reports whatever it reports. ⛔ Selection on the eval
  split invalidates it (held-out selection per F3).
- **Does NOT falsify:** Mamba-2 beating the raw table would be the audit's FIRST positive rival
  row — report it exactly as the protocol scores it (unexplained win permitted, uncontrolled never).

## The work
1. **Minimal faithful Mamba-2 arm** (selective SSM with the SSD/state-space-duality update per the
   published rule; chunk granularity matched to the rig's) at **matched params AND state-bytes**
   (iso-state head-width derivation in the report, same as the existing arms; learned-initial-state
   rule: init = PARAMETERS, per-stream deviation = STATE, both declared).
2. **Full audit columns on `aggregate`** + the labelled `overload` byte-frontier row (frontier =
   labelled column, never a dividend family, §A14.2): full · launder (projected + raw) · dividend ·
   +0 B margin · raw-table margin · blank · same-keys null · two-sided byte ledger (corrected law) ·
   substitute audit.
3. **Pooled n=9 from the start** (A18.1: no n=3 rescue verdict exists to retract if you never make
   one) · **the F3 tuning discipline**: the full grid, held-out selection, the before/after check —
   the same standard every other arm survived; a hobbled Mamba-2 is the referee attack the Head
   funded this row to avoid.
4. Cite Mamba-2 from the published paper (Dao & Gu; verify venue/year — flag `bprime-cite-check`'s
   pattern if any citation fact is single-sourced).

## Acceptance
The row lands with every column at n=9, aggregation byte-identical to the shipped rule, rescue-gate
verdict at n=9, ledger identity checks green, tests green on your branch (extend
`tests/test_bprime_rivals*` patterns). Declared NOT-RUNs never nulls. Report → Hub, spawn nothing.
**Ownership:** your arm's module + additive hunks in `exp_bprime_rivals.py` + your tests. ⛔ Not the
factored-store files, not `monitors.py`, not `train_cluformer.py` (lane-parallel's). **Git:** branch
+ scoped worktree; never push `origin`; `clu-dev` only.
