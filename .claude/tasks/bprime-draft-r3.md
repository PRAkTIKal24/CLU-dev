# bprime-draft-r3 — the POST-referee revision of the B′ audit paper

**Campaign 2, wave C2W5. Agent:** paper-writer. **NO WORKTREE. ⚠ GATED — spawns only when ALL
THREE have landed:** (1) `.claude/outputs/bprime-referee.md` (the referee report on r2), (2) the
two rider artifacts in `.claude/outputs/pilot-placement-probe/` (`n9_full_columns.json` + the
labelled deltanet frontier row), (3) `.claude/outputs/bprime-cite-check.md`. Produces
**`.claude/papers/bprime/draft-r3.md`** + a CHANGELOG line. Charter **ADDENDUM 4 §A19 task 3**
(the B′ hardening chain's writer leg), per rulings **A18.3/A18.5/A18.6**.

⛔ **Revision convention (A18.5, Head-ruled): `r2`, `r3`, … — NEVER "v2"** (collision with the V2
short). The prior revision is `draft-r2.md` (renamed from `draft-v2.md`, CHANGELOG records it).

**Read first:** `.claude/tasks/bprime-draft.md` (your predecessor task — §1's claims→artifact table
is still binding; extend it for anything you touch); `draft-r2.md` + `CHANGELOG.md`; the three
gating artifacts above; `claims_matrix.md` §0 + CM-27/28/29; `PREREG-Bprime.md` +
`ERRATA-Bprime.md`; charter **§A13** (tier-i discipline: the reframe claimed nowhere) · **§A14.2** ·
**§A18.1** (the ratified never-quote additions — enforce every one); the Positioning Charter
(**C-1 POST-reversal: no defensive audit paragraph**; the agent brief was fixed 2026-08-01 — if
your brief still shows the pre-reversal form, the Charter's dated reversal GOVERNS, A18.2).

---

## The revision, in order
1. **Referee must-fixes** — all of them, each logged in the CHANGELOG line with its referee item
   number. Should-fixes: apply unless they conflict with a binding ruling; log which were skipped
   and why (one line each). Nice-to-haves: your judgement, logged.
2. **The uniform-n=9 fold:** replace App. I.1c's un-aggregated columns with the rider's
   `n9_full_columns` table; re-label every previously-mixed-n site; delete the "honest and
   temporary" mixed-n framing wherever it is thereby retired. If any adjudication sentence changes
   sign or significance under the uniform columns, **the claim changes with it** (the F3
   pre-commitment pattern) — flag such changes prominently in the CHANGELOG.
3. **The deltanet frontier row:** fold it in, explicitly labelled **byte-frontier column, never a
   dividend family** (§A14.2), no verdict language in either direction beyond the label.
4. **Citations:** apply `bprime-cite-check`'s corrections (ready-to-paste BibTeX; the Feng quote
   per its verdict — paraphrase-with-citation or cut if UNVERIFIABLE).
5. **Never-quote sweep (A18.1, mechanical, last):** grep-verify zero violations — n=3 rescue
   verdicts · "≥ 3.6 SE" · ∞ ratio without sign-SGD · pilot toy numbers as tier-iii results ·
   unscoped γ-bands · rival numbers without the F3 before/after beside them · "CLU-former" ·
   tier-ii/iii vocabulary · semantic well names · the MUNKEY workshop name.

## Standing discipline (unchanged from your predecessor)
You do not invent results, run experiments, or touch `chlu/` code. Every number traces to a named
artifact; the claims→artifact→caveat table stays 0-unsourced. Appendix-maximalism policy holds.
No LaTeX build unless the Head asks. Multi-seed numbers only.

## Acceptance
`draft-r3.md` complete; CHANGELOG line appended (referee items dispositioned; any claim changes
from the n=9 fold flagged); the §5 grep sweep results printed in your report; the extended
claims→artifact table delta included. Report → Hub, spawn nothing.

---

# HUB ADDENDUM (2026-08-01, at gate-open — the referee, riders and cite-check have ALL landed; this
block resolves every "Hub ruling owed" the referee filed, so you are unblocked on all of them)

**Your spec is `.claude/outputs/bprime-referee.md` MF-1…MF-5 + SF-1…SF-7 + SF-9/SF-10 (SF-8 figures
are NOT yours — engineer task, separate). Rulings:**
1. **MF-1 (the CLU ✅-RESCUED defect):** apply the referee's fix direction exactly — print the CLU's
   gate status honestly (**NOT RESCUED; full −0.5261 ± 0.0863 reads BELOW its blank −0.4221**),
   state the below-blank fact as a finding *consistent with the thesis*, extend §4.2's arm-count
   qualifier to the CLU as it is to TTT-MLP. ⛔ **New never-quote (Hub, on the referee's candidate):
   the CLU's "✅ rescued" at n=3.** The CLU n=9 column is a HEAD decision at the wave review — do
   NOT wait on it; if funded it folds at a later revision.
2. **MF-3:** CM-27(c)'s coverage referent is **N186** ("`aggregate`/`manifold` 0/3 write-admissible
   after escalation; every Route-3/FB4 verdict rests on ONE family") — wire the anchor-vs-family
   objection (N170/N171) + this coverage clause into §2.5 in our own voice.
3. **SF-7 / CM-28(ccc):** discharged by rider 2 — **App H re-points at
   `n9_deltanet_frontier_table.md`** (n=9, post-F3 code path, 0/15 rescued, a LABELLED NULL; the
   C2W4 first-pass column may remain as history beside it with its NOT-RUN note).
4. **Riders' new findings need homes (probe report §1):** **R4** — TTT byte-ledger columns vary per
   seed with the selected mini-batch `b`; ⛔ never quote a single TTT byte figure as *the* n=9
   value; state the `b → (d_head, bytes)` mapping (this also closes SF-2). **R5** — both TTT arms
   sit BELOW their own same-keys null at n=9 (−0.2063 ± 0.1016 / −0.1995 ± 0.0665) while all three
   delta arms sit above; a new rival-side observation, no CLU claim changes.
5. **Cite-check (`.claude/outputs/bprime-cite-check.md`):** GDN-2 → "Hatamizadeh et al., 2026"
   (§1 line 54 + BibTeX); keep the Feng PARAPHRASE (do not quote; if you must quote, ACL P19-1554
   wording only) and add the scope clause ("by the same logic" — Feng is about datasets, the
   memory transfer is our analogy); **add the one EDA clause** (*"concurrently, Erase-then-Delta
   (Li et al., 2026) makes the same erase/write-decoupling move at larger scale"*) in §5 — it turns
   "the reference arm has moved" into a trend and pre-empts the convenient-frontier read; SDM: add
   the optional "(our own convention, not SDM's column)" clause at l.89. MUNKEY: cited 0×; if any
   sentence adds it, use ERRATA **E2a**'s form (no workshop name, NO "(oral)").
6. **MF-5:** write the supplementary-commitment sentence + appendix note (pre-registration
   documents, dated, to supplementary); the packaging itself is Head-owned at venue time. Reword
   "previously published" → "the closed form stated in our pre-registration" (all three sites).
7. **Mixed-n residue:** rider 1 (`n9_full_columns_table.md`) closes App I.1c and SF-3/SF-4a — quote
   the n=9 dividend directly where §4.3 currently argues by two components; attach n and spread at
   first use of −0.0789's successor; wire SF-4b's one-sentence stopping defense (I.1a has the
   material).
8. **SF-1:** state B.5's direction rule once (the gate suppresses comparative margins in the
   flattering direction, not an arm's own loss-to-table) and reword §4.1.1's sentence.
9. **N-1/N-2 (register/glyphs):** apply N-2 (cut the internal-process sentence); N-1 (glyph strip)
   is deferred to the LaTeX pass — not now.
