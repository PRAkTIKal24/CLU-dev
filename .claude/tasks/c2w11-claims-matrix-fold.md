# c2w11-claims-matrix-fold — the ONE C2 close-out debt still open (A8, partial)

**Owner:** `doc-curator` · **Wave:** C2W11 close-out (Head-ruled 2026-08-13: **the fold stays C2W11's**,
not C3W1's) · **Worktrees: ZERO** · **Bash: not needed** · ⛔ **Zero code, zero git footprint.**

## Why this exists

`.claude/tasks/c2-closeout-debts.md` **A8** names **two** registries: `negative_results.md` **and**
`claims_matrix.md`. The C2W11 curator fold landed **N302–N312** in the negatives registry,
`FW-C2W11-1…12` in `future_work.md`, a dated ⟲ ledger addendum and primer record 11.30 — but
**`claims_matrix.md` was never touched.** Verified 2026-08-13: the file is still **v2.15** (the C2W8
fold), its last row is **CM-39**, its last section is **§0.13 (C2W10)**, and it contains **zero**
occurrences of *"unaskable"*.

⛔ **The consequence, in the C2W11 Hub's own words:** *"a paper-writer loading only the matrix would
today miss the entire C2W11 never-quote list, including 'the organizer swap failed' and any 'K5
verdict'."* That is the highest-value open doc gap in the program and it is the last thing standing
between Campaign 2 and a clean close.

## Your job

Open **v2.16** in `.claude/claims_matrix.md` as an **ADDITIVE-ONLY** pass, in the exact shape of the
v2.15 / §0.12 / CM-38+CM-39 precedent you already set:

1. **NEW §0.14 — the C2W11 never-quotes**, the whole wave.
2. **NEW row CM-40 — forbidden claims (C2W11).**
3. **NEW row CM-41 — approved wording (what C2W11 actually established).**

⛔ **Change no existing clause, wording, number, tier or version status.** v2.15 and everything below
it keep their status verbatim behind your line. ⚠ **Mark the version PROPOSED, PENDING HUB
CONFIRMATION** — the curator never self-confirms.

## The content — charter Add.15 **§A43.6**, folded entire

Every one of these is already ratified and already carries a registry entry (N302–N312); your job is
to give them a **matrix home**, not to re-derive them.

**Forbidden (CM-40 / §0.14):**
- ⛔ **any *"K5 verdict"* from this wave** — second consecutive abstain, mechanical δ = 0.01, 5/5
  seeds, `K5_PASS = null`; **no table-expressibility claim exists.**
- ⛔ **the banked `0.2719` launch ceiling in any C2W11 context** — recomputed **`0.3449 ± 0.0175`** on
  feature-factored launches; the C2W5 instrument is **invalid** under them.
- ⛔ ***"the organizer swap failed"* / *"physics lost the swap"* in any form.** ✅ **The only quotable
  form is *"UNASKABLE at `d_addr = 4`"*** — a **DECLARED NOT-RUN**, never a null.
- ⛔ **V2a's `0.7254` as a calibration *win*** — floor pass only, swap NOT-RUN. Quote **only** with the
  blank-store (0.5029) and shuffled-labels (0.4972) negatives beside it **and** the R2
  non-discrimination stated (the registered permuted-payload negative measures **0.7903** and is
  **structurally non-discriminating for a depth-keyed channel**).
- ⛔ ***"a flat anytime curve ⇒ the store carries nothing"*** — the **disjunction rule** (§A42.7):
  flatness licenses only *"carries nothing **OR** cannot be addressed"* (oracle-addressed
  0.0223 → 0.8219 → 0.8711 on the same store/physics/budgets).
- ⛔ ***"the store is inert"* at this substrate** (§A42.2) — the store is **EXONERATED**.
- ⛔ **N5's SEEN `0.4766`**, and **N4's `set_code` numbers without the noiseless-key flag.**

**Re-specifications to record:**
- `PREREG-C2W11.md` **§5**'s permuted-payload negative is **re-specified for depth-keyed channels**
  (label-permutation beside it, per spoke C's §5.2). ⛔ Preregs are **flagged, never edited**.
- `PREREG-TierII.md` **§3.5**'s F5 discussion **gains both halves** — the 0.89-vs-0.26 gap was **the
  optimiser**, measured closing to **0.9497 vs 0.8239** at a real budget.

**Approved wording (CM-41)** — build these from the banked findings, each with its scope attached:
- ⭐⭐ **the store's exoneration:** oracle-addressed exact-set **`0.8621 ± 0.0036`**, occupancy 0.9673,
  payload error 0.0002, **zero-parameter read** ⇒ *the store, settle and payload composition work the
  moment addressing is correct* — **the blocker is the launch head's ASSERTION** (M3 `0.9402 ± 0.0124`,
  +82 SE; launch-head precision **0.2356**).
- ⭐ **the bound, from two blind spokes on two instruments:** `0.3290 ± 0.0074` · `0.3449 ± 0.0175`.
- ⭐⭐ **the `d_addr` squeeze (N312):** placement headroom **0.0851 → 0.0375 → 0.0109 → 0.0000** across
  `d_addr` 4/6/8/12 — at `d_addr = 12` the launch head's own identity assignment **is already the
  optimal bijection** ⇒ ⛔ ***raising `d_addr` does not open the tier-ii question.***
- **the V2a MECHANICS floor:** AUROC `0.7254 ± 0.0126` vs a 0.60 floor, graded response monotone
  (−1.740 / −1.415 / −1.158 at 0/1/2 novel channels) — **mechanics only, never calibration value.**
- **the wave disposition:** *C2W11 closes as a MECHANICS wave; the tier-ii VALUE surface is a
  DECLARED NOT-RUN with a bound and a named next dial.*
- ⛔ **the toy substrate is DEMOTED** (Add.16 §A44.1) — smoke/regression instrument only, **never a
  claim venue**; no VALUE number, no organizer-swap verdict, no paper number from that family.

⚠ **Riders R1/R2 travel with any V-leg sentence:** the V-leg launch key and the novel/known split are
**NOT** in `FROZEN-INTERFACES-C2W11.json` ⇒ ⛔ **no V2/V3 swap difference may EVER be computed from
C2W11 artifacts** until key/split identity across spokes is byte-confirmed.

## Sources (read these, quote nothing else)

`.claude/advisor-head-c2-charter.md` **§A42 + §A43** (⛔ **append-only-frozen — flag, never edit**) ·
`.claude/negative_results.md` **N302–N312** (your own entries — the numbers live there) ·
`.claude/outputs/c2w11-physics-organizer.md` · `.claude/outputs/c2w11-organizer-swap-nulls.md` ·
`.claude/outputs/c2w11/DADDR-CEILING-PROBE.json` · `.claude/outputs/doc-curator-c2w11-c2-close.md`
(your prior pass — do not duplicate it).

## Acceptance criterion (mechanical)

`claims_matrix.md` contains **§0.14**, **CM-40** and **CM-41**; the file's version line reads
**v2.16 … PROPOSED, PENDING HUB CONFIRMATION**; the string **"unaskable"** appears in it; and
**v2.15 / §0.13 / CM-38 / CM-39 are byte-unchanged**. Report to `.claude/outputs/` as usual, with the
honest first-10-lines reconciliation list.

✅ **PART A LANDED 2026-08-13** — verified by the Hub: `claims_matrix.md` is at **v2.16** with §0.14,
CM-40, CM-41, PROPOSED-pending-confirmation, *"unaskable"* present. **Do not redo it.**

---

# ⭐⭐ PART B — THE C3 EXTENSION (added by the C2W11 Hub, 2026-08-13, on the Head's relay amendment item 7)

**Charter basis, on disk now: `.claude/advisor-head-c3-charter.md` → C3 ADDENDUM 1 §1, §2, §3.**
**Head ruling (verbatim in substance):** *"Guardrail (a) is a CLAIMS obligation, not a code detail. It
lives in `claims_matrix.md` as a never-quote row — folded by the extended A8 spoke — with the loader
assertion as belt-and-braces, never as the only enforcement."*

⛔⛔ **THE MATRIX IS UNSAFE FOR ANY PAPER-WRITER UNTIL THIS LANDS. It is the wave's most urgent
zero-worktree item** (C3 Add.1 §3). ⛔ **ONE curator pass. TWO clearly-separated campaign sections,
accounting kept separate** — C2W11's §0.14/CM-40/CM-41 stay under **C2** accounting (already landed,
byte-unchanged); everything below opens a **distinct C3 block** so a C2 row and a C3 row can never be
conflated by a reader or a grep.

## B0. Namespace (Hub-specified so accounting cannot bleed)
Open a **new top-level C3 division** — ⛔ **not** a continuation of the `§0.x` series, which is C2's:
- **§C3.1 — C3W1 venue never-quotes** (the prose section).
- **Rows `CM-C3-1` … `CM-C3-5`**, one per item below. ⛔ **Do NOT extend the CM-40/41 numbering** —
  C3 rows must be greppable as C3 rows.
- The version line becomes **v2.17**, again **PROPOSED, PENDING HUB CONFIRMATION**, and states in one
  clause that it opens the C3 division while leaving **every C2 row and section byte-unchanged**.

## B1. ⭐⭐ `CM-C3-1` — N-CMAPSS guardrail (a): the exemplar reference row (the Head's headline item)
- **N-CMAPSS DS02 is an APPLICATION venue (company-research relevance, e.g. HEPA). ⛔ NEVER a
  primary-claim venue.**
- ⛔ **Every N-CMAPSS table PRINTS the matched-bytes exemplar reference row — `7.988` RMSE — and its
  TIE with the weakest published deep model, `FNN 7.89`.** **Disclosure is the defense.**
- ⛔ **Any N-CMAPSS number quoted WITHOUT that reference row beside it, and without the FNN-tie fact,
  is NEVER-QUOTE.**
- ✅ **Admissible claim form: capability / application ONLY.** ⛔ **Never *"beats SOTA on RUL"***, in
  any wording.
- Record guardrail **(b)** as the venue's scope: its CLU-native questions are **the persistent-store-
  across-units question** (C2W10's re-homed VALUE question), **the anytime curve**, and
  **interpretable-store probes** — ⛔ nothing else is what this venue is for.
- Record guardrail **(c)**: ⛔ **the CAFE embargo is ABSOLUTE, BOTH DIRECTIONS** — original NASA files
  only; **HEPA comparisons internal-only unless re-derived on corrected labels.** ⚠ Cross-reference
  the banked CAFE label-bug finding: ⛔ **no CAFE C-MAPSS number is externally comparable.**
- ⚠ **State in the row that the loader-side assertion is BELT-AND-BRACES, never the only enforcement**
  — that is exactly why this row exists.

## B2. `CM-C3-2` — CAMELS-US caveats (⭐ and its positive, which must travel too)
- ⭐⭐ **The positive, recorded because it is criterion 4's FIRST measured survivor in six attempts:**
  `E = 0.2559` at matched bytes vs `S = 0.758`; `margin_rel = −0.6625` against a `−0.02` threshold;
  **no fire vs all 14 references; no fire at 536× budget.** Mechanism **as pre-registered**: the target
  depends on **unobserved accumulated storage**.
- ⛔ **The shipped SAC-SMA output is CALIBRATION-PERIOD — never quoted against the LSTM.**
- ⛔ **`maurer` forcing is measured DEFECTIVE** (`tmax == tmin` on **>99 %** of days, **20/20** basins)
  ⇒ **`daymet` forcing ONLY.**
- ⛔ **The reference currency is the MEDIAN `0.758`, not the mean `0.72`.**
- Licence `cc-by-4.0` (Zenodo 15529996).

## B3. `CM-C3-3` — the state-byte convention
- **Convention: TOTAL state bytes, AS-DEPLOYED dtype.** ⛔ **No dtype normalization** — the fp32 store
  pays its real cost. **`dtype_bytes` is DECLARED per row, never inferred.**
- ⛔ **Never quote a state-byte figure without its convention AND its dtype.**
- ⛔ **The scout's single-store `1,966,080 B` row is NEVER quotable as *"the CLU's state"*.**
- ⚠ Record that **the ceiling digit binds at the rival-ladder PREREG, not before**, and that the ladder
  prereg **comes to the Advisor before any ladder arm trains**.

## B4. `CM-C3-4` — the dynamic-evaluation category error
- ⛔ **Dyn-eval's published `0.94` bpc is at `277 M` parameters — NEVER placed beside a `40 M` number.**
- ⛔ **Dyn-eval is an ARM WE RUN, not a citation.**

## B5. `CM-C3-5` — run-3 exemption labelling
- **Run 3 is EXEMPT by PRE-REGISTRATION, not re-ruled:** a `preregistered_continuation` exemption,
  **fingerprint-verified bit-identical to run 2 except `erosion_partition`**, printed in the ledger.
- ⛔ **Run-3 results are NEVER quoted as budget-compliant ladder rows.**
- ⚠ Also record Track A's scope: **enwik8 primary · WT-103 secondary · PG-19 an INTERNAL retention
  instrument, ⛔ never an external comparison.**

---

# ⛔⛔ PART C — A HUB RULING YOU MUST EXECUTE IN THE SAME PASS: the duplicate N302 / N303

**Your own v2.16 pass flagged this and correctly refused to decide it. It is the Hub's call and it is
now ruled.** Verified on disk: `negative_results.md` contains **TWO `### N302`** and **TWO `### N303`**
— the **C2W11** pair (fold symbol **✰**: the launch-head bound; the store's exoneration) and the
**C2W10** pair (fold symbol **✯**: four specification/measurement defects; the prereg scorecard),
the latter **appended after** the C2W11 block.

## ⚖ RULING — renumber the **C2W10 (✯)** pair, not the C2W11 pair
**`N302 ✯ → N313` · `N303 ✯ → N314`.** Basis, measured rather than argued — live citation counts:

| document | N302 | N303 | N312 |
|---|---|---|---|
| `claims_matrix.md` | **24** | **12** | 14 |
| `handover_context.md` | 2 | 3 | 2 |
| `future_work.md` | 4 | 1 | 1 |
| `c3-handover.md` | 1 | — | **3** |
| ⭐ `advisor-head-c3-charter.md` | — | — | **1 (C3 Add.1 §4)** |

⇒ the C2W11 block is load-bearing across **five** live documents **including the C3 charter**, while the
C2W10 pair's only external pointer is primer 11.29. **Renumbering the C2W10 pair is the minimal-damage
resolution.**

**How:** move **content verbatim** — ⛔ change not one number, verdict or word of either entry — add a
dated line to each renumbered entry (*"renumbered N302 ✯ → N313 by Hub ruling 2026-08-13; a duplicate
of the C2W11 fold's N302 ✰; content unchanged"*), and keep both in their existing file positions.
⛔ **Do not renumber, reorder or touch the C2W11 ✰ block.**

## ⚠ C1 — AND CORRECT MY OWN OVER-CORRECTION IN THE PRIMER
In the C2W11 close fold **I struck primer record 11.29's citation *"registry N297 / N298 / N302 /
N303"*** on the reasoning that N302/N303 *"did not exist when filed and now resolve to entries on a
different subject."* ⛔ **The first half of that reasoning was WRONG:** they were a **forward reference**
to entries the C2W10 fold subsequently filed, and those entries **are** on-topic for that record.
⇒ **Restore the citation as `N297 / N298 / N313 / N314`**, and **replace my annotation** with a dated
one that states plainly: *the numbers were a forward reference that later collided with the C2W11
fold; the collision is resolved by the 2026-08-13 renumbering, and the original citation intent is
restored.* ⭐ **File the whole episode as a registry-hygiene negative** — *two curator passes on
adjacent waves allocated the same N-numbers because neither reserved its range before writing* — with
the standing fix: **a fold reserves its N-range in the registry header before it writes its first
entry.**

---

# Acceptance (Parts B + C, mechanical)
1. `claims_matrix.md` at **v2.17 … PROPOSED, PENDING HUB CONFIRMATION**, containing **§C3.1** and rows
   **`CM-C3-1` … `CM-C3-5`**, with **every C2 section and row (incl. §0.14 / CM-40 / CM-41) byte-unchanged.**
2. The strings **`7.988`**, **`FNN 7.89`**, **`0.758`**, **`daymet`**, **`1,966,080`**, **`277 M`** and
   **`preregistered_continuation`** all appear in the C3 division.
3. `negative_results.md` has **exactly one** `### N302` and **one** `### N303`; **`N313`/`N314` exist**
   carrying the C2W10 content verbatim with their dated renumbering lines.
4. Primer **11.29**'s citation reads **`N297 / N298 / N313 / N314`** with the corrected dated note.
5. A new registry entry files the **N-range-reservation** lesson.
6. Report to `.claude/outputs/` with the first-10-lines reconciliation list. ⛔ Zero code, zero git
   footprint, zero worktrees. ⛔ The curator **never self-confirms** a version.
