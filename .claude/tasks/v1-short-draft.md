# Task: v1-short-draft — draft the V1 short as an ML4PS position/theory paper (w8; Head-decided framing 2026-07-08)

- **Agent:** `paper-writer` · **Output:** report to `.claude/outputs/v1-short-draft.md`; draft under `.claude/papers/v1-short/`.
- **Read first (in order):** protocol · Positioning Charter (C-1 REVERSED — no audit ¶) · `.claude/claims_matrix.md` v1.3 (**your load-bearing rows: CM-2, CM-3 [forbidden], CM-7, CM-8 [cost final / accuracy PENDING], CM-12 [pillar-4 PASSED]**) · ledger Ch.4 + w6/w7 addenda · `.claude/negative_results.md` (V1 tier-A: N1/N2/N3/N24 + the intra-CLU-savings correction) · evidence: `paid-access-theory.md` + `paid-access-experiments.md` (the headline), `v1-pivot.md`, `v1-wormhole-routing.md`, `v1-router-baseline.md`, `v1-hopfield-stress.md`, `anchor-robustness.md` item 2, `regime-remap-2000ep.md` (Item 3 = the cost framing, FINAL).

## Head-decided framing (binding)
- **Venue-shape: ML4PS position/theory paper.** Physics-forward, mechanism-and-certificate led — NOT a benchmark paper. The thesis: *test-time compute on a conservative memory as PAID ACCESS — every mechanism that buys capability carries an explicit physical receipt (volume, energy ledger, latch transport), and the certificates are theorems, not empirics.*
- **LEAD with the physics win (pillar 4, CM-12 verbatim):** the reach/escape dichotomy (causal box C_T, energy-blind; squeeze cures escape and provably cannot beat the box — crossover bracket [L, L+p₀sinhζ/M₀]; intra-unit wormhole cures reach at det J=1 exact with ledger=0 and exact latch transport pᵀXΔ; beats the no-physics router which has no volume certificate, and the dense-V arm which fails reach; Newtonian control confirms the cap). Scope in-sentence: oracle placement.
- **Then the honest supporting pillars:** calibrated gate + LTT certificates on an escalatable memory (CM-2 — mechanism is memory-agnostic, say so; the CLU-conditional asset is escalatability, 4.8×@kv16 with accuracy gains); the 1-hop-edge mechanism (CM-7 salvage — flat 1.18e8 FLOP vs chain 1.76→2.94e8; the ROUTER result stated plainly as the boundary); the regime map with the corrected cost framing (CM-8: Hopfield cheaper at 1 matvec; the 9–10× is intra-CLU compute rationing — never "beats Hopfield on cost").
- **CM-8 ACCURACY = MARKED SLOT** (V3-§3.5 precedent): the under-trained-map finding + 3-cell closure is stated as preliminary with a `[pending: regime-remap-complete]` slot for the full-grid numbers — do NOT claim the accuracy reversal grid-wide until it lands (it is running concurrently; fold at revision).
- **Future work (explicit, per Head):** learned entrance-steering (the engineering crux — "we address this at scale in forthcoming work" [the ICLR long, unnamed per C-8]); γ-reabsorption timing; **an MCMC-certificate framing of retry acceptance is under theoretical development** — leave a one-sentence horizon hook + a `[optional slot: thread9-mh-kernel]` marker; if the theorist's note lands before the referee pass, a short "acceptance as a certified kernel" paragraph can be folded (physics-heavy, on-venue).
- **FORBIDDEN (CM-3):** any claim that energy is a better signal than learned alternatives — the router and margin results are stated as findings, the paper's value is the certificate stack.

## Mechanics
4–5 pp main + appendix-maximal (C-10): App = flag-provenance (C-7), the full router/regime honest tables, tier-A negatives from the registry (N1 squeeze-retry null WITH the §3.3 reach-vs-retry disambiguation — crucial: the paper must preempt "didn't your own retries fail?"), certificates table (paid-access-theory §4.1). CLU naming continuity sentence. Hermetic citations (J&P 2026 + theory note "(Anonymous, 2026)" + external published: HMC/basin-hopping/Lieb-Robinson cites per paid-access-theory §4.2 flags — cite precedent for the bound, claim the mechanism-design consequence). Placeholders for title (propose 3, position-paper register) + authors. Build with tectonic.

**Report:** evidence map (claim → source → CM-x), open editorial questions, missing-number flags. → `paper-referee` (w9 `v1-referee`).
