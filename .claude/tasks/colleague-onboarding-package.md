# Task: colleague-onboarding-package — make V2 reviewable by the HEP-theorist colleague (pre-w16, paper-writer)

- **Agent:** `paper-writer` · **Output:** TWO deliverables: `.claude/outputs/AI_for_physicists_primer.md` (durable) + `.claude/outputs/V2_colleague_cover.md` (this-send-specific) + a one-line status note in `.claude/outputs/colleague-onboarding-package.md`.
- **Read first:** `.claude/papers/v2-short/draft.md` (the paper the colleague will review — know exactly which ML concepts it uses) · `.claude/outputs/HEP_primers.md` (the INVERSE doc — physics-for-ML-experts; mirror its register, opposite direction) · `.claude/outputs/v2-symmetry-deepdive.md` (the rich HEP companion + its OPEN questions O1/O2/O6/O7, V4 seeds, running-decay-constant) · `.claude/papers/f5-note/f5-note.tex` (the rigorous companion) · `.claude/claims_matrix.md` (what's a settled claim vs a labeled negative) · Charter (authorship/anonymization: `[AUTHORS PLACEHOLDER]`, "CLU introduced as CHLU in Jawahar & Pierini 2026", third-person self-citation).
- **The reader:** a **particle-physics theorist with only basic AI knowledge.** He will read the physics (Goldstone/SSB/GMOR/ChPT/custodial/exceptional-points) *faster than we can* — do NOT explain physics to him. He needs the **ML/AI scaffolding** the draft assumes. The whole point is to unlock the draft for him and invite his theory contributions.

## Deliverable 1 — `AI_for_physicists_primer.md` (the inverse of HEP_primers; durable, reusable for any physicist collaborator)
Short, analogy-driven, physics-native framing throughout. Cover exactly the ML concepts the V2 draft leans on, each in ≈1 tight section with a physics analogy:
1. **What a recurrent network / "recurrent memory" is** — operationally: a state updated step-by-step by a learned map; "memory" = information about the past surviving in the state. The exploding/vanishing-gradient problem that gates (LSTM) exist to fight.
2. **The CLU primitive in his language** — a learned separable Hamiltonian `H=T(p)+V_θ(q)` evolved by a **symplectic (leapfrog/velocity-Verlet) step** with optional damping `p→(1−γ)p`. He knows this integrator; the novelty for him is *it's a neural-network layer whose `V_θ` is learned from data*. This framing is his fastest on-ramp — lead with it.
3. **What "trained" / "learned `V_θ`" means** — gradient descent on a data-fit loss; parameters = the discretized potential. And **wake–sleep / contrastive divergence** (the training objective): the energy-based-model view — push energy down on data, up on "hallucinations" — in stat-mech terms he'll recognize (it's close to a Boltzmann-machine learning rule).
4. **The ML baselines and why they're the foil** — LSTM, LEM, coRNN: what they are (gated / oscillator-based recurrences), why the paper compares against them (they are the *engineered* memory the physics-structured unit is contrasted with), and what "well-trained baseline" means (lr-swept, best-RMSE kept — a fair, not straw-man, comparison).
5. **The ML metrics & protocols** — RMSE (rad); the **autonomous-retention protocol** (write a phase, hold with zero input, count map-steps until drift exceeds a tolerance); "map-step" as the unit of memory lifetime; the per-step-compute normalization caveat.
6. **"Mo's law"** — the recently-published ML single-exponential lifetime law the paper is head-to-head with (its `λ̂`, its overdamped regime); why matching it in one band and correcting it past the crossover is the paper's headline.
7. **The ML-venue framing** — ML4PS/NeurReps workshops (non-archival, 4pp, the reviewer's concerns: novelty, baselines, honesty); and the program's **verification-vs-evidence labeling** discipline (designed-testbed = verification of exactness; learned-system = evidence).
Keep it to what the draft actually uses. No new physics. Where a term has an exact physics analogue, use it (e.g. "the potential's normal modes" for spectral masses — which he already gets).

## Deliverable 2 — `V2_colleague_cover.md` (the reading guide + the ask)
1. **Reading order:** (a) the AI primer (≈20 min), (b) the V2 draft PDF, (c) the deep-dive `v2-symmetry-deepdive.md` for depth, (d) the F5 note for the rigorous proofs. State what each is.
2. **What we're asking** — feedback / additions / amendments, and be **specific about where he can contribute most**: the deep-dive's OPEN questions (O1 the (μ,γ,T) crossover, O2 the curved-coset diffusion correction, O6 the underdamped RB bound, O7 the large-ζ companding failure), the **non-abelian V4 seeds** (SO(3)→SO(2), torus banks, custodial), and the **running-decay-constant** thread. These are genuine open theory a HEP theorist can move.
3. **Settled vs open** — point him at the claims matrix's approved claims (don't relitigate the verified numbers) vs the honest negatives (Appendix F) that are deliberately open. Tell him which results are "verification" (machine-precision, designed) vs "evidence" (trained models) so he calibrates his skepticism correctly.
4. **Housekeeping he should ignore** — the deep-dive contains program-internal references (the Head, the Hub, wave-numbers, `.claude/` task slugs). Tell him these are workflow bookkeeping; read the deep-dive for its physics (§§2–7bis). Assess whether the deep-dive needs any light de-internalization before sending, or whether this pointer suffices — **recommend, don't unilaterally edit the deep-dive.**
5. **Authorship / naming context** — placeholder authors (his potential co-authorship is the Head's call), CLU-introduced-as-CHLU continuity, the anonymization posture (why the theory note is cited as "Anonymous 2026").

## The package to send (state it in the cover note)
V2 draft PDF + F5 note PDF + `v2-symmetry-deepdive.md` + the AI primer + this cover note.

**Acceptance:** the AI primer covers exactly the draft's ML concepts, physics-native, analogy-driven, no new physics, reusable; the cover note gives reading order + specific asks (pointing at the real open questions) + settled-vs-open + housekeeping + authorship context; both are HEP-legible. Flag whether the deep-dive needs de-internalization. **Do not edit the V2 draft or the deep-dive** — these are companion docs only.
