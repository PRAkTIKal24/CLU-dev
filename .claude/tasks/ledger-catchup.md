# Task: ledger-catchup — close the w11/w12 ledger gap and the w11–w14 negatives gap (w15, curator)

- **Agent:** `doc-curator` · **Output:** `.claude/outputs/ledger-catchup.md`
- **Read first:** protocol (**§5 now carries the pre-registration rule and the reconciliation-list-owner corollary — both came out of your own w13 docs-debt list**) · your `.claude/outputs/transfer-docs-sync-w13.md` (items 2, 3, 5, 6 of the docs-debt list are this task) · `.claude/handover_context.md` §10 entries for w11, w12, w13, w14 · `.claude/claims_matrix.md` **v1.9**.
- **Head decision (2026-07-10):** the catch-up is approved — *"shouldn't be too hard given all agents write their individual output reports."* Use the reports as the primary source; the running log is the index.

## Item 1 — the missing ledger addenda (w11, w12) + w13, w14
`philosophy-synthesis.md` has **no ⟲ addendum for w11 or w12.** That science reached the running log and the matrix but never the ledger, which is the program's epistemic record and the debugging record for the ICLR/NMI-scale runs. Write the missing addenda, then w13 and w14.

Sources, per wave:
- **w11:** `v3-interference-extra` (MF-1 refuted; the "×2.56 vs ×2.18 parallel" was 3-seed noise; structural identity upgraded), `v1-certificate-payoff` (router det J = 0 **measured**; Payoff A/B + fine print), `v2-revision-3`, `transfer-docs-sync-w10`.
- **w12:** `f5-corrigendum` (the kinetic-isotropy clause was false; **the "2.6" retraction**), `f1-gmor-condensate` (GMOR proper, CM-15), `t-lever-forgetting` (CM-16; **and the "13.9×" prediction that turned out wrong**), `v1-revision-2`, `v3-revision-3`.
- **w13:** `v5-gate`, `xy-lattice-theory`, `fix-pack-5`, `v2-revision-4`, and `v2-symmetry-deepdive` v1.1 (§7bis).
- **w14:** `f5-corrigendum-2`, `relativistic-gibbs-expc`, `rb-bound-trained`, `causal-memory-floor`, `fix-pack-6`, `lattice-xy-prereqs`.

**Do not merely transcribe.** The ledger's job is the 7-part rubric — idea → theory → experiment → revision — and its addenda are the *diff record*. The chapter deltas that matter most:
- **Ch. 5 (mass / allocation):** "M is the budget allocator" went slogan → **conservation law** (`rb-bound-trained`: product invariant to 1.0386× vs prereg 1.0388×, 5/5 seeds) — *with the scope R5 never stated: it is a **saturated-write** law; below the causal cap the relativistic arm reproduces Newtonian `P ∝ 1/M`.*
- **Ch. 7 (instruments):** the deepest lesson of w13–w14. A tier-A negative (**N10**) was drawn from an instrument that was itself broken, and the sampler defect was found **after** the first sampler defect had been fixed. N10 nevertheless **survived** re-test on a correct instrument. Write this up as *instrument validity precedes inference* — it will happen again.
- **Ch. 3/4:** `causal-memory-floor` **falsified R7's floor as written** (332/787 adversarial cells) and returned the correct object (a Cartesian light-cone bound, never violated, exactly attained). A falsification-with-repair is the healthiest outcome the program has produced; record how the task was *written to invite it*.
- **A new epistemics entry:** three separate w14 agents **re-derived rather than trusted**, and two of them corrected the Hub's own claims matrix. Record the counterfactual — what would have shipped otherwise.

## Item 2 — the negatives registry, w11 → w14
Last swept at **w10**. Specifically: **N45's stated w11 resolution (`v1-certificate-payoff`) is unrecorded and its tier is still provisional** — it was due to discharge-or-promote at w11. Sweep w11–w14 for unregistered negatives. Known candidates beyond your N46–N51: `causal-memory-floor`'s **three** refutations (R7's floor as written; the analyst's own `γ_c/γ` refinement to R6; R3's transport law failing at 1% by `ζ₀ ≈ 0.19` on the trained ring, not "0.2% at ζ≤3"); `rb-bound-trained`'s residual-coefficient correction; `relativistic-gibbs-expc`'s **refutation of "raise `c`" as a fix** (it makes the imbalance *worse natively*: f(3589) 0.484→0.771 legacy, 0.599→0.786 fdt, on a potential trained at `c=5`); and the **`d=1`-table-quoted-at-`d=784`** error in matrix v1.8.

**N10 gets a special entry.** It was marked UNDER RE-TEST by you; it is now **UPHELD and strengthened** — the imbalance survives `c=5`, `m₀=25`, *and* an exactly-Gibbs MJ momentum law. Record that a negative which was challenged and re-closed is **stronger** than one never challenged, and that the challenge was worth running even though it confirmed the original.

## Item 3 — propagate the two new protocol rules
Protocol §5 now carries (a) the **pre-registration rule** for measured ratios/exponents/laws, and (b) the **reconciliation-list-owner** corollary. Both came from your w13 report. Reflect them in the ledger's methods chapter and in `HEP_primers.md` where the primers describe how the program validates a claim.

## Item 4 — the cosmetic flag you raised, and the slug offset
Resolve `v5-gate`'s §0 rounding (`1.1e-3 … 3.0e-3`) against its §3.2 table (`1.060e-3 … 2.963e-3`) — the table is what is transcribed everywhere, so make the docs use the table. Also: the `transfer-docs-sync-w<N>` slugs carry a wave offset (the w9 slug was the w8 pass). **This task is named for what it does, not a wave** — going forward, name curator tasks for the wave they *cover*.

**Acceptance:** w11/w12/w13/w14 ledger addenda exist and are diff-records, not transcriptions; negatives registry current through w14 with N45 discharged-or-promoted; N10's re-test recorded; both protocol rules propagated; docs-debt list returned. Nothing deleted (C-9).
