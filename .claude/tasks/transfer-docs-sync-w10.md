# Task: transfer-docs-sync-w10 — bring transfer docs current through waves 9–10 (doc-curator; standing)

- **Agent:** `doc-curator` · **Output:** `.claude/outputs/transfer-docs-sync-w10.md`
- **Read first:** protocol · your def (**now 4 docs — the new `future_work.md` is in your remit**) · `transfer-docs-sync-w8.md` (your w7/w8 pass) · handover §10 w9 + w10 review entries · the w9/w10 outputs: the three revisions (`v1-revision`, `v2-revision-2`, `v3-revision`/`v3-revision-2`), `v3-scaling-figure`, the three referee reports (`v1-referee`, `v2-referee-2`, `v3-referee`), `v3-banding-figure` · claims matrix **v1.5** (CM-9 UNDER RE-SCOPE, CM-4 misprediction canonical).

## Items
1. **`future_work.md` — first curator pass** (Hub seeded it wave-8; you now own its per-wave upkeep). Fold the NEW boundaries the referees surfaced: V3 block-structured-monolith control + trajectory-mediated interference channel + dynamical interference half-life + interference-at-N>8 (all now referee-named); V1 certificate-payoff-as-measured (was assumed, now a task); V2 width-matched per-step ratio. Verify the existing entries still read true; mark none `→SHOWN` yet (revisions are wording, not new science).
2. **HEP_primers through w10:** the referee-driven clarifications are mostly editorial, but two are physics-worth-noting — the CM-9 structural-vs-empirical-scaling distinction (interference is *coordination-bounded* O(1), NOT an empirical growth-rate claim — important honesty for the §9 interference block) and the CM-4 misprediction canonical (3.2× trained / 5× exact-map-only). Dated blockquotes.
3. **negative_results.md:** the "5×"-mislabel (process, traces to v2-full-runs L79 → tier C); the V3 two-point-slope framing risk (process/near-miss the referee caught, tier C — good "referee caught it pre-submission" provenance like N36); V1 "beats-router-is-definitional" (tier B until v1-certificate-payoff resolves it).
4. **Ledger w9+w10 addendum (⟲):** all three shorts drafted→refereed→revising; three weak-accepts; the shared critical-path (theory-note-must-go-live blocks all three §-provenance); CM-9 re-scope. Chapter deltas + scorecard + gap-list.
5. Report per-doc edits + contradictions.
