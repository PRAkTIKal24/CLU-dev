# Task: scout-cafe-hepa — map the CAFE benchmark + extract HEPA, for the CLU-in-CAFE ICLR anchor (web-scout)

- **Agent:** `web-scout` · **Output:** `.claude/outputs/scout-cafe-hepa.md`
- **Read first:** **`.claude/reference/CAFE_README.md`** (the CAFE readme — Hub pulled it 2026-07-20 from the private `Forgis-Labs/CAFE` repo; it is the authoritative benchmark spec) · the handover's 2026-07-20 CAFE-pivot entry · the Forgis HEPA paper (public — arXiv:2605.07675, C. Mazzoleni et al., builds on voraus-AD/AURSAD/FactoryWave).
- **Why:** the ICLR systems paper is pivoting to anchor on **CAFE (Classification / Anomaly Detection / Event Prediction)** — the Forgis benchmark — with CLU as the "larger companion to HEPA" in the special case. We have the CAFE readme; this task turns it into an actionable integration + positioning map.

## Deliver
1. **The CAFE benchmark map** (from the readme + the `cafe-bench` repo structure — you may `git clone git@github.com:Forgis-Labs/CAFE.git` if creds are on the machine; the Hub confirmed access): per task (Classification/Anomaly/Event-Prediction) — the datasets, the primary metric (Macro-F1 / VUS-PR / **h-AUROC**), which datasets are **public + auto-downloadable** (UCR/UEA/TSB-AD-M/NAB/C-MAPSS FD001–004/PhysioNet) vs gated, and the current leaderboard numbers where filled (esp. HEPA's Event-Prediction: FD001 0.918, FD002 0.661, FD003 0.960, FD004 0.627).
2. **The `Model.encode()` registration interface** — exactly what a model must implement to register in `cafe-bench` (`raw data → Dataset.load() → Model.encode() → Evaluator`). This is what the engineer will implement for CLU. Extract the base class / required methods / the encode→evaluator contract (embeddings? task-head-appended? kNN/CoxPH default heads?). **This is the single most important deliverable for the engineer.**
3. **HEPA extraction** (the model CLU is positioned against and "companions"): architecture, params (~2.16M per the sample script), how it `encode`s, its training objective, and *why it leads Event-Prediction* on C-MAPSS — the mechanism. We want to know exactly what CLU must match/beat and where CLU's basin-exit/stability structure could differ.
4. **The parametric peers:** MOMENT and UniTS (time-series foundation models) — what they are, param scale, how they plug into CAFE — since these are CLU's real weight-class comparison (not kNN/CoxPH). And the EBM/specialized baselines already in CAFE: **DeepSVDD, LSTM-AE** (anomaly), **DeepHit, CoxPH** (event) — one-line each so we know the field.
5. **Positioning verdict:** where is CLU's most-differentiated, most-winnable CAFE entry? (Hub's hypothesis: **Event-Prediction / fault-onset** = basin-exit/instability, closest to HEPA; Anomaly = valley/EBM locality; Classification = needs global-structure mechanisms, hardest.) Confirm or revise from the actual benchmark shapes + metrics.

6. **⚠ Settle: is `voraus-AD` a CAFE dataset?** The Head believes CAFE includes it; the saved `CAFE_README.md` does not mention it (`grep -i 'voraus|aursad|factorywave'` → nothing); a prior Hub entry asserts it is NOT a CAFE set. **Answer from the actual `Forgis-Labs/CAFE` clone (the dataset registry / loader list), not the README** — the README may be stale or partial. This decides whether our existing voraus work is *covered* by the CAFE anomaly track or *orphaned*. Same check for AURSAD/FactoryWave (the other HEPA-lineage sets).

**Discipline:** verify from the readme/repo/paper, not memory. Quote the `Model` interface verbatim. Flag any dataset that is NOT freely accessible (gates ICLR reproducibility). If `cafe-bench` clone fails, work from `.claude/reference/CAFE_README.md` and say what's missing.
