# bprime-referee-closures — the CLU n=9 column + figure renders (referee missing-experiments 1 & 7)

**Campaign 2, wave C2W5 (Head-funded at the wave review — spawn only on the Head's line). Agent:**
experiment-engineer (or results-analyst; plotting scope). **Small worktree** (the audit rig is in
`main`). Branch `bprime-referee-closures`. Writes `.claude/outputs/bprime-referee-closures.md` +
figures under `.claude/papers/bprime/figures/`.

**Read first:** `.claude/outputs/bprime-referee.md` **§6 items 1 + 7, MF-1, SF-8**;
`.claude/outputs/pilot-placement-probe.md` §1.1 (rider 1's aggregation pattern — reuse it);
`bprime-rivals-f3.md` (the seed protocol you extend); `draft-r2.md` App K (the five figure specs).

## 1. The CLU n=9 column (referee missing-experiment 1 — "cheap; highest-leverage")
Extend the CLU arm to seeds 3–8 under the F3 protocol (same code path, same aggregation as rider 1;
the rival arms took ≈ 31 min for the full F3 pass — this is one arm). Deliver the full CLU column
set at uniform n=9: full · launder · dividend · +0 B margin · raw-table margin · blank · same-keys
null · rescue-gate verdict (A17.4 power rule applies — that is the point of going to n=9). ⛔ The
n=3 "✅ rescued" is a never-quote (Hub ruling at the review); your n=9 verdict replaces it,
whatever it says. This closes the paper's visible n-asymmetry and MF-1's clean upgrade.

## 2. Figure renders (SF-8; Fig 1 renderable now, gate closed)
Render the five App-K specs from the banked artifacts (matplotlib, repo plotting utils; ⛔ read the
dataviz skill's guidance if styling choices arise). No number invented: every plotted value traces
to a named JSON. Figure files + a provenance table (figure → artifact → field) in your report.

## Acceptance
CLU n=9 column JSON + table (aggregation byte-identical to rider 1's rule) · 5 figures + provenance
table · tests green if you touch any code (prefer none) · declared NOT-RUNs never nulls.
**Ownership:** figure files + your aggregation script under `.claude/scratch/`; ⛔ no `chlu/` edits
expected — flag the Hub if one becomes necessary. **Git:** worktree only if code is touched; never
push `origin`. Report → Hub, spawn nothing.
