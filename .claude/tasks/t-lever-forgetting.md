# Task: t-lever-forgetting — is TEMPERATURE the right forgetting lever (and does "Forgetting" warrant its own V5 short)? (deep-dive F-5/F-6; Head-approved decider)

- **Agent:** `results-analyst` · **Output:** `.claude/outputs/t-lever-forgetting.md` (+ figures/npz)
- **Read first:** protocol · **`.claude/outputs/v2-symmetry-deepdive.md` §5 (the T>0 coset-diffusion law) + S8 + F-5 + F-6** (the derived + toy-verified prediction: D_θ = εT(2−γ)/(2M_ch r*²γ); latch n₁/₂ ∝ F²γ_c/T; **∂n/∂γ > 0 — more friction ⇒ LONGER memory, the opposite sign to massive-mode forgetting**) · F5 Cor-13 (friction *latches* flat content, cannot erase it) · `gamma-field-build.md` + `fit-gap-anatomy.md` N12/N13 (the γ_φ −24% "wrong tool" negative this would EXPLAIN) · the trained latched SO(2) checkpoints (v2-full-runs designed150 / anchored3000) + the existing Langevin rollout (`stochastic_rollout`, FDT `langevin_noise="fdt"`).
- **Why (Head decision-gating):** the deep-dive proves (toy) that friction cannot erase flat-direction memory but temperature can. **This check decides scope:** if it lands clean+rich on TRAINED models, "Forgetting" is a candidate **V5 short** (sibling to V2 "Memory"); if it's a one-figure result, it's a V2 appendix. Repo read-only for Part 1.

## Part 1 — the decider (analyst, cheap; on a trained latched SO(2) checkpoint)
1. **The sign-flip result:** sweep global temperature T and friction γ on a latched designed-SO(2) checkpoint (Langevin, FDT noise). Measure coset-value retention n₁/₂. Confirm the three predictions: (a) the latch **survives any γ at T=0** (Cor-13); (b) under T>0 the coset **diffuses/decays** with n₁/₂ ∝ **F²γ_c/T** (D_θ law); (c) **∂n₁/₂/∂γ > 0** — more friction gives LONGER memory (opposite sign to the massive-mode budget-table forgetting). This is the headline: *temperature is the forgetting lever for flat-direction memory; friction is not (and fights it).*
2. **Retro-explain the negative:** connect to `fit-gap-anatomy`'s γ_φ −24% (N12/N13) — friction couldn't recover fit because friction *can't delete latched content at all*. State it as the first-principles explanation.
3. **Contrast with massive-mode forgetting:** show that for a MASSIVE (curved) mode, more γ = faster forgetting (the existing budget table), while for the FLAT mode it's the reverse — the two regimes have opposite γ-dependence. This is the conceptual core of a "Forgetting" story.

## Part 2 — the mechanism (ENGINEER SPEC only; conditional on Part 1 + Head V5 call)
4. Spec (do NOT build): a **localized temperature field T_φ(q)** ("hot trash region", Thread-1's original Hawking framing) — the T-analogue of the existing γ_φ(q) machinery — that deletes latched coset content *locally* where a γ_φ(q) hole provably cannot. Give the engineer the exact change (a `temperature_field` mirroring `friction_field` in the Langevin step; erasure rate ∝ T_φ/(γ_c F²)) so it's ready if the Head greenlights V5.

## Deliverable — the scope recommendation
State plainly: **V5-worthy** (the sign-flip + T-law + the γ_φ-negative explanation + the T_φ mechanism = a coherent "physics of forgetting" short, sibling to V2) **OR V2-appendix** (a clean but single result). Give the honest read. Flag-provenance per §5 (checkpoint, T/γ grids, FDT flag). Negatives (if the T-law doesn't hold on trained models, or the sign-flip is muddy) fully written per C-9.
