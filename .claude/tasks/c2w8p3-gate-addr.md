# c2w8p3-gate-addr — G-ADDR: give the gate a leg that can FAIL on the thing that matters

**Campaign 2, wave C2W8 PASS 3. Agent:** experiment-engineer. **SPAWNS FIRST — nothing else is scored
before your deliverable exists** (charter §A30.1, binding order).
**Worktree 1 of ≤3** (the cap is back in force). Branch **`c2w8p3-gate-addr`** from `main @ 1eda6a0`,
worktree `../CHLU-c2w8p3a`. ⚠ **Name the base explicitly** — the shared checkout sits on another
spoke's branch: `git worktree add ../CHLU-c2w8p3a -b c2w8p3-gate-addr main`.
Writes `.claude/outputs/c2w8p3-gate-addr.md` + artifacts to `.claude/outputs/c2w8p3-gate-addr/`.
**Budget:** ≈ 1 day. No new science cells — this is instrument work, plus one compliance
counterfactual.

**Binding documents, read first, in this order:**
- `.claude/outputs/c2w8-well-lifecycle/PREREG-C2W8-PASS3.md` — **§2 (G-ADDR, yours), §4 (the
  scale-invariance guard), §7 (Ruling 3's counterfactual, yours), §8 predictions Q1/Q2/Q7/Q8. You
  implement these; you do not re-derive or re-tune them.** Additions go in a dated
  `ERRATA-C2W8-PASS3.md` filed **BEFORE** the cells they govern.
- charter **ADDENDUM 10 §A29–§A30 IN FULL** (§A30.1 is your mandate; §A30.3 is your §4 item).
- `.claude/outputs/c2w8p2-{compact-atoms,emission-head}.md` + raw `capture_armA.json` + pass 1's
  `census.json` · intervention **§5/§8**.

## ⭐ DIAL DECLARATION (protocol §7, C2 form) — echo before your first result
- **Dial:** none — **instrument construction + a compliance counterfactual.** ⛔ No claim cell, no
  performance number, no tier-ii/full-CLU/I2 verdict, **and no arm-race adjudication** (§A30.1: the
  race is VOID as a comparison and stays unadjudicated).
- **Laundering control:** G-ADDR's own A3 leg IS a launder margin; state **matched-items vs
  matched-bytes explicitly on every quotation**, and the **1 253×** byte-ratio caveat travels.
- **Falsifies:** a G-ADDR that cannot fail its own designed negatives does not ship. **Ruling 3's
  counterfactual failing REVERSES the `atom_site_local_init` compliance ruling — escalate, do not ship.**
- ⛔ Depth is not feature importance (§A23.5 ACTIVE). ⛔ N94 discipline on any reading.

---

## ⛔⛔ WHY YOU EXIST — the defect class, three instances in one wave

pass 1's **vacuous `M`** (geometric leg ~10× the address resolution ⇒ refused nothing) · monitor #3's
**never-refusing admission gate** (refusal rate **0.000**, still open) · pass 2's **gate blind to
addressability**. The third was caught **by its own spoke, against its own interest**: arm B passed
**all three legs, 3/3 seeds**, with **`n_never_read = 16/16`** and launder margin **−0.594**.

> **A GATE THAT CANNOT FAIL ON THE THING THAT MATTERS.**

**The measured fact you are building the instrument for** (Hub-re-derived from arm A's raw cells):
reads landing in **no basin 58 / 62 / 62 of 64 — DIGIT-IDENTICAL to pass 1** — while every capture
metric moved dramatically. **The wells are real; queries do not reach them.**

## 1 — G-ADDR (prereg §2). Three sub-legs, all TWO-SIDED, each with a designed negative, pytest-asserted

- **A1 — correct-basin rate:** reads landing in the basin of **the QUERIED item**, not merely in
  *some* basin. ⛔ The pass-2 instrument only ever asked "any basin" — that is precisely how 58/64
  unassigned coexisted with a passing gate. **A1 is the leg the wave was missing.**
- **A2 — never-read fraction.**
- **A3 — margin vs the kNN-in-φ launder at the DECLARED matching.** ⛔ Every quotation states
  **matched-items vs matched-bytes**.

**Designed negatives, minimum (both mandatory, both pytest-asserted):**
1. ⭐ **Arm B's banked configuration MUST FAIL G-ADDR.** It is the measured blind spot, already banked
   and free to use. **A G-ADDR that passes arm B is not a gate** — this is the single most important
   assertion in your task.
2. **Planted-permutation control:** queries deliberately mapped to wrong sites ⇒ A1 ≈ 0.

⚠ **Also re-score pass-2 arm A on G-ADDR** and report it (registered prediction **Q2: A1 = 0.03–0.12**).
That is a *measurement of the banked arm*, **not** an adjudication of the race.

## 2 — The scale-invariance guard (prereg §4) — the anti-trivial-pass leg

The rig sets `scale = 1 / r95(‖φ(pool)‖)` (addresses normalised to unit radius) while **σ_q = 0.15 is
absolute** ⇒ **σ_q / spacing is movable by rescaling φ alone, with zero information gain.**
- **Report every geometric quantity as a DIMENSIONLESS RATIO with the scale stated.**
- **Build the SCALE-ONLY control:** identical φ, scale × a declared constant ⇒ **G-ADDR must move by
  ≈0** (registered **Q8 = 0.90**). ⛔ **If rescaling moves G-ADDR, the leg measures the scale and not
  the memory, and it does not ship.**

## 3 — Deliverable, and it is the wave's mechanical precondition

> **`.claude/outputs/c2w8p3-gate-addr/GATE-ADDR-VALIDATED.json`**

It must carry: the three legs' definitions and thresholds · **both designed negatives' measured
results** · the scale-only control's result · arm A's and arm B's re-scored G-ADDR · and a top-level
boolean **`gate_addr_validated`** computed **mechanically**. ⛔ **Every later pass-3 spoke is gated on
this file existing with `gate_addr_validated == true`.** If it is false, say so and hand back — a
gate that cannot fail is the thing this pass exists to stop shipping.

## 4 — RULING 3's COUNTERFACTUAL: outcome, not identity (prereg §7) — placed here deliberately

`atom_site_local_init` is COMPLIANT **conditional on** this check, and it sits in the FIRST spoke
because it is a validity precondition on **arm A's entire pass-2 result** — burying it in the spine
would place it after the work it can invalidate.
**Show the attractor CAN move off the stored key when the write objective demands it:** a
write-objective term preferring a **displaced minimum** on a designed cell ⇒ the settled point
follows it off the key by a **measurable margin**, reported with its magnitude.
⛔ **If the attractor provably CANNOT move, capture was bought by algebraic pinning, the compliance
ruling REVERSES (§A30.3), and you STOP AND ESCALATE TO THE HUB — do not ship, do not work around it.**

## 5 — Census housekeeping you now own (`well_lifecycle.py` was read-only to pass-2 arms; **it is not read-only to you**)

1. ⛔ **`own_foreign_site_depth` hard-codes the Gaussian kernel** ⇒ kernel-mismatched under any
   compact-atom arm (arm A's own/foreign was reported *through* it, labelled). **Fix it, with a
   designed CROSS-KERNEL test** proving it now reads correctly under both kernels.
2. **K7's two instrument properties belong in the census's own documentation:** the
   **`tol / expansion_rate` positive floor** (a barely-moving site reads positive **with no basin**)
   and the **confinement-minimum FALSE POSITIVE** (≈`r_hi` with no atom depth). They are pytest-pinned
   in the arms' files; the census must document them itself.
3. **`theta_att`'s dynamic range is arm-dependent** (degenerates to **0.0000** when everything
   captures) ⇒ **any cross-arm `P` comparison must say so.** Record it in the instrument's docs.

## FILE OWNERSHIP (declared)

**You own:** `chlu/core/well_lifecycle.py` (**yours now — items 1–3 + G-ADDR**) ·
`chlu/experiments/exp_well_lifecycle.py` (G-ADDR wiring only) · `tests/test_gate_addr.py` (**new**) ·
`tests/test_well_lifecycle.py` (additive) · `chlu/config.py` (**additive only**).
⛔ **DO NOT TOUCH:** `chlu/core/emission_head.py` · `chlu/core/memory_potentials.py` (pass-2 arms' —
read them, do not edit) · **the live `pilot-ttt-nan-and-d5-wiring` spoke's territory:
`scripts/csf3/`, `chlu/training/train_cluformer.py`, `chlu/core/blocks.py`,
`chlu/experiments/exp_cluformer_pilot.py`** · `chlu/experiments/{exp_cl_entry,phi_encoders,exp_phi_read_in}.py`
(**wt2's this pass**) · the C2W6/C2W7 files.
⚠ Work **in your worktree**, never the shared checkout — it is occupied by a live spoke on its own branch.

## Acceptance (mechanical)
1. `GATE-ADDR-VALIDATED.json` exists with a **mechanically computed** `gate_addr_validated`.
2. **Both designed negatives measured and green** — arm B's config FAILS; planted-permutation ≈ 0.
3. Scale-only control reported; G-ADDR moves ≈0 under it.
4. Ruling-3 counterfactual reported with its margin — **or escalated**.
5. Housekeeping items 1–3 landed, item 1 with its cross-kernel test.
6. Full suite green on your branch, count arithmetic stated. ⚠ **Base count is checkout-dependent:
   state the checkout with the number** (the directive's 1445 is the shared checkout; a fresh
   worktree collects fewer — three modules use `importorskip`/`skipif` and lack cached datasets).
7. Reconciliation list in the **first 10 lines**; NOT-RUNs declared as NOT-RUNs, never nulls.

⛔ You do NOT build merge/prune/restoration or any §2.7 claim cell. ⛔ You do NOT adjudicate the arm
race. ⛔ Never push `origin`; the Hub integrates and pushes `clu-dev`.
