# doc-curator-c2w5-fold — the C2W5 fold (a paper wave's registries never lag its drafts)

**Campaign 2, wave C2W5. Agent:** doc-curator. **NO WORKTREE.** Writes per each transfer doc's own
protocol + `.claude/outputs/doc-curator-c2w5-fold.md` (audit trail). Runs ALONGSIDE
`bprime-draft-r4` (same evidence base; r4 quotes registries).

**Read first:** the `[C2W5]` review + second-review §10 entries (the Hub rulings are the primary
source); the five second-batch reports (`orgdiv-null-arms` · `bprime-mamba2-arm` ·
`bprime-referee-closures` · `plan-pass-vectorise` · `bprime-draft-r3`, each's "Proposed handover
updates" + reconciliation lists); `orgdiv-cat-test.md` + `pilot-placement-probe.md` (first batch —
their registry candidates were deferred to this pass).

## The fold (verify-then-delta where the top of the entry already exists)
1. **N-entries for the wave's results** (tiers proposed, Hub confirms at the close): the K5
   vacuous kill + its quotable form ("physics, untuned and collapsed, does not clear its own K5") ·
   the settle-destroys-launch-information mechanism (0.406 → 0.297; occupancy scored against the
   blank/launch baseline, never `F/N_a`) · ⭐ **the read-protocol refutation** (P=4 occupancy read:
   2.20 distinct wells, ≥F on 5.0 %, exact-set 0.0000/2560; the settle LOWERS it to 1.70; ceiling
   0.272 decodable from the same launches) · N1's memorise-perfectly-compose-zero (1.0000 train /
   0.0000 held-out, 5× bytes) · `null*` computed for the first time (0.00117, 584 configs) · the
   CLU NOT RESCUED at n=9 (−0.0465 ± 0.0406) · the Mamba-2 row (0-of-6; frontier labelled null) ·
   the TTT-below-own-null finding (r3 reconciliation 2 — homeless, file it) · H1-harmful/H2-inert
   (probe; N111's third harness) · the ψ payload-compression mechanism (30–50 % delivered, 7–25×
   compressed) · R3 well-destruction (depth → 1e-63).
2. **Never-quote / form updates (§0.8 + CM-28):** ⛔ the CLU "✅ rescued" at n=3 (ruled at the first
   review; fold now) · ⛔ "0 of 5" (superseded: 0 of 6) · ⛔ gdn "+1.02" / gdn2 "+0.88" n=3 dividends
   (retired by the n=9 fold: +0.5960/+0.6824; r3 reconciliation 1) · ⛔ a single TTT or CLU byte
   figure unlabelled (modal-value rule; R2/R4) · ⛔ **"no matched-capacity organizer clears" never
   without the 0.272 ceiling in the same sentence** (null-arms risk 4, Hub-ruled standing) · ⛔
   "the family is refuted" — the refuted object is **the P=4 occupancy read protocol** (null-arms
   reconciliation 3, Hub-ruled) · ⛔ rescue verdicts quoted where selection-dependent (deltanet,
   gdn2 — the third-path ruling) · ⚠ cat-test F5-does-not-fire carries the imitability caveat
   (0.89 imitable vs 0.26 read-objective-recoverable — an optimisation gap, not a non-VQ channel).
3. **Doctrine/practice candidates (the ⟲ ledger):** *build the kill-condition's PRE-condition too* —
   **K0** (`P(≥F distinct wells)` computable from launch geometry alone, seconds, before any store)
   joins K1–K5 for any future compositional cell · *an organizer audit needs an in-class fit
   anchor* (L1) · *quote the decodability ceiling beside every null-arm null* · *a seen-validation
   split inherits the family's own held-out rule* · the A17.4 extension to SELECTION rules (verdicts
   quoted only where stable across registered selections).
4. **Corrections with existing homes:** N196/future_work — the plan-pass story completes (jit done
   21.7×; lane-parallel done 4.93×; the probe's <30 %-any-bracket claim corrected, banner on the
   report) · `orgdiv-cat-test` §13 Q1 CLOSED (2.20 launch / 1.70 settled) · FROZEN-interfaces
   banner noted (Hub-discharged) · `PREREG-TierII.md` errata (NOT edited — dated erratum file
   beside it, the PREREG-Bprime pattern): rule-4's m=1 payload assertion (0.5 %), the K1 a≥12
   prediction (refuted), the §5.1 ratio spelling (d=4,m=1 only), §4.3's null* now computed, §3.5's
   F5 imitability caveat.
5. **HEP_primers:** likely nil again — say so explicitly if so.

## HEAD RIDERS (added at spawn approval, 2026-08-01 — charter ADDENDUM 5 §A20 is now FILED and is
your primary source for items 1–3 below; read §A20 in full first)
1. ⛔ **The mandatory re-labelling is §A20.2 verbatim and applies EVERYWHERE** (registries, ledger,
   primers, matrix): *"the P-particle occupancy read protocol is refuted at P = 4"* — NEVER "the
   compositional family is refuted". Sweep for the family-refutation form the way the C-1 reversal
   was swept (and remember N210: sweep the filesystem, not the document).
2. ⛔ **The 0.272 ceiling is always labelled "reference ceiling from an out-of-class decoder"** —
   what the launch information supports, never an arm's bar (the arm bar stays chance + 0.05,
   reader class ≤ N_a·m).
3. **The effective-`s` ruler erratum (§A20.5):** the estimator must subtract `α‖q‖²` (1.44×
   inflation measured); `bprime-c6`'s `s = 0.40` is FLAGGED FOR A CHECK, not refuted — file the
   erratum with both numbers (0.438 unsubtracted / 0.304 subtracted on the cat-test store; 0.318
   its own measured value) and the §7.28 pointer.
4. **Record the FROZEN-interfaces erratum** once the `psi-payload-residual` engineer's rider
   corrects the two rows in place (the Hub's dated banner is the audit trail; your record points at
   both).
5. **The shorts re-pass checklist gains §A20.5's item:** every short states its substrate scope
   ("these laws govern the store; end-to-end performance additionally depends on the encoder,
   measured separately, φ-bytes ledgered") — file it wherever the A18.7 re-pass checklist lives.

## Acceptance
Per-item VERIFIED/FOLDED audit trail; matrix version bump proposed (not self-confirmed); no body
edits to published reports (C-3); task files untouched (C-4). Report → Hub.
