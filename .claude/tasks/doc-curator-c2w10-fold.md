# doc-curator-c2w10-fold — the C2W10 fold (registries, the scout's two verified exclusions, the caveat status)

**Campaign 2, C2W10 ("The persistent store"). Agent:** doc-curator. **ZERO worktrees.**
Writes `.claude/outputs/doc-curator-c2w10-fold.md`; edits the transfer/registry documents per their
own protocols. **Budget:** ≈ half a day. **Spawns at the wave close**, after the Hub's review entry.

⚠ **Deconfliction:** a concurrent C2W8 close-out and the C2W11 wave are live. **Fold only the items
listed below.** If you find an item that plainly belongs to C2W8's or C2W11's fold, **list it in your
reconciliation section and leave it** — do not adopt another wave's fold.

**Binding documents:** `.claude/outputs/c2w10-persistent-store/PREREG-C2W10.md` · the `[C2W10]` §10
entries in `.claude/handover_context.md` · `.claude/outputs/c2w10-*` (whichever have landed) ·
charter **ADDENDUM 12 §A34.3**, **§A20.6**, **Add.7 §A23.5**, **Add.9 §A27.1**, **Add.11 §A31**.

## ⭐ DIAL DECLARATION (protocol §7)
**Dial:** none — documentation/registry maintenance. **Laundering control:** N/A.
**Falsifies:** an entry that cannot be sourced to a landed artifact is not filed.
⛔ You add no new science and you re-score nothing.

## ⛔⛔ SCOPE UPDATE, 2026-08-11 — the wave closed as a MECHANICS wave; fold what it actually produced
Binding additions: charter **ADDENDUM 14 §A39–§A41** (Head-ratified) and **`PREREG-C2W10.md`
AMENDMENTS 3–7**, which contain every number and every never-quote below in their landed form.
**A. The two admissibility findings → `negative_results.md`, and they ROUTE TO B′ as protocol evidence
(FB4's class).** ⭐ Quotable form, verbatim: *"at laptop byte budgets an exemplar store at matched bytes
sits at or above the strong-baseline frontier on both of the best-documented real streaming venues —
and on one of them, destroying temporal order does not hurt it."*
- **INSECTS (measured):** SAM-kNN L=5000 at **0.634 MiB = 76.92 %** vs ARF-100 **78.81 %** ⇒
  `b2_margin_pts = −1.8983`, `criterion4_cleared = false`, **firing against every ARF reference tried**
  (−0.2143 vs the published 77.13 · −0.4972 vs ARF-10).
- **Metro (measured):** k-NN over past windows at **0.634 MiB, MAE 314.58**, **beating every strong
  baseline** incl. tuned GBDT **335.20** (at 3.6 MB of state), firing against **all 8** references.
- ⇒ **criterion 4 now has SIX confirmations** (MAD/zoology/MQAR · three of four gym families via FB4 ·
  ELEC2 + Covertype + Poker-hand · the SAM-kNN real-stream family · INSECTS · Metro). ⭐⭐ **FRAME IT
  AS RULED: a FALLBACK BEING RETIRED, NOT A VENUE CRISIS** — Track-2's primary (bounded-state sequence
  modelling) is live on CSF3 and the tier-ii compositional family is live in C2W11.
**B. The 24-h embargo finding — file as independently useful BENCHMARKING METHODOLOGY**, not merely as
a wave detail: plain prequential at a 24 h horizon **leaks up to 23 h of future traffic** to any
continuously-updated learner (**+10.9 %** to a 250-exemplar k-NN, **−0.3 %** to GBDT). ⚠ The leak is
**asymmetric in the direction of firing** — the analyst closed a leak biased toward its own tripwire.
**And:** seasonal-naive(`t−24 h`) is **DEGENERATE** at a 24 h horizon (bit-identical to persistence);
the non-degenerate naive is **`t−168 h`**.
**C. The two Advisor errata (Add.14 §A40), both caught by this wave's spokes:**
(a) **the I1 misattribution** — `events 27/40/70, rates 0.593/0.050/0.043` are the **`p1_off` GUARD-OFF
BASELINE**, not the I1 arm; **the I1 arm is 44/62/59 events with 6/0/0 pre-guard violations**;
**charter §A22 is CORRECT AS PRINTED** (the error was in the relay and propagated into
`PREREG-C2W10.md` §4 L5-b and the task file — **both already corrected in place, dated, append-only;
verify and record, do not re-edit**). ⭐ **Bonus to record:** §A23.2's *"zero post-guard violations"*
was **never in the artifact** (`n_rewrite_violations_post_guard` is `null` in all three C2W6 files);
recomputed from raw as **0/0/0** ⇒ **that charter claim has evidence for the first time.**
(b) **the I2 deferral was defective** — the **count** was checked, the **channel** was not: this rig
runs **no outer objective and no optimizer step on store parameters**, so the Head's mechanism
(gradient magnitude ∝ contribution) **cannot act here at all** (live-well count was adequate, 57–60
scored). ⇒ **I2-c's mechanism is a DECLARED NOT-RUN at this rig, not a null**, and **the mechanism leg
RE-HOMES to a rig with a live outer objective (C2W11's organizer, or the block)**. ⛔ **§A23.5 stays
ACTIVE and TRAVELS until it is measured there.**
**D. Never-quotes (fold all of these):** ⛔ `ρ(U, depth_raw) = +0.18/+0.22/+0.19` is **NOT positive
evidence** (below the detection floor 0.272/0.265/0.272 **and** confounded with age,
`ρ(E_raw, age) = +0.74/+0.94/+0.85`) · ⛔ INSECTS' band **`b = 4`** is contaminated ⇒ `R(4)`/`A(4)`
uninterpretable · ⛔ *"ARF is the reference"* without its byte caveat (**14.35×** SAM-kNN) · ⛔ the
scout's §5 *"river ships SAM-kNN"* is **WRONG** (a ported reference implementation was required; every
cost estimate citing *"one-line baseline"* is **void**) · ⛔ **`out-of-control` has NO DATA SOURCE**
⇒ V3 is a declared NOT-RUN · ⚠ **I2-d as registered tests WRITE-TIME depth, not live depth**
(netting makes depth time-invariant here, `|last/first − 1| ≈ 4.5e-7`).
**E. ⛔ THE VALUE SURFACE IS A DECLARED NOT-RUN FOR WANT OF AN ADMISSIBLE VENUE — never a null, in any
artifact, ever.** Sweep for any sentence that reads as a null and fix it.
**F. ⭐ HAND FORWARD (the C2W5→C2W7 pattern): the persistent-store VALUE question is NOT dead — it
moves to COMPOSITIONAL territory.** If C2W11's tier-ii claim lands, *"does a persistent store help
across compositional streams"* is the right question, **on a venue where a table does not win by
theorem.** Record it as a design input, not as a leftover.

**G. ⭐⭐ A PROPOSED NEW STANDING INSTRUMENT RULE (Hub-proposed, ADVISOR-FACING — file it as PROPOSED,
not as adopted).** The twin of C2W6's *"ICC ≤ 0 ⇒ UNDEFINED"*:
> **Reliability is necessary but not sufficient.** `E_netted`'s split-half reliability was **0.78–0.89**
> while the quantity itself was **≈4 ULP of float32 round-off** (netted log-drop **4e-7 nats** vs
> **0.58–0.62 nats** raw, ratio **6.8e-7**). ⇒ every erosion/usage proxy carries **two** checks:
> **(a) reliability** and **(b) magnitude against the designed channel**; a proxy at **≤1e-6 of the
> designed channel is UNDEFINED regardless of its reliability.**
Attach the two consequences: **any future undesigned-erosion cell runs store amplitudes in float64**
(at float32 the instrument's noise floor is above any effect it could see), and **§5's detection floor
is 0.272 / 0.265 / 0.272**, not the registered "≈ 0.25" (`n_live = 64` is transient; the scored
populations are 57 / 60 / 57).

## The fold (items, in order)

1. **The scout's two VERIFIED exclusions → `.claude/negative_results.md`** (they are independent of
   any C2W10 cell and should be banked whatever this wave returns):
   - **(N-new-a) Electricity / ELEC2 is inadmissible under intervention §6.2** — the naive persistence
     classifier scores **85.3 %** and beats 10 of 12 MOA adaptive classifiers and 12 of 17 published
     results (Žliobaitė 2013, arXiv:1301.3524; **independently corroborated** by Souza et al. 2020
     §4.2: **85.33 %** vs NB-DDM 81.23 %). The same source also excludes **Forest Covertype**
     (No-Change **95.07**) and **MOA-Poker-hand** (**74.51**, with a temporal dependence absent from
     the UCI original).
   - **(N-new-b) The Losing/SAM-kNN real-stream family (NOAA-Weather, Rialto, Outdoor, Cover Type)
     fails criterion 4** — a plain 5 000-example windowed kNN is 2nd of 6 by real-world average rank
     (2.33) and an explicit dual exemplar store is 1st (2.00) (Losing et al., ICDM 2016). ⭐ File it
     as **the metric-native theorem confirmed in the literature, on the exact family we were about to
     shop in — a fifth confirmation obtained without spending a wave.**
   ⚠ Quote only the aggregate rows and the in-text sentences from that table: the scout flagged the
   **per-dataset cells of Table IV as OCR-ambiguous** — those are never quotable.
2. **Strike-list sweep:** any prior wave document naming **Electricity, NOAA-Weather or Rialto** as a
   candidate stream now carries the §6.2/§6.4 exclusions. ⚠ **`.claude/**` is gitignored, so a
   directory-level grep over it can return a false "no matches" — sweep per-file and run a positive
   control on every negative sweep.
3. **The C2W10 metric ruling** into the reporting conventions: **prequential accuracy (window 1 000,
   Souza convention) + κ_per + κ⁺**, with the **No-Change persistence baseline mandatory in every
   table**, and Žliobaitė's Prop. 8 hazard recorded (false drift alarms can raise accuracy under
   temporal dependence ⇒ controller event counts are reported beside accuracy).
4. **The I2 caveat's status, whatever it is.** If the Hub posted no lift, record that **the
   depth-as-feature-importance caveat REMAINS ACTIVE** and cite the C2W10 branch that fired. If the
   Hub posted a lift, record it **with its configuration scope verbatim** and mark it **pending
   Advisor ratification** — ⛔ never as a program-wide lift.
5. **The three-state lifecycle vocabulary** into the transfer docs: **PROTECTED ⇄ ACTIVE → TRASH**;
   promotion usage-gated with hysteresis; **demotion = re-exposure to designed decay, NOT the trash
   region**; trash reserved for the never-useful/spurious. ⭐ Record the **rich-get-richer designed
   negative** (an early-popular-then-abandoned well must demote) as the standing test of that design.
6. **The netting build requirement** as standing practice: **every depth curve raw AND netted**; the
   decay exponent drifts with `last_write_chunk`; un-netted curves overstated recovery by up to 34 %
   (C2W6) and 14–20 % (C2W8). ⛔ No erosion / flattening / restoration statement is legal un-netted.
7. **N-registry entries** for whatever C2W10 landed, each with tier, source artifact and date; include
   any **refuted pre-registration** from `PREREG-C2W10.md` §7's numeric predictions — the predictions
   are scored either way and the misses are filed, not dropped.
8. **`HEP_primers.md` / `philosophy-synthesis.md`** per their own protocols (⟲ dated addenda): the
   persistent-store/continual-memory thread, and the honest statement of where the wave landed.

## Acceptance
1. Every item above dispositioned — landed, or listed with a reason.
2. Every new registry entry sourced to a **landed artifact path**; nothing sourced to a report's prose.
3. The gitignored-grep hazard honoured: **a positive control accompanies every negative sweep** you
   report.
4. Reconciliation list in the **first 10 lines**, including anything you left for C2W8's or C2W11's fold.
