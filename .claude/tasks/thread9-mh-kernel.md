# Task: thread9-mh-kernel — is squeeze-proposal + Metropolis–Hastings a certified kernel on the CLU memory EBM? (w8; Thread 9, Head + theory colleague)

- **Agent:** `physics-theorist` · **Output:** `.claude/outputs/thread9-mh-kernel.md` (HEP-legible standalone note + numerical checks in `.claude/scratch/thread9-mh-kernel/`).
- **Read first:** protocol · `.claude/brainstorm_log.md` **Thread 9** (the idea + Hub formalization + 3 risks) · `paid-access-theory.md` (S^(M) properties: det=1, bounded injection, latch behavior) + `paid-access-experiments.md` §7.2–7.3 (measured certificates) · F5 Prop-9/check (e) (FDT-correct σ*; nominal-T ~11× miscalibration, κ≈0.09) · `generative-studies` outputs (temperature semantics) · `v1-pivot.md` (learned-τ — the baseline any acceptance rule must face) · Def-2 nomenclature.
- **Why (Head, 2026-07-08):** if squeeze+MH is a proper kernel, V1's retry cascade becomes *test-time compute as MCMC with a stationarity certificate* — physics-heavy content that strengthens V1's ML4PS position-paper framing. Repo read-only.

## Items
1. **The kernel, stated precisely.** Proposal = S^(M)_ζ (symplectic, det=1, volume-preserving ⇒ no Jacobian correction) at sampled/line-searched ζ; acceptance min(1, e^{−ΔH/T}). Prove/verify detailed balance w.r.t. the Gibbs measure of the trained H. State the exact relation to HMC (Duane et al. 1987; Neal 2011): squeeze replaces the leapfrog trajectory as the proposal — what does that cost in mixing vs gain in certificate simplicity? Is a squeeze-then-relax composite (the actual retry cascade) still a valid kernel, or does the governed relaxation between proposals break stationarity? If broken: formalize the correct decomposition (e.g., alternating MH kernel + deterministic contraction toward a *different* target — characterize what the composite converges to).
2. **The governor in the loop (the crux).** The governor is state-dependent dissipation γ(H) — it destroys the Gibbs invariant. Options to analyze: (a) freeze γ=0 during MH segments (kernel valid, governor as an outer safety layer); (b) treat governor+MH as a Metropolis-within-annealing scheme (what certificate survives?); (c) FDT-corrected Langevin (σ*, our unique asset) as the proposal noise instead — when is that strictly better? Recommend one architecture with its certificate.
3. **Latch/coset behavior.** Acceptance of a channel-isotropic squeeze preserves Q (measured w7, 1.2e-7); rejection = identity. But the Gibbs measure is FLAT along an exact coset ⇒ the kernel random-walks the latch coordinate. Is that acceptable (latch = quenched, only touched by explicit writes?) or does MH sampling erode stored coset content over many retries? Derive the latch diffusion rate vs (ζ, T, acceptance rate) — this is the "does certified retry destroy memory" question and MUST be answered before V1 folds any MH paragraph.
4. **Temperature semantics.** With FDT-correct scales, what is the principled T for acceptance given cross-model energy incomparability (the raw-R lesson)? Analyze: per-model T from write-time self-test probes (the calibration apparatus repurposed) vs a global T. State the honest conclusion even if it is "a learned per-instance temperature re-imports the learned gate" (that partially deflates the parameter-light pitch — say so; CM-3 discipline).
5. **Numerical checks (≥3):** detailed balance on a toy trained-EBM (histogram vs Gibbs); the composite-kernel stationary distribution (what it actually converges to with governor on/off); latch diffusion rate vs prediction. F5 App-N standards.
6. **Deliverables:** verdicts tagged [proven]/[verified]/[design hypothesis]; a **V1-grade position paragraph** (physics-heavy, ML4PS register — the Head wants this candidate content for the V1 short's horizon/mechanism section, foldable at revision if it lands clean); a **w9 experiment spec** (vs learned-τ AND router baselines, per CM-3 discipline); prior-art positioning notes (HMC, delayed-rejection, parallel tempering — cite, don't claim; our differentiator = governor-composed, bounded-injection, latch-accounted acceptance on a *trained conservative memory*).
