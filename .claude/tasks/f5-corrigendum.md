# Task: f5-corrigendum — fix the proven kinetic-isotropy error in F5 (BLOCKS the arXiv push) + decay-constant naming

- **Agent:** `physics-theorist` · **Output:** `.claude/outputs/f5-corrigendum.md` (change-log + the exact replacement text) — and EDIT the F5 docs in place (this is the one theorist task authorized to edit the F5 sources, since the fix is a proven correction to the theorist's own note).
- **Read first:** protocol · **`.claude/outputs/v2-symmetry-deepdive.md` §4.1 + §10 R3 + the §7.15/7.16 handover-update block** (the proof, the sites, the replacement) · `formalism-note.md` (F5 source) · `.claude/papers/f5-note/f5-note.tex` + `.claude/outputs/f5-arxiv-note.md` (the arXiv-bound copies) · `v2-full-runs.md` item 5 (the contradicting measurement) · claims matrix (check no CM row inherits the false claim).
- **Why BLOCKING:** the false clause sits UNHEDGED in the arXiv-bound note (`f5-note.tex:124`) and is cited by both shorts. F5's arXiv push is the program's #1 critical-path item and must not go out with a claim contradicted by our own data + an elementary rank argument.

## The correction (proven; verified 0.0 exactly in the deep-dive)
1. **Kinetic-isotropy error.** Replace the claim "*if M does not isotropize, the channel becomes a pseudo-Goldstone one with μ² ∝ the mass splitting*" — at ALL THREE sites (`formalism-note.md:233` §4.1, `formalism-note.md:435` §9, and `f5-arxiv-note.md:139` → `f5-note.tex:124`) — with the **kinetic-spurion blindness theorem**: for any invertible M, congruence M^{−1/2}KM^{−1/2} preserves rank/inertia (Sylvester), so **a flat direction stays μ²≡0 for any anisotropic M**; the vacuum manifold, every zero mode, and the γ>0 latch are exactly intact. Kinetic anisotropy perturbs ONLY the Noether *current*: on the ring Q oscillates between √(2E)r√M_min and √(2E)r√M_max — a **bounded oscillation** (amplitude √(2E)r(√M_max−√M_min)), NOT a mass and NOT secular drift.
2. **The `[proven; verified (g)]` tag over-covers.** Check (g) verified only the *charge* half ("M=(1,2) charge drifts by 2.6"), never the (false) μ²∝splitting half — and that "2.6" is very likely the bounded-oscillation *envelope* (predicted 0.414·√(2E)·r for M=(1,2)), not drift. Re-scope the tag; if cheap, re-run check (g) to confirm oscillation-not-drift (ties to deep-dive F-2).
3. **Decay-constant naming (7.16, secondary but fold in):** the object in GMOR and in Q=F²θ̇ is **F = √(M_eff)·r***, not the orbit radius r*. F5's formulae are correct (μ²=δn²/(M_eff f²) ⇒ F²=M_eff f²); only the *name* "decay constant = orbit radius" is misplaced. Reserve **F** for √(M_eff)·r*; call r* the vacuum radius / condensate Σ.

## Scope discipline
- This is a **corrigendum**, not a rewrite: change only the false clause + its tag + the naming, and add the blindness theorem as a short proposition (it is strictly stronger than what it replaces). Do NOT restructure F5 or import the deep-dive's other new results (GMOR-exact, custodial ρ_CLU, T>0 diffusion, non-abelian bank) — those are separate, Head-gated decisions.
- Keep both the internal `formalism-note.md` and the arXiv-bound `f5-note.tex`/`f5-arxiv-note.md` **consistent** (all three must carry the corrected statement; the arXiv copy is the one that gates the push).
- If any claims-matrix CM row or either short's draft leans on the false clause (e.g. a `tie_channel_mass` "else pseudo-Goldstone" justification), FLAG it in your report for the Hub — do not edit the shorts.

## Acceptance
All three F5 sites carry the blindness theorem (μ²≡0 under anisotropy; current = bounded oscillation); the over-covering tag re-scoped; decay-constant renamed; a one-line "what this unblocks" note. The F5 note is then genuinely arXiv-ready on this axis. Report lists every edit + any downstream draft/matrix references to reconcile.
