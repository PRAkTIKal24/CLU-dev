# Task: anti-collapse-characterization — VICReg-for-CLU: keep the structural levers alive under task training (physics-theorist, first pass)

- **Agent:** `physics-theorist` · **Output:** `.claude/outputs/anti-collapse-characterization.md`
- **Read first:** protocol · the handover's 2026-07-20 "anti-collapse regularization" brainstorm entry · `.claude/claims_matrix.md` **CM-5** (mass-narrowness — the hierarchy doesn't emerge, must be designed-in/induced), **CM-16** (vacuum erosion / symmetry restoration under CD + the energy-anchor cure), N7 (no mass hierarchy), N46 (emergent has no coset register) · `.claude/outputs/{seed-sweeps,sleep-erosion-study,anchor-robustness,t-lever-forgetting}.md` (the collapse phenomena we've already measured) · JEPA/VICReg + SigReg (representation-collapse prevention — the analogy).
- **The idea (Head, 2026-07-20):** letting CLU self-organize on a task tends to **collapse its structural levers** — the mass spectrum narrows (memory-timescale diversity gone), CD erodes the designed symmetry (coset structure gone), modes collapse to one band. JEPA prevents *representation* collapse with VICReg's variance/covariance terms; CLU needs the analogous thing for its **dynamical/structural** degrees of freedom. **We already have one such regularizer — the energy anchor that stops vacuum erosion (CM-16).** Generalize it into a *family*.

## This first pass = characterize + formalize (theory + small numerics; NOT the full method)
1. **Enumerate CLU's levers and their collapse modes** (unify the existing findings under one frame): mass-spectrum diversity (→ narrows, CM-5/N7), symmetry/coset structure (→ CD erosion, CM-16), the three memory modes latch/overdamped/underdamped (→ collapse to one band when μ² spectrum + γ don't span the bands), forgetting/γ_φ structure, potential coercivity (→ V flattening). For each: **what is the order parameter of the collapse, and what training condition drives it?**
2. **The VICReg analogy, made precise for a *dynamical* latent.** VICReg = variance (anti-collapse) + invariance (task) + covariance (decorrelation). State the CLU analog: task loss + **structural-diversity terms that keep the levers' order parameters away from their collapsed values.** Candidates to formalize: a **mass-spectrum-spread** term (penalize low Var(log_mass) / target a spectral range — induces the hierarchy CM-5 says won't emerge); the **energy-anchor** generalized (keep flat-direction content, CM-16 — we have this); a **mode-band-coverage** term (keep some modes near critical damping, some flat, some overdamped — so all three memory bands stay reachable). Derive what each penalizes and why it doesn't fight a reasonable task objective.
3. **Which collapses are benign vs fatal for a given task?** Not every lever matters for every task (fault-onset needs stability/basin-exit; anomaly needs valley structure; classification needs global reach). Map lever→task so the regularizer is *task-appropriate*, not a blanket clamp. This connects to the CAFE-task↔mechanism mapping (Anomaly←valleys, Event←basin-exit/stability, Classification←global/wormholes).
4. **Small numerics:** on a toy CLU, show a lever collapsing under plain task training and a candidate regularizer preventing it (e.g. mass-spread reg maintains Var(log_mass) where plain training narrows it). Precision-checked, honest — refute your own candidate if it doesn't hold.

## Acceptance
A collapse taxonomy (lever · order parameter · driver · fatal-for-which-task); the VICReg-for-CLU formalization (task + a named set of structural-diversity regularizers, each derived); the lever→task appropriateness map; ≥1 numerical collapse-and-cure demo. **This is the theory scaffold for a potential headline methods contribution — "structural regularization for physics-primitive networks" — that makes CLU actually leverage its levers on real tasks. Flag the engineer follow-ups (implement the regularizers) but do not build production code.**
