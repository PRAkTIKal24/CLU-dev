# doc-curator-w26-sync — the OWED w26 transfer-doc pass

**Agent:** doc-curator. **No worktree** (transfer docs only). Base local `main @ 082d095` (post-w26).
**Campaign tag: [C1W27].** ⚠ **This pass is OWED and nothing is on disk yet.** All transfer docs are
**w25-synced**; w26 is **not folded**. Until this lands, no one may quote roadmap or matrix per-R
statuses as current for w26. Scope = **w26 only** — C1W27's own results do not exist yet and are a
later pass.

## ⭐ DIAL DECLARATION (protocol §7)
- **Dial:** none — **transfer-doc upkeep.** You register and scope other people's evidence; you
  generate none.
- **Laundering control:** every performance N-entry you write must carry its laundering verdict in
  the same sentence as its number. An entry that states a win without its launder is non-compliant.
- **Falsifies:** any entry whose number you cannot trace to a raw artefact under `.claude/outputs/`
  or `.claude/scratch/` — file it as **unverified** rather than registering it.
- **Does NOT falsify:** disagreeing with a Hub tier — say so and flag it as an open question, as you
  did (correctly) in w25.

## 1. The N-entries the Hub owes w26 (write all of them)
1. ⭐⭐ **The R2 unclamping (positive)** — `K_learned(4)` 16 → 32 via the annealed read
   (0.8210±0.0034 → 0.9997±0.0006, 3 seeds, ~110 SE, **zero extra bytes/dims/Verlet steps, stored
   format unchanged**) and 16 → **128 = `K_designed(4)`** via the multi-channel payload code (tax
   1/8 → 1/1 at every noise level measured). ⚠ **Scope: d=4 ONLY; d=6/d=8 NOT RUN and no exponent
   re-measured.** Register the **three controls** that make it a mechanism: `readonly` reproduces
   baseline to 4 dp (⇒ the whole gain is **address acquisition**, which explains `basin ≡ strict` at
   every failing cell since w23) · `static` reaches basin 0.9993 and reads the **wrong value** (⇒ the
   continuation is load-bearing; "wider wells" is a different, failing result) · `spectator` (+0.034
   for extra dims + 43 % params) vs the code (+0.195). And register the **payload-noise condition**:
   w25's `pscale` manipulation **dies** under noise (0.9995 → 0.5894 / 0.8936 @ σ=0.010) where the
   constant-Δ code passes (0.9980).
2. **The vacuous-`tol`-at-m>1 methodology negative** — a blank payload scores **1.0000** under
   `pass_metric="tol"` at m>1. Note that C1W27 changes the default to `decode` (Head ruling).
3. **Substitutes / N98 non-replication (negative)** — `atom_init_local` is a **NULL at the shipped
   width** (+0.007±0.016); the two levers are **substitutes** (interaction **+0.459**, 14.3 SE),
   refuting the engineer's own registered additivity prediction; ⛔ **N98's +0.051 does not reproduce
   on this harness** ⇒ **the N98 cross-harness caveat** the Hub owes you, stated as a reader warning
   on N98 itself. No default was changed (B1.4 respected).
4. **PGCP landed + the overflow scope boundary** — the N99 update block: `AUC(z_hole)` 0.99985 →
   **0.5000 ± 0.0000**, all six statistics 0.5000, `TPR@FPR1%` 1.000 → 0.000, IN-after-delete
   byte-equal **3072/3072** (still exact with a survivor cascade, mean 1.13 moves); rematch under the
   real Verlet read **61/64, per-admitted 1.0000, per-offered 0.9531** (H5 scope gap CLOSED; "0.953"
   is now quotable **as measured**). ⛔ At overflow the claim **FAILS** (`AUC(n_live)` = 1.000,
   `AUC(s4)` = 0.914) — P2 waitlist unbuilt in w26 (it is being built in C1W27; do **not** pre-write
   its outcome).
5. ⭐⛔ **The same-keys-launder negative — the sharpest negative the program has on its own read.**
   The same-keys launder (= **the settle deleted, nothing else differs**) beats CLU **6/6 on both
   axes**; on clean classification the physics costs **≈1 pp**. Pair it with the honest positive:
   at matched **bytes** CLU beats the whole replay field on ACC **6/6** (0.903 vs iCaRL 0.885) and
   forgetting 5/6 — but ⛔ **the matched-bytes launder is never beaten (6/6 both axes)** ⇒ Candidate 1
   files as **SUPPLEMENTARY**, the pre-registered falsifier having fired. Also register the store
   **saturating at 314k floats** (6085/7658 wells, **gate-bound not budget-bound**) as the packing
   law appearing inside a benchmark, and the corrections: **24.5× → 19.1× floats**; iCaRL ran **over
   budget** at small budgets; LwF is **not** a tuning issue (flat over a 200× α sweep — a CE-scope
   convention) and is **4.3 pp low** wherever that baseline table is quoted.
6. **The τ-regime scoping (tier B)** — the w24↔w25 contradiction is **resolved, retracting neither**:
   τ is binding **iff `#{cos₀ < τ} < k·step_n`**. At p=0.8 all four thresholds are **bit-identical**
   (`max|Δ| = 0`, 3/3 seeds × 2 snapshots); at p=0.5 in the **same store** τ=1.0 costs
   **−0.485±0.064**. ⛔ **NOT a φ-space property** — the analyst's own pre-registered hypothesis,
   falsified.
7. **The allocator leak is a CURVE, not a constant → N99 upgrade** — AUC **0.6715 / 0.9165 / 0.9961 /
   0.99985** at **2 / 4 / 6 / 8** offers; mechanism: IN-world min `z_hole` = `d_safe` to 4 dp at every
   load ⇒ **`placement-landing`'s acceptance test is load-dependent and must be stated at a load.**
   This is the concrete instance of the new standing rule **"quote the curve, not the endpoint"** —
   register the rule where future waves will hit it.
8. **The BG07 attribution correction** — ⛔ **Blelloch–Golovin FOCS'07 own the algorithm outright**;
   our Thms 1–2 are **instantiations** of their Thm 3.2 + `DELETE`, and the fix-up cascade is
   **theirs**. Defensible novelty = the four-part composition (packing certificate · decaying content
   + commutation proof · canonical energy function · **negative** SHI price vs Buchbinder–Petrank's
   proven exponential cost). Register the newly banned term **"deletion-compliant"** (EUROCRYPT'20
   term of art) alongside the standing "certified"/"unlearning" bans, and the Hartline
   **reversibility** caveat. Candidate 2 is **occupied in pieces** — adopt Ginart Def A.5 + Sekhari
   Def 3; **do not coin a benchmark.**
9. **The TRILEMMA** — value fidelity · amplitude-independent hold · amplitude-independent latency:
   **pick two.** Register that the third corner **is the compute-adaptive-read dial**, and that both
   mia-D3 fixes (a) and (b) were **REFUTED** ((b) breaks the anti-decoration guard: the trivial
   substitute passes on **0.672** of queries with no dynamics at all, amplitude-independently).
10. **The saddle criterion supersedes w25's own reach bound** — not `|a|max/κ` but a saddle condition
    on `L = √(|c|² + a²)`, verified ±2.5 % on the shipped integrator and **31/32 items on the trained
    shipped `V` with ZERO free parameters**. Register: **reach is logarithmically un-buyable**
    (κ 4→5 needs 55× depth; 4→6 needs 9200×) · `sep` **alone** classifies 11/11 Stage-0 cells where
    `sep/w` does only d≥4 ⇒ **the ratio is a reparameterisation, not a cause** · `learned_confine` and
    the ball radius are **reach parameters, not free knobs** · ⛔ **κ ≈ 3 wording is superseded**.
11. **The φ gate STRADDLES** — 0.339±0.015 at the registered arm (**MISS**) → **0.357±0.019 at 2.5×
    fit compute (CLEARS, by < 1 seed-σ)**; w25's "kNN caps at 0.21" **SUPERSEDED**. ⭐⭐ The strict-φ
    cost **finally bites on a working φ: +0.020±0.010 (3/3 seeds)** vs +0.003 at PCA — *exactly the
    difference between clearing and missing* (the number w24 asked for and w25 could not give). The
    CIFAR null has **two** causes: address space (+0.14) **and** the 200-item budget (+0.02/doubling;
    clears at 400). ⚠ **Every number here is a kNN/launder number** — the encoder raised the launder
    by construction. ⚠ **Never quote "the gate was cleared" without the fit budget.** ⭐ **The CIFAR
    entry run is a pre-registered NOT-RUN, by Head ruling (2026-07-29): file the pre-registration
    (CLU 0.30–0.36, `laundered=True`) as a registered-and-declined item, NOT as a null.** The gate
    straddle is the reported finding.

## 2. Hub rulings to fold (these are answers to your w25 open questions)
- **N97 ↔ N104 = EXTENSION, not contradiction** (Hub ruling). The scopes genuinely differ —
  analytic Gaussian + gradient flow vs the shipped flat-vacuum potential. **Keep your reader-warning**
  that N97 alone must not be used to ship R3.
- **N100–N108 tiers: CONFIRMED** as you filed them (curator-concurred, now Hub-confirmed).
- **`claims_matrix.md` v2.4: CONFIRMED.** Roll to **v2.5** with the w26 entries above.
- **N103 — Hub ruling: register it as a full registry entry AND cross-reference CM-23(r)**, not
  matrix-only. Reason: the negatives registry is the debugging record future waves actually read; a
  matrix-only entry is invisible to a spoke that never opens the matrix. Flagged as a Hub call the
  Head may overturn.
- **§7 cosmetic fix (Hub's own drafting error):** `matched-bytes-frontier`'s dial declaration sits at
  line 21 behind a preamble instead of opening the file. Note the template rule — **the dial block
  opens the task file** — wherever you keep the §7 convention.

## 3. Doc-by-doc deliverables
- `negative_results.md` — the entries above, continuing from **N108**, with tiers and the dated
  amendment blocks for anything superseded (N98's cross-harness caveat; N99's curve upgrade; the
  κ≈3 supersession).
- `claims_matrix.md` → **v2.5**, w26 folded.
- `research_roadmap.md` → **v0.9**: per-R statuses refreshed for **w26 outcomes** (R2 **unclamped at
  d=4, scope stated**; R1 **closed in scope**; the w26 table becomes outcomes, not scope). ⚠ Do
  **not** touch the Part-4 R1 line — `r1-positioning-pass` is drafting its replacement for Head
  approval this wave; leave it and note the pending edit.
- `philosophy-synthesis.md` — the ⟲ **w26** dated addendum (the Hub wrote none, as in w25).
- `HEP_primers.md` — continue from **§11.15**.
- **§7 audit** of the five C1W27 task files (`r2-d-sweep-close`, `deletion-waitlist-stiffness`,
  `learned-confine-causal`, `r1-positioning-pass`, this one) as your standing bonus pass.
- ⚠ **C1W27 is Campaign 1's FINAL wave.** Structure the w26 entries so the C1 ledger can close
  cleanly one pass later; flag anything that would be left dangling.

## File ownership
**You own:** `.claude/negative_results.md`, `.claude/claims_matrix.md`, `.claude/research_roadmap.md`,
`.claude/outputs/philosophy-synthesis.md`, the primers.
⛔ **Do NOT edit** `.claude/papers/**` (`r1-positioning-pass` owns drafts this wave), any task file,
`.claude/handover_context.md` (Hub-owned), or any `chlu/` code.

## Deliverable
`.claude/outputs/doc-curator-w26-sync.md` — the per-doc changelog, the N-numbers assigned, your tier
for each, any Hub tier you dispute, the §7 audit table, and a short list of the wave's **most
misquotable sentences** with the guard you placed on each (your w25 pass did this and it was the most
useful thing in it).
