# doc-curator-tierii-read-fix-catchup — register READ ITERATION 1 (the fold that was never done)

**Campaign 2, catch-up fold (opened at the `[C2W7-CLOSE]`). Agent:** doc-curator. **No worktree, no
branch** (registries are under `.claude/**`, gitignored). Writes
`.claude/outputs/doc-curator-tierii-read-fix-catchup.md` + the registry edits.

## Why this exists (`doc-curator-c2w7-fold` gap 2, Hub-accepted)
⛔⛔ **`tierii-read-fix` (read iteration 1) has NO N-entry at all — it was never folded** — yet C2W7's
re-labels, refutations and never-quotes all cite its published numbers. **The registry currently
documents the correction of a result it never registered.** Close that, and close it *after* C2W7 so
every entry is written in its already-corrected form (this is the cheap ordering, not an oversight).

**Read first:** `.claude/outputs/tierii-read-fix.md` (full — it is the subject) · the C2W7 fold's
entries **N232–N240** and its dated banner on `tierii-read-fix.md` (⭐ the corrections are already
written there — **inherit them, do not re-derive or re-litigate**) · charter **ADDENDUM 5 §A20** (the
iteration's own specification) and **ADDENDUM 8 §A24/§A26** (what C2W7 later corrected) · the
`[C2W7-CLOSE]` §10 entry.

## What to register (iteration 1's own findings, in their CORRECTED form)
⭐ **Every entry must be written already-corrected.** Iteration 1 is the wave that repaired addressing
and failed expressivity; three of its own headline statements were later re-scoped, and the entries
must not re-introduce the superseded forms.

1. ⭐ **The addressing repair (the wave's real positive):** `K0` **0.0477 → 1.0000** (bar 0.90) ·
   distinct wells occupied **2.20 → 11.82** launched / **11.27** settled · the settle's destruction cut
   from **−23 %** to **−4.6 %** raw, and **+0.076** gated. ⛔ **Carry the C2W7 corrections in the same
   entry:** `K0` is **REACHABILITY ONLY** (N235 — a collapsed head scores 1.000 while every reader reads
   0.0000), and the `S_eff` **16.00 "IN BAND"** must be registered as **"all 32 wells visited"** — the
   only attainable in-band value (N239), ⛔ never as "comfortably inside a band".
2. ⛔ **The expressivity failure and its guard:** exact-set occupancy **0.0023 ± 0.0047** (R1's clear bar
   0.02, INDETERMINATE — one seed carried it) · `OD_min` **−0.0008 ± 0.0016**, an explicitly **vacuous
   TIE** · **`G1` fires: −0.0016 ± 0.0019.** ⛔ **Register with C2W7's two corrections attached:** the
   audit's zero-parameter re-score makes this **`G1` WORSE, −0.0016 → −0.0023** (N237 — the fitting
   artifact was not hiding a dividend), and `OD_min` reproduces **identically**.
3. ⛔ **The `k`-capacity claim, REFUTED at C2W7:** iteration 1's registered guard-4 result (`k` 12→24
   buys **+0.2383** coverage) is **refuted at a multiplicity head** (N234: 0.0000 change at 2× flops).
   Register the original **with its refutation in the same entry** — `k` is a **resolution** dial once a
   head commits.
4. ⭐ **The mechanism it handed forward (now load-bearing for the write-side iteration):** the store's
   actual attractors sit **1.09 ≈ 1.4 `sep`** from its designed anchors; **476 of 508 "minima" have
   depth 0.0000** (confinement-bowl resting points, not wells). ⭐ This is the measurement C2W7's
   scatter finding (N233) is *explained by* — cross-link both ways.
5. **Learned `p₀` as a reach lever:** coverage **0.009 → 0.112 (12×)**; at a harder cell `w|A`
   **0.022 → 0.352 (16×)**; ⛔ it buys **no** gated precision (0.350 vs 0.378) — reach and selectivity
   are different quantities. (C2W7 re-priced it to **7.6× on the score** at a multiplicity head, N232 —
   cross-link.)
6. **The consolidate/trash first use** (§A20.3(d)): 508.4 of 512 probes found minima, merged to the
   designed budget **32.0**, **476.4** trashed, `γ_φ(q)`'s **first use ever**; ⛔ **not adopted at that
   cell** (+0.030 raw precision, **−0.202 gated**). ⚠ **C2W8 owns the lifecycle now** — register as
   prior art with a pointer, ⛔ not as an open thread.
7. **The dedupe verb measured INERT** at that head (`sum` ≡ `noisy_or` in every digit), and **C2W7's
   sharper finding on top:** a working `F`-commitment makes the verb **irrelevant**, not live (its
   engineer's P13 failed and the failure was the finding).
8. **Its two unowned reconciliations** (the report's own list): the **`0.272` launch ceiling is
   `P=4`-mean-specific** and must be scoped with its `(d, draws)` noise model wherever quoted
   (0.072 at `d=4` · 0.695 at `d=8`); and **`FROZEN-interfaces.md`'s reader-param row needs a `(d, m)`
   column** (at `d=8, m=8`: 136 / 72 / 108 / 0 / 72). Register both; the second is a one-line doc fix.
9. **The program-wide hazard it discovered** (if not already registered): reverse-mode AD through an
   un-`jax.checkpoint`-ed `lax.scan` settle **OOM-kills the process silently — exit code 0, no
   traceback, no partial output** (~7 GB taped). ⭐ Check first whether a prior fold already carries it;
   if so, cross-reference rather than duplicate.

## ⛔ SCOPE DISCIPLINE
- ⛔ **Nothing is re-derived, re-scored or re-adjudicated.** Transcribe from `tierii-read-fix.md` and
  inherit C2W7's corrections verbatim; where the two differ, **C2W7's corrected form governs** and the
  superseded form is shown struck, never silently dropped.
- ⛔ **No full-CLU verdict** (§A28.4) · ⛔ **no implication that a read iteration 3 exists** (§A26.6 — the
  read track is PAUSED and C2W7's close ENDED it) · ⛔ **`depth_ratio` stays DEFERRED** (§A26.8; iteration
  1's own **+0.035 gated coverage** is one of the two numbers in that standing conflict — log, don't
  adjudicate).
- ⚠ **Concurrency:** C2W6's fold-2 and C2W8 may be live. **Take fresh N-numbers after the current
  maximum, verify no collision at end of pass** (the C2W7 fold's own N231 collision is the precedent),
  and touch no other fold's rows.
- ⛔ **`PREREG-TierII.md` is never edited.** No erratum is owed by this pass (the audit returned
  SURVIVES).
- The matrix is **additive only**; the curator never self-confirms — propose, and the Hub confirms.

## Acceptance
Iteration 1 has N-entries covering items 1–9, each written in its **already-corrected** form with the
C2W7 cross-links live in both directions · the three superseded forms (`S_eff` "in band", `K0` as
health, `k` as capacity) appear **only** struck-with-replacement · its two reconciliations registered
with owners named · N-numbering verified collision-free at end of pass · matrix additions proposed with
a dated line · report → Hub, spawn nothing.
