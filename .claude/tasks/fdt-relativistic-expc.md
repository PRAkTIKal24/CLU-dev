# Task: fdt-relativistic-expc — validate the exact thermostat on real Exp-C, and close the N10 loop (w16, analyst)

- **Agent:** `results-analyst` · **Output:** `.claude/outputs/fdt-relativistic-expc.md`
- **Read first:** protocol (**§5 pre-registration MANDATORY** — measured law) · `.claude/claims_matrix.md` **CM-17** (the `d·Θ` law + the F2 latent-mass thermostat + N10 UPHELD) · `.claude/outputs/relativistic-gibbs-expc.md` (the N10-upheld result + the real Exp-C path, checkpoints, instrument) · `.claude/outputs/fix-pack-7.md` (the shipped `langevin_noise="fdt_relativistic"` thermostat + `gibbs_defect_parameter`) · `chlu/core/integrators.py` (the new mode), `chlu/experiments/exp_c_dreaming.py`.
- **Repo:** read-only. Runs on the real Exp-C path (`mnistFFF` + the matched native-`c` retrains from `relativistic-gibbs-expc`).
- **Why:** `fix-pack-7` shipped the exact relativistic thermostat (F2, InvGauss scale-mixture). It has been verified on a **toy harmonic well** (bias `−0.727→+0.0011`). It has **never run on the real d=784 Exp-C path**, and the two questions it answers are both worth an appendix:

## The two questions (pre-register both predictions)
1. **Does F2 actually sample Gibbs on real Exp-C?** Under `fdt_relativistic`, the momentum marginal of a trained Exp-C chain must be **Maxwell–Jüttner**, not Gaussian — the exact opposite of what `relativistic-gibbs-expc` measured for the broken `fdt`/`legacy` samplers (Gaussian, `Var/(M_eff T)` off by up to `785×` at `d=784`). **Predict:** F2 restores the MJ marginal (QQ/kurtosis test vs the MJ reference) and drives `Var(p)/(M_eff T)` to the theory value. This is the **first real-data validation of the F2 fix** and it belongs in the F5 note's appendix as the empirical companion to the proof.
2. **Does a *correct* sampler change the MNIST 3/5/8/9 imbalance? (The N10 closure.)** N10 is **UPHELD**: the imbalance is the *learned landscape*, not the sampler — `relativistic-gibbs-expc` showed it survives `c=5`, `m₀=25`, and an exactly-Gibbs MJ law. F2 is now the *cleanest possible* correct sampler. **Predict (from N10): the imbalance is UNCHANGED under F2** — because the sampler was never the cause. Measure the digit histogram under `fdt_relativistic` vs the `legacy`/`fdt` arms (same classifier, same paired inits as `relativistic-gibbs-expc` so it's directly comparable). **A confirmed null here is the point** — it closes the loop: "we built the exact fix, proved it samples Gibbs on real data, and confirmed it does not move the imbalance — the sampler was never the cause; the landscape is."

## Items
1. **PREREG** both predictions (MJ-restoration + imbalance-unchanged) before running.
2. **Momentum-marginal shape** under `fdt_relativistic` on ≥2 Exp-C checkpoints: kurtosis + KL to MJ vs to Gaussian; `Var(p)/(M_eff T)` vs the `d·Θ` prediction. Report `d·Θ` (`gibbs_defect_parameter`) for each cell.
3. **Digit histogram** under `fdt_relativistic`, paired against the `legacy`/`fdt` arms from `relativistic-gibbs-expc` (identical inits/keys/classifier). χ² vs those arms; report flips.
4. **The honest triangulation:** three things must be consistent — F2 samples Gibbs (Q1), N10 says imbalance is the landscape, and therefore F2 leaves the imbalance put (Q2). If any two disagree, something is wrong — dig in and report it (that would be the more interesting outcome).
5. **Scope/confounds:** `fdt_relativistic` is a *sampler* change at dream time; the checkpoints were trained under the shipped path. State whether you also need a native retrain (probably not — this is a dream-time sampler swap), and flag the `d·Θ` regime you're in.

**Acceptance:** PREREG first; MJ-restoration measured on real Exp-C (the F2 fix validated on real data for the first time); the imbalance-under-F2 histogram vs the prior arms; the three-way consistency stated explicitly; every cell carries `d·Θ` + flag-provenance. **A clean "F2 samples Gibbs, imbalance unchanged, N10 closed" is the target result** — an F5-appendix-grade loop closure. Don't manufacture a difference that isn't there (CM-3, and N10 predicts none).
