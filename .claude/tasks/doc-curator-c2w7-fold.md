# doc-curator-c2w7-fold — the C2W7 registry fold (charter ADDENDUM 8 §A26 + ADDENDUM 9 §A27.2/§A28)

**Campaign 2, wave C2W7 (close). Agent:** doc-curator. **No worktree, no branch** (registries live under
`.claude/**`, gitignored). Writes `.claude/outputs/doc-curator-c2w7-fold.md` + the registry edits.
⚠ **C2W6's fold is in flight as fold-2 — coordinate, do not collide:** C2W6 owns the erosion/N223
entries and the Add.9 §A27.1 erosion erratum; **you own C2W7's read-track entries only.** If a shared
row needs both, write yours and flag the overlap in your report rather than editing theirs.

**Read first:** charter **ADDENDUM 8 (§A24–§A26) and ADDENDUM 9 (§A27.2, §A28.4)** — binding, and they
are your specification · `.claude/outputs/c2w7-read-cardinality.md` · `.claude/outputs/reader-fitting-audit.md`
· the `[C2W7-CLOSE]` §10 entry (the close record; it carries the Hub's verified numbers).

## ⛔ THE WAVE'S FINDING, IN ITS CORRECT FORM (use this wording; do not paraphrase it looser)
> **The read's EXPRESSIVITY IS REPAIRED, and it is ENTIRELY LAUNCH-SIDE.** `R1` clears for the first
> time in the program (0.0023 → **0.065625 ± 0.023876**, mean−2SE 0.0417 vs bar 0.02) and the read
> commits to a cardinality (`F_hat = 4.542 ± 0.122`; asserted set exactly `F` on 39.4 % of queries).
> **`G1` fires significantly (−0.007031 ± 0.001562, 5/5 seeds negative): the store subtracts value
> from its own learned launches.** The gate fails **on G1 alone as substance**; no organizer swap ran;
> **the tier-ii verdict is UNREACHED for a third wave — NOT refuted — and the honest-null path does
> NOT fire.**

## The six folds (each is a registry edit, not a summary)

**1. ⭐⭐ RE-SCOPE C2W7 reconciliation 1 — RESOLVED, NOT WITHDRAWN (Add.9 §A27.2).**
⛔ The sentence *"every arm this programme has scored at ≈ 0 may have been scored through a reader
that destroys its own signal"* is **MEASURED FALSE for C2W5** and must not survive anywhere.
**Replace with the narrower true form, verbatim:**
> *A fitted reader destroys signal only in proportion to the fraction of queries whose asserted set is
> **already exactly right** — which is why it cost C2W7 (~18 %) and cost C2W5 nothing (2/2560 ·
> 3/1280 · 0/2560).*
Record with it: `reader-fitting-audit` returned **SURVIVES** — `null*` = **0.00117 UNMOVED**, `OD_min`
**−0.00078 identical**, N1 **1.0000/0.0000 unmoved**, three reproduction gates **bit-for-bit**
(including non-zero seed-asymmetric cells), largest identity-minus-fitted gain anywhere **+0.0023**
(21× below the 0.05 bar). ⛔ **Record the REFUTED HOPE explicitly:** adding the zero-parameter reader
makes iteration 1's **G1 WORSE, not better (−0.0016 → −0.0023)** — *the fitting artifact was not
hiding a physics dividend.* ✅ **Add.5 §A20 stands as written; NO erratum is owed** (do not create one).
⭐ The mechanism is **CONFIRMED, not refuted** (lstsq `diag(W)` 0.13–0.45 at every cell; the `tol`
crossing fires on 2) — it simply had nothing to destroy.

**2. ⭐ REGISTER `K6` (Advisor-endorsed, Add.9 §A28.3 / §A26.3's general form).**
> **K6 — the exactly-right fraction.** Before any reader is fitted, report the fraction of queries
> whose asserted set is already exactly right. It is computable **without a store, a fit or a reader**,
> it is the **closed-form ceiling of every zero-parameter reader**, and it is the **exact headroom of
> any reader-fitting artifact** (18 % ⇒ the artifact can be worth 28×; 0.1 % ⇒ it is worth 2 queries).
**Mandatory at every future reader registration**, alongside §A26.3's rule that every reader class
carries a **zero-parameter member** — *added, never substituted, never reported alone* (the twin can be
**strictly worse**: measured +0.0109 on a real cell, > 0.99 on a designed α-scaling control, so
substitution would itself launder). A fitted-reader 0 is quotable **only** beside the zero-parameter
member's score on the same latent.

**3. RE-REGISTER `S_eff` (§A26.4) — retire the band, two-sided labels.**
The `[8, 16]` band is **retired**. Arithmetic on the record: `S_eff = K·F/W = 512/W` with `W ≤ N_a = 32`
⇒ **`S_eff ≥ 16` always**, so the band's lower half is **unreachable by construction** and its sole
attainable value is 16 (= every well visited). The instrument becomes **direct wells-visited `W/N_a`
with two-sided labels**; ⛔ **"COLLAPSED" is reserved for CONCENTRATION.**
**Retroactive re-labels (both mandatory):** iteration 1's *"S_eff = 16.00, in band"* → **"all 32 wells
visited"** (the only attainable in-band value — it must never read as "comfortably inside a band");
C2W7's *16.77 COLLAPSED* → **"30.6 of 32 wells visited" (slight UNDER-usage)** — ⛔ **the opposite
failure to C2W5's 34–51 concentration, which the same word described.**

**4. FOLD THE NEVER-QUOTES (§A26.5) into the never-quote registry.**
⛔ `R1` / the F-commitment / *"the cardinality mechanism works"* **without `G1` in the same paragraph**
(inherited from iteration 1, now sharpened: G1 significant, 5/5 seeds) · ⛔ `K0` alone as *"the read is
healthy"* — **reachability only**; a collapsed head scores `K0 = 1.000` while every reader reads
**0.0000** (measured) · ⛔ *"doubling `k` raises the score"* — **REFUTED at a multiplicity head**
(`k` 12→24 at exactly 2× ledgered flops moves best-reader and coverage by **0.0000**); `k` is a
**RESOLUTION** dial once the head commits · ⛔ any upper-edge `S_eff` "COLLAPSED" quoted as
**concentration** · the **0.691** ceiling only with its `(d, draws)` noise model · ⛔ **the un-forced
M15 trip is `S_marg ≈ 7.9`, NOT 6.9** — ⭐ **Hub-verified from raw this close: `pilot1` arms stage reads
7.9173** (the report's 6.9 is wrong; both ≪ the 16 band, so the trip is real either way). **Quote ≈7.9
and stage-label it.**

**5. CLOSE the audit's two carried items.**
(a) ⭐ **`orgdiv-cat-test` §6.1's SEEN column has NO artifact provenance — Hub-verified this close.**
`sum_linear = 0.0109375` and `well_table = 0.003125` appear **nowhere in any** `orgdiv-cat-test/results/*.json`,
and `stage_arm.json` carries **no `seen`/`unseen` keys at all** (top-level: `cells`/`readers`/`aggregate`).
The values **with** provenance are `reader-fitting-audit`'s re-measurement: **SEEN `sum_linear` 0.0094 /
`well_table` 0.0047**. ⇒ **Footnote the published column as unreproducible-from-artifacts and cite the
re-measured values instead.** ⚠ **No verdict moves** — SEEN was never the metric (unseen is; SEEN is the
in-sample liveness anchor), so this is a provenance footnote, not an erratum.
(b) **The proposed §7 Known-Issue entry must ship WITH its qualifier attached** (it currently reads as a
program-wide invalidation, which is measured false). Required form: *a least-squares-fitted reader can
score an informative latent at exactly 0 under a thresholded metric — **and the artifact's size is
bounded by K6, the exactly-right fraction**: 18 % at C2W7 (worth 28×), 0.08 % / 1.2 % / 0 % at C2W5's
three cells (worth 2–3 queries). The C2W5 zeros were re-scored at 3 cells × 5 seeds and **do not move**
(`reader-fitting-audit`, SURVIVES).* Add the companion entry: **least squares against a thresholded
metric is unstable in BOTH directions** — measured shrinkage `diag(W)` 0.128–0.446 **and inflation
+46.5** (`sum_linear` at the cat-test cell, mean residual 13.48 vs the identity's 1.84) ⇒ **a fitted
reader's residual is never evidence that a store is "close."**

**6. THE N-REGISTRY ENTRIES for C2W7** (tier per the registry's own rules; these are the wave's
negatives and instrument findings):
- the multiplicity/counting-code read **repairs expressivity and the gain is entirely launch-side**
  (G1 −0.0070 ± 0.0016, 5/5 seeds; the designed/untrained head **beats** the trained one, 0.0695 vs
  0.0539 — **w20 at the claim budget**);
- **the settle SCATTERS a committed allocation** (4.834 ± 0.125 → 5.668 ± 0.396 distinct wells) because
  the store's attractors sit **~1.4 `sep`** from its designed anchors;
- **`k` is a resolution dial, not a capacity dial**, once the head commits (guard 4, 0.0000 at 2× flops);
- **`K0` is a reachability statement only** (a collapsed head passes it at 1.000 while reading 0.0000);
- **`R2` as registered cannot see the multiplicity failure** — it fires only if settled < launched, and
  the failure has the **opposite sign**;
- ⭐ **the monitored-first/regularized-second order was exercised UN-FORCED** (M15 tripped on a real
  training run at `head_lr = 3e-2` before any regularizer was in the loop; the regularizer then measured
  inert on the quiet claim cell) — and the **marginal/per-query distinction is load-bearing**: per-query
  perplexity **3.94** (≈ `F`, the design working) vs marginal **21.22**;
- **the reader-fitting pathology + its K6 bound** (fold 1) and **the two-directional lstsq instability**
  (fold 5b);
- ⚠ **the trained head's `K0` "PASS" is MEAN-LEVEL**: 0.9070 ± 0.0125, but **per-seed only 3/5 clear the
  0.90 bar** (0.8945 / 0.8906 / 0.9102 / 0.9180 / 0.9219) — record the qualifier (Add.8 §A24 footnote ii).

## ⛔ SCOPE DISCIPLINE
- ⛔ **No full-CLU verdict may be stated or implied anywhere (Add.9 §A28.4).** The C2W7 rig had the
  k-particle head, learned `p₀` and per-particle mass/friction but **no ψ at all** (declared,
  grep-verified), no consolidation/trash (C2W8), no traversal/wormholes (C2W9), no persistent store
  (C2W10), and ran in the factored-store gym rig, **not** the streaming block. **The null is
  VEHICLE-SCOPED.** ⚠ And per §A28.4(b): ⛔ **the missing components are NOT the deepest defect and must
  never be offered as the explanation** — on this family the landscape is written identically for every
  query, so the settle cannot beat its own launches **by construction**.
- ⛔ **The `depth_ratio` conflict is DEFERRED (§A26.8), not resolved** — log it with numbers on both
  sides (`depth_ratio = 1` beats the claim cell by **+29 %** on the best reader here; **+0.035** gated
  coverage in iteration 1; against F5's falsifiability requiring **≥3×** heterogeneity). ⛔ Do not
  adjudicate it; it is decided when a claim actually depends on it.
- ⛔ **No third read iteration exists and none is scoped (§A26.6)** — the read track is PAUSED, and the
  next tier-ii iteration is **WRITE-SIDE**. Registries must not imply an in-flight read iteration 3.
- Quote only `.claude/outputs/*`, the charter, and the §10 entries; every number carries its source.

## Acceptance
All six folds landed in the registries (`negative_results.md`, `claims_matrix.md`, the never-quote
registry, `FROZEN-interfaces.md` where the reader class / `S_eff` instrument are specified) · the
re-scoping of reconciliation 1 verified absent in its old form by grep · K6 and the zero-parameter-member
rule registered as standing preconditions · the two retroactive `S_eff` re-labels applied wherever the
old wording appears · the cat-test SEEN footnote added · matrix version bumped with a dated line ·
report → Hub, spawn nothing. ⛔ **`PREREG-TierII.md` is never edited.**
