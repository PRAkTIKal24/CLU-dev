# v5-rebuild — paper-writer (V5 → v0.2: the ground-up rebuild — the budget cube + the V-curve headline + the deletion estate + the lifecycle)

**Commissioned by the Shorts Advisor (charter: `.claude/advisor-head-shorts-charter.md`, Addendum 18; Head rulings: Q1 (V5 IN, rebuilt around the cube + post-w12 R1 estate, Add.2), Q11 (V5 OWNS the V-curve law, Add.8), V2/V5 priority (Add.18), 2026-08-18).** Read `.claude/AGENT_PROTOCOL.md`, then this file. You edit only `papers/v5-short/` (draft.md + draft.tex per house practice + CHANGELOG.md).

**DIAL DECLARATION: none — rebuild pass; no new measurement; no laundering control applies.**

## What this is
V5 v0.1 (w16) predates the program's entire deletion estate and the shipped lifecycle — its best material is not in it. This is a REBUILD, not a revision: new structure, one paper, one contribution arc: **forgetting in this memory is predictable and controllable — a three-axis budget cube (μ, γ, T) with a damping optimum, byte-exact deletion, and a shipped memory lifecycle.**

## Inputs
1. `papers/v5-short/draft.md` v0.1 + CHANGELOG (what survives: the cube framing, CM-16a/b split discipline, the vault, App-E erosion ship rules, the N46 owned negative in §3.4).
2. `.claude/outputs/shorts-evidence-inventory.md` §4 (V5's map — ⚠ built at v2.11/N224: navigation, NOT completeness; the lifecycle and C2W10 material postdate it).
3. `claims_matrix.md`: CM-16a/CM-16b (⛔ always split) · CM-23(v)(y) · CM-25(f) · §0.12/§0.13 (the lifecycle's approved wording + its scope) · the §0.x sweeps.
4. Registry: N99 · N108 · N112 · N118 · N127 · N128/N129 · N131 · N46 · N149/N150.
5. Outputs: `t-lever-forgetting` · `v5-gate` · `order-independent-placement` · `mia-decay-measurement` · `deletion-waitlist-stiffness` · `unlearning-recon`.

## The paper's structure (contribution arc; your judgment in execution)

**1 — The headline V5 now owns (Add.8 Q11): the V-curve.** The critical-damping retention minimum at `γ_crit = 2εμ` — one damping-optimum curve across ~11 decades in μ², generalizing to the emergent arm 3/3 seeds — is contribution 1 and leads the abstract. ⛔ C-8 hermetic: no reference to any other short of ours (V2 carries the corollary as a supporting appendix result; overlap is sanctioned by Q3, cross-citation is not).

**2 — The budget cube (μ, γ, T)** as the organizing frame: mass sets the floor, friction preserves (CM-16a face — designed-only scope with N46 owned, as v0.1 already does in §3.4), temperature erases (CM-16b face — the coset-diffusion law + sign flip). The refrigerator vault **107.77 ± 4.78×** (⛔ never `13.9×` — never-quote).

**3 — NEW: the deletion estate (the R1 material V5 was built to carry).**
- **Exact deletion:** the full-scope form CM-25(f) ("every load 0.29×–1.71×") and/or the scoped form CM-23(v) — ⛔ **the Blelloch–Golovin attribution is MANDATORY wherever exact deletion appears** (N118: BG-FOCS'07 own the algorithm; our contribution is the store that makes it applicable). ⛔ **"certified" is NEVER used for R1 deletion** (charter §4 never-quote). ⛔ CM-22(m)(n)(o)(p)(x)'s forbidden deletion claims govern.
- **The trilemma** CM-23(y): quotable corner = the compute-adaptive read.
- **Lifetime instruments:** R₅₀ survives (N128) with N129's `γ_read = 0` oscillatory caveat; the waitlist result with N99's history-dependence cleared-by-P2 framing.
- **Leakage:** the MIA decay measurement, N108's own sentence — *"stops answering before it stops leaking"* — is a REQUIRED honest statement, not optional; N112 (overflow failure) enters as a scope clause; N127's capacity qualifier stays removed; N131: cite Guo Eq.(1), ⛔ no Ginart-A.5 sentence without M1 (M1 is an open C1-CLOSE item — treat as not-available).

**4 — NEW: the three-state lifecycle (shipped).** Present via §0.13's APPROVED wording only: *"PROTECTED ⇄ ACTIVE → TRASH, 7/7 legs, kill-conditions committed BEFORE the verbs, every designed negative shipping with the named mutation that makes it fail; L4 labelled UNEXERCISED (0 refusals on the stream)"* — with its two mandatory mechanics riders: demotion is re-exposure to designed decay ⛔ never the trash region; the trash criterion is keyed on `read_hits` ⛔ never on depth. **Scope discipline:** this is a MECHANICS/capability demonstration on a toy synthetic build — ⛔ no VALUE number, no benchmark claim, no C2W8 cell number (`flags.promotable = false` material stays out); the C2W10 value surface is a DECLARED NOT-RUN, never a null.

**5 — The blast-radius sweep (V5's biggest hazard).** N149/N150: the pseudo-Goldstone tilt is refuted IN SIGN on a learned store, and any ε-lifetime dial sentence carries the `τ_max = Γ/2α` ceiling clause. Every lifetime/tilt/ε sentence in the rebuilt draft is checked against both entries; the designed-symmetry-precondition sentence stays in main text.

**6 — Standing obligations:** the §A20.5 substrate-scope sentence, once, in the paper's voice · the Q5/Q9 scale line (future-work section: scope choice + named scaling experiments, ⛔ no C3 number) · negatives distributed to appendices (Q4/C-9) · App E's erosion-as-restoration ship rules preserved (b/c novel, a/d cite-substrate) · anonymization discipline, ⚠ PLUS PALM's own extension: anonymization covers *"any supplementary or linked material as well, including code"* — no linked-repo reference survives that isn't the anonymized snapshot.

**7 — Venue-class header → PALM SHORT track (Head ruling, 2026-08-18; limits Head-verified from the PALM site same day).** **Hard limit: 4 pp main text, excluding references and supplementary materials — no overflow page.** The register is deliberate: an honest workshop contribution, not a main-track long paper — a 9-pp treatment would invite long-paper expectations (real-data experiments this short does not carry; that is the long paper's ground). Consequences for your cut decisions: ONE contribution arc in main text (the cube with its V-curve headline + the deletion/lifecycle capabilities in compact form); full tables, extended derivations, the complete negatives ledger, and all secondary instruments go to the supplementary/appendices. PALM site verbatim (2026-08-18): *"Full-Length Papers: Up to 9 pages (excluding references and supplementary materials). Short Papers: Up to 4 pages (excluding references and supplementary materials)."*

## Guards
- ⛔ CM-16a/b split, never "CM-16" whole · ⛔ 13.9× · ⛔ "certified" deletion · ⛔ "CLU-former" · the name-debut form unchanged.
- Toy-compositional substrate: the Add.10-F3 false-friend care applies in reverse — nothing in V5's designed/lifecycle material is the demoted family, and no scope sentence should imply it is.
- Every quantitative statement gets a flag-provenance row; every number traces to an output or a registry row.

## Acceptance criteria
1. CHANGELOG entry: the rebuild map — every section with its evidence sources and authorities; the estimated main-text page count stated (hard limit 4 pp); the main-text-vs-appendix cut map listed.
2. Blelloch–Golovin attribution present at every exact-deletion site; N108's sentence present; the lifecycle quoted in §0.13 form with both riders; zero VALUE claims on lifecycle/deletion mechanics.
3. Never-quote sweep printed (per-file, positive-controlled) over the finished draft vs matrix §0.1–§0.14.
4. Zero edits outside `papers/v5-short/`.
