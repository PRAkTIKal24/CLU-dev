# doc-curator-w25-sync — wave-25 transfer-doc synchronisation

**Agent:** doc-curator. **Worktree: NOT needed** (no `chlu/` code). You own ALL transfer docs
this pass: `negative_results.md`, `claims_matrix.md`, `research_roadmap.md`,
`philosophy-synthesis.md`, `HEP_primers.md`, `future_work.md`. No parallel curator this wave.

**Sources (read all five + this file):** `.claude/outputs/{r2-geometry-revival,
cl-entry-build, lattice-sharded-store, order-independent-placement,
mia-decay-measurement}.md` — every number below is verified against raw outputs by the Hub;
quote from the reports, not from memory.

---

## ⭐ DIAL DECLARATION (protocol §7)
- **Dial:** none — documentation pass. Declared for form's sake per §7: this task moves no
  scientific claim; it records w25's rulings verbatim.
- **Falsifies / does NOT falsify:** N/A (no measurement).

## ⭐ NEW STANDING DUTY (addendum §A4, baked in from this pass on)
At every curator pass, **check every task file created since the last pass for §7
dial-declaration compliance** (a DIAL DECLARATION block at the top: dial · laundering
control · falsifies · does-NOT-falsify) and record the result in your report. For this
pass: audit the five w25 task files (`cl-entry-build.md`, `r2-geometry-revival.md`,
`lattice-sharded-store.md`, `order-independent-placement.md`, `mia-decay-measurement.md`).
The Hub's reading is all five comply (each spoke echoed its declaration); verify and record.

---

## 1. Negatives registry — NINE new entries (Hub-proposed tiers; word them, Hub confirms)

| # | source | one-line content | tier |
|---|---|---|---|
| **N100** | r2geom §8 (text supplied there, adopt near-verbatim) | the learned-capacity ceiling is a **read-out REACH condition** (well width bounded below by σ_q, above-usefully by \|a\|_max/κ, κ≈3); width route to R2 CLOSED (0.30→0.15 drops K_learned(4) below 16, 3 seeds, 28 SE); sep/width is a perfect d≥4 *correlate* (boundary 2.30–2.49) but NOT the cause (forced ratio 4.90 fails at 0.594); w23 walls sit on the excursion's far side (half excursion: d4K32 0.824→1.000, d4K64→0.9922, both widths) — **mechanism result, no K_learned quotable at pscale≠1** | **A** |
| **N101** | cl-entry | the CL entry **wins the rehearsal-free Class-IL class on Split-MNIST** (0.707±0.012 vs EWC 0.196, +0.510, 3 seeds) **and is laundered** by kNN-in-φ at matched memory (−0.036); the win is over a class whose incumbents fail by construction; deficit vs iCaRL −0.153 filed per CM-23(n); the store's own axis is **forgetting** (BWT −0.169 vs −0.99 parametric; better than ER at matched items with 24.5× fewer floats) | **A** |
| **N102** | cl-entry | **Split-CIFAR-10 is a NULL** (0.149±0.013, below LwF 0.162): the failure is the **feature space** (kNN over the same PCA-32 φ = 0.21; strict-φ cost +0.001 — strictness does NOT bite, refuting the w24 expectation), not the stream discipline; entry scope = MNIST-class data until a CIFAR-capable φ exists | **A** |
| **N103** | cl-entry | **first non-losing anytime read**: in the R3-native regime (pixel corruption, φ store — no constructible mask oracle) gated retry **TIES** matched-compute feedforward-in-φ (+0.8±1.6 pp, 6 seeds) and beats random-kick +4.6±2.2 pp 6/6, ensemble +4.4, ungated collapses to 0.000, auto-stop 1.40±0.20×; w24 was −3.5…−38 pp in 8/8. ⚠ one dataset, one corruption level; τ is NOT the binding gate element in φ-space (1-seed; behavioural difference from w24) | **A** |
| **N104** | shard §2 | **settling-displacement routing (R3-router) is BROKEN on the shipped flat-vacuum geometry** — 1.000/0.549/0.236/0.228 at N=1/2/4/8 while pre-settle energy (R2-router) stays 1.000 — `BallRegisterPotential` is flat inside the ball so forces ≈0 in every shard while energies differ by 4 orders; only R2 + the classical registry survive | **A** |
| **N105** | shard §1 | **sharded additivity is real on the write side and partly spent on the read side**: d=6 K=64 fixed by sharding (0.9251±0.0030 R2+N98-init vs monolithic 0.8802±0.0075 at identical init/atoms; 0.9043 vs 0.8288 registry-routed, no init change); ~60% of the monolithic deficit at d=6 is routing; write additivity literal (separate parameter trees, zero sync) and 4–5× cheaper wall-clock; ⛔ never "capacity multiplies by sharding" as a dynamical claim; `K=32N` NOT unclamped | **A** |
| **N106** | shard | the d=4 K=32 wall is **geometry, confirmed with routing removed** (RG 0.8932/0.8984, blank-controlled, both arms) — quote as "0.89–0.90, does not clear 0.90", not a comfortable failure | **B** |
| **N107** | shard | the w23 atom floor `512·√2^d` is **per-STORE, not per-item** — parameter-matched N-way sharding is automatically N× starved; matched-arm wins are conservative, N≥4 collapses are NOT capacity results (N92 protocol) | **B** |
| **N108** | mia | **amplitude decay does not reduce an exact adversary's per-example distinguishability at any point in an item's life** (white-box AUC 1.000 at all 18 levels; what decays is effect size, gap = 0.3935·A to 4 dp, d′ 1560→79.6); the graded curve exists only vs a resolution-limited adversary (A₇₅ = 2.57·σ_obs, confirmed to ~2%) or in **retrieval geometry** (R₅₀ 1.146→0.752, 1.52×, vs TTL-dict constant step 0.77 — the differentiator needing no adversary caveat); pre-registered as the expected outcome (PREREG P3) | **A** |

## 2. Amendments to existing entries (dated HUB RULING blocks, never rewrite history)

1. **N92 + N96 — the contested mechanism is SETTLED, and neither contestant won.** Add dated
   blocks: the ceiling is neither "the write operator (d-independent)" nor width-locked
   packing; it is the N100 reach condition. `lattice-capacity-theory` §5.2 is **REFUTED BY
   EXPERIMENT**; §4.2 survives as correlate only. The N98 measurement (see 3 below) is a
   second, additive contribution (atoms starting inside other items' wells). Measured numbers
   in N92/N96 stand.
2. **N97 — EXTEND** (per N104): "post-settle energy" → "post-settle energy (R1-router) AND
   settling displacement (R3-router) are both broken; only pre-settle energy (R2-router) and
   the classical registry survive." Keep the original text, add the dated extension.
3. **N98 — add the w25 measurement + promotion note**: localized init measured as a
   first-order lever (+0.051 monolithic / +0.082 sharded at d=6 K=64, 3 seeds; repairs the
   dynamical router 0.913→0.970; ships OFF, bit-identical when disabled). Do NOT promote to
   default — Hub/Head decision pending a joint measurement with the width finding (they may
   be the same effect twice).
4. **N99 — two updates in one dated block**: (a) the history-dependence gap now has a
   MAGNITUDE: the allocator trace is a near-perfect post-evict membership oracle (z_hole AUC
   0.99985±0.0007, TPR 1.000 @ FPR 1%, at 7/8 occupancy — the dominant residual leak); (b)
   exit (b) is DELIVERED at theory+reference level: **PGCP** (priority-greedy canonical
   placement, SHI hashing transplanted to the continuous designed store) — Store(S) proven a
   set function, delete = set-minus bit-exactly, decay/permanence commute (Thm 4),
   permutation harness green at 0 tolerance, packing price NEGATIVE (61/64 = 0.953 vs RR
   42.8/64), delete churn 2.84 moves/delete at full load. Blocking before any deletion
   sentence ships: engineer landing (`Controller.delete` does not exist), prior-art citation
   verification (Naor–Teague, Blelloch–Golovin, Hartline — asserted from training knowledge),
   and the numeric acceptance test **drive z_hole AUC 0.99985 → 0.5** re-run on the MIA
   harness. "certified" stays forbidden.
5. **N91 / controller-mvp §3(b) correction (mia D2):** the "leaky item retention 1.00 through
   8 ticks" line is a BEST CASE (1-element-site basin test vacuous + most decay-robust
   payload probed): full-basin, 8-value re-measurement gives **0.886±0.19 at A=0.0608, 0.500
   for a=+1**. The permanent-item row (1.000 ∀t) is unaffected. Correct every quoting site.

## 3. Claims matrix → v2.4

- CM-23: add the CL-entry block — the three computed sentences (wins class +0.510 / deficit
  −0.153 CM-23(n) / laundered −0.036) **always travel together**; CIFAR null scope clause;
  R3-native tie wording (never "wins", "ties the honest floor in the one regime with no
  constructible oracle").
- CM-22: add (from mia + placement) — retention≠distinguishability both halves travel
  together; "the store stops answering before it stops leaking" is safe wording; any
  deletion-flavoured sentence blocked until the three N99 blockers clear.
- **Do-not-quote additions:** "0.953" as measured retention (it is deterministic admission ×
  gradient-flow per-admitted, pending the real-read rematch) · "d=8 K=64 = 0.883 FAIL" (no
  longer a wall: 0.9121±0.0029 in-run, 0.9067±0.0068 w24) · any `K_learned` at pscale≠1 ·
  "the ceiling is the write operator" / width-lock-as-cause · "capacity multiplies by
  sharding" · "decayed items answer perfectly up to the floor" · "leaky retention 1.00
  through 8 ticks" unqualified · decay-reduces-distinguishability per se · "the item is gone
  after evict" under shipped placement (allocator trace + uncleaned rows).

## 4. Roadmap → v0.8 (the deferred status refresh + w25)

Per-R statuses from addendum §A2/§A3 + w25: **R1** — reframed to scheduled retention +
(pending landing) exact store-level deletion; MIA line measured (N108); PGCP theory done;
blockers listed above. **R2** — width route CLOSED (N100); the open route is the **read-out
excursion** (multi-channel payload, Head decision pending) + sharding facts (N105/N107).
**R3** — first non-losing native cell (N103); mechanism 5-wave clean. **R4** — the CL entry
exists; MNIST won-but-laundered; CIFAR-capable φ is the single blocking dependency.
**R5/CLULayer** — unchanged, Phase 4.

## 5. Ledger + primers

- `philosophy-synthesis.md`: Chapter 8 ⟲ dated w25 addendum — the wave in one arc: the
  contested mechanism resolved as a third thing (reach); the CL entry exists and the
  four-dial thesis survived contact with a real benchmark exactly as the pattern predicted
  (baseline owns the headline, CLU owns forgetting/anytime/retention axes); deletion went
  from "blocked by history-dependence" to "a proven placement rule + a numeric acceptance
  test"; the lifetimes dial got its adversarial accounting (honest both ways).
- `HEP_primers.md`: plain-language records for (i) the reach condition (a narrow deep well
  is invisible from far away — the read launches from payload 0), (ii) PGCP (parking by
  fixed priority: any arrival order parks the same cars in the same spots), (iii)
  retention-vs-leakage (the store stops answering before it stops leaking). Imagery intact.
- `future_work.md`: fold w25 follow-ups — multi-channel payload experiment; CIFAR-capable φ
  (ae arm, then conv/SSL); joint N98×width measurement; PGCP landing + rematch cell;
  occupancy sweep on the allocator trace; D3 payload-dependent-lifetime fix (theorist ruling
  first); LwF re-tune before the baseline table is published; multi-seed of the τ-not-binding
  observation.

## 6. §7-CURRENT defects (handover doc is Hub-owned; you list them in your report for the Hub)

mia D1 (evict leaves `centers`/`payloads` verbatim — wording + one-line engineer fix), D2
(vacuous basin in `retention_of` — engineer fix, moves controller-mvp §3(b) row), D3
(lifetimes dial payload-dependent, r=−0.85 with a_i² — theorist ruling needed before any
potential change), cl-entry item 7 (w24 `_feedforward_ladder` clip-TTA invalid in φ-space —
use `_feedforward_ladder_phi`).

**Report:** `.claude/outputs/doc-curator-w25-sync.md` — per standing format: what changed
per doc, N-tier proposals vs Hub proposals (flag any disagreement explicitly, w24 precedent:
you may override with reasons), the §7-compliance audit result, anything you refused to fold.
