# Task: v3-referee-2 — clean-pass re-review of the revised V3 short (w14)

- **Agent:** `paper-referee` · **Output:** `.claude/outputs/v3-referee-2.md`
- **Read first:** protocol · Charter (**C-1 is REVERSED — no audit-confession paragraphs**) · `.claude/claims_matrix.md` **v1.7** (esp. **CM-9 = RESOLVED**, CM-13) · your prior report `.claude/outputs/v3-referee.md` (MF-1, SF-1…4, N-1…3) · the evidence that settled MF-1: `.claude/outputs/v3-interference-extra.md` · the revision report `.claude/outputs/v3-revision-3.md` · then the revised draft `.claude/papers/v3-short/draft.md` (v0.4).
- **Context:** you graded V3 **"weak-accept, borderline→REJECT if MF-1 is left as written."** MF-1 has since been **refuted as a weakness and reframed**: 12 seeds × 6 values of N (N∈{2..16}) show the slopes genuinely diverge (modular b=+0.46±0.31 vs monolith b=+1.18±0.17, Welch p=3.3e-4, non-overlapping CIs, 11/12 seeds); the published "×2.56 vs ×2.18 parallel" was **3-seed noise** (per-seed CI ±2.27). The structural claim is now an **exact identity**: R ≡ 0 off the coupling graph (0/4,656 nonzero across 72 runs) ⇒ **S = deg · R̄_edge to 6 decimals**. This is the clean pass.
- **Repo state:** wave-13 is live; the repo tree is **read-only for you**. The v3 draft has been stable since 20:17. Report only — never edit drafts.

## Specific checks (beyond re-verifying MF/SF closure)

1. **MF-1 — did the reframe actually land?** The draft should lead **structural-first** (the boxed identity S = deg·R̄_edge; R_far ≡ 0 exactly), with the empirical slope divergence as *corroboration*, not as the headline. Per **CM-9 RESOLVED** the sanctioned wording is **"degree-bounded vs width-linear"** — the phrases **"O(1) in N"** and **"flat in N"** are now **forbidden for a chain**. Grep the abstract, contributions, §3.2, §4 and every figure caption for residuals of the old growth-rate framing.
2. **The chain residual — verify it is stated preemptively, not buried.** A ring (deg ≡ 2) is statistically flat (b = +0.071 ± 0.183); a **chain is not**, because mean degree rises with N. The draft is supposed to own this *before* a reviewer finds it, using the degree-normalized b. If it is only in an appendix, that is a **SHOULD-FIX**; if it is absent, **MUST-FIX** (it is the one reviewer-reachable inversion left).
3. **The block-monolith control (your N-3).** The discriminating control now exists and is decisive: the firewall is **parameter separation, not block structure** (`block_untied` gives S ≡ 0 exactly; deep-sets/tied per-pair R̄ = 0.97 — **6× worse than the naive monolith**, and therefore the candidate *primary foil*). Confirm this reaches §3.2 main text with a one-sentence pointer, per your N-3. Confirm capacity is stated as decoupled.
4. **SF-2 — the sync bound.** Confirm the draft now says **"≤7.5% relative to the registered prediction"** and has not rounded back up to a rounder-looking "≤8%".
5. **SF-1 / SF-3 / N-1.** S=1.38 softened away from a storage-capacity reading; per-figure `[verification]`/`[evidence]`/`[structural]` grade labels present; scope qualifiers inlined per C-5.
6. **CM-13 (reversible O(1)).** If §3.5 claims the 946× peak-memory result, confirm the two scope clauses ride with it: **exact only at γ=0** (γ>0 gives a finite horizon (1−γ)^−n), and it is **gradient mechanics only — not yet wired into `train_chlu`**.
7. **Known-pending — flag once, do not spend findings.** (a) One bibliography item is still unresolved and awaits the **Jul-11 venue scout**; (b) the theory note is still `(Anonymous, 2026)` pending the Head's title/author call. Both are standing dependencies, not draft defects. (c) SF-4 (six contributions is over-budget) is deferred by policy to the **C-10 pruning pass** once page limits are known — do not re-raise it as a blocker.
8. **New-inconsistency hunt.** The headline `fig_scaling_curve.png` was regenerated at 6 points and **overwrote the 2-point figure at the same path**. Verify no caption, number, or sentence still describes the superseded 2-point version.

**Report:** verdict (is the near-reject retired? is V3 submission-ready modulo pruning + the arXiv id + the pending bib string?) · residual MUST/SHOULD · an explicit statement of whether **MF-1 is now CLOSED** · the three sentences a hostile reviewer would quote.
