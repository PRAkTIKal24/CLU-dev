# c2w11-physics-organizer — the label-free organizer, the set-level read, the graded-novelty channel, the anytime curve

**Campaign 2, wave 11 (THE COMPOSITIONAL WAVE). Agent:** experiment-engineer. **ONE worktree (wt1,
re-used after `c2w11-substrate-and-kills` frees it).**
Branch **`c2w11-physics-organizer`** from **`main @ <the Hub names the base at spawn — the commit
carrying spoke A's merge>`**, which is **`2e1cdb2` or later by construction**. ⛔ **NEVER pass 3's
`9e0bb25`** — four C2W8-close repairs bind your measurements: **A3 is DIAGNOSTIC in census code and
G-DRIFT is TWO-SIDED (drift → 0 now FAILS)** · **`d_safe` and the G-ADDR cue are sized on the STORE
population** (and an arm's co-scaled width with them — read `70b11ae`) · **`covered`/`n_never_read`
are SPLIT — use `settle_covered` and the settle-side telemetry for every addressability statement** ·
**the census refuses a non-selected width**. ⚠ **The shared checkout sits on a LIVE spoke's branch**
(`pilot-ttt-nan-and-d5-wiring`). Work **in your worktree**, cwd = the worktree on every command.
Writes `.claude/outputs/c2w11-physics-organizer.md` + artifacts to
`.claude/outputs/c2w11-physics-organizer/`.
**Budget:** ≈ 2.5 days. ⭐ **You produce the PHYSICS side of all three VALUE legs. You do not
adjudicate any of them** — the swap is computed at the wave review from your artifacts and spoke C's.

---

## ⛔⛔ MECHANICAL PRECONDITIONS — VERIFY ALL THREE ON DISK BEFORE ANY CODE

> 1. ⭐ **`.claude/outputs/c2w11/GATE-SEMANTICS-C2W11.json`** with
>    **`kills_all_discriminating_passed == true`** ← **the gate, per charter ADDENDUM 13**
> 2. **`.claude/outputs/c2w11/FROZEN-INTERFACES-C2W11.json`** — the frozen family/launches/φ/reader
>    class/byte-ledger/`v3_budget_grid`/`k8_structural_split` (⚠ its own `kills_all_passed` is
>    **superseded** by (1); read the *data* from it, the *verdict* from (1))
> 3. **`.claude/outputs/c2w11-loss-package/LOSS-PACKAGE-DONE.json`** with
>    **`loss_package_complete == true`**

If **any** is missing or present-and-false → **report `BLOCKED`, write nothing, take no worktree, run
zero cells, make zero git footprint**, name which failed, hand back. ⛔ Do **not** judge whether a
failing item matters — the Hub's call.
When all three are true: **quote (1)'s per-leg table and (3)'s six-term table into your §1**, and take
the frozen family, launches, φ, reader class and byte-ledger template **from the JSONs, never
re-derived**.

### ⭐ WHAT THE GATE MEANS, AND WHAT IT DOES **NOT** MEAN (charter ADDENDUM 13, Head 2026-08-10)
**K3, K4 and K5 ABSTAINED** — they were scored on an arm structurally incapable of producing both
outcomes (spoke A's own `vacuous` flags, 3/3 cells; `all_cells_vacuous = true`), so under **§A37**
(*a gate leg must be scored on an arm capable of producing both outcomes*) they **neither unlock nor
block**. The gate is the AND over the **discriminating** legs — **K0, K1, K2, K7-CAP, K8 — all
passed.** Consequences that bind you:
- ⛔⛔ **THE WAVE HAS PRODUCED NO K5 VERDICT.** Nothing about **table-expressibility** may be quoted
  from spoke A anywhere (Head condition (d)). **You produce the wave's first one.**
- ⛔ **Family soundness is NOT established** — K3 and K4 abstained. It rests on **your** K4-at-full-ψ.

### ⛔⛔ YOUR FIRST ACT, BLOCKING, BEFORE ANY OTHER CELL
> **Re-run K5 on the ORGANIZED arm.** K5 is defined on the physics arm **including its training
> signal** (`PREREG-TierII.md` §4.1/§10); spoke A ran with **`organizer ⛔ NOT RUN`**, which is why it
> abstained. **On your arm K5 is BLOCKING and it is the first thing you report.**
> **Registered abstain rule (δ fixed, never a judgement call):** K5 abstains iff **BOTH** the per-item
> table **AND** your best read score **≤ chance + δ**, with **δ = 0.01** (≈5.1 metric grains at
> 1/512; 10× below K5's own 0.10 margin bar). Otherwise K5 **scores**, and a failure is a real verdict.
> ⛔ If it abstains again on a trained arm, **report that and stop before the VALUE legs** — it means
> the organizer did not lift the read off the floor, which is a finding and not a licence to proceed.

**Binding documents, read first, in this order:**
1. `.claude/outputs/c2w11/PREREG-C2W11.md` **IN FULL** — you implement **§5 (V1, V2, V3, physics
   side)**, **§6 M3 and M7**, and **§7's traversal half**.
2. charter **ADDENDUM 12 §A33–§A34 IN FULL** — especially **§A33.1** (MECHANICS/VALUE), **§A33.2**
   (the three VALUE legs), **§A34.1** (binding is the READ + ψ's job), **§A34.4** (the graded-novelty
   read — the Head-flagged novelty piece), **§A34.6** (the training regime), **§A34.8** (G-ADDR is
   MECHANICS-only), **§A34.9** (the loss package you are implementing).
3. `.claude/outputs/c2w11-loss-package.md` **IN FULL** — the theorist's formalization is your spec.
4. `.claude/outputs/c2w11-substrate-and-kills.md` — your substrate, its deviations and its NOT-RUNs.
5. `.claude/outputs/orgdiv-prereg/PREREG-TierII.md` §0, §1, §6, §7 · charter **§A20.3(c)** (the
   four guards on the k-particle learned-launch head) · **§A28.1** (the designed write→φ gradient).

### ⚠⚠ THE PAYLOAD-REPAIR TRAP — it will silently corrupt every score if you miss it
Spoke A's repair moved **`payload_radius` 1.0 → 0.60** (`atom_payload_init_radius` co-scaled) to close
a measured reach gap (`‖v_j‖ / capture` **1.172 → 0.692**; the needed well was outside its own basin
by arithmetic). **`tol`, `chance` and every y-scale are homogeneous of degree 1 in the payload radius
and MOVED WITH IT:** post-repair **`tol = 0.28696`**, `chance_per_seed = [0.0, 0.001953125, 0.0]`.
⛔ **`run1` artifacts carry the PRE-REPAIR `tol = 0.47827`.** **Read `family.tol` and
`family.chance_per_seed` from `FROZEN-INTERFACES-C2W11.json` on EVERY cell; never hard-code or carry a
pre-repair constant.** ⚠ The family's **difficulty is unchanged** — K2's payload half is
scale-invariant and spoke A **verified** that rather than assuming it.

⭐ **And inherit the mechanism, because it reframes what you are testing.** With reach closed, the
settle went from **destroying** launch information to **preserving** it: the M6 occupancy dividend
moved **−0.1567 ± 0.0052 → −0.0015 ± 0.0026** (a TIE), settle precision **0.0736 → 0.2288**, distinct
wells **3.807 → 3.990**. ⛔ **The settle is information-NEUTRAL, not positive**, and this was measured
on an **UNORGANIZED** store. **The remaining blocker is LAUNCH-HEAD PRECISION** — occupancy precision
**0.2303**, correct-and-distinct **0.92 of 4**; the launch head does not select the needed well for
~3 of every 4 needed wells. ⭐⭐ **Training φ/placement toward semantic metric structure is loss term
(a) — YOURS — and it is the named mechanism that could move that number. That is this spoke's central
question.**

## ⭐ DIAL DECLARATION (protocol §7, C2 form) — echo before your first result
- **Dial / pillar:** **TIER ii — the physics organizer.** ⛔ You produce the physics arm's numbers for
  V1/V2/V3 and your own MECHANICS legs. ⛔ **No `OD`/`OD_min`, no swap verdict, no paper number, no
  tier-ii verdict, no full-CLU verdict.** The VALUE legs exist **only at wave level** and only under
  the organizer swap (§A33.1).
- **Laundering control:** ⛔ **NO launder margin is a pass condition anywhere** (§A33.1). Report the
  **launch-only launder** (the store must be shown to add value over its own launches — §A20.3(c)'s
  guard, now DIAGNOSTIC), the **settle-deleted launder** (inherited tier-i diagnostic) and the byte
  ledger beside every reading, all labelled DIAGNOSTIC.
- **Falsifies:** a MECHANICS leg that cannot fail its designed negative does not ship. V2's floor
  (`AUROC_phys ≤ 0.55`) makes V2 a null **regardless of the swap** — report it, do not tune it away.
- **Does NOT falsify:** a table-like inference read (**explicitly permitted on both arms**;
  post-training reduction to a table is a **computational win**, §A14.1) · losing to a table on SEEN
  queries · a dividend ≈ 0 on the tier-i launder.
- ⛔ Depth is not feature importance (§A23.5 ACTIVE). ⛔ N94 discipline. ⛔ Every γ statement is
  **read-budget-scoped**. ⛔ **Wells are never named semantically** — copy `PREREG-TierII.md` §2.6's
  sentence verbatim and obey it in code comments and figure captions.

---

## WHAT YOU BUILD

### (1) ⭐ THE TRAINING REGIME (§A34.6, RATIFIED — this is binding and it is counter-intuitive)
> **The organization objective is LABEL-FREE. Supervision enters ONLY at the READ HEAD.**

Measured basis, and quote it in your report: **the pass-3 inversion** — task supervision built the
**WORST** address geometry beyond 2 SE (`simclr − randconv` A1 = **−0.1406 ± 0.0508**, 0/3 seeds;
`simclr − pca` = **−0.1276 ± 0.0589**, 0/3), while **unfitted `randconv` bought the geometry for
free**. ⇒ a purely supervised organizer is the **measured wrong bet**, and **the address block is its
own head** (task features ≠ address features).
Shape: **self-supervised organization** (co-occurrence / reconstruction-through-ψ / prediction) +
**a supervised downstream head for the benchmark** + ⭐ **ONE optional weakly-supervised-organizer
arm raced as an ablation** (see (6)). **Downstream heads are swappable; the store is the foundation
object.**

### (2) The loss package (§A34.9 (a)–(e), per the theorist's spec)
Implement terms **(a) label-free organization** (through the write — **the DESIGNED write→φ
organization gradient**, §A28.1: routed through the **write objective**, byte-ledgered, auditable,
declared in the arm's ledger) · **(b) sharing/refresh** · **(c) curvature-shape** · **(d) set-level
compositional read** · **(e) calibration**. ⛔ **(f) kinetics is NOT built this wave** — declared
NOT-RUN; term (c) exists so that what kinetics would select on is defended rather than assumed.
⛔ **Every term's coefficient at 0 must be BIT-IDENTICAL to the shipped objective**, pytest-asserted
(the C2W2 invariant, and the K6/K2-fingerprint pattern).
⚠ **The designed write→φ gradient must be distinguishable from the accidental leak.** Banked: the
accidental leak is **27 %** of layer-0 φ gradient flowing through the write whenever
`atom_place_radius > 0`, and it is an unaudited entanglement with the **outer** loss — the same
coupling that erodes the store. **Assert the designed channel is live and the accidental one is
accounted for**, per the theorist's stated assertion.
⚠ **Staged store-then-launch co-training is mandatory** (w20). Banked and load-bearing: **the
organizer is untrainable until the wells exist** — grad norms **1e-10 – 1e-9** at init, **O(1)** after
the write.

### (3) ⭐ THE SET-LEVEL READ + ψ (§A34.1 — binding is the READ's job)
The `k` feature-factored particles land; a **DeepSets pooled ψ** binds them into the downstream answer
with a **likelihood weighted by captured-vs-scattered particles**.
⛔ **DeepSets ONLY.** `AttentionPsi` is **QUARANTINED for trajectory input** (C2W2 reconciliation 1;
live in `chlu/core/psi_readout.py::AttentionPsiLeakError`) — the pooled DeepSets ψ is explicitly
**not** quarantined. **Any attention-ψ number is a declared NOT-RUN this wave.**
⛔ **No binding structure is built** — the latent space may be disjoint/independent per feature.
Co-activation / wormhole edges are the **C2W9** pointer, not yours.
⛔⛔ **RE-RUN spoke A's frozen K4 harness at FULL ψ capacity and with the novelty head live**
(`k4_full_psi_obligation` in the frozen-interfaces JSON). **This is BLOCKING.** Rationale: `y = Σ v_j`
is a **linear code in `1_A` with `N_a·m` parameters** — an OLS fit on the true indicator scores
**1.0000** exact-set with `‖v̂−v‖∞ = 4.25e-15` **on a blank store**. A ψ with enough capacity can
therefore solve the family with no store at all. **The blank-store and query-only controls at full ψ
are the mechanical guard, and the measured leak SETS your ψ budget — you do not choose it.**

### (4) ⭐⭐ THE GRADED-NOVELTY READ (§A34.4 — flagged by the Head as a major novelty piece; VALUE leg ii)
"Close to dog, not a dog, snout anomalous" = **per-particle capture / depth / residual diagnostics
composed per feature + overlap-as-confidence** (§A20.3(c)). Trainable via **feature-dropout-as-
pseudo-novelty** (loss term (e)).
Emit both registered statistics (`PREREG-C2W11.md` §5 V2): **V2a per-feature novelty AUROC** (with
its `> 0.60` floor and its `≤ 0.55` null condition) and **V2b set-level answer ECE**.
⛔ **DESIGNED NEGATIVE, PYTEST-ASSERTED (mandatory):** a store with **permuted payloads** must give
`AUROC ≈ 0.5`. A novelty channel that cannot report chance on a scrambled store does not ship.
⚠ Overlap-as-confidence has a banked shape to honour: **confident ⇒ the `k` particles collapse to `F`
unique; unfamiliar ⇒ scattered guesses.** Report the collapse statistic beside the AUROC.

### (5) ⭐ NAVIGABILITY (VALUE leg iii) — ⭐ **RE-FORMED: THE SWAP-DIFFERENCED CURVE**
V1's metric vs **read compute on ONE shared ledger: particles-evolved × Verlet steps (+ hops if any)**.
Emit the **full curve**, **≥ 6 budget points, 5 seeds**. ⛔ **Quote the curve, not the endpoint.**
- ⭐ **V3-PRIMARY is VALUE and it is the SWAP-DIFFERENCE, not the curve's existence.** The organizer
  swap hands the null arm the **same reader class** — it changes **who decides where the wells go, not
  how they are read** — so **spoke C's N1/N2/N3 stores are read with the IDENTICAL k-particle anytime
  read and therefore HAVE curves too.** Emit your curve **at the registered budget points spoke C will
  also use**, so `OD_V3(b)` is computable at the review. ⛔ **You do not compute the difference.**
  ⚠ **Coordinate the budget-point grid through `FROZEN-INTERFACES-C2W11.json`** — if it is not frozen
  there, freeze it in your `PREREG.md` and say so loudly, because a mismatched axis voids the leg.
- **V3-MECHANICS (demoted from primary, and it still carries real information):** the curve is
  **monotone and non-flat** — a navigation dial exists. ⭐ **N199 measured a FLAT curve when the store
  carried nothing:** *a memory that carries nothing cannot be read better by reading it longer.* So
  **non-flat is positive evidence something readable is in there, and FLAT is a MECHANICS FAILURE** —
  if it is flat, say so and **diagnose it** rather than reporting a VALUE number on top of it.
- **V3-REPORTED, mandatory in every table, never primary:** the **compute-to-parity ratio against
  EXTERNAL baselines**. ⛔ **Label it as what it is — a READ-COMPUTE RATIO** (mult-adds), not wall-clock
  and not training cost. ⚠ Banked reference: the C2W5 physics read cost **3 360× N1's
  matched-capacity static read (6.88e7 vs 20 480 mult-adds) at a TIE**.
- ⚠ **Positioning, carried:** the anytime curve is a **SHAPE claim, not a uniqueness claim** (§A3 —
  DEQs / EBTs / Titans-Revisited occupy it). "We have a curve and they don't" (vs the static nulls
  **as readers**) is **§8.3-barred as a primary claim** and stays in the "and also" position. **That
  is a different comparison from V3-PRIMARY**, where the competition is present by construction.

### (6) ⭐ THE WAVE'S SINGLE ABLATION — the weakly-supervised organizer arm (§A34.6)
One extra **coefficient** on the organization objective (labels shape well-sharing), raced on the
**same cells** as the label-free arm. ⛔ It is a **coefficient, not a new capability** — §3.1's
one-capability-at-a-time doctrine is not violated and must not be stretched further.
⭐ **Its value is that it tests the A31.4 inversion at the ORGANIZER level:** is label information
helpful, neutral, or **harmful** to address geometry when it enters the organizer rather than the
encoder? Register your prediction before you run it.

### (7) The MECHANICS legs you own: M3 and M7
- **M3 — per-feature G-ADDR** (§A34.8: **MECHANICS-ONLY, permanently barred from VALUE duty**). Does
  feature `f`'s particle resolve to feature `f`'s well **AND** land inside that well's **measured**
  SC-6 capture radius? ⛔ **Report `margin_in_SE` beside every boolean** (pass 3: `randconv` scored
  31/31/29 against a 32/128 threshold — it failed **by ONE read** on 2 of 3 seeds; a discrete
  threshold with no margin turns a tie into a verdict). ⛔ **`any_basin` is reported and is NOT the
  leg** — banked: on 4 of 9 pass-3 cells `any_basin ≥ 0.98` while A1 ≤ 0.24.
  **Designed negatives, pytest-asserted:** (a) a planted permutation (same store, same queries, wrong
  declared targets) must score 0 while `any_basin` stays 1.0000; (b) a **narrow-wells** rig
  (retrievable at its own sites, unreachable from a cue) must fail.
  ⚠⚠ **THE D2a TRAP, measured at design level:** across pass 3's 9 cells **ρ(A1, G-DRIFT) = −0.967**
  and **ρ(A1, settle↔launder agreement) = +0.933** — the completed gate's score was almost a
  **monotone function of settle-collapse**. ⛔ **Drift → 0 is never a TARGET**, and if your M3 leg
  cannot fail on a planted near-zero-drift table-like store, it does not ship.
  ⭐⭐ **BUT THE TWO-SIDED G-DRIFT FLOOR IS A *DIAGNOSTIC* HERE, NOT A BLOCKER (charter ADDENDUM 13,
  Head ruling 2, citing §A13).** The placing write's measured site drift is **2.0e-6 × spacing** ⇒
  `fails_low_D2a_table_expressible` against C2W8-close's floor of `0.01 × spacing`. **§A13 EXPLICITLY
  PERMITS table-like inference reads on BOTH arms — that permission IS the reframe and is the reason
  the organizer swap exists** — so this is the **expected and permitted tier-ii configuration, not a
  failure.** ⛔ **Report the floor's verdict as a DIAGNOSTIC column; it may not block or fail any leg
  you score.** The floor **remains BLOCKING where it was built** (C2W8's capture gate — component
  mechanics hygiene) and **does not travel into tier-ii value scoring** (§A37's corollary: a leg built
  to police one question must not silently police a different one).
- **M7 — the curvature-shape term:** does a within-well **soft direction survive superposition**?
  ⚠ **§A4.2 REFUTED the tilt instantiation on a learned store** (tilt monotonically *reduces* `λ_min`,
  +0.099 → −8.28, two independent implementations, every family; a written site's vacuum residual
  0.140–0.343 vs a 0.167 random baseline; `λ = ε` holds only in the single-atom geometry it was
  specified in). **Measure it two-sided; do not assume it.** The shipped confinement floors the soft
  mode at **`2α`**, so `τ_max = Γ/2α` travels with every lifetime statement (α is the **ceiling**, and
  lowering α breaks the write).
- ⭐⭐ **M8 — THE CURVATURE-SPECTRUM CHECK, and it is MANDATORY, not optional polish.** At **end of
  training**, emit the **`λ_min` distribution** and the **participation ratio** at written sites, for
  the physics arm **and** the coefficient-zero arm. ⚠ **Banked trap you must not fall into:** undug
  wells report `λ_min ≈ 0.0993` **because `2α` is what `λ_min` reports when nothing was written** —
  mark the `2α = 0.1000` floor on the axis and define a soft direction as a `λ` **at or near the floor
  that is not the floor itself**.
  ⭐ **Why this is mandatory:** term (c) **CREATES** the soft directions a future kinetics wave would
  exploit, and **w20's law says undefended designed structure gets ERASED** — landing (c) now means
  C2W12 inherits **surviving** soft directions instead of fighting erasure. ⛔ **But (c) has no other
  consumer this wave**, so without M8 an undefended (c) could be **silently tuned to inertness and
  nobody would know.** This is the §A4.2 tilt lesson applied **prospectively**.
  **Designed negative:** the coefficient-zero arm must show **no excess** soft directions.

### (8) ⭐ THE C2W9 TRAVERSAL TRIGGER (you own the traversal half — §7 of the prereg)
Instrument, in flight: **does the evidence point outside the current particle's causal diamond?**
⛔ **If traversal failure fires at your registered threshold, APPEND a dated section to
`.claude/outputs/c2w11/TRAVERSAL-FAILURE-SIGNATURE.md`** (spoke A may have created it for coverage;
append, never overwrite) with the measured signature: mode, fraction of queries, per-feature
breakdown, reach radii, seeds. ⛔ **If it does not fire, do NOT create the file** and say so
explicitly. Register the threshold **before** the run. ⚠ Banked scoping input that does **not** itself
fire the trigger: `d ≥ 16` inertness was measured at a **fully honoured** atom budget ⇒ the binding
constraint is **REACH, not capacity**.

---

## FILE OWNERSHIP (declared)
**You own:** `chlu/core/psi_readout.py` (**additive** — the DeepSets set-pooling path) · **new**
`chlu/core/novelty_read.py` · **new** `chlu/experiments/exp_c2w11_organizer.py` ·
`chlu/training/losses.py` (**additive**) · **new** `tests/test_c2w11_organizer.py`.
⛔ **DO NOT TOUCH `chlu/cli/experiment_cmd.py`** — spoke A landed your `exp-c2w11-organizer`
subcommand stub in its first commit; wire your handler inside **your own module**.
⛔ **DO NOT TOUCH:** `chlu/config.py` (**zero C2W11 spoke touches it**) · `chlu/core/factored_store.py`,
`chlu/core/feature_launch.py`, `chlu/experiments/exp_c2w11_substrate.py`, `tests/test_c2w11_substrate.py`
(spoke A's — **read them, do not edit**) · `chlu/core/null_arms.py`, `chlu/experiments/exp_null_arms.py`,
`tests/test_null_arms.py` (spoke C's, running **concurrently**).
⛔ **DO NOT TOUCH — the live CSF3 pilot spoke's territory:** `scripts/csf3/` ·
`chlu/training/train_cluformer.py` · `chlu/core/blocks.py` · `chlu/experiments/exp_cluformer_pilot.py`.
⛔ **DO NOT TOUCH — C2W8-close's territory (READ-ONLY all wave):** `chlu/core/well_lifecycle.py` ·
`chlu/experiments/exp_well_lifecycle.py` · `chlu/core/clu_system.py` · `chlu/core/soft_certificate.py` ·
`tests/test_gate_addr.py` · `tests/test_well_lifecycle.py` · `tests/test_cifar_strong_phi.py`.
⚠ Anything you need changed outside your ownership is a **rider handed back to the Hub**, never an edit.

## Acceptance (mechanical)
1. Both preconditions verified **on disk, by you**, quoted into §1, before any code.
2. ⛔⛔ **ONE SECTION, BEFORE ANY VALUE NUMBER, CARRYING ALL THREE FALSE-POSITIVE GUARDS TOGETHER —
   `K4-at-full-ψ` · `K7-CAP` · `K8` — never scattered** (charter ADDENDUM 13, Head ruling 2's stated
   consequence). ⭐ **With the G-DRIFT floor demoted to DIAGNOSTIC and K5 moved to you, these three
   carry the ENTIRE false-positive load of the wave.** ⚠ And **K4's store-only form ABSTAINED**
   (vacuous 3/3) — so **your K4-at-full-ψ is not a re-confirmation of a green control; it is the FIRST
   informative run of that control in this wave, and family soundness rests on it.** A leak above
   chance + 0.05 **voids the family** and you report that, not a score.
   ⚠ This section bears directly on the Hub's registered **Q11 — `P(ψ does the work | V1 clears)` =
   **0.15** with both K4-at-full-ψ and K8, **0.40** on the measured guard alone.
3. **V1 / V2a / V2b / V3** all emitted for the physics arm and the weak-supervision arm, **5 seeds**,
   with `mean ± 2 SE` and the per-reader curve for V1, and **V3 at the frozen budget-point grid spoke
   C also uses**. ⛔ **You compute no `OD` and no swap verdict.**
3b. ⭐ **THE K8 CONFIRMATORY CELL:** score **V1 once at spoke A's frozen `k8_structural_split`
   (`K < N_a`)**, headline configuration only. ⛔ **A V1 clear at `K > N_a` that does NOT survive K8 is
   reported as a ψ-capacity artifact, not as a tier-ii result** — state the sign agreement explicitly
   either way. Rationale to carry: **ψ-does-the-work is this wave's most likely false-positive mode,
   and a false positive here IS the tier-ii headline.**
4. **Designed negatives pytest-asserted for M3 (both) and V2 (permuted payloads)**, and the M7
   coefficient-zero bit-identity, and every loss term's coefficient-zero bit-identity.
5. The M3 drift leg **fails on a planted near-zero-drift table-like store** (the D2a trap), asserted.
6. Traversal trigger: the file appended **or** its non-firing stated explicitly.
7. **Byte ledger on every arm** including φ, projection params, ψ params, novelty-head params, reader
   params — and on the launders.
8. Full suite green on your branch, **count arithmetic stated with the checkout named**.
9. Reconciliation list in the **first 10 lines**; NOT-RUNs declared as NOT-RUNs, never nulls;
   registered deviations argued, never silent.
10. A `PREREG.md` under `.claude/outputs/c2w11-physics-organizer/`, filed **before your first cell**,
    with your own numeric predictions for V1/V2a/V2b/V3, M3, M7 and the weak-supervision arm, and a
    scorecard against it at the end.

⛔ You do NOT build any null arm, the lifecycle verbs, the hierarchy, the kinetics head, wormholes or
learned `p₀`. ⛔ You do NOT adjudicate the swap, the arm race, or anything else.
⛔ Never push `origin`; the Hub integrates. `clu-dev` only, and only the Hub pushes it.
