# Task: scout-hepa-predictor — extract HEPA's predictor/head spec exactly, so CLU can be dropped into it (web-scout)

- **Agent:** `web-scout` · **Output:** `.claude/outputs/scout-hepa-predictor.md`
- **Read first:** protocol · **`.claude/outputs/scout-cafe-hepa.md`** (the prior HEPA extraction — architecture, both loss equations, the survival-CDF form; do NOT redo it, extend it) · **`.claude/outputs/clu-cafe-integration.md`** (what the engineer measured and the four premise corrections) · the handover's 2026-07-20 WAVE-17 REVIEW block (the **four-protocol table** — this task exists to nail protocol 3).
- **Primary sources, in priority order:**
  1. ⭐ **`https://github.com/Forgis-Labs/HEPA-SP`** — Head-authorized. **HEPA-SP = HEPA *with* semantic priors; we want the BASELINE HEPA underneath it** (the Head's framing: "get an idea of how the baseline HEPA works without the semantic priors from HEPA-SP"). Read the source. You have Bash — **clone it** (the prior scout could not, and that was the single biggest gap in the last wave).
  2. **arXiv:2605.11130v3** (HEPA paper) — App. G/H are already transcribed into the handover; the *middle rows* of Tables 5 and 6 (PSM/MBA/GECCO/ETTm1/BATADAL) **pasted badly and are NOT trustworthy in our notes — re-extract them from the PDF.**
  3. The Head will supply a pointer to the exact head location in the HEPA codebase — **if it has arrived, it supersedes guesswork; if not, find it yourself and say so.**

## Why this is the critical path
The program has pivoted to a **drop-in encoder-swap claim** (Head, 2026-07-20): ***HEPA-with-a-transformer-encoder vs HEPA-with-a-CLU-encoder*** — everything else held fixed. That makes CLU-vs-a-transformer-block the claim, not CLU-vs-HEPA-the-system. It only works if we replicate HEPA's downstream **exactly**. This task delivers that spec.

## Deliver
1. ⭐ **The 198K-param dt-conditioned MLP head, in implementable detail.** Paper protocol quote: *"All comparison methods share the same 198K-param downstream MLP head, positive-weighted BCE loss, and evaluation protocol; only the frozen encoder differs."* Extract from source: exact layer sizes/depth/activations (verify the 198K count adds up — **state the arithmetic**); **how `Δt` is conditioned** (concatenated? embedded? FiLM? sinusoidal?) — this is the crux, because the engineer proved a *non*-dt-conditioned probe (CoxPH) gives a **bit-identical ranking at all 125 horizons** (rank corr 1.0000), which kills the per-horizon headline figure; the **positive-weighting scheme** for BCE (how `w⁺` is computed); optimizer/lr/schedule/epochs/early-stopping; and how the **monotone survival CDF** `p(t,Δt)=1−∏(1−λ_j)` is assembled (sigmoid hazards → cumulative product; where monotonicity is enforced).
2. ⭐ **THE PREDICTOR, AND WHETHER IT CAN BE ITERATED** (added 2026-07-20 — the Head's rollout point; this is co-equal with item 1 in importance). HEPA's predictor is a **2-layer MLP** mapping `(encoder output, horizon Δt) → predicted future representation ĥ`. It is a **direct horizon-conditioned map — it jumps to Δt**, whereas CLU **integrates** to Δt by iterated symplectic rollout. That contrast is the second planned experiment: *direct prediction vs iterated dynamics, as Δt grows past the training horizon.* Extract:
   - The predictor's **exact signature and shapes** — what `h` is (pooled vector? sequence?), how `Δt` enters, what `ĥ` looks like, and whether `ĥ` lives in the **same space as `h`**. ⚠ **This last point decides whether HEPA can be iterated at all**: feeding `ĥ` back in as `h` for another step is only well-defined if the output space equals the input space. **Answer it explicitly — yes/no + the evidence.** If they differ, say precisely how (projection? normalization? different dim?), because that determines whether an "HEPA-iterated" arm is even constructible or whether the fair comparison has to be built differently.
   - **The training horizon set `H`** — the exact Δt values the predictor is trained on, and whether Δt is sampled or enumerated. The rollout experiment's whole claim lives *beyond* `H`, so we need to know exactly where "beyond" starts.
   - Any **normalization/stop-gradient/EMA** between the online and target branches (JEPA methods usually have one) — it changes what iteration even means.
   - Whether the repo already contains any **multi-step or autoregressive** prediction path, or any evaluation beyond the trained horizon. **If HEPA already does this, our experiment must be positioned against it rather than presented as novel** — check honestly and say so.
3. **The encoder→head interface.** What exactly does the frozen encoder hand the head — pooled vector, per-patch sequence, what dimensionality? Is there a projection/normalization between them? **This is the socket CLU must fit.** Note HEPA's dual read mode (causal for query, bidirectional + attention-pooled for target) and say which one feeds the downstream head.
4. **Data/protocol, exactly.** Context length, patch size, stride, normalization (paper says per-context instance norm), train/val/test splits for C-MAPSS, the horizon set `H`, and **how labels are constructed** — ⚠ **cross-check against the engineer's finding that CAFE's `cmapss.py` labels `t = max_cycle − i` (cycles-remaining-in-recording) and ignores the true-RUL file, so `t=0` is assigned to a test unit whose true RUL is 112.** **Does HEPA use the true-RUL file?** If HEPA and CAFE label C-MAPSS differently, that alone may explain the 0.918 / 0.81 / 0.73 spread — **this is a high-value question, answer it explicitly.**
5. **Re-extract App. G (Table 5) and App. H (Table 6) cleanly** — all rows, all columns, from the PDF. Our transcription is reliable only for the C-MAPSS and SMAP rows. Flag any cell we currently have wrong.
6. **MTS-JEPA** (`[4]` in HEPA's refs) — identify it properly (venue, arXiv, authors), what its contrastive+codebook objective is, and whether a public release exists. It is a **published SSL-objective-only ablation against the same head** — i.e. the exact template for our encoder-swap claim, and a baseline we did not previously know about.
7. **HEPA-SP vs HEPA:** one clear paragraph on what the "semantic priors" add, so we never accidentally compare CLU against the SP variant when the paper numbers are baseline HEPA (or vice versa). **Say which variant each published number belongs to.**

## Discipline
- **Quote code verbatim** for anything the engineer will implement; cite file + line. Paraphrase is not good enough for a head we are replicating.
- **Distinguish repo-source facts from paper-text facts from your own inference** — label each. The last scout did this well; match it.
- If the repo is private/404 or the head is not in `HEPA-SP`, **say so plainly and immediately** and deliver what the paper alone supports — do not reconstruct a plausible head and present it as extracted. A fabricated head spec is worse than a gap, because the engineer would build against it.
- ⚠ **Known-bad numbers, do not propagate:** CAFE README's `0.918` (placeholder — no `leaderboard.json`, no `results/`, and CAFE's aggregation provably matches the paper); `arXiv:2605.07675` is **Factorybench**, NOT HEPA (HEPA = **arXiv:2605.11130**); `T_KT = 0.1786` (retracted, correct value `0.0893`).

## Acceptance
An implementable spec of the 198K dt-conditioned head (layers + dt-conditioning mechanism + loss + optimizer, with the param count verified by arithmetic) · **an explicit yes/no on whether HEPA's predictor can be iterated (`ĥ` in the same space as `h`?) plus the training horizon set `H`** · the encoder→head socket definition · the exact C-MAPSS protocol **with an explicit answer on true-RUL vs cycles-remaining labelling** · clean App. G/H tables · MTS-JEPA identified · HEPA vs HEPA-SP disambiguated. **End with: "what the engineer can now build without guessing" vs "what remains unknown" — the second list is as important as the first.**
