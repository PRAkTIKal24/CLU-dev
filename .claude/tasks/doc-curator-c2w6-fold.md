# doc-curator-c2w6-fold — the C2W6 registry fold + two program-wide propagations (§A23.3, §A23.4)

**Campaign 2, wave C2W6 close-out. Agent:** doc-curator. **No worktree, no branch** (registries and
docs only). Writes `.claude/outputs/doc-curator-c2w6-fold.md` + the registry edits themselves.

⛔ **Mechanical precondition (standing doctrine, §A23): none for items 1–4 — run immediately.**
**Item 5 (the φ-qualifier sweep) requires `.claude/outputs/c2w6-phi-leak-docstring.md` to EXIST**
(it carries the engineer's grep list of every in-code instance). If that file is absent, do items
1–4, mark 5 `BLOCKED-PENDING`, and say so — do not guess the list.

**Read first:** charter **ADDENDUM 7 (§A22 verification record + §A23's five rulings)** —
authoritative for every number below · `.claude/outputs/c2w6-anti-erosion.md` **including its dated
Hub erratum banner at the end** · `.claude/outputs/c2w6-erosion-adjudication.md` (the analyst's
`BLOCKED` report — its F1/F2/F3 are the source of items 2 and 3) · the `[C2W6]` §10 review entry.

## 1. The C2W6 registry fold (N-registry + claims matrix)
Four N-registry candidates, all measured, 3 paired seeds, ⛔ TOY 0.16 M, monitor #13/N94 riding
every w4 reading:
- **erosion is a symptom of uselessness** — residual-off retention **0.141 ± 0.095×** untrained vs
  residual-on **0.708 ± 0.57×**; destructive-rewrite rate **0.512 ± 0.238** (residual off) /
  **0.228 ± 0.182** (run-2) / **0.009 ± 0.009** (P1 on). ⛔ DIAGNOSTIC-labelled corner, never a
  claim cell.
- **the masked write is C3-local to float32** — 0 violations of `D·group_scale²` across
  5553 + 1355 + 5546 events, max residual **3.3e-07**. (A regression check now, not a result.)
- **P1's depth protection** — paired ON/OFF final depth **21.53 / 1.516 / 1.464**, geo **3.628×**.
  ⛔ **`w40`-SCOPED** (§A22): w4 is 2/3, geo 1.421×. Never quote the 3/3 without the budget.
- **the write→φ placement leak** — **27 %** of φ's layer-0 gradient (0.0908 → 0.0659), live
  whenever `atom_place_radius > 0`.
Plus: **I2 = `NO_USAGE_STRUCTURE`** (ρ read-selection −0.2571 ± 0.1512, 5–6 wells/seed) — filed as
**accepted, re-measurement deferred to C2W10** (§A23.5), **not** as a clean null.
⚠ **CORRECTED BY THE ADJUDICATION (2026-08-06), supersedes this line as originally written:** the
ρ(LOO) = +0.0667 ± 0.1627 figure this task file first carried is **RETRACTED** — that proxy's
ICC(1,1) is **negative on 3/3 seeds** (−0.205/−0.218/−0.248) so its attenuation ceiling is **0.000**
and the value is **UNDEFINED, not a null**. ⛔ Never fold it as a measured correlation.

## 2. ⭐ PROPAGATION A — the censoring erratum (§A23.4, program-wide)
**"4.95e-63" is a CENSORING STATEMENT, never a point estimate.** It is the arithmetic mean of
per-seed depths spanning **114.8 orders of magnitude** (1.439e-87 / 1.486e-62 / 2.493e-177);
the geometric mean is 3.764e-109. **Replacement wording:** *"200 outer steps drive well depth below
1e-62 on 3/3 seeds (1.4e-87 / 1.5e-62 / 2.5e-177)."* Sweep `pilot-placement-probe.md` §7 and
**every downstream quotation** (registries, ledger, any draft or short). File the erratum dated;
⛔ do NOT edit `PREREG-*.md` files (a revised pre-registration stops being one) — annotate instead.
⚠ The qualitative R3 claim is untouched and if anything strengthened.

## 3. Estimator hygiene (the analyst's F3, adopted as reporting practice — NOT a re-scoring)
Depth is positive and spans decades; within-arm seed spread is 52–128×. **Any pooled depth
aggregate must report geometric alongside linear.** Concretely, record the standing never-quote:
⛔ *"there is no erosion at the run-2 config"* (arith 0.708× vs **geo 0.327×** — estimator-dependent);
the defensible form is ⭐ ***"N223's monotone collapse does not reproduce at the run-2 config."***
⛔ **These estimator corrections are POST-HOC with respect to C2W6's cells and may never be applied
as a re-scoring of a registered verdict** (§A23 process lesson) — they are reporting-side only.

## 4. Never-quote additions (Head-adopted as proposed) + one caveat re-affirmed
Add: the estimator-dependent erosion sentence (item 3) · "4.95e-63" as a point estimate (item 2) ·
any C2W6 depth aggregate without linear-vs-geometric stated · the engineer's own refuted **A1-3**
("0 rewrite events, structurally" — refuted by 27/40/70 after training; quoted nowhere downstream) ·
any I2 ρ presented as a measured null rather than an underpowered reading · **"P1 improves bpc"**
(the claim is **no harm**; w4 improves at 6.23 SE but the w40 point estimate is slightly worse, n.s.).
⛔ **RE-AFFIRM AS ACTIVE: depth is NOT quotable as feature importance** (charter §A21/§A23.5) — only
the refutation branch could have lifted it and I2 returned no-structure.
✅ Also record: **the CSF3 abort criterion is SUSPENDED** (§A23.1) — §7.27's watch item is
**instrumentation only**, no run is paused or escalated on depth; the run-1 seed-0 override is
**VINDICATED BY MEASUREMENT**; the accepted risk (a destroyed store burns its allocation) is the
Head's, stated knowingly.

## 5. ⭐ PROPAGATION B — the φ qualifier (§A23.3, curator half; gated on the engineer's file)
**Every "the write cannot train φ" statement in the program now needs the placement qualifier.**
Using the engineer's grep list, sweep the docs/registries/ledger/shorts for that claim in any
phrasing (including T3-adjacent "settled-point γ/M gradients are 0.0 bitwise" contexts where the
φ-channel claim rides along) and attach: *"— severed by sign-SGD only when `atom_place_radius = 0`;
in the shipped run-1/2/3 config the localized-placement path carries 27 % of φ's layer-0 gradient,
closed by `erosion_partition`."* Report what you changed and what you deliberately left.

## Constraints
Registries stay HUB-CONFIRMED-pending (the Hub confirms versions, not you) · every number above is
already Advisor-verified — ⛔ do not re-derive or re-run anything · declared NOT-RUNs never nulls ·
⛔ do not edit `.claude/papers/**` (the Head writes all prose) · ⛔ do not touch any `PREREG-*.md`
body · ⛔ never push anything.
