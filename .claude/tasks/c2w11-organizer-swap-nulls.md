# c2w11-organizer-swap-nulls — the matched-capacity non-physics organizers at F3-grade tuning

**Campaign 2, wave 11 (THE COMPOSITIONAL WAVE). Agent:** experiment-engineer. **ONE worktree (wt2).**
Branch **`c2w11-organizer-swap-nulls`** from **`main @ <the Hub names the base at spawn — the commit
carrying spoke A's merge>`**, which is **`2e1cdb2` or later by construction**. ⛔ **NEVER pass 3's
`9e0bb25`** (four C2W8-close repairs bind: A3 DIAGNOSTIC + two-sided G-DRIFT · `d_safe`/cue sized on
the STORE population · `covered` split launch-vs-settle · refuse-at-unselected-width). ⚠ **The shared
checkout sits on a LIVE spoke's branch** (`pilot-ttt-nan-and-d5-wiring`). Work **in your worktree**, cwd = the worktree
on every command. ⚠ **You run CONCURRENTLY with `c2w11-physics-organizer` (wt1).** Your ownership
lists are disjoint by construction — verify by diffing your branch against the base before you finish.
Writes `.claude/outputs/c2w11-organizer-swap-nulls.md` + artifacts to
`.claude/outputs/c2w11-organizer-swap-nulls/`.
**Budget:** ≈ 2 days.

---

## ⛔⛔ MECHANICAL PRECONDITIONS — VERIFY BOTH ON DISK BEFORE ANY CODE

> 1. ⭐ **`.claude/outputs/c2w11/GATE-SEMANTICS-C2W11.json`** with
>    **`kills_all_discriminating_passed == true`** ← **the gate, per charter ADDENDUM 13**
> 2. **`.claude/outputs/c2w11/FROZEN-INTERFACES-C2W11.json`** — the frozen family/launches/φ/reader
>    class/byte-ledger/`v3_budget_grid` (⚠ its own `kills_all_passed` is **superseded** by (1); read
>    the *data* from it, the *verdict* from (1))

If either is missing or present-and-false → **report `BLOCKED`, write nothing, take no worktree, run
zero cells, make zero git footprint**, name the failing items, hand back. ⛔ Do not judge whether they
matter — the Hub's call. When both are true: **quote (1)'s per-leg table into your §1** and take the
frozen family, launches, φ, reader class and byte-ledger template **from the JSONs, never re-derived**.

⚠ **K3, K4 and K5 ABSTAINED** on spoke A (scored on an arm structurally incapable of producing both
outcomes — §A37). The gate is the AND over the **discriminating** legs (K0, K1, K2, K7-CAP, K8 — all
passed). ⛔ **No K5 verdict exists in this wave**, and **nothing about table-expressibility may be
quoted from spoke A** (Head condition (d)). None of this changes your brief — it changes what you may
say about the substrate you inherit.

**Binding documents, read first, in this order:**
1. `.claude/outputs/c2w11/PREREG-C2W11.md` **IN FULL** — you produce the **`null*`** side of **§5's
   V1, V2, V3**.
2. `.claude/outputs/orgdiv-prereg/PREREG-TierII.md` **§0, §1, §4 IN FULL** — the metric, the exact
   matching ledger, and the five arms in their **strongest registered forms**.
3. `.claude/outputs/orgdiv-null-arms.md` **IN FULL** — this is your direct ancestor. Its arms, its
   guards, its `null*` computation and its **four internal-validity anchors L1–L4** are your baseline
   standard, not your ceiling.
4. charter **ADDENDUM 12 §A33.1–§A33.2** (MECHANICS/VALUE; the three VALUE legs) · **§A13 tier ii**.
5. `.claude/outputs/c2w11-substrate-and-kills.md` — the frozen substrate and its NOT-RUNs.

### ⚠⚠ THE PAYLOAD-REPAIR TRAP — it will silently corrupt every score if you miss it
Spoke A's repair moved **`payload_radius` 1.0 → 0.60** (`atom_payload_init_radius` co-scaled) to close
a measured reach gap (`‖v_j‖ / capture` **1.172 → 0.692**). **`tol`, `chance` and every y-scale are
homogeneous of degree 1 in the payload radius and MOVED WITH IT:** post-repair **`tol = 0.28696`**,
`chance_per_seed = [0.0, 0.001953125, 0.0]`. ⛔ **`run1` artifacts carry the PRE-REPAIR `tol =
0.47827`.** **Read `family.tol` and `family.chance_per_seed` from `FROZEN-INTERFACES-C2W11.json` on
EVERY cell; never hard-code or carry a pre-repair constant.** ⚠ The family's **difficulty is
unchanged** — K2's payload half is scale-invariant and spoke A **verified** that rather than assuming
it.

## ⭐ DIAL DECLARATION (protocol §7, C2 form) — echo before your first result
- **Dial / pillar:** **TIER ii — the ORGANIZER SWAP's null side.** ⛔ **You are the control.** You
  compute no `OD`, no swap verdict, no paper number, no tier-ii verdict, no full-CLU verdict.
- **Laundering control:** ⛔ **NO launder margin is a pass condition anywhere** (§A33.1). Your own
  guards are: (i) **byte-compared φ and a bit-identical launch protocol** (assert both), (ii) the
  **two-sided byte ledger per arm**, (iii) a **shuffle-φ launder** run beside every arm's score,
  (iv) the **L1–L4 internal-validity anchors**.
- **Falsifies:** nothing of yours — you produce `null*`. ⛔ **Selection on `Q_unseen` anywhere would
  invalidate the wave**; document the mechanical guard that prevents it.
- **Does NOT falsify:** an arm **beating** the physics arm is a legitimate outcome, not a defect, and
  you report it without softening.
- ⛔ Depth is not feature importance (§A23.5 ACTIVE). ⛔ N94 discipline.
- ⛔ **Wells are never named semantically** — copy `PREREG-TierII.md` §2.6's sentence verbatim.

---

## ⭐⭐ THE STANDARD — F3-GRADE TUNING, AND IT IS NOT NEGOTIABLE

> **A hobbled null is the same referee attack in mirror image.** Banked precedent: the `bprime-rivals-f3`
> tuning pass **closed the "you hobbled the competition" attack by measurement** — and the C2W5 physics
> arm ran at **ONE** configuration, which is why its numbers can only carry *"physics, untuned and
> collapsed"*.

- **Registered budget: ≥ 5 lr × 3 capacity × 3 seeds per arm**, selected on a **held-out-from-seen**
  validation split. **The physics arm gets the same budget, no more.**
- ⛔ **`null* = max over ALL non-physics arms AND their ENTIRE registered tuning grid` — COMPUTED, not
  estimated.** C2W5 computed it over **584 configs × 5 score seeds**; that is now the permanent
  standard. Report it as an explicitly **oracle-selected upper bound**, so the verdict reads *"no
  configuration clears"* rather than *"the one we picked didn't"* — and never as any arm's own score.
- ⚠ **The seen-validation split must inherit the family's OWN rule-4 held-out rule.** C2W5 held out 32
  SEEN rows with `|A_val ∩ A_train| ≤ F−2` against **every** retained training row (32/32 on all 5
  seeds, asserted). A naive seen-holdout is a *different, easier* problem and selection on it rewards
  near-neighbour interpolation.
- ⚠ **Honest-power note from C2W5, and design around it:** with 32 validation rows the metric grain is
  `1/32 = 0.031`, so **every** config scored `val_acc = 0.0000` and selection ran entirely on the MSE
  tie-break. **Either buy more validation rows or declare the tie-break, and run the grid-max over
  `Q_unseen` regardless** so the verdict does not depend on the selection statistic resolving.

## THE ARMS (`PREREG-TierII.md` §4.2, each in its **strongest** registered form)

| arm | mechanism | the standard it must meet |
|---|---|---|
| ⭐ **N1 gradient-placed atoms** | **identical store parameterisation** (`c_j, log s_j, amp_j`), plain Adam on the read objective, **static** assignment rule, **no rollout anywhere** | ⭐ **the cleanest and most damaging swap** — same landscape family, non-physics training signal. Any physics margin is then attributable to **the dynamics as a training signal** and to nothing else. ⚠ Banked: N1 with **28 672 free floats** (5× the physics store) fit **1.0000** of its training items and scored **0.0000** on rule-4-valid held-out — *it memorises perfectly and composes not at all.* Reproduce that anchor (L1) or explain why it moved |
| **N2 VQ** | k-means / VQ-STE codebook of `N_a` codes + per-code payload | best of {k-means++ ×10 restarts, VQ-STE with EMA, product-VQ}; commitment cost swept ≥ 5 points |
| **N3 fitted static-geometric** | `argmin_j [‖z−c_j‖²/2σ_j² − b_j]` — the decision rule the physics arm provably has to leading order (Thm O2) | fit `(c, σ, b)` jointly on SEEN. ⚠ Banked: N3's in-sample fit was **0.0799** — capacity-starved by construction, the weakest arm-side number in C2W5. **Give it a fitting budget that reaches a real optimum**, and report the reached optimum |
| **N4 kNN** | raw item keys + payloads, `k` nearest with IDW | `k` swept `{1,2,3,5,10}`, uniform and IDW (the C2W1 `knn2_idw` substitute is the arm that beat us) |
| **N5 Titans-style write** | fast-weight memory `M_t` updated by a surprise-gated online rule | ⭐ **init = PARAMETERS; per-stream deviation = STATE** — both declared. Chunk granularity matched to the physics arm's; momentum + weight-decay variant per the published rule. ⚠ Banked: **81/240** configs diverged at the momentum × lr corners — record divergences and assert they cannot win selection |

⭐ **`null*` also includes N3 fitted on the PHYSICS arm's own assignments** — the **oracle-imitation**
null (T5.2 rider (i)): *a physics arm that cannot beat an imitation of itself has no organization
claim.* ⚠ Banked and worth carrying: that agreement measured **0.8888** when fitted to the assignments
directly but only **0.2576** when fitted to the read objective. **The 0.89-vs-0.26 gap is an
optimisation gap in fitting the diagram, NOT evidence of a structurally non-VQ channel** — quote that
sentence wherever F5 is discussed.

## ⭐ WHAT IS NEW RELATIVE TO C2W5 — THREE ADDITIONS, ALL MANDATORY

1. ⭐⭐ **A MATCHED CONFIDENCE CHANNEL ON EVERY ARM (VALUE leg ii's null side).** V2 scores the
   graded-novelty read under the swap, so each arm must emit a confidence: **N2** distance-to-codebook ·
   **N1** read-objective residual · **N3** the fitted rule's margin · **N4** neighbour distance ·
   **N5** the surprise gate. Emit **V2a per-feature novelty AUROC** and **V2b set-level ECE** for every
   arm on the identical novel/known feature split. ⛔ **An arm with no principled confidence channel is
   a declared NOT-RUN for V2 — never scored as "uncalibrated".**
2. ⭐⭐ **VALUE LEG iii's NULL SIDE — AND IT IS NOT A FLAT LINE. THIS IS THE BIGGEST CHANGE TO YOUR
   BRIEF, SO READ IT TWICE.**
   The organizer swap hands your arm the **SAME READER CLASS** — it changes **who decides where the
   wells go, not how they are read** (`PREREG-TierII.md` §1: identical φ, bytes, capacity, launch
   protocol **and reader class**; §A13: table-like inference reads permitted **on both arms**).
   ⇒ ⭐ **Your organized store is INSTANTIATED AS A LANDSCAPE and READ WITH THE IDENTICAL k-PARTICLE
   ANYTIME READ, so it HAS a curve**, and V3-PRIMARY is the **swap-differenced** curve at matched read
   compute. **Emit your curve at the SAME frozen budget-point grid the physics arm uses** (from
   `FROZEN-INTERFACES-C2W11.json`; if it is not frozen there, coordinate and say so loudly — **a
   mismatched axis voids the leg**).
   **Which arms admit a landscape — registered by the Hub so you do not discover it mid-run:**
   - ✅ **N1** — *identical store parameterisation by construction*; it is the **headline** V3 null.
   - ✅ **N2 (VQ)** and **N3 (static-geometric)** — codebook / fitted `(c, σ, b)` + payloads
     instantiate as well centres.
   - ⛔ **N4 (kNN over raw rows)** and **N5 (Titans fast weights)** — **no landscape exists.**
     **DECLARED NOT-RUN for V3**; report their static scores as **flat reference lines**. ⛔ They are
     **never** scored as "un-navigable" — that would be scoring an arm for not being the object under
     test.
   ⇒ **`null*_V3 = max over {N1, N2, N3}`** over their registered grids.
   ⚠⚠ **AND THE MIRROR-IMAGE REFEREE ATTACK IS YOURS TO CLOSE.** Instantiating N2/N3's codebook as a
   landscape means choosing atom amplitudes, widths and counts. ⛔ **Match them to the physics arm's
   atom budget and F3-tune them like every other null hyperparameter.** *"You gave the null a badly
   instantiated landscape"* is the same attack as *"you hobbled the competition"* — **close it by
   measurement, not by assertion.**
   **Still mandatory:** emit each arm's per-query read compute on the physics arm's ledger (mult-adds;
   particles-evolved × steps). ⚠ Banked reference, and **label it as a READ-COMPUTE RATIO**: the C2W5
   physics read cost **3 360×** N1's matched-capacity static read (6.88e7 vs 20 480) **at a tie**.
3. ⭐ **THE DECODABILITY CEILING, RECOMPUTED ON THE NEW LAUNCHES.** The out-of-class combinatorial
   matched filter over the launch points, ⛔ **declared out-of-class and NEVER scored as an arm.** It
   is what turns "no arm clears" into an **attributable** finding. ⚠ Banked at C2W5's launches:
   **1.0000** noiseless, **0.2719 ± 0.0126** as-launched — *the information was in the launch; the read
   protocol could not express it.* **Recompute both on this wave's feature-factored launches and put
   the number in the same sentence as any "no arm clears" statement.** ⛔ It is never quoted without it.

## THE FOUR INTERNAL-VALIDITY ANCHORS (L1–L4) — mandatory, and they are what make a null a statement about the PROBLEM
| # | anchor | bar |
|---|---|---|
| **L1** | N1 fits its own training items | ≥ 0.50 (banked **1.0000**) |
| **L2** | N4 `k=1` memorises SEEN | ≥ 0.95 (banked **1.0000**) |
| **L3** | shuffle-φ launder | ≤ chance + 0.005 (banked ≤ **0.00039**) |
| **L4** | N1 capacity flatness across the `a` axis | ≤ 0.02 (banked **0.0000** at 12/32/64) |
⭐ **Standing doctrine this instantiates: an organizer audit needs an in-class fit anchor.** N1 at
**1.0000 train / 0.0000 held-out** is what makes "no arm clears" a statement about the problem rather
than about your optimiser. **No "nothing works" verdict ships without L1.**

## FILE OWNERSHIP (declared)
**You own:** `chlu/core/null_arms.py` (**additive**) · `chlu/experiments/exp_null_arms.py`
(**additive**) · `tests/test_null_arms.py` (**additive**) · **new**
`chlu/experiments/exp_c2w11_nulls.py`.
⛔ **DO NOT TOUCH `chlu/cli/experiment_cmd.py`** — spoke A landed your `exp-c2w11-nulls` subcommand
stub in its first commit; wire your handler inside **your own module**.
⛔ **DO NOT TOUCH:** `chlu/config.py` (**zero C2W11 spoke touches it**) · `chlu/core/factored_store.py`,
`chlu/core/feature_launch.py`, `chlu/experiments/exp_c2w11_substrate.py`, `tests/test_c2w11_substrate.py`
(spoke A's — **read them, do not edit**) · `chlu/core/psi_readout.py`, `chlu/core/novelty_read.py`,
`chlu/experiments/exp_c2w11_organizer.py`, `chlu/training/losses.py`, `tests/test_c2w11_organizer.py`
(spoke B's, running **concurrently**).
⛔ **DO NOT TOUCH — the live CSF3 pilot spoke's territory:** `scripts/csf3/` ·
`chlu/training/train_cluformer.py` · `chlu/core/blocks.py` · `chlu/experiments/exp_cluformer_pilot.py`.
⛔ **DO NOT TOUCH — C2W8-close's territory (READ-ONLY all wave):** `chlu/core/well_lifecycle.py` ·
`chlu/experiments/exp_well_lifecycle.py` · `chlu/core/clu_system.py` · `chlu/core/soft_certificate.py` ·
`tests/test_gate_addr.py` · `tests/test_well_lifecycle.py` · `tests/test_cifar_strong_phi.py`.
⚠ Anything you need changed outside your ownership is a **rider handed back to the Hub**, never an edit.

## Acceptance (mechanical)
1. Precondition verified **on disk, by you**, quoted into §1, before any code.
2. **All five arms landed in their strongest registered forms**, the **full registered grid run**, and
   **`null*` COMPUTED over the entire grid × 5 score seeds** — with the config count stated.
3. **L1–L4 all measured and reported.** ⛔ No "nothing works" statement ships without L1.
4. **V1 / V2a / V2b / V3** emitted per arm on the identical family, launches, φ, reader class and
   splits as the physics arm. ⛔ **You compute no `OD` and no swap verdict.**
4b. ⭐ **V3's landscape instantiation landed for N1/N2/N3**, read with the identical k-particle anytime
   read, **at the frozen budget-point grid**, with the instantiation's atom budget/width/amplitude
   **matched to the physics arm and F3-tuned**, and the tuning reported. **N4/N5 declared NOT-RUN for
   V3** with their flat reference lines stated. ⚠ You **construct** a store from your codebooks by
   importing spoke A's `factored_store` — ⛔ **you do not EDIT it**; if instantiation needs a seam that
   does not exist, that is a **rider handed back to the Hub**, not an edit.
5. **φ byte-identical and launch points BIT-IDENTICAL to the physics arm's**, both **pytest-asserted**
   (the C2W5 pattern: `test_phi_is_frozen_…`, `test_launch_points_are_bit_identical_…`).
6. **The selection guard documented mechanically:** where `Q_unseen` is constructed, every place it is
   read, and the assertion that no fit / hyperparameter / arm ever sees it.
7. **Two-sided byte ledger + per-query read compute per arm**, including φ, projection and reader
   params, and the flat-curve constant for leg iii.
8. **The decodability ceiling recomputed** (noiseless + as-launched) and placed in the same sentence as
   any "no arm clears" statement.
9. Full suite green on your branch, **count arithmetic stated with the checkout named**.
10. Reconciliation list in the **first 10 lines**; NOT-RUNs declared as NOT-RUNs, never nulls.
11. A `PREREG.md` under `.claude/outputs/c2w11-organizer-swap-nulls/`, filed **before your first cell**,
    with your own numeric predictions per arm and per VALUE leg, and a scorecard against it at the end.

⚠ **A known repo hazard, and it bit this exact file last time:** a module mixing explicitly-cast
float32 data with flag-following `jax.random` initialisers is **silently x64-dependent** — it passes
alone and fails in the full suite (another test enables `jax_enable_x64` process-wide). Pin dtypes
explicitly and take carry dtypes from `jax.eval_shape`. **A per-file test run will not catch it.**

⛔ Never push `origin`; the Hub integrates. `clu-dev` only, and only the Hub pushes it.
