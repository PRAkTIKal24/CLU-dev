# bprime-cite-check-3 — third citation sweep (GDN equation + two unverified venues)

**Campaign 2, wave C2W5. Agent:** web-scout. **Small. NO WORKTREE.** Writes
`.claude/outputs/bprime-cite-check-3.md`. Three items, Head-funded 2026-08-01 on the
`bprime-cite-check-2` §"Next search" recommendations:

1. **GDN's presentation of Mamba2 as `S_t = α_t S_{t−1} + v_t k_tᵀ`** — currently single-sourced
   to `rival-recon` §1.4 (arXiv HTML 404 for 2412.06464 v1 and v2). Double-source the equation from
   the ICLR 2025 OpenReview PDF and/or the NVlabs / flash-linear-attention (FLA) repo. ⭐ **poppler
   is NOW INSTALLED on this machine** (`pdftotext` 26.07.0) — PDF sources are readable this wave;
   `Bash` + `pdftotext` on a fetched PDF is in-kit. If double-sourced → r4 may QUOTE the equation;
   until then r4 must paraphrase (standing restriction from the cite-check-2 review).
2. **Mitzenmacher 2018** — draft §1 (r3 l.≈68) cites by year, venue unverified. Full citation facts
   + BibTeX.
3. **Kipf et al. 2019 (SOSD / learned-indexes benchmark) + the accompanying PVLDB study** — same
   §1 sentence, unverified. Full citation facts + BibTeX; keep the SOSD "30–80× larger" /
   "4 orders-of-magnitude" quotes' source pinned while you're there (draft §5.3 quotes them).

Verdicts per item (VERIFIED / CORRECTED / UNVERIFIABLE), each double-sourced, ready-to-paste
BibTeX. Also feeds the r4 ⟦CITE2⟧ marker set. Tool notes: `scholar.archive.org` full-text phrase
search is in-kit (it closed the Poliak double-source); OpenReview has been bot-blocked 5 straight
waves — if it blocks again, the PDF route via arXiv/proceedings + pdftotext is the fallback.
Report → Hub.
