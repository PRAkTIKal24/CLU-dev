# bprime-cite-check-2 — second citation sweep (two single-sourced items)

**Campaign 2, wave C2W5. Agent:** web-scout. **Small. NO WORKTREE.** Writes
`.claude/outputs/bprime-cite-check-2.md`. Two items, both flagged by C2W5 spokes as single-sourced:

1. **Poliak et al. (\*SEM 2018)** — incl. the "6 of 10 NLI datasets" figure quoted in draft §5.3
   (r3 reconciliation 5). Verify venue, pages, and the figure against the published text;
   double-source. Ready-to-paste BibTeX.
2. **Mamba-2 (Dao & Gu)** — venue/year/arXiv id and the reference-implementation state accounting
   (`bprime-mamba2-arm` reconciliation 4 quotes `rival-recon` §1.4's pinned record, never
   re-verified). Verify the citation facts the draft prints; the SSD state-size convention
   (d_head·d_state per head) double-sourced against the paper/official code.

Verdicts per item (VERIFIED / CORRECTED / UNVERIFIABLE), sources cited. Feeds the r4 ⟦CITE2⟧
marker. Report → Hub.
