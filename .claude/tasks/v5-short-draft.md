# Task: v5-short-draft — the V5 "Forgetting" short, first draft (w16, paper-writer)

- **Agent:** `paper-writer` · **Output:** `.claude/outputs/v5-short-draft.md` + `.claude/papers/v5-short/{draft.md,draft.tex}` + PDF.
- **Read first:** Charter (**C-1 REVERSED** — no audit confessions; **C-9** negatives→appendix; **C-10** appendix maximalism) · `.claude/claims_matrix.md` (**CM-16a/b — the split is BINDING**; CM-17; CM-3 forbidden) · `.claude/outputs/v5-gate.md` (R3 vault + R1 emergent split) · `.claude/outputs/t-lever-forgetting.md` (the (μ,γ,T) laws) · `.claude/outputs/venue-follow-up.md` §3 (the erosion-novelty verdict — ship rules) · `.claude/outputs/sleep-erosion-study.md` (§4 honest framing to preserve verbatim) · the V2 draft `.claude/papers/v2-short/draft.md` (V5 is its **sibling** — shared vocabulary, no contradiction; V5 must be matrix-consistent with V2).
- **Venue (provisional):** EBM/generative or ML4PS-appendix-grade (venue-follow-up §2); confirm at the next venue scout. Format target: 4pp + refs/appendices, double-blind, non-archival (TS4H/ML4PS fingerprint).

## The thesis (honest, and it is the G7c template instantiated)
*A physics-structured memory whose **forgetting is predictable and controllable from the model's own parameters** — the (μ, γ, T) budget cube — with a designed-symmetry precondition.* Retention lifetime is read off `(μ, γ, T)`; **friction preserves flat-direction memory, only temperature erases it**; and a localized friction hole is a **memory vault**. No baseline (LSTM/LEM/Transformer) can state its own forgetting law.

## What is load-bearing (all trained-model or exact, all matrix-checked)
1. **The (μ,γ,T) budget cube** (CM-16b): retention `n₁/₂ ∝ μ⁻²` up to the critical-damping crossover `εμ≈γ/2`, then a mass-independent floor; the **V-curve** (non-monotone `n₁/₂(γ)`, minimum at `γ_crit=2εμ`) — *one damping-optimum curve across 11 orders of magnitude in μ², generalizing to emergent nets 3/3 seeds.* **Lead with this** — it is the part that survives outside the designed testbed.
2. **Friction preserves, temperature erases** (CM-16a designed + the coset-diffusion law `D_θ=εT(2−γ)/(2F²γ)`): the **sign flip** `∂n₁/₂/∂γ>0` (more friction ⇒ *slower* erasure, Einstein) — the counter-intuitive headline.
3. **The refrigerator vault** (v5-gate R3): the shipped absorb-only `γ_φ(q)` hole is a **107.77 ± 4.78× memory vault** — a brake *and* a refrigerator (`T_local=1.26e-4` vs `1e-3`), because it damps momentum without rescaling noise. **NOT 13.9×** (that coupled-bath number is retracted).
4. **The mandatory scope + honest negative (C-9, → appendix + a main-text sentence):** **CM-16a is designed-only.** On emergent (MLP) nets the coset direction is a mid-spectrum massive mode (1.7–4.9× softer than the *stiffest* mode), any written value relaxes completely, capacity ≈1–1.6 bits — **an emergent CLU has no continuous coset register** (N46). This is the "designed symmetry is the precondition, not a convenience" point — it *strengthens* the paper (it says exactly when the physics buys you memory).

## Binding wording rules
- **`langevin_noise="fdt"` AND Newtonian kinetic mode** in every quantitative caption (CM-16 mandatory flag; the laws are undefined under `legacy` and in relativistic mode per CM-17).
- **Never quote `n₁/₂` without `Δ` and `ℓ_θ/Δ`** (the instrument warning — raw exponents don't discriminate designed vs emergent).
- **Erosion novelty (venue-follow-up §3):** the erosion-as-symmetry-restoration material ships as *"a sharp, quantified instance + the degeneracy demarcation + the frequency-horizon law + the anchor cure"* — claims (b) demarcation and (c) frequency-law are NOVEL; (a) inversion and (d) anchor are **cite-the-substrate** (Fischer-Igel / Nijkamp / Décelle-line RBM arXiv:2503.21536 for (a); Du & Mordatch 1903.08689 for (d)). **NEVER claim we discovered CD is biased.** Preserve `sleep-erosion-study §4`'s honest framing verbatim.
- **CM-3:** no energy-as-superior-signal claim anywhere.
- Cite the F5 note as `(Anonymous, 2026)`, third person; no cross-short citations (C-8).

## Structure (appendix-maximalism, C-10)
Main text (≤4pp): the (μ,γ,T) cube + the sign flip + the vault + the designed-symmetry-precondition sentence + one figure (the V-curve across μ² decades is the natural headline). Everything else — the erosion-as-restoration transition, the coset-diffusion derivation, the emergent-arm negative (N46) in full, the Coleman/Mermin-Wagner T>0 caveat, the anchor-cure recipe — to appendices, fully written (ICLR/NMI feedstock; do not pre-prune).

**Acceptance:** a building draft (draft.md + draft.tex + clean PDF); thesis led by CM-16b, CM-16a scoped designed-only with N46 owned; the vault at 107×; every caption carries the fdt+Newtonian flag and `Δ`/`ℓ_θ`; erosion novelty shipped per the rules; matrix-consistent with V2; F5 cited as Anonymous. Report any place V5 and V2 could be read as contradicting each other.
