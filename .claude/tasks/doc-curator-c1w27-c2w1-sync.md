# doc-curator-c1w27-c2w1-sync — the THREE owed passes: C1W27, all of C2W1, and now all of C2W2

> ## ✅⭐ UNPARKED — LAUNCH IMMEDIATELY, EARLY IN C2W3, AND THE SCOPE HAS GROWN TO THREE PASSES
> **Head ruling ADDENDUM 2 §A9.12, 2026-07-31, verbatim: *"The curator unparks: the parked 3-wave sync
> (C1W27 · C2W1 · C2W2; task file `.claude/tasks/doc-curator-c1w27-c2w1-sync.md`) runs in C2W3, EARLY —
> a paper wave cannot cite registries three waves stale."*** The C2W2-era park (Head Q4, "skip for now")
> is **lifted**. This task now folds **three** passes: **Pass A (C1W27) · Pass B (C2W1) · ⭐ Pass C
> (C2W2, new — §3A below)**.
>
> ⚠ **WHY "EARLY" IS LOAD-BEARING THIS WAVE AND NOT A PREFERENCE.** The C2W2 gate fired negative, **B′
> is ACTIVATED**, and C2W3's spine is **the audit paper**. Every other spoke in this wave is currently
> operating under a standing instruction to quote **only** `.claude/outputs/*` and the §10 entries,
> because `negative_results.md` (**N122**), `claims_matrix.md` (**v2.5, pending Hub confirmation**),
> `research_roadmap.md` (**v0.9**), the ledger (**⟲ w26**) and the primers (**§11.15**) are **three
> passes behind**. **You are what lifts that restriction.** A registry that is silent about a result is
> **not** evidence that the result does not exist.

**Campaign 2, wave C2W3. Agent:** doc-curator. **No worktree, no production code. Launch immediately —
you gate on nothing, and three other spokes are waiting on your output.**
Charter **ADDENDUM 2 §A9.12 and §A11 task 6** (*"the unparked 3-wave sync, early in the wave, incl. the
never-quote fold"*).

**Read first:** `.claude/AGENT_PROTOCOL.md`, `.claude/handover_context.md` **§10: the `[C1-CLOSE]`
entry, the `[C1W27]` review entry, BOTH `[C2W1]` entries, and ⭐ the live `2026-07-31` `[C2W2]` review
entry** (they are the primary evidence for this pass), `.claude/advisor-head-c2-charter.md` **IN FULL —
base charter + ADDENDUM 1 + ⭐ ADDENDUM 2 (§A7 the gate verdict · §A8 the interpretive findings · §A9 the
twelve rulings · §A10 the Route-3 prereg · §A11 the C2W3 scope)**, which is the C2 frame your entries
must be consistent with **and which strikes-and-annotates parts of Addendum 1 in place**,
`.claude/advisor-head-intervention.md` **§5/§6/§8**, and the outputs named below.

---

## ⭐ DIAL DECLARATION (protocol §7) — echo before your first result
- **Dial:** none — transfer documents. **Laundering control:** n/a; but every entry you write must carry
  its control's verdict where one exists (a performance number without its launder does not get filed).
- **Falsifies:** an entry that cannot be traced to a landed output file or a §10 review entry.
- **Does NOT falsify:** a negative that is larger than expected; an entry that supersedes a Hub wording
  (three curator passes have corrected the Hub and each correction was adopted).

---

## 0. Why two passes
- **C1W27's sync was never written.** C1 retired with it explicitly owed and unwritten (`[C1-CLOSE]`:
  *"until it lands, C1W27's own results are NOT in the registries"*). C1 has no Hub. The results are
  real, they are quoted in the review entry, and no future agent can cite them safely from the registry
  because they are not in it.
- **C2W1's pass has nothing on disk at all** — six landed tasks, four independent task families, 1764
  probe cells, and a structural positive, none of it registered.
⚠ **Registry state at your start:** `negative_results.md` through **N122** · `claims_matrix.md` **v2.5**
(v2.5 is curator-written and **pending Hub confirmation** — the Hub confirms it at this wave's review;
propose **v2.6**) · `research_roadmap.md` **v0.9** · ledger through **⟲ w26** · primers through §11.15.

## 1. Pass A — the owed C1W27 fold (12 items, listed by the C1 Hub itself)
From the `[C1W27]` review entry's "CARRIED INTO THE NEXT CURATOR PASS" block, verbatim:
the **R2 close** (both d-points, the **pinned vs lower-bounded** distinction, and the **m=4 is d=4-only**
correction with its measured **write-side** mechanism) · the **arms-are-substitutes** interaction
(`I = −0.1509`, **18.3 SE**) · ⛔ **Candidate 3's death** (**0.0243 bits/param** vs the designed
construction's 1.14 and a transformer's 2) · the **`tol` → `decode` default** and its reach into
`exp_write_ceiling` · the **P2 waitlist** removing the capacity qualifier (AUC **0.5000 ± 0.0000** at
every load **0.29×–1.71×** capacity) **+ the w26 harness-artefact correction** · **`R₅₀` survives the
gate** + **B3 as an effect size, not a correlation** (retention std **0.274 → 0.016**, worst codeword
**0.398 → 0.972**) · the **τ_y refutation** (`γ_read = 0` ⇒ oscillatory, `T = 2π/√(κ(g₀+A))`, slope
−0.5264) · **(★) causal but a LOG lever** (2.27× buys **+9.3 %** reach; the `|a|=1.0` rung is
**unreachable at any α ≥ 1e-4**) · the **Guo citation defect** (**"Def. 1/Def. 2" does not exist**;
ε-certified removal is §3 **Eq. (1)** inline ⇒ **CM-22(m)** + a BibTeX note) · the **sd-convention split**
(r2-*geometry*-revival quotes **sample** sd; r2-*excursion*/d-sweep quote **population** — ⚠ the C1 Hub's
own task file asserted "population" as the r2 convention and **that was wrong**) · **CM-22(q)/(r)**
unblocking.
⭐ **Plus two corrections the C1 Hub found at verification and flagged for you:** the **d=8 wording
amendment** — the designed wall is **bracketed at d=4 and d=6, LOWER-BOUNDED at d=8** (K=1024 passes at
0.9785; **K=2048 was never run**) — and the two **α-contamination** corrections to w26 theory (the
`2α|c|` product claim is **approximate**; **`D_fit` is α-contaminated**, ≈0.37α, so w26's D = 0.910/0.459
are ≈0.89/0.44 as well depths).
⚠ **Three C1 items are NOT RULED and may not be filed as settled by anyone**: the program-wide
"certified" ruling · **M1** (amortized delete cost vs n) · the d=8 wording amendment's ratification.
File the first two as **open questions**, not decisions. (The d=8 amendment is mechanical; propose it.)

## 2. Pass B — the C2W1 fold (the wave whose verdict is a null, honestly met)
Sources: `.claude/outputs/{full-clu-harness,memory-gym-v0,trainability-spike,trainability-spike-theory,
controller-doctrine,rival-recon}.md` + both `[C2W1]` §10 entries. N-entries owed, at minimum:
1. ⭐⭐ **The v0 dividend null** — ≈0 or negative on **all four** gym families (`overload/base`
   **−0.6852 ± 0.0668** · `aggregate/base` **−0.0789 ± 0.0620** · `aggregate/tight` **+0.0084 ± 0.0348**
   · `recency` **−0.1477 ± 0.0495** · `manifold` **−0.1802 ± 0.1708**), and the shipped anchor at
   **exactly 0.0000 with `D = 0`**. Charter §2.1 **pre-registered this outcome**; file it as such.
2. ⭐⭐ **The byte-floor theorem** — `ratio = atoms_per_item·(dim+2)/dim + d/dim = 1.4·A + 0.8`,
   independent of K, verified to **1e-9 in all 28 cells** ⇒ **`ratio ≥ 2.20×`; matched bytes is
   UNREACHABLE BY CONSTRUCTION** under a masked write (measured min **2.28×**). ⛔ New never-quote:
   **any gym cell as a byte-matched dividend**; and retire *"load the store beyond the launder's table"*.
3. ⭐⭐ **`∂q*/∂q₀ = 0` exactly** — `‖∂L/∂φ‖` **0.0** (implicit) / **2.654e-9** (unroll) / **6.421e-3**
   (trajectory), ratio **2.42e6**: a settled-point read sends **no** gradient to its read-in. The exact
   form of N61, and a benchmark-free structural argument.
4. ⛔ **Pillar 1's Stage-0 null** — 0 qualifying cells of **588 × 3 seeds**, `γ_read ∈ [0, 0.2]`
   *including `γ_read = 0`*, strides 1–32, 4 targets, 4 probes, 3 ambiguity bands; the only
   cross-seed-replicating effect is **31–63 % reproduced by a store with nothing in it**. File with its
   scope: *this harness, at v0, with a store never written for the trajectory.*
5. ⛔ **The trivial-substitute audit, 0-for-4** — a **+0 B** substitute of the launder's own table
   matches or beats CLU on every family (order **0.776** vs 0.302; echo **1.0000** vs −0.180).
6. ⭐ **`D` is the dividend's VARIANCE, not its magnitude** — the largest `D` (0.931) has the **worst**
   dividend (−0.875). ⛔ Never a progress signal.
7. ⛔ **No flat directions exist** — `λ_min ≈ 2α = 0.10` at **14/18** sites; and a multi-target **ridge
   write is a SADDLE** (`λ_min = −0.5946`, spectator participation **1.000**), because `write_loss`
   never constrains the connecting path to equal depth.
8. ⭐ **The annealed read as a controller verb** — acquisition 0.828 → **1.000**, decode 0.906 →
   **1.000**, clearing #5 and #9 simultaneously at **+0 bytes**; it introduces a #1 trip that 2× read
   steps buys back.
9. ⛔ **The physics buys nothing separability is not already providing** — at `sep/σ_q` **3.07** the CLU
   reads **0.479** while its own launder holds **0.854**.
10. **Monitor first-fires and defects** — #3 (×190, both legs), #6 (applicable for the first time, ×58 —
    **29 of them an ε-artefact**), #11 (×7); **#4 blank passes everywhere (0/56)**; **#10 and M14 remain
    unexercised across the whole of C2W1 — untested, not green.**
11. **Prop D1's `sep/2` proxy fails where `U → 0`** (up to **7.44×**) and **holds where `U` is large**
    (0.81–1.04) — the error is **structured**, not loose.
12. **The gradient machinery, confirmed** — implicit vs re-settled FD **5.11e-10**; truncation `= ρ^k`
    ±5 % at k=180/270/449; ridge bias **4.134 %** at the predicted 0.35424; ψ trajectory cost **17.1×**.
13. **`rival-recon`'s positioning results** — MAD/zoology/MQAR **inadmissible as Track-2 primary**; the
    anytime curve is a **shape** claim, not a uniqueness claim; **Sparse Delta Memory arXiv:2607.07386**
    as a near-collision; and the favourable half: **every rival family surveyed is metric-native or
    weakly so — the launder ceiling is the field's problem, not ours alone.**
14. **Two mechanism findings that generalise** — eviction must **RE-DRAW** a freed atom group (zeroing
    starves the next item *and* leaves a membership trace); **`place` needs an INJECTIVITY guard**, not
    just `λ_min > 0` (a shared minimum is a perfectly good minimum, and it committed two items to one).

## 3. Also owed in this pass
- **The never-quote list, consolidated and current** — merge the `[C1-CLOSE]` list, the C2W1 additions
  (any gym cell as a dividend · *"the trajectory axis knob is inert"* → say **"nothing consumes the
  buffer"** · `k* ≈ 269` without *"of `∂q_N/∂θ`"* · the refuted address-leak prediction · `sep/2` as a
  certified inradius · `D` as a progress signal), and `rival-recon`'s additions (§Deliverable 5 /
  handover item 2). One list, one place, dated.
- **`claims_matrix` → v2.6** with the C1W27 + C2W1 rows, and confirm-or-dispute v2.5's pending status.
- **`research_roadmap` → v1.0** — it is **w25-current** and two campaigns behind. The C2 charter and
  Addendum 1 now govern direction: reconcile, do not duplicate.
- **The ledger (`philosophy-synthesis.md`) ⟲ C1W27 and ⟲ C2W1**, and **`HEP_primers.md`** — the C2W1
  physics that belongs there: the Ward identity and project-and-transport, the pitchfork-vs-saddle-node
  crossing taxonomy (exponents **1.03** vs **0.49–0.50**), Liouville forbidding an attractor at `γ = 0`,
  and the pseudo-Goldstone tilt ruling (**never exactly flat**; `λ_min = ε`; lifetime **∝ 1/ε**).
- **A §7 audit of all six C2W2 task files AND all six C2W3 task files** (the dial block must **open** the
  file, after at most a short administrative header, never behind rationale prose — the w25 slip, folded
  as a template rule at w26).
- ⚠ **The version targets grow with Pass C:** `claims_matrix` → **v2.7** (not v2.6 — v2.6 absorbs
  C1W27+C2W1, v2.7 absorbs C2W2); `research_roadmap` → **v1.0**, and it must now reconcile against the
  **C2 charter + Addendum 1 + ADDENDUM 2**, where Addendum 2 supersedes parts of Addendum 1 in place
  (§A4.2 struck, C2W3's factored store superseded by §A11, C2W5 collapsed into B′ by §A9.11).

---

## ⭐ 3A. Pass C — the C2W2 fold (NEW; this is why the task was unparked)
Sources: `.claude/outputs/{traj-write-objective,ssb-shell-atoms,phi-particle-head,doctrine-repairs,
track2-admissibility}.md` + `.claude/outputs/track2-admissibility/PREREG-Bprime.md` + **the live
`2026-07-31` `[C2W2]` §10 review entry** + **charter ADDENDUM 2 (§A7–§A11)**. ⛔ **Take the scope from
the §10 review entry and Addendum 2, not from any single spoke report.**

### 3A.1 The wave's verdict — file it as the pre-registered outcome it is
1. ⚖⚖ ⭐⭐ **THE C2W2 GATE FIRED NEGATIVE AND `B′` ACTIVATED.** Zero families cleared 0 by 2 SE on
   **either** route; the grade is **not** "weak proceed" — a **clean ≤0 on both routes**. Route 1:
   9 `le_zero_vote` arms, `aggregate`/`manifold` **ABSTAIN** 0/12 each after spending their one bounded
   escalation. Route 2: **90 cells**, 16 `le_zero_vote` · 12 `abstain` · 2 `under_powered_grid`, best
   multi-seed arm `manifold/shell_fixed` at **−0.0072**, best single cell **+0.0005 (n=1)** against an
   `echo` substitute of **1.0000**. Both §A6-era admissibility rulings **did real work in both
   directions**. File the branch AND the grade.
2. ⭐ **Asking the write to put information in the trajectory is a MONOTONE COST**, and that is the
   honest reading — not "no effect". On the one family where the store demonstrably works:
   **−0.0278 (endpoint-only control) → −0.1111 → −0.1806 → −0.1944 → −0.2639** across a three-decade
   `λ_traj` grid; the `path` term is worse than the `traj` term; both together **−0.4722**.
   ⚠ **Scope it exactly as the engineer declared it: the null is at `rollout_steps = 60`**, with a
   measured mechanism — the write loss is exactly **`L_endpoint + 0.55·λ`**, a constant the optimiser
   never reduces, because a 60-step path reaches only **0.27–0.50** of a payload the read climbs over
   **400** steps. ⛔ **And file the Advisor's ruling with it: the read-length re-price is REPLACED, not
   deferred** — §A8.1 rules the read-length requirement *an artifact of the payload convention, not
   intrinsic*, and the store-attribution curve replaces it. Do not file "run a longer rollout" as owed
   future work; it is explicitly retired.

### 3A.2 The refutations — charter-amendment weight
3. ⛔⛔ **Charter §A4.2's pseudo-Goldstone TILT is REFUTED on a learned store**, in **sign**: tilt
   **monotonically REDUCES** `λ_min` (**+0.0994 → −8.28** envelope; **+0.3291 → −1.198** depth), on
   **two independent implementations** and **every family**. ⭐ **The mechanism is measured, not
   guessed: a designed degeneracy does not survive superposition** — `tilt_vacuum_residual`
   **0.140–0.343** against an ideal of 0 and a *random-orientation* baseline of **1/dim = 0.167**, with
   a group's atom-centre spread **1.19–1.95×** the designed shell radius ⇒ **a written site is not a
   common vacuum of its own atoms; it is at or WORSE than random.** ⭐ **`λ = ε` holds only in the
   single-atom geometry it was specified in** (unit-tested to 5 %). **The pseudo-Goldstone ruling
   survives as GEOMETRY; its shipped instantiation does not.**
4. ⛔ **`ε` is NOT the manifold-payload lifetime dial.** The registered damped-mode floor `τ = 2/Γ = 5.0`
   is **CONFIRMED (4.23–6.58)** and the above-knee slope **0** confirmed, but **the `1/ε` branch is
   unreachable** — the shipped confinement floors `λ_soft` at **2α**, **2.5× above the knee**. ⇒
   **`τ_max = Γ/2α = 4.0`; `α` is the ceiling, and lowering `α` breaks the write.** ⛔ **Every
   manifold-lifetime claim now carries the 2α coercivity coupling** — this is a new never-quote and it
   supersedes the C2W1 primer text you were told to file in §3 (*"the pseudo-Goldstone tilt ruling —
   never exactly flat; `λ_min = ε`; lifetime ∝ 1/ε"*). ⚠ **File the C2W1 version and the C2W2 refutation
   together, in that order, as the record of a charter claim being tested and failing.**
5. ⚠ **A third, weaker and more insidious failure mode, new to the taxonomy: THE WRITE IS BLIND TO THE
   DESIGN.** Learned shell radius `r` moves **0.500 → 0.501** in 300 steps. w20 said free learning
   *erases* design; C2W2 adds: **design the objective cannot SEE is neither erased nor used.** This
   belongs in the ledger and beside monitor #8 in the primers — it is a distinct mode from w20's.

### 3A.3 The positives — preconditions, not dividends, and file them as such
6. ⭐⭐ **CHARTER §A2.2 IS SUPPORTED — the first measurement in 29 waves.** A trajectory read sends
   **1.74e-3 / 1.17e-2 / 1.88e-3** to a per-query **mass** and **2.4e-2 / 4.1e-2 / 3.3e-2** to a
   per-query **friction**, while the settled-point arm sends **8.7e-9 / 4.0e-8 / 1.1e-8** (unroll) and
   **exactly 0.0, BITWISE** (implicit) ⇒ ratios **1.7e5–2.9e5** (mass), **2.6e5–4.9e5** (friction),
   **3/3 seeds**, cross-checked against float64 finite differences. **"Mass as selector" is live for the
   first time**, and the point arm's zero is **structural (Prop Q1.1: `∇V` contains neither `M` nor
   `γ`)**, not lucky. ⭐ **Friction is the ~14× stronger channel.** ⚠ Its declared band's low end is
   **monitor-#1 hot** (`γ ≤ 0.03` trips on S0).
   ⭐⭐ **File it JOINTLY with C2W1's `∂q*/∂q₀ = 0` as ONE statement** (§A11 task 5 frames it this way and
   the theorist is writing it): **a settled-point read is untrainable end-to-end, IN BOTH DIRECTIONS.**
7. ⭐⭐ **§A2.5 ADJUDICATED, and the answer is BOTH** — confirmed **7/7** under the shipped rule (0
   witnesses in a 72-config extension), **REFUTED 11/12** under the soft certificate. ⭐ **The cause is
   structural and nobody had seen it: the shipped harness sets `d_safe := 2s_max + κ′σ_q`, so the
   admission radius IS the certificate radius — the two "mutually exclusive" bands are ONE OBJECT.**
   Decouple them (`d_safe = 0.6·sep`) and **11 of 12 configs clear all 13 bands while violating the
   certificate by 1.5–32.9 % of `sep`**, `ρ_ex` up to **6.3×** at a `λ_min` price of **2.2–6.0×**.
   ⛔ **And file the honest half in the same entry: the dividend there stays ≈0** (+0.0043 … −0.0067),
   exactly as §A2.1 predicts. ⭐ **The non-separable region is a PRECONDITION, not a result.**
8. ⭐ **Soft-certificate spec SC-1…SC-7 delivered**, violation budget **`B = 0.33`** of `sep`, and it is
   now **standing harness doctrine, orthogonal to paper shape** (§A9.8). ⚠ **`B`'s outer edge was
   located by a broken proxy** — R1's corrected radius is a **prerequisite** for setting it, not an
   independent cleanup. File the budget with that caveat attached.

### 3A.4 The instrument — three defects found and fixed, and one escape hatch closed
9. ⭐ **The recency family was a HARNESS DEFECT, not a null.** `queries_recency` grades a **K-way**
   answer on a **2-way** curve and the CLU's answer falls outside its own pair **19.4 %** of the time.
   Fixed behind `restrict_index_to_pair` (**default off** ⇒ old numbers preserved). Post-fix dividend
   **−0.0028 ± 0.0619** (was **−0.1477 ± 0.0495**). ⛔ **RESTATE every site quoting `0.3019 ± 0.0679`
   as a null** — including the gym's F3 row (reconciliation 3) and Pass B item 1 above, which you are
   filing in the same session. **They must not disagree.**
10. ⭐⭐ **D5 closes the last escape hatch: the longer-write curve is FLAT.** 6× the write budget moves
    decode by **−0.014 / +0.056** against a registered **≥ +0.25**; the loss plateau **0.20–0.22** is
    unmoved. ⇒ **the gym's sub-shipped cells are a genuine CAPACITY result, not an optimisation
    artefact** — the plateau is the atom budget's expressivity floor. ⛔ **Restate the gym's §8.1
    wording from "the store was never written" to a capacity statement** (reconciliation 4).
11. ⛔ **D6 / spike R-4 CLOSED and it FIRES: `AttentionPsi` leaks `φ(x)`** — reads `q0_only` at
    **0.35–0.45** against a bar of **0.19**, **at every stride**, blank store **0.37–0.47** ⇒ **the
    pooled-vs-attention hypothesis is CONFIRMED and NO attention-ψ trajectory number is quotable
    store-relative** (reconciliation 1; the quarantine lands in C2W3). New never-quote.
12. ⭐ **Monitor #6's repair, and its arithmetic — get this exactly right, three C2W1 rows depend on it.**
    `n_trips_pre_repair` **58** → `n_trips_post_repair` **27**, **`n_new_trips = 0`**, every other
    monitor **bit-identical** across all 28 C2W1 gym cells; killed readings sit at `|slope| ≤ 5.19e-17`
    against survivors `≥ 8.13e-7` — a **10-order** gap, so `eps` is **not a tuned threshold** (its
    decision is invariant over **12.8 orders**). ⛔ **The artefact count is 31 of 58, NOT 29**
    (reconciliation 8 — and Pass B item 10 above says "29"; **you are filing both, so fix it there
    too**). ⛔ **C2W1's monitor rows must be filed in their REPAIRED form, with the pre-repair numbers
    kept as the defect's evidence** — and **"58 trips" without *"pre-repair"* is never-quote.**
13. ⭐ **19 cell-level `monitor_table` rows change** (`objective_divergence: TRIP → clear`);
    **§5.3 of `phi-particle-head.md` is the only corrected record until you file it** (reconciliation 10).
14. ⭐ **Monitor #3's validity leg is REPLACED, not repaired**: the correlation leg is **retired**
    (sign-unstable, four named causes) → a **C3 first-order calibration test**, spearman **+0.914 vs
    +0.412**, **0/12 sign flips vs 1/12**, **at zero extra cost**. And **monitor #10's "dead axis"
    wording** is corrected (R-3).
15. ⭐ **The rig is bit-faithful across two independent branches** — the `gauss` control reproduces
    `memory-gym-v0` digit-for-digit (`overload` per-seed 0.1667/0.3889/0.2222 ⇒ **0.2593**), and the
    r=0 gate is bit-identical on **12/12** cells in **both** `full` and `settle_deleted_launder`, in f32
    **and** f64, including at `q == c`. ⚠ **The shell's byte surcharge is real and predicted:
    52.00 → 58.40× at `dim = 6` = +12.5 % on the atom term, exactly `1/(dim+2)`** — **a basis change
    RAISES the architectural byte floor.**

### 3A.5 The theory corrections — file with their DOMAINS, every time
16. ⭐⭐ **"Prop D1 is violated (1.5–7.44×)" is RETIRED.** `D ≤ U` is a theorem **under a certified
    ball**, and the gym computed `U` from `sep/2` on stores whose sites **were not minima** — **all 7
    cells with `D/U > 4` have `λ_min ∈ [−1.199, −0.372]`.** The corrected inradius is **14.55×** more
    accurate **inside its stated domain**, `s/sep ∈ [0.15, 0.30]`. ⛔ **This supersedes Pass B item 11
    above — you are filing both in one session; make them agree.** New never-quote: **`sep/2` as a
    certified inradius**, and the corrected proxy **outside** its domain.
17. ⛔ **Two new blockers, both load-bearing:** below **`s/sep ≈ 0.15`** the basin boundary is
    **INERTIAL**, not a static watershed — **no static proxy is valid there**; and **`λ_min > 0` does
    NOT certify a nonempty basin** — a genuine minimum at **`λ_min = +0.910`** measured a capture radius
    of **0.000**. Both are new never-quotes.
18. ⭐ **Truncated BPTT is parameter-selective:** **`k*` governs `∂q_N/∂θ` ONLY where fixed-point
    sensitivity dominates the transient** (ratio 64× ⇒ holds; **27 396× ⇒ error 0.448, flat in `k`**) —
    and in a K-item store **the far-well parameters are exactly the interference gradients.**
    ⛔ Never quote `k*` without that qualifier (it extends the C2W1 `k* ≈ 269` never-quote).
19. ⚠ **Reconciliation 9 — doctrine I-7's gauge is `newtonian_learned`-ONLY, not "Newtonian".** Under
    `newtonian_identity` the orbit is **not a gauge orbit at all** (residual **0.2505** vs **2.52e-7**).
    ⭐ **Joint with the theorist** (`bprime-theory` T4.6) — take the corrected wording from their report.
20. ⚠ **Reconciliation 2 — the ridge saddle `λ_min = −0.5946` is SINGLE-SEED (seed 0).** The 3-seed
    mean at `λ_path = 0` is **+0.1770 ± 0.4694** (−0.5946 / **+1.026** / +0.0996) ⇒ **charter §3.5's
    "a multi-target ridge write produces a saddle" needs a multi-seed qualifier.** ⛔ **This supersedes
    Pass B item 7 above**, which you are filing in the same session with the bare −0.5946. **Fix it
    there.** New never-quote.
21. ⚠ **Reconciliation 11 — the theorist's 10-item site list (S1–S10)**, including their own two
    errata: the **γ-band row mixes tolerances**, and the **`sep/2` 4.8 % figure needs its validity
    domain.** ⭐ **Joint with the theorist** (`bprime-theory` T4.7).

### 3A.6 Positioning — the literature corrections, and they are urgent
22. ⛔⛔ **ZERO synthetic Track-2 candidates survive.** `rival-recon`'s single admissible synthetic is
    **OVERTURNED by arithmetic**: MAD `compression`'s own iso-state normalisation (**4096 dims**)
    exceeds the task's maximum payload (**224 B**) by **73× at fp32 / 36× at bf16** ⇒ compression in MAD
    is **never beyond-capacity**, and criteria **2 and 3 both fail**. ⭐ **Combined with §A2.3, pillar
    (a) has no admissible public venue at this weight class.** ⛔ New never-quote: **"MAD `compression`
    is the admissible synthetic"** — and it **supersedes** whatever Pass B item 13 files from
    `rival-recon`. **Make them agree.**
23. ⭐ **Exactly one Track-2 primary survives, and properly: enwik8/WikiText-103 at 26–47 M params.**
    The **+0 B pure-kNN** read is **degenerate** (λ=1 ⇒ ∞ perplexity, Xu et al. ICML'23; tuned
    λ ≈ 0.25) and count-based PPM sits at **1.52–1.71 bpc** against **1.232** for a 47 M AWD-LSTM.
    ⚠ **But criterion 3 is weak for a PUBLISHED reason** — Krause et al. ICML'18 reach **1.08 bpc** on
    Hutter Prize by **gradient adaptation to recent history alone** ⇒ **binding: the dividend must be
    claimed on a long-range slice with three mandatory substitute columns INCLUDING dynamic evaluation,
    and if it vanishes once dynamic evaluation is in the table, the primary is dead.**
24. ⭐ **The B′ pre-registration is FILED** (`.claude/outputs/track2-admissibility/PREREG-Bprime.md`,
    **written before the gate was adjudicated — so it is a genuine prereg**), with **P1–P5** and its own
    falsifiers **FB1–FB5**. **FB4 is the wave's first run.** File the prereg as a citable artifact.
25. ⚠ **Pillar-4 near-miss: MUNKEY (arXiv:2603.15033, ICML 2026)** publishes unlearning-by-design
    evaluated with **MIA AUROC → 0.5 — our own instrument** — but it is **NOT exact** (gap to retraining
    **0.56 ± 0.21**) and is a **ViT classifier, not a sequence memory** ⇒ **our claim SURVIVES,
    materially NARROWED.** ⛔ New never-quote: **"we alone delete"**. And say the obvious thing before a
    referee does: **a table deletes exactly by construction** — exact deletion is a result only for a
    *learned/superposed* store.
26. ⛔ **Citation corrections, all urgent, all propagating:** **Titans is NeurIPS 2025, PEER-REVIEWED —
    every "preprint" citation is wrong**; **Sparse Delta Memory (arXiv:2607.07386) has official code but
    needs SM 80+ CUDA ⇒ unrunnable on this machine**, and **its Table 1 ratios CONFLICT across two
    extractions ⇒ quote NONE of them**; the **`2·|M_θ|` Titans momentum accounting is OUR
    RECONSTRUCTION — the paper states no convention — and is captioned every time.**
27. ⭐ **The learned-initial-state byte rule (B′ §4.1), and it is ours:** for any memory with a learned
    init (TTT `W₀`, SDM `M₀`, our `V_θ` init) **the initialisation is PARAMETERS; only the per-sequence
    deviation is STATE. Both declared.** Counting the init as state **inflates**; counting the deviation
    as parameters **launders**. File it as a protocol convention, not a passing note.

### 3A.7 The Addendum-2 rulings themselves
28. **File the twelve §A9 rulings** as the C2 direction of record, and mark the Addendum-1 text they
    **supersede in place**: **§A4.2's tilt instantiation refuted** · **§A1's ridge saddle qualified as
    single-seed** · **C2W3's factored store SUPERSEDED by §A11** (deferred, **not closed**, and any
    future revival inherits **§A9.9's deletion-vs-sharing frontier**) · **C2W5 COLLAPSED into B′**
    (§A9.11; the surviving Track-2 primary becomes B′'s **optional real-data leg**, Head-reserved at the
    C2W3→C2W4 boundary, **no compute pre-committed**).
29. ⛔ **Do NOT fold C2W3's own results** — they are not measured yet. If a C2W3 branch has landed by the
    time you write, ignore it; the C2W3 fold is a C2W4 pass. ⚠ **Exception:** if `bprime-theory` or a
    C2W3 engineer report lands a **correction to something you are filing** (reconciliations 1, 9, 11 are
    all jointly owned), take the correction — that is not folding a result, it is not filing a known
    error.

### 3A.8 ⛔ C2W2's declared NOT-RUNs — file as NOT-RUNs, never as nulls
The **Route-1 × Route-2 2×2** (condition not met at Route 2's final run; **both engineers independently
flagged it as the wave's most informative unrun cell**; ⭐ **funded and running in C2W3**) · **a
read-length write rollout** (400–1200 steps — ⛔ **explicitly REPLACED by §A8.1's store-attribution
curve, not owed**) · `manifold`/`aggregate` **at the shipped atom budget** (only `overload` got the
permitted escalation) · **rank>1 tilts, per-atom tilt directions, a learned `ε`, and a tilt direction
CHOSEN to minimise the measured vacuum residual** · **the trajectory launder on Route 2** (no ψ there
sees the address block) · **OpenReview seed conventions** (bot-blocked, twice, reported).

### 3A.9 ⛔ The consolidated never-quote list — this is the deliverable other spokes are waiting on
Merge into **one dated list, in one place**: the `[C1-CLOSE]` list · the C2W1 additions · `rival-recon`'s
additions · and **all of C2W2's**, which are currently homeless except for the §10 entry and the reports:
monitor #6's **"58 trips"** without *"pre-repair"* (**27**) · **"29 of 58"** as the #6 artefact count
(**31**) · the ridge saddle **`λ_min = −0.5946`** as multi-seed (**seed 0**; 3-seed mean **+0.177 ±
0.469**) · the recency family's **`0.3019 ± 0.0679`** as a null (**scoring-domain defect**) · **"Prop D1
is violated (1.5–7.44×)"** (retired) · **`sep/2`** as a certified inradius, and the corrected proxy
outside `s/sep ∈ [0.15, 0.30]` · **`λ_min > 0`** as certifying a nonempty basin (**0.000** at
`λ_min = +0.910`) · **`k*`** without *"of `∂q_N/∂θ`, and only where the fixed-point sensitivity dominates
the transient"* · any **`AttentionPsi`** trajectory number · **`ε` as "the manifold-payload lifetime dial
∝ 1/ε"** without the **2α** coercivity ceiling · any **SDM Table 1 state/param ratio** · **Titans as "a
preprint"** · **"MAD `compression` is the admissible synthetic"** · **"principled forgetting"** as a
novelty phrase · **"we alone delete"** · any C2W2 **or C2W3** cell as a **byte-matched** dividend
(**min ratio anywhere is 17.11×**; the shell *raises* the floor by `1/(dim+2)`).

## 4. Falsifiers / discipline
- ⛔ An entry that cannot be traced to a landed output file or a §10 review entry ⇒ do not file it; list
  it as *unsourced, needs an owner*.
- **Tiers:** propose them with rationale; mark them **CURATOR-PROPOSED** until the Hub confirms
  (standing convention). **Dispute a Hub wording where the evidence disputes it** — three passes have
  done so and every correction was adopted.
- **Declared NOT-RUNs are filed as NOT-RUNs, never as nulls** (the declined CIFAR entry is the standing
  precedent). C2W1 has several: the anisotropic-query-law cell, F2/F3/F4 at the shipped atom budget, the
  longer-write arm, a real #11 reach probe, the 2×2 combination.
- ⚠ **Do not resolve the three unruled C1 items.** File them as open.

## 5. Compute / priority
⭐ **Priority P0 — EARLY IN THE WAVE, by Head ruling §A9.12** (*"a paper wave cannot cite registries
three waves stale"*). No worktree, no engineer slot, no compute; **launch immediately**, in parallel
with `bprime-fb4-gate`, `route3-stage1-plus-2x2` and `bprime-theory`. Estimated ~6–9 h for three passes.

⚠ **Sequencing note the Hub will act on:** the moment your pass lands, the standing *"quote only
`.claude/outputs/*` and §10"* restriction in every C2W3 task file is **lifted by the Hub**, not by you.
Do not tell other spokes it is lifted; tell the Hub, in your `## Proposed handover updates`.

⚠ **Three passes in one session is the risk this task carries.** Pass B and Pass C **contradict each
other in at least five places by construction** (recency's null · the #6 artefact count 29 vs 31 · the
ridge saddle's seed count · Prop D1's "violation" · MAD `compression`'s admissibility) — because Pass B
files C2W1's record and Pass C files the C2W2 measurements that corrected it. ⛔ **File the corrected
form as the entry and the superseded form as the entry's own history — never both as live facts.**
Every one of those five is called out at its Pass-C item above; if you find a sixth, say so.

## 6. Output
`.claude/outputs/doc-curator-c1w27-c2w1-sync.md` (protocol §5 format) **plus the direct edits** to
`negative_results.md`, `claims_matrix.md` (**→ v2.7**), `research_roadmap.md` (**→ v1.0**), the ledger
(**⟲ C1W27, ⟲ C2W1, ⟲ C2W2**) and the primers, per each document's own protocol. ⛔ **Do not edit
`.claude/handover_context.md`** — put proposed handover updates in your report's
`## Proposed handover updates` section. Report to the Hub.

⭐ **Lead your report with the consolidated never-quote list (§3A.9) and the five Pass-B/Pass-C
contradictions you resolved** — those are the two things three other spokes and the audit paper are
waiting on.
