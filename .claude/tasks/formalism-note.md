# Task: formalism-note (F5) — the unified CLU-Net formalism all our papers import

- **Agent:** `physics-theorist` · **Output:** `.claude/outputs/formalism-note.md` (+ scratch scripts in `.claude/scratch/formalism-note/`)
- **Read first:** `.claude/AGENT_PROTOCOL.md`, `.claude/handover_context.md` (§1–§2), `.claude/brainstorm_log.md` (**all threads — this note consolidates them**), `.claude/research_roadmap.md`, and skim `chlu/core/` (the code is ground truth for current definitions).

## Why
Three-to-four short papers must assemble into one ICLR paper in September. That only works if they share one notation, one set of definitions, one formalism from day one. This note is that artifact. It will also be read by an external HEP theorist collaborator (HEFT/SMEFT background).

## Audience & format — IMPORTANT
- **Markdown, not LaTeX.** Equations in clean fenced math or inline code that renders in GitHub preview (use `$$...$$` blocks; keep them simple enough to display). The external colleague works with well-formatted md — write for a HEP theorist: standard physics notation, explicit assumptions, no ML hand-waving.
- Self-contained: define everything; cite the CHLU paper for provenance but do not require it.

## Required sections
1. **Nomenclature (new — defines the rename):** the **CLU (Causal Learning Unit)** family — a learning primitive defined by its *lever interface* (symplectic/structure-preserving core, causal bound c, mass matrix M, friction γ, temperature T, learned potential/energy function, energy diagnostics), with instantiations as placeholders per program principle P2. The current implementation is the **H-CLU** (Hamiltonian CLU) = what the code calls CHLU; reserve **L-CLU** (Lagrangian) as the naming pattern. State continuity: "introduced as CHLU in Jawahar & Pierini (2026)."
2. **The H-CLU:** state z=(q,p); the three kinetic modes (Newtonian-identity/Newtonian-learned/relativistic `T(p)=√(c²pᵀM⁻¹p+m₀²c⁴)`); dissipative Verlet map (write the exact update from `chlu/core/integrators.py`, γ-step included); Langevin extension; the governor. Precise statements of what is/isn't preserved for γ=0 vs γ>0 (symplecticity, phase-space volume, shadow Hamiltonian).
3. **Mode taxonomy & the mass spectrum (the centerpiece):** linearization at an attractor q* — mode frequencies from `M⁻¹·Hess V_θ(q*)`; the three-mode taxonomy (flat/Goldstone = transport/memory; massive = rationed relaxation; expanding/Lyapunov = sensitivity/chaos) with the honest symplectic-pairing statement ((σ,1/σ) — no vanishing, possible explosion). Derive: (a) *"dissipation kills Goldstone momentum but cannot erase Goldstone displacement"* — make exact for the (1−γ) momentum-damping step; (b) per-mode retention half-life as a function of (mass/curvature, γ) — overdamped/underdamped regimes; (c) pseudo-Goldstone: small explicit breaking ⇒ small mass ⇒ half-life ∝ 1/mass² (state the precise exponent your derivation gives — do not copy the conjecture).
4. **Symmetry & Goldstone engineering:** Noether for our setting (continuous symmetry of H ⇒ conserved quantity ⇒ flat direction); #channels = dim(G/H); the two parameterization routes — **SMEFT-like** (linear realization: G-invariant/equivariant V_θ) vs **HEFT-like** (nonlinear realization: coset G/H coordinates, sigma-model kinetic term) — defined precisely enough for the colleague to engage; where EFT power counting would enter ("EFT of memory" — sketch only, flag as the colleague-collaboration surface.
5. **The mass matrix as budget allocator (Thread 5):** the gas-of-particles reading (d particles (q_i,p_i), mass M_i); mass = semantic inertia (boost-stiffness: response of q̇ to squeezes ∝ M⁻¹); per-particle relativistic regime (light → v≈c governor-dominated, heavy → Newtonian); mass-hierarchical timescales; block-diagonal M = composite particles; integrate-out-heavy = coarse-grained inference (RG/EFT bridge). Keep claims honest — label which are theorems vs design hypotheses.
6. **⚠ The interference problem (mandated open-problem section — Head's caveat):** particles share one V_θ(q_joint); training updates for one mode/task can overwrite another's structure (θ-space gradient interference). Formalize the problem statement; catalog candidate mechanisms with brief analysis each: (i) **modularity** — separate units own separate V, coupling only via V_c (interference bounded by coupling strength — this is why V3 scales by mass AND size); (ii) **symmetry firewalls** — different irreps of G don't mix at linear order; (iii) block/separable V; (iv) local parameterizations (RBF/local experts); (v) replay (the CD buffer as anti-interference); (vi) curvature/EWC-style protection ("mass for weights" — parameter-space inertia; assess whether this dual is precise or merely cute). No solutions required — a sharp problem statement + assessed catalog is success.
7. **CLU-Net (lattice) formalism:** joint `H = Σ_i H_i + Σ_(ij∈E) V_c(q_i,q_j)` on a coupling graph E; what survives composition (everything, if the joint map stays symplectic — state conditions); wormholes = energy-gated sparse non-local V_c; shells/hierarchy by mass sector; where boosts/squeezes act (define the squeeze operator on (q_i,p_i) pairs, show det=1/symplecticity in one line); the escalation cascade (residual energy → boost retries → shell jump) defined as an inference-time policy on this object.
8. **Notation table** — every symbol, one line each. This table is what the short papers import verbatim.

## Numerical sanity checks (small, in `.claude/scratch/formalism-note/`)
At minimum: (a) verify the Goldstone-displacement-survives-friction claim on a 2D toy (flat×curved potential, γ>0); (b) verify the derived retention-half-life formula against simulation on the same toy; (c) one-cell check that the squeeze map preserves dq∧dp numerically. Use jax/numpy; seeds fixed; report numbers in the note. **JAX cold-start on this machine is slow (~20 min possible) — budget for it; these are tiny scripts, keep the session warm.**

## Scope guards
Do NOT touch `chlu/` production code. Where a derivation contradicts something in the brainstorm log or handover (e.g., the exact half-life exponent), **your derivation wins — flag the correction explicitly** in `## Proposed handover updates`. Distinguish proven / evidenced / conjectured throughout.
