# bprime-r7-referee-closures — paper-writer

**Commissioned by the Shorts Advisor (charter: `.claude/advisor-head-shorts-charter.md`, Addendum 12; the Q6 sequence's referee-closure step, 2026-08-18).** Read `.claude/AGENT_PROTOCOL.md`, then this file. You write exactly two files: `.claude/papers/bprime/draft-r7.md` and a new `CHANGELOG.md` entry. `draft-r6.md` stays untouched.

**DIAL DECLARATION: none — revision pass; no new measurement; no laundering control applies.**

## Inputs (authority order)
1. `.claude/outputs/bprime-r6-referee.md` — the findings you close (MF/SF/NICE numbering below is its).
2. `.claude/outputs/bprime-r6-cite-check.md` — verified records, BibTeX for Appendix Q, and reconciliation items 1–3 + 5.
3. `claims_matrix.md` §0.9 (the N224 `s`/subtraction clause) and the §0.12/§0.13 blocks (riders unchanged).

## Close these (r6-referee numbering)
- **MF-1** — re-render Fig 1 to carry the Mamba-2 bar (uniform n = 9, from the banked artifacts App K names; the render pipeline is banked — ⛔ zero new measurements). Update App K render-status and delete the "pending re-render" disclaimer once true.
- **MF-2** — DISCHARGED by the cite-check (all anchor numbers verified exact). Your share: fold the six verified BibTeX entries into Appendix Q with their caveat notes verbatim; rewrite editorial item 10 as discharged (cite-check reconciliation item 5).
- **MF-3** — add the N224 rider at §4.6/L9(ii) (the `α‖q‖²` subtraction convention sentence; `s = 0.40` FLAGGED-FOR-A-CHECK not discharged-in-general; correction direction stated: `s` smaller, `d/s` larger — strengthens the claim) + the s-re-measurement DECLARED NOT-RUN into Appendix J.
- **MF-4** — NOT yours: a Hub/Head matrix-record item. Your only action: one CHANGELOG line noting the ruling is routed.
- **SF-1 … SF-10** — all ten, as specified in the referee report. SF-9 (fold the r6 evidence forward into abstract/§1.1 contributions/§7) is HEAD-APPROVED for this pass: the real-image replication at the 2,364× handicap enters the abstract's armour, the two venue firings + embargo methodology enter the contribution list with their riders, §7 closes on the strengthened base. Approved wordings stay character-faithful; riders stay adjacent.
- **Cite-check reconciliation items 1–3:** the Webb clause (use the report's recommended replacement verbatim — TV is ours, framework is theirs); the MOA-vs-river ARF clause in §R.2.1 ("river ARF ≠ MOA ARF; ours is river 0.25.0, theirs MOA; ensemble size 100 = MOA default, our inference"); relabel "a permitted 3,000" ("a 3,000-exemplar STM cap from the authors' reference-implementation default `ltm_size = 0.4`; the ICDM paper publishes k/L_min/L_max only").
- **NICE N-1 … N-7** — at your judgment; N-7 (the favorable N190 companion clause) is recommended-in.

## Rules
1. r-convention (`draft-r7.md`); no restructuring beyond the listed items; every changed passage listed in the CHANGELOG entry with its finding number.
2. Never-quote sweep on the touched regions before finishing (per-file, positive controls); approved wordings never edited, riders never separated from their numbers.
3. ⛔ Zero new measurements. The missing-experiment list is a separate Head decision — nothing from it enters this pass.
4. Anonymization discipline unchanged.

## Acceptance criteria
1. Every MF/SF item either closed (with location) or explicitly deferred with a reason in the CHANGELOG entry; no silent drops.
2. Appendix Q carries the six new entries with caveats; no aggregator-sourced field replaces a verified one (the never-copy traps: "Souza" not "de Souza" · "Pfahringer" · Webb DOI `-015-` uncorrected · JMLR record not PyPI's).
3. Fig 1 renders seven→six-rival bars correctly at uniform n = 9 with zero new measurements; App K provenance updated.
4. `draft-r6.md` byte-untouched; zero files outside the two named.
