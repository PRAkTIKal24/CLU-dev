# orgdiv-cat-test — the cat test: physics-trained organization vs the organizer swap (tier ii's spine)

**Campaign 2, wave C2W5. Agent:** experiment-engineer. **THE WAVE'S SPINE — first claim on worktrees
(worktree 1 of ≤3).** Branch `orgdiv-cat-test`, scoped worktree. Writes
`.claude/outputs/orgdiv-cat-test.md` + `.claude/outputs/orgdiv-cat-test/*`. Charter **ADDENDUM 4
§A19 task 1**, executing **`.claude/outputs/orgdiv-prereg/PREREG-TierII.md` VERBATIM — that document
is BINDING and is your specification.** Deviations from the prereg must be argued in your report,
never silent.

**Read first:** `.claude/AGENT_PROTOCOL.md`; **`PREREG-TierII.md` in full — twice**; charter
**§A13** (the three tiers; controls move UP a level) · **§A19** (your scope + the operating-point
constraint) · **§A14.1** (Route 3's training-time machinery is LIVE as your tooling) · **§A4.5**
(the factored store) · **§A17.1** (the pilot's three candidate fixes are your design freedom);
`.claude/advisor-head-intervention.md` §5 + §8; `.claude/outputs/bprime-c6.md` (**measured
`s = 0.40` and the re-located soft-certificate edge `B ≥ 0.542` — the prereg §6 rule-5 dependency
LANDED; use these numbers, declare them**); `.claude/outputs/cluformer-pilot.md` §5.3 (the organizer
tooling); the `[C2W4-CLOSE]` §10 entry. **Registries are CURRENT (v2.9)** — quote freely.

---

## ⭐ DIAL DECLARATION (protocol §7, C2 form) — echo before your first result
- **Dial / pillar:** **TIER ii — the organization dividend.** The question: *does physics-trained
  organization beat non-physics-trained organization, scored on compositional/unseen queries, with
  table-like inference reads permitted on both arms?*
- **Control:** ⭐ **the ORGANIZER SWAP** (`orgdiv-null-arms`' N1–N5 at identical φ/bytes/capacity/
  launch protocol/reader class; `null*` = max over all arms × their full registered tuning grid).
  ⛔ **The settle-deleted / matched-bytes launder is TIER i's control and is the WRONG control
  here** — you still RUN it and report it, labelled *"inherited diagnostic"*, never as tier-ii
  evidence.
- **Falsifies:** the prereg's F1–F5 verbatim (§3), each with its registered sign/threshold/seed
  count. F1 (no `OD_min` margin over `null*`) kills tier ii at this weight class. K1–K5 are
  pre-conditions that can kill the family before any arm comparison — **that is the point.**
- **Does NOT falsify:** a TIE (`|OD_min| ≤ 0.05`) is the registered most-likely outcome and is a
  FINDING, not a null (prereg §3.6: prior 0.55). Losing to a table on SEEN queries is Theorem
  O1/D2a, not news. Dividend ≈ 0 on the *inherited tier-i launder* is expected by design at tier ii
  (CM-27(b) — but its adjacency rule applies: the organizer-swap result must sit in the same
  paragraph).

---

## 0. The operating point — FIRST-CLASS CONSTRAINT (charter §A19, verbatim)
⛔ **The shipped operating point (`d/s ≈ 4.3`, `d_safe = 4.4 s`) is VOID for tier ii** — the
settled-point organization there is EXACTLY nearest-centroid VQ (`D = 0.0000`, Theorem O2). You run:
- **`d/s ∈ [2.5, 2.9]` on MEASURED `d/s`** — never ≥ 4.0 (provably zero dividend), never ≤ 2.01
  (merger; axial curvature `2α` exactly at the edge).
- ⭐ **First-day instrument, BLOCKING:** the **effective-`s` estimator** (fit `A e^{−r²/2s²}` to
  each written well's radial profile; prereg §7 bracket + OQ-1). `bprime-c6` measured `s = 0.40` on
  the shipped store — your learned multi-atom wells get their own measurement, and the operating
  point is set on it.
- **Multi-particle occupancy read, `P ≥ 4` designed launches — NEVER a settled point** (Theorem O1:
  composition cannot live in a settled point).
- **γ:** claim cells at **γ ∈ [0.05, 0.1]** (§A19). The registered axis `{0.02, 0.05, 0.2}` still
  runs as diagnostics — `γ = 0.2` is the *internal VQ-collapse control* (Prop O4's ×600 collapse),
  never a claim cell. ⚠ Every γ statement is **read-budget-scoped** (C2W4 standing: ρ_conv 0.83 at
  48 steps vs 2.6e-7 at 1200 — state the budget beside the band).
- **Depth heterogeneity ≥ 3×** between neighbouring wells (at 1.5 the non-VQ mass is 0.0013).
- **Design point:** `N_a = 32, F = 4, K = 128, a = 12` (ratio 5.00×, **reported, never claimed**;
  ⛔ `N_a = 16` is FORBIDDEN — the rule-4 split is empty at `K ≥ 128`). ⛔ **No byte-matched
  tier-ii promise** (prereg §5.2: that costs K ≈ 3.6k items and is a wave of its own).

## 1. What you build (prereg §2 + §10, in the registered order)
1. **Effective-`s` estimator + operating-point calibration** → **K1** write-admissibility sweep over
   `a ∈ {4, 12, 32}` (registered prediction: passes at `a ≥ 12`, fails at `a ≤ 4`; if K1 fails at
   every `a` with ratio ≤ 5, **F4 fires** and the vehicle is unaffordable — report and stop).
2. **The family:** `N_a` shared wells, `F`-subset items, payloads `v_j` drawn at write time,
   `y(x) = Σ_{j∈A(x)} v_j` (sum registered; mean = declared alternate needs re-verified tolerance).
   **K2** rule-4 assertions per query at construction (`|A∩B| ≤ F−2` ∀ stored B; `min_B ‖y(A)−y(B)‖
   ≥ tol`); a failing split is **rejected, not repaired**. Sample 512 of the 23 193 valid held-out
   combinations, 5 seeds, insertion order re-shuffled per seed.
3. **K3/K4/K5 kill-conditions on the physics arm alone, BEFORE any arm comparison:** K3 nearest-item
   table + strongest +0 B substitute ≤ 0.60 of range (else protocol-invalid); K4 the four leak
   controls (blank store · query-only reader · permuted payloads · address-leak probe) all ≤ chance
   + 0.05 (else family VOID — N68 says this is the most likely death); **K5 the per-item table
   launder — the read must beat a K-row nearest-stored-item table by > 0.10 on ≥ 1 reader, else
   tier ii is dead by the C2W3 flow-map mechanism.** Report the flow-map pair + Fisher ratio per
   arm (reference: 0.218 / +15 % / 6.3 → 1.47e7).
4. **The physics arm (`O_phys`):** factored store, masked/local write per well-group, admission +
   canonical placement, **soft certificate ON** (SC-1…SC-7 with `bprime-c6`'s re-located
   `B ≥ 0.542` — declare the number and its source); training through implicit gradients at settled
   points and/or trajectory reads (mandatory for anything upstream of `∇V`). ⭐ **The pilot's §5.3
   tooling is your design freedom, not a patch: localized atom init (`atom_local_radius`) ·
   trajectory write term · ψ payload residual** — each one deployed is a *designed mechanism*,
   declared in the byte ledger (§A19.1). Watch `pilot-placement-probe`'s early readout if it lands
   in time; do not block on it.
5. **The frozen interfaces** (these gate `orgdiv-null-arms`' spawn — publish them as a checkpoint
   artifact `.claude/outputs/orgdiv-cat-test/FROZEN-interfaces.md` the moment they are fixed):
   **(i)** the φ instance (frozen, bytes ledgered — identical on every arm, `PhiMismatchError`
   precedent); **(ii)** the reader class — nearest-well table · kNN · linear · small MLP, **sizes
   and fitting protocol frozen before the first arm runs**, fitted on SEEN only, reader params
   ledgered; **(iii)** the split (seeds, query sets); **(iv)** the launch protocol (`P` designed
   offsets = parameters, ledgered; occupancy vector = per-read transient).
6. **The comparison** (after `orgdiv-null-arms` delivers its arms + grids): `OD(R)` per reader,
   **`OD_min` primary**, 5 seeds, `null*` computed over the full grid — **quote the curve, not the
   endpoint** (F2 checks the margin isn't one reader's). Your tuning budget = the null arms' budget,
   **no more** (prereg §4.3; a hobbled *physics* arm is also an invalid experiment — F3-grade
   standard on yourself too).
7. **The deletion curve** (prereg §5.3) as the "and also" column — two series (exactness on the
   private fraction; measured degradation on the shared fraction), never a single scalar.

## 2. Design-rule compliance (prereg §6 — echo the table, satisfied-or-waived per row)
Includes: report `‖∂L/∂·‖` at init for every trainable group (reference scales 0.0 / 2.654e-9 /
6.421e-3) · assignment-logit liveness + `S_eff ∈ [S/2, S]` monitoring (a collapsed run is reported
as COLLAPSED, not null) · the 2α coercivity floor carried on any lifetime statement · if `allocate`
is used: pure function of `(query, shared policy params)`, allocation-shuffle test BLOCKING.

## 3. Riders (yours because you own the files)
- **`monitors.py`:** verify monitor #11's zero-depth guard lives in `monitors.py` itself, not only
  in the pilot's caller (commit `7bc166a` guarded a test helper path — check where the guard sits;
  move/duplicate it into the monitor with a test if it is caller-only).
- **Byte ledger:** corrected law `[A(D+2)+d]/(d+m)` everywhere (`harness-debt`'s fixed version).
- **Compute declaration BEFORE the first run** (prereg §8.7): 5 seeds × 5 arms × 4 readers × 3 γ =
  300 cells — declare budget + stagger in the task-file-echo section of your report.

## 4. Acceptance
- Steps 1–3 (§1) complete with every K-verdict stated — **these can kill the wave cheaply; a clean
  kill is a full acceptance.** If they pass: the frozen-interfaces checkpoint published, the physics
  arm scored, and (jointly with `orgdiv-null-arms`) the `OD_min` table, 5 seeds, per-reader curve,
  falsifier verdicts F1–F5 mechanical against the prereg's own bars.
- Multi-seed before ANY number leaves your report. Declared NOT-RUNs listed, never as nulls.
- ⛔ Claim-form discipline: prereg §2.6 verbatim — **no well is ever named semantically** ("the fur
  well" is forbidden in every artifact including captions and comments).
- Tests green on your branch; file ownership declared below; report → Hub, spawn nothing.

**File ownership (zero-conflict rule):** you own `chlu/**` files you create for the factored-store
family + `monitors.py` + your tests. ⛔ Do NOT touch `eval/race.py`, `eval/fb4_gate.py`,
`psi_readout.py` (AttentionPsi stays quarantined), or any file `orgdiv-null-arms` /
`pilot-placement-probe` declares. Declare your exact list in your report's first section.
**Git:** branch + scoped worktree; never push `origin`; `clu-dev` only (Hub pushes at integration).
