# pj-restore-v2 — paper-writer — BOUNDED RESTORATION of V2 `pj_sub.tex` (additive + enumerated corrections; the Head's prose is otherwise untouched)

**Commissioned by the Shorts Advisor (charter: `.claude/advisor-head-shorts-charter.md`, Addendum 59, 2026-08-22), on the Head's ruling this session: *"add back in the missing information in pj_sub without rewriting the rest… strictly follow the tone and academic brevity plus accuracy that currently exists in pj_sub.tex… add back the actual figures everywhere… don't worry about page limits."*** Read `.claude/AGENT_PROTOCOL.md`, then this file, then the four inputs below. Output report: `.claude/outputs/pj-restore-v2.md`.

**⭐ THE EDIT-BAR ON `pj_sub.tex` IS LIFTED FOR THIS PASS ONLY, in this scope only** (Head ruling 2026-08-22; Add.53's bar otherwise stands). You edit exactly ONE content file: `.claude/NIPSsubmission/v2-neurreps/pj_sub.tex` (+ rebuild artifacts `pj_sub.pdf/.aux/.log/.out`). ⛔ Everything else in the folder — `submission.tex`, `submission.pdf`, `BUILD-NOTE.md`, `figs/*`, the style file — is byte-untouched, and you write nothing outside this folder except your build note and report.

**DIAL DECLARATION: none — editorial restoration; ⛔ zero new measurements, zero new numbers. Every number you add must be copied from `submission.tex` (or the named output file) with its ancestor line cited in the build note.**

## Inputs (read in this order)
1. This task.
2. `.claude/outputs/pj-fidelity-v2-r2.md` — the authoritative loss/drift inventory (§A.2 S-1…S-7 · §A.3 · §B.4 R2-1…R2-12 · §B.5 P-1…P-9).
3. `.claude/outputs/pj-referee-v2-r2.md` — MF-1…5, SF-1…9, N-1…7.
4. `.claude/NIPSsubmission/v2-neurreps/submission.tex` — the restoration source. `pj_sub.tex` — the object.

## The three rules of this pass
1. **ADDITIVE by default.** The Head's prose is not rewritten, reordered, or compressed. Missing sentences/clauses/rows are inserted at the sites the fidelity report names. The ONLY non-additive edits permitted are the enumerated corrections in Part B — nothing else changes.
2. **Verbatim where verbatim is owed.** Matrix-approved wordings, mandatory riders, scope qualifiers and never-quote-adjacent forms are restored **VERBATIM from `submission.tex`** (Add.30 boundary — style never paraphrases them). Connective prose you write yourself matches `pj_sub`'s register: academic brevity, plain terms, ⛔ **no intensifiers** ("strictly/precisely/completely/remarkable" — do not imitate that layer).
3. **Nothing invented.** If an item on the worklist has no ancestor in `submission.tex` or a named output file, you STOP on that item and report it — never draft a substitute.

## Part A — ADDITIVE restorations (consolidated worklist; per-item detail in the two reports)
**A1. The two mandatory sentences (highest priority):** the §A20.5 substrate-scope sentence (base 145(iii), verbatim, into the Discussion) — P-1; the score sentence in its **measured** form (base 145(ii): *"No external benchmark is won on its own headline metric anywhere in this paper"*, plus *"the honest gap is part of the claim…"* and the task-RMSE-axis clause, base 136/145) — P-2/P-5(a)/P-7.
**A2. The claim-site riders:** the sufficiency fence (base 86: *"The theorem states sufficiency; equivariance is not necessary…"* + base 103(b)'s measured non-equivariant battery clause) — pairs with correction B(a); the CM-17 sampler fence (base 361, all three elements: sampler-not-thermodynamics · never "has no equilibrium" · `newtonian_learned` scope) — R2-2; the single-checkpoint caption disclosure (base 267, verbatim, into caption l.304) — R2-3; the `legacy`-default warning + both flag names (base 105) at l.83 — P-6; the N46 counterexample half at the negative's site (base 132 + Vafidis cite) — P-3; the anchor non-novelty clause at l.130 (base 140: *"That a corrective term can keep a flat direction alive is not new"*) — P-4; the finite-horizon robustness sentence + its App-F numbers (base 122, 376) — R2-12.
**A3. Appendix repairs:** the 6 missing negatives rows + the within-row dropped numbers (base App E; fidelity P-5(a)–(g) lists them all); Appendix G's fences (base 419 F.6 scope · the δ=0.3 no-NLO clause · the "do not quote 2.2×10⁻¹⁶" precision fine print · the demotion label; resolve the orphan `x` symbol per base) — R2-7; the width-match confound + timing methodology (base 273, 299) — R2-8; App A gets its three sentences of prose (referee SF-5, pointing at §4.4 + the figure).
**A4. Precision restorations on correct numbers (S-items):** "on 5/5 seeds" at l.80 (S-2) · "at, or just below" + the 5-seed count + ≈8× V-depth (S-3) · the grid/pair counts at l.385 (S-6) · "except the γ_φ rung" + the not-a-clean-single-knob footnote at l.268 (S-7/R2-4).
**A5. Referee wiring items:** fit-spec clauses at BOTH slope sites (−0.956 = per-point fit over all overdamped rows; −0.961 = seed-mean OLS over the 7 overdamped δ — source `figure-render-pass.md:207`; ⛔ do not pick one, label both) — MF-1; "0.9987 overdamped-only; 0.973 pooled — the drop is the regime structure" — MF-4; the 4.5-vs-4.6-decade grid clauses — SF-1; the 5-seed/3-seed experiment attribution in §4.4 — SF-2; the sleep-provenance flags (f5/s500/CD, `anchor-robustness.md:146`) — SF-3; restore "≈35×" on the wake-MSE cost — SF-8; the floor-ripple clause (±8 steps at δ=4) — N-2; unify "70" phrasing — N-6; the coRNN honest-weak footnote — N-3.
**A6. Citations:** re-cite Huang et al. 2025 at l.141 and Hairer et al. at the h<2 site; restore the base's one-sentence uses for the four orphan entries (EDEN occupancy · UnICORNN gradient-bound · Ramsauer coincidence · Jelassi fixed-state limits, which carries the score-adjacent clause) so no entry is decorative; restore the claim-bearing status annotation `(preprint; single author)` on arXiv:2605.03338. (DOIs/other annotations: NOT restored this pass — note in the build note as a one-word Head option.)
**A7. Figures: verification only.** All 5 available PNGs are already used (Advisor-verified). Confirm 5/5 in the build note; ⛔ no figure work beyond that.

## Part B — ENUMERATED corrections (the ONLY non-additive edits; each printed before→after in the build note)
(a) l.28: *"exact equivariance is required to protect"* → *"exact equivariance protects"* (R2-1). (b) l.60: *"guarantees $\dim(G/\mathcal H)$"* → *"guarantees at least $\dim(G/\mathcal H)$"* (S-5). (c) l.44: *"This chapter"* → *"This paper"* (MF-2). (d) l.28: restore *"most"* before *"infinitesimal perturbations"* (S-4). (e) the Mo-boundary wording (MF-5, referee's exact forms): abstract → *"no closed-form constitutive law linking the transverse curvature of a trained potential to lifetime has been measured"*; §2 → *"these estimator-based (kinematic) lifetime predictions and existence proofs"*. (f) l.264: restore the base's form of the contraction-forbidden sentence (measured, not "proven mathematically") + *"not the causal velocity cap, inactive here"* (R2-5/#22). (g) l.381: un-fuse §G.1 — restore the base's two separate sentences (onset-grid "down to" + the f=2,4 quality-factor condition + the dynamically-silent clause) (S-1/R2-6). (h) l.47/l.104: *"recently published"* → *"recently posted"* (P-8). (i) the unverified authorless poster (MF-3): soften to uncited prose per the referee's wording (*"concurrent workshop work has explored soft symmetry regularization for continuous attractors"*) and drop its References entry. (j) l.124: *"wall-time efficiency claim"* → *"compute claim"* (CM-4 approved form) and add CM-21's approved wording at l.64 (*"controllability, which we evidence, not capability, which we do not claim against transformers or state-space models"*) (P-9).

## After editing (all mandatory)
1. Rebuild: `/Library/TeX/texbin/pdflatex -interaction=nonstopmode pj_sub.tex` ×2, inside the folder; 0 errors, 0 undefined references; report the new page count and split.
2. **Two-way numeric check** (every numeric token in the edited file has a base/output ancestor; every token you added is on the edit map) — printed in the build note.
3. **Per-file sweeps, positive-controlled** (the standing Grep hazard): the fidelity report's §0.2 never-quote pattern · the author-token rule (`\bMo\b` = bibliography-only; Morse survives) · `pseudo-gap` = 0 · semantic-hermeticity (companion/program/sibling language) = 0.
4. **`BUILD-NOTE-R3.md` in the folder = deliverable #1**: every edit mapped to its worklist item and its base/output ancestor line; the Part-B before→after table; the md5 of the final `pj_sub.tex`; any item you could NOT execute, stated honestly with why.
5. Report → `.claude/outputs/pj-restore-v2.md`: summary, deviations, the not-executed list, page split.

## Acceptance criteria
- Every Part-A item present or honestly reported blocked; every Part-B correction applied exactly as enumerated; ⛔ zero edits outside the enumerated set + additive insertions.
- Verbatim riders diff-identical to their base forms; zero new numbers without ancestors; sweeps clean; build clean.
- `submission.tex` and all other folder files byte-untouched; no writes outside the folder except the report.
