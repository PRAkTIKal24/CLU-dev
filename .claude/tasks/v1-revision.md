# Task: v1-revision — fold the settled CM-8 grid + the Thread-9 design rules into V1 (w9)

- **Agent:** `paper-writer` · **Output:** report to `.claude/outputs/v1-revision.md`; edit `.claude/papers/v1-short/` in place (CHANGELOG v0.2).
- **Read first:** protocol · claims matrix **v1.4** (CM-8 SETTLED — use the approved wording verbatim incl. the noise wall; CM-12 flag now ADOPTED; **CM-14 new** — the Thread-9 design rules) · `.claude/outputs/regime-remap-2000ep.md` §"w8 completion" (grid numbers + Item-4 negatives + the cost caveat to attach) · `.claude/outputs/thread9-mh-kernel.md` (the V1-grade position paragraph §"V1-grade position paragraph" + Prop-MH2 latch-diffusion).

## Items
1. **§4.3 + App C.4 — replace the `[pending: regime-remap-complete]` slot with CM-8 SETTLED verbatim.** The full story: under-training artifact confirmed (n=8); clean/correlated-cue reversal at kv≤64 (Δ+0.02; ρ=0.9 widening = Hopfield fragility, say so); epoch-budget wall not capacity (kv96@4000, kv128 tie); kv32 over-trains; **THE NOISE WALL as a first-class negative** (0/6 close under cue noise; gate 0.36 vs Hop 0.71 at σ=0.6 despite fidelity 1.0) — this is on-thesis for a position paper: *the gate's rationing works on clean retrieval; noise robustness is Hopfield's* — and feeds the honest regime-map figure (`fig_regime_map.png`, `fig_frontier_clean.png` assets exist). Attach the intra-CLU cost caveat to every accuracy sentence (Item-4 wording).
2. **§5 — fill the `[optional slot: thread9-mh-kernel]` with the theorist's position paragraph + the CM-14 design rules**, physics-heavy per the ML4PS framing: squeeze-MH is a certified HMC-family kernel under stated conditions; the governed cascade is Metropolis-within-annealing (MAP-seeking — a *feature* for retrieval, honestly not-Gibbs); **certified retry erodes the latch unless proposals are projected off the coset** (D=½s², verified) — "even the certified retry carries a receipt: coset diffusion." This is the paper's closing design-rule flourish. Experiments = future work (w9 spec exists; do not claim runs).
3. **Editorial answers (Hub, binding):** Q1 — position-paper posture CONFIRMED (verification-grade lead is correct for this venue-shape; keep the banner labels). Q2 — keep the Hopfield 0.18→0.88 via the matrix citation (CM-2 is the approved wording; add minus-the-physics to the App-A provenance list). Q3 — EBT prominence CONFIRMED (prominent, flagged as preprint). Q5 — anonymized build at the pruning/submission pass, not now.
4. CM-12 footnote: matrix flag is now ADOPTED — remove any "pending Head confirm" hedge if present. Rebuild PDF.

**Acceptance:** both slots filled from v1.4 approved wordings; noise wall present as a first-class negative (a referee finding it missing would be fatal); PDF builds. → `v1-referee` (w10).
