# bprime-draft-r4 — fold the post-r3 measurements (Mamba-2 · the CLU n=9 column · the closure cracks)

**Campaign 2, wave C2W5. Agent:** paper-writer. **NO WORKTREE.** Produces
**`.claude/papers/bprime/draft-r4.md`** + CHANGELOG line. ⛔ r-convention (A18.5). **Mechanical
fold** — r3 closed the referee pass; this revision folds three artifacts that landed AFTER r3 froze
(16:12): the CLU n=9 column (16:11 report, published after), the Mamba-2 row (17:34), and the
closure reconciliations. **Spawn alongside `doc-curator-c2w5-fold` — quote registries freely; the
curator's fold is running on the same evidence.**

**Read first:** `draft-r3.md` + `CHANGELOG.md`; `.claude/outputs/bprime-referee-closures.md` (R1–R5
+ §1's what-changes list); `.claude/outputs/bprime-mamba2-arm.md` (§1, reconciliations 1–3, the
frontier row); `.claude/outputs/orgdiv-null-arms.md` §5 (the F5 imitability caveat — touches §4.6's
neighbourhood if quoted); the `[C2W5]` second-review §10 entry (the Hub rulings below are recorded
there).

## The fold, in order
1. **The CLU row at n=9 (closures §1):** full −0.4370 ± 0.0417 · +0 B/raw margin −0.2897 ± 0.0328
   (8.8 SE) · dividend −0.0561 ± 0.0315 (**1.78 SE — a sign statement; soften every "launder beats
   the store" to "reads no worse than"**) · lift −0.0465 ± 0.0406 ⇒ ⛔ **NOT RESCUED, now BY
   MEASUREMENT at the same n as every rival** — replace r3's n=3 sign-fact framing; the below-blank
   language becomes "statistically indistinguishable from an empty store" (the n=9 point is the
   wrong side of zero but < 2 SE). §4.2's CLU sentence re-quotes at n=9. The raw-vs-+0 B
   float-identity on 9/9 seeds retires the "(its own table is already raw)" parenthetical into a
   measured statement.
2. **The Mamba-2 row (mamba2 §1):** headline **0-of-5 → 0-of-6**; range −0.2563 … −0.4602 (min SE
   multiple unchanged at 4.43); the row into §4.1.1 + App O gains the SSD arm spec; App H gains its
   frontier row (NOT RESCUED, 3/3 selections, labelled column); §5.2's "SSMs named, none measured"
   limitation RETIRES; the survey sentence updates. Both honest qualifiers carried verbatim (best
   `full` = a tie with gdn2, not a win; +0 B margin = a tie with zero).
3. ⚖ **Hub ruling (selection stability — extends A17.4 to selection rules):** rescue verdicts are
   quoted only where STABLE across the registered selection rules. Stable: **gdn, mamba2 RESCUED ·
   ttt_mlp NOT · ttt_linear INIT-UNSTABLE (unchanged)**. **deltanet and gdn2 become
   SELECTION-DEPENDENT** (rescued under `f3`/`f3_lite`, below the bar under held-out `f3_val`) —
   r3's "under both code paths" sentence gains the third path and the label. No margin quoted
   against a selection-dependent arm in the flattering direction.
4. **Closure cracks:** R2 — the CLU byte ledger is **modal (8 of 9)**: 5456 B/100 B/54.56×, seed 8
   = 5472/120/45.60 (6 items admitted); label at every site exactly as the TTT R4 pattern. R3 — fix
   §4.6's audited-configuration cell to **`d/s_fit = 3.59`** (the one mid-table ruler switch;
   3.72 = sep/s_fit) — ruler named per SF-6's pattern; Figure 3 already plots 3.59. R4 — the
   `±0.40` on the 0.814 row → **± 0.31** (2·grad_se) or drop the interval from the inadmissible
   row. R5 — App K Fig-1 caption: DELETE the mixed-n language (the render is uniform n=9, six
   bars, CLU hatched NOT-RESCUED).
5. **Hub rulings on r3's own reconciliations:** (rec 3) the N186 causal framing STAYS, softened to
   the n=9 form ("a candidate mechanism for why the written store reads no better than blank");
   (rec 4) the 2605.17590 cut is RATIFIED (unverifiable = out); (rec 5) Poliak stays pending
   `bprime-cite-check-2` — mark with a ⟦CITE2⟧ marker, do not block.
6. **Figures:** captions updated where r4 changes them (Fig 1: six bars, uniform n=9, NOT-RESCUED
   hatch; Fig 5 caption at n=9); if the mamba2 bar is missing from Fig 1, add the render request to
   your report (the closures engineer's script is in `.claude/outputs/bprime-referee-closures/`).
7. **Sweep, last:** the A18.1 sweep re-run (now including: no CLU n=3 rescue verdict · no
   "0 of 5" · no unlabelled single TTT/CLU byte figure · no selection-dependent rescue quoted
   unlabelled) — grep results in your report.

## Acceptance
`draft-r4.md` complete; CHANGELOG line with per-item disposition; any claim whose magnitude or
verdict changed at n=9 flagged (the F3 pre-commitment pattern); sweep results printed. Report →
Hub, spawn nothing.
