# doc-curator-c2w6-fold-2 — the φ-qualifier last mile + the adjudication fold

**Campaign 2, wave C2W6 close-out, pass 2. Agent:** doc-curator. **No worktree, no branch.**
Writes `.claude/outputs/doc-curator-c2w6-fold-2.md` + the registry edits.

**Why there is a pass 2, stated plainly so it is not read as rework:** your pass-1 `BLOCKED-PENDING`
on item 5 was **correct at the moment you checked** — you Glob'd for
`.claude/outputs/c2w6-phi-leak-docstring.md`, it was genuinely absent, and you did the right thing
by refusing to guess the grep list. It landed **while you were still writing pass 1**. The
precondition is now satisfied. Separately, the analyst's adjudication landed after your fold and
**supersedes part of what you filed** — that is the second half of this task.

⛔ **Mechanical preconditions (verify BOTH before starting; if either is absent, stop and report —
do not guess):** `.claude/outputs/c2w6-phi-leak-docstring.md` **and**
`.claude/outputs/c2w6-erosion-adjudication.md` must exist.

**Read first:** `.claude/outputs/c2w6-phi-leak-docstring.md` (its **grep hit-list table** is the
worklist you were missing) · `.claude/outputs/c2w6-erosion-adjudication.md` **in full** (the
superseding source) · `.claude/outputs/c2w6-anti-erosion.md`'s **two** dated Hub erratum banners ·
your own `doc-curator-c2w6-fold.md`.

## 1. ⭐ ITEM 5 — the φ-qualifier last mile (§A23.3's curator half, now unblocked)
The engineer corrected **6 in-code sites** and its report's disposition table lists **every hit it
found, including the ones it deliberately left** and why. Take its three named highest-value
doc-side targets and sweep the prose/registries/ledger/shorts for them:
- anything quoting **design rule 1** ("the block trains end-to-end only through the trajectory read");
- anything quoting the **`0.0 / 2.654e-9 / 6.421e-3`** reference triple as an *end-to-end* statement;
- anything calling `settled_point` the **"zero-gradient control"**.
Attach the binding qualifier you already filed (N228 / matrix §0.10 / CM-32(ffff)).
⚠ **Preserve the engineer's scope-out:** the **T3 settled-point bitwise zeros (N135/N152/N201) are
NOT affected** — those were measured at `atom_place_radius = 0`. ⛔ Do not widen the qualifier onto
them; you already scoped this correctly in N228, keep it.
⚠ Also carry forward the engineer's note that **`implicit_grad.py`'s wording was judged correct at
its own layer** and deliberately left — flag it in the registry as a site a reader *could* over-read,
not as an error.

⭐ **THE STANDING CONSEQUENCE to fold (Add.9 §A27.3, Advisor-registered — file it as doctrine, not
as a C2W6 detail):**
- **`settled_point` is a zero-gradient control ONLY at `atom_place_radius = 0`.**
- **Every banked `gradient_probe` traj/point ratio measured at the run-1/2/3 config carries a
  non-zero denominator floor ⇒ the published ratio UNDERSTATES the read's share — a conservative
  bias, NEVER an inflation.** Say the direction explicitly wherever you file it: no claim built on
  those ratios is inflated, so nothing is retracted; the ratios are lower bounds.
- ⛔⛔ **Boundary, do not cross it:** the **T3 settled-point bitwise zeros (N135/N152/N201) were
  measured at `atom_place_radius = 0` and are NOT affected.** The qualifier must not widen onto
  them. You already scoped this correctly in N228 — keep it, and state the boundary in the doctrine
  entry itself so a future reader cannot over-apply it.

## 2. ⛔⛔ THE ADJUDICATION FOLD — what supersedes your pass-1 entries
Your pass 1 folded from the **`BLOCKED`** version of the analyst report, so these are new. Each is
Advisor-grade (re-derived from raw, zero transcription error found) — ⛔ **transcribe, do not
re-derive, and never re-score a registered verdict.**
1. ⭐⭐ **N223's addendum and every "premise retired" framing you filed must be SUPERSEDED IN PLACE**
   (dated, body untouched). The replacement finding: a **large transient trough** — depth falls to
   **0.9 % (step 150) / 5.3 % (step 275)** of untrained on 2/3 seeds (112× / 19× losses vs 1.05–1.33×
   reading noise), then partially recovers (final/min geo **4.20×**); **the banked N223 anchor was
   taken at 200 steps, inside the trough**, so E1's `final/step-200` was structurally measuring
   recovery, not decay. ⇒ **"N223's mechanism fires at the run-2 config but is NOT TERMINAL"** —
   ⛔ never *"the premise is retired"*, never *"there is no erosion"*. **This is an upgrade, not a
   retraction**, and it restores N223 as a live mechanism. The E1 verdict (0/3) and the gate are
   **unchanged**, raw and decay-netted.
2. ⛔ **N229 / any I2 row: ρ(LOO) = +0.0667 is UNDEFINED, not a null** (ICC(1,1) negative 3/3:
   −0.205/−0.218/−0.248 ⇒ ceiling 0.000). Strike it as a measured correlation wherever you folded
   it. **Hub-verified target list** (the three registries your pass-1 fold reached):
   `negative_results.md` · `claims_matrix.md` · `future_work.md`. ⚠ **`0.0667` also appears in
   `doctrine-repairs.md`, `carried-remeasurements.md` and `learned-confine-causal.md` — those
   predate C2W6 and are almost certainly a DIFFERENT quantity: verify each hit is the C2W6 ρ(LOO)
   before touching it, and leave anything that is not.** Add: the two registered usefulness proxies are **anti-correlated** (−0.505 ± 0.125, 4.04 SE),
   and **8/18 wells are net-harmful**.
3. ⭐ **N229 gets a genuine strengthening:** `NO_USAGE_STRUCTURE` is **not merely underpowered** —
   the rig **excludes the Head's registered effect size**: P(mean ρ ≤ −0.2571 | true ρ = +0.5) ⇒
   **ρ_true ≥ +0.5 excluded at p ≈ 0.022** at the measured combined reliability 0.41 (and it survives
   down to reliability 0.2). What it **cannot** do is separate ρ_true = 0 from |ρ_true| ≤ 0.4. File
   both halves. ⛔ The depth-as-importance caveat still **STAYS ACTIVE** (unchanged).
4. ⚠ **New instrument caveat (A1):** the erosion curve is **raw, not netted of designed decay**, and
   the exponent drifts (`last_write_chunk` 0→12 within a slot). The law itself is exact (0.039211
   predicted vs measured over 717 readings). Netting moves E1 seed 0 **9.78 → 6.47 (−34 %)** and
   makes the arm look **more** eroded. ⛔ No verdict moves. File as a **C2W8/C2W10 build requirement**.
5. ⚠ **A3:** the engineer's §6 "pooled (n wells)" ρ column is **non-registered and flips sign**
   (+0.2198 vs registered −0.1619 ± 0.2806) — if you folded that column or its "weakly predictive"
   sentence anywhere, relabel it.
6. ✅ **Record the clean result:** the **P3 re-price trigger did NOT fire** (within-wave paired leg;
   ON below OFF on 1/3 seeds; geo 1.421× w4 / **3.628× 3/3 w40**) — the partition did **not** starve
   the write. Also: the analyst's **own** pre-registered prediction was **partially refuted** (it
   registered no-structure on 2/3 seeds; measured 1/3) and it reported that as a finding — worth a
   process line, it is the second n-blind estimator error caught in two waves.

## 3. Two housekeeping rulings from the Hub (you asked)
- ✅ **RATIFIED:** your dated curator erratum banner on `.claude/outputs/pilot-placement-probe.md` is
  **correct and stays** — ruling §A23.4 names that file explicitly, so a spoke-report banner is in
  scope and the C-3 precedent is the right instrument.
- ✅ **Your R1-wording flag is accepted and already handled** by the Hub in erratum banner 2 (E1's
  rule is ≤0.3× with ≤0.5× as the band). No curator action needed beyond not propagating R1's line.

## Constraints
⛔ Transcribe only — no re-derivation, no re-running, no re-scoring of a registered verdict ·
⛔ every superseded entry is **annotated in place, dated, body untouched** (never deleted, never
silently rewritten) · ⛔ do not touch any `PREREG-*.md` body · ⛔ do not edit `.claude/papers/**` ·
registries stay CURATOR-PROPOSED pending Hub confirmation · ⛔ never push anything.
