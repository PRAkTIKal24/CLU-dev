# doc-curator-c2w8-pass3-close-fold — the pass-3 fold, the two Advisor errata, and the wave's close

**Campaign 2, C2W8 CLOSE-OUT. Agent:** doc-curator. **NO worktree.** **Spawns NOW** — pass 3 is
closed, merged and green at `main @ 9e0bb25` (1555/0). ⛔ **The gate-hardening spoke's results are OUT
OF SCOPE** (it has not run); its fold is a later pass.
Writes `.claude/outputs/doc-curator-c2w8-pass3-close-fold.md`.

**Binding documents:** charter **ADDENDA 11 (§A31–§A32) and 12 (§A33–§A34)** — the errata you
propagate are **§A31.1–2** and the rule that re-reads pass 3 is **§A33.1** ·
`.claude/outputs/c2w8p3-{gate-addr,phi-geometry,capture-strong-phi}.md` + their JSONs · the
`[C2W8-PASS3]`, `[C2W8-PASS3-SPINE]` and `[C2W8-CLOSE]` §10 entries · `AGENT_PROTOCOL.md` §5.

## ⛔⛔ READ FIRST — the sweep hazard, and the Hub has now tripped it once itself
A **directory-scoped `Grep` over `.claude/` silently returns nothing** (`.claude/**` is gitignored),
while the same pattern against a **named file** returns hits. ⛔ **Any "swept and found nothing" claim
is VOID unless per-file, or via a tool that ignores gitignore (`grep -rn` via Bash does not skip
ignored files).** ⛔ **Positive-control every negative sweep and report the control's hit count beside
every "no hits" claim.**

---

## 1 — ⛔⛔ THE TWO ADVISOR ERRATA (RATIFIED; propagate with their never-quotes)

**ERRATUM 1 (§A31.1) — the "digit-identical unassigned reads" evidence was LAUNCH-POINT VACUOUS.**
`covered` is computed on **q₀** against the codebook (`clu_system._read_diagnostics`) ⇒ **the same φ +
the same codebook gives the same number whatever the store does.** The banked `n_never_read` inherits
it. ⛔ **NEVER-QUOTE: the telemetry-based sentence "reads landing in no basin, 58/62/62, digit-identical
to pass 1" and every quotation of it — the statistic is store-invariant by construction.**
✅ **The replacement, measured by G-ADDR, is the wave's sentence:**
> **BOTH arms address — arm A correct-basin A1 = 0.50 / 0.42 / 0.52 (threshold 0.25), arm B
> 0.93 / 0.98 / 0.88 — and both still fail the A3 launder-margin leg (launder 0.79–0.90 vs store
> ≈0.5). THE STORE CAN ADDRESS AND STILL LOSES TO A ZERO-PARAMETER READ OF ITS OWN KEYS.**
⛔ **Pass 2's "arm B never addresses" is RETIRED together with the telemetry that produced it.**
⚠ **Also retire the Hub's own registered prior** — Q2 predicted A1 = 0.03–0.12; measured 0.42–0.52.
**Falsified upward, and it should be recorded as falsified, not quietly dropped.**

**ERRATUM 2 (§A31.2) — the "σ_q ≈ spacing" mechanism is RETRACTED as wrong-population.**
`median_nn_task1` is the NN spacing of a **~200-key sizing set**; the store holds **16 items**.
Against the true store population σ_q/spacing = **0.32** (MNIST) and **0.19–0.37** (CIFAR) — queries
are **~3× CLOSER** to their own key than to a neighbour. ⛔ **NEVER-QUOTE: "σ_q ≈ spacing ⇒ the items
are not separable by any read", and every quotation of it.**
✅ **What stands: the failure is REAL and reproduces; its CAUSE is OPEN** — registered as the open
mechanism question (§A31.2), consistent with the settle-destroys-launch-information family (N213,
C2W7's scattering), **theorist-priced, not assumed.**
⭐ **Bank the mechanical consequence:** `d_safe` derives from that same 200-key spacing applied to a
16-item store ⇒ **monitor #3's 0.000 refusal rate was ARITHMETIC, not a finding.**

## 2 — THE PASS-3 FOLD (N-registry + claims matrix)

- ⭐⭐ **The no-daylight row:** branch (b), **0/9 cells, 3/3 arms**, computed by the registered rule,
  priors honoured (Hub Q6 = 0.70), **nothing tuned**. **|A3b| ≤ 0.047 on 7/9 cells — the store and an
  832-byte table are statistically indistinguishable on held-out stream reads.** ⭐ **Attributable,
  not confounded:** store NOT inert at d=12 (9/9), `geometry_go = true` verified in advance,
  `gate_addr_validated = true`. ⇒ **FLAG IT TO THE B′/SHORTS TRACK as tier-i protocol evidence at a
  THIRD substrate** (gym → LM block → CL census). ⛔ **You MARK it only — the B′ revision itself is the
  writer track's and is Head-sequenced. Do not draft it.**
- ⭐⭐ **The inversion (§A31.4):** the task-strong encoder is the **address-WORST** arm beyond 2 SE
  (`simclr − randconv` A1 = **−0.1406 ± 0.0508**, 0/3; `simclr − pca` **−0.1276 ± 0.0589**, 0/3),
  while **unfitted `randconv` buys the geometry for free** (clears the GO rule at simclr's margin,
  **0 fit steps**). ⇒ **Task accuracy and address geometry are ANTI-CORRELATED on this substrate** —
  vindicating §A4.3's separate address-block design. ⛔ **"strong φ" must NEVER again be used as one
  undifferentiated notion.**
- ⭐⭐ **The D2a signature at design level (§A31.5):** ρ(A1, G-DRIFT) = **−0.967**, ρ(A1,
  settle↔launder agreement) = **+0.933** across 9 cells; the per-arm boolean hid it (0.004 ties).
  **The gate rewards table-behaviour; it is barred from further quotation until hardening lands
  (§A32.3).**
- **Reach-not-capacity, now on a censused cell:** store digs at d=8/12, **INERT at d=16 with 131 072
  atoms, budget honoured exactly** ⇒ d=16 a **declared NOT-RUN**, operational **d=12**.
- **R3 discharged:** `attractor_can_move_off_the_key = true` (follow-fraction ≈1.008 at δ=0.30) ⇒
  **the `atom_site_local_init` compliance ruling STANDS — capture was an outcome, not pinning.**

## 3 — ⛔⛔ THE QUALIFIER PROPAGATION (the Hub ran the sweep; this is your verified worklist)

**Hub sweep, per-file, positive-controlled** (`DIAL DECLARATION` = 1 in each probe file, so the sweep
is live). Files using "strong φ" ≥3× **with the inversion qualifier ABSENT**:

| file | "strong φ" hits | inversion qualifier |
|---|---|---|
| ⛔⛔ **`c2w8-cifar-strong-phi.md`** | **27** | **0** ← *the artifact that banked the claim pass 3 inverted* |
| `c2w8p3-phi-geometry.md` | 13 | 3 (partial) |
| `cl-encoder.md` | 4 | **0** |
| `c2w8p2-instruments-and-debt.md` | 4 | **0** |

⇒ **Add the inversion qualifier to each** (dated banner, body untouched — the C-3 precedent).
⚠ **`c2w8-cifar-strong-phi.md` is the priority**: it banks *"Split-CIFAR re-priced at strong φ reads
0.319 vs 0.161"* with **no indication that this gain does not transport to addressability and in fact
inverts.** Its **mandatory provenance sentence still stands and still travels** — you are **adding** a
second qualifier, not replacing the first.
⚠ **Also confirm the `M` vacuity qualifier travels** — pass-1/2 `M` (0.2333/0.2417/0.2417) is never
quotable as a merge population without it, and any census quoting `M` states **which criterion**
produced it (K9 is registered with refusal negatives; **the merge VERB stays unbuilt**).

## 4 — ⛔ THE K6 CROSS-REFERENCE SLIP GETS ITS OWNER (4th–5th occurrence; stop banking it)
Add.9 **§A27.2** cites *"K6, §A28.3"*; **§A28.3 is the well-lifecycle ruling**, K6's home is
**§A26.3**'s program-wide reader-class rule. It has been banked at consecutive Hub sessions without
an owner. ⇒ **File it with a NAMED owner and a disposition** (fix the pointer in the registry, or
record it as an Advisor-only charter edit with the diagnosis attached so the next reader does not
re-derive it). **Say which you did.**

## 5 — ⚠ THE MECHANICS/VALUE RE-READ (§A33.1) — apply it to how pass 3 is FILED
> Every gate leg is **MECHANICS** or **VALUE**. Mechanics legs are pass/fail at component level.
> **VALUE legs exist only at tier level with the tier's own control.** ⛔ **A launder margin on a
> component gate is a DIAGNOSTIC, never a pass condition — pass 3's A3 leg is the last of its kind
> and its "failure" is re-read accordingly.**
⇒ **File the wave verdict in the ratified form: MECHANICS all green; VALUE untested by design at
component level.** ⛔ **Do not file "the completed gate failed on all three arms" as the wave's
result** — that reading is superseded: it was driven by A3, which is now a diagnostic.
⚠ **Keep both readings visible with their dates** (struck and annotated, never deleted) so the
supersession chain is legible.

## ⛔ Out of scope (declare as such, never as gaps)
The gate-hardening spoke's results (not run) · any `PREREG-*` file (**never edited**; errata are dated
blocks) · all code · the B′ draft itself (**you MARK the row; the writer track drafts**) · the arm A
vs arm B race (⛔ **UNADJUDICATED — do not record a winner**) · any tier-ii/full-CLU/I2 verdict.

## Acceptance
1. Both errata propagated **with their never-quotes**, and the replacement sentences installed.
2. The pass-3 fold landed; the **no-daylight row FLAGGED** to the B′/shorts track (marked, not drafted).
3. The four files in §3's table carry the inversion qualifier; the `M` qualifier confirmed travelling.
4. **K6 has a named owner and a disposition.**
5. The wave verdict filed in the **§A33.1 ratified form**, with the superseded reading struck and dated.
6. Every negative sweep reported **with its positive control's hit count**, per-file.
7. Your report's **first 10 lines** name anything you could not close and why.
