# Task: f5-corrigendum-2 — Prop-9 is Newtonian-only (w14, theorist, BLOCKING the F5 arXiv push)

- **Agent:** `physics-theorist` · **Output:** `.claude/outputs/f5-corrigendum-2.md`
- **Read first:** protocol · **`.claude/outputs/v2-symmetry-deepdive.md` §7bis R8 + §10 X6** (the defect, with the free-particle proof and the `T/(m₀c²)` scaling table) · your own `.claude/outputs/f5-corrigendum.md` (the first corrigendum — same three-site discipline) · `chlu/core/integrators.py` (`langevin_step`, the O-step) · `chlu/core/chlu_unit.py` (`effective_inertia`).
- **Git:** theory docs only. **No tracked code.** Edit `.claude/outputs/formalism-note.md`, `.claude/outputs/f5-arxiv-note.md`, and `.claude/papers/f5-note/f5-note.tex`; rebuild the PDF.
- **Status of the push:** the Head has **parked the F5 arXiv push until a few days before the shorts' submission**. So this is *not* a same-day emergency — but the note **must not go out with Prop-9 as written**, and every wave that passes makes a stale claim more likely to be cited internally. Land it now.

## The defect (proven, not conjectured)
F5 **Prop-9** presents `σ*_i = √(M_eff,i · T · γ(2−γ))` as *the* exact discrete-FDT noise, as a class-level statement. It is exact **only for the Newtonian kinetic modes.** The coded O-step `p ← (1−γ)p + σξ` is an **autonomous linear OU recursion**, so its stationary momentum law is **exactly Gaussian**. In `kinetic_mode="relativistic"` the Gibbs momentum marginal is **Maxwell–Jüttner** (`π(p) ∝ exp(−(c/T)√(pᵀM⁻¹p + m₀²c²))`), which is non-Gaussian. **Therefore no choice of σ gives the coded relativistic Langevin a Gibbs invariant.** The defect is governed by `T/(m₀c²)` alone (verified: `(c=1,T=8)` and `(c=0.5,T=2)` give bit-identical `−0.7290074`).

## Independent confirmation already exists — use it, don't duplicate it
`xy-lattice-theory` §5(v) reached the same conclusion from a different direction, on a lattice, and states the **root cause more crisply than the deep-dive does**:

> *The Gibbs-preserving underdamped Langevin damps the **velocity** `∇_pT`; the code damps `p` (`p ← (1−γ)p`). For Newtonian these coincide (`Γ = γM`). For `T(p) = √(c²pᵀM⁻¹p + m₀²c⁴)`, `∇_pT ∝ p/T(p) ≠ p/const`, so FDT is violated.*

It measured the consequence on a real lattice: `⟨cosΔθ⟩/Gibbs = 1.006 / 1.040 / 1.134` at `T = 0.02/0.05/0.10`, with **drift ≈ 0** — i.e. a genuinely *different stationary measure*, not a transient. And it isolates the subtlety that must be in the note: **the exact Gibbs measure is itself relativity-insensitive** (the momentum integral factorizes out of the configurational marginal). **The failure is in the sampler, not in the thermodynamics.** Prop-9′ should say exactly that — it is the difference between "relativistic CLUs have no equilibrium" (false, and a reviewer would catch it) and "the coded relativistic Langevin does not sample its equilibrium" (true).

## Items
1. **Re-derive independently.** Do not take the deep-dive's word. Confirm (a) the free-particle p-recursion is autonomous and linear ⇒ Gaussian stationary law; (b) the relativistic Gibbs momentum marginal is Maxwell–Jüttner; (c) the impossibility follows. If you find the argument survives only under extra assumptions (e.g. `V=0`, separability), **state the exact scope** — the interesting question is whether an interacting `V` can restore non-Gaussianity (it cannot, if the O-step is a distinct substep, but say why).
2. **Restate Prop-9 with a kinetic-mode scope clause,** at all three sites. Precedent: the first corrigendum found the *corrected* statement was **stronger** than the false one. Same here — "no σ exists" is a sharper, more citable theorem than "here is σ*". Suggested shape: **Prop-9 (Newtonian)** = the exact σ*; **Prop-9′ (relativistic no-go)** = no σ yields Gibbs, with the `T/(m₀c²)` control parameter and the three fixes ranked by cost (raise `c` or `m₀` so `T ≪ m₀c²` — free; Metropolis-adjust; exact Maxwell–Jüttner momentum refresh).
3. **Check whether the same linear-OU argument bites anything else in the note** — anywhere Prop-9's σ* is used as an input (Thread-9's `½MALA(σ*)+½squeeze` mixture in CM-14 is the obvious consumer; the FDT-correct σ* is called "load-bearing" there). Report the blast radius explicitly.
4. **S2 naming check (carry-over).** Confirm the first corrigendum actually landed the decay-constant rename (`F = √(M_ch)·r*`, **not** the orbit radius `r*`) at all three sites. If any site still says "f = orbit radius," fix it here.
5. **Downstream reconciliation list** (as last time): which CM rows, negatives-registry entries, `future_work.md` sections and draft sites inherit the unqualified Prop-9. **Known already:** V2 App-F states the σ* fix as a class-level neutral theorem and needs the qualifier; matrix CM-16's mandatory flag says "holds only under `fdt`" and must become "under `fdt` **and a Newtonian kinetic mode**"; negatives **N18** ("shipped sampler never sampled Gibbs") needs an addendum that the *fixed* sampler still does not, in relativistic mode.
6. Re-run all numerical checks; rebuild `f5-note.pdf` clean; bump the CHANGELOG.

**Acceptance:** Prop-9 correctly scoped at all three sites, the no-go stated as a theorem with its control parameter, blast radius reported, PDF clean, downstream list delivered. Anonymization discipline unchanged (no CLU coinage, neutral title/authors).
