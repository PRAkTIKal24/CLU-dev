# Task: v3-revision-3 — the consolidating V3 revision: reframe MF-1 (resolved), embed final figures, close SF + bib (w12)

- **Agent:** `paper-writer` · **Output:** report to `.claude/outputs/v3-revision-3.md`; edit `.claude/papers/v3-short/` in place (CHANGELOG v0.4).
- **Read first:** protocol · `.claude/outputs/v3-referee.md` (MF-1 + SF-1/2/3 + NICE) · **`.claude/outputs/v3-interference-extra.md` (THE MF-1 resolution — its §4 CM-9-wording recommendation is your §3.2/§4 target, verbatim-grade)** · claims matrix **v1.6 CM-9 RESOLVED** (degree-bounded vs width-linear — use this wording) · `.claude/outputs/scout-modular-interference.md` (the bib, already spliced in v0.3) · `.claude/outputs/v3-banding-figure.md` (Fig 2 asset).

## Items
1. **MF-1 — REFRAME (the near-reject; now defensible AND stronger).** Recast the headline per CM-9-resolved:
   - **Lead with the structural identity** (upgraded from assertion to exact measurement): interference is **degree-bounded, not width-dependent** — R≡0 off the coupling graph (0/4,656 nonzero, 72 runs, 3 topologies) ⇒ **S = deg·R̄_edge to 6 decimals**; ring (deg≡2) statistically flat (b=+0.07±0.18, p=0.39), circulant-4 = 2× ring.
   - **Then the growth claim, now with 12 seeds:** modular b=+0.46±0.31 vs monolith **N^{1.18±0.17}**, slopes DIVERGE (Welch p=3.3e-4). **NEVER write "flat in N" for a chain** — the chain's +0.26 residual is its degree ramp (1.5→1.88), vanishing under degree-normalization; **state this preemptively** (the one number a sharp reviewer can still pull). Replace all "O(1)-in-N vs O(N)" with **"coordination-bounded vs width-linear."**
   - **Fold the block-monolith control (referee missing-exp #2):** firewall = **parameter separation, not block structure** (block_untied S≡0 exactly; shared-trunk/tied still O(N); capacity decoupled — 1,185-param tied = worst, 18,960-param untied = zero). **Consider promoting `block_tied` (deep-sets, per-pair R̄=0.97, 6× worse than the naive monolith) to the primary/co-primary foil** — it's a real architecture and makes the point harder. Frame CHLU's claim honestly: modular = block-untied + an O(κ²) graph-local leak bought for communication (nothing physics-specific buys the firewall; the physics keeps a *single joint symplectic Hamiltonian with priced communication* while getting it).
2. **Figures:** embed the NEW 6-point `fig_scaling_curve.png` as headline Fig 1 (the analyst overwrote the path — no swap needed, but drop the old swap-note); embed the banding Fig 2 (`v3-banding-figure` asset); per-figure grade labels [verification]/[evidence]/[structural] (SF-3).
3. **SF-1:** S=1 crossing (N≈5.9) → phrase as "aggregate cross-unit force perturbation exceeds a unit's own update," not storage. **SF-2:** sync "≤8%" → **"≤7.5% relative to the registered prediction"** (do not round up). **N-1:** inline the N≤8… scope on contribution-2.
4. **Bib:** the modular refs are spliced (v0.3). The Mo / Di Bernardo-Keller / RevNet-checkpointing-momentum strings land with the **Jul-11 venue scout** (sub-task 8) — if that report is in `.claude/outputs/venue-follow-up.md`, splice them; else leave the marked slots and flag.
5. Rebuild PDF.

**Acceptance:** MF-1 reframed per CM-9-resolved (near-reject retired); figures embedded; SF-1/2/3 closed. → `v3-referee-2` (w13) for the clean-pass verdict. Note the referee's SF-4/N-a pruning (demote §3.5/§3.6, Fig 3→appendix) is for the DEDICATED pruning pass, not this task (C-10).
