# c2w8p2-instruments-and-debt — riders 2 + 3a: the merge criterion's re-registration and the x64 CNN gap

> ⛔ **RIDER 1 (the co-scaling cell) IS CANCELLED BY HEAD RULING** — banked and routed to C2W9. See ITEM 1 below: it is now a one-paragraph reporting obligation, not work.

**Campaign 2, wave C2W8 PASS 2. Agent:** experiment-engineer.
**Worktree 3.** ⭐ **The ≤3 engineer-worktree cap is LIFTED for this overnight pass only** (Head, `ERRATA-C2W8-PASS2.md` §1) — all three pass-2 spokes run in parallel alongside the concurrently-live `pilot-ttt-nan-and-d5-wiring` spoke. ⚠ The cap returns to ≤3 at the end of this pass; this is a one-pass exception, not precedent. Branch **`c2w8p2-instruments-and-debt`** from `main @ 80d7d4b`, worktree `../CHLU-c2w8c`.
Writes `.claude/outputs/c2w8p2-instruments-and-debt.md` + artifacts to
`.claude/outputs/c2w8p2-instruments-and-debt/`. **Budget:** ≈ 0.25 day across two bounded
items (rider 1 cancelled). **Runs in parallel with wt1/wt2; zero file overlap, declared below.**

**Binding documents:** `.claude/outputs/c2w8-well-lifecycle/ERRATA-C2W8-PASS2.md` **§1 (the four Head
rulings — Q1 CANCELS your item 1; read this first)** · `PREREG-C2W8-PASS2.md` (**K9 is yours**) ·
the Head+Advisor **C2W8 PASS-2 DIRECTIVE** riders 2–3 · `.claude/outputs/c2w8-well-lifecycle/ERRATA-C2W8.md`
§3 · `census.json` · charter **§A4.3** (`(d, atom budget)` is ONE joint dial) · **§A9.9** ·
`.claude/outputs/c2w8-cifar-strong-phi.md` reconciliation 4.

## ⭐ DIAL DECLARATION (protocol §7) — echo before your first result
- **Dial:** none — **instrument, registration and test-coverage debt.** ⛔ No claim cell, no
  performance number, no verdict of any kind.
- **Laundering control:** N/A (no performance claim; no cell is run by this spoke).
- **Falsifies:** item 2's criterion cannot be made to refuse on either leg ⇒ report that as the finding.
- ⛔ Item 1 runs no cell (Head ruling); it cannot falsify anything this wave.
- ⛔ N94: unchanged; no promotable reading is produced by this spoke.

---

## ITEM 1 — the φ-dim / co-scaling finding. ⛔ **CANCELLED AS WORK — REPORTING OBLIGATION ONLY.**

⭐ **The Hub has already discharged most of this from banked evidence — read this before you plan
work.** The directive asks: *was the reported `d ≥ 16` inertness measured with the `(d, atom budget)`
co-scaling honoured?* **It was.** `ERRATA-C2W8.md` §3's `n_atoms` column matches
`round(512·√2^d)` **exactly** at every dimension (Hub-verified):

| `addr_dim` | `round(512·√2^d)` | ERRATA `n_atoms` | match |
|---|---|---|---|
| 4 | 2 048 | 2 048 | ✅ |
| 8 | 8 192 | 8 192 | ✅ |
| 12 | 32 768 | 32 768 | ✅ |
| 16 | **131 072** | **131 072** | ✅ |

⇒ **The store was NOT starved. The binding constraint is REACH, not capacity** — the directive's
second branch — and ERRATA §3's own second table gives the mechanism: the nearest of *all* atoms
recedes as `√dim` (0.294 at d=4 → **1.483** at d=16) while `atom_width` stays 0.3, so the write
gradient's `exp(−r²/2s²)` underflows from 6.18e-01 to **4.98e-06**.

### ⛔⛔ HEAD RULING (2026-08-06, `ERRATA-C2W8-PASS2.md` §1 Q1): **ITEM 1 IS CANCELLED. DO NOT RUN THE CELL.**

The Head has ruled the confirmation cell **not worth its cost**. The finding is **BANKED as it
stands** and **ROUTED to C2W9**:
> *the `d ≥ 16` inertness was measured at a fully co-scaled atom budget, so the store was **not
> capacity-starved**; the binding constraint is **REACH*** — and the fix is **learned p₀ /
> wormholes, C2W9 territory, not more atoms.**

⛔ **You run no `addr_dim = 16` cell. You build nothing for this item.** It is a **DECLARED NOT-RUN**
and ⛔ **must never be reported as a null.**
**Your only obligation here is one paragraph in your report**, stating the banked finding in the
form above **with its provenance carried honestly**: the arithmetic came from a **scratch probe of 3
designed-site writes** (`.claude/scratch/c2w8/timing16.py`), **not** a censused CL-stream cell, and
that qualifier travels with the conclusion wherever it is quoted. ⚠ Note in the same paragraph that
this is what now answers *whether "full CLU on a CL stream" has a non-empty operating window — and
therefore whether **C2W10's persistent store has a home*** — by the reach route rather than by buying
atoms. ⛔ **Do not re-derive, re-argue or re-measure it.**

⇒ **Your budget drops to ≈ 0.25 day: items 2 and 3a only.**

## ITEM 2 — re-register the merge criterion (prereg K9). **BEFORE any merge verb exists anywhere.**

Pass 1's criterion was **vacuous** and must never ship as-is. Hub-verified from `census.json`:
- **all 28 / 29 / 29 admitted pairs have `payload_dist` EXACTLY 0.0** (the only value present);
- `R_cert` **1.5402 / 1.4177 / 1.6482** vs key spacing (`geometry.median_nn_task1`) **0.1407 /
  0.1375 / 0.1468** ⇒ **10.9× / 10.3× / 11.2×**, so the geometric leg **refused nothing**;
- monitor **#3 `vacuous_gate` tripped 3/3 at refusal rate 0.000**.
⇒ `M` measured *"a class-incremental stream contains two items of the same class"* — **true by
construction**. ⛔ **Shipping this criterion would ship "collapse to one well per class" wearing a
certificate costume.**

**Deliver:** a **re-registered criterion** at an operating point where `R_cert` is **commensurate
with the measured key spacing**, plus a **designed negative PROVING it can refuse** — a pair the
criterion declines — **pytest-asserted**. ⚠ Both legs need a negative: a pair refused on **geometry**
and a pair refused on **payload**. ⛔ **You implement the criterion (a predicate) and its refusal
test ONLY. You do NOT build the merge verb** — merge stays unbuilt until the capture gate passes.
File the registration as a dated block in `ERRATA-C2W8-PASS2.md`.

## ITEM 3a — `cl_baselines.ConvNet` has NO regression coverage under `jax_enable_x64`

`ConvNet` builds conv weights at the **ambient JAX dtype** while `build_cl_stream` always supplies
**float32** images ⇒ under `jax_enable_x64` the convolution raises
`lax.conv_general_dilated requires arguments to have the same dtypes`. **No shipped result is
affected — all real runs are x64-off** — but the `backbone="cnn"` path is **untestable inside the
full suite**, on a path that produces **potentially quotable numbers**.
⚠ **This is the §7.23 / N211 hazard class and it has now bitten three times** (the C2W4
module-scoped-fixture x64 ordering incident; wt1's own two ordering bugs in pass 1; this).
**Deliver:** the dtype fix + **a regression test that runs the CNN path under x64**, so the coverage
gap closes rather than being documented. Keep the fix minimal and behaviour-preserving at x64-off;
assert bit-identity of the x64-off path against pre-fix behaviour.

---

## FILE OWNERSHIP (declared)

**You own:** `chlu/experiments/cl_baselines.py` (item 3a) · `chlu/core/soft_certificate.py` (item 2,
the criterion predicate) ·
`tests/test_merge_criterion.py` (**new**) · `tests/test_cl_baselines_x64.py` (**new**) ·
`.claude/outputs/c2w8-well-lifecycle/ERRATA-C2W8-PASS2.md` (**you may create/append; the Hub and
wt1/wt2 also append — append only, never rewrite another block**).
⛔ **READ-ONLY / DO NOT MODIFY:** `chlu/core/well_lifecycle.py` · `chlu/experiments/usage_telemetry.py`
(**the frozen census**) · `chlu/core/memory_potentials.py` + `chlu/core/emission_head.py` +
`chlu/core/clu_system.py` + `chlu/config.py` (**wt1/wt2's**) · `chlu/core/friction_field.py` ·
`chlu/experiments/exp_cl_entry.py` / `phi_encoders.py` (the pass-1 CIFAR spoke's) · the C2W6 files ·
the C2W7 files.
⚠ Work **in your worktree**, never the shared main checkout.

## Acceptance (mechanical)
1. Item 1: **no cell run** (cancelled by Head ruling); **one paragraph** stating the banked reach
   finding with its scratch-probe provenance, and listing it as a **DECLARED NOT-RUN, never a null**.
2. Item 2: criterion re-registered in `ERRATA-C2W8-PASS2.md` **with two designed negatives
   (geometry + payload), pytest-asserted**. No merge verb built.
3. Item 3a: dtype fixed, **CNN path covered under x64**, x64-off bit-identity asserted.
4. Full suite green with count arithmetic stated.
5. Reconciliation list in the **first 10 lines**; NOT-RUNs declared as NOT-RUNs, never nulls.

⛔ Never push `origin`; the Hub integrates and pushes `clu-dev`.
