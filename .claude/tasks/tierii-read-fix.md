# tierii-read-fix — the read-protocol iteration (§A20.3(b)–(f); ⚠ SPAWNS ON THE HEAD'S GO after task-file review)

**Campaign 2, wave C2W5/6 boundary. Agent:** experiment-engineer. **Worktree 2 of ≤3 on spawn.**
Branch `tierii-read-fix`. Writes `.claude/outputs/tierii-read-fix.md` + artifacts. Charter
**ADDENDUM 5 §A20.3 (binding)**; tier-ii control = **the ORGANIZER SWAP** (§A13; the settle-deleted
launder is an inherited diagnostic only).

**What is being replaced, verbatim from the ruling (§A20.1–2):** the **P = 4 frozen-offset
occupancy read is REFUTED at P = 4 — replaced, not patched.** Measured basis: P=4 reaches 2.20
distinct wells of the F=4 the sum needs (≥F on 5.0 % of queries; exact-set occupancy 0.0000/2560);
the settle LOWERS it to 1.70; the launch information supports 0.272 (out-of-class reference
ceiling, never an arm bar); P alone does not fix it (0.40 precision at every P — the assignment
rule is also wrong).

**Read first:** charter **§A20 in full — it is your specification**; `.claude/outputs/
orgdiv-null-arms.md` **§3–§4 + §12** (the mechanism, the ceiling, the two protocol requirements);
`orgdiv-cat-test.md` §5.2/§13 (the S_eff collapse — your arm inherits its fix), §3 (m=8/a=32 as
measured constraints); `psi-payload-residual`'s report when it lands (the read-out fix composes
with yours); `PREREG-TierII.md` (still binding where §A20 doesn't supersede it — K1–K5, the ledger
rules, the claim form §2.6); the `[C2W5]` second-review §10 entry.

## ⭐ DIAL DECLARATION (protocol §7, C2 form) — echo before your first result
- **Dial:** TIER ii — the organization dividend, read-protocol iteration.
- **Control:** the ORGANIZER SWAP (the null-arms rig is merged and warm: `chlu exp-null-arms`; the
  arms re-run against your new read under the SAME launch/read budget — §A20.3(c) guard 4 puts k
  on the byte ledger for ALL arms).
- **Falsifies:** the prereg falsifiers you re-register (below) — each with sign/threshold/seeds
  BEFORE the arms run. **K0 (new, standing):** `P(≥F distinct wells reachable)` computed from the
  launch geometry alone, store-free, BEFORE building — if your protocol's K0 < 0.9, redesign
  before running anything.
- **Does NOT falsify:** N1-still-composing-zero (the nulls' failure is not your claim); anything
  about tier i/iii.

## The five design inputs (§A20.3, each a deliverable)
1. **(b) The multi-well read protocol** — your core deliverable. Requirements measured by
   null-arms §12: the read must **address F distinct wells** and **not quantise away the continuous
   launch coordinate** (quantisation is the destructive step — N1/N2/N3 all die on it; the 0.272
   ceiling decodes the continuous code).
2. **(c) The k-particle learned-launch head:** the encoder emits **k full particles** (k q₀ +
   per-particle mass + friction — the A4.3 particle head, now plural); **overlap-as-confidence**
   (confident ⇒ the k collapse onto F unique wells; unfamiliar ⇒ k scattered guesses);
   **dedupe/evolve-unique as a controller verb** (designed verb, learned policy — §3.2) doubling
   as the compute dial. ⛔ **The four guards, all blocking:**
   - **launch-only launder, RECOMPUTED LIVE (Advisor amendment A1, binding):** the launder is the
     task's OWN learned launches with the **store deleted**, scored through the **same reader class
     at the same k** — **the full read must beat THAT recomputed score.** ⛔ 0.272 is the OLD
     protocol's out-of-class reference ceiling: it may appear in your report as a reference line
     only, **never as the live bar** (a strong launch head could clear a stale bar with the store
     deleted — that is exactly the leak this guard exists to catch);
   - **soft-occupancy training signal** (hard assignments don't backprop; the write-first ordering
     is measured: gradients 1e-10 until wells exist, O(1) after);
   - **staged store-then-launch co-training** (w20: free learning erases design — stage it);
   - **k on the byte ledger, every arm matched** (k is capacity; a hidden k is ledger drift).
3. **(d) Consolidate-to-budget + trash-region pruning:** over-dig freely → **merge to the designed
   well budget** on mechanical, measurable criteria → **pruning BELOW budget is a controller
   decision**, pruned wells routed to the trash region `γ_φ(q)` (built in C1, never used — its
   first use). Spurious shallow wells are TRASHED, never merged into meaningful ones. This is also
   the S_eff collapse fix's home: S_eff ∈ [S/2, S] monitored, a collapsed run reported COLLAPSED.
4. **(e) Learned p₀ as a registered lever** (ballistic reach across informationally dead inter-well
   gaps; complements wormholes; OQ-2's revival trigger territory — inference-read claims stay
   closed per §A14.1, this is a TRAINING/reach lever). Trajectory-read/implicit gradients only.
5. **(f) The taxonomy on the record in your report:** launch selects the memory · particle
   attributes select the read style · well geometry determines the memory mode.

## Protocol constraints carried
`m = 8` / `a = 32` as measured constraints (K2b/K1) · measured-`s` operating point,
confinement-subtracted (§7.28) · soft-certificate band, multi-particle-descended read only, never a
settled point (Thms O1/O2) · the F3-equal tuning standard BOTH ways (your arm and the swap arms get
the same grid) · A17.4 + the selection-stability rule · multi-seed (5) before any number ·
`PREREG-TierII.md` §2.6's claim form (no well ever named semantically) · declared NOT-RUNs never
nulls · ⛔ no byte-matched tier-ii promise (prereg §5.2 unchanged).

## Pre-registration obligation
Before the harness runs: file `PREREG.md` with K0 + re-registered falsifiers (the F1 organizer-swap
bar at `OD_min > +0.05` on the worst reader stands; F5's imitability caveat carried) + your own
numeric predictions (the w14 rule). **Steps in the null-arms style: the kill-conditions and K0
run before the arms.**
⛔ **ψ version pinning (Advisor amendment A2, binding):** the PREREG declares WHICH ψ each cell
uses — shipped or payload-residual-fixed — and **no mid-task hot-swap**. If `psi-payload-residual`
lands before your science runs start, adopt the fixed ψ **uniformly across ALL arms** and record
the adoption (date + commit) in the PREREG and the report. ⛔ ψ's marginal value is NOT measured
here — it stays in its own pre-registered ablation (CSF3 run 1 vs run 2); this task never
entangles the read-protocol comparison with a ψ A/B.

## Acceptance
K0 pre-condition adjudicated first · the four (c) guards implemented and each shown firing on a
designed negative (a guard that cannot fire is N74's vacuous gate) · the organizer swap re-run
against the new read with `OD_min` per reader × seed · S_eff in band or the run labelled COLLAPSED ·
tests green · report → Hub, spawn nothing. **Ownership (declare exact list):** new read-protocol
module + additive `factored_store.py` hunks (coordinate if cat-test files move) + your tests. ⛔ Not
`psi_readout.py`'s quarantined path, not the rivals/null-arms scoring code (extend via its public
API). **Git:** branch + scoped worktree; never push `origin`; `clu-dev` only.
