# doc-curator-c2w4-fold — the C2W4 fold: verify the top-up, fold the A18 rulings, close the deltas

**Campaign 2, wave C2W5. Agent:** doc-curator. **NO WORKTREE. Spawns immediately.** Writes updates
per each transfer doc's own protocol + `.claude/outputs/doc-curator-c2w4-fold.md` (the audit
trail). Charter **ADDENDUM 4 §A19 task 4**.

⚠ **Scoping note (Hub, 2026-08-01):** much of §A19.4's literal list was ALREADY covered by
`doc-curator-c2w4-topup` (accepted at the C2W4 close: zero placeholders, N191–N210 filed, matrix →
v2.9 Hub-confirmed, §3's stale C-1 corrected). **Your task is therefore VERIFY-then-DELTA: confirm
each §A19.4 item against the v2.9 registries, fold only what is missing — and fold the A18 rulings,
which post-date the top-up.** Do not re-file what is already current; your audit trail says
"VERIFIED PRESENT (where)" or "FOLDED (what)" per item.

**Read first:** charter **ADDENDUM 4 in full (§A16–§A19)** — the A18 rulings with their updated
ratification statuses are your primary source; the `[C2W4-CLOSE]` + second-review §10 entries
(incl. reconciliations 1–7); `claims_matrix.md` (v2.9) §0/§3; `negative_results.md` (through N210);
`.claude/papers/bprime/CHANGELOG.md` (the r2 rename you must record).

---

## The checklist (verify first; fold the gaps)
1. **A18.1 never-quote additions, in their FINAL ratified form → `claims_matrix.md` §0:** ⛔ any
   rescue verdict at n=3 *including C2W4's own* (the "no margin against DeltaNet is quotable"
   sentence is RETRACTED — at n=9 deltanet IS rescued and loses to the raw table by −0.3057 ±
   0.0316) · ⛔ "≥ 3.6 SE" (minimum is 4.4) · ⛔ the ∞ gradient ratio without naming sign-SGD ·
   ⛔ pilot toy numbers as tier-iii/pilot-scale results · ⛔ unscoped γ-bands (read-budget scoping,
   reconciliation 2 — coordinate wording with the theorist's formulation if one exists; else file
   the C2W4 measured form: ρ_conv 0.83 @ 48 steps vs 2.6e-7 @ 1200) · ⛔ any C2W4 rival number in a
   draft without the F3 before/after verdict beside it. Verify the top-up's versions match the
   ratified wording exactly; amend where they differ.
2. **A18.2 / C-1:** verify `claims_matrix.md` §3 now carries POST-reversal C-1 (top-up says done)
   AND that the **paper-writer agent brief** carries it with the incident dated (Hub fixed it
   2026-08-01 — verify, don't re-fix).
3. **A18.5 (the r-convention) → filed as a standing editorial rule:** B′ revisions are `r2/r3/…`,
   never "v2"; `draft-v2.md` → `draft-r2.md` renamed 2026-08-01 (CHANGELOG records it); the
   `[C2W4-CLOSE]` ruling 5 `draft-vN` form is superseded for B′. Record wherever your protocol
   keeps editorial conventions (and cross-link the collision rationale: the V2 short).
4. **CM-26(mm):** monitor #6 = **27 CONFIRMED, PROVISIONAL lifted** — verify present.
5. **Pilot known-issues entries** (verify vs top-up; fold gaps): `saddle_reach_threshold`
   zero-division at `D → 0` (guard belongs in `monitors.py` — note the C2W5 engineer owns the
   verification) · the module-scoped x64 fixture hazard (cost 10 red tests; fix pattern filed) ·
   plain-SGD vs sign-SGD store-channel severing (the ∞-ratio scoping) · read-budget-scoped ρ_conv
   bands · the φ-gain calibration as declared anti-collapse init for streaming venues · "enwik8
   chunk summaries are not naturally `d_safe`-separated" (a reportable property of the venue).
6. **N-registry fold of the pilot's refuted preregistrations** (verify N191–N210 coverage; fold any
   gap — e.g. the write-budget refutation: 16× inner steps buys zero acquisition).
7. **A17.4 as standing practice** (paired/multi-init controls or n ≥ 9 on any gate whose control
   has learned-init variance) — file in the philosophy-synthesis ledger with a ⟲ dated addendum if
   not already there, plus the wave's other standing lessons: *build the kill-condition before the
   thing it can kill* (adopted A12) · *a memory that carries nothing cannot be read better by
   reading it longer* · *the tier's own control in the same paragraph as any "dividend ≈ 0 is a
   feature" sentence* (CM-27(b) adjacency).
8. **HEP_primers.md:** fold only if the pilot/F3 material adds onboarding-relevant physics content
   (likely nil — say so explicitly if so).

## Acceptance
Audit trail with a per-item VERIFIED/FOLDED verdict and location; registries version-bumped only if
you changed them (matrix edits propose a version + wording for Hub confirmation, per standing
practice); zero body-edits to published spoke reports (dated erratum banners only, C-3 precedent);
task files untouched (C-4). Report → Hub.
