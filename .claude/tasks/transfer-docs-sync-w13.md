# Task: transfer-docs-sync-w13 — the wave-13 doc pass (w14, curator)

- **Agent:** `doc-curator` · **Output:** `.claude/outputs/transfer-docs-sync-w13.md`
- **Read first:** protocol · your standing duty (4 docs: `HEP_primers.md`, `philosophy-synthesis.md` ledger addendum, `negative_results.md`, `future_work.md`) · the w13 outputs: **`v2-symmetry-deepdive.md` (v1.1, §7bis)**, **`xy-lattice-theory.md`**, **`v5-gate.md`**, `fix-pack-5.md`, `v2-revision-4.md` · `.claude/claims_matrix.md` **v1.8** (CM-16 tightened, CM-17 new — already landed by the Hub; do not re-edit the matrix).
- **Scope:** transfer docs only. Never edit drafts, the matrix, or `chlu/`.

This is a heavy wave: two proven code defects (one blocking), one proven theory defect, a correction to a number this program has been quoting, **two dictionary errors in a thesis the Hub itself wrote**, and a V5 GO. Nothing below is optional.

## 1. `negative_results.md` — new entries and two amendments
**New (propose tier yourself, justify):**
- **The emergent arm has no coset register at all.** `v5-gate` R1: on emergent (MLP) checkpoints the coset direction is only **1.7–4.9× softer than the stiffest direction in the whole spectrum** — a middle-of-the-spectrum massive mode, not a near-Goldstone. `1−|λ_coset| ≈ 1e-3` vs designed `≤1.1e-15` (**~12 orders**). Any written coset value `δ ∈ {0.1,0.3,0.5}` rad relaxes **completely** (`|retained| ≤ 2.1e-3`); capacity is `log₂(2–3 washboard minima) ≈ 1–1.6 bits`, not a continuum. **Cor-13 / CM-16(a) is designed-only.** (The CM-16 *unification* — the V-curve with its minimum at `γ_crit = 2εμ` — **does** generalize, exactly, 3/3 seeds. Record both halves; the split *is* the result.)
- **⛔ `fdt` noise has a NaN gradient at γ=0** (`xy-lattice-theory` §5 i-b) — `sqrt(0)` differentiated w.r.t. the learnable `log_mass`. Trigger is **all repo defaults except `langevin_noise="fdt"`**; `train_chlu` NaNs every parameter at epoch 0's sleep step. Still live at `d6f8bac`. **No FDT-correct model has ever been trainable at defaults.** → `fix-pack-6`.
- **`GatedCoupling` returns the mean energy, not the free energy** (§6.2) — force reverses sign at `v = 0.8020`, *inside* the open region; the shipped wormhole **repels its endpoints**; on an XY bond it flips the exchange antiferromagnetic. **No shipped claim contaminated** (only `v3-lattice-build`'s smoke ever built one; V1's wormhole uses its own held gate) — a latent trap. Note the *methodological* lesson: the smoke checked the gate's **energy** and never its **force**.
- **`spring_coupling`'s default random `W` breaks the global U(1)** — `P(J<0)=0.52` at init; after 400 epochs on ground-truth-XY data `J/J_true ≈ 0.02` while `h₂/|J| = 0.6–2.1`. Conformal init (`W=𝟙₂`) recovers `J/J_true ≈ 1.03–1.07` **at equal-or-better loss**. A `p=2` anisotropy is a **relevant** perturbation at the KT fixed point (`x₂ = 1/2`) ⇒ the shipped default would destroy any 2-D memory phase.
- **`two_timescale_orbits.py` cannot identify a coupling** — independent uniform per-unit phases, different `ω` ⇒ no inter-unit phase correlation; and circular-orbit data is fit by an isotropic harmonic well, so trained `so2_invariant` units put their minimum at the **origin** (`r* < 1e-3`, 3/3 seeds): **no vacuum ring, no coset.** ⇒ `exp-lattice`'s banded-vs-uniform smoke says nothing about coupling physics or Goldstone structure. **Scope limit, not a bug** — and it does **not** touch the banding claims (CM-5/CM-10), which are about mass, not coupling. Say that explicitly so nobody over-reads it.
- **A methodological negative worth its own entry:** `v5-gate` §3.5 — the raw `d log n₁/₂/d log T` and `d log n₁/₂/d log γ` exponents are **not discriminating** between designed and emergent; a matched designed control on the same grid gives the same shallow slopes. Both families carry CM-16(d)'s `ℓ_θ/Δ` boundary-layer bias. **Any designed-vs-emergent claim quoted from raw exponents would have been an artifact.** Generalize the lesson: *never quote `n₁/₂` without `Δ` and `ℓ_θ/Δ`.*

**Amendments:**
- **N10** ("FDT per-mode-T violation does NOT drive the MNIST imbalance") — **re-opened, not refuted.** Exp-C is `relativistic` (`config.py:220`), and CM-17 proves *no* σ gives relativistic mode a Gibbs invariant ⇒ **both arms of N10's comparison were non-Gibbs samplers**, with a variance-level instrument blind to a tail-shape error. Mark it **UNDER RE-TEST**, pointing at `relativistic-gibbs-expc`. Do not delete it (C-9).
- **N18** ("shipped sampler never sampled Gibbs") — addendum: the **fixed** sampler still doesn't, in relativistic mode. `T/(m₀c²)` is the control parameter; Exp-C runs at `1.0` (`KL = 0.384` nats); `finalA` ran at `0.04`.

## 2. A number this program has been quoting is wrong
`t-lever-forgetting` §7 predicted a `γ_φ` friction hole is a **≈13.9× memory vault**, and that number propagated into the running log and `future_work.md`. **It is wrong** — it assumed the hole is a locally thermalized bath. The shipped `FrictionField` is **absorb-only** (it damps `p` but does not rescale the noise), so `n₁/₂ ∝ γ_eff²` and the hole is a **brake *and* a refrigerator**. `v5-gate` pre-registered both hypotheses and measured **107.77 ± 4.78×** vs the pre-registered `110.25×`, **rejecting the coupled-bath `13.88×` by a factor `8.11 ± 0.37`** against its own dedicated control. `T_local = 1.26e-4` inside vs `1e-3` outside.

**Sweep every doc for "13.9" / "13.88" and correct it**, noting the mechanism (absorb-only ⇒ refrigerator) and that the absorb-only design is now **load-bearing and must not be "fixed"**.

## 3. `future_work.md`
- **Thread-10 — two dictionary errors, both load-bearing, both the Hub's:** `ρ_s = J = 2κr*²`, **not `F²`** (`F² = M_ch r*²` is an *inertia* and cancels from the Gibbs measure); and **`n = dim(G/H) + 1`**, not `dim(G/H)` (S¹⇒XY/O(2), S²⇒Heisenberg/O(3), Z₂⇒Ising/O(1)). O(n) universality only for *spherical* cosets — a torus bank `T^k` is `k` decoupled XY models. Correct them wherever they appear.
- **Replace "a 2-D array remembers" with what is actually true.** Equilibrium forgets any generic written pattern **in every dimension**. What a 2-D array below `T_KT` remembers is **topological**: the winding numbers `(w_x,w_y) ∈ π₁(S¹)² = ℤ²`, with `τ ∝ L^{πρ_s/T − 2}`, the exponent vanishing **exactly at** `T_KT` by the Nelson–Kosterlitz universal jump `ρ_s/T → 2/π`. **The KT transition *is* the memory transition.** `T_KT = 1.786 κ r*²`. And the same `π₁` that bounds a single register's dynamic range (aliasing) is the one that creates the vortices — **aliasing and vortices are the same winding**; an `S²` bank has `π₁ = 0` ⇒ no aliasing *and* no 2-D memory phase. Also record the honest null: `D_Θ = D_θ/N` for the global mode holds in **every** dimension — a trivial `N`-fold gain that must never be sold as a phase transition.
- **New from the deep-dive §7bis:** the retention–bandwidth bound (R5), the rapidity/companding register (R3), the causal memory-lifetime floor (R7), and R6's **conditional** aliasing protection (`sinh(ζ*)/ζ*`: `48.9×` at γ=0.05, `1.29×` at γ=0.01, `→1` as γ→0). Also the new open questions O6/O7/O8.
- **V5 "Forgetting" is a GO** (Head decision pending Hub brief). Record its evidence base and its honest scope: the vault + the sign flip + the V-curve unification generalize; **the latch register does not** (designed-only).
- **T_φ(q) build:** still **HELD** (Head, this wave).

## 4. `HEP_primers.md`
New sections, in the primers' "physics rebuilt for ML experts" register: **(a)** the linear-OU-vs-Maxwell–Jüttner no-go (why damping `p` instead of the velocity `∇_pT` breaks Gibbs the moment the kinetic term stops being quadratic) — this is the most ML-legible physics lesson of the wave; **(b)** the XY/KT dictionary with the two corrections above, and the memory–vortex correspondence; **(c)** *modulus vs Goldstone* (S5: V-flatness + γ>0 suffices; equivariance is sufficient but **not necessary** for neutral memory) — with the nomenclature-ledger entry.

## 5. Ledger (`philosophy-synthesis.md`)
Wave-13 ⟲ addendum. The chapter deltas that matter: **Ch. 5 (mass/allocation)** — R5 turns "M is the budget allocator" from slogan into a candidate conservation law, now under test (`rb-bound-trained`). **Ch. 7** — a second, independent sampler defect, found *after* the first was fixed and after a tier-A negative had been drawn from the broken instrument; the epistemic lesson is about **instrument validity preceding inference**, and it should be written up as such, because it will happen again. Also record the wave's two *good* process artifacts: `v5-gate`'s pre-registration caught a wrong published number, and the theorist reported two of his own refutations (X4, X5) rather than dropping them.

**Acceptance:** four docs current through w13; the "13.9×" sweep done; N10 marked under-re-test and N18 amended; the two Thread-10 dictionary errors corrected everywhere; nothing deleted (C-9). Report a docs-debt list if anything is left.
