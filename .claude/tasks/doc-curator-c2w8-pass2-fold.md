# doc-curator-c2w8-pass2-fold — the owed pass-2 fold + the `M` vacuity qualifier + the count-arithmetic rule

**Campaign 2, wave C2W8 PASS 3. Agent:** doc-curator. **NO worktree.** **Spawns NOW** — its scope is
**pass 2 only**, which is closed, merged and green at `main @ 1eda6a0` (1495/0). ⛔ **Pass-3 results
are OUT OF SCOPE** (nothing is measured yet) and are a later fold.
Writes `.claude/outputs/doc-curator-c2w8-pass2-fold.md`.

**Binding documents:** `.claude/outputs/c2w8p2-{compact-atoms,emission-head,instruments-and-debt}.md`
+ raw `capture_armA.json` · charter **ADDENDUM 10 (§A29–§A30)** — the adjudication your fold must
match · the `[C2W8-PASS2]` §10 entry · `ERRATA-C2W8-PASS2.md` · `PREREG-C2W8-PASS2.md` (**never
edited**) · `AGENT_PROTOCOL.md` §5.

## ⛔⛔ READ THIS FIRST — the sweep hazard, and it has now caught two Hubs

A **directory-scoped `Grep` over `.claude/` silently returns nothing** because `.claude/**` is
gitignored, while the same pattern against a **named file** returns hits. ⛔ **Any "I swept `.claude/`
and found nothing" claim is VOID unless the sweep was per-file, or used a tool that ignores gitignore
(`grep -rn` via Bash does not skip ignored files).** ⛔ **Positive-control every negative sweep** —
search first for a string you know is present in the same scope, and **report the control's hit count
beside every "no hits" claim**. ⚠ The C2W8 Hub tripped this wire itself (asserted "grep-checked"
without running it; caught and corrected on the record). **Do not make it three.**

## The fold

**1 — ⭐⭐ THE HEADLINE, and it must be filed as TWO facts that travel together, never one:**
> **CAPTURE IS FIXED — basins 46/48 (was 1/48), median capture radius 0.29–0.43 = 2.0–2.9 σ_q,
> self-probe decode +3.65…+4.75 SE above chance (was exactly chance), site drift 0.001–0.007 vs
> spacing ≈0.14, own-beats-foreign 44/48 (was 3/48) — AND ADDRESSABILITY DID NOT MOVE: reads landing
> in no basin 58/62/62 of 64, DIGIT-IDENTICAL to pass 1; items never read 15/15/14 of 16 (pass 1:
> 14/14/14, i.e. marginally WORSE); read-vs-launder margin −0.354 (pass 1: −0.333).**
⛔ **Neither half is ever quoted without the other.** ⭐ **The wells are real; queries still don't
reach them.**

**2 — ⭐ THE TWO ATTRIBUTION FINDINGS THAT OUTRANK THE GATE RESULT (§A29.4):**
- **(i) Width co-scaling is the lever, NOT compactness** — a plain Gaussian at the same co-scaled
  width also clears the gate 3/3 and **decodes better** (4.75–6.94 SE vs the compact arm's
  3.65–4.75). ⛔ **No artifact may say "compact atoms made the store capture."** The registered form
  is *co-scaled width + `atom_site_local_init`*; **kernel form is a secondary axis.**
- **(ii) Pass 1's foreign-atom domination was MANUFACTURED BY THE WRITE** — a 300-step gradient write
  drags atoms across the ball and the displaced atoms are everyone else's background; a write that
  *places* atoms gives foreign>own on **0/48** (pass 1: 45/48). ⛔ **Pass 1's §1(e)/(f) diagnosis was
  right about the SYMPTOM and wrong about the CAUSE** — fold that correction in place, struck and
  annotated, never deleted.

**3 — ⭐⭐ THE DEFECT CLASS, registered as standing doctrine (three instances, one wave):**
pass 1's **vacuous `M`** · monitor #3's **never-refusing admission gate** (refusal rate **0.000**,
**still open**) · pass 2's **gate blind to addressability**. ⇒ **"A GATE THAT CANNOT FAIL ON THE
THING THAT MATTERS."** ⭐ **Record that the third instance was caught by its OWN SPOKE against its own
interest** — arm B passed all three legs 3/3 with `n_never_read = 16/16` and margin −0.594 and said
so. **That conduct is the thing to reinforce.**

**4 — HOUSEKEEPING (the directive's items 6 and 7), both yours:**
- ⛔ **Pass-1/pass-2 `M` is NEVER quoted without the vacuity qualifier**, and **any census quoting `M`
  must state WHICH criterion produced it.** Pass 1's criterion had `R_cert` at **10.3–11.2×** the key
  spacing (refused nothing) against a population whose `payload_dist` is **identically 0.0**. **K9 is
  registered with two designed refusal negatives and the merge VERB stays UNBUILT.**
- ⚠ **The count-arithmetic base is CHECKOUT-DEPENDENT and that is the rule to file, not a number.**
  The directive gives **1445 at `80d7d4b`** (the shared checkout); the Hub measured **1438 collected
  / 1436 selected** at the same commit in a *fresh worktree* — a **7-test delta** because three
  modules (`test_bprime_rivals`, `test_anti_erosion`, `test_industrial_loaders`) use
  `importorskip`/`skipif` and a fresh worktree lacks the cached datasets. ⭐ **File the standing rule:
  test counts are comparable only WITHIN one checkout; state the checkout with the number.** Both
  figures are correct in their own environment; neither supersedes the other.

**5 — ALREADY DONE, verify and record rather than redo:** the directive's housekeeping item 4 (the
`ERRATA-C2W8-PASS2.md` §2 section-number collision) was **resolved by the Hub at pass-2 integration**
— wt1's later block renumbered to **§5** with a dated banner explaining why the *earlier* block kept
`§2` (it is cited by hash-stable references in tracked code). **Confirm it on disk and record it as
closed; do not re-resolve it.**

**6 — Three Hub process failures, filed as process record** (all the C2W8 Hub's own, all already on
the §10 record): a `config.py` merge resolution that **consumed a `@dataclass` decorator** ⇒ 19
suite failures from one line, invisible to parse+import and caught only by the suite · a confirming
suite **lost to a volatile `/private/tmp` worktree** · a `-q` partial log **under-reported** as "at
least one F" when there were 19. ⭐ Standing forms: *a mechanical keep-both-sides merge resolution is
not safe across a decorator boundary; parse+import is not verification of a hand-resolved merge* ·
*a confirming suite belongs on stable disk* · *never characterise failures from a `-q` partial log —
use `-rf`.*

**7 — Still open and needing an owner, not a fourth banking:** ⛔ **the K6 cross-reference slip**
(Add.9 §A27.2 cites *"K6, §A28.3"*; §A28.3 is the well-lifecycle ruling, K6's home is §A26.3). It has
been banked at **three consecutive Hub sessions**. Record its status prominently.

## ⛔ Out of scope (declare as such, never as gaps)
Pass-3 results (nothing measured) · `PREREG-C2W8*.md` (**never edited**; errata are dated blocks) ·
all code · the arm A vs arm B race (⛔ **VOID as a comparison and UNADJUDICATED per §A30.1** — do not
record a winner) · any tier-ii / full-CLU / I2 verdict.

## Acceptance
1. §1's two halves filed as one inseparable pair; §2's two attribution corrections folded **in place,
   struck and annotated, nothing deleted**.
2. The defect class registered as standing doctrine, with the arm-B conduct note.
3. Items 6 + 7 filed; item 4 verified-and-recorded as already closed.
4. Every negative sweep reported **with its positive control's hit count**, per-file.
5. Your report's **first 10 lines** name anything you could not close and why.
