# Task: v3-interference-extra — save the V3 headline: N∈{2,6} interference points + block-monolith control (w11; retires the near-reject MF-1)

- **Agent:** `results-analyst` · **Output:** `.claude/outputs/v3-interference-extra.md` (+ updated `fig_scaling_curve.png`)
- **Read first:** protocol · `.claude/outputs/v3-referee.md` **MF-1** (the near-reject: "O(1)-vs-O(N)" is a 2-point claim, both points same slope — modular grew ×2.56, monolith ×2.18 across N=4→8; R_nn doubled) + missing-experiment items 1–2 · `v3-interference-ntk.md` §1/§3 (the harness + the R metric + `interference_init.json`) + its scratch scripts · claims matrix CM-9 (UNDER RE-SCOPE — this task decides its final wording).
- **Why:** V3's headline currently rests on two lattice sizes whose growth rates don't differ. This task either (a) makes the growth claim defensible with 4 points showing divergent slopes, or (b) confirms the growth rates are parallel → the paper reframes around the structural argument (R_far≡0 + coordination-number bound + 4-decade separation). **Either outcome saves the paper; not running it leaves a near-reject.** Repo read-only; scratch in `.claude/scratch/v3-interference-extra/`; laptop, same harness (~min-scale per the w6 run).

## Items
1. **Interference at N∈{2,6}** (add to the existing N∈{4,8}), 3 seeds, both modular (banded≡uniform) and shared-V_θ monolith arms, the SAME R metric (CD-update basin displacement, row-sum off-diagonal). Deliverable: the **4-point S_B-vs-N curve** with seed error bars + the fitted slopes. **State plainly whether the modular and monolith slopes diverge** (log-log slope comparison with error bars) — this is the MF-1 verdict.
2. **Block-structured-monolith control** (referee missing-exp #2 — "is it modularity or just separate nets?"): a SINGLE potential V:ℝ^{Nd}→ℝ with explicit block-diagonal structure (no cross-block terms) vs the full shared-V_θ monolith vs the modular lattice, at N=4/8. Deliverable: does block structure ALONE recover the firewall (R→~1e-5), or is the coupling-graph locality doing the work? This is the discriminating control that either strengthens ("modularity = the block structure, and it's sufficient") or bounds the claim.
3. **Regenerate `fig_scaling_curve.png`** with all 4 N points + error bars + fitted-slope annotations (replaces the 2-point version; v3-revision-3 embeds it as headline Fig 1).
4. Deliverable for the Hub: a one-paragraph **CM-9 final-wording recommendation** — either "scoped growth claim survives (slopes diverge: modular b=X, monolith b=Y)" or "ship structural-only (slopes parallel; lead with R_far≡0 + coordination bound + 4-decade separation)."

Flag-provenance per §5. Negatives (if block-monolith already recovers the firewall, or if 4 points still don't separate slopes) written per C-9.
