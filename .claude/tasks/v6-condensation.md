# v6-condensation — paper-writer (V6: the TAE audit paper, condensed from `draft-r7`)

**Commissioned by the Shorts Advisor (charter: `.claude/advisor-head-shorts-charter.md`, Addendum 17; Head rulings: Q6 sequence complete (fold → referee → closures done), V6 → TAE ratified (Add.14), 2026-08-18).** Read `.claude/AGENT_PROTOCOL.md`, then this file. You create `papers/v6-short/` (draft.md canonical + CHANGELOG.md). ⛔ `papers/bprime/*` is read-only source — the long draft is NOT edited.

**DIAL DECLARATION: none — condensation pass; no new measurement; no laundering control applies.**

## The venue format (TAE, Head-verified 2026-08-18 — this is the paper's shape)
- **Up to 8 pages main text, EXCLUDING references and appendices.** Appendices unlimited — ⛔ but "reviewers are not required to read appendices" ⇒ **every load-bearing result, control, and defense lives in the main 8 pages.** Appendix-maximalism governs what overflows: nothing is cut outright, it moves to appendices.
- Double-blind, single PDF, OpenReview, non-archival. Full desk-reject checklist (Add.2): scrubbed metadata · no acknowledgments/funding · third-person self-citation ("the CLU, introduced as CHLU in Jawahar & Pierini (2026)") · ⛔ never a reference to any other short of ours (C-8 hermetic) · theory note cited per its current status.

## Source and what MUST survive into main text (Add.12 V6 gates + r6-referee)
Source = `papers/bprime/draft-r7.md` (the refereed + closure lineage; its abstract/contributions already carry the folded evidence — start from them, don't resurrect r5's).
1. **The protocol as the contribution** (TAE's genre: evaluation protocols as the object of study): matched-byte table launder · two-sided byte ledger + learned-initial-state rule · +0 B substitute audit · same-keys null · blank-store control · rescue gate.
2. **The self-implicating headline**: 0 of 6 rivals AND not the CLU either (−0.2897 ± 0.0328, 8.8 SE), rescue gate disqualifying our own arm, reported as a finding.
3. **Both protocol findings** (the wrong-control/manufactured-dividends result; the gate-power + selection-split findings).
4. **The R.2 venue firings + the R.3 label-embargo methodology** (with every rider adjacent: ARF 14.35× · nine references · registered k = 5 = 306.76 · band-4 exclusion · fallback-being-retired framing · embargo delta convention).
5. **The §4.8 real-image substrate result at the 2,364× byte handicap** — the toy-scale armour; ⛔ it does not move to an appendix.
6. **The §A20.5 substrate-scope sentence** in main text (r7 carries it — keep it there).
7. **The three hostile-quote defenses** (r6-referee): the "expected to win" rebuttal paragraph (SF-7's one-place assembly) · the "indistinguishable from an empty store" framing with its N190-companion clause (inference-cost win, not failure) · the thinness answer (now: two real-venue firings + the real-image substrate; ⚠ if `bprime-dividend-family-2` lands before freeze, its result supersedes the thinness defense — fold it with its own riders and prereg status, whatever its outcome).
8. **The byte-floor identity** (deletion ≡ compression, the ≥2.20× floor) and the `exp(−½(d/s)²)` coupling suppression — B′'s theory assets — at least in compact form.

## Rules
1. Approved wordings stay character-faithful; riders never separate from their numbers; the modal-value rule on byte figures; "reads no worse than" discipline; the genuine-win score sentence where performance is discussed.
2. Honest-negatives distributed to appendices per C-9/Q4; App L's negatives ledger condenses, never disappears.
3. Figures: Fig 1 (all seven bars, uniform n = 9, from r7's render) is the headline; re-use r7 figures, render nothing new.
4. Never-quote sweep (per-file, positive controls, zero-hit list printed in the CHANGELOG entry) over the finished draft against matrix §0.8–§0.14.
5. Placeholder title/authors per house policy; the draft carries the TAE venue-class header.
6. ⚠ If the dividend-family spoke has NOT landed at freeze, the draft ships without it and the CHANGELOG notes the fold-point for a later revision — do not wait, do not pre-claim.

## Acceptance criteria
1. Main text ≤ 8 pp (state the estimated page count in the CHANGELOG entry) with items 1–7 all in main text.
2. Every number traces to r7 (and through it to the registries); zero new claims not present in r7.
3. CHANGELOG entry: the cut map — what stayed main-text, what moved to which appendix, what was dropped-to-long-only (each with one line of reasoning).
4. Zero edits outside `papers/v6-short/`.
