# doc-curator-w19-w20-sync — doc-curator report

Task + acceptance criterion: process the w19 backlog (negatives registry + claims matrix), record the w20 vision reframing, add the w19 physics to the primers, register the shelved KT tranche — all four docs current, pending blocks cleared, four retired claims struck everywhere.
**Status: done.** 5 files edited (the task's 4 + `future_work.md`, which my standing protocol requires every wave). **No tracked code touched. No git footprint.**

> ⚠ **RECONCILIATION LIST — needs an owner (protocol §5, first 10 lines).**
> 1. **⚠ THE LEDGER AND `future_work.md` HAVE A TWO-WAVE HOLE: w17 and w18 have NO addendum/sweep.** The ledger runs …w15 · w16 · **[w17 missing] · [w18 missing]** · w19. The w19 handover references w18 work as delivered (`scout-hepa-predictor`, `cmapss-fd002-004-fetch`, `hepa-protocol-*`). **I did not fill it** — reconstructing two reviewed waves' verdicts from a task scoped to w19/w20 would be exactly the reinterpretation the charter forbids. **Hub-owned catch-up pass.**
> 2. **The addressable-memory claims family (CM-18/19/20/21) has NO NAMED PAPER.** Its "appears in" column points at a *"memory-architecture paper"* that does not exist in the portfolio (V1/V2/V3/V5 + ICLR long + Nature-MI). Until the Hub decides where it lands, those rows are consistency-guards, not drafting targets.
> 3. **N66 (KT) is transcribed from the task record, not from a spoke report.** No `kt-csf3-tranche` output file exists in `.claude/outputs/`; I saw neither a report nor the raw `reduced_xy.json`. **Its numbers are second-hand — verify against run artifacts before any draft use.**
> 4. **A ledger Chapter 8 (the addressable-memory thesis) is recommended but NOT opened** — new chapters are a Hub structural decision. Scattering w19 across Chapters 1–5 (as my addendum necessarily does) loses the fact that it is *one* result.

---

## What I changed, per document

### 1. `negative_results.md` — pending block DISCHARGED
- **N61–N65 registered and tiered** from the Hub's candidates, written to full schema (tried · exact numbers · mechanism · scope · tier · vertical · source), plus **summary-index rows**.
  - **N61** (A) gradient cross-basin address search dead — 4 protocols, 2 agents. **N62** (A) the BPTT objection inverted. **N63** (A) the `dt` units bug. **N64** (A) common-mode mass runaway + mass-blind EBM term. **N65** (B) four retired positioning claims.
- **N66 added** (B) — the shelved KT `winding2d` tranche (Item 5): 1 usable seed of 3, CPU fallback (CUDA 303), **P2a CONFIRMED / P2b FAILED / P2c UNSCORABLE**, `T_KT` 0.898 vs 0.893, **no soft exponent on either arm**, arm (b) already HELD under §7.23.
- **N7 AMENDED in place, not deleted** (explicit task instruction): a dated Update block recording that `log_mass` **does** move (+3.56) on the eval path ⇒ mechanism wrong there, **conclusion (no timescale hierarchy) CONFIRMED**, both proposed mechanisms wrong. ⚠ I scoped the amendment explicitly to the eval path and wrote the guard *"do not quote 'N7 is wrong' flatly."*
- **W2 added** (watch item, not a negative) — the 8-item ceiling as a 2-D-ring artifact, **plus the three capacity regimes**, with the d-dimensional scaling test as its promote-to-negative trigger.
- **NEW section `## ⚠ OPEN RECONCILIATIONS — wave-19`** with **R19-1…R19-4**, recording the conflicts *as conflicts*: the two persistence gates; the reinstated single-basin collapse; the N7/D1/CM-5 amendment scope; the "start arbitrary and restructure" tension. **I adjudicated none of them.**
- **Paper-writer mining notes:** new memory-architecture appendix family + two new cross-thread patterns. **Provenance flags:** tier rationale for N61–N66/W2, the non-adjudication statement, N66's second-hand provenance, N65's inherited confidence labels.
- **Pending block:** header replaced with a **DISCHARGED** note mapping every candidate → its final entry; **original text retained struck-through as provenance (C-9)**. Top-of-file maintenance note updated.

### 2. `claims_matrix.md` — v1.9 → **v2.0**, all 8 owed items applied
- **New rows: CM-18** (the loop works *under design*, with the three mandatory qualifiers written into the *claim wording* so they cannot be dropped in transcription), **CM-19** (Prop 2 hard read isolation + Props 1/3 as qualifiers), **CM-20** (three capacity regimes + the do-not-quote-8 guard), **CM-21** (the four retired positioning claims, with citations and **approved replacement wording**).
- **CM-5 amended** — frozen-mass/null-direction wordings now explicitly **FORBIDDEN on the eval path**; conclusion confirmed; scope + `dt` entanglement flagged.
- **CM-16a/b sharpened** — SDR = prerequisite, the finite-λ hard limit (mode-(a) latches cannot come from any soft regularizer), the `dt` gate, and the **"proposal under test"** label on the resolution.
- **CM-3 gains a new instance** — all FD00x numbers provisional; plus the "energy read-out is a kinetic read-out" corollary.
- **§1 canonical constants:** two new rows — the **K-dependent D1 partition** (`1/11` marked ⛔ wrong) and the **per-address-mass** consequence — so downstream derivations inherit the corrections.
- **Owed block:** discharged with an item-by-item disposition map; original retained struck.

### 3. `HEP_primers.md` — new **Part XI**, plus an in-place §2.3 correction
- **Part XI — *The addressable memory: write, address, retrieve*** (§§11.1–11.8), house style throughout (concept → math → → CLU → status tag → reading pointers): the formal object; **the loop as demonstrated**; **Prop 2** (+1/3); **Props 4 & 5** with the candidate no-go; **Prop 6** + the corrected D1 partition; **the three capacity regimes**; the retired claims; **the architectural gap + the controller**.
- ⚠ **Appended after Part X deliberately** — `§10.1`–`§10.4` are cross-referenced program-wide; renumbering would break citations. Rationale is stated in the Part's header so no one "fixes" it.
- ⚠ **The designed-vs-emergent boundary is restated in EVERY subsection**, per the task's instruction that it is the single most misquotable thing in the program — including a header warning, and the blank-landscape control presented as *the load-bearing number*.
- **§2.3 (inertial mass) — dated `> **🔴 Update (wave-19)**` blockquote** recording the N7/N64 amendment in place (the section previously taught the frozen-mass mechanism). Original text preserved; the update states the path scope and the `dt` entanglement.
- **Maintenance blockquote** updated with the w19/w20 pass.

### 4. `philosophy-synthesis.md` — full **⟲ Wave-19 addendum**
- **Full addendum, not a `(curator supplement)`** — the Hub wrote a §10 review entry but **no ledger addendum** (last was wave-16). ⟲ protocol respected: **chapters untouched, deltas appended, superseded numbers named**.
- **Chapter deltas:** a new **framing block above all chapters** (the rejected mismatch hypothesis + the architectural gap); **Ch. 2** (mechanism replaced, conclusion re-confirmed); **Ch. 4** (the retry promoted to structural replacement for gradient capture); **Ch. 1/3/5** (retroactive unification; Ch. 3's trash region **blocked by its own vault theorem**); **Ch. 7** (the wave's richest — the *"mechanism wrong, conclusion right"* pattern named, 7 prereg scalps, 5 self-caught instrument defects, independent convergence).
- **Scorecard deltas:** rows 2, 3, 4, 7 moved; **row 8 (addressable memory) proposed but flagged as a Hub call**.
- **Gap-list:** the **top gap changed** (controller ⟶ architectural; real data remains the credibility gap); the I/O gap; the `dt` blocking gate; SDR as prerequisite-with-a-limit; the d-scaling test; the named falsifier; **superseded numbers named**; **six new scope guards (hh)–(mm)**.
- **Doc-hygiene:** the w17/w18 hole; the two places the handover understates its own reports; explicit non-adjudication list; single-seed exposure; the out-of-scope sweep list.

### 5. `future_work.md` — swept (my standing per-wave duty; not in the task's four)
- **Three new sections:** **Memory architecture** (13 boundaries incl. the d-scaling test, the derived-address gate, the controller, the I/O gap, key/value separation, per-launch mass override, distillation, the payload>address falsifier, basin-violation-on-hand-off, the curriculum hazard, untested mass-key arms, un-run staircase repairs, the relativistic scope check); **Positioning & prior-art debts** (7, incl. **3 blocking on novelty claims** — SRNN, Ramsauer Thm 3, the citation-graph sweep); **Engineering debts** (5, incl. the `dt=1.0` re-run that gates all SDR).
- **Status moves:** **Thread-10/KT → `PARKED(Head SHELVED, w20)`** with the w16 core explicitly ring-fenced as untouched; the **random-initial-latent hypothesis DISCONFIRMED → closed**; `clu-horizon-encoder` re-gated.
- **Nothing marked `→ SHOWN`** — deliberate, and stated: w19's positives are **hand-designed with zero learning**, and marking them SHOWN would assert an emergence the program has explicitly not measured.

---

## How I verified
- Read in protocol order: `AGENT_PROTOCOL.md` → handover §10 (all three 2026-07-21 blocks: WAVE-19 review, the vision block, the reframing block) → all four w19 reports **in full** → each transfer doc's existing structure before editing.
- **Every number transcribed from the source report with a citation**, cross-checked against the handover. **Found no output↔handover numeric disagreement.** Two shorthand *understatements* in the handover are corrected in the docs and flagged (not adjudicated): N61's negative is localized to the address selector; N62 is half a positive.
- **Retired-claims sweep, grep-verified** across all five files for `no analogue` · `weighted sum` · `competitor to the transformer` · `escape hatch` · `1/11` · `ceiling of 8` / `8-item` · `prior mismatch` · `masses stay at init`. **Result: zero live assertions.** Every hit is either (a) my own struck/retiring text, (b) the struck superseded blocks, or (c) `escape hatch` at primer §8.4, which is the **unrelated paid-access** usage (licensed-cheat pricing) and correctly left alone.
  - ⚠ **Note on method:** `Grep` over `.claude/` returns nothing because the directory is gitignored; per-file paths are required. Recording this so the next curator doesn't read a false clean sweep.
- The four retired claims **did not pre-exist in any transfer doc** (they originated in the w19 vision block, Hub-owned, already struck there) — so this pass *inoculates* rather than repairs. `research_roadmap.md` is the likeliest home of stale positioning language and was **deliberately not updated by the Hub**; flagged.

## What I deliberately left
- **All four w19 reconciliations UNRESOLVED** — recorded as conflicts (R19-1…R19-4). Adjudication is the Hub's; premature resolution destroys the debugging record.
- **Did not open ledger Chapter 8**, did not renumber primer Parts, did not fill the w17/w18 gap, did not touch `research_roadmap.md`, `brainstorm_log.md`, the handover, any task file, or tracked code.
- **Did not mark anything `→ SHOWN`** in `future_work.md` (rationale above).
- **Did not re-tier any pre-existing negative.** N7 amended only, per instruction.
- **Scope note:** `claims_matrix.md` is not in my standing four docs; the task file assigned it explicitly. It is gitignored, under `.claude/`, and not the handover or a task file — I treated the assignment as valid and am recording the extension here for the Hub's awareness.

## Open questions / risks
1. **The w17/w18 ledger hole is the biggest epistemic risk on the docs side** — two reviewed waves with no addendum, and the program is近 a freeze.
2. **The memory-architecture family has no paper vehicle.** CM-18/19/20/21 and the whole of primer Part XI are currently homeless.
3. **Single-seed exposure on the wave's most quotable numbers** (4.2%, 1.58 bits, the four CAFE arms at seed 42). Mechanisms are seed-independent; the **−0.0021** for tuned+mass10 is within noise and I flagged it against being read as a regression.
4. **N63's fix is untested end-to-end**, and much of N64's mechanism may dissolve at `dt=1.0`. I wrote both entries so they degrade gracefully if that happens rather than needing retraction.
5. **N65's prior art inherits the scout's own gaps** — Ramsauer's constant, UnICORNN, PonderNet, and above all the **unread SRNN initial-state-optimization passage**, which is the most likely preemption of any learned-address novelty claim.

## Proposed handover updates (for the Hub)
1. **Task a w17/w18 ledger + future_work catch-up** (curator), or explicitly declare the waves addendum-exempt and record *that*.
2. **Decide the vehicle for the addressable-memory family** — new short? ICLR-long chapter? — before anything in CM-18/19/20/21 is drafted.
3. **Consider opening ledger Chapter 8** (the addressable-memory thesis); the science is currently split across four chapters because I had no authority to unify it.
4. **Verify N66 against the KT run artifacts**, and confirm the shelving is recorded as the Head's decision rather than a program conclusion.
5. **Adjudicate R19-1** (the two persistence gates) — note it has *two independent* halves: the output↔output disagreement, and the Hub's separate withdrawal of the gate as testing a claim we don't make. **Resolving the second does not resolve the first.**
6. **Re-verify R19-2** (single-basin collapse) before re-promoting — it is reinstated by one measurement at a `dt` that has never been run end-to-end.
7. **Sweep `research_roadmap.md`** for stale positioning (CAFE-anchored spine, transformer-competitor language) — deliberately left unupdated at w19 and now the most likely home of retired claims.
