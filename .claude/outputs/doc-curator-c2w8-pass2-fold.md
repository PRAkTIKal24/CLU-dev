# doc-curator-c2w8-pass2-fold — doc-curator report

**Task + acceptance criterion:** fold C2W8 **pass 2** (closed, merged, green at `main @ 1eda6a0`, 1495/0) into the four transfer docs — the inseparable headline pair, the two attribution corrections folded **in place**, the defect class as standing doctrine, the `M` vacuity qualifier and the count-arithmetic rule. **Status: done.**

## ⛔ FIRST 10 LINES — WHAT I COULD NOT CLOSE, AND WHY (acceptance 5)
1. **The K6 cross-reference slip (task item 7) is ESCALATED, NOT CORRECTED — deliberately, and this is now the FOURTH banking.** Add.9 §A27.2 cites *"K6, §A28.3"*; §A28.3 is the well-lifecycle ruling, K6's home is §A26.3. **The charter is the Advisor's document, outside my four transfer docs, and no Hub instruction authorises the edit** (same reasoning as the pass-1 fold, `claims_matrix.md` precedent). Filed as a **dated STATUS ESCALATION on N237** naming the full banking history + ledger doc-hygiene item 3. ⛔ **It needs an Advisor correction or an explicit curator authorisation — a fifth re-derivation adds nothing.**
2. **THE COUNT ARITHMETIC DOES NOT RECONCILE, and I did not adjudicate it (task item 4).** The task file gives **1445 = shared checkout** and **1438 collected / 1436 selected = fresh worktree**; the §10 entry gives **1436 selected at base** in the verification worktree; but **all three pass-2 spokes independently measured 1445 COLLECTED at `80d7d4b` in DETACHED/FRESH worktrees** (arm A §9, arm B §9, wt3 §4) and all three flagged the banked 1443 as stale by 2. **These cannot all describe the same environment.** ⭐ **I filed the RULE the task asked for (counts are comparable only within one checkout; state the checkout with the number) and the conflict as a conflict** — N273 + ledger doc-hygiene item 1. **No verdict rides on it; 1495/0 is internally consistent.**
3. **Three items are recorded with owner-shaped holes I cannot fill:** the **frozen census's own documentation** (K7's floor + bowl-minimum false positive, `θ_att`'s arm-dependence, the Gaussian-hardcoded own/foreign) — no arm may edit it and it has no owner; **arm B's constant `decode` = 0.2500** (I filed the quotation hold, not an explanation — the census records no per-well decode, so it is not decomposable from the artifact); **`tests/test_cifar_strong_phi.py:66-72`'s stale comment** (the bug is fixed, the comment is not, and it is the pass-1 CIFAR spoke's file).
4. **`monitor #3` (never-refusing admission gate, refusal 0.000) is recorded as STILL OPEN, not closed** — it is instance 2 of the registered defect class and nothing in pass 2 touched it (correctly).
5. **OUT OF SCOPE, declared as such and NOT as gaps:** **pass-3 results** (nothing measured; pass-3 rulings appear only as *dispositions*, labelled) · **`PREREG-C2W8*.md`** (never edited) · **all code** · ⛔ **the arm A vs arm B race — VOID as a comparison and UNADJUDICATED per §A30.1; I recorded no winner and stated in all four docs that the silence is not a null** · any tier-ii / full-CLU / I2 verdict.

---

## What I changed, per document (Hub diff-review list)

### 1. `.claude/negative_results.md` — **N262–N273 registered** (12 entries) + 6 dated blocks + a pass-2 NOT-RUN block
| edit | content |
|---|---|
| **Header fold blockquote** (new, inserted immediately **above** the C2W8 PASS-1 paragraph; pass-1 paragraph byte-untouched) | Pass-2 scope fence (`main @ 1eda6a0`, 1495/0, HEAD-stable, origin frozen, zero worktrees), ⛔ pass-3-out-of-scope, **the inseparable headline pair quoted in full**, all 12 headlines, the amendment list, the NOT-RUN pointer, ✭ tier flag, N-numbering disclosure. |
| **Summary-index rows N262–N273** | 12 rows appended **after the N261 row**, each with tier · vertical · source-report citation. |
| **New section `## Entries — the C2W8 PASS-2 fold`** (inserted after N261's entry, **before** the pass-1 NOT-RUN block) | Section preamble (scope · "nothing re-derived" · the inseparable-pair rule · the VOID race · standing scope statements) + **three full flag-provenance blocks** (arm A / arm B / wt3, commits, seeds, every non-default flag, env, suite figures) + **N262–N273** in full schema (tried · numbers verbatim · mechanism · scope/limits · disposition). |
| **New `## ⛔ DECLARED NOT-RUNs — C2W8 PASS 2` block** | Led by ⛔ **the VOID/unadjudicated arm race** (the item most likely to be misread as a null), then the unbuilt lifecycle + `test_no_merge_verb_exists`, wt3's re-affirmed item-1 NOT-RUN + no full-population `M′` + the uncalibrated payload leg, arm A's cut baseline/seed 8/CLI hook/anisotropic support, arm B's factored store + nothing at `d ≥ 12` (named candidate escape, hypothesis only), and the standing scope statements. |
| **Dated block on N250** (body untouched) | This entry is now **instance 1 of the registered defect class** (3 instances named); ✅ **the `M` half PARTLY DISCHARGED by K9** with its constants, its anti-vacuity/unmeasured-ruler clauses and R1–R3 outcomes; ⛔ the `P` leg, K1 and the merge **verb** not discharged; §A30.1 supplies the missing leg for the other gate. |
| **Dated block on N252** (body untouched) | ✅ the capture half **REVERSED** (all five numbers, at identical bytes) ⇒ ⛔ this entry's numbers are pass 1's operating point and are not the store's standing state; ⛔ the addressability half **NOT** reversed; ⭐ K7 confirms pass 1's zeros were a **measurement**; the pass-1 transcription flags left **unadjudicated** with the Hub's pass-2 monitor-row adjudication recorded (*both citations right, neither complete*). |
| **Dated CORRECTION on N253** (body untouched, nothing deleted) | ✅ what stands (parameter-locality exact; `rate_up_foreign`; 45/48) vs 🔴 the **superseded causal clause**, struck (`~~…~~`) with the replacement numbers (0/48; 0.0012–0.0152 vs 0.611–1.261) and the approved combined form; the diagnostic-not-target caveat and N251 explicitly untouched. |
| **Dated FOLLOW-ON on N254** (body untouched) | REACH thesis gains a **positive** datum at `d = 8`; ⛔⛔ this entry's own `atom_local_radius` clause (and ERRATA-C2W8 §3, §7.24's "by construction") **REFUTED in its strong form**, with the compliance ruling + attached check; the emitting-write candidate escape as **THEORY-ONLY**; C2W9/C2W10 dispositions unchanged. |
| **Dated RESOLUTION on N259** (body untouched) | The `ConvNet` x64 half **CLOSED** at `42b781c` (cause, one-line fix, 5 tests incl. exact `np.array_equal` bit-identity against a reimplementation of the pre-fix code, function-scoped fixture, verified real regression test); ⚠ the **stale comment** and the unbuilt `conftest.py` guard still open/unowned. |
| **Dated STATUS ESCALATION on N237** (body untouched) | The K6 slip at its **fourth** banking, with the full history, the reason the curator again did not edit the charter, and the consequence of a fifth. |

**Tiers proposed (✭, Hub confirms):** **A** — N262 · N263 · N264 · N265 · N267 · N269 · N270; **B** — N266 · N268 · N271; **C** — N272 · N273. *Rationale (the file's standing criterion):* **A** where a program-wide quotation rule follows or a claim/gate moves (the inseparable pair, the gate-blindness doctrine, the two attribution corrections, the instrument caveat that must travel with every capture number, the byte/claim-bar finding, the write→φ design rule); **B** for mechanism-scoping and instrument-debt items; **C** for the two process items (N272 sits in N260's family at C; **N273 is proposed C but is arguably B** — it is a standing measurement rule, not a one-off — flagged rather than decided).

### 2. `.claude/outputs/philosophy-synthesis.md` (the ledger) — **⟲ C2W8 PASS-2 addendum appended**
⭐ **Verified per-file before writing: the Hub wrote NO ⟲ addendum for pass 2** (the only `⟲ C2W8` heading was **PASS-1**, and the file ended at its doc-hygiene item 7) ⇒ **this is pass 2's PRIMARY ledger record, not a `(curator supplement)`.** ⟲ protocol respected: **no chapter body edited, no earlier addendum rewritten, nothing deleted.**
Contents: provenance/scope blockquote (pass-2 fence · pass-3-measured-nothing · the VOID race · the standing scope statements) · **THE WAVE IN ONE ARC (7 beats + the rhyme)** · **chapter-by-chapter deltas** (Ch. 8 gains the retrievability/addressability separation; Ch. 3 gains K9 + the deliberately-unbuilt verb; Ch. 5 gains the positive REACH datum **and** the refuted ERRATA clause; ⛔ Ch. 2 and Ch. 4 explicitly gain **nothing**, stated so silence is not coverage) · **standing practices 25–30** (continuing pass 1's 20–24) · **four exemplars + three self-filed Hub anti-exemplars** · **scorecard deltas** (6 rows; ⛔ **no verdict moves**) · **gap-list updates** (5 SUPERSEDED/CORRECTED forms named + 5 new gaps + 4 carried) · **7 doc-hygiene items** (incl. the unreconciled counts, the **verified-closed** ERRATA §2 collision, the fourth K6 banking, the pass-2/pass-3 boundary, the void race, and the 1 331× vs 1330.8× rounding note).

### 3. `.claude/outputs/HEP_primers.md` — **§11.26 + Records 31–33**, and two dated in-place updates
- **New `## 11.26`** with the pass-2 scope preamble and three records in house style (concept → math → CLU connection → status tag → Def-2 nomenclature → reading pointers): **Record 31 "The wells nobody can find"** (the inseparable pair, with the before/after table and the `σ_q ≈ spacing` reason), **Record 32 "The lever that was not the one we named"** (the 2×2 that took the arm's headline away + the cause correction, with the *"an atom outside its support has identically zero gradient"* line of math), **Record 33 "The exam that cannot ask the question"** (the defect class, the same-cells table, the `exp(−1.0²/2·0.30²) = 4e-3` line, and the conduct note).
- **Dated `> **Update (C2W8 pass 2, 2026-08-09)**` blockquote on Record 28** — registers the record as **instance 1** of the named defect class, names instances 2 and 3, and records K9's partial discharge. The record body is **not** rewritten.
- **Dated update blockquote on Record 29** — the record whose central fact was **repaired** and whose central **cause** was corrected: five numbered items (capture reversed / cause corrected with the superseded clause marked 🔴 / the unmoved addressability half / the K7 instrument caveats / the refuted `atom_local_radius` clause). ⛔ **The superseded wording survives only inside the correction, quoted in order to correct it** (the pass-1 fold's precedent for that treatment).

### 4. `.claude/future_work.md` — **C2W8 pass-2 fold block** (8 status moves + 11 new boundaries)
- **Status moves (provenance kept, nothing deleted):** the reach fix → **◐ SHOWN IN PART** (site-side solved, **query-side now the binding item**) · the own-well-dominance write → **→ SHOWN (both arms)** with its **cause corrected** and its diagnostic caveat intact · "a K1 that can actually fail" → **still OPEN**, with the class named and K9 as partial discharge · the `R_cert`-commensurability item → **◐ ADDRESSED BY RE-REGISTRATION, not by measurement** (with wt3's stated assumption and the note that arm A's landing moves `R_cert` on its own) · learned `p₀`/wormholes → **TASKED (C2W9) unchanged**, premise strengthened, one clause **refuted** · the matched-bytes launder → **OPEN**, fence widened to 1 253× / 1 331× / 81.7× · "does the settle ever beat arg-min" → **OPEN, same question, different blocker** · consolidate/trash + depth threshold + deferred components → **all three unchanged**, with the Head-reserved build decision recorded as reserved.
- **Eleven new boundaries, schema-complete** (what-not-shown · numbers · why-it-matters · where-it-lands · status): **G-ADDR** `TASKED (pass 3)` · **the D2a counterfactual on drift** `TASKED (pass 3)` · **an operating point where `σ_q` < key spacing** `TASKED (pass 3)` · **anisotropic atom support** (machinery exists, unwired) · **a trained shared vocabulary** (interface machine-checked, training NOT-RUN) · **an emitting write at `d ≥ 12`** `THEORY-ONLY` · **per-well decode in the census** · **a full-population `M′` + a non-degenerate payload population** · **the census's own documentation** (three caveats, unowned) · **an arm-independent `θ_att`/`P`** · **a re-registered `attribution_margin_penalty`**.

---

## How I verified — every negative sweep with its POSITIVE CONTROL's hit count (acceptance 4)
⛔ All sweeps **per-file** (`Grep` on a named path), per the `[C2W6-CLOSE-FINAL]` standing rule and the task's ⛔⛔ hazard note. **No directory-scoped sweep was run; no "no hits" claim below stands alone.**

| # | file | negative sweep | result | **positive control (same file, same tool)** |
|---|---|---|---|---|
| 1 | `negative_results.md` | `wendland\|atom_site_local_init\|emission_head\|46/48\|58/62` (is pass 2 already folded?) | **0 hits** ⇒ nothing of pass 2 was present | `capture_radius` on the same file → **6 hits** ⇒ sweep live |
| 2 | `future_work.md` | `wendland\|atom_site_local_init\|emission head\|emission_head\|arm B` | **1 hit, and it is a FALSE POSITIVE** — verified with `-o`: the match is `arm B` inside *"upper-bounds the physics **arm B**Y CONSTRUCTION"* (line 570, a C2W7 item) ⇒ **no pass-2 content** | `capture` on the same file → **5 hits** ⇒ sweep live (and the false positive is itself a positive control on the regex) |
| 3 | `HEP_primers.md` | `PASS-2\|pass 2\|arm A\|arm B\|wendland\|emission` | **3 hits, ALL unrelated** — read individually: line 915 (F5 erase-scope, "re-**emission** (S2)"), 922 (same), 1257 (horizon thermodynamics, "re-emission") ⇒ **no pass-2 content** | the same query returned those 3 hits ⇒ live |
| 4 | `philosophy-synthesis.md` | `pass 2\|pass-2\|PASS 2\|PASS-2` **before editing** | **7 hits, ALL forward references** from the pass-1 addendum (its scope fence, its "the pass-2 fold is owed" line, the byte-fence line, doc-hygiene 5) + one unrelated tier-iii "pass 1 / pass 2" line at 2236 ⇒ ⭐ **no `⟲ …PASS-2` heading exists** | `^# ⟲` on the same file → **30+ headings, ending at the C2W8 PASS-1 addendum** ⇒ **the Hub wrote no pass-2 addendum**; the headings are the control |
| 5 | `ERRATA-C2W8-PASS2.md` | *(verification, not a negative sweep — task item 5)* `^#{1,3} ` | headings read **§1 · §2 · §5 · §3 · §4**, and §5 carries the Hub's dated renumbering banner naming wt3's hash-stable code citations | ⇒ ✅ **item 4 of the directive is CONFIRMED CLOSED ON DISK; I did not re-resolve it** |

**Post-edit integrity checks (all per-file):** `^### N26[2-9] —\|^### N27[0-3] —` → **exactly 12 headings, in order, no gap, no duplicate** (2825…2917) · `^\| N2[67][0-9] \|` index rows → **14** = N260 + N261 + **12 new** · `^### N2[67][0-9] —` entry headings → **14** (same two + 12) ⇒ **index and entries agree** · the five dated-block markers dated `2026-08-09` → **6 hits** = N250 · N252 · N253 · N254 · N259 · N237 · `⟲ C2W8` in the ledger → **3** (pass-1 heading, pass-2 heading, one in-text reference) · `Record 31\|Record 32\|Record 33\|11.26` in the primer → **8** · the pass-2 fold header in `future_work.md` → **1**.

## Flag provenance / discipline
- ⛔ **I produced no numbers.** Every figure is **transcribed** from `c2w8p2-{compact-atoms,emission-head,instruments-and-debt}.md` (+ `ERRATA-C2W8-PASS2.md`, `ERRATA-ARMB.md`), the `[C2W8-PASS2]` §10 entry, and charter **Add.10 §A29–§A30**, each carrying its report+section citation. **Nothing was re-derived, re-scored or re-adjudicated.** ⭐ **The addressability numbers (58/62/62 of 64 · 15/15/14 of 16 · −0.354) are the ADVISOR's, computed from raw `capture_armA.json` and foregrounded in neither arm's report — transcribed and labelled as such at every site.** Full flag-provenance blocks for all three spokes are in the registry section (commits `ebe0592` / `4f92d7a` / `c13f953`+`42b781c`, seeds, every non-default flag, main-venv-reuse, per-run wall times).
- ⛔ **No verdict reinterpreted, and two conflicts transcribed rather than resolved:** the **test-count** conflict (§first-10-lines item 2) and the **1 331× vs 1330.8×** rounding of the same ratio. The arm race is recorded as **VOID/unadjudicated** in all four docs, in the Head's own words.
- ⛔ **The two attribution corrections were folded IN PLACE, STRUCK AND ANNOTATED, NOTHING DELETED** (acceptance 1): N253's causal clause is struck with `~~…~~` and replaced beside the untouched body; N254's `atom_local_radius` clause likewise; primer Record 29 carries both as a dated update with the superseded wording quoted **only inside the correction**.
- **Nomenclature (Def-2) applied:** `s` = atom/well width, `R = 2.5 s` = support radius, `σ_q` = query jitter, `r_cap` = capture radius, `θ_att` = depth floor, `λ_min` = smallest Hessian eigenvalue at the **relaxed** site, `α` = confinement coefficient, `φ` = read-in map — ⛔ **none called a mass**; each new primer record carries an explicit *"neither inertial $M$ nor spectral $\mu$ enters this record"* line (both arms ran `newtonian_learned` and neither varied a mass axis).
- **Status tags** follow the ledger's two-layer scheme; `future_work` items use {OPEN / THEORY-ONLY / TASKED / PARKED / →SHOWN} with provenance kept on every move (two moves this pass are **◐ partial**, and both say which half moved).
- **Git footprint: EMPTY.** No worktree, no branch, no commit, no tracked file touched. All four edited files are under `.claude/` (gitignored). ⛔ No `PREREG-*.md`, no `ERRATA-*.md`, no task file, no handover doc, no spoke-report **body**, no charter.

## Open questions / risks for the Hub
1. ⚠⚠ **THE COUNT CONFLICT NEEDS AN OWNER, NOT A THIRD FIGURE.** Three sources disagree about what `80d7d4b` collects and in which environment (first-10-lines item 2). **The rule is filed; the reconciliation is not mine.**
2. ⚠ **Tier consistency:** **N273 is proposed C** (process family, beside N260/N272) but it is a **standing measurement rule**, which in this file has historically earned **B**. **Pick one convention** — the same flag the pass-1 fold raised about N259 vs N249, still unresolved.
3. ⚠ **N262 is partly a POSITIVE filed in the negatives registry** (the capture repair), on the N109/N241/N261 precedent, **because its second half is what will be dropped in quotation.** Curator-initiated; **flag for ratification.** If the Hub prefers, it splits cleanly into a positive (capture) + a negative (addressability) — ⛔ **but I recommend against it: the task's binding instruction is that the two halves never travel apart, and one entry is the cheapest enforcement.**
4. ⚠ **N265/N253: I recorded a CAUSE correction, not a retraction.** If the Hub reads pass 1's §1(e)/(f) as *retracted* rather than *symptom-right/cause-wrong*, **N253's dated block is the paragraph to sharpen** — the numbers are unchanged either way.
5. ⚠ **Concurrency:** other folds may be live. **N-numbers taken after the then-maximum N261 and verified collision-free per-file at end of pass; no other fold's rows were read-modified.** The Hub should still diff in-flight folds together (standing C2W6-fold-2 / C2W7 precedent).
6. ⛔ **Do not read this fold's silence as coverage of pass 3.** Every artifact this pass wrote carries a *PASS 2 ONLY* fence, and pass-3 rulings appear only as labelled dispositions.

## Proposed handover updates (for the Hub)
- **§10 / relay:** ⛔⛔ **the K6 §A27.2 pointer slip is at its FOURTH banking and is still charter-side and unowned** — it needs an Advisor routing **or** an explicit instruction authorising a curator edit. The registry now carries the full banking history so it is not re-derived a fifth time.
- **§7 (Known Issues), three items the registries now carry and that belong in the Hub's own list:** (a) **the frozen census's three instrument caveats** (capture-radius floor + bowl-minimum false positive; `θ_att` arm-dependence; Gaussian-hardcoded `own_foreign_site_depth`) — **no arm may document them inside the instrument, and the instrument's documentation has no owner**; (b) **`tests/test_cifar_strong_phi.py:66-72`'s stale comment** (the x64 bug it describes is fixed at `42b781c`); (c) **the `conftest.py` x64 guard is still unbuilt** (the bill now stands at five).
- **§7.24 needs the WEAKER form, not the stronger one** (this reverses the pass-1 fold's proposal): `atom_local_radius` is **not** unusable for φ-addressed streams *by construction* — applied **at admission** it is usable and load-bearing (**N266**). ⛔ **The pass-1 proposal to strengthen §7.24 must not be actioned as written.**
- **Quotation rules now live in the registries and should propagate to any draft:** ⛔ **the inseparable capture/addressability pair (N262)** · ⛔ **never *"compact atoms made the store capture"* (N264)** · ⛔ **the write-manufactured-interference correction on N253 (N265)** · ⛔ **the quotation HOLD on arm B's `decode` = 0.2500 (N268)** · ⛔ **`NO_TIER_II_CLAIM` travels with every arm-B number including the 15.34× (N269)** · ⛔ **no performance number at 1 253× / 1 331×** · ⛔ **the arm race is VOID and unadjudicated, and its absence is not a null.**
- **A standing-practice note for the next scoping entry:** practices **25–30** are numbered to continue pass 1's 20–24 in the ledger; **25 (*a gate that cannot fail on the thing that matters*) and 28 (*a metric that goes to zero because the system became degenerate is not a win*) are the two most likely to be needed at pass-3 review.**
