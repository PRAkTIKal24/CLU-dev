# doc-curator-c2w8-pass1-fold — doc-curator report

**Task + acceptance criterion:** fold C2W8 **pass 1** (closed/merged/green) into the four transfer docs and file rider 3b's `cl-encoder` amendment. **Status: done.**

## ⛔ FIRST 10 LINES — WHAT I COULD NOT CLOSE, AND WHY (acceptance 4)
1. **The K6 cross-reference slip (item 4) is FLAGGED, NOT CORRECTED — deliberately.** Add.9 §A27.2 cites *"K6, §A28.3"*; §A28.3 is the well-lifecycle ruling, K6's home is §A26.3. **The charter is the Advisor's document and outside my four transfer docs** (no explicit Hub instruction to edit it, unlike the fold-2 `claims_matrix.md` precedent) ⇒ filed as a **dated FLAGGED REGISTRY NOTE with its diagnosis on N237** + ledger doc-hygiene item 3. ⚠ **Now banked at three consecutive Hub sessions; it needs an owner, not a fourth banking.**
2. **Charter §A21's "we hold a laundered win" wording (C2W8 scoping decision 1) — NOT touched.** Same reason (charter = Advisor's; the Head ruled it non-blocking and said *a later wave's doc-curator may carry the charter-side fix*, which is a charter edit I am not scoped for). ⚠ **Advisor confirmation is still pending, not obtained.** The *correct* form (launder margin −0.036 is the headline quantity) is carried in the registry/ledger regardless.
3. **Two number-level discrepancies TRANSCRIBED, NOT ADJUDICATED** (curator does not resolve report-vs-Hub conflicts): **(a)** the tripping-monitor row number — report says **#12 `starvation` / `vacuous_gate`** (cites #9/#12), the pass-2 scoping entry says **"monitor #3"**; the refusal rate **0.000 agrees**. **(b)** site drift's low end — report **0.22**–1.47, Hub's own re-derivation **0.003** (seed 0), Hub-declared not decision-grade. Both filed as published, in N252 and ledger doc-hygiene item 1.
4. **`cl_baselines.ConvNet`'s x64 defect has no owner** — registered (N259) with the fix's size, not assigned.
5. **OUT OF SCOPE, declared as such, not as gaps:** the **pass-2 fold** (wt1/wt2/wt3 measured nothing at fold time) · `PREREG-C2W8.md` / `PREREG-C2W8-PASS2.md` (never edited) · all code · C2W6/C2W7 in-flight curator targets · `claims_matrix.md` (not in my four, no instruction).

---

## What I changed, per document (Hub diff-review list)

### 1. `.claude/negative_results.md` — **N250–N261 registered** (12 entries) + 3 dated blocks + a NOT-RUN block
| edit | content |
|---|---|
| **Header fold paragraph** (new blockquote after the `Maintained by` block) | C2W8 pass-1 fold paragraph: scope (**PASS 1 ONLY**, `main @ 80d7d4b`, 1443/0 HEAD-stable), the central finding (gate licensed by neither leg), the program-wide relabel, 12 headlines, the amendment list, ✬ tier flag, N-numbering disclosure. |
| **Summary-index rows N250–N261** | 12 rows appended **after N249** (⚠ I first inserted them before N249 and immediately corrected the ordering with two follow-up edits — the N249 row is byte-identical to the original; **please diff it**). |
| **New section `## Entries — the C2W8 PASS-1 fold`** (after N249's entry, before the READ-ITERATION-1 NOT-RUN block) | Section preamble with **both spokes' full flag-provenance blocks** + the pass-1/pass-2 fence + the CM-23(q) travel rule, then **N250–N261** in full schema (tried · numbers verbatim · mechanism · scope/limits · disposition). |
| **New `## ⛔ DECLARED NOT-RUNs — C2W8 PASS 1` block** | Lifecycle NOT-RUNs (I2 correlation test → C2W10, cross-stream criterion, tier-ii/full-CLU/I2 verdicts, CSF3, **stage 2 §2.2–2.7 = DECLARED CUT WITH CAUSE**) + the six CIFAR NOT-RUNs + ⛔ **the CANCELLED `addr_dim = 16` confirmation cell, flagged as the easiest to misread as a null.** |
| **Dated block on N121** (body untouched) | rider 3b: the `convae` **0.238** / *"never beat the untrained trunk"* row is **read-out-scoped**; approved form; ⛔ do not invert. Everything else in N121 explicitly unmoved. |
| **Dated block on N237** (body untouched) | the **K6 §A27.2 → §A28.3 pointer slip**, filed as a flagged registry note with diagnosis, owner and the reason the curator did not edit the charter. |
| **Dated block on N246** (body untouched) | what C2W8 pass 1 **did** ship from the lifecycle it was handed (γ_φ inside `CluSystem`, OFF) and what it **did not** (the verbs; the adoption test; ⚠ the depth-threshold decision **still never fired**). |

**Tiers proposed (✬, Hub confirms):** A — N250 · N251 · N252 · N253 · N254 · N255 · N256 · N257 · N258; B — N261; C — N259 · N260. *Rationale (the file's standing criterion):* **A** where a program-wide quotation rule follows or a banked entry/claim-gate moves (the relabel, the gate's re-registration requirement, the re-price's provenance sentence, the laundering flagship, the forgetting HOLD, the `cl-encoder` table correction, the `phi_dim ≥ 16` re-pricing); **B** for the shipped-but-scope-fenced positive (N261, the N109 positive-in-the-negatives-registry precedent); **C** for the two process/engineering items (N259 sits in the N72/N211 family at C — ⚠ **N249 was proposed at B for the same family, so the Hub may want these consistent**).

### 2. `.claude/outputs/philosophy-synthesis.md` (the ledger) — **⟲ C2W8 PASS-1 addendum appended**
⭐ **Verified per-file before writing: the Hub wrote NO ⟲ addendum for C2W8** (the file ended at the READ-ITERATION-1 catch-up addendum's doc-hygiene item 5) ⇒ **this is C2W8's PRIMARY ledger record, not a `(curator supplement)`.** ⟲ protocol respected: **no chapter body edited, no earlier addendum rewritten, nothing deleted.**
Contents: provenance/scope blockquote (incl. the PASS-1 fence and the "no full-CLU/tier-ii/I2 verdict" statements) · **THE WAVE IN ONE ARC (7 beats + the rhyme)** · **chapter-by-chapter deltas** (Ch. 8 gains a clause; Ch. 3 gains the trash region's landing + the compact-gate doctrine; Ch. 5 is **re-priced**, not moved; ⛔ Ch. 2 and Ch. 4 explicitly gain **nothing**, stated so silence is not read as coverage) · **standing practices 20–24** (numbered to continue C2W7's 15–19 and avoid the 15′–17′ / 15–18 collisions) · **3 exemplars + 2 self-filed Hub anti-exemplars** · **scorecard deltas** (6 rows; ⛔ **no verdict moves**) · **gap-list updates** (4 SUPERSEDED forms named + 3 new gaps + 3 carried) · **7 doc-hygiene items.**

### 3. `.claude/outputs/HEP_primers.md` — **§11.25 + Records 28–30**, and one dated in-place correction
- **New `## 11.25`** with the wave's scope preamble and three records in house style (concept → math → CLU connection → status tag → Def-2 nomenclature → reading pointers): **Record 28 "The gate that could not fail"** (the two forced-false conjuncts, with the `θ_att` degeneracy written as one line of math; plus the mislabel), **Record 29 "The wells nobody can knock on"** (curvature ≠ basin; parameter- vs function-space locality; the `exp(−r²/2s²)` underflow ladder), **Record 30 "The better the address, the less the physics adds"** (the re-price, the ±0.0007 same-keys tie, the byte ledger, the packing-slack reason).
- **Dated `> Update (C2W8 pass 1)` blockquote on §11.9's wave-26 block** — the one primer site that carried *"reconstruction fitting −0.006 (bought nothing — `convae` never beat the untrained trunk)"* as an objective statement. The block is **corrected in place with the reason** (primer is pedagogical, not append-only), and it also records that the CLU settle has now actually been run at that φ. ⛔ **The forbidden wording survives in the primer only inside this correction, quoted in order to correct it** (the C2W7 fold's precedent for that treatment).

### 4. `.claude/future_work.md` — **C2W8 pass-1 fold block** (7 status moves + 9 new boundaries)
- **Status moves (provenance kept, nothing deleted):** consolidate/trash `TASKED (C2W8)` → **◐ SHOWN IN PART** (plumbing shipped OFF; verbs cut) · the trash region's **depth-threshold decision** → **still OPEN, carried a second wave, and the risk it named ("easy to inherit as tested") is now realised on the record** · the deferred-components row → C2W8 dated & partly discharged, **C2W9 gains the reach scoping input**, C2W10 re-priced · ⭐ **the w26 `PARKED (registered-and-declined)` Split-CIFAR entry → `→ SHOWN (C2W8 pass 1)`, with w26's pre-registration accurate on every clause** · *"is there ANY read where the settle beats arg-min?"* → **still OPEN and sharper** · the `apply_cifar10` clobber trap → **✅ CLOSED** · the strict-φ/20 k items → **OPEN, now priced**.
- **Nine new boundaries, schema-complete** (what-not-shown · why-it-matters · where-it-lands · status): a **K1 that can actually fail** · an **operating point where `R_cert` is commensurate with the key spacing** · a **write that makes an item's own well dominant at its own site** (with the ⚠ own/foreign-is-a-diagnostic caveat binding) · a **reach fix for wells that exist but cannot be knocked on** · **learned `p₀`/wormholes** `TASKED (C2W9, scoping input)` · ⭐ **why our class-balanced eviction loses 0.014 to a plain ring buffer** (the cheapest live route to N8) · the **matched-bytes forgetting frontier at strong φ** (a HOLD) · **`cl-encoder` §3 re-run at the measured read-out** · a **matched-BYTES launder on the lifecycle rig** (quotation-fenced at 1 253×).

### 5. `.claude/outputs/cl-encoder.md` — **rider 3b: dated curator amendment banner** (⚠ scope disclosure)
A dated banner **at the top, body untouched, nothing deleted** (C-3 precedent, the instrument the Hub ratified for `pilot-placement-probe.md`). It states what §3 says, what was measured at the chosen read-out (**convae CLU 0.267 ± 0.006 / kNN-ring 0.277 ± 0.003 vs randconv 0.213 ± 0.006 / 0.235 ± 0.005 = +0.054**, N7 **+0.1063 ± 0.0038 paired, 3/3**), the **never-quote line**, ⛔ **the explicit refusal to restate it as "reconstruction helps"**, the approved form, and what the banner does **not** touch (§2 gate values, §4's sweep, §5's **unamended** +0.020 ± 0.010, §7's geometry, and §4's own selection-on-the-decision-metric caveat, which makes `convae`'s hit conservative).

---

## How I verified — every negative sweep with its POSITIVE CONTROL's hit count (acceptance 3)
⛔ All sweeps **per-file** (`Grep` on a named path), per the `[C2W6-CLOSE-FINAL]` standing rule. **No directory-scoped sweep was run, and no "no hits" claim below stands alone.**

| # | file | negative sweep | result | **positive control (same file, same tool)** |
|---|---|---|---|---|
| 1 | `HEP_primers.md` | `C2W8` (is the wave already folded?) | **0 hits** | `C2W7\|C2W6` → **19 hits** ⇒ sweep live |
| 2 | `HEP_primers.md` | `ceases to exist as an attractor\|drives depth to ZERO\|erosion drives depth` | **0 hits** ⇒ the erosion mechanic's wording is **not** in the primer, so the relabel needs no in-place strike there | same file returned 19 hits on control 1 and 7 on `Split-CIFAR\|CIFAR` ⇒ live |
| 3 | `HEP_primers.md` | `reconstruction bought nothing\|convae\|randconv` | **1 real site (§11.9 wave-26 block, line 1637)** — corrected in place | (positive by construction: the sweep returned hits) |
| 4 | `negative_results.md` | `population_eroded\|eroded_not_attractor\|erosion drives depth` | **0 hits** ⇒ the mislabel was never propagated into the registry | `reconstruction bought nothing\|0.238\|convae` on the same file → **17 hits** ⇒ live |
| 5 | `negative_results.md` | `convae` | **2 sites, both in N121** — dated block appended | — |
| 6 | `future_work.md` | `reconstruction bought nothing\|0.238\|convae` | **0 hits** ⇒ rider 3b needs no propagation there | `matched-bytes\|forgetting frontier\|BWT` → hits at 8 lines; `CIFAR` → **22 hits** ⇒ live |
| 7 | `philosophy-synthesis.md` | `convae\|population_eroded\|eroded_not_attractor` | **0 hits**; `bought nothing` → **4 hits, ALL unrelated** — each read individually (lines 211 / 219 / 499 / 1198: all the **N1 squeeze-retry** result, Ch. 4/V1) | the same query returned those 4 hits ⇒ live |
| 8 | `philosophy-synthesis.md` | `C2W8` before editing | 9 hits, **all forward references** (`TASKED (C2W8)`, "C2W8/C2W10" homes) — ⭐ **no `⟲ C2W8` heading** ⇒ **the Hub wrote no addendum** | (the 9 hits are the control) |

**Post-edit integrity checks (all per-file):** `^### N2(4x|5x|6x) —` → **exactly one heading each for N240–N261, no gap, no duplicate** (22 headings, ordered) · index rows `N248\|N249\|N250–N261` → **14 rows = 1 + 1 + 12**, so exactly one `N249` row survives the ordering fix · `⟲ C2W8` in the ledger → **1** · `Record 28\|29\|30\|11.25` in the primer → **9** · `N250\|N261` in `future_work.md` → **4**.

## Flag provenance / discipline
- ⛔ **I produced no numbers.** Every figure is **transcribed** from `c2w8-well-lifecycle.md` (+ `ERRATA-C2W8.md`, `census.json`), `c2w8-cifar-strong-phi.md`, and the two `[C2W8]` §10 entries, each carrying its report+section citation in the registry entries. **Nothing was re-derived, re-scored or re-adjudicated.** Where the Hub re-derived a form from raw independently of the report, **the Hub's figure is the one transcribed and is labelled as such** (Head ruling Q2: canonical = **median**; own **0.518/0.282/0.123**, foreign **1.261/0.947/0.611**).
- ⛔ **No verdict reinterpreted.** `stage2_unlock = true` is recorded as *"mechanically correct and licensed by neither leg"* — the Hub's words, not a curator judgement; the §2.2–2.7 cut is recorded as **the engineer's call, Hub-ratified**.
- **Nomenclature (Def-2) applied:** `θ_att` = depth floor, `r_cap` = capture radius, `s` = atom/well width, `σ_q` = query jitter, `R_cert` = certificate radius, `φ` = read-in map — ⛔ **none called a mass**; where a record could be misread, an explicit *"neither inertial $M$ nor spectral $\mu$ enters this record"* line is included.
- **Status tags** follow the ledger's two-layer scheme; `future_work` items use {OPEN / THEORY-ONLY / TASKED / PARKED / →SHOWN} with provenance kept on every move.
- **Git footprint: EMPTY.** No worktree, no branch, no commit, no tracked file touched. All five edited files are under `.claude/` (gitignored). ⛔ No `PREREG-*.md`, no `ERRATA-*.md`, no task file, no handover doc, no spoke-report **body**.

## Open questions / risks for the Hub
1. ⚠ **Tier consistency:** N259 is proposed **C** in the N72/N211 silent-hazard family, but **N249 was proposed B** in that same family. **Pick one convention.**
2. ⚠ **N261 is a POSITIVE filed in the negatives registry** (N109/N241 precedent) because its **scope clause** — shipped **OFF**, verbs cut, adoption never tested — is what will be dropped in quotation. **Curator-initiated; flag for ratification.**
3. ⚠ **N254 folds a pass-2-scoping Head ruling (Q1) into a pass-1 entry.** I judged the *finding* (co-scaling honoured ⇒ reach-bound) to be pass-1 evidence (ERRATA §3 + `clu_system.py:314`) and the *ruling* to be its disposition — recorded as a **DECLARED NOT-RUN, never a null**, with its scratch-probe provenance attached. **If the Hub considers this pass-2 territory, N254's disposition paragraph is the block to strike; the numbers are pass-1 either way.**
4. ⚠ **Concurrency:** other folds may be live. **N-numbers taken after the then-maximum N249 and verified collision-free per-file at end of pass; no other fold's rows were read-modified.** The Hub should still diff all in-flight folds together (the standing C2W6-fold-2 / C2W7 precedent).
5. ⚠ **The index-ordering repair** (N249 moved back above N250) touched one pre-existing line by re-inserting it verbatim — **worth one glance in the diff.**
6. ⛔ **The pass-2 fold is owed as a separate later pass.** Every artifact this pass wrote carries a *PASS 1 ONLY* fence; **do not read this fold's silence as coverage of pass 2.**

## Proposed handover updates (for the Hub)
- **§10 / relay:** the **K6 §A27.2 pointer slip** and the **§A21 "laundered win" wording** are **both charter-side and both still unowned after three sessions** — they need an Advisor routing, not another banking. The registry now carries both diagnoses so neither is re-derived.
- **§7 (Known Issues):** `cl_baselines.ConvNet` under `jax_enable_x64` (N259) — one-line fix, no owner, `backbone="cnn"` untestable in the suite; and the **fourth bill** for the never-built `conftest.py` x64 guard.
- **§7.24 needs the stronger form** the engineer measured: `atom_local_radius` is not merely dead in the `LearnedVStore` path — it is **unusable for φ-addressed streams by construction** (per-group targets are needed at **init**).
- **Quotation rules now live in the registries and should propagate to any draft:** the `population_eroded_not_attractor` relabel (N251) · the mandatory Split-CIFAR provenance sentence (N255) · the `cl-encoder` §3 never-quote (N258) · the CIFAR-forgetting HOLD (N257) · ⛔ no performance number at the **1 253×** ratio (N252).
