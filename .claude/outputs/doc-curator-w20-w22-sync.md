# doc-curator-w20-w22-sync — doc-curator report

Task + acceptance criterion: run the owed transfer-doc sync for WAVES 20–22 — register the w20–w22 negatives, write the three missing ledger addenda (incl. the Head's performance-first rescoring), touch the primers only where physics-onboarding needs it, and propagate the four corrections (19×→2.1× · "CLU extrapolates" scoped · K=8 wall = geometry · mass stores nothing) through the curator's documents.
**Status: done.** 4 files edited (`negative_results.md`, `philosophy-synthesis.md`, `HEP_primers.md`, `future_work.md`). **No tracked code touched. No git footprint.** `research_roadmap.md`, `handover_context.md`, `claims_matrix.md`, all task files and everything under `chlu/` untouched, per instruction.

> ⚠ **First-10-lines flags (protocol §5):**
> 1. **Tiers on N67–N82 are CURATOR-PROPOSED** (marked as such in the registry header, on every entry, and in a provenance-flag block with the rationale) — the Hub confirms or re-tiers. **The six w17/w18 staged candidates (C17-1…3, C18-1…3) were NOT touched and NOT tiered**, per instruction.
> 2. ~~**`claims_matrix.md` was NOT updated** — not in my standing four and not assigned this pass~~ → **DISCHARGED same session:** the Hub assigned it; **matrix synced to v2.1** — see the dated section appended at the end of this report.
> 3. **One new reconciliation opened as a conflict, not resolved: R20-1** (ballistic fraction 98.3% vs 79.7%, unreconciled — quote neither digit).
> 4. **W2 is CLEARED, not promoted** — its recorded trigger (the d-scaling test) resolved in CLU's favour at w20; struck-with-provenance per the watch-item protocol (C-9).

---

## How I verified
- Read in protocol order: `AGENT_PROTOCOL.md` → handover §10 (all three review entries: WAVE-20 `2026-07-21 night`, WAVE-21 `2026-07-23`, WAVE-22 `2026-07-23 night`, plus the `2026-07-21 (later)` Head-decisions entry) → my predecessor's `doc-curator-w19-w20-sync.md` → the wave reports (headline/reconciliation/findings sections read in full for: `learned-landscape-write-read`, `mass-visible-objective`, `primitive-harness`, `gamma-read-sweep`, `sequential-write-interference`, `potential-function-class`, `relaxation-fiber-capacity`, `designed-mechanism-learned-content`, `gated-write-performance` incl. §§2–5, `hopfield-capacity-benchmark` incl. §§1–5) → each transfer doc's structure before editing.
- **Every number transcribed carries a report+section citation; cross-checked against the handover. No output↔handover numeric disagreement found in any of the three waves.** One nuance recorded (not adjudicated): the handover rounds the retry lift to "+47pp"; the source digit is **+46.9pp** (`hopfield-capacity-benchmark` §4.1) — the docs carry the source digit.
- **Corrections sweep, grep-verified per-file** (⚠ directory-level `Grep` over `.claude/` still returns nothing — gitignored; per-file paths required, re-confirming the w19 method note): `19×`/`17×` — every occurrence in the four docs is now inside a correction or a do-not-quote guard, zero live assertions; `extrapolat` — only unrelated hits (KT finite-size fits, "HEPA-extrapolated") plus the new scoped guards; `K=8`/`8-item` — all guarded; mass-as-storage — no live assertion anywhere (the `clu-autoencoder` task file's wrong channel table is Hub/engineer-owned and flagged, not edited).
- **Self-caught edit defect:** my §11.3 primer update initially swallowed the `## 11.4` heading (the old-string included the heading's first tokens); caught by the verification grep and repaired — the heading is restored verbatim.

---

## What I changed, per document

### 1. `negative_results.md` — N67–N82 registered; N63/N64 updated; W2 cleared; R20-1 opened
- **New entries, full schema, tiers curator-proposed:**
  - **W20:** **N67** (A — learning destroys the loop: no rung clears 0.9 5-seed; locality 0.000→2.9e-2…5.0e-1; strict 1.000→0.000 after one write; w21 amendments folded — better-family passes rung 1, `local_rbf` was an init artifact) · **N68** (A — the blank-control protocol too weak: nearest-centroid blanks 0.992–1.000 on empty landscapes; strongest-read + value-recovery rule, N51 family) · **N69** (A — 0-of-3 + the Hub's Prop-2 premise falsified as pre-registered, attention drop 0.001 vs CLU 0.137; **the w22 2.1× correction baked into the entry** so §1b's 19× can never be quoted from here) · **N70** (A — `(1+2R/w)^d` falsified in form, β=0.96/1.28/1.65 vs β=d; repair `Δ_req≈3.1·max(w,σ_query)`, `d_eff≈0.72–0.83d`; travels with the `4·2^d` positive + censoring/precision caveats) · **N71** (B — three proven safety guards: naive retry 2/267, the origin attractor 80–100%, the Newton saddle; + `a*=relax(c)` never implemented under learning) · **N72** (C — the x64 import-order landmine 0.906→0.594 + the `dt·ω` no-margin hazard).
  - **W21:** **N73** (A — γ refuted, flat to 0.5σ, no transition at γ*=0.0108, 16/19 preregs; the real cause = the LINEAR write current, conjunction R²0.02–0.09 vs Σv R²=1.000; the 65× gate = catching up; read-mode axis degenerate; trajectory read never helps) · **N74** (A — the gate cannot fire, spacing 1.4142 vs d_safe 1.10; C3 refusal-only strict-B 0.000; retracts the Hub's "~5 orders" adjudication — the 5 orders are the WRITE OPERATOR's, 1.008e-4 reproduced to 26%; + the C3-ratio law over 4.6 decades and the designed-store 1.000-vs-0.16 with the 6.0±0.9/16 price) · **N75** (A — CLU worst of four, 0.16 vs gru 0.57; "wasted compute" refuted, replaced by infeasibility 0.00→1.00 censoring K=8→32; contextual-vs-parametric labels) · **N76** (A — Prop F1: mass SELECTS, never STORES; V↔M exact gauge to 1e-15 for any V; `B_total=K_spatial·B_fiber`; the parameter ceiling `B_total≤P·b_θ`; the w22 third confirmation −0.005±0.007) · **N77** (B — capacity-per-parameter ≈1.1–1.6 vs 2 bits/param; retrieval-cost false + occupied; MQAR Ω(N); + the Titans/UnICORNN/SRNN cite-and-differentiate debts).
  - **W22:** **N78** (A — **the 19× correction, program-wide**: rescued GRU 0.085 ⇒ 2.1× at kv=16; what survives — crossover kv≈4, pre-exists the gate, attention still wins, cost 1.48×/2.16×; Mamba-lite caveat) · **N79** (A — extrapolation falsified: 0.0020→2.01→2.96, worse than the mean, worst of three; **scope "CLU extrapolates" to autonomous/designed only**) · **N80** (A — Prop-2 robustness non-transfer: worse at every σ, slope +0.406 vs +0.242) · **N81** (A — the NN pixel floor beats everyone; benchmark doesn't need memory; noise win vs Hopfield only; **admissibility MNIST-only** (CIFAR closed-form Hopfield at chance for all β; SOTA needs a learned kernel); salvage = retry +46.9pp/−38.3pp + per-item retention; protocol pins `sqdiff`/dropout-mask) · **N82** (B — the K=8 wall is GEOMETRY, H-LEARNING rejected; exponent UNPINNED (1.18/R²0.26 vs 1.64/R²0.94 d≤6); d=8 = optimizer failure near-provably; widening tax ¼→⅛→≤1/16; + the 197×→6332× write-operator amplification as the traveling positive).
- **Summary-index rows for all 16** (ordering defect on first insertion — N66 displaced below N82 — caught and fixed) + a **† tier-flag note** under the table.
- **N63 Update block:** the `dt` fix delivered (`dt=0.125`, 6/6 preregs), **FD001-NEUTRAL** to the digit; **two w19 conclusions reverse** (`H≈V` corr 0.945; spectrum *expands* 1.349); the −0.08 was an integrator artifact, not a finding; "98.3% ballistic" quarantined to R20-1.
- **N64 Update block:** blindness reproduced post-fix (bit-zero, analytic), **fixed at source** (0.00%→42.70%; `M_max/M_min` 4.1051; **τ∝M^0.779, R²=0.9968**; spread 1.27→3.05×), **ships OFF, no task metric measured**; `τ∝√M` retired; softplus trap downgraded (1.089×); the Prop-F1 selection-not-storage scope added.
- **W2 → CLEARED** (header struck, dated clearing block with the `4·2^d` numbers and the three inheritances N70/N82/N77). First watch-item to clear via its recorded trigger.
- **New § Open reconciliations — waves 20–22:** **R20-1** (98.3 vs 79.7 ballistic — quote neither; say "free-streaming-dominated, ≈80–98%, unreconciled") + a carried-discharged note (the kv-γ/rescue caveat and the gate-follow-up debt both discharged by w22 — recorded so nobody re-opens them).
- **Paper-writer notes:** a new **performance-appendix family** (N69+N78 inseparable; N79/N80 as the scope fence; N81's salvage; the Head's zero-benchmarks scoring sentence; a consolidated do-not-quote list) + the **"six Hub claims overturned by agents who checked"** cross-thread pattern. **Provenance flags:** tier rationale; w22 single-seed exposure (all Hopfield numbers = seed 0; the d=8 rescue cells are the thinnest in N82); the 26%-agreement two-implementation status of the drift law.

### 2. `philosophy-synthesis.md` — three full ⟲ addenda (w20, w21, w22)
- The Hub wrote §10 reviews but **no ledger addendum for any of the three waves** → full curator addenda, ⟲ protocol respected (chapters untouched, deltas appended, superseded numbers named). Where a w20 statement was later corrected (the gate adjudication, the γ coda, the 19×), **the correction is named inline in the w20 addendum** so it cannot be quoted stale.
- **W20 addendum:** the designed/learned split (exponential capacity + W2 cleared + N70 repair vs the central negative N67/N68 + the 0-of-3); the Hub's falsified Prop-2 premise (Ch. 7); two-phase ACCEPTED with the three amendments + MVC-0/Props C-N/C-D; the `dt`/mass fix arc with the two reversals; scorecard rows 2/4/5/7/8-candidate; gap list — `dt` gate discharged, controller gap narrowed to "specified, unbuilt", `a*=relax(c)` still untested, Mamba-lite, superseded list.
- **W21 addendum:** the four falsified Hub hypotheses (table) → the ONE convergent mechanism (the write-operator law, C3 ratio, 4.6 decades); the corrected gate statement (refusal; decisive-on-designed with the capacity price); CLU worst-of-four; the linear-write-current cause of the 0-of-3; the fiber corrected (Prop F1, composition, parameter ceiling, the governor-as-gauge-fixer positive); the scout's three dead claims + the one admissible benchmark + Titans/UnICORNN/SRNN; the w17/w18 catch-up acknowledged; scorecard rows 2/4/7/8-candidate.
- **W22 addendum:** **the Head's performance-first rescoring recorded verbatim as binding** (score = external benchmarks won = ZERO; the filing rule; no pre-registered retreat) with a ledger note that it changes *readings*, not measurements; the primitive-decider (geometry, exponent unpinned, d=8 optimizer failure, 197×→6332×); levelling-not-beating + the one surviving edge (2.1×) + the two falsifications; **Ch. 4's first measured retry benefit (+46.9pp/−38.3pp — the w19 "structural role, unmeasured" promotion is now measured, novelty half-discharged)**; the benchmark verdict + admissibility caveat; **the plain-language hilly-landscape re-grounding + the φ/ψ gap restated + the leaky-valley precision**; scorecard rows 4/2/7/8-candidate with filing tiers; gap list (the Head's two threads + the budget re-run; the φ bottleneck; the superseded list; the owed inventory); doc-hygiene (no disagreements; the +46.9 digit; the claims-matrix gap; **the Chapter-8 recommendation, now three waves deep**).

### 3. `HEP_primers.md` — Part XI updated in place + two new subsections (house style)
- **§11.2 update:** the loop does not survive learning (N67/N68) + the "retrieval requires dissipation" row **superseded** by the two-phase result (γ_address only; γ_read free).
- **§11.3 update:** Prop 2 non-transfer, all three axes (recall N69, robustness N80, extrapolation N79) + what Prop 2 remains (designed-store guarantee, the Ramsauer answer, the 2.1×-never-19× capacity support). *(Edit defect here self-caught and repaired — §11.4's heading restored.)*
- **§11.6 update:** designed capacity exponential (`4·2^d`, W2 cleared) with the N70 repair replacing the falsified bound quoted in the section body; the learned wall = geometry/unpinned/d=8-optimizer (N82); the Hu–Wu–Liu + bits-per-parameter positioning discipline.
- **§11.7 update:** the guardrail list grown (19× · extrapolation scope · UnICORNN · MQAR Ω(N) · mass-storage · retrieval-cost · "beat SOTA" · naked fiber bits · graceful-degradation label) + Titans as a must-cite.
- **§11.8 update:** the Head's rescoring + filing rule; **the hilly-landscape plain-language grounding as canonical onboarding material** (with the frozen-surface decay precision); the w22 state-of-the-program paragraph.
- **NEW §11.9 — the write operator** (concept → the C3 law → the gate-can-only-refuse correction → the 197×→6332× amplification → status/pointers/N74-N75-N82).
- **NEW §11.10 — the fiber** (Prop F1 → mass selects, never stores → composition slots×bits → the parameter ceiling → the τ∝M^0.79 selection reading + the third confirmation → status/pointers/N76 + the `clu-autoencoder` warning).
- Maintenance blockquote extended with the pass log. **Nothing outside Part XI changed** (the §2.3 w19 update already carries the mass mechanism; τ∝√M never appeared as primer text).

### 4. `future_work.md` — swept (standing per-wave duty)
- **Marked `→ SHOWN`:** the **d-scaling test** (w20 — with the three inheritances stated inline so the SHOWN cannot be over-read) and the **`dt` end-to-end re-run** (w20 — FD001-neutral, reversals, R20-1 guard). **Partial:** per-launch `mass_override` implemented (codebook still a change-list); softplus debt largely discharged (residual: measure FD001 with the knob ON).
- **New section: "⭐ Performance & the benchmark frontier"** (Head's scoring in the context header): the retry/test-time-compute study · the capacity-edge benchmark that must beat *attention* (with the Ω(N) constraint on task choice) · the dimension-aware-budget re-run · the φ-before-benchmarks gate (cross-ref) · the real-Mamba debt · the 0.87-bar sweep · E3 — plus a **discharged list** (γ sweep, gate test, Hopfield protocol, kv re-check, multiplicative-gate follow-up) so none is re-run.
- **Annotated in place:** the mass-key arms (Prop F1 condition — coupled landscape or momentum launch; selection, never storage); the derived-address gate (codebook-gated retry + no-Newton guards; **`a*=relax(c)` still never implemented — arguably its own cheap experiment**); voraus escalated (shipped defaults changed at w20 — do not quote until re-run); the `conftest.py` x64 fix added.
- **Deliberately NOT marked SHOWN:** the controller (specified ≠ built), the derived-address gate (composition untested), the retry *study* (the +46.9pp is one run inside one benchmark, not the pre-registered accuracy-vs-compute study the entry asks for).
- Tail sweep note appended; nothing deleted (C-9).

---

## What I deliberately left
- **All open reconciliations open** (R19-1…4 untouched; R20-1 opened as a conflict). **Adjudicated nothing.**
- **Did not tier or touch C17-1…3 / C18-1…3**; did not touch the w17/w18 pending block.
- **Did not open ledger Chapter 8** (recommended again — third consecutive pass; Hub structural call).
- **Did not update `claims_matrix.md`** (not assigned; flagged) or `research_roadmap.md` (Hub rewriting this session) or `brainstorm_log.md` or the handover or any task file (`clu-autoencoder`'s wrong channel table is flagged in N76/§11.10, not edited — task files are not mine).
- **Did not resolve the +46.9 vs +47 rounding** — docs carry the source digit, the difference is noted.

## Open questions / risks
1. **The Hub's tier confirmation on N67–N82 is the one thing that blocks paper-appendix mining from this pass.**
2. **The w22 single-seed exposure** (Hopfield seed 0; d=8 rescue cells) sits under the wave's most quotable numbers — mechanisms are large-margin, digits are not sweep-backed.
3. **The registry is now 82 entries + 2 watch items + 5 reconciliations** — approaching the size where the paper-writer needs the per-paper mining notes more than the entries; I extended the notes, but a Hub-owned consolidation pass may be worth scoping before any drafting wave.
4. **R20-1** is cheap to close (one re-measurement of the ballistic fraction under the split-`dt` code) and un-owned.

## Proposed handover updates (for the Hub)
1. Record the doc-curator w20–w22 pass as **DISCHARGED** in the owed list (negatives + ledger + primers + future_work; **claims matrix explicitly still owed** — assign it).
2. **Confirm/re-tier N67–N82** (rationale in the registry's provenance-flag block) in the same sitting as the C17/C18 tiering.
3. Decide **Chapter 8** (or the paper vehicle for the addressable-memory/performance family) — three waves of central results are now filed against a chapter that does not exist.
4. **Own R20-1** or declare the qualitative statement sufficient.
5. When the roadmap rewrite lands, the four falsified claims it must not re-import are exactly **N69/N77/N78** (+ the N79/N80 scope fences) — the registry entries are written to be quoted directly.

---

# Appended section — claims_matrix.md w20–w22 sync (2026-07-23, Hub-assigned follow-up; same curator, same session)

**Choice of report location: appended here, not a separate file** — one diff-reviewable record for the whole w20–w22 pass. (`doc-curator-claims-matrix-w20-w22.md` was NOT created.)

**Status: done.** `.claude/claims_matrix.md` **v2.0 → v2.1**, dated change-log blockquote prepended per house style. `negative_results.md` NOT re-edited (per instruction — it is current; I only *read* the Hub's N83–N88 discharge note to get the cross-references right).

## What changed in the matrix, item by item
- **Header:** v2.1 change log + **the Head's binding performance-first filing rule and no-retreat clause recorded verbatim**, with the pointer that **`research_roadmap.md` v0.6 is the canonical home — the matrix mirrors, the roadmap governs**.
- **§1 canonical constants — five new rows + one amended:** the **designed capacity law** (`4·2^d`, R²=1.0000, with the repaired `Δ_req`/`d_eff` form REPLACING the falsified `(1+2R/w)^d`, and the bits-per-parameter denominator rule bolted on); the **write-operator/C3 interference law** (relaxed-fixed-point evaluation; 70×/103×/gate-refusal; the 197×→6332× amplification); the **fiber composition + parameter ceiling + Prop F1** ("mass selects, never stores"; `B_total = K_spatial·B_fiber`; `B_total ≤ P·b_θ`); the **honest capacity margin** (**2.1×**, with the 19×/17× never-quote); the **mass-timescale exponent** (`τ ∝ M^0.779`, `√M` retired). The **per-address-mass row** amended: `mass_override` shipped (w20), selection-scope added.
- **CM-3:** the `dt` fix **DELIVERED and FD001-neutral** (scope cell updated from "UNTESTED end-to-end"); "98.3% ballistic" quarantined to R20-1 in-cell; **and the two grounds on which FD00x numbers stay quote-restricted post-fix: N83 (raw-stats companion mandatory) and N87 (no external comparability under the label bug); N86/R19-1 quote-block restated.**
- **CM-5:** the "may-not-survive-`dt=1.0` (untested)" clause **RESOLVED** — the blindness reproduces exactly post-fix (bit-zero, structural) and is **fixed at source** (`neg_momentum_scale`, ships OFF; 0.00%→42.70%; `M_max/M_min` 4.1051; spectrum functional). No-task-metric guard + softplus downgrade + the Prop-F1 selection scope written into the cell.
- **CM-16a/b:** the SDR `dt` gate clause updated — **gate OPEN at w20** (1.77e-6→7.5e-2, 42,000×) at a stability price (retune `dt` per λ; N72 cross-ref).
- **CM-18:** three-part amendment — the loop does NOT survive learning (N67, with the w21 function-class nuance); the blank-control discipline strengthened (N68); qualifier (3) ("retrieval requires dissipation") **superseded-not-wrong** by the two-phase γ_address-only result.
- **CM-19:** **Prop 2 re-scoped to DESIGNED-STORE-ONLY** — the three measured non-transfers (recall N69, robustness N80 ⛔-worded, extrapolation → CM-22(b)) + what survives (the designed denoising win over Hopfield, the Ramsauer answer, the 2.1× partial support).
- **CM-20:** the falsified `(1+2R/w)^d` struck in-cell (§1 repair is canonical); **designed capacity exponential, W2 CLEARED, death zone = 1-D-ring property**; the **K=8-is-geometry** result added with the approved "rising trend, base 1.2–1.7, NOT a clean `A^d` law" wording; Hu–Wu–Liu + denominator discipline; scope cell updated (d-scaling now MEASURED).
- **CM-21:** scope cell updated — **all three retrieval gaps closed** (Ramsauer via Hu–Wu–Liu "as restated in"; **UnICORNN verified and it OCCUPIES symplectic stability**; SRNN not a preemption) + **Titans** as a new must-cite.
- **NEW CM-22 (⛔ forbidden performance claims):** (a) 19×/17× · (b) unscoped extrapolation/symplectic-stability (with the UnICORNN occupation) · (c) Prop-2 robustness/isolation in learned blocks · (d) mass as storage · (e) "beat SOTA on associative memory" · (f) clean `A^d` law · (g) K=8 as a learning ceiling · (h) graceful-degradation without the contextual label — plus the two inherited w21 kills (bits/param denominator; retrieval-cost). Every item carries its registry number and source section.
- **NEW CM-23 (approved wording, surviving results):** the mandatory **zero-benchmarks scoreboard sentence**; approved forms for (a) the capacity edge (with its four mandatory companions), (b) **retry +46.9pp/−38.3pp** as a capability-not-a-win, (c) per-item retention, (d) the Hopfield verdict **with the admissibility caveat** ("nothing learned on both sides" is MNIST-only; CIFAR SOTA requires a learned kernel; `sqdiff`/dropout protocol pins), (e) the primitive-decider statement, (f) the write-operator law with the price-and-all gate contrast.
- **§4 open slots:** the paper-vehicle slot updated — now six rows deep (CM-18…23), tied to the ledger's Chapter-8 question and the Head's filing rule.

## Verification
- Grep sweep over the matrix post-edit: every occurrence of `19×/17×`, `(1+2R/w)^d`, `98.3`, `√M`, "UNTESTED end-to-end" is now inside a correction, a ⛔ guard, or the struck v2.0 provenance block (C-9); the CM-19 `√M` hits are Prop 1's *legitimate* exact-time-dilation usage, not the retired τ-law.
- Cross-references to N83–N88 taken from the Hub's discharge note read this session (N83 raw-stats companion · N85 +0.0027 margin · N86 = R19-1 quote-block · N87 label bug); tier confirmations on N67–N82 noted as Hub-confirmed.
- Numbers transcribed from the same source sections already cited in the main report; no new reinterpretation; the Head's clauses quoted verbatim from handover §10 W22.

## Deliberately left / flags
- The struck v2.0 owed block retained untouched (C-9 precedent) even though its items 6/8 quote superseded numbers — the block is headed "struck as superseded."
- CM-4/CM-8 and the other pre-w19 rows untouched (no w20–w22 evidence bears on them).
- The matrix's mirror of the Head's framing is deliberately second to roadmap v0.6 — if the roadmap's wording of the filing rule differs, **the roadmap wins and the matrix header should be conformed** (flag for the Hub's roadmap-consistency check).
