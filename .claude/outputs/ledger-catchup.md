# ledger-catchup — doc-curator report

**Task + acceptance criterion:** close the w11/w12 ledger gap + the w11–w14 negatives gap; w11/w12/w13/w14 ledger addenda exist as diff-records; negatives current through w14 with N45 discharged-or-promoted; N10 re-test recorded; both new protocol rules propagated; docs-debt returned; nothing deleted (C-9).

**Status: done.** All four transfer docs current through w14. Every acceptance clause met. **Zero output↔handover contradictions found** — every number transcribed agrees with handover §10 (w11/w12/w14 reviews), the source reports, and matrix v1.9, including the ones that overturn prior claims (R7's floor, "raise `c`", the matrix's own `d=1` table). No code touched; no tracked file touched (all four docs gitignored under `.claude/`).

**Reconciliation-list owner note (per protocol §5, first-10-lines rule):** this report contains **no new downstream-reconciliation list** — I *executed* the two outstanding ones (N45's w11 discharge, and confirming the w12 "2.6"/N4 item already folded at w13). The remaining owner-actions are Hub-only and listed under "Docs-debt returned" below (matrix already at v1.9; Hub-owned-file sweeps).

**Sources, protocol order:** `AGENT_PROTOCOL.md` (§5 pre-reg + reconciliation-owner rules) → handover §10 entries for WAVE-11/12/13/14 → `claims_matrix.md` **v1.9** → my `transfer-docs-sync-w13.md` (items 2,3,5,6 of its docs-debt list = this task) → the four docs I own. w14 reports summarized via handover §10 (the reports' numbers are transcribed through it, as the Head directed: "reports are the primary source, the running log is the index").

---

## Edits per doc (for Hub diff-review)

### 1. `.claude/negative_results.md` — now current through w14
- **Header "Maintained by":** extended with a **wave-15 CATCH-UP SWEEP** line (w11–w12 gap + w14 folded in one pass); **curator-debt flag DISCHARGED** (was the w13 ⚠) with the w11/w12 sweep result recorded (cert-payoff/GMOR/t-lever produced no new experimental negatives beyond N45's discharge + the N4 retraction already folded).
- **Summary index:** N10 row → **UPHELD & STRENGTHENED (w14)**; N45 unchanged in index (discharge in entry); **N52–N58 rows added**.
- **N45 → DISCHARGED** (Update block): the w11 `v1-certificate-payoff` resolution finally recorded — router **det J = 0 exactly** (measured), PAYOFF A embedded (latch transport vs erasure, `pᵀXΔ` to 1.2e-7); definitional qualifier retired; **coercive-membership caveat** replaces it (PAYOFF B still needs "receipt buys it, not the jump"). Tier B-resolved, kept per C-9. *This is the resolution the w13 curator-debt flag pointed to.*
- **N10 → UPHELD** (two Update blocks): the w14 F-9 re-test on a **nearly-correct instrument** (exact-MJ momentum law) — survives `c=5`, `m₀=25`, MJ law; **χ²=216.0, p=2.7e-42, 80.7% flips** ⇒ digit law is not a function of the defect. Recorded the epistemic payoff: *a negative challenged and re-closed is stronger than one never challenged.* Tier A retained. **Curator did not adjudicate — transcribed.**
- **N18 → w14 sharpening** (Addendum block): characteristic-function proof supersedes free-particle; **control parameter `d·Θ` not `Θ`** (785×/31.4× at Exp-C `d=784`, not KL 0.384); "raise `c`" downgraded; F2 latent-mass thermostat is the exact fix; Prop-9→Prop-9′ at all three sites; CM-14 blast radius.
- **New entries N52–N58** (curator-proposed tiers, justified on record):
  - **N52 (A)** — R7's angular floor FALSE on a curved coset (332/787), REPAIRED to the assumption-free **Cartesian light-cone bound** (0/787, 1 ulp @1e18, exactly attained). *Falsification-with-repair; the task was written to invite it.*
  - **N53 (C)** — the analyst's own `γ_c/γ` refinement to R6 self-refuted.
  - **N54 (B)** — R3's transport law is a stiff-ring idealization, fails at 1% by `ζ₀≈0.19` on the trained ring (not "0.2% at ζ≤3").
  - **N55 (C)** — `rb-bound-trained`'s prereg corrected the R5 residual to `((2−γ)/2)²(εμ/γ)²`, not bare `(εμ/γ)²`. *Pre-reg pays.*
  - **N56 (A)** — "raise `c`" REFUTED as a native fix (worse natively: f(3589) 0.484→0.771; collapses Exp-C under legacy). CM-17 FIX RANKING replaces the deep-dive's.
  - **N57 (C)** — the Hub's own matrix-v1.8 `d=1`-table-at-`d=784` error (CM-17), caught by two re-deriving agents, corrected in v1.9.
  - **N58 (C)** — `training.sleep_friction` silent no-op on `train_generative` (fourth silent knob).
- **Paper-writer notes + cross-thread patterns:** added the w14 "closed (N10 upheld)" note; a new **"falsification-with-repair + re-derive-rather-than-trust"** cross-thread pattern with the counterfactual; N56/N52/N58 added to the physics-audit line.
- **Provenance flags:** N45 discharged; N52–N58 tier rationales; N10 non-adjudication; N52/N54/N53 rest on `causal-memory-floor` trained-ring runs (re-verify before paper use).

### 2. `.claude/future_work.md` — R5/R7/O8 → SHOWN, R3/R6 corrected
- **R5** → `→ SHOWN (wave-14, rb-bound-trained)` with the **saturated-write scope** and the residual-coefficient correction it never stated (product 1.0386× vs prereg 1.0388×, 5/5).
- **R7** → falsified-and-repaired (angular floor struck through; Cartesian light-cone bound is the object).
- **R6** → SHOWN + the self-refuted `γ_c/γ` refinement (N53).
- **R3** → corrected: stiff-ring idealization, `ζ₀≈0.19` breakdown (N54).
- **O8** → `→ SHOWN: NO` — the MJ tails do not cause the imbalance; **N10 UPHELD**; "raise `c`" refuted.
- **New dated footer** (wave-14 content, 2026-07-19): `→ SHOWN` list, corrections, "no new boundaries opened," P1/N47 fixed, the two w15 engineer follow-ups (`thermal_causal_ratio()`=`Θ`-not-`d·Θ`, N58). Nothing deleted (C-9).

### 3. `.claude/outputs/philosophy-synthesis.md` (the ledger) — w11, w12, w14 addenda appended
**⟲ protocol honoured: chapters untouched; three dated addenda appended after w13** (w13 already existed from my w14-review pass). w11/w12 written as **dated catch-up addenda (2026-07-19, Head-approved 2026-07-10)** — ordered after w13 to preserve the append-only record, each header naming the wave it covers (I did **not** fabricate contemporaneous-looking entries — the w13 addendum's own objection).
- **⟲ Wave-11 addendum:** Ch. 5 (firewall → exact identity `S=deg·R̄_edge`, CM-9 RESOLVED, parameter-separation), Ch. 4 (F3 discharged, N45 measured), Ch. 1 (V2 clean). Scorecard rows 5, 4. Method-lesson: *replace a weak empirical statement with an exact structural one* (recurs at w13/w14).
- **⟲ Wave-12 addendum:** Ch. 6 (F5 unblocked; corrigendum stronger-when-corrected; **"2.6" retracted** — the un-owned reconciliation item at its birth), Ch. 1 (**GMOR proper**, CM-15; `F²=M_ch r*²` survives w13's `ρ_s≠F²` correction), Ch. 3 (CM-16; sign flip; the unification; **the "13.9×" wrong number recorded at its birth**). Scorecard rows 6, 1, 3. Positioning: *w12 generated the evidence for its own procedural fixes* (both recurrence vectors born here).
- **⟲ Wave-14 addendum:** the four task-named chapter deltas — **Ch. 5** ("M is the budget allocator" slogan → **conservation law**, SHOWN, saturated-write scope named as the boundary); **Ch. 3/4** (R7 falsification-with-repair, *how the task was written to invite it*, task-design principle recorded); **Ch. 6** (Prop-9′ finalized, **N10 re-closes UPHELD**); **Ch. 7** (the w13 prophecy recurred and was handled; **NEW epistemics entry — "re-derive, don't trust," three agents corrected the Hub, with the counterfactual**; **both protocol rules recorded as now-binding §5 — the ledger's methods-chapter update**). Scorecard rows 2, 3/4, 6, 7. Gap-list (blocker cleared, N10 closed, two w15 follow-ups, scope guards w–z). Doc-hygiene: **continuity gap CLOSED**; Item-4 rounding resolved to the table; slug offset retired.

### 4. `.claude/outputs/HEP_primers.md` — protocol rules + w14 physics updates
- **Provenance masthead block:** new **"How the program validates a claim (three procedural layers)"** note — flag-provenance / **pre-registration** / **reconciliation-owner** (the two new rules, Item-3 propagation), + the *instrument-validity-precedes-inference* corollary.
- **§5.3:** the "raise `c`" fix struck; dated w14 Update — `d·Θ` not `Θ`, refuted native fix, **F2 latent-mass thermostat**, corrected F1/F2/F3 ranking.
- **§10.3 ledger:** R5 row → **SHOWN on trained (saturated-write)**; R7 row → **falsified→light-cone bound** (+ R3/R6 self-corrections); CM-17 row → w14 `d·Θ` sharpening + N10 UPHELD; MNIST/N10 row → **UPHELD** (χ²=216.0).
- **Masthead maintenance line:** wave-14 catch-up pass recorded.

---

## Item-by-item acceptance check
- **Item 1 (ledger addenda):** w11 ✓, w12 ✓, w14 ✓ (w13 pre-existing). Diff-records, not transcriptions — each is chapter-delta + scorecard + gap-list + positioning, per the 7-part rubric. The four task-named deltas all landed (Ch.5 conservation law + scope R5 never stated; Ch.7 instrument-validity recurrence + "will happen again"; Ch.3/4 falsification-with-repair + how the task invited it; new "re-derive not trust" epistemics entry + counterfactual).
- **Item 2 (negatives w11→w14):** N45 **DISCHARGED** (w11 resolution recorded); N52–N58 added; the three `causal-memory-floor` refutations (N52/N53/N54), the residual correction (N55), the "raise `c`" refutation (N56), the `d=1`@`d=784` matrix error (N57) all folded. **N10 UPHELD** with the "challenged-and-re-closed is stronger" note.
- **Item 3 (two protocol rules):** propagated to the ledger (w14 addendum Ch.7, recorded as now-binding §5 = the methods-chapter reflection) **and** to `HEP_primers.md` (Provenance three-layer note).
- **Item 4 (cosmetic + slug):** `v5-gate` §0 rounding vs §3.2 table → **resolved to the table** (on record in the w14 ledger doc-hygiene; no doc quotes the rounded band); **slug offset retired** — curator tasks now named for the wave they cover (recorded).
- **C-9:** nothing deleted anywhere — all supersessions struck-through with the number named (R5/R7 in future_work + primers; "2.6"/"13.9×" carried as named-superseded).

## How I verified
- `Grep` confirmed the ledger's addendum sequence and the absence of w11/w12 entries before editing; confirmed no live `13.9`/`13.88` in the four docs (still true — w13 finding holds).
- Cross-checked every transcribed number against handover §10 (WAVE-11/12/14 entries) and matrix v1.9. Spot-matches: R5 product 1.0386×/1.0388× (§10 w14 ¶`rb-bound-trained`); R7 332/787 + light-cone 0/787 (¶`causal-memory-floor`); N10 χ²=216.0, p=2.7e-42 (¶`relativistic-gibbs-expc`); `d·Θ` 785×/31.4× + KL 3.24e6/3.05e5 (¶`f5-corrigendum-2` / matrix CM-17); N45 router det J=0 + `pᵀXΔ` 1.2e-7 (§10 WAVE-11 ¶`v1-certificate-payoff` / CM-7); GMOR 1.33e-15 (§10 WAVE-12 ¶`f1-gmor-condensate` / CM-15). All agree.

## What I deliberately left alone
- **`claims_matrix.md` (v1.9)** — Hub-owned; already carries CM-16 split, CM-17 rewrite, CM-14 amendment, N10 UPHELD. **Not touched.**
- **`handover_context.md`** — Hub's. Not touched (proposed updates below).
- **Ledger chapters 1–7** — ⟲ protocol. Untouched; all deltas appended.
- **Verdicts and tiers** — summarized/organized, never reinterpreted. N52–N58 tiers are **curator-proposed** and flagged as such (Hub may re-tier). N10 UPHELD and N45 DISCHARGED were **transcribed from the handover**, not adjudicated.
- **Hub-owned out-of-scope files** (`brainstorm_log.md`, `research_roadmap.md`) — not swept (see docs-debt).

## Docs-debt returned (for the Hub)
1. **The w11–w14 ledger + negatives continuity gap is CLOSED.** Note for the record: the w11/w12 ledger addenda are **2026-07-19 catch-ups**, dated honestly, not contemporaneous.
2. **Two w14 engineer follow-ups still open (→ w15):** (a) `thermal_causal_ratio()` returns `Θ` where the operative parameter is `d·Θ` — the shipped helper and its `RelativisticGibbsWarning` are **under-conservative at high `d`**; (b) `training.sleep_friction` silent no-op on `train_generative` (N58). Both small; from the handover's own "TWO FOLLOW-UPS THIS WAVE CREATED."
3. **Hub-owned-file sweeps still outstanding** (carried from w13, re-flagged): grep `research_roadmap.md`, `brainstorm_log.md`, `claims_matrix.md` for residual `ρ_s ↔ F²` / `n = dim(G/H)` / `13.9`/`13.88`; and now also for **N57's pattern** (CM-17's `d=1` table quoted anywhere against a `d>1` experiment) and **R7's angular floor** wherever the memory-lifetime bound appears (ship the light-cone form).
4. **The reconciliation-list-owner rule is now the standing fix** for the class of debt this task cleaned up (the "2.6" that sat un-owned for two waves). Recommend the Hub apply it at every review going forward — this task was itself the manual cleanup a properly-owned list would have avoided.

## Git footprint
**None.** No tracked file created, modified, or deleted. All four edited files are gitignored under `.claude/`. Repo untouched.

## Proposed handover updates (for the Hub)
- **§10 / next-wave block:** *"Transfer docs current through **w14**. Ledger continuity gap CLOSED — w11/w12 catch-up addenda + w14 addendum appended (chapters untouched). `negative_results.md` at **N58** (N52–N58 added; **N45 DISCHARGED**, **N10 UPHELD & STRENGTHENED**, **N18 sharpened**). `future_work.md`: R5/R7/O8 → SHOWN, R3/R6 corrected. `HEP_primers.md`: the two new protocol rules propagated (Provenance three-layer note); §5.3 "raise `c`" refuted; §10.3 R5-SHOWN / R7-light-cone / CM-17-`d·Θ` / N10-UPHELD. Zero output↔handover contradictions."*
- **Standing scope-guard additions (drafters, verbatim, w14):** (w) **R5** = saturated-write, relativistic-mode conservation law (product 1.0386×; below the cap Newtonian `P∝1/M`; residual `((2−γ)/2)²(εμ/γ)²`); (x) ship the **Cartesian light-cone bound** `|q_{n+1}−q_n| < ε·c/√M_min`, never R7's angular floor; (y) relativistic-Gibbs fix ranking **F1/F2-exact/F3**, "raise `c`" is not free, control parameter is **`d·Θ`**; (z) **N10 UPHELD** (imbalance is a landscape property), never "refuted."
- **Two Hub actions the curator cannot take:** the Hub-owned-file sweeps (item 3 above); and the two w15 engineer micro-tasks (item 2).
- **Process note the wave's own evidence supports:** the reconciliation-list-owner rule (now §5) would have prevented this entire catch-up task — the w12 "2.6" and "13.9×" both propagated for lack of an owner. Both new §5 rules have now each caught a wrong number (pre-reg: 13.9× w13, R5 residual w14; owner-rule: the class this task cleaned). Recommend enforcing both at every review.
