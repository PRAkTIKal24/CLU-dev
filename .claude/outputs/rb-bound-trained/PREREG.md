# PREREG — rb-bound-trained (F-12: the causal retention–bandwidth bound on trained checkpoints)

**Written and frozen BEFORE any `n₁/₂`, `θ̇_max`, or product was measured.**
Everything below is derived from (i) the closed-form R5 algebra and (ii) the δ-broken vacuum
geometry `Σ = r*(δ)`, which is a property of `V` alone (M cannot enter `∇V = 0`). Generated by
`p0_prereg.py` → `prereg_numbers.json`. Only the geometry pass had been run at freeze time.

Repo commit `d6f8bac` · float64 · designed150_s{42,43,44,45,46} · `retie` on ·
`spurion_delta = δ = 0.02` (LinearSpurionPotential, `u = e₀`) · `ε = dt = 0.05` · `γ = 0.05` ·
`m₀ = 1`, `c = 1` · bands `M_ch ∈ {0.5, 1, 2, 4, 8}` (16× span) · **T = 0 everywhere**.

---

## P0. The instrument choice, registered (CM-17 trap)

CM-17: the coded relativistic Langevin has **no Gibbs invariant for any σ**. Therefore **no
equilibrium/thermal `n₁/₂` instrument is admissible in the relativistic arm.**

**Registered choice:** `n₁/₂` is measured as a **T = 0 deterministic relaxation half-life** of a
written coset displacement. Consequences, registered:

- `T/(m₀c²) = 0` in **every** relativistic cell. No Gibbs measure is invoked anywhere.
- The thermal persistence length `ℓ_θ = ε√(T/M_ch)/(γ r*)` is **identically 0**, so CM-16(d)'s
  boundary-layer bias `1 + 3.099·ℓ_θ/Δ` is **absent by construction**, not merely small.
- Read tolerance `Δ_read = θ₀/2 = 0.025 rad` (write `θ₀ = 0.05 rad`). Every `n₁/₂` in the report
  will carry `Δ` and `ℓ_θ/Δ = 0`.
- `θ̇_max` is measured **analytically/pathwise** (`q̇ = ∇_p T` at tangential `p`), never thermally.

## P1. Registered geometry (measured before freeze; `Σ` is the only measured input)

| seed | `r*(0)` | `Σ = r*(δ)` | `δΣ` | `M_ch` native | `P₀ = 2γln2·m₀c²/((2−γ)ε²δΣ)` |
|---|---|---|---|---|---|
| 42 | 0.966992651236 | 1.009325043591 | 2.01865e-2 | 0.683715 | **704.352** |
| 43 | 0.966262586053 | 1.002961887550 | 2.00592e-2 | 0.684414 | **708.821** |
| 44 | 0.979706476404 | 1.004480959307 | 2.00896e-2 | 0.664707 | **707.749** |
| 45 | 0.959136140888 | 0.985754563348 | 1.97151e-2 | 0.660380 | **721.194** |
| 46 | 0.990693318180 | 1.011977152986 | 2.02395e-2 | 0.687487 | **702.506** |

Registered: **`Σ` is M-independent to machine precision** (`|Σ(M)/Σ(M') − 1| < 1e-12` for all band
pairs). This is what "hold `δΣ` fixed while `M` varies" means, and it is *measured*, not assumed.

## P2. Registered per-band predictions (γ = ε = 0.05, δ = 0.02; seed-mean)

`F² = m₀·M_ch·r*(δ)²` · `μ² = δΣ/F²` (GMOR, CM-15) · `θ̇_max = c/(√M_ch·r*)` · `x ≡ εμ/γ`

| `M_ch` | `μ` | `x = εμ/γ` | `n₁/₂` (overdamped law) | `n₁/₂` (exact 2×2 map) | `θ̇_max` | `P_od = n_od·θ̇²` | `P_exact` | `P_exact/P₀` |
|---|---|---|---|---|---|---|---|---|
| 0.5 | 0.19972 | 0.19972 | 356.49 | **342.42** | 1.41024 | 708.92 | **680.94** | 0.96052 |
| 1.0 | 0.14122 | 0.14122 | 712.98 | **699.20** | 0.99719 | 708.92 | **695.22** | 0.98067 |
| 2.0 | 0.09986 | 0.09986 | 1425.96 | **1412.32** | 0.70512 | 708.92 | **702.14** | 0.99043 |
| 4.0 | 0.07061 | 0.07061 | 2851.93 | **2838.35** | 0.49860 | 708.92 | **705.55** | 0.99524 |
| 8.0 | 0.04993 | 0.04993 | 5703.85 | **5690.31** | 0.35256 | 708.92 | **707.24** | 0.99762 |

`n₁/₂` spans **16.6×**, `θ̇_max` spans **4.0×**, and the product is predicted **invariant to the
`x²` correction**. All bands are deep-overdamped: `h = εμ ∈ [0.0025, 0.0100] < h*(0.05) = 0.025643`,
`γ_crit = 2εμ ∈ [0.0050, 0.0200] ≪ γ = 0.05`. **No band leaves R5's derived band.**

### Registered predictions (falsifiable)
1. **PR-1 (product invariance).** `P_meas(M) := n₁/₂^meas(M) · θ̇_max,meas(M)²` is invariant across
   the 16× `M_ch` band to within the residual of PR-2. Specifically
   `max_M P_meas / min_M P_meas = 1.0388 ± 0.001` (the residual spread), **not** 16× and **not** 1.000.
2. **PR-2 (the residual is the exact map, not a fit).**
   `P_meas(M)/P₀ = n₁/₂^exactmap(μ_GMOR)/n₁/₂^od(μ_GMOR)` per band, i.e.
   **0.96052 / 0.98067 / 0.99043 / 0.99524 / 0.99762** for `M_ch = 0.5/1/2/4/8`.
   Deficits **3.948 / 1.933 / 0.957 / 0.476 / 0.238 %**.
3. **PR-3 (a sharpening of R5, registered in advance).** The deep-dive labels the residual
   `(εμ/γ)²`. Expanding the exact 2×2 map (`tr = (2−γ)(1−h²/2)`, `det = 1−γ`) gives
   ```
   1 − n_exact/n_od  =  ((2−γ)/2)² · (εμ/γ)²  +  O(x⁴)
   ```
   so `(εμ/γ)²` is the **γ→0 limit**; at γ = 0.05 the coefficient is `((2−γ)/2)² = 0.950625`.
   Predicted deficits from `((2−γ)/2)²x²`: **3.792 / 1.896 / 0.948 / 0.474 / 0.237 %** — the O(x⁴)
   term supplies the rest at the largest `x`. The deep-dive's own analytic-map table already
   contains this: its measured deficits `3.95/1.94/0.96/0.48 %` sit on the exact map
   (`3.948/1.933/0.957/0.476`, rel. dev ≤ 0.4 %) and **not** on `x² = 4.00/2.00/1.00/0.50`
   (rel. dev 1.3–4.4 %). **Registered claim: my measured deficits will follow the exact map,
   not bare `x²`, and the discrepancy will be resolved by the γ-sweep (PR-4).**
4. **PR-4 (γ-sweep discriminator).** `d/x² → ((2−γ)/2)²` as `x→0`. Registered values:
   `γ = 0.02 → 0.9801` · `0.05 → 0.950625` · `0.1 → 0.9025` · `0.2 → 0.8100`. Bare `x²` predicts
   `1.000` at every γ. Spread across the γ grid = **21 %** — decisive.
   *Scope:* only cells with `x ≤ 0.25` are in R5's derived band; at γ = 0.02 that restricts to
   `M_ch ∈ {2,4,8}` (at `M_ch = 0.5`, `x = 0.353` and the exact map already gives `d/x² = 1.675`).
5. **PR-5 (`μ²` cross-check).** The autodiff angular Hessian eigenvalue `μ²_H` will satisfy
   `μ²_H·F² = δΣ` to ≲1e-12 relative (CM-15 on these very checkpoints), so `μ_GMOR` is not a
   free parameter.
6. **PR-6 (Newtonian ≡ relativistic retention).** At `p ≈ 0` the relativistic kinetic term
   linearizes to `M_eff = m₀M`, so with `m₀ = 1` the **measured `n₁/₂` in `newtonian_learned` and
   `relativistic` will agree to ≲1e-10 relative** at every band. *Retention alone therefore contains
   no bound.* (This also certifies that a Newtonian-run retention measurement is admissible under CM-17.)
7. **PR-7 (the Newtonian null — half the result).** Driving tangential momentum `p₀` over 4 decades:
   - relativistic: `θ̇(p₀) → θ̇_max = c/(√M_ch r*)` (saturates; `θ̇/θ̇_max > 0.999` by `p₀ = 100`);
   - `newtonian_learned`: `θ̇(p₀) = p₀/(M_ch r*)` — **linear, unbounded, no supremum.**
     `sup_p θ̇ = ∞` ⇒ `n₁/₂ · sup θ̇² = ∞` for every band. **The product is not invariant in the
     Newtonian arm; it is undefined.** The conservation law is *created* by the causal governor.
8. **PR-8 (`M` allocates, `m₀c²` funds).** `θ̇_max = c/√M_ch` is **independent of `m₀`**, while
   `n₁/₂ ∝ F² = m₀M_ch r*²`. Registered:
   - `M_ch → λM_ch`: `n₁/₂ → λn₁/₂`, `θ̇_max² → θ̇_max²/λ`, **`P` invariant** (allocator).
   - `m₀ → λm₀`: `n₁/₂ → λn₁/₂`, `θ̇_max²` **unchanged**, **`P → λP`** (budget).
   - `c → λc`: `n₁/₂` unchanged, `θ̇_max² → λ²θ̇_max²`, **`P → λ²P`** (budget).
   Registered grid (seed 42, `M_ch = 1`): `m₀ ∈ {0.5,1,2}` ⇒ `P/P₀(m₀=1) = 0.5/1/2`;
   `c ∈ {0.5,1,2}` ⇒ `P/P₀(c=1) = 0.25/1/4`. Each to the same `x²`-order residual.

## P3. What would falsify

- `P_meas(M)` drifting monotonically by more than the registered `3.9 %` across the 16× band
  (e.g. a residual `∝ M^±1`) ⇒ **R5 fails on trained models**; report as a clean negative.
- `n₁/₂^meas` deviating from the exact 2×2 map by more than the `μ²_H` vs `μ²_GMOR` discrepancy
  ⇒ the trained potential's radial–angular / spectator coupling is not negligible and the
  single-mode reduction (F5 Prop-2) is doing work the theory does not license.
- `Σ` moving with `M` ⇒ the confound is real and the experiment is void.
- Newtonian `θ̇(p₀)` saturating ⇒ PR-7 wrong, the "governor creates the bound" story collapses.

## P4. Analysis discipline
Half-lives quoted from **three** instruments: (a) full `2·dim` step-Jacobian coset eigenvalue
[ground truth, no first-crossing artifact], (b) exact 2×2 map from `μ²_H`, (c) nonlinear T=0
rollout with log-interpolated crossing at `Δ_read = θ₀/2`. Rollout is only trusted where the mode
is overdamped (all bands here: `γ ≫ γ_crit`) — the F5 Appendix-N / v5-gate C-9 first-crossing
artifact does not apply. Amplitude linearity checked at `θ₀ ∈ {0.02, 0.05, 0.1, 0.3}`.
