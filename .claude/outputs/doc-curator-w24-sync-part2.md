# doc-curator-w24-sync-part2 — doc-curator report
Task + acceptance: fold wave-24's eight experimental outputs into the transfer docs — register the w24 negatives/positives, update the claims matrix → v2.3, apply the packing-slack retraction + R1 reframe, apply the Hub's rulings on the 7 chapter-8 critique items, write the Chapter-8 ⟲ w24 part-2 addendum, and FLAG (not settle) the write-ceiling cross-report tension.
Status: **done.** Five docs edited; nothing pushed; no tracked code touched; no git footprint (`.claude/**` gitignored).

> **⚠ FIRST 10 LINES — decisions/tensions the Hub owns:**
> 1. **Tiers on N95–N99 are CURATOR-PROPOSED (◇), pending Hub confirmation:** N95 A · N96 A · N97 A · N98 B · N99 A. Rationale in the registry's ◇ tier-flag block.
> 2. ⚠⚠ **REPORT↔TASK TENSION, FLAGGED NOT RESOLVED (discipline rule).** Task item 6 directed folding the seed-0 NN-gap range ("3.5–17.6 pp mask / 42.2 pp noise") into v2.3. `multi-seed-w23` **supersedes it** with the 5-seed range **mask −3.9…−20.7 / noise −9.7…−48.2 pp (40/40 pairs)** and says BOTH prior wordings are too narrow. I recorded the 5-seed range as canonical and flagged the tension in negatives (N90 update + header), matrix (v2.3 banner + CM-23), and ledger gap-list. **Hub call on which the paper quotes.**
> 3. ⚠⚠ **The write-ceiling mechanism is CONTESTED and I did NOT settle it** (Hub ruling applied): `write-ceiling-break` ("write operator / optimization interference") vs `lattice §4.2` (geometric width-lock). Both recorded LIVE; adjudicator = the free `lattice §5.0` trained-`log_width` dump. N92/N96 carry the qualifier; never write "the ceiling is the write operator" as settled.
> 4. **Packing-slack "1.08" retracted program-wide** (unit artifact; corrected 0.227). It lives in `phi-read-in.md` §5 (an output report I do NOT own) and echoes in N89's mechanism line — recorded the retraction in all docs I own; the phi-read-in.md fix is a Hub/engineer action.
> 5. **`phi_dim ≥ 16`** is now a binding protocol parameter (recorded in matrix CM-23(o), N89 update, future_work, primers).

## Files edited (all gitignored under `.claude/`)
| file | nature of change |
|---|---|
| `.claude/negative_results.md` | N95–N99 registered (full entries + index rows + ◇ tier-flag block); dated confirmation updates on **N89** (stream + multi-seed), **N90** (multi-seed + 3rd NN-gap supersession), **N92** (d=4 survives / d=5 softens + CONTESTED-mechanism ruling); packing-slack retraction blockquote; "Maintained by" header w24 pass note |
| `.claude/claims_matrix.md` | header → **v2.3**; new v2.3 banner block; **CM-22 +(m)/(n)/(o)** (certified / deletion-by-construction / exact-deletion-false); **CM-23** b/g NN-gap supersession + **+(l) R3 headroom-NO · (m) R2 exemption + contested-ceiling · (n) CL filing ruling · (o) task1_only φ + phi_dim≥16 · (p) Prop L2** + packing-slack retraction |
| `.claude/future_work.md` | 4 `TASKED → → SHOWN` updates (unlearning/lattice/headroom/φ-stream) with their findings; R2 entry w24 update (write-lever route closed, geometric route opened); new subsection **"⭐ Wave-24 experimental fold (part 2)"** (8 new boundaries) |
| `.claude/outputs/HEP_primers.md` | §11.14 atoms record: **"not numerics" clause resolved** — scale-dilution ruled out by measurement (\|Δ\|=0.010), mechanism CONTESTED (not "write operator" as settled); §11.6 w24 update (ceiling survives every lever, d=4/d=5 seeds, contested); §11.13 w24 update (R3 headroom-NO, multi-seed, 3rd NN-gap range) |
| `.claude/outputs/philosophy-synthesis.md` | ⭐ **`⟲ Wave-24 addendum, part 2 of 2`** appended — CONTESTED-mechanism section, Hub's 7 rulings applied, dial-by-dial deltas (Prop L2 new §2 entry · φ-stream §5.1(2) · write ceiling §5.1(3)/§5.3 · headroom dial 4 · multi-seed §5.2/§6 · R1 reframe §7), packing-slack retraction, scorecard row-8 current-state, gap-list with superseded numbers NAMED |

## Negatives registered (N95–N99, tiers curator-proposed ◇)
- **N95 (A)** — R3 leaderboard = decision-grade NO, now WITH HEADROOM PRESENT (`headroom-retry-benchmark`). Masked-NN oracle=1.000 in all 8 cells; CLU −3.5…−38 pp. CM-23(b) retraction proven cannot-be-saturation. Mechanism survives.
- **N96 (A)** — K≈32 write ceiling survives masked/sequential (backfires) + scale-invariant + crowding-aware (`write-ceiling-break`). ⚠ **Mechanism CONTESTED** — worded around the measured levers that fail, NOT "the ceiling is the write operator."
- **N97 (A)** — post-settle energy is NOT a routing/confidence signal at equal well depth (`lattice` checkD, ≤chance); R2/R3 exact. Broad program-wide guard.
- **N98 (B)** — shard independence violated by atom INIT not physics (`lattice` checkB, 1.4% / 3 orders above tail); carries the fix (localize group init).
- **N99 (A)** — ⛔ MVC-0 controller history-dependence blocks even EXACT deletion (`unlearning-recon`). R1's blocking technical gap.

## Positives registered
- **Prop L2** (ledger §2 new entry + CM-23(p)): sharding = N independent optimizers (proven+measured); `K_total = min(K_addr, N·K_ceiling)`; the Head's optimizer-sync worry has no referent; routing = classical NN indexing (never a dynamical result), requires a global allocator.
- **task1_only φ RATIFIED** + new binding **`phi_dim ≥ 16`** (CM-23(o), N89 update, future_work, primers).

## Confirmation lines (no new N)
- **N89** UPHELD + EXTENDED — stream discipline (all 4 + all 16 stress cells; watch-item does NOT fire) AND multi-seed (5/4 seeds, 0 wins in 22 pairs).
- **N90** multi-seed-clean (0/40 per control); ⚠ NN-gap superseded a 3rd time (5-seed range).
- **N92** — d=4=16 SURVIVES at 3 seeds (0.928); d=5=32 SOFTENS (0.898, straddles 0.90 → "≥16, consistent with 32"); ceiling robust to write operator (N96); mechanism CONTESTED.

## Discipline notes
- Numbers copied exactly from source reports with report+section citation; nomenclature Def-2 respected; two-layer tags used.
- No verdict reinterpreted. The one output↔task disagreement (NN-gap range) and the one output↔output disagreement (write-ceiling vs lattice mechanism) are both FLAGGED, not resolved.
- Chapter 8 §6 verdict `[split — lookup refuted, indexing refuted, control validated]` UNCHANGED; scope note strengthened (more seeds), tag applied `control validated-with-scope`.

## Gaps flagged for the Hub
1. Confirm N95–N99 tiers.
2. Rule on the NN-gap wording (seed-0 vs 5-seed) — I defaulted to the 5-seed range with the tension flagged.
3. `phi-read-in.md` §5 (an output report, not a doc I own) still quotes slack 1.08 — needs a Hub/engineer fix; I recorded the retraction everywhere I own.
4. The write-ceiling mechanism adjudicator (`lattice §5.0` trained-`log_width` dump) is now the highest-value experiment; it is currently unowned.
5. PALL (ICLR'25) added to the `continual-learning-recon` must-cite list in future_work — but that recon's own task file / any draft must reflect it.

## Proposed handover updates (for the Hub)
- **§10 / doc state:** `negative_results.md` now runs to **N99** (N95–N99 curator-proposed ◇) · `claims_matrix.md` → **v2.3** (CM-22 +m/n/o; CM-23 +l/m/n/o/p; NN-gap superseded) · `future_work.md` gains the **wave-24 experimental fold** + four `→ SHOWN` marks · `HEP_primers.md` §11.6/§11.13/§11.14 updated (the "not numerics" clause resolved to scale-dilution-ruled-out, mechanism contested) · `philosophy-synthesis.md` gains the **⟲ w24 addendum part 2** (Chapter 8 dials moved by addendum; verdict unchanged).
- **Binding new rules:** never quote "1.08" packing slack (use 0.227 / "ran past the bound") · never write "the ceiling is the write operator" as settled (mechanism CONTESTED) · `phi_dim ≥ 16` for any strict-φ claim · quote only the 5-seed NN-gap range · CM-22(m)/(n)/(o) forbidden claims (certified / deletion-by-construction / exact-deletion) · never "unlearning" for CLU's mechanism.
- **Carried unchanged:** 0 external benchmarks won · multi-seed before any paper number · laundering control mandatory · N94 in force · voraus re-run · real Mamba before any SSM claim.
