# doc-curator-w26-sync — doc-curator report **[C1W27]**

**Task + acceptance criterion:** discharge the **OWED w26 transfer-doc pass** — 11 N-entry groups registered with tiers, the Hub's five rulings folded, `claims_matrix.md` → **v2.5**, `research_roadmap.md` → **v0.9**, the ledger's ⟲ **w26** addendum, primers continued from §11.15, plus the §7 audit of the five C1W27 task files.
**Status: done.** All six docs edited (five named + `future_work.md`, my standing brief). No tracked code touched, no branch, no commits — every artefact is under gitignored `.claude/`.

## ⭐ DIAL DECLARATION (protocol §7, echoed)
- **Dial:** none — **transfer-doc upkeep.** I register and scope other people's evidence; I generate none.
- **Laundering control:** every performance entry I wrote carries its laundering verdict **in the same sentence as its number** (checked entry by entry — see §6, item 2).
- **Falsifies:** an entry whose number I cannot trace to a raw artefact under `.claude/outputs/` — **none occurred**; every number below is traced to a report + section.
- **Does NOT falsify:** disagreeing with a Hub tier and flagging it. **I dispute no Hub tier this pass** (the one Hub-named tier, N117 = B, I concur with) and I flag **two curator judgements** for Hub ruling (§4).

---

## 0. ⭐ THE §7 COMPLIANCE AUDIT — the five C1W27 task files: **5/5 PASS**

| task file (C1W27) | block | line | dial | laundering control | falsifies | does-NOT-falsify |
|---|---|---|---|---|---|---|
| `r2-d-sweep-close` | ✅ | **L10** | capacity (R2 law, exempt per CM-23(m); *"its figure is never framed as beating anything"*) | the **designed** write at matched geometry reaching `K_designed(d) = 4·2^d` at **every d run, on the same payload format**, + value-blank on every PASS, + the explicit **N46 scope-collapse** clause | ✅ *"no wall movement at d=6/d=8 at ≥3 seeds with payload noise ON ⇒ the w26 unclamping is a **d=4 artefact**"* — ⭐ **and the file says to report that as the result: "a clean, publishable close"** | ✅ failing to reach exactly `4·2^d`; any kNN/external comparison; **the metric-native-ceiling theorem** |
| `deletion-waitlist-stiffness` | ✅ | **L10** | two, named separately (isolation/deletion = Part A; lifetimes = Part B) | Part A: the **flat-datastore row-delete**, exact *by construction* — ⛔ *"never write a sentence in which the store beats a dict on a deletion-exactness axis"*; Part B: **N108 stands** (a TTL flag is equally detectable), so the only differentiator under test is `R₅₀` | ✅ per part, **both numeric** (A: `AUC(n_live)` not 1.000 → 0.500 with byte-equality at 3 seeds; B: the `R₅₀` contraction disappearing under **both** `g₀` settings) | ✅ losing to a dict/TTL on any AUC axis (standing result); **a step-shaped retention curve at `g₀ = amp_floor` — "the trilemma's stated price, not a defect"** |
| `learned-confine-causal` | ✅ | **L8** | **none — theory/instrument**, with an explicit *"must not be reported against any performance axis"* | n/a (no performance number) — **replaced by a registered numeric band, scored after** | ✅ no movement, or movement outside the registered band ⇒ **(★) downgraded to a correlation** | ✅ a smaller-but-right-direction effect inside the band; any absolute retrieval level (*"this is a difference test"*) |
| `r1-positioning-pass` | ✅ | **L10** | **none — positioning/wording**, *"no new evidence is generated or claimed"* | the **flat-datastore row-delete** named in **every** R1 sentence, with no claim to beat it | ✅ ⭐ the sharpest falsifier in the wave: *"any sentence you produce that survives only because a scope qualifier was dropped"* | ✅ **that our theorems are instantiations of prior work — "that is the finding"** |
| `doc-curator-w26-sync` (this file) | ✅ | **L9** | none — transfer-doc upkeep | every N-entry's laundering verdict in the same sentence as its number | ✅ any number untraceable to a raw artefact ⇒ file as unverified | ✅ disagreeing with a Hub tier, if flagged |

**Result: 5/5 comply, and the quality is higher than w25's.** Three observations for the Hub:
1. ⭐ **Four of five files pre-name an outcome that would otherwise read as a failure and declare it a *result*** — the d=4-artefact close, the `R₅₀`-disappears branch, the step-shaped curve, and *"our theorems are instantiations — that is the finding."* This is the governance fix doing exactly what w24 designed it for, and it is now the house norm rather than an exception.
2. ⚠ **The §7 cosmetic item from w25 is FOLDED, and the fix is a template rule, not a correction.** `matched-bytes-frontier`'s block sat at **L21 behind a 14-line rationale preamble**. All five C1W27 files put it at **L8–L10, behind a short administrative header only** (agent · worktree · base commit · campaign tag · required reading). ⇒ **Recommended wording, now recorded in roadmap v0.9's cautions:** *"the DIAL DECLARATION opens the task file — after at most a short administrative header, and never behind rationale prose."* Under that rule all five pass literally, and the w26 deviation is retro-classified as a drafting slip rather than a template ambiguity.
3. **The "outcome against the declaration" line** (recommended at w25) was used unprompted by **three** w26 spokes (`matched-bytes-frontier`, `cl-encoder`, `carried-remeasurements` — the last one scoring its falsifier **per item**). Recommend making it mandatory; it costs a line and converts §7 into a scorecard.

---

## 1. `negative_results.md` — **14 new entries (N109–N122)**, 7 dated amendment blocks, 4 structural additions

| # | one line | tier |
|---|---|---|
| **N109** | ⭐ **(POSITIVE) the d=4 wall is a property of the READ-OUT CODE and it moves** — 16→32 (annealed read, 0 bytes/dims/steps, ~110 SE) and 16→**128 = `K_designed(4)`** (multi-channel code); tax **1/8→1/1**; three controls; ⚠ **d=4 ONLY** | **A** ✧ |
| **N110** | ⛔ **the `tol` value criterion is VACUOUS at m>1** — a blank scores **1.0000**; decode blank = 1/32 exactly; the w25 `pscale` free lunch **dies** under noise (0.9995→0.5894) | **A** ✧ |
| **N111** | ⛔ **init and width are SUBSTITUTES** — `atom_init_local` **null at the shipped width** (+0.007±0.016), interaction **+0.459 (14.3 SE)**; ⛔ **N98's +0.051 does not cross-replicate** | **A** ✧ |
| **N112** | ⛔ **(scope guard) at OVERFLOW canonical placement is NOT exact** — `AUC(n_live)` 1.000, `AUC(s4)` 0.914, byte-equal 0.000; the P2 waitlist is unbuilt | **A** ✧ |
| **N113** | ⛔ **the matched-BYTES contested win FAILED as pre-registered** (5th laundering) — ACC 6/6 and forgetting 5/6 vs replay, launder **never beaten 6/6**; **24.5×→19.1×**; iCaRL over budget | **A** ✧ |
| **N114** | ⭐⛔ **THE SAME-KEYS LAUNDER — deleting the settle IMPROVES the store** (6/6 both axes; mean +0.013 ACC / −0.011 forgetting) ⇒ **the physics costs ≈1 pp** | **A** ✧ |
| **N115** | ◐ **budget-bound → gate-bound at ~314k floats** (6 085±66 of 7 658, admitted 0.57–0.62, `refused_full`=0) — the packing law inside a benchmark; **P3(b) falsified** | **A** ✧ |
| **N116** | ◐ **LwF is a CE-scope CONVENTION, not a tuning failure** — flat over a **200× α sweep**; current-task CE gives 0.556; stays **4.3 pp low** wherever the table is quoted | **B** ✧ |
| **N117** | ◐ **τ binds iff `#{cos₀<τ} < k·step_n`** — bit-identical at p=0.8 (`max\|Δ\|=0`, 6/6), **−0.485±0.064** at p=0.5; ⛔ **NOT a φ-space property** (own hypothesis falsified) | **B** ✧ (Hub-named) |
| **N118** | ⛔ **BG07 own the algorithm** — Thms 1–2 are instantiations, the cascade is theirs; **+"deletion-compliant"** and **"our fix-up cascade"** banned; Hartline reversibility caveat; Candidate 2 occupied in pieces | **A** ✧ |
| **N119** | ⛔⭐ **THE TRILEMMA**, and **both D3 fixes REFUTED** ((b) breaks the anti-decoration guard — the trivial substitute passes **0.672** amplitude-independently); option (d) recommended; **the third corner is dial 4** | **A** ✧ |
| **N120** | ⭐ **the saddle criterion supersedes the reach bound** — ±2.5 % on the shipped integrator, **31/32 on the trained `V`, zero free parameters**; **reach logarithmically un-buyable**; **`sep` alone 11/11**; ⛔ **κ≈3 retired** | **A** ✧ |
| **N121** | ◐⭐ **the φ gate STRADDLES** (0.339 MISS → **0.357 CLEAR at 2.5× compute, <1 seed-σ**); "kNN caps at 0.21" **superseded**; ⭐ the CIFAR entry is a **registered-and-declined NOT-RUN** | **A** ✧ |
| **N122** | ⭐ **the strict-φ cost bites on a WORKING φ** (+0.003 PCA vs **+0.020±0.010**, 3/3 seeds) — *exactly the clear/miss difference*; the CIFAR null has **two** causes | **A** ✧ |

**Tier rationale (✧ block written under the index).** Same standing A/B criterion the file has used since w23. **A** where an entry or approved wording is overturned, a program-wide quotation rule follows, or a claim-gate moves (N109, N110, N111, N112, N113, N114, N115, N118, N119, N120, N121, N122). **B** for the two scope guards that supersede no prior entry and impose no program-wide never-quote: **N116** (one baseline table) and **N117** — where **the Hub named tier B in the task file and I concur**: it *narrows* N103's clause rather than overturning it.

**Seven dated amendment blocks — nothing rewritten, nothing deleted:**
1. **N99** — a single block with **four** updates: **(a)** PGCP **LANDED + acceptance MET** (0.99985 → **0.5000 ± 0.0000** on all six statistics, TPR@1 % 1.000 → **0.000**, byte-equal **3072/3072**, cascade-firing variant still exact at mean **1.132** moves; rematch under the **real** read 61/64 / 1.0000 / **0.9531** ⇒ **"0.953" is quotable as measured**; cascade cost restated as *"~3–4 at full load"* since 2.84 is one key-set realization vs **3.865** over 200); **(b)** ⛔ the **overflow boundary** (→ N112); **(c)** the **leak is a CURVE** (0.6715/0.9165/0.9961/0.99985 at 2/4/6/8 offers; IN-min `z_hole` = `d_safe` to 4 dp at every load; paired control 0.5000 in 24/24) + the standing *"quote the curve"* rule; **(d)** the **attribution correction**. ⇒ **all three w25 blockers CLEARED; N99 is no longer blocking, it is scoped.**
2. **N100** — ⭐⭐ confirmed (the conflict, the `readonly` localisation) · **dissolved at d=4** (two different axes) · ⛔ **superseded wordings** (κ≈3; the sep/w explanation) · what did **not** change (every measured number; the `pscale` never-quote, now *reinforced*).
3. **N98** — the **cross-harness reader warning** the Hub owed: N98's +0.051 does **not** reproduce (+0.0068 ± 0.016) on the designed-mechanism harness; **both numbers stand in their own harnesses**; what is forbidden is the generalisation. Tier B unchanged.
4. **N101** — matched-bytes **reverses** the matched-items picture, without rescuing the claim; ⚠ **24.5× → 19.1×**; iCaRL over-budget; LwF closed as *not a fix*.
5. **N102** — the null has **two** causes; the 0.21 cap **superseded**; ⚠ **and its own `strict_phi_cost = +0.001` is a property of a broken φ** — strictness *does* bite at +0.020 ± 0.010.
6. **N103** — the τ clause **narrowed to a rule**, not retracted; the "φ-space vs pixel-space" framing **falsified**; **the tie is unchanged and not upgraded.**
7. **N108** — **D3 RULED** (trilemma; both fixes refuted; option (d); **`R₅₀` unmeasured under the gate**; retention magnitudes are **placement-dependent**), **D1 RESOLVED**, and the leak-as-curve pointer.

**Structural additions:** a **w26 paper-writer family block** (four sets — capacity · deletion · CL/performance · lifetimes — each with what travels and what is forbidden); **two new cross-thread entries** (*"quote the curve, not the endpoint"*, with the three places future waves will hit it next; and *"the fourth our-own-control-refutes-us event — this time it refuted a proposed fix before it was built"*); **three new provenance flags** (the two curator judgements, the population-vs-sample sd convention, and **the three things that would dangle at the C1 close**); and a w26 paragraph at the head of the *Maintained by* block.

## 2. `claims_matrix.md` → **v2.5**
- **v2.4 recorded as ✅ HUB-CONFIRMED** (2026-07-29) in the header; new **v2.5 banner** (six items, four supersessions), ending with the unchanged score sentence.
- **CM-22 gains (v)–(bb), and TWO v2.4 BANS ARE LIFTED:** ⚠ **(q)'s blanket deletion block → a narrower, absolute scope clause** (*"below capacity or set-function eviction"*), because all three blockers cleared; ⚠ **(r)'s "0.953" ban LIFTED** (quotable as measured, with per-admitted beside it). New: **(v)** no `tol` number at m>1 + no `K_learned` without σ_obs and the designed reference · **(w)** no *"R2 unclamped"* without *"at d=4"*; bits-per-param **STALE**; `atom_init_local` not a general gain · **(x)** the six BG07/vocabulary bans · **(y)** no matched-bytes sentence without its launder clause; 19.1×; the two baseline-table corrections · **(z)** no *"gate cleared"* without the fit budget; the strict-φ scoping; the declined run is not a null · **(aa)** κ≈3 and `sep/w`-as-cause retired; neither refuted D3 fix may be described as available · **(bb)** the *"quote the curve"* rule with its two instances.
- **CM-23 gains (u)–(aa):** **(u)** R2 unclamped at d=4 (with the scope clause **inside** the approved form and all three controls mandatory) · **(v)** the **scoped** deletion paragraph + the measured sentence + **the mandatory BG07 attribution paragraph** + Ginart/Sekhari vocabulary · **(w)** the matched-bytes frontier **with its own falsifier in the sentence**, the *"clean vs ambiguous"* lead, and the saturation companion · **(x)** the saddle criterion (**supersedes (s)'s κ clause**), the logarithmic no-go, the `sep` demotion, and the write's width adaptation · **(y)** the **trilemma** and the compute-adaptive-read corner · **(z)** the gate straddle + the strict-φ cost (supersedes the 0.21 cap) · **(aa)** the **τ-regime rule** (narrows (r)'s caveat; retracts neither wave).

## 3. `research_roadmap.md` → **v0.9**
New top block + a supersession banner. Contents: the **campaign split** (C1 / C1W27-final / C2, with the ≤3-worktree cap) · **what w26 changed in four scoped sentences** · **two pinned-law amendments** (the criterion is now a saddle condition; the capacity law's scope line made explicit, with bits-per-param **STALE**) · a **refreshed per-R table (R1–R5) for w26 OUTCOMES** (R1 **closed in scope**; R2 **unclamped at d=4, scope stated**; R3 scoped by the τ rule + the trilemma's third corner; R4 the failed contested win + the gate straddle; R5 → now C2's subject) · the **C1W27 wave table** (outcomes, not scope) · **four new standing cautions** (*quote the curve* · the §7 template rule · *a control that kills a proposed fix counts as a result* · name `config.py` in every ownership list).
⚠ **The Part-4 R1 line is NOT touched**, as instructed: it is flagged in-place as **PENDING EDIT (owner `r1-positioning-pass` + Head)**, with the reason it is triply forbidden as written and a pointer to read it through CM-23(v) meanwhile.

## 4. `philosophy-synthesis.md` — the ⟲ **wave-26** addendum
**The Hub wrote none** (the file ended at the w25 addendum), so this is the wave's **primary** ledger record, written to the ⟲ protocol: chapters **not** rewritten, Chapter 8's §-bodies untouched and moved by addendum, nothing deleted. Contents: provenance & scope (incl. the campaign note and *"nothing here pre-writes a C1W27 result"*) · **the wave in one arc** (five events; the rhyme is *"the controls did the science"*) · **dial-by-dial deltas** · **chapter deltas** (Ch. 8 dials move, **verdict unchanged**; dated pointers on Ch. 5 and Ch. 7; Ch. 1–4/6 untouched) · **scorecard deltas** (row 8 current-state, row 7 + three exemplars) · **gap-list updates with every superseded number NAMED** · **eight doc-hygiene flags**.
⭐ **One flag is a trigger firing:** at w25 I recorded *"if w26's contested-win experiment lands either way, that is the natural moment for a Chapter 8 §5 restatement."* **It landed.** Chapter 8 now carries **four** addenda over unrevised bodies and a reader assembles each dial from five places, with **C1 closing one wave from now — the last cheap moment.** Recorded as a recommendation; **the call is the Hub's** (I do not restructure a document whose structure encodes the program's beliefs).

## 5. `HEP_primers.md` — **new §11.16** (three records) + five dated updates
- **NEW §11.16 — "Three pictures from wave-26"**, in the §11.14/§11.15 house style (picture → symbols → clause-by-clause evidence table → ⛔ boundary → *"→ CLU, said plainly"* → status + reading pointers): **Record 6 — The shorter reach** (*put the answers nearer the line, or lend the reader a torch and take it back* — with the three controls as three separate table rows, because they are what make it a mechanism); **Record 7 — The saddle** (*two forces, a ridge between them; you cannot buy reach by digging; a perfect classifier is not automatically a mechanism*), carrying (★) and the `√(2 ln ·)` no-go; **Record 8 — The launder that deletes the physics** (⭐ **explicitly labelled a NEGATIVE record**: *same keys, same wells, same bytes — delete the settle and it gets better*), with the *clean vs ambiguous* contrast as its one surviving lead.
- **§11.15 Record 3 (Reach)** — dated update: the imagery **survives**, the **boxed formula is superseded** (→ Record 7), and *"there is no room underneath it"* is **dissolved at d=4** (two axes, not one width).
- **§11.15 Record 4 (The parking rule)** — dated update: **built · number hit · H5 closed · the lot has a capacity (overflow fails) · the bays are Blelloch–Golovin's** · the cascade-cost restatement.
- **§11.15 Record 5 (Answering vs leaking)** — dated update: the leak is a **curve**; D1 fixed; **D3 ruled as a trilemma** with both fixes refuted and `R₅₀` still unmeasured under the gate.
- **§11.6 (capacity)** — dated update: the wall moved from the **read-out side**; the three controls; ⛔ the `tol`-at-m>1 never-quote; ⚠ **d=4 only**; bits-per-param **STALE**; the substitutes result.
- **§11.11 (the read-in φ)** — dated update: a working CIFAR φ **exists**, what bought it (contrastive objective + fit compute; **reconstruction bought nothing**), the **launder raised by construction**, the strict-φ cost, and ⭐ **the crowding mechanism CONFIRMED (slack 0.332), not fixed, by a better φ.**
- **§11.13 (test-time compute)** — dated update: the τ clause narrowed to a **measurable rule**, the falsified framing, the two harness facts that let the contradiction survive, and ⭐ **a physical home for the dial** (the trilemma's third corner).

## 6. `future_work.md` — **9 items marked `→ SHOWN`, 12 new boundaries** *(beyond the named deliverables — my standing brief; flagged for the Hub)*
`→ SHOWN (wave-26, report)` with provenance kept: the fair excursion experiment (both arms) · the init × width factorial (**shown, and the answer was no promotion**) · a CIFAR-capable φ (**straddles**) · the forgetting-vs-bytes frontier (**falsifier fired**) · PGCP's landing + rematch · the occupancy sweep · the D3 ruling · the multi-seed τ check · the LwF re-tune (**closed as not-a-fix**) · (+ the designed-arm-at-half-excursion item, shown in the form that mattered) · (+ the prior-art debt, **which corrected us**).
**New fold (12 entries, schema-complete):** the **d=6/d=8 sweep** (⛔ *a FAIL is a clean close, not a gap*) · the **arm(a)×arm(b)** cell · **capacity-per-byte re-derived** (Candidate 3's whole basis) · the **P2 waitlist** (⛔ outcome not pre-written) · **option (d) + `R₅₀` under the gate** · ⭐ **"is there ANY read for which the settle beats arg-min over the same wells?"** — *the highest-value open item in the register* · **R1 in existing vocabulary** · the **`learned_confine` causal test** · the **neighbour/interference factor** of the cell-level law (`THEORY-ONLY`, the theorist's own open question) · **address quantization for φ-derived addresses** · **saturation as a law** (with its falsifiable prediction that the launder gap re-opens) · **five cheap digit-blockers** · **two registered NOT-RUNs + one parked probe** (`PARKED`: the Head-declined CIFAR entry — ⛔ never a null; and P4 — ⛔ never re-attempted parameter-matched) · **five harness/hygiene debts**.

---

## ⭐ The wave's most misquotable sentences, and the guard I placed on each

| # | the sentence a skimmer will produce | what it actually is | guard, and where it now travels |
|---|---|---|---|
| **1** | *"`K_learned(4)` = `K_designed(4)` = 128 — the capacity law is closed."* | a **d=4** result at a stated noise level, with K=128 on **2 seeds** | **Four guards, all now in the same sentence as the number in registry/matrix/roadmap/primer/ledger:** *d = 4 ONLY, d=6/d=8 NOT RUN, no exponent re-measured* · *σ_obs + the designed-arm reference* · *no `tol` number at m > 1* · *no `K_learned` at `pscale ≠ 1`*. **This is the w25 "0.824 → 1.000" problem one wave later with higher stakes — it is now a capacity number, not a mechanism one.** |
| **2** | *"At matched bytes CLU beats the whole replay field."* | true — and **reversible in one clause**: both launders beat it 6/6 | The launder clause is written **into the same sentence** in CM-23(w), N113 and roadmap v0.9. ⛔ **A draft that puts them in different paragraphs is non-compliant** (stated in the ledger's hygiene list). |
| **3** | *"Deletion is exact."* | exact **below capacity or under set-function eviction**; at overflow `AUC(n_live)` = 1.000 | The scope clause is inside the **approved paragraph** (CM-23(v)), inside N99(b), and registered **as its own entry (N112)** so a spoke that never opens the matrix still hits it. ⛔ *The overflow numbers are never the deletion claim.* |
| **4** | *"We proved order-independent placement."* | **Blelloch–Golovin FOCS'07 proved it**; ours is an instantiation | The **attribution paragraph is mandatory in the same breath as any theorem** (CM-23(v), N118, primer Record 4). ⛔ *"our fix-up cascade"* and ⛔ *"deletion-compliant"* added to the ban list in four docs. |
| **5** | *"The CIFAR gate was cleared."* | cleared **at 2.5× the registered fit compute, by +0.007 against a ±0.019 seed spread**; missed at the registered arm | ⛔ *"never quote 'the gate was cleared' without the fit budget"* — in CM-22(z), N121, primer §11.11, roadmap R4. The approved form is the engineer's own: *"cleared on the 3-seed mean at 20 000 fit steps, by less than one seed-σ."* |
| **6** | *"The encoder improved CLU."* | **every number in that report is a kNN/launder number**; no settle was run at that φ | Stated in N121's headline, CM-22(z), primer §11.11 and the ledger. ⛔ *"Nothing in this report is a store result."* |
| **7** | *"The post-eviction leak is AUC 0.99985."* | the **top of a curve** (0.6715 at 2 offers) | The **curve** replaces the constant in N99(c), CM-22(bb), primer Record 5 — and the standing rule *"quote the curve, not the endpoint"* is now in the roadmap's cautions with **three named places future waves will hit it next.** |
| **8** | *"τ doesn't matter in φ-space."* | true at p = 0.8, **worth −0.485 at p = 0.5 in the same store**; the space framing is **falsified** | Replaced by a **rule** (`#{cos₀ < τ} < k·step_n`) in N117, N103's block, CM-23(aa), primer §11.13. ⛔ *every threshold statement names the corruption level.* |
| **9** | *"Reach requires `s ≳ |a|max/κ`, κ ≈ 3."* | **superseded** one wave after being written | κ≈3 retired in N120, N100's block, CM-22(aa)/CM-23(x), primer Record 3 → Record 7. **Every measured number in N100 stands; only the criterion moves.** |
| **10** | *"The CIFAR entry showed nothing"* / *"the d-sweep showed no effect."* | **NOT RUN** — one Head-declined with its outcome pre-registered, one unfunded | Both filed as **registered-and-declined / declared NOT-RUN** in N121, future_work (`PARKED`), and the ledger's hygiene list. ⛔ *A "not run" later cited as "no effect" is the exact failure mode this discipline exists to prevent.* |

---

## What I deliberately did NOT do (and why)

1. **I did not resolve the population-vs-sample sd difference** between `r2-excursion-reach` (± 0.0028 / ± 0.0005) and the Hub's §10 entry + C1W27 task file (± 0.0034 / ± 0.0006). The Hub had already labelled it (*"do not 'correct' its tables"*). **The docs carry the report's numbers with the convention named**, and I flagged that **the papers must pick one convention once, program-wide** — this is precisely the class of thing a referee finds by comparing two sections.
2. **I did not pre-write any C1W27 outcome** — the P2 waitlist, the d-sweep, `R₅₀` under option (d) and the `learned_confine` test appear **only** as tasked items with their acceptance criteria, never with a predicted result.
3. **I did not touch the Part-4 R1 line** (owned by `r1-positioning-pass` + Head approval this wave). It is flagged in place as **PENDING EDIT** with the reason it is currently triply forbidden.
4. **I did not restructure Chapter 8** despite the w25 trigger having fired. Recorded as a Hub-owned recommendation with the reason it is now time-boxed (C1 closes one wave from now).
5. **I did not re-tier anything from previous waves**, and I did not soften N98 — its **own** number stands in its **own** harness; only the generalisation is banned.
6. **I did not merge the two D3 fix refutations into the option-(d) recommendation.** N119 records the refutations, the toy's numbers, **and** the fact that the theorist refuted his own first implementation — separately, because the third is an epistemics datum and the first two are design guards.
7. **I did not quote** any `tol`-metric number at m>1, any `K_learned` at `pscale ≠ 1`, the retracted slack 1.08, "24.5×", or "the gate was cleared" unqualified — anywhere, in any doc.

## Gaps / risks flagged for the Hub

1. **⚠ Two curator judgements need a ruling** (§4 above and the registry's provenance flags): **N112 as its own entry** (I applied the Hub's own N103 reasoning to a scope boundary every future deletion sentence depends on; deleting it costs nothing since the numbers are in N99(b)), and **N109 as a POSITIVE in the negatives registry** (the source proposed it; it is here because *its scope clause is the thing most likely to be dropped*).
2. **⚠ The seed exposure has moved onto the positive.** w25's thin flank was Stage-0; w26's is the unclamping itself — **K=128 is 2 seeds**, **arm (b)'s K=64 FAIL is 1 seed + the atom re-check**, and **the theorist's 31/32 has no error bar**. Margins are enormous (~110 SE at the decisive cell); the *digits on the ladder's top rung* are thin, and that table is the one most likely to become the R2 figure.
3. **⚠ Three things would dangle at the C1 close** (registered in the provenance flags): the `pass_metric` → `decode` **re-render** (fold it, do not re-derive it); the **overflow scope sentence**, written so that one edit updates it everywhere when the waitlist lands; and **`R₅₀` + the d-sweep** — if they do not land, **N119 and N109 close with their scope clauses *as the finding*, which is a clean close and should be stated as one.**
4. **⚠ `future_work.md` was folded although this task's file-ownership list omits it** (the five named docs are the deliverables). It is in my standing brief and the w25 pass folded it; C1 closes next wave and its provenance trail (9 `→ SHOWN`, 12 new) is exactly what a C1-close pass needs. **Flagged, not assumed** — if the Hub wanted it left, the fold is one contiguous dated block and is trivially removable.
5. **No output↔handover numeric disagreement found this pass.** Every headline in the Hub's §10 w26 entry reproduces against the seven reports (the Hub re-derived four itself from raw JSON); the only difference anywhere is the sd convention in item 1, already labelled by the Hub. **Nothing to adjudicate.**

## Git footprint
**None.** No tracked file created, modified or deleted; no branch, no commits. All six edited documents live under gitignored `.claude/` (`negative_results.md`, `claims_matrix.md`, `research_roadmap.md`, `future_work.md`, `outputs/philosophy-synthesis.md`, `outputs/HEP_primers.md`) plus this report.

## Proposed handover updates (for the Hub)
1. **Doc state after this pass:** `negative_results.md` **through N122** (116 entries; tiers on N109–N122 **curator-proposed ✧**; N98/N99/N100/N101/N102/N103/N108 carry dated w26 blocks; N100–N108 marked **HUB-CONFIRMED**) · `claims_matrix.md` **v2.5** (v2.4 marked confirmed; CM-22 +(v)–(bb) with **two bans lifted**; CM-23 +(u)–(aa)) · `research_roadmap.md` **v0.9** (per-R refreshed for w26 **outcomes**; C1W27 table; Part-4 line **untouched and flagged**) · ledger **⟲ w26 addendum** (Hub wrote none) · primers **§11.16** + five dated updates · `future_work.md` **w26-folded**.
2. **Confirm or re-tier N109–N122**, and rule on the two curator judgements (**N112 as its own entry**; **N109 as a positive in this file**).
3. **Adopt the §7 template wording** in roadmap v0.9's cautions (*"the dial block opens the file, after at most a short administrative header, never behind rationale prose"*) — it converts the w26 cosmetic flag into a rule, and all five C1W27 files already satisfy it.
4. **Make the "outcome against the declaration" line mandatory** — three of seven w26 spokes did it unprompted, one of them per-item.
5. **Chapter 8 §5 restatement: the trigger I recorded at w25 has fired.** Hub call; the last cheap moment is before the C1 close.
6. **Pick the sd convention once** (population vs sample) before any paper table is drawn.
7. **At the C1-close pass:** fold the `decode` re-render rather than re-deriving it; update the overflow sentence in the four places it is written to be one-edit updatable; and if `R₅₀`/the d-sweep do not land, **close N119/N109 on their scope clauses as findings**, not as gaps.
