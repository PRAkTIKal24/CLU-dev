# fix-pack-3 — experiment-engineer report
Task + acceptance criterion: implement the 8 w3-discovered fixes; full suite green (≥136), atomic tagged commits, behavior changes flagged.
Status: **done** (8/8; item 7 is a no-code intel reconciliation, reported below). ⚠ One cross-agent contamination found & reported (does not affect correctness) — see "Git footprint / concurrency note".

## What I did (one commit each)
1. **Exp-D sleep-erosion guard** (`55892a8`): `ExperimentDConfig.train_epochs` default **1000 → 150** (the v2-full-runs Finding-0 erosion regime); new `experiment_d.sleep_mode ∈ {"on","off"}` — `"off"` sets `config.training.sleep_frequency = 10**9` (wake-only, the data-pinned regime, r*≈1.0 through 1000 ep). Wired the `sleep_mode` param through `run_experiment_d` + CLI `--sleep-mode`. Erosion finding documented in the config docstring. **Behavior change:** default Exp-D training is now 150 ep (was 1000) — reproduce the old collapsed-vacuum run with `experiment_d.train_epochs=1000` (config/YAML) or `run_experiment_d(train_epochs=1000)` (no CLI flag for train_epochs on exp-d; `--quick` unaffected).
2. **Exp-D Noether guard** (`da18224`): `exp_d_goldstone.py` Noether block now guards `r* < 1e-6` — emits a `RuntimeWarning` and sets `Q`/`Q_pred`/`noether_max_err` to NaN explicitly instead of the silent `/r*→0` NaN. (New `import warnings`.)
3. **Harness `mu_floor`** (`9631a64`): `mode_amplitude` docstring gains the ≈`10*sqrt(baseline residual μ²)` guidance (f32 flat-mode residual μ≈4e-8 > default 1e-8 → spurious latch inflation, v2-full-runs §5); `mu_floor` now a threaded argument of `perturb_and_track`. Default unchanged (1e-8) = zero behavior change.
4. **Adaptive-K spawning** (`a758617`): `friction_field.py` gains `add_hole`/`prune_holes` (K-resize via `eqx.tree_at`), `AdaptiveKState`+`init_adaptive_state`, and `maybe_adapt_holes` — spawn a hole at the weighted-mean **uncovered** persistent-hallucination locus (same energy gate as training) once decayed density crosses a threshold; prune holes below a strength floor. Config-gated `training.friction_field_adaptive_k: bool = False` (+ `_max_k`, `_spawn_threshold`, `_spawn_min_dist`, `_spawn_radius`, `_spawn_strength`, `_prune_floor`). `train.py` sleep-phase wiring; **structural edits reset the optimizer state** (leaf shapes move). Default False = zero behavior change.
5. **Compact-support horizon gate** (`f77fedf`): `FrictionField` gains static `gate ∈ {"sigmoid"(default),"compact"}`. `"compact"` = smoothstep on `clip((r_k−dist)/w,0,1)` → γ_φ **identically 0.0 beyond r_k** (closes the sigmoid tail-leakage retention gap). Config `training.friction_field_gate="sigmoid"` default; wired through `build_friction_field`. Default unchanged.
6. **Exp-C dream-rollout FDT wiring** (`b7fa9f7`): `run_dream_batch` now passes `noise_mode=config.training.langevin_noise` into `stochastic_rollout`. Default `"legacy"` = zero behavior change.
7. **Wang arXiv-id reconciliation** (no code — see "Findings" + "Proposed handover updates").
8. **Config round-trip smoke as permanent test** (`cd99a02`): `test_config.py` gains a default-config full `asdict` round-trip and a **mutated-every-group** round-trip (flips a bool/numeric in every CHLUConfig group, asserts full `asdict` equality — a group dropped from save/load reverts and fails). `608cf43` = ruff-format fixup for the item-4 additions.

## How I verified (real outputs)
- **Full suite (my branch, excluding the untracked concurrent `tests/test_wormhole.py`):** `146 passed, 1 skipped` (98 s). Baseline post-w3 = 136 ⇒ **+10 new tests** (compact ×3, add/prune+adaptive ×5, config ×2). No failures, no NaNs.
- **Item 4/5 targeted:** `pytest -k "adaptive or add_and_prune"` → **5 passed**; `-k "compact or build_friction"` → **4 passed**.
- **Item 4 end-to-end wiring** (`train_chlu` with `adaptive_k=True`, buffer seeded with structured garbage at [3,3], lone K=1 hole at origin): `final K = 2`, spawned hole `centers=[[-0.06,-0.06],[2.997,2.996]]` — spawn + `opt_state` reinit path clean, training continued without error.
- **Item 5:** `test_compact_gate_exact_zero_beyond_cutoff` asserts `float(field(q))==0.0` bitwise at/beyond r_k (and sigmoid leaks positive there).
- **Item 8:** `test_config.py` 5 passed incl. the mutated-every-group round-trip (covers all groups present in the working-tree config, including the concurrent `experiment_v1_wormhole`).
- **Item 7:** `curl` to arxiv.org resolved both ids (this machine has internet; login-node caveat N/A here):
  - **2606.24945** — Wang, Hongbo, *"When Do Conservation Laws Survive Learned Representations? Certified Horizons for Latent World Models"* — certified horizons on the **decoded physical invariant**; shell-horizon certificates decomposing into representation/readout budget. → relevant to **V3** certified-conservation-under-learned-representation.
  - **2606.24946** — Wang, Hongbo, *"Conformal Orbit-Valid Trust Horizons for Equivariant World Models"* — trust-horizon certification for latent world models with **known group symmetries**; split-conformal multiplicative factor (audit γ_α=1.0; 0/50 anti-conservative violations, exact-binomial 95% UB 5.8%). **This is the Mo-citer** (equivariant → V2+V3).
- `ruff check` clean on all changed files; `ruff format --check` clean on the files I authored (see concurrency note re: shared-file pre-existing drift).

## Findings / results
- **Item 7 verdict:** both ids are **real, distinct papers, same author (Wang, Hongbo)**. The handover reference `2606.24945` is a *different* paper from the Mo-citer `2606.24946`. Both are relevant; cite `24946` (equivariant conformal trust horizons — the Mo-citer) for V2/V3 and `24945` (certified conservation under learned representations) for V3. Neither is a red-alert scoop; both are certified-horizon/trust-horizon works adjacent to our budget-table/certified-horizon claims.
- **Item 4 mechanism note:** in a full `train_chlu` run the seeded garbage was diluted in the default 10k buffer, so batches rarely contained the locus and no spawn fired (K stayed 1, gracefully). Spawning fires when the persistent-hallucination locus actually appears in sampled negatives (verified with a garbage-dominated small buffer). The unit tests exercise `maybe_adapt_holes` directly and decisively.

## Git footprint
- Branch **`agent/experiment-engineer/fix-pack-3`** off `main` @ `b1782b0`. **8 commits** (all `[experiment-engineer]`-tagged), listed above. Merge-base = current `main` (no rebase needed; main unmoved). Not pushed, no PR.
- Files (net vs main): `chlu/config.py` (+115*), `chlu/core/friction_field.py` (+171), `chlu/training/train.py` (+41), `chlu/experiments/exp_d_goldstone.py` (+57), `chlu/experiments/exp_c_dreaming.py` (+9), `chlu/experiments/goldstone_harness.py` (+15), `chlu/cli/experiment_cmd.py` (+5), `tests/test_config.py` (+38), `tests/test_friction_field.py` (+189).

### ⚠ Concurrency note (cross-agent collision — REPORT, did not clobber)
A **concurrent v1-wormhole-routing agent is editing the SAME working tree without a worktree.** Its uncommitted/untracked work is present in the tree: modified `chlu/cli/experiment_cmd.py`, `chlu/experiments/__init__.py`, `chlu/utils/plotting.py`; untracked `chlu/experiments/exp_v1_wormhole.py`, `tests/test_wormhole.py`. **I left all of these untouched** (not staged, not reverted).
- **Contamination:** my commit **`a758617`** (item 4) ran `git add chlu/config.py`, which by then also carried that agent's uncommitted **`ExperimentV1WormholeConfig`** additions (the dataclass + CHLUConfig field + load/save entries, ~90 lines). So `a758617` contains ~90 lines that are **not mine**. (The `friction_field_adaptive_k` block in the same diff *is* mine.)
- **Why I did NOT rewrite it:** (a) the block is a **self-contained dataclass** — imports nothing, breaks nothing; my branch is coherent and green standalone (verified: HEAD `experiment_cmd.py`/`__init__.py` have **no** wormhole refs, `exp_v1_wormhole.py` is untracked in my branch). (b) That config work now lives **only** inside `a758617`; surgically removing it risks clobbering the concurrent agent's / Pratik's (the live commit author) work. (c) This is the **same "identical-content dedup" situation the Hub already handled in w3** (triple-@dataclass fix merged cleanly). Per protocol §3.5/§3.7 (unsure/out-of-scope ⇒ report, don't guess), I report rather than rewrite.
- **Hub action at integration:** when the v1-wormhole branch lands, its identical `ExperimentV1WormholeConfig` addition will dedup against `a758617`'s copy (as in w3). If the two versions differ, expect a `config.py` conflict localized to that block — resolve by keeping the v1-wormhole agent's authoritative version. **Root cause / prevention:** concurrent `chlu/`-editing agents must use `git worktree` (protocol §3.2); this collision is evidence the wormhole agent was spawned into the shared tree.

## Open questions / follow-ups / risks
- Adaptive-K defaults (`spawn_threshold=5.0`, `prune_floor=0.02`, `spawn_radius/strength=0.5/0.15`) are first-guess; an analyst sweep on the S1 setup would tune them and confirm the spawn beats the fixed-K=4 arm (gamma-field-build follow-up).
- Compact gate: with `gate="compact"` an oracle hole should now reach exactly the γ=0 retention ceiling (the tail-leakage gap the S1 pilot found); worth an S1 re-run arm.
- `exp-d --quick` is still not wired in `cmd_exp_d` (pre-existing; out of scope for this pack).
- Item 4 opt_state reset drops Adam moments on each structural change — acceptable (spawns are rare); noted in code.

## Proposed handover updates (for the Hub)
- **§7 / §7.14 (Exp-D erosion) — partially addressed by fix-pack-3:** default `experiment_d.train_epochs` now **150**; `experiment_d.sleep_mode="off"` gives the wake-only data-pinned regime; Noether metric guarded at r*≈0 (NaN + warning); harness `mu_floor` guidance documented + threaded through `perturb_and_track`. Reproduce the collapsed-vacuum run with `train_epochs=1000, sleep_mode="on"`.
- **§3 config (new knobs, all default = zero behavior change):** `experiment_d.sleep_mode` ("on"); `training.friction_field_gate` ("sigmoid"; "compact" = exact-zero-beyond-r_k); `training.friction_field_adaptive_k` (False) + `_max_k`/`_spawn_threshold`/`_spawn_min_dist`/`_spawn_radius`/`_spawn_strength`/`_prune_floor`. CLI `exp-d --sleep-mode {on,off}`.
- **§2/§4:** `friction_field.py` API grows `add_hole`/`prune_holes`/`AdaptiveKState`/`init_adaptive_state`/`maybe_adapt_holes` (adaptive-K) and the compact gate; `train_chlu` sleep phase does config-gated adaptive-K spawn/prune with optimizer-state reinit on structural change.
- **§7.9 residue:** Exp-C dream rollouts now honor `training.langevin_noise` (`fdt` selectable end-to-end; default legacy) — closes the generative-studies code-gap 1.
- **Wang id correction:** the handover's V3 reference should distinguish **2606.24945** (Wang — certified horizons for *latent* world models; V3) from **2606.24946** (Wang — *equivariant* conformal trust horizons; the **Mo-citer**; V2+V3). Both verified real via arXiv abstracts (quoted in this report). Reconcile the §10 (2026-07-06 night) note that flagged "2606.24945 vs 24946".
- **New §7 issue candidate — cross-agent shared-tree collision:** a concurrent v1-wormhole agent edited the shared working tree (no worktree); its `ExperimentV1WormholeConfig` got swept into fix-pack-3 commit `a758617`. Harmless (self-contained dataclass, dedups at merge like the w3 triple-fix) but flags a **process gap**: enforce `git worktree` for parallel `chlu/`-editing spokes (protocol §3.2).
