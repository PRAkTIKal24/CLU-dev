# r2-geometry-revival — experiment-engineer report (w25)

**Task + acceptance criterion:** Stage 0 = the free `lattice-capacity-theory` §5.0 trained
`log_width` dump (adjudicate the geometric vs operator account of the `K_learned` ceiling);
if it survives, Stage 1 = the one-flag revival `atom_init_width 0.30 → 0.15` with a
`2^d`-predicted effect; Stage 2 = the d-sweep figure, conditional on Stage 1 moving the wall.

**Status: IN PROGRESS — this file is being written as runs land.**

## ⭐ DIAL DECLARATION (protocol §7, echoed)
- **Dial:** capacity (the R2 law) — a law about the primitive; never framed as beating
  anything (CM-23(m)).
- **Laundering control:** the designed write at matched geometry.
- **Falsifies:** §5.0 — trained widths < ~0.18 or minsep/width varying >2× across d.
  §5.2 — no movement of the wall at ≥3 seeds under an adequate budget.
- **Does NOT falsify:** failing to reach the designed `4·2^d` (the 4× prefactor gap is
  expected); any comparison to kNN or external methods.

(placeholder — full report written at completion)
