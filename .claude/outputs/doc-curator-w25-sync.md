# doc-curator-w25-sync — doc-curator report

**Task + acceptance criterion:** synchronise ALL six transfer docs to wave-25 — nine new negatives (N100–N108), five dated amendment blocks, matrix → v2.4, roadmap → v0.8, the ledger's ⟲ w25 addendum, primer records, the `future_work` fold — plus the new standing §7 dial-declaration compliance audit.
**Status: done.** All six docs edited; no tracked code touched; no branch (nothing to commit — every artifact is under gitignored `.claude/`).

## ⭐ DIAL DECLARATION (protocol §7, echoed)
- **Dial:** none — documentation pass. This task moves no scientific claim; it records w25's rulings and numbers verbatim.
- **Falsifies / does NOT falsify:** N/A (no measurement).

---

## 0. ⭐ THE §7 COMPLIANCE AUDIT (new standing duty, addendum §A4) — **5/5 PASS**

Audited every task file created since the last curator pass, against the §7 requirement (a **DIAL DECLARATION** block at the top carrying **dial · laundering control · falsifies · does-NOT-falsify**).

| task file (w25) | block present | dial | laundering control | falsifies | does-NOT-falsify | echoed in the report? |
|---|---|---|---|---|---|---|
| `r2-geometry-revival.md` (L6–10) | ✅ | capacity (R2 law, exempt per CM-23(m)) | designed write at matched geometry | ✅ per-stage (§5.0 widths <0.18 / ratio >2×; §5.2 no movement at ≥3 seeds) | ✅ (the 4× prefactor gap; kNN comparisons) | ✅ report §"DIAL DECLARATION" |
| `cl-entry-build.md` (L6–10) | ✅ | **all four** | kNN-in-φ at matched memory, **two forms** | ✅ (losing the replay-free class; being matched by the launder; flat retry) | ✅ (below replay/GDumb/iCaRL — CM-23(n); oracles) | ✅ + an explicit "outcome against the declaration" line |
| `lattice-sharded-store.md` (L6–10) | ✅ | capacity + isolation | monolithic store at same `K_total`, matched atoms | ✅ (neither d=6 nor d=8 fixes its wall; Prop L4 degradation) | ✅ (d=4 K=32 still failing; per-offered abstention under undersized geometry) | ✅ |
| `order-independent-placement.md` (L6–10) | ✅ | lifetimes + admission | TTL-dict / kNN-row-delete **claim-structure** comparison | ✅ (an impossibility proof — *and it is stated as a valuable outcome*) | ✅ (overflow eviction staying order-dependent; cost vs refuse-and-relocate) | ✅ |
| `mia-decay-measurement.md` (L6–10) | ✅ | lifetimes | CLU-with-a-TTL-flag **+** a TTL vector-store | ✅ (distinguishability not decaying with amplitude) | ✅ (distinguishability outliving retention — *"a finding either way"*) | ✅ + an explicit verdict-on-the-falsifier line |

**Result: the Hub's reading is confirmed — all five comply, and all five spokes echoed the declaration before their first result.** Two quality observations worth keeping as the house standard:
1. **Three of the five declarations pre-named an outcome that then occurred** (`mia`'s "distinguishability persisting longer than retention is not a falsifier" — it happened and is now the headline; `order-independent-placement`'s "an impossibility theorem is acceptance too" — the opposite happened; `lattice`'s "the d=4 control still failing is *supposed* to happen" — it did). **This is the governance fix doing exactly what w24 designed it for: no verdict in this wave could be scored against an axis the declaration excluded.**
2. `cl-entry-build` and `mia-decay-measurement` both added an **"outcome against the declaration"** line to their reports. **Recommend the Hub make that line mandatory** — it converts the declaration from a preamble into a scorecard at zero cost.

**Bonus audit (not asked for; the w26 files were on disk):** all **7** w26 task files + this curator task also carry compliant blocks (`placement-landing` L6, `cl-encoder` L6, `matched-bytes-frontier` **L21**, `r2-excursion-reach` L7, `readout-channel-theory` L7, `deletion-prior-art` L6, `carried-remeasurements` L7). ⚠ **One cosmetic deviation for the Hub:** `matched-bytes-frontier`'s block sits at **line 21**, behind a 14-line "why this task exists" preamble — it satisfies the *content* requirement but not the literal *"every task file **opens** with"*. Flagged, not corrected (task files are not mine to edit).

---

## 1. `negative_results.md` — nine entries, six dated amendments, three structural additions

**Nine new entries registered (full schema: tried · verbatim numbers · mechanism · scope · disposition), plus nine index rows:**

| # | one line | tier |
|---|---|---|
| **N100** | the learned-capacity ceiling is a **read-out REACH condition**; width route to R2 CLOSED; `sep/w_atom` a perfect d≥4 correlate but not the cause; ⛔ no `K_learned` at pscale≠1 | A ✦ |
| **N101** | the CL entry **wins the rehearsal-free class on Split-MNIST (+0.510) and is LAUNDERED (−0.036)**; deficit −0.153; the store's axis is forgetting (BWT −0.169, 24.5× fewer floats) | A ✦ |
| **N102** | **Split-CIFAR-10 is a NULL** (0.149 < LwF 0.162) — the **feature space**, not the discipline (strict-φ cost +0.001) | A ✦ |
| **N103** | **first non-losing anytime read — a TIE** (+0.8±1.6 pp, 6 seeds) in the one regime with no constructible oracle; ⚠ τ not binding in φ-space (1 seed) | A ✦ |
| **N104** | **R3 (settling-displacement) routing is BROKEN** on the shipped flat-vacuum geometry (1.000/0.549/0.236/0.228 vs R2's flat 1.000) | A ✦ |
| **N105** | sharded additivity real on the **write** side, partly spent on the **read** side; d=6 K=64 fixed; **`K = 32N` NOT unclamped**; ~60% of the d=6 deficit is routing | A ✦ |
| **N106** | the d=4 K=32 wall is **geometry**, confirmed routing-free (0.89–0.90, does not clear 0.90) | B ✦ |
| **N107** | the w23 atom floor is **per-STORE** ⇒ parameter-matched sharding is auto-starved; N≥4 collapses are not capacity results | B ✦ |
| **N108** | **decay buys nothing against an exact adversary** (white-box AUC 1.000 at all 18 levels); the graded curve is resolution-relative or geometric (`R₅₀` 1.146→0.752) | A ✦ |

**Tier disposition: I CONCUR with all nine Hub-proposed tiers and propose no re-tier.** Rationale written into a ✦ tier-flag block under the index (A = overturns an entry/approved wording, imposes a program-wide quotation rule, or moves a claim-gate; B = scope/design guards superseding no prior entry — the N92/N93 and N98 precedents). **One wording judgement flagged for review:** N103 is filed as a **"not-a-win" entry rather than a negative**, so the tie is quotable *only* as a tie; if the Hub prefers it live only as CM-23(r), the registry row can be re-labelled without moving a number.

**Six dated amendment blocks — nothing rewritten, nothing deleted:**
1. **N92** — `✅⭐ WAVE-25` block: the contest is **SETTLED with a third answer**; §5.2 REFUTED BY EXPERIMENT; §4.2 correlate-only; N98 recorded as a **second, additive** contribution; **every measured number in N92 stands** — only the attribution moves; ⛔ both "the ceiling is the write operator" and width-lock-as-cause retired.
2. **N96** — companion block: **the CONTESTED flag is CLEARED**; **N96's levers-that-fail wording is explicitly left as written** (it was right); only the attribution sentence changes; the `×2^d` prediction is dead.
3. **N97 — EXTENDED** (per instruction): original text untouched; the extension records R3 broken on the shipped geometry with its mechanism (owning ‖∇V‖ 2.9e-4 vs foreign 2.3e-3, energies differing 4 orders), narrows the recommendation to **R2 + the classical registry**, and generalises the rule to *"the routing statistic must be evaluable without running the dynamics."* **N97's own numbers are explicitly preserved** with the scope reconciliation (analytic Gaussian + gradient flow vs the shipped flat-vacuum potential).
4. **N98** — w25 measurement + **NOT-YET-a-default** note (+0.051 / +0.082, router 0.913→0.970, ships OFF and bit-identical when disabled, address-axes-only *and why*); tier **B kept** (the *lever* status is recorded, not re-tiered — Hub may re-tier).
5. **N99** — a single block with **both** updates: **(a)** the magnitude (`AUC(z_hole) = 0.99985 ± 0.0007`, TPR 1.000@1%, with the **7/8-occupancy caveat**, and the paired-placement 0.5000 ± 0.0000 contrast) and **(b)** PGCP delivered (Thms 1–4, the 0-tolerance permutation evidence, the H7 byte-equality on the shipped PyTree, the **negative** packing price, churn 2.84) with **the three blockers and the two quotation guards**. N99 stays **registered and open**.
6. **N91** — ⚠ CORRECTION block: the decay-demo *"leaky retention 1.00 through 8 ticks"* is a **best case** (vacuous 1-element basin + the most decay-robust payload); full-basin 8-value **0.886 ± 0.19 at A=0.0608, 0.500 at `a=+1`**; **permanent row unaffected**; corrected wording supplied for every quoting site.

**Structural additions:** a **w25 paper-writer family block** (three sets — capacity · CL · lifetimes/deletion — each with what must travel and what is forbidden); a new **cross-thread entry** (*"the free check that authorised the experiment that killed it"*, plus the pre-registered falsifier that fired to 4 dp); **six new provenance flags** (tier concurrence · seed/scope exposure · the occupancy caveat · unverified load-bearing citations · the N97↔N104 scope tension · the report↔ruling separation on the CL win · the three shipped-code defects); and a w25 paragraph at the head of the "Maintained by" block.

## 2. `claims_matrix.md` → **v2.4**
- **New version banner** (five items, three of them supersessions), with the standing note that **nothing in v2.4 moves the score sentence.**
- **CM-22 gains six forbidden claims (p)–(u):** decay-reduces-distinguishability *per se* · "the item is gone after evict" **+ the blanket block on every deletion-flavoured sentence until N99's three blockers clear** · "0.953" as measured retention · **"the ceiling is the write operator" AND width-lock-as-cause** (with the `pscale≠1` never-quote) · "capacity multiplies by sharding" · "d=8 K=64 = 0.883 FAIL" and "leaky retention 1.00 through 8 ticks" unqualified.
- **CM-23 gains four approved wordings (q)–(t):** the **CL-entry block** (three sentences that always travel + the CIFAR scope clause + the Head's supplementary-only ruling) · the **R3-native TIE** wording (*"ties the honest floor in the one regime with no constructible oracle"* — never "wins") · the **settled ceiling mechanism** (explicitly superseding (h)'s and (m)'s attribution clauses) · the **measured sharding verdict** for (p)'s Prop L2.
- Evidence/scope columns of both rows extended with the five w25 sources and their **per-claim seed counts**; the CM-23 scope cell's *"the ceiling-mechanism is CONTESTED"* is struck and replaced.

## 3. `research_roadmap.md` → **v0.8** (the deferred status refresh)
New top block: a supersession banner (v0.8 refreshes **statuses only**; v0.7's spine, rulings and laws carry forward) · the **new standing Head ruling** (no direction closed as lost; win-by-construction = supplementary only) · the **contested-win strategy §B2** (three candidates) · **two pinned-law amendments** (the reach attribution; the sharding clause `K_total = min(K_addr, Σ_r K_write)`) · a **refreshed per-R table (R1–R5)** replacing v0.7's status column · the **w26 wave table** with its compute-contention warning and spawn order · **three new standing cautions** (NOT-RUN ≠ null · additive mid-sweep instrumentation must re-run a cell · the curator's §7 audit as a standing duty). The v0.7 heading is annotated, not edited.

## 4. `philosophy-synthesis.md` — the ⟲ wave-25 addendum
**The Hub had NOT written a w25 addendum** (the file ended at w24 part 2), so this is the wave's primary ledger record, written to the ⟲ protocol: chapters **not** rewritten; Chapter 8's §-bodies untouched and moved by addendum; one dated ⟲ note appended under the w24 gap list (five of its nine owed items discharged).
Contents: provenance & scope · **the wave in one arc** (the four events and their rhyme — *the honest instrument was built before the claim, and three of four produced a result its own author pre-registered against*) · **dial-by-dial deltas** (capacity ×2, dial 4, dial 2/R1, epistemics) · **chapter deltas** (Ch. 8 dials move, **verdict unchanged**; dated pointers on Ch. 5 and Ch. 7; Ch. 1–4/6 untouched) · **scorecard deltas** (row 8 current-state; row 7 + three exemplars) · **gap-list updates with every superseded number NAMED** · **eight doc-hygiene flags**.

## 5. `HEP_primers.md` — one new section, three in-place updates
- **NEW §11.15 — "Three pictures from wave-25"**, in the §11.14 house style (picture → symbols → clause-by-clause evidence table → boundary → *"→ CLU, said plainly"* → status + reading pointers): **Record 3 — Reach** (*a narrow deep well is invisible from far away; the read launches from payload 0*), **Record 4 — The parking rule** (*painted bays + fixed ticket numbers; any arrival order parks the same cars in the same spots*), **Record 5 — Answering vs leaking** (*the store stops answering before it stops leaking*). Imagery intact; every clause carries its measurement; every record carries its ⛔ boundary.
- **§11.14 Record 1 — the fourth clause RESOLVES.** The w24 CONTESTED block is answered in place with a dated update and an **approved replacement for the record's closing enumeration**; the Head-authored blockquote itself is **untouched** (§11.14's rule).
- **§11.6** — dated w25 update: the contest resolved; three numbers in the section move (d=8 K=64 is not a wall; sharding fixes d=6; `K = 32N` not unclamped); the `pscale≠1` never-quote added.
- **§11.12** (controller) — dated update: the **D2 correction** to the decay-demo retention row (with the permanent row explicitly preserved) + the controller's first live-stream accounting (**budget removes 6 492, the schedule removes 71 — ≈91×**).
- **§11.13** (test-time compute) — dated update: the R3-native **TIE**, the two scope items, the rebuilt φ-space control, and the *no-forgetting-by-age* inversion.

## 6. `future_work.md` — six items marked `→ SHOWN`, nine new boundaries folded
`→ SHOWN` (kept with provenance, never deleted): the width-lock free check (**both accounts lost**) · the §5.1 2×2 sharding experiment · the N98 localized init (**built; ⛔ not a default**) · the not-metric-native retrieval regime (**a tie**) · the order-independent placement rule (**theory+reference; landing is new-OPEN**) · the partially-decayed distinguishability measurement · Split-CIFAR-10 φ-stream (**expectation refuted**) · the w23 CL-entry item · the w23 write-ceiling item.
**New w25 fold (nine entries, schema-complete):** the fair excursion experiment **with its five fairness conditions** · the joint init × width factorial · a CIFAR-capable φ **with its decisive gate and its by-construction trap** · the forgetting-vs-**bytes** frontier · PGCP's landing + rematch (**the acceptance criterion is a number**) · the occupancy sweep · the D3 payload-dependent-lifetime fix (**theorist ruling first**, incl. Advisor-2's calibration option) · the multi-seed τ check · LwF re-tune + three thin-seed re-checks · two unowned small boundaries (the designed arm at half excursion; `permanent_per_task`'s price curve) · the deletion prior-art/positioning debt.

---

## What I deliberately did NOT fold (and why)

1. **I did not resolve the N97 ↔ N104 R3 result difference** — filed as an **extension** (which is what both reports and their scopes support) with an explicit flag that a reader of N97 alone must not ship R3. *If the Hub wants it filed as a contradiction, that is a Hub call.*
2. **I did not merge the CL measurement with the Head's filing ruling.** `cl-entry-build` explicitly handed the score-sentence question up; the Head ruled NO. Both are recorded, **separately**, in N101, CM-23(q) and the ledger.
3. **I did not promote N98 to a default, re-tier it, or soften "ships OFF"** — the Head ruled *not yet* and the factorial is w26's Stage A.
4. **I did not touch the Head-authored blockquotes in §11.14** (imagery is the deliverable); the resolution lives in curator annotation around them.
5. **I did not restructure Chapter 8** despite it now carrying three addenda over unrevised §-bodies. Recorded as a **recommendation with a trigger** (a §5 restatement after w26 lands), in the spirit of the w19–w23 refusals: a curator does not reorganise a document whose structure encodes the program's beliefs.
6. **I did not register a separate N-entry for D1/D2/D3.** D2's *consequence* is folded as a correction to N91 (it moves a quoted row); D1 and D3 are recorded inside N108's scope/exploratory blocks and in the §7-CURRENT list below. **If the Hub wants D3 (payload-dependent lifetimes) as its own tier-B entry once the theorist rules, that is a clean future registration** — I did not pre-empt the ruling.
7. **I did not quote any `K_learned` at pscale ≠ 1, or "0.953", or "1.08", anywhere** — and I added both to the never-quote lists in two docs.

## §7-CURRENT defects — for the Hub (handover is Hub-owned; listed, not edited)

| id | where | what | moves a published number? |
|---|---|---|---|
| **D1** | `chlu/core/memory_potentials.py`, `AtomStorePotential.evict` (L1017–1027) | clears `active`/`amps` only — `centers[slot]`/`payloads[slot]` retain address and value **verbatim** (max err 5.62e−8 / 0.0 over 3 072 evictions). Fix: also zero both rows | **No** — `V` multiplies both by `active`. It changes what *"the slot is freed"* means (wording, program-wide) |
| **D2** | `chlu/experiments/exp_controller_mvp.py`, `item_decay_demo.retention_of` | passes a **1-element** `sites` array ⇒ `basin_of`'s argmin is vacuous; and probes the codebook's most decay-robust payload. Fix: score against the full live-site set, average over several `leaky_id`s | **YES** — `controller-mvp` §3(b) / N91's decay row: **0.886 (8-value mean), 0.500 at `a=+1`**, not 1.00. **Folded as an N91 correction this pass** |
| **D3** | design-level: the payload term + the `q₂(0)=0` guard | a decayed site is a payload **hill** `0.5κa_i²` against a well of depth `A` ⇒ effective lifetime correlates **r = −0.85 with `a_i²`**. Candidate fixes: scale the bump by `amps`; launch at `q₂ = S(q_addr)`; or the calibration option (user sets half-life, store solves the leak) | **Conceptually** — it is what makes the lifetimes dial payload-dependent. ⛔ **Theorist ruling before any potential change** (`readout-channel-theory` Q3) |
| **(w24 carry)** | `chlu/experiments/exp_retry_compute.py`, `_feedforward_ladder` | `clip(\|q+noise\|,0,1)` TTA is valid for pixels, **destructive for signed φ vectors** (the "floor" collapsed to ~0.005 with k). Corrected control `_feedforward_ladder_phi` exists in `exp_cl_entry.py` | **No published number**, but **any future φ-space use of `exp_retry_compute` is invalid without it** — recorded in N103 |

## Gaps / risks flagged for the Hub

1. **⚠ The wave's most misquotable sentence is its most important one.** *"Halving the read-out excursion takes the w23 wall from 0.824 to 1.000"* will be read as a capacity result by anyone skimming. It has **three guards** (no `K_learned` at pscale≠1 · the noiseless-payload caveat · the successor's fairness conditions) and I have made all three travel in **every** doc — registry, matrix, roadmap, ledger, primer. **This is the "2^d exactly the designed rate" problem one wave later with higher stakes; recommend the Hub check it at every draft review.**
2. **⚠ Seed exposure is concentrated in the most important result.** N100's decisive cell is 3 seeds at 28 SE, but **Stage 0 is 1 seed per cell** (the perfect classifier and the 2.30–2.49 boundary have **no error bars**) and the excursion cells are **1 seed, written-only, no blank control**. The verdicts are robust; the *digits* are thin, and the Stage-0 table is the one most likely to become a figure.
3. **⚠ Two headline numbers carry scope that must not be lost:** `AUC(z_hole) = 0.99985` was measured at **7/8 occupancy** (it scopes both a headline and `placement-landing`'s acceptance criterion), and the d=4 K=32 RG band (**0.89–0.90**) sits closer to the bar than pre-registered.
4. **⚠ PGCP's novelty sentence rests on unverified citations.** Load-bearing and model-knowledge-only; the scout gates the wording, not the build. The *expected* finding (the discrete skeleton **is** prior art) is on the record in advance in three docs.
5. **⚠ Chapter 8 is becoming expensive to read** (three addenda, unrevised §-bodies). Trigger-based recommendation recorded in the ledger; **Hub call.**
6. **No output↔handover numeric disagreement found this pass.** Every headline in the Hub's §10 w25 entry reproduces digit-for-digit against the five reports; the two places where the handover compresses (the CL "wins" clause, the excursion clause) both carry their qualifiers in the same sentence. **Nothing to adjudicate.**

## Git footprint
**None.** No tracked file created, modified or deleted; no branch, no commits. All six edited documents live under gitignored `.claude/` (`negative_results.md`, `claims_matrix.md`, `research_roadmap.md`, `future_work.md`, `outputs/philosophy-synthesis.md`, `outputs/HEP_primers.md`) plus this report.

## Proposed handover updates (for the Hub)
1. **Doc state after this pass:** `negative_results.md` **through N108** (tiers HUB-PROPOSED + curator-concurred; N91/N92/N96/N97/N98/N99 carry dated w25 blocks) · `claims_matrix.md` **v2.4** (CM-22 +(p)–(u), CM-23 +(q)–(t); the CONTESTED flag struck) · `research_roadmap.md` **v0.8** (per-R refresh + w26 table) · ledger: **Chapter 8 open + ⟲ addenda w24 ×2 and w25** · primers **through §11.15** (+ §11.6/§11.12/§11.13/§11.14 dated updates) · `future_work.md` **w25-folded** (six `→ SHOWN`, nine new boundaries).
2. **Confirm or re-tier N100–N108** (curator concurs as proposed) and rule on the one wording question: **N103 as a registry entry vs CM-23(r) only.**
3. **Consider making the "outcome against the declaration" line mandatory** in every spoke report — two w25 spokes did it unprompted and it converts §7 from a preamble into a scorecard.
4. **Cosmetic §7 item:** `matched-bytes-frontier.md`'s DIAL DECLARATION is at line 21 behind a preamble; the protocol says task files *open* with it.
5. **Not mine to fix, restated for §7-CURRENT:** D1 (wording + one-line fix) · D2 (engineer fix; the row it moves is already corrected in N91) · D3 (theorist ruling first) · the `_feedforward_ladder` φ-space trap.
