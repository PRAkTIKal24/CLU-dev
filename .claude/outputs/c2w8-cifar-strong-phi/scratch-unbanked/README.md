# UNBANKED SCRATCH — not a claim cell, never quotable

Recovered by the C2W8 Hub from the `c2w8-cifar-strong-phi` worktree before it was
removed (2026-08-06). These are the harness's DEFAULT output-path files from a
**single-seed (`seeds = [0]`) `randconv` working run**, not part of the wave's
measured grid:

* `exp_cl_entry_cifar10_metrics.json` — `clu_ACC = 0.195`, `clu_minus_launder = 0.0`, **n = 1**
* `cl_entry_table_cifar10.png`

⛔ **Not a result.** The wave's `randconv` arm is `../results/randconv_metrics.json`
(multi-seed, `clu_minus_launder = +0.0003 ± 0.0003`). A single-seed reading is not a
cell under this program's multi-seed rule, and the C2W4 rescue-gate lesson (any gate
whose control carries learned-init variance is underpowered at n=3, let alone n=1)
applies directly to encoder inits.

Preserved rather than deleted so the worktree removal destroyed nothing.
