# doc-curator-c2w7-fold — doc-curator report

**Task + acceptance criterion (one line):** fold C2W7's read-track findings into the registries per charter **ADDENDUM 8 §A26** + **ADDENDUM 9 §A27.2/§A28.4** — the six folds, the re-scoping verified absent in its old form, K6 + the zero-parameter-member rule registered, the two retroactive `S_eff` re-labels applied, the cat-test SEEN footnote added, matrix version bumped.
**Status: done.** All six folds landed. ⚠ **Two disclosures lead this report: (1) a concurrent-curator collision with `doc-curator-c2w6-fold-2` (it claimed `N231` mid-pass; I shifted to N232–N240 and touched none of its edits); (2) I added dated curator banners to THREE spoke reports** (`c2w7-read-cardinality`, `tierii-read-fix`, `orgdiv-cat-test`) **because fold 1's *"must not survive anywhere"* and the acceptance item *"the two retroactive re-labels applied wherever the old wording appears"* cannot otherwise be met — every other live site is charter/handover, outside curator scope.** Bodies untouched (C-3 precedent, ratified at C2W6 for `pilot-placement-probe`). **Revert-safe: each banner is a single appended block.**

## ⭐ DIAL DECLARATION (protocol §7, echoed before the first result)
- **Dial:** ⛔ **none — registry/transfer-document fold.** No claim is made, no verdict reinterpreted, no number re-derived.
- **Laundering control:** every number is transcribed with its source report + section; where a report and the Hub/Advisor disagree I record the Hub's verified figure **and** name the report's as wrong (the 6.9 vs 7.9173 case), never silently.
- **Falsifies:** a registry line that cannot be traced to `.claude/outputs/*`, the charter or a §10 entry.
- **Does NOT falsify:** the tier-ii verdict remaining UNREACHED — that is §A26.1's ruling, not a curator finding.

## Sources read (in protocol order)
`AGENT_PROTOCOL.md` → `handover_context.md` §10 (the 2026-08-05 `[C2W7]` spoke-review entry with the four-leg gate table + the three escalated reconciliations; the 2026-08-06 `reader-fitting-audit` accept entry + integration record) → `.claude/outputs/c2w7-read-cardinality.md` (full) → `.claude/outputs/reader-fitting-audit.md` (full) → charter **ADDENDUM 8 (§A24/§A25/§A26)** and **ADDENDUM 9 (§A27.2/§A28)** → the task file → the target registries + `doc-curator-c2w6-fold.md` (house style, and to establish fold-2's live scope).

## How I verified the things that were checkable
1. **The re-scoping grep (acceptance item).** `rg -U "may have been scored through a reader that destroys its own signal"` and `"destroys its own\s+signal"` over `**/*.md`: ⭐ **zero live assertions remain.** Surviving occurrences are **all** either (a) the forbidden form quoted *in order to correct it* (charter §A27.2, handover ×2, my own `negative_results.md` ×2 / `claims_matrix.md` ×3 never-quote lines) or (b) the two spoke reports — **and both of those now carry a dated banner striking it** (`c2w7-read-cardinality` original; `reader-fitting-audit` already quoted it only to correct it).
2. **N-number collision check.** `rg "^### N23|^\| N23"` after the pass: `N230 → N231 (fold-2) → N232…N240 (mine)`, summary index in the same order, **exactly one N231, no gap.**
3. **`S_eff` old-wording sweep.** Live sites of *"16.00 … in band"* / *"16.77 COLLAPSED"* are: charter §A21/§A24, handover §10 tables, `tierii-read-fix.md` §1/§4/§8, `c2w7-read-cardinality.md` §3/§8. ⛔ **The first two are outside curator scope (flagged); the last two now carry the re-label in a banner.**
4. **Fold-2 liveness.** Detected via stale-file warnings on my first `negative_results.md` edit; confirmed by reading its header block (it registered N231 and is amending §0.10/CM-32/CM-33 in `claims_matrix.md`). ⛔ **I edited none of its lines and re-numbered myself rather than it.**
5. ⛔ **Nothing re-derived.** Every figure below is transcribed. The one arithmetic statement I *declined* to make: converting C2W5's `S_eff` 34.1/51.2/36.6 into wells-visited units — recorded as the Hub's/Advisor's to publish.

---

## 1. Edits, per document (every one, for diff-review)

### A. `.claude/negative_results.md` — the N-registry (**9 new entries, 4 dated addenda/footnotes, 1 NOT-RUN block**)

| # | edit | content |
|---|---|---|
| A1 | **header fold paragraph** (inserted after *"Maintained by doc-curator, per wave."*, newest-first) | the C2W7 fold summary; leads with **the wave's finding in its correct form** (expressivity repaired **and** entirely launch-side) and with **§A28.4's vehicle scope + the anti-alibi clause**; tiers marked **CURATOR-PROPOSED (✪)**; carries the **N-number collision disclosure** |
| A2 | **summary-index rows N232–N240** (inserted after N230's row; fold-2's N231 row landed between them — final order N230 → N231 → N232 … N240) | one line each with tier / vertical / source |
| A3 | **N232** (tier **A**✪) | expressivity repaired + entirely launch-side: `R1` per-seed + **0.065625 ± 0.023876**, mean−2SE **0.041749**, 28× · `G1` per-seed + **−0.007031 ± 0.001562**, 5/5 · designed head **0.0695 vs 0.0539** (R1 0.0883 vs 0.0656) · `F_hat` 4.542 ± 0.122 / set 4.194 ± 0.224 / exactly-`F` 0.394 ± 0.031 · ceiling 0.691 with its noise model · full flag-provenance block · ⛔ §A28.4 scope + the anti-alibi clause · the `p₀` 7.6× and weight/verb ablation riders |
| A4 | **N233** (tier **A**✪) | the scatter: **4.834 ± 0.125 → 5.668 ± 0.396**, precision 0.4578 → 0.4107, attractors **1.09 = ~1.4 `sep`** from anchors; the `3/3/3/3 → 3/2/3/2/2` mechanism; names §A26.6(a) as the lever with its **already-measured** success criterion |
| A5 | **N234** (tier **B**✪) | `k` 12→24 at 2× flops: **0.04296875 → 0.04296875**, coverage **0.234375 → 0.234375**; iteration 1's **+0.2383** refuted; the ledger guard stays live |
| A6 | **N235** (tier **A**✪) | `K0` reachability-only (collapsed head **1.000** / readers **0.0000** / precision 0.124) **+ the mean-level PASS** (0.9070 ± 0.0125; per-seed 3/5 clear); the `card_b` cost table (0.044 of exact-set) |
| A7 | **N236** (tier **B**✪) | `R2` blind by sign (settled 5.668 > launched 4.834; P7 above band); the standing lesson on inherited falsifiers |
| A8 | **N237** (tier **A**✪) | the reader-fitting pathology + **SURVIVES**: the four-cell `I − F` table, `null*` **0.001171875** unmoved with per-seed, `OD_min` −0.00078125, three bit-for-bit gates, `gated_well_identity` = published R1 per-seed, `diag(W)` 0.128–0.446, the two crossings with residual ratios **2.47× / 2.91×** and kept **3/0 · 2/0**, **K6** 2/2560 · 3/1280 · 0/2560 vs ~18 %, the **√2 R** derivation, ⛔ the refuted hope **−0.0015625 → −0.00234375**, the "tie at ≈ 0" qualifier, both standing preconditions, the engineer's shared-checkout self-report |
| A9 | **N238** (tier **B**✪) | two-directional lstsq instability: shrinkage list + **inflation `diag(W) = +46.53`**, residual **13.48 vs 1.84**; ⛔ residual ≠ "close"; ⚠ may be a **store** fact — owner needed |
| A10 | **N239** (tier **A**✪) | the `S_eff` arithmetic (`512/W`, `W ≤ 32`), per-seed 17.07/16.00/18.29/16.52/16.00 with wells 30/32/28/31/32; **both retroactive re-labels**; ⛔ COLLAPSED reserved for concentration; ⚠ no verdict moves; ⚠ **I decline to re-derive C2W5's numbers in wells-visited units** |
| A11 | **N240** (tier **B**✪) | M15 quiet 0/5 (`S_marg` 23.158 ± 2.655), regularizer ON/OFF inert (−0.90 / −0.0016), the designed negatives 4.000/1.000, ⭐ **the un-forced trip at 7.9173 with the report's 6.9 named wrong**, per-query **3.94** vs marginal **21.22**, the "#15" registry note |
| A12 | **N215 — dated C2W7 ADDENDUM** (body untouched) | the entry was put in question and **does not move**; `null*` unmoved with per-seed; ⛔ the never-quote + the binding replacement verbatim; ✅ Add.5 §A20 stands, no erratum owed; the honest "tie at ≈ 0" qualifier; K6 + zero-parameter-member registered on its own instrument |
| A13 | **N214 — dated C2W7 ADDENDUM** | both named revival requirements were **built**, and requirement (i) is **met** (`R1` clears, 28×) — ⛔ and it does not rescue the tier (`G1` 5/5); `K0`'s adoption gains its reachability caveat; ⛔ **the §A20.2 re-labelling is NOT re-opened** |
| A14 | **N213 — dated C2W7 ADDENDUM** | the same defect at a multiplicity head has the **opposite sign** (merge vs scatter), with both number pairs; ⛔ the two must never be quoted as one another; `R2` blindness noted |
| A15 | **N212 — dated C2W7 PROVENANCE FOOTNOTE** | the SEEN column is **unreproducible from artifacts** (`stage_arm.json` has no `seen`/`unseen` keys); cite **0.0094 / 0.0047**; ⛔⛔ **provenance footnote, NOT an erratum — no verdict moves**; + the identity-worse and inflation notes; + the `S_eff` note that this cell's COLLAPSED label **survives** as concentration |
| A16 | **new section: ⛔ DECLARED NOT-RUNs — C2W7** | the swap (and the deliberately-unused `--force-swap`) · `k = 16/24` as arms · a ψ A/B (no ψ exists) · N2–N5 swap arms + the robustness arm · the γ axis · `d ∈ {4,16,24}` · consolidate/trash (C2W8) · `pilot1/` · the discarded greedy-pursuit design · **and the audit's**: the full 584-config grid, no re-tuning/re-training/`Q_unseen` selection, no `ERRATA-TierII.md`, `PREREG-TierII.md` untouched · ⛔ **and the standing "no read iteration 3 exists"** |

### B. `.claude/claims_matrix.md` — **v2.13 (PROPOSED, pending Hub confirmation)**

| # | edit | content |
|---|---|---|
| B1 | **title line** | v2.13 prefixed, prior title line preserved verbatim behind it; **ADDITIVE ONLY**; ⚠ carries the **concurrency disclosure** (fold-2's `v2.12a` block deliberately left un-versioned by its author; I opened v2.13 for **my** additions only and left the fold-2/v2.12 merge call to the Hub) |
| B2 | **v2.13 version blockquote** (inserted **before** fold-2's v2.12a block) | the wave's finding in its correct form + the five-line "what moved": the §A27.2 re-scoping (with the refuted hope), K6 + the zero-parameter member, the `S_eff` retirement with both re-labels, §A28.4's no-full-CLU-verdict + anti-alibi clause + the paused read track, and the two new rows. **Score sentence restated unchanged.** |
| B3 | **NEW §0.11** (C2W7 never-quotes; permanent home) | the re-scoping and the vehicle scope as the two governing preambles; **8 wave clauses** (R1-without-G1 · K0-alone + mean-level PASS · `k`-doubling · upper-edge `S_eff` + both re-labels · the 0.691 noise model · **M15 ≈7.9 not 6.9** · `R2` blindness · fitted residual ≠ "close" · the cat-test SEEN provenance); **two standing preconditions** (K6; zero-parameter member); **two scope statements** (no read iteration 3; `depth_ratio` DEFERRED with numbers on both sides) |
| B4 | **NEW CM-34** | forbidden claims C2W7 — clauses **(rrrr)–(bbbbb)** in the file's letter-series style, each pointing at its N-entry |
| B5 | **NEW CM-35** | approved wording C2W7 — (a) the finding, never split · (b) the mechanism + the next lever · (c) the reader doctrine with its bound · (d) the instrument correction · (e) **the mandatory scope sentence** · (f) the monitor-first doctrine sentence |

⛔ **No existing matrix clause, wording, number or tier was changed by this pass** (fold-2's in-place amendments to §0.10/CM-32/CM-33 are **its** edits, untouched by me).

### C. `.claude/outputs/philosophy-synthesis.md` — the ledger (⟲ protocol)
- **C1: appended `# ⟲ C2W7 addendum`** — verified first that **the Hub wrote no C2W7 addendum** (the file ended at the C2W6 addendum's doc-hygiene item 6), so this is **C2W7's primary ledger record, not a `(curator supplement)`**. I also marked C2W6's item 6 (*"C2W7 is not in this addendum"*) **✅ DISCHARGED** with a pointer — the only C2W6-addendum line I touched, and it is an append to that line, not a rewrite.
- Contents: provenance/scope banner (incl. the **concurrency disclosure** and the ⛔ vehicle-scope block) · **the wave in one arc (6 beats)** · **chapter deltas** (Ch. 8; Ch. 4 loses an inherited claim; Ch. 3/Ch. 2 scope notes; Ch. 1/5/6 explicitly untouched) · **standing practices 15–19** (K6-before-you-fit · check what your instrument *can* say · metric-vs-fitting-criterion · inherited falsifiers · monitor-first, exercised un-forced) · **Ch. 7: three exemplars** · **scorecard deltas** · **gap-list updates with every superseded number and FORM named** · **6 curator-flagged doc-hygiene items.**
- ⛔ **No chapter body edited, no earlier addendum rewritten, nothing deleted.**

### D. `.claude/future_work.md` — the untested-extensions register
- **D1: appended `### ⭐⭐ C2W7 FOLD`** (after the C2W6 fold, before the waves-17–18 CAFE section): banner with both binding rulings (§A26.6 paused read track; §A28.4 vehicle scope + anti-alibi) · **6 status moves** on earlier items (the k-particle head **→ SHOWN** with all four guards · learned `p₀` **→ SHOWN and re-priced** · the organizer swap still **DEFERRED**, now *behind a write-side iteration* · the `S_eff` collapse fix **RE-SCOPED** — half of it was an instrument artifact · consolidate/trash **TASKED (C2W8)** · the `null*` convention **strengthened by audit**) · **11 new boundaries**, all schema-complete (*what-not-shown · why-it-matters · where-it-lands · status*).
- New boundaries: **attractor–anchor alignment** (the named lever, success criterion already measured) · **query-conditional store geometry / a family not written identically per query** · **K6 never yet reported prospectively** · does a zero-parameter member ever change a *future* verdict · the `sum_linear` inflation as a possible **store** fact (unowned) · a head-training recipe that **transfers** · ψ's marginal value at a multiplicity read (structurally out of scope here) · a two-sided wells-visited gate (**thresholds unset**) · the full 584-config re-score (`PARKED` — its registered trigger did not fire) · the deferred components **each with a dated wave and kill-conditions** (§A28.4(a)) · read iteration 3 (`PARKED` **by ruling**) · `depth_ratio` (`DEFERRED` by ruling).
- ⛔ **Nothing deleted; every provenance line kept; no item marked `→ SHOWN` without a report citation.**

### E. `.claude/outputs/HEP_primers.md` — the primer
- **E1: NEW `## 11.23` with Record 25 and Record 26** in house style (concept → math → CLU-specific connection → status tag → reading pointers).
  - **Record 25 — "The read that learned to count"**: the counting code, the gate table, the scatter table, the `m_j = F_hat·c_j/Σc` line with the `3/3/3/3 → 3/2/3/2/2` failure, the by-construction launder ceiling, and the two riders (`k` as resolution; monitor-first with 3.94 vs 21.22). Status tag carries the never-quote, the gate's G1-only substance, **the §A28.4 vehicle scope + anti-alibi clause**, and the paused read track.
  - **Record 26 — "The ruler that erased its own reading"**: the shrinkage-vs-threshold mismatch, the four-cell `I − F` + **K6** table, the crossing residual ratios, the `√2 R ≈ 2.96–3.02 × tol` derivation, and the two mandatory preconditions. Status tag carries **SURVIVES**, ⛔ *"SURVIVES ≠ the readers are fine"*, the tie-at-≈0 qualifier, and the refuted hope.
- **E2: dated `Update (C2W7)` blockquote** after **Record 20**'s status line: the precondition is met and `R1` clears; ⛔ the store still loses to its own launches; `K0`'s reachability caveat; `null*` survived.
- **E3: dated `Update (C2W7)` blockquote** after **Record 21**'s existing C2W6 update: the settle's failure **changes sign** at a multiplicity head, the attractor–anchor cause, `R2`'s blindness, the store-side consequence.
- ⛔ **No existing record body, number or status tag was edited** — the corrections land as dated update blockquotes exactly where a reader would otherwise inherit the stale form.

### F. Three dated curator banners on spoke reports (bodies untouched, C-3 precedent)
- **F1 `.claude/outputs/c2w7-read-cardinality.md`** — **the mandated re-scoping** (old sentence struck, the binding replacement quoted verbatim, the SURVIVES evidence, the refuted hope, the two standing preconditions) · **the `S_eff` re-label of its own §3 line** (16.77 COLLAPSED → *"30.6 of 32 wells visited — slight under-usage"*) · **the M15 6.9 → ≈7.9 correction** · the §A28.4 scope + paused-read-track statements. ⛔ **No measured number retracted.**
- **F2 `.claude/outputs/tierii-read-fix.md`** (iteration 1) — **the `S_eff` re-label** (*"16.00 IN BAND"* → *"all 32 wells visited"*, and why 16 was the only attainable value) · the **`k`-capacity refutation** of its own registered claim · **`K0` reachability-only** · ⛔⛔ **its own `G1` gets WORSE under a zero-parameter reader (−0.0016 → −0.0023)** while `OD_min` and `S_eff` reproduce · the C2W7 result it handed forward.
- **F3 `.claude/outputs/orgdiv-cat-test.md`** — the **SEEN-column provenance footnote** (⛔ **not an erratum**; unseen reproduces bit-for-bit; cite 0.0094 / 0.0047) + the identity-worse and lstsq-inflation notes + the `S_eff` note that **its own COLLAPSED label survives** as concentration.
- **F4 `.claude/outputs/orgdiv-cat-test/FROZEN-interfaces.md`** — the **reader-class specification** gains, as a dated addendum banner: **K6**, the **zero-parameter-member rule** (with the identity reader's named failure mode), the **two-directional lstsq caveat**, the **SEEN provenance footnote**, and the **`S_eff` re-registration**. ⛔ **§(ii) itself untouched; no published number of that cell moves.**

---

## 2. What I deliberately did NOT do
1. ⛔ **No `PREREG-*.md` body edited** — `PREREG-TierII.md` untouched, and **no `ERRATA-TierII.md` exists or is owed** (the audit returned SURVIVES; errata are owed only on MOVES/PARTIAL).
2. ⛔ **No re-derivation, no re-scoring, no verdict reinterpreted.** Where the engineer's report and the Hub disagree (`S_marg` 6.9 vs 7.9173) I recorded **the Hub's raw-verified figure and named the report's as wrong**, as the task instructs — I did not adjudicate anything not already adjudicated.
3. ⛔ **I did not convert C2W5's `S_eff` 34.1/51.2/36.6 into wells-visited units.** The Advisor calls them "concentration"; that word I transcribe. The arithmetic is the Hub's/Advisor's to publish.
4. ⛔ **I did not adjudicate `depth_ratio`** (§A26.8) — logged with numbers on both sides, in three places, un-resolved.
5. ⛔ **No C2W6 row touched.** The erosion/N223 story, the §A27.1 erratum, `N229`/`N231` and the §0.10/CM-32/CM-33 amendments are **fold-2's**; I neither edited nor restated them.
6. ⛔ **No `handover_context.md` edit, no task-file edit, no charter edit, no `.claude/papers/**` edit, no tracked code, no branch, no commit, no push.**
7. ⛔ **No tier self-confirmed** — N232–N240 are **CURATOR-PROPOSED (✪)**; matrix **v2.13 is PROPOSED**. The Hub confirms versions and tiers.
8. ⚠ **I did not imply a read iteration 3 anywhere**, and I checked each new section for that failure mode explicitly.

## 3. Numbers transcribed (spot-list, all with source; none re-derived)
`0.065625 ± 0.023876` / mean−2SE `0.041749` / per-seed `0.0586·0.0625·0.0313·0.1055·0.0703` · `−0.007031 ± 0.001562` / per-seed `−0.0078×3, −0.0039, −0.0078` / mean−2SE `−0.008594` · `0.0539 ± 0.0207` / `0.0609 ± 0.0218` / `0.0695 ± 0.0268` · R1 `0.0883` vs `0.0656` · `F_hat 4.542 ± 0.122`, set `4.194 ± 0.224`, exactly-`F` `0.394 ± 0.031`, `P(≥F) 0.9234 ± 0.0172` · `4.834 ± 0.125 → 5.668 ± 0.396`; precision `0.4578 ± 0.0260 → 0.4107 ± 0.0191`; anchors `1.09 = ~1.4 sep` · `S_eff 16.7737 ± 0.8527`, per-seed `17.07/16.00/18.29/16.52/16.00`, wells `30/32/28/31/32` · `S_marg 23.158 ± 2.655`, 0/5; un-forced trip **7.9173**; designed negatives `4.000 / 1.000`; per-query `3.94` vs marginal `21.22`; regularizer `ΔS_marg −0.90`, `Δbest −0.0016` · `k` guard `0.04296875 → 0.04296875`, coverage `0.234375 → 0.234375`, distinct `5.72 → 5.99` · `K0` designed `0.9195`, trained `0.9070 ± 0.0125`, per-seed `0.8945/0.8906/0.9102/0.9180/0.9219`; collapsed head `1.0000` with `F_hat 8.01`, precision `0.124`, `rho 0.25 → −0.176` · `G2 0.0 / 0.9586 / 0.99877` · `G3 6.70e-8 vs 15.58` (ratio `4.3e-9`), designed-blank `26.04` · `p₀` `0.0063 → 0.0477` (7.6×), coverage `0.012 → 0.100` (8.3×) · weight modes `both 0.0539` / `none 0.0578` / `noisy_or 0.0578`; verb gap `0.0039` · `depth_ratio = 1`: `+0.0140 (+29 %)`, `+0.0149` · ceiling `0.691` (`d = 8`, 1 draw, `σ_q = 0.15`) · bytes `75 448 B`, `read_flops/query 2.507e8`, `tol 0.2338` · audit: `null* 0.001171875` per-seed `[0, 0.00391, 0.00195, 0, 0]`; `OD_min −0.00078125`; `G1_min −0.0015625 → −0.00234375`; `I−F` `+0.00000 / +0.00078 / −0.00039 / +0.00039 / +0.00156 / −0.00078`; `gated_well_identity 0.00234375` per-seed `[0,0,0,0.01171875,0]`; `diag(W)` `0.128/0.218/0.276/0.357/0.376/0.430/0.446` and `+46.53`; residuals `13.48 vs 1.84`, `1.3539 = 2.91× tol`, `0.5737 = 2.47× tol`; K6 `2/2560 · 3/1280 · 0/2560` vs `~18 %`; identity-worse `+0.00078`, `+0.0109` (0.8078 vs 0.7969), `>0.99` (1.0000 vs ≤0.0104); cat-test SEEN `0.0109375 / 0.003125` (no provenance) vs `0.0094 / 0.0047` · suites `1379 / 1397 / 1410`, merges `fdab86d` / `d1149a4` / `d70898b`, `origin` frozen at `40c2f31`.
**Sources:** `c2w7-read-cardinality` §1/§2/§3/§4/§4.1/§4.2/§4.3/§5/§6/§7/§8/§10 · `reader-fitting-audit` §1/§3/§4/§5/§6/§6.1/§6.2/§7/§8/§9 · charter **§A24/§A25/§A26/§A27.2/§A28.4** · the `[C2W7]` §10 entries of 2026-08-05 and 2026-08-06.
**Provenance of the wave's cells:** base `main @ 104ca19`, **jax 0.9.0 / equinox 0.13.4 / float32**, main venv reused, **seeds 0–4**, one claim cell (`N_a/F/K/n_unseen = 32/4/128/256`, `a/m/d = 32/8/8`, `k = 12`, `depth_ratio 3.0`, read **400 + 800** Verlet at `dt = 0.05`, `γ 0.05/0.02`), **0 A100-hours**.

---

## 4. Gaps and risks flagged for the Hub
1. ⚠⚠ **CONCURRENT-CURATOR COLLISION — handled, but the Hub must diff-check two things.** `doc-curator-c2w6-fold-2` was writing `negative_results.md`, `claims_matrix.md`, `future_work.md` and `philosophy-synthesis.md` **at the same time as this pass** and claimed **N231** mid-write. I shifted to **N232–N240** and touched none of its edits. **Check: (a) exactly one `N231` exists and the summary index has no gap** (verified at my end of pass: `N230 → N231 → N232…N240`, ordered); **(b) the matrix now carries fold-2's `v2.12a` block inside a `v2.13` title I opened for my own additions** — fold-2 deliberately left the version call to you, so **the v2.12/v2.12a/v2.13 stack is a Hub decision, not a curator one.**
2. ⛔⛔ **`tierii-read-fix` (READ ITERATION 1) HAS NO N-ENTRY AT ALL.** It was never folded, yet C2W7's re-labels, refutations and never-quotes all cite its published numbers (`S_eff 16.00`, `R1 0.0023`, `G1 −0.0016 ± 0.0019`, the `k`-doubling +0.2383, the 1.4-`sep` anchor offset, `OD_min` the vacuous tie). ⇒ **the registry documents the correction of a result it never registered.** **Cheapest close: a small catch-up fold of `tierii-read-fix.md`** (its own reconciliations 2–3 are also unowned). Not this task's to do; flagged with a named owner requirement.
3. ⛔ **THE `S_eff` RE-LABEL'S LAST MILE IS OUTSIDE CURATOR SCOPE — the §A20.2 pattern, fourth occurrence.** The old wording still lives in **charter §A21/§A24** and the **handover §10 gate tables**. The binding form is filed in N239, §0.11/CM-34, the ledger and the primer; **the two editable sites (both spoke reports) now carry banners.** **Someone with the charter/handover in scope owns the rest.**
4. ⚠ **THE PRIMER'S RECORD 21 C2W6 UPDATE CARRIES A NOW-FORBIDDEN WORDING — and it is fold-2's, not mine.** Its item 2 says *"Quotable form: **N223's monotone collapse does not reproduce at the run-2 config**"* — which **Add.9 §A27.1 / fold-2's own CM-32(nnnn) now makes a never-quote** (the approved form is *"N223's mechanism FIRES … but is NOT TERMINAL"*). ⛔ **I did not edit it — it is C2W6 territory and co-editing was forbidden.** **Flagged so it does not fall between the two folds.**
5. ⚠ **`orgdiv-cat-test`'s SEEN-column provenance is footnoted, not RESOLVED.** Nobody has located where `0.0109375 / 0.003125` came from. If it was a different (e.g. pre-deviation) run, that run's artifacts are not in `results/`. ⚠ **No verdict depends on it — but the report reads as if the column is reproducible, and now it visibly does not.**
6. ⚠ **THE `sum_linear` INFLATION HAS NO OWNER AND MAY BE A PHYSICS FINDING.** `diag(W) = +46.53` with the settled payload block at ~1/46 of the family's scale — the engineer flags it as possibly `tierii-read-fix` D10's basin-reach issue. **It is filed as an instrument caveat (N238) because that is what is measured; if it is a store fact, it belongs to the write-side iteration.**
7. ⚠ **`K6` and the two-sided wells-visited instrument are REGISTERED BUT UNEXERCISED.** K6 has only ever been computed retrospectively, and the replacement `S_eff` instrument has **no thresholds set**. Both are in `future_work.md`; ⛔ **neither can gate anything until a prereg names its numbers.**
8. ⚠ **BANNER SCOPE — my own judgement call, stated for reversal.** My standing brief names the transfer docs; the task named the registries. **Fold 1's *"must not survive anywhere"* and the acceptance item on the retroactive re-labels are only satisfiable by touching spoke reports**, so I used the C2W6-ratified dated-banner instrument on three of them, bodies untouched. **If the Hub prefers report bodies to stay banner-free, each banner is a single trailing block and reverts cleanly** — but then the old sentence and the old `S_eff` labels remain live in those reports and the acceptance item is unmet.

## Proposed handover updates (for the Hub)
1. **§10 / registry status:** `negative_results.md` carries **N232–N240** (tiers **CURATOR-PROPOSED ✪**) + dated addenda on **N212 / N213 / N214 / N215** + a **C2W7 NOT-RUN block**; `claims_matrix.md` is at **v2.13 (PROPOSED)** with new **§0.11 / CM-34 / CM-35**; the ledger has its **⟲ C2W7 addendum** (primary, not a supplement); `future_work.md` has the **C2W7 fold** (6 status moves, 11 boundaries); `HEP_primers.md` has **§11.23 Records 25–26** + dated updates on Records 20/21; **four dated curator banners** on `c2w7-read-cardinality.md`, `tierii-read-fix.md`, `orgdiv-cat-test.md` and `orgdiv-cat-test/FROZEN-interfaces.md`. **Hub to confirm tiers + v2.13.**
2. **Two decisions are yours, not the curator's:** the **version-stack shape** (fold `v2.12a` into v2.12, or carry it inside v2.13), and whether the **spoke-report banners** stay (item 8 above).
3. **One task worth scoping:** a **`tierii-read-fix` catch-up fold** (item 2 above) — the registry currently corrects a wave it never registered.
4. **One cross-fold item to route:** the primer's Record 21 C2W6 update carries a wording that fold-2's own ruling now forbids (item 4 above). **C2W6 territory; C2W7 did not touch it.**
5. ⛔ **Nothing spawned, nothing pushed, no branch, no commit** — all edits are under `.claude/**` and gitignored.
