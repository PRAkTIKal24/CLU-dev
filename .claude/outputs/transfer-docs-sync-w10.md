# transfer-docs-sync-w10 — doc-curator report

Task + acceptance criterion: bring the **four** transfer docs current through **waves 9–10** — first curator pass on `future_work.md`; two physics-worth-noting clarifications into `HEP_primers.md` (CM-9 structural-vs-empirical; CM-4 misprediction canonical); three w9/w10 negatives into `negative_results.md`; write the ledger's **Wave-9+10 addendum** (⟲). Per-doc edit list + output↔handover contradictions.

Status: **done.** All four docs edited; **no tracked code touched**; only gitignored `.claude/**` files written. **No git footprint.**

Inputs read (protocol order): `AGENT_PROTOCOL.md` · doc-curator def (4 docs) · `transfer-docs-sync-w8.md` (my w7/w8 pass) + `transfer-docs-sync-w9.md` (the w8 pass — note the slug/wave offset, see hygiene flags) · handover §10 **WAVE-10** entry (2026-07-08/09, source of truth) + the **WAVE-9** entry · the eight w9/w10 outputs {`v1-revision`, `v2-revision-2`, `v3-revision-2` (+`v3-revision` context), `v3-scaling-figure`, `v1-referee`, `v2-referee-2`, `v3-referee`, `v3-banding-figure`} · `claims_matrix.md` **v1.5** (CM-4 + CM-9 rows read in full) · all four transfer docs.

Discipline: every number transcribed with report+section citation; **no verdict reinterpreted** (the three weak-accepts, the V3 near-reject, and the CM-9 re-scope are stated as the referees/Hub stated them); Def-2 nomenclature; two-layer status tags preserved. Doc state before this pass = **through w8**; this pass advances all four to **w10**.

**Prior-state note:** the Hub had **not** written a w9 or w10 ledger addendum (last was ⟲ Wave-8), so this is a full curator addendum, not a `(curator supplement)` block. Per the handover, the curator **deliberately skipped w10** ("referee passes add no claims") and resumed here — hence w9+w10 folded into one addendum.

---

## Doc 1 — `future_work.md` (first curator pass: 2 new entries, 1 entry restructured with per-item statuses, footer)

1. **V3 › "Interference — the unmeasured channels"** — original bullet **kept verbatim** (provenance), with a nested `> **Wave-10 update**` block giving **per-item status** for its five sub-items, all now **referee-named** (`v3-referee` MF-1 / SF-1 / N-3 / missing-experiment list):
   - **(iii) block-structured monolith** → `TASKED(v3-interference-extra, w11)` — the referee's *"is it modularity or just separate nets?"*, named as a reviewer's first question + hostile-quote #2.
   - **(v) → intermediate N** → `TASKED(v3-interference-extra, w11)`; N≫8 stays `OPEN`. Numbers folded: modular **6.79e-5 → 1.74e-4 (×2.56)**, monolith **0.635 → 1.384 (×2.18)**, N=4→8 only.
   - **(i) trajectory-mediated channel** → `OPEN` (referee-named).
   - **(iv) dynamical interference half-life** → `OPEN` (referee-named; **gates a wording, not a result** — it licenses the S>1 storage gloss).
   - **(ii) irrep firewall** → unchanged `OPEN`.
2. **NEW — V1 › "The certificate's PAYOFF, measured"** (`v1-referee` F3) → `TASKED(v1-certificate-payoff, w11)`. Carries the definitional-vs-demonstrated framing, the router numbers (1.000/0.948 @8.81e7 vs 0.887/0.715 @1.18e8), the referee's "one addition that lifts V1 above weak-accept," and the **check-the-logs-first** caveat (may be a wiring note, not an experiment).
3. **NEW — V2 › "Width-matched per-step cost"** (`v2-referee-2` missing-experiment #4) → `OPEN` (fortification). States the confound explicitly (CLU h64 vs baselines h16) and that the **sign** of the compute inversion is robust (two ∇V backprops), so it is not blocking.
4. **Footer** — "last swept" → wave-10; records that **nothing is marked `→ SHOWN`** (w9/w10 = wording, not new science) and that all pre-existing entries were re-verified against the w9/w10 outputs and **still read true**.

**Deliberately left unchanged:** all V2/V4/cross-cutting/foundational entries (re-verified, no wave-9/10 evidence bears on them); V1 MH-kernel entry stays `THEORY-ONLY` (handover: spec exists, **not launched**); V1 noise-wall entry stays `OPEN`. No entry deleted; no status downgraded.

## Doc 2 — `HEP_primers.md` (2 dated blockquotes + header + 2 ledger rows)

1. **Part IX §9.2 — NEW `> **Update (wave-10 …)`** (`v3-referee` MF-1 + `v3-scaling-figure`; **CM-9 UNDER RE-SCOPE**). Explicitly corrects the wave-6 block's "O(1) … flat" wording *in place* (as a dated update, not a rewrite): the two-point slope problem with exact numbers (incl. **R_nn 4.53e-5 → 9.92e-5**, non-overlapping error bars); the **structural** core that survives (R_far ≡ 0.0 exactly ⇒ **coordination-bounded ≤2**, N-independent; ~4-decade separation; ∝κ² slope 1.99); the honest sentence to carry; the **physics reading** (a *support property* of the kernel is exact and N-independent; a growth exponent is asymptotic — "locality is not scaling," the finite-range-propagator vs correlation-length distinction); the SF-1 note that **S=1 is a normalization reference line, not a storage capacity**. Metric discipline (report R, never the ≈0.99 cosine) restated.
2. **§3.3 — NEW `> **Update (wave-10 …)`** placed immediately after the w8 SF-2 caveat (**CM-4 misprediction canonical**): trained-model ceiling **≈3.2×** (meas/pred **0.309** exact-gap / **0.304** Mo λ̂ at δ=4; **no deeper underdamped row in the 14-δ sweep**) vs **exact-map-only ≈5.3×** (ratio **0.19**, F5 check (k), γ=0.1, ε=0.1); the **provenance of the error** (`v2-full-runs` L79 parenthetical mislabel → draft abstract); the canonical form — *quote both or neither.*
3. **Maintenance header** — "through wave-7" → **"through wave-10"**; added the w9/w10 line naming the two loci where the *physics reading* changed + noting these were referee/revision waves (wording, not new science); added `future_work.md` to the companion-docs list.
4. **§10.3 claim-status ledger** — 2 new rows: **CM-9 ⚠ UNDER RE-SCOPE** (structural, not growth-rate; framing final at w11) and **CM-4 misprediction canonical** (3.2× trained / 5× exact-map-only; mislabel traced).

**Deliberately left unchanged:** Parts I–VIII core derivations and all w4–w8 update blocks (the wave-6 Part IX bullet is **corrected by the dated w10 block above it, not edited** — pedagogical honesty: the reader sees what we thought and what falsified it); §6.5, §8.4, §8.5 (w7/w8-current); §10.4 reading list (no new literature this wave). **No new subsections** — neither w10 clarification is a new *mechanism*, so house style says update-in-place, not a new §.

## Doc 3 — `negative_results.md` (N43–N45; **now 45 entries**)

- **N43** — (process) the **"5×" mislabel**: a 3.2× ratio annotated as "5× failure" in `v2-full-runs` **L79**, propagated to a draft abstract, survived one referee pass, caught at the second. **Tier C**, V2 (process). Records the **provenance-hygiene hole**: the number lived in a *parenthetical aside*, which the flag-provenance rule (tables) doesn't cover.
- **N44** — (process) the **V3 two-point-slope framing**: "O(1)-in-N vs O(N)" rests on N∈{4,8} whose slopes match (×2.56 vs ×2.18); mechanism = *we measured a support property and wrote an asymptotic exponent*. **Tier C**, V3 (process). Same "referee caught it pre-submission" provenance as N36 — with the live consequence that **CM-9 wording is not final until w11**.
- **N45** — **"beats-the-router is definitional, not demonstrated"** (`v1-referee` F3). **Tier B, PROVISIONAL** — re-tiers at w11: discharge (payoff measured) or **promote to tier A** beside N35. Notes that **CM-12/certificates are untouched** (det J=[1.0]×6, ledger=[0.0]×6, latch err 0.0) — this is a *claim-support* negative, not a mechanism refutation. Flags it as the **5th instance** of the deflation pattern but a **distinct mechanism** (earlier four: cheap baseline *equals/beats* the physics; N45: the receipt is *exact* but its consequence is *unmeasured*).
- **Updated:** summary index (3 rows) · maintenance line (through wave-10; explicitly states **w9/w10 produced no new experimental negatives**) · companion-doc list (added `future_work.md` as the not-yet-attempted sibling) · paper-writer appendix-mining notes (V1 +N45 with the drafting prohibition; V2 +N43 with the mandatory CM-4 canonical form; V3 +N44 with the ship-structural instruction) · open-provenance flags (N43/N44 are process records like N36; N45's provisional tier + check-logs-first + the 5th-instance note).

## Doc 4 — `philosophy-synthesis.md` (ledger — WROTE the **Wave-9 + Wave-10** addendum; ⟲ chapters untouched)

Appended `# ⟲ Wave-9 + Wave-10 addendum (2026-07-09, doc-curator)`:
- **Framing:** the revise→referee waves; **no new science, no code** (main `37dc664`, 194 green); what changed is *what we are entitled to say*. One-line epistemic lesson recorded: **all the defects were over-strong verbal framings sitting on honest, reproducing numbers** (V1: zero number-mismatches).
- **Chapter deltas:** **Ch.4** (V1 weak-accept, exact-to-the-digit trace table; **N45** the definitional comparative + why a landing-rate observable can't express a volume certificate; the 6 craft failures F1–F7; w9's CM-8/CM-14 folds). **Ch.1** (V2 weak-accept near-final, prior punch-list fully closed; **MF2-A → N43**; **MF2-B**, a new contradiction *introduced by the previous wave's own fix*; the accepted compute-inversion residue). **Ch.5** (V3 weak-accept/near-reject; **MF-1 → N44**; the structural core; *locality is not scaling*; SF-1's S=1 reference line; both figure assets landed with the **swap-ordering constraint**).
- **Scorecard deltas** (rows 4, 1, 5) + **gap-list** (the **shared critical path — the F5 note must go live**, now confirmed from two directions and Head-owned; CM-9 frozen-pending; CM-4 canonical; N45 resolves at w11; `future_work.md` live under curator upkeep; unchanged real-data/reversible/steering/noise-wall backlog) + **five new drafter scope-guards (l)–(p)**.
- **Positioning ripples:** the referee stage catches a **class, not incidents** (N36/N42/N43/N44/N45 + MF2-B, the fix-induced one) — data discipline holds, *prose* discipline is what gets corrected; V1's self-portrait sharpened (M3 **flagged, not adjudicated**); V2 the spine; **V3 the wave's lesson** ("the structural claim is *stronger*, being exact rather than asymptotic — take it").
- **Doc-hygiene:** the long-standing matrix header lag is **CLEARED** (v1.5/wave-10); `v2-referee-2`'s "add the canonical form to CM-4" is **already done** by the Hub (recorded closed, not as an open ask); one sequencing dependency flagged for w11 task files.
- ⟲ protocol respected: **no chapter rewritten**; frozen scorecard left at its ⟲ state; verified the addendum covers all eight w9/w10 outputs before writing.

---

## How I verified
- Every quantitative statement cross-checked against its source-report section before transcription: the interference two-point numbers (`v3-referee` MF-1 ¶1 + `v3-scaling-figure` §"Numbers", incl. the S_B row-sum definition and 3-seed error bars); R_far≡0 / 1:9,000 / slope 1.99 (`v3-referee` charter audit + primer w6 block, unchanged); the 5× determination table (`v2-referee-2` §"The '5×' determination": 0.309 / 0.304 / 0.31 / 0.30, the censored-rows-are-overdamped fact, F5 check (k) → 0.19); MF2-B's four MSEs + commits (`v2-referee-2` MF2-B: 0.190 / 0.0128 / 0.2066 / 0.0216 / 0.0047, `b41410f` vs `9a13455`); V1's router numbers and 12-row trace table (`v1-referee` §"Charter/claims-matrix compliance" + F3); banding bars (`v3-banding-figure`: 1.180±0.216 / 2.416±0.345 / 6.924±1.883 / 12.791±0.698, gap 0.000, 5 seeds).
- Confirmed `claims_matrix.md` **v1.5** (header *and* body) carries CM-9 UNDER RE-SCOPE and CM-4 misprediction-canonical, matching the handover §10 WAVE-10 entry and the referee outputs, **before** transcribing status tags.
- Confirmed prior doc state was **through w8** (primer maintenance line, ledger last addendum = Wave-8, negatives at N42, `future_work.md` footer "last swept: wave-8") — this pass advances all four to w10.
- No tracked code touched; all writes under `.claude/`. No branch, no commits.

## Output ↔ handover contradictions found (flagged, NOT resolved — per mandate)
- **None substantive.** The eight w9/w10 outputs, the handover §10 WAVE-9 + WAVE-10 review entries, and matrix v1.5 are mutually consistent on: three weak-accepts; V3's MF-1 near-reject + CM-9 re-scope; V2's MF2-A/MF2-B; V1's F3/MF-1; the shared theory-note critical path.
- **One reconciled apparent tension (resolved *by the sources themselves*, recorded not adjudicated):** `v2-revision-2` (w9) flagged the "5×" as *"presumably a more-extreme censored row"* of the 14-δ sweep and left it untouched as out-of-scope; `v2-referee-2` (w10) then **determined the opposite** — the censored rows are the **overdamped** side, there is **no deeper underdamped row**, and the 5× is an exact-map borrow + a `v2-full-runs` L79 mislabel. I transcribed the **w10 determination** (later, evidence-bearing, and adopted into matrix v1.5 CM-4). The w9 conjecture is preserved in N43's mechanism paragraph as the propagation path, not as a competing verdict.
- **One curator-visible ordering constraint** (from `v3-referee` known-pending, not a contradiction): the Fig-1 scaling-curve swap **visually exposes** MF-1, so the reframe must land **before or with** the swap. Recorded in the ledger addendum; the Hub owns whether `v3-revision-3`'s task file states it.

## Docs-debt / notes for the Hub
1. **All four transfer docs current through w10.** `future_work.md` is now under standing curator upkeep (per-wave), as the agent-def update requires.
2. **Slug/wave offset (cosmetic, worth knowing when diffing):** `transfer-docs-sync-w8.md` is the pass that brought docs through **w7**, `-w9.md` through **w8**, and this file (`-w10.md`) through **w9+w10**. The slug names the *wave the task was launched in*, not the wave of content folded. No action needed; flagged so a future Hub doesn't read a gap.
3. **Ledger frozen scorecard (⟲ structural):** current state now spans **six addenda (w4–w8, w9+10)** plus the frozen w3 scorecard — a reader reconciles seven locations. Unchanged from prior flags; a single current-state scorecard would violate "chapters never rewritten." **Head/Hub call.** (Suggestion, unactioned: a one-page "current verdicts" index that *points* at addenda rather than restating them would not violate ⟲.)
4. **Prose-provenance gap (new, from N43):** the flag-provenance rule (critique M4) governs **tables of results**. The "5×" entered the program through a **parenthetical annotation** in a source report and survived a full referee pass. Consider extending the rule: *any number appearing in a source report's prose that a draft may quote must also appear in that report's provenance table.* Curator recommendation only — the Hub owns AGENT_PROTOCOL.
5. **Matrix hygiene:** the version-label lag flagged in w6/w7/w8 is **cleared** (v1.5 / wave-10). Residual nit: header date "2026-07-08" for a review entered 07-08/09.

## Proposed handover updates (for the Hub)
- Transfer docs current through **w9+w10**: `future_work.md` (**first curator pass** — 2 new boundaries, 4 V3 channels upgraded to referee-named, 2 → `TASKED`; **nothing `→ SHOWN`**), `HEP_primers.md` (2 dated blockquotes: Part IX CM-9 re-scope, §3.3 CM-4 canonical; header + 2 ledger rows), `negative_results.md` (**N43–N45; now 45 entries**), `philosophy-synthesis.md` (**Wave-9+10 addendum** written per standing duty — the Hub had not written one).
- **Five new drafter scope-guards now live in both the ledger addendum and the registry:** **(l) CM-9** ship *structurally O(1)*, never "flat in N" on two N-points; **(m) CM-4** ≈3.2× trained / ≈5× exact-map-only, quote both or neither; **(n) N45** the wormhole *"reaches with a receipt the router cannot supply"* — it does **not** "beat" the router until a guarantee-violation is measured; **(o)** the `minus-the-physics` ↔ `fit-gap-anatomy` MSE scales are **not cross-comparable**; **(p)** S=1 is a normalization reference line, not a storage capacity.
- **Two live wording freezes:** **CM-9** (framing final only at w11 from `v3-interference-extra`) and **N45's tier** (discharges or promotes at w11 from `v1-certificate-payoff`). Neither should be drafted as final before then.
- **The #1 critical-path item is re-confirmed from a second direction:** `v1-referee` **MF-1** makes the **F5 theory note going live** a *submission dependency* — §3's load-bearing propositions are unreachable to a reviewer, and the same "(Anonymous, 2026)" placeholder blocks all three shorts' §-provenance. **Head decision (title/authors → arXiv); no spoke can clear it.**
- **Curator recommendation (protocol-level, Hub-owned):** extend the flag-provenance rule to **numbers quoted from source-report prose**, not just tables — this is exactly how N43's "5×" reached an abstract.
- **M3 portfolio tension unchanged and still not adjudicated by the curator:** the V1 referee independently articulates the Head's own "weakest short" concern (*"the physics buys a demonstrated advantage only where the authors control the testbed"*) and names the single experiment that would answer it (`v1-certificate-payoff`). Recorded as evidence for the Head's decision, not as a verdict.
