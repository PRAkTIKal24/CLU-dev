# nips-v2-clean — paper-writer report

**DIAL DECLARATION (echoed): none — framing/editorial pass; zero number changes.** Laundering control: n/a. Falsifies the claim: n/a. Does NOT falsify: n/a.

Task + acceptance criterion: build V2's clean iteration base at `.claude/NIPSsubmission/v2-neurreps/` from the plain build, adding 2025–26 audience scoping and the conservative N1 novelty scoping, in strict PJ prose, with zero number changes and the page count reported but not optimised.
**Status: done.** All four acceptance criteria met; three flags and five editorial questions below.

## ⚠ DOWNSTREAM RECONCILIATION LIST (first-10-lines rule) — needs an owner
1. **I applied one citation-accuracy correction owed by a report that landed 14 minutes before this pass opened.** `outputs/n1-fulltext-and-track-check.md` (2026-08-21 19:01) states in its own first ten lines: *"any CHLU/V2 site that characterises N1 as containing a lifetime law must be corrected. N1 contains no closed-form gap→lifetime relation anywhere in its text."* This build accordingly says **"lifetime estimator"** at all six sites where the source said *"lifetime law"* (`grep -c "lifetime law"` = **0**). ⛔ **It is one-line reversible.** **Owner: the Hub** — decide whether this correction is ratified here and propagated to the other V2 lineage sites (`papers/plain/v2`, `papers/v2-neurreps-descoped`, `papers/v2-short`, `neurreps-variants`, and any primer/ledger sentence calling N1 a law), or reverted pending the Hub's own reading.
2. **The same report closes the task's conditional narrowing pass.** The task said a further narrowing pass follows *if* the full-text read finds a **law**. It found the opposite (a prediction validated by correlation, formula recoverable only from released code). ⇒ **No narrowing pass is owed on this evidence.** Owner: the Advisor, to close the Add.48 "still in flight" line.
3. **One citation in this build rests on an unverified record (refresh flag F2).** *Symmetry-regularized learning of continuous attractor dynamics* (NeurReps 2025 poster) is cited by **title and venue only**, with "author list not resolved" printed in the reference entry, because the refresh could not read it (OpenReview bot-walled) and its own instruction was *"do not cite until read."* **Owner: the Hub/Head** — either resolve the record via a human OpenReview fetch before submission, or cut the sentence (§2, one sentence, self-contained).

## What I did
- Created `.claude/NIPSsubmission/v2-neurreps/` (new folder; `NIPSsubmission/` did not exist): `BUILD-NOTE.md` (deliverable #1, complete), `submission.tex`, `submission.pdf`, `figs/` (5 PNGs), `neurips_2025_ml4ps.sty`, `submission.log`. Source = `papers/plain/v2/`, copied, never edited in place.
- **Verified all five carry-overs before editing** (typography, main-text bold, author token, figure widths, single-seed labelling) — nothing had slipped; no repair needed.
- **Audience scoping, ten changes**, itemised with their refresh provenance in BUILD-NOTE §2: SSB stated directly in abstract/§1/§3; *canonicalization* replacing the 2022 term; *flow* disambiguated on first use (and the two pre-existing "first-order flow" uses re-worded to "first-order dynamical system" so the reserved sense is clean); **N5/N5b positioned as the anchor** with the *"they generalize, we price"* split; N2 as the emergent-arm competitor; **N4 named before a referee can ask for it**, twice, as a direction only; N6 cited as the organizers' live line with the arrow stated in both directions; N7 used as licence, deliberately **not** cited (flag F5, no author lists); *memory lifetime* bridged to `n_{1/2}` once; the whole framing re-pitched as a dynamics paper.
- **N1 scoping, conservative** (BUILD-NOTE §3): the chain is attributed in the abstract *before* any of our results; a dedicated §2 paragraph says the existence result and the qualitative lifetime prediction *"are established there and are not claimed here"* and that our contribution is the **exchange rate**; §4.2 repeats the fence at the point of contact; the units difference (Lyapunov exponent 1/time vs curvature 1/time²) is printed at **three** sites with the statement that we never convert on paper — §4.2 converts by running their instrument; and **the word *pseudo-gap* now appears zero times.**
- **PJ pass on the framing only** — ABT openings, macro-to-micro, contributions **enumerated** on page 1, short declarative sentences. Every approved wording, rider, scope qualifier, fine-print block, negatives table and appendix paragraph is byte-unchanged.
- Ran and printed: the two-way numeric check, the three sweeps (per file, positive-controlled), the anonymization audit incl. a decompressed-stream sweep, and the byte-integrity manifests.

## How I verified (commands + observed output)
- **Build:** `/Library/TeX/texbin/pdflatex -interaction=nonstopmode submission.tex` ×3 → **0 errors, 0 undefined references/citations, 1 overfull `\hbox` (3.57 pt — the same loan-ladder table, same size, as the source), 0 overfull `\vbox`, 36 underfull**. `Output written on submission.pdf (18 pages, 996193 bytes)`.
- **Page split** (`pdftotext -bbox`, text block 72–720 pt — the lineage's own instrument): **main text 7.80 pp · references 2.45 pp · appendices 7.79 pp · total 18 pp**. Plain source for reference: 7.00 pp main / 17 pp. ⛔ **NOT optimised, not a page result.** The +0.80 pp is entirely the new framing content (two new §2 paragraphs, the enumerated contributions, the longer abstract) plus four new reference entries. Nothing was compressed or cut.
- **Numeric two-way check** (all numeric tokens; `\includegraphics` options and `\setlength` args excluded as typography): distinct **392 source / 395 build**; **IN SOURCE, NOT IN BUILD = `{}` (empty)**. Build-only, 18 occurrences, each accounted for: `2025`×9, `2026`×2, `2502.07256`, `2410.03972`, `2601.01075` (the four new references), `2605.03338`×1 (one extra occurrence of an identifier already in the source — the abstract attribution), `1`×2/`2`×2 (repeats of `$C^1$`, `$n_{1/2}$`, `$\mu^2$`, `$\mu^{-2}$`, `$S^1$` in reworded sentences), `4`×1/`5`×1 (the C-5 scale qualifier `latent dimension 4`, `≤5 seeds`, both the paper's own canonical numbers). **Zero numbers changed, rounded, smoothed or added.**
- **Sweep A (author token):** `grep -c "\bMo\b"` → **1**, the bibliography entry; prose/captions/labels/filenames → **0**; **positive control on the pre-anonymization descoped source → 9**. `Morse` 1 and `Moser` 1 survive; pronoun sweep → **0**.
- **Sweep B (internal apparatus, 24 patterns):** **0 hits**; **positive control on the task file → 6 hits**.
- **Sweep C (semantic hermeticity):** **0 hits**; sanctioned unpublished references unchanged — `Anonymous` ×2 (theory note), `"introduced as CHLU in Jawahar & Pierini (2026)"` ×1, verbatim.
- **Sweep D (anonymization):** `\author{}` empty; `\@notice` suppression retained; `pdfinfo` Title/Subject/Keywords/Author all empty; decompressed-stream sweep over **68 streams / 31.5 MB** → 0 for `Forgis`, `x10719pj`, `Users/user`, `Desktop`, `CERN`, `Manchester`, `.claude`, `NIPSsubmission`, `/tmp/`; **positive control `Goldstone` = 8**.
- **Sweep E (typography carry-over):** `footnotesize|scriptsize|\small|\tiny|raggedbottom|\@startsection` → **0**; all 5 `\includegraphics` at `\linewidth`; `\textbf` in main text (before `\appendix`) → **0**.
- **Source folders byte-untouched:** `shasum` manifests before/after — `.claude/papers/**` **168 files, diff empty**; `.claude/outputs/v2-full-runs/**` **16 files, diff empty**. All 5 figures `shasum`-identical to the plain source. `pdflatex` was run **only** inside the new folder.

## Findings / editorial substance
- **The headline figure is Figure 1** (`fig1_gmor.png`, §4.1): the μ⁻² law, the floor and the metric bifurcation on one canvas — it is the single contribution rendered once. Figure 2 (head-to-head) is the evidence figure and the natural second slide.
- **The best positioning sentence in the paper is now the N5/N5b split:** *"That line and this paper share an object — a continuous symmetry carried in a recurrent state over a long horizon — and split the question cleanly: they generalize, we price."* It converts the strongest 2025–26 neighbour from a threat into a division of labour, and it is followed immediately by the explicit no-generalization-claim clause.
- **The N1 fence is now stated three times, escalating in precision** (abstract attribution → §2 "not claimed here" + the exchange-rate statement with its scale qualifier → §4.2 "that prediction is its result, not ours"). A reviewer who has read arXiv:2605.03338 meets our fence before meeting our claim, in the abstract.
- **Dropping *pseudo-gap* entirely is the strictest available reading of the directive** and costs one unit of vocabulary alignment with the room (the refresh's §3.1 recommends *"adopt the word, and cite"*). The attributed-quotation form is one line away if the Hub prefers it (open question 1).
- **Prose lifted from the scout reports:** all 2025–26 neighbour characterisations and every vocabulary decision come from `outputs/audience-refresh-2025-2026.md` §1.5 (N1–N8), §1.7 (the four shifted terms) and §3.1 (the updated NeurReps map); the N1 law-vs-prediction wording comes from `outputs/n1-fulltext-and-track-check.md` §Q1.1–Q1.3. No prose was taken from any paper draft outside the plain source.
- **Charter compliance, spot-stated:** C-1 (no audit-confession paragraph — the paper contains none, and none was added; J&P 2026 is cited for the primitive's introduction only) · C-2 (verification/evidence labels intact and unmoved, incl. the abstract's closing sentence) · C-3 (ML-first: the head-to-head leads the evidence, the physics is the derivation apparatus) · C-4 (N4 and the μ-beyond-SO(2) items are named as directions, never as cargo) · C-5 (the one new generalizing claim carries `dim 4 / S¹ / ≤5 seeds` in-sentence) · C-6 (all fine print byte-unchanged, still adjacent to its claim) · C-8 (hermetic; zero references to sibling shorts) · C-9/C-10 (no negative dropped, no appendix pruned — nothing was cut for space anywhere).

## Open editorial questions (for the Hub/Head)
1. **Restore *pseudo-gap* as an attributed quotation?** Currently zero occurrences. The directive forbids it *as ours*; the audience map recommends adopting-and-citing for vocabulary alignment. One-line change in §4.2 if wanted.
2. **Ratify or revert the "law" → "estimator" correction** (reconciliation item 1), and decide whether it propagates to the rest of the V2 lineage.
3. **Resolve or cut the N2 citation** (reconciliation item 3) — it is the only citation in the file whose author list is unresolved.
4. **Verify the Lillemark et al. (2026) entry before submission** (refresh flag F4: three title variants observed; author list index-sourced). I used the arXiv abs-page title, *"Flow equivariant world models: Memory for partially observed dynamic environments"*, arXiv:2601.01075.
5. **Title.** The file keeps the lineage's working title, *The Price of a Flat Direction: Transverse Curvature Sets Retention in a Trained Recurrent Memory* — it is **not** in the `[WORKING TITLE: …]` placeholder form the agent brief specifies, because the whole V2 lineage has carried it and this is an iteration base. Titles are workshopped at the end; if the Head wants the room's current word in the title, the two candidates the framing now supports are *"Pricing a Spontaneously Broken Direction: …"* and *"What a Flat Direction Costs: Transverse Curvature Sets Retention in a Trained Recurrent Memory."* No change made.
6. ⚠ **The venue-fit fact worth a decision, not made here:** `n1-fulltext-and-track-check` establishes that the NeurReps 2026 **Findings track's archival status is unstated on every venue-owned surface**, that it has **no page limit**, and that its advisory board is all systems-neuroscience. That is a track-choice call for the Advisor/Head; it changes nothing in this build, which remains venue-neutral.

## Risks
- **Main text is 7.80 pp against a 4-pp Extended-Abstract limit.** Deliberate, per the task. The compression pass will have to cut ~3.8 pp of *content*, and the honest levers are now visible: the four retirements (§2, one paragraph), the fine-print block (§3), the scope box (§5), and Figure 1's demotion. **None of them can be taken by typography** — Add.48's finding.
- **Four new references** grew the reference list to 2.45 pp. A pruning pass should treat the reference list as a target in its own right.
- No code was touched; no git footprint (everything under `.claude/`, gitignored).

## Proposed handover updates (for the Hub)
1. **`NIPSsubmission/v2-neurreps/` exists and is the V2 iteration base** (BUILD-NOTE complete, PDF built, 18 pp / 7.80 pp main, not optimised). `papers/plain/v2/` is now lineage record and is byte-verified untouched.
2. **The Add.48 "still in flight" line can be closed for N1:** the full-text read landed; it found a **prediction, not a law**; the conditional narrowing pass is **not owed**; and the reverse correction (our own "law" wording) is applied in this build and pending Hub ratification for the rest of the lineage.
3. **Add a claims-matrix-adjacent never-quote candidate:** *"N1 publishes a lifetime law"* — it does not; it publishes a theorem about zero exponents plus a code-level single-exponential predictor validated by correlation.
4. **Two citation records still need a human fetch before any submission:** the N2 poster (authors unknown) and the Lillemark et al. title/author list. Both are flagged in-file and in BUILD-NOTE §2/§6.
