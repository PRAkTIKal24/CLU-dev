# BUILD-NOTE-R4 — three presentation fixes, then two figure insertions

**Pass:** `v1-figure-fix-and-insert` (results-analyst spoke) · **Date:** 2026-08-27
**Files written:** `pj_sub.tex` (two `figure` environments, nothing else) ·
`figs/fig4_bibo.png` · `figs/fig_frontier_clean.png`
**Not written:** `figs/fig1_certificate.png` (already correct — see §1), `submission.tex`,
`pj_sub.pdf`, `.claude/papers/v1-short/**`.

## 0. Pin check — PASSED

| | md5 | lines |
|---|---|---|
| required pin (task §0) | `08d31733b5648ed6ab4a6bbc5dc07ed8` | 382 |
| **measured at start** | `08d31733b5648ed6ab4a6bbc5dc07ed8` ✅ | 382 ✅ |
| after this pass | `727ebee2b8498b4095f8bb7159258f90` | 402 (+20 = 11 + 9 inserted lines) |

**Protected-file manifest (byte-untouched, verified by md5 before and after):**

| file | md5 |
|---|---|
| `submission.tex` | `caef2272f9dc96d349b46486563d24ee` |
| `.claude/papers/v1-short/draft.tex` | `208797d113fa9d6efa6de67d05705ea6` |
| `.claude/papers/v1-short/draft.md` | `00d703d58a15c0cb77051a9c55674684` |
| `.claude/papers/v1-short/draft.pdf` | `141f2c37ee8089c814932ce27e5f4fa4` |
| `.claude/papers/v1-short/CHANGELOG.md` | `a2bc48c0e0c2f3ceab1fa7cf34f655c8` |
| `.claude/papers/v1-short/draft.log` | `86d8f80c82f90c4ab1fa0ce8a384cc0d` |
| `.claude/papers/v1-short/fig1_certificate.png` | `679647f639bfb8b3b7ecfa1333f43b69` |
| `.claude/papers/v1-short/fig2_regime_map.png` | `b0cfbf53651ac187bacee0f977d93f1e` |
| `.claude/papers/v1-short/fig4_bibo.png` | `708b6fae2dadc755291b07c2962d102f` |
| `.claude/papers/v1-short/fig_frontier_clean.png` | `bcc5f32dcd85e01740638c6608f26320` |
| `.claude/papers/v1-short/fig_regime_map.png` | `8b1dfddc54b4e48da0254a3bf35b9159` |
| `.claude/papers/v1-short/paid_access_reach.png` | `fc372ae54cf39d7181f68837ad0e463b` |

**Method.** Each figure is regenerated by the *staged* generator from
`.claude/scratch/v1-replot-pass/`, edited only in text/layout, and re-run under
`tap.py` — the data tap that hashes every numeric array handed to a matplotlib
plotting call. Digests are compared against the staged baseline before the PNG is
installed. The `pj_sub.tex` insertion is done by `insert.py`, which re-checks the pin,
asserts each anchor matches **exactly once** against the whole file, asserts no task
glyph or retired term is in either caption, and writes only if all assertions pass.

---

## 1. ⛔⛔ A1 — `fig1_certificate.png`: the fix was **already in the banked figure**

**No change was made. None was needed.** The banked PNG already carries the corrected
string; `priced` is not in it.

| | value |
|---|---|
| annotation as actually drawn (harvested from the live `Text` artist) | `squeeze: beyond\nthe swept $\zeta \leq 2.0$` |
| occurrences of `priced` in the generator | **0** (positive control `squeeze` = 5) |
| occurrences of `priced` among the 81 text artists drawn into the PNG | **0** (positive control `squeeze` = 3) |
| `figs/fig1_certificate.png` md5 before = after | `47adc87bf09ccef4fbf08df504584677` |
| re-run of `new_fig1.py` reproduces the banked PNG | byte-identical ✅ |

The task's premise — that panel (a) reads `squeeze priced out of the swept ζ ≤ 2.0`
— was **stale at scoping**. Filesystem timestamps show why: the prior spoke's
`new_fig1.py` was last written **20:47:45** and the banked PNG **20:48:19**, i.e. the
corrected string landed ~18 minutes *after* the rest of the replot pass (fig4 and the
frontier are stamped 20:33:11). The Advisor's §A1 note ("the corrected string was
agreed with the Head but never written into the prior task file") describes a fix that
did in fact reach the figure. **Nothing in fig1 was touched:** title, both panel
headings, `ΔV = 0`, and `state-replacing map` ×3 are all as banked.

## 2. A2 — `fig_frontier_clean.png`: detached tick, wrapped labels, panel symmetry

Generator: `.claude/scratch/v1-figure-fix/fix_frontier.py` (variant `pad`).

### 2.1 The detached `500`
**Cause found.** The staged generator ends with an explicit two-row hack:

```python
for a in ax:                      # drop the crowded "500" label onto a second row
    a.get_xticklabels()[0].set_y(-0.095)
    a.xaxis.labelpad = 7.0
```

Both lines are **removed**. The crowding they were compensating for does not exist at
the figure's true printed width: measured gap between the `500` and `1000` tick labels
is **+4.33 pt** (positive = clear). All four labels are now ordinary tick labels on a
single baseline — measured y-spread of the four tick-label boxes = **0.000 px** on both
panels (was: label 0 displaced by 0.095 axes-heights ≈ 9.6 pt below the other three).

| tick pair | measured gap (printed pt) |
|---|---|
| 500 → 1000 | **+4.33** |
| 1000 → 2000 | +22.02 |
| 2000 → 4000 | +61.88 |

### 2.2 y-axis label wrap
| panel | before | after | height / axes height |
|---|---|---|---|
| left | `CLU-EBM storage\nfidelity` (2 lines) | `CLU-EBM storage fidelity` (1 line) | 98.8 pt / 105.4 pt |
| right | `CLU gated\naccuracy` (2 lines) | `CLU gated accuracy` (1 line) | 79.9 pt / 105.4 pt |

Both fit inside the axes height. **No word changed** — only the line break was removed.
To make the longer label fit, the canvas height was raised 2.05 → 2.20 in, which takes
the axes height 94.6 → 105.4 pt.

### 2.3 Panel symmetry
The left title must stay two lines: a one-line
`Fidelity vs epochs (corr=0, ne3, 3 seeds)` measures **184.0 pt** against a
**152.4 pt** panel — it does not fit, and the task forbids dropping `3 seeds`.
Symmetry was instead obtained by giving the right title an **empty second line**
(`"Gated acc vs epochs" + "\n "`), so both title boxes are **18.6 pt** tall and both
titles' visible text is top-aligned at the same height.

⛔ **The right title's text is unchanged** — `Gated acc vs epochs`, no word added. The
padding is a blank line, not prose. Both panels now have: identical axes boxes
(153.4 × 105.4 pt), identical title-box heights, single-line y-labels, identical
legends and identical tick treatment.

✅ Left correct, verified: Hopfield band still absent; right title still
`Gated acc vs epochs`; left title still carries `3 seeds`; all four kv curves and both
panels present (8 tapped data calls = 4 kv × 2 panels).

## 3. A3 — `fig4_bibo.png`: legend off the data

Generator: `.claude/scratch/v1-figure-fix/fix_fig4.py`.

**The defect, quantified** (`probe_fig4_baseline.py`, run against the *unmodified*
staged generator): the `loc="center left"` legend covered **24.3 % of the axes area**
(325 666 px²) and hid:

| artist | fraction of its plotted length under the legend |
|---|---|
| wormhole, screen ignored (ablation) | **41.6 %** |
| state-replacing map (coincides with ablation) | **41.6 %** |
| wormhole + screen (refuses exit) | **54.7 %** |
| coercive edge $x_b$ = 3.54 | 32.9 % |
| escape radius | 0 % |

This is not cosmetic: b = 1.0/2.0/3.0 is exactly where all three arms **coincide**
(r\* = 1.0114 / 2.0079 / 3.0125 identically for all three), which is the caption's
"the state-replacing map coincides exactly with the screen-ignored ablation" claim.
The legend was sitting on the evidence for that sentence.

**Change:** one line. `loc="center left"` (inside) →
`loc="upper center", bbox_to_anchor=(0.5, -0.155)` — the legend moves **outside**,
below the x-label. Canvas height 2.94502 → 3.84000 in to hold it.

**Post-fix occlusion test (hard assertion in the generator; the run aborts if it fails):**

| check | result |
|---|---|
| legend ∩ axes area | **0 px²** |
| pink "screen ignored" points under legend | **0 / 4000** |
| purple "state-replacing map" points under legend | **0 / 4000** |
| green "wormhole + screen" points under legend | **0 / 4000** |
| escape radius / coercive edge endpoints under legend | **0 / 2** each |

The **data area is preserved**: axes 273.5 × 158.7 pt → **273.6 × 159.2 pt**
(+0.1 / +0.5 pt). No limit, scale, colour or z-order changed.

A two-column legend below the axes was measured and **rejected**: 331.1 pt wide against
a 318.2 pt canvas — `unclip` reported `STILL CLIPPED`. A legend kept *inside* the axes
was also rejected: it is 202.1 pt wide = 73.9 % of the axes width, and the only
data-free bands are 41 pt tall (between r\*=3.01 and the escape line) against a 52.2 pt
legend, so keeping it inside requires either occluding a curve or padding the log axis
with ~1.8 empty decades.

✅ Left correct, verified: title (two lines, unchanged), the three
`screen`/`state-replacing` legend entries (unchanged strings), and the red annotation
with `free` intact — all present in the 47 harvested text artists.

## 4. ⛔ Data tap — every plotted value proven unmoved

`tap.py` hashes each numeric array passed to `plot/errorbar/axhline/axvline/...`;
the digest is an order-insensitive sha1 over the sorted multiset of per-call hashes.

| figure | staged baseline digest | this pass | calls | verdict |
|---|---|---|---|---|
| `fig1_certificate` | `88273eb233a356633d565825aacd7351fd9f9343` | `88273eb233a356633d565825aacd7351fd9f9343` | 13 = 13 | **MATCH** |
| `fig4_bibo` | `ae96d8b34e077002514d2a36e7a14e08c46329f9` | `ae96d8b34e077002514d2a36e7a14e08c46329f9` | 5 = 5 | **MATCH** |
| `fig_frontier_clean` | `e02915edfab102311999edffb248c2908ad55fe2` | `e02915edfab102311999edffb248c2908ad55fe2` | 8 = 8 | **MATCH** |

Baselines are `.claude/scratch/v1-replot-pass/digests/new_*.json`. The full sorted call
list (not only the sha1) was compared element-wise; all three are identical.
Independently, re-running all three staged generators reproduced the banked PNGs
**byte-for-byte** (`47adc87b…`, `5fdd6bd2…`, `2a217098…`), so the baseline itself is
reproducible and not merely asserted.

## 5. Printed-box measurements — from the built PDF, via `mutool trace`

No `\linewidth` arithmetic was used. `\linewidth` measures **468.00288 pt** in the
built PDF (letterpaper, `geometry margin=1in`). "BEFORE" for fig4 and the frontier is a
real build (`build/beforefigs/`) of the *inserted* tex carrying the *old* PNGs, so the
comparison is like-for-like at the same `\includegraphics` width.

| figure | printed box BEFORE (pt) | printed box AFTER (pt) | ΔW | ΔH |
|---|---|---|---|---|
| `fig1_certificate` (`width=\textwidth`) | 468.003 × 169.840 | 468.003 × 169.840 | **0.000** | **0.000** |
| `fig4_bibo` (`width=0.68\linewidth`) | 318.235 × 212.157 | 318.259 × 276.497 | +0.024 | **+64.340** |
| `fig_frontier_clean` (`width=0.82\linewidth`) | 383.764 × 147.601 | 383.772 × 158.405 | +0.008 | **+10.803** |

### Type at printed size (Part B: ticks ≥ 7, labels/legend ≥ 8, titles ≥ 9 pt)

| figure | canvas→print scale | title | label/legend | tick | verdict |
|---|---|---|---|---|---|
| `fig1_certificate` | 0.998470 | 9.006 | 8.008 | 7.009 | ✅ |
| `fig4_bibo` before | 1.000550 | 9.005 | 8.004 | 7.004 | ✅ |
| **`fig4_bibo` after** | **1.000060** | **9.001** | **8.000** | **7.000** | ✅ |
| `fig_frontier_clean` before | **1.138900** | 10.250 | 9.111 | 7.972 | ⚠ oversized |
| **`fig_frontier_clean` after** | **1.000030** | **9.000** | **8.000** | **7.000** | ✅ |

⚠ **Third defect found in the frontier, not in the task.** The staged PNG was built for
a 4.680 in printed box, but C2 specifies `width=0.82\linewidth` = 5.330 in — a
**1.1389× upscale** that would have printed its type at 10.25/9.11/7.97 pt, visibly
larger than fig1 and fig4 on the same page and inconsistent with the house 9/8/7.
The canvas was re-targeted to 5.330 in exactly, so scale is now 1.000030.

## 6. Insertions — exactly two hunks

`diff` of `pj_sub.tex` before → after: **2 hunks, 20 inserted lines, 0 deleted lines,
0 modified lines.**

### C1 — after `\subsection*{B.2 The BIBO Battery}` (anchor matched 1×, was l.270)
```latex
\begin{figure}[t]\centering
\includegraphics[width=0.68\linewidth]{figs/fig4_bibo.png}
\caption{\textbf{The BIBO battery (App.~B.2, verification grade).} Maximum excursion radius
$r^\ast=\max_t\|q_t\|$ at horizon $2T$, against the requested exit locus $b$. Exits inside the
coercive component stay bounded for every arm. Beyond the coercive edge $x_b=3.54$ the
unscreened arms grow as $r^\ast\propto T$, while the screened wormhole holds at $r^\ast=0.09$
because it \emph{refuses} the jump rather than making it safe. The state-replacing map
coincides exactly with the screen-ignored ablation, so what buys boundedness is the screen and
not the jump. Note $b=5.0$: the energy change is exactly zero and the exit still escapes.}
\label{fig:bibo}
\end{figure}
```

### C2 — after `\subsection*{C.3 Regime Map: Capacity Axis and Epoch Frontier}` (anchor matched 1×, was l.326)
```latex
\begin{figure}[t]\centering
\includegraphics[width=0.82\linewidth]{figs/fig_frontier_clean.png}
\caption{\textbf{Epoch-scaling frontier (App.~C.3, evidence grade; $n=3$ seeds).} CLU-EBM
storage fidelity and gated accuracy against training budget for kv$\in\{32,64,96,128\}$.
Required epochs scale with capacity rather than saturating at a fixed ceiling, and kv$32$
over-trains between $2000$ and $4000$ epochs. Note this panel is $n=3$, not the $8$ pooled
seeds of the capacity axis below.}
\label{fig:frontier}
\end{figure}
```

Both captions are the Advisor's draft **verbatim**, with the single mandated
edit: the `⛔` glyph before "Note this panel is $n=3$" is stripped and the sentence
renders as plain prose. Glyph count in the inserted text: `⛔`/`⚠`/`⭐` = **0**.
Both float `[t]`; each lands at the top of the page carrying its own subsection —
Fig. 2 (BIBO) on **p. 11**, Fig. 3 (frontier) on **p. 13**.

## 7. Build

`pdflatex` (TeX Live, `/Library/TeX/texbin`), 2 passes, `-halt-on-error`.

| build | errors | undefined refs | LaTeX warnings | pages | main text | appendix | refs |
|---|---|---|---|---|---|---|---|
| before insertion, banked figs | 0 | 0 | — | **14** | 8.44 pp | 4.15 pp | 1.41 pp |
| after insertion, **old** figs | 0 | 0 | — | 15 | 8.44 pp | 5.11 pp | 1.45 pp |
| **after insertion, new figs (final)** | **0** | **0** | **0** | **15** | **8.44 pp** | **5.23 pp** | **1.33 pp** |

Grep-negatives on the log are positive-controlled (`pdfTeX` = 1, `Overfull` = 1 on the
same log, so the pattern engine is live).

**Page-cost decomposition.** The two insertions cost **+0.96 pp** of appendix; the
re-renders add a further **+0.12 pp**. **Main text is 8.44 pp in all three builds —
unchanged to 0.01 pp.** Both figures are appendix floats and neither touches the
main-text budget.

## 8. Residual sweep — positive-controlled

`priced` · `receipt` · `ledger` · `no-physics router`. PNGs carry no text layer, so
each generator was re-executed and **every** `matplotlib.text.Text` artist actually
drawn was harvested (`sweep_png_text.py` → `png_text_harvest.json`), then searched.

| target | artists | `priced` | `receipt` | `ledger` | `no-physics router` | positive control |
|---|---|---|---|---|---|---|
| `fig1_certificate.png` | 81 | 0 | 0 | 0 | 0 | `squeeze` = **3** ✅ |
| `fig4_bibo.png` | 47 | 0 | 0 | 0 | 0 | `screen` = **3** ✅ |
| `fig_frontier_clean.png` | 58 | 0 | 0 | 0 | 0 | `epochs` = **4** ✅ |
| the two new captions (20 lines) | — | 0 | 0 | 0 | 0 | `wormhole` = **1** ✅ |
| whole `pj_sub.tex` (402 lines) | — | 0 | 0 | 0 | 0 | `wormhole` = **21** ✅ |

All sweeps used `/usr/bin/grep -o … | wc -l` (occurrences, not lines) or Python
substring counts; every negative carries a positive control on the same corpus.

## 9. Caption edits believed needed but NOT made

Listed, not applied — per task §Deliverables 5.

1. **C1, "at horizon $2T$".** The banked data has `r_star_2T` **and** `r_star_T`; the
   figure plots only the 2T series, so the caption is correct, but B.2's own prose one
   line below says "at horizons $T$ and $2T$". A reader may expect two series. Suggest
   the prose, not the caption, acknowledge that the figure shows 2T only.
2. **C1, "$r^\ast\propto T$".** The figure is a single horizon, so linear growth in T
   is not visible in it — it is inherited from §3.3/B.2's table. Consider "(App. B.2
   table)" after that clause so the figure is not read as evidence for a claim it
   cannot show.
3. **C1, "$r^\ast=0.09$".** The plotted value is 0.09084149152040481, identical at
   b = 3.6, 4.0 and 5.0. "holds at $r^\ast=0.09$" is accurate to 2 s.f.; no change needed.
4. **C2, "kv$32$ over-trains between $2000$ and $4000$ epochs".** Supported on the
   gated-accuracy panel (1.00 → 0.89) but **not** on the fidelity panel, where kv32
   ends at 0.99. Suggest "over-trains in gated accuracy between 2000 and 4000 epochs".
5. **C2, "the capacity axis below".** Correct as placed — the 8-pooled-seed table does
   follow the figure on p. 13. This wording breaks if the float ever moves to a
   different page; "the capacity axis of this subsection" would be placement-proof.

## 10. Flagged, not acted on

- ⚠ **`pj_sub.pdf` in this directory is stale and was already stale before this pass.**
  It is 14 pages and embeds a **2480 px** fig1 — i.e. the *pre-replot* figure, not the
  2604 px one banked at 20:48. It predates both the replot pass and this one. It is
  outside this task's declared write scope (`figs/*.png` + `pj_sub.tex`) so it was
  **not** overwritten. The current 15-page build is at
  `.claude/scratch/v1-figure-fix/build/after/pj_sub.pdf`. **Someone should be told to
  refresh it**, or a reviewer opening the directory will read a two-passes-stale paper.
- ⚠ **Placement alternative (report, do not act — task §C).** The frontier is the
  compute dial's epoch story and could sit in §4.1 main text. It is in the appendix
  here. Measured cost if moved: the figure's printed box is 383.77 × 158.41 pt, and its
  caption runs 5 lines; main text is currently **8.44 pp** against a ~5 pp target, so
  moving it in would add roughly **0.35 pp** to a block already 3.4 pp over. **The Head
  decides.**

## 11. Reproduce

```
cd .claude/scratch/v1-replot-pass
../../../.venv/bin/python ../v1-figure-fix/runtap.py base_fig1     new_fig1.py
../../../.venv/bin/python ../v1-figure-fix/runtap.py base_fig4     new_fig4.py
../../../.venv/bin/python ../v1-figure-fix/runtap.py base_frontier new_frontier.py
cd ../v1-figure-fix
FIG4_NCOL=1 FIG4_H=3.84                     ../../../.venv/bin/python runtap.py fix_fig4     fix_fig4.py
FRONTIER_VARIANT=pad                        ../../../.venv/bin/python runtap.py fix_frontier fix_frontier.py
../../../.venv/bin/python probe_fig4_baseline.py     # the 24.3% / 54.7% occlusion numbers
../../../.venv/bin/python sweep_png_text.py          # §8
../../../.venv/bin/python insert.py                  # pin-checked, assertion-guarded
```
Environment: `.venv` Python, **matplotlib 3.10.8, numpy 2.4.1**, Agg backend, dpi 400,
`pdflatex` TeX Live at `/Library/TeX/texbin`, `mutool 1.27.2`, `pdftotext` (poppler).
No JAX, no model code, no experiment: all three figures are re-renders from banked JSON.
