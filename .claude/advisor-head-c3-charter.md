# HEAD + ADVISOR — CAMPAIGN 3 CHARTER: Full CLU on real data

**Filed 2026-08-12 · Head + external Advisor · BINDING for all Campaign-3 scoping.**
**This document is deliberately LIGHT. It states the vision, the venues, the invariants, and the current wave — and points at the archive for everything else. ⛔ Do not grow it back into the C2 charter: corrections and wave verdicts are dated addenda kept to the same density as this base.**

Archive (read-only history; consult when returning to the shorts, the toys, or any C2-era claim):
`.claude/advisor-head-c2-charter.md` (C2 ledger, closed at Add.16) · `.claude/advisor-handover.md` (Advisor continuity through C2) · `.claude/handover_context.md` (Hub log through C2) · `.claude/advisor-head-intervention.md` (⭐ still in force — §5 monitors, §6 admissibility criteria, §8 prohibitions) · `.claude/claims_matrix.md` + `.claude/negative_results.md` (registries, live and shared across campaigns).

---

## 1. The vision (Head, 2026-08-12)

**The CLU is a general memory primitive for models of the physical world.** It works on text, but its intended home is **physical AI and world models: spatio-temporal modalities — long videos, and above all LONG-HORIZON MULTIVARIATE TIME SERIES** (alignment with the Head's wider research, incl. HEPA). The north star is unchanged (C2 §A33, verbatim there): a continuous, semantically and physically consistent latent space that an unseen sample can *navigate*, instead of a black-box latent.

**The paper program:** the full-CLU LONG paper(s) — the primitive, the control system, the real-data result at scale, the capabilities others structurally lack (anytime reads, byte-exact deletion, settable lifetimes), with the physics-predicts-the-knobs laws as the explanatory spine. Shorts and B′ remain Head-sequenced products of the archive. Venue/timing = the Head's alone.

**Why the pivot is principled (C2 Add.16):** the toy era ended by measurement — the store was exonerated (0.86 exact-set under oracle addressing) and the blocker localized to φ's decomposition (the launch head). That blocker is only honestly testable where φ *earns* its decomposition: real data. A designed family that hands φ the features cannot test it.

## 2. Claim architecture (carried from C2 §A13, re-homed)

- **Primary claim = tier iii:** the CLU as the memory of a streaming block on real data, at **matched params AND matched state-bytes**, vs the **system-level swap** (TTT-class cell — the two-sided control; GRU cannot be matched both ways) + tuned rivals (real Mamba/Mamba-2 · Gated DeltaNet-2 · sliding attention). Emergence claims live here and only here.
- **The organization question (ex-tier-ii) = in-block ablation:** same block, physics write vs matched non-physics write at identical bytes. No standalone organizer venue exists any more.
- **Capabilities (deletion, lifetimes, anytime curve) = the "and also" position, forever** (intervention §8.3).
- **Toy substrate = engineering smoke/regression instrument only. ⛔ Never a claim venue, never a wave deliverable.**

## 3. Venues (Head-ratified 2026-08-12)

| track | venues | status |
|---|---|---|
| **A — bounded-state LM** (ready; community currency) | **enwik8** (primary, 26–47 M, bpc) · WikiText-103 · **PG-19** (long-horizon) · within-document retention/revisit slices computed on real text | pilot infra + D5 anytime curve built; CSF3 runs 1–2 banked |
| **B — spatio-temporal / physical AI** ⭐ the Head's direction | long-horizon **multivariate time-series** first (forecasting · multi-step rollout · regime re-identification · prognostics); long video later | **scout-gated** — see the admissibility rule below |

⛔ **Admissibility (binding, no exceptions):** every candidate passes intervention §6's five criteria, **with criterion 4 MEASURED, not argued** — the matched-bytes exemplar-store tripwire (the C2W10 instrument) runs before any venue is adopted. The theorem has SIX confirmations; we do not buy a seventh. Known hazards in track B, on the record: classic streaming venues die by criterion 4 at matched bytes; long-horizon forecasting suites often fail criterion 2 (linear/naive baselines at the frontier); ⛔ no CAFE C-MAPSS number is externally comparable (the banked label-bug report). Banked assets for track B: `chlu/eval/rollout_diag.py` (+ the standing rule: **a rollout must beat persistence at n = 1 before any rollout claim**), the CAFE bug report, the C2W10 drift maps + embargo methodology.

## 4. Compute doctrine

CSF3 credits effectively unconstrained; **per-job limits stand (2×A100, 4-day)**. ⛔ **Credits change SCHEDULING, never CONTROLS.** Multi-seed = seeds-as-jobs. Iteration-by-resume is first-class (the journal-fingerprint repair). Anything that works ships with its swap and becomes a paper ablation; an uncontrolled win never enters the paper.

## 5. Invariants (the epistemics, unchanged from two campaigns — the program's most valuable asset)

Prereg with numeric falsifiers BEFORE each job ladder · kill-conditions built first · ≥ 3 seeds before any paper number · the tier's own control on every claim (system swap; in-block write swap; launders as inherited diagnostics — ⛔ never a pass condition on a component gate, C2 §A33.1) · dynamic-evaluation substitute column on every LM table · byte ledgers on every arm incl. φ · declared NOT-RUNs never nulls · a gate leg must be scorable on an arm capable of both outcomes (C2 §A37) · reliability AND magnitude on every proxy (C2 §A43.7) · quote the curve, not the endpoint · N94 epoch discipline · every γ statement read-budget-scoped · depth ≠ feature importance (§A23.5, still active until measured on a live-outer-objective rig) · wells never named semantically · "CLU-former" stays a placeholder · registries stay current per wave · ⛔ **push `clu-dev` only, never `origin`** · ≤ 3 engineer worktrees, declared file ownership, mechanical (file-based) preconditions for gated spokes.

Design rules carried (all pytest-pinned in the repo): placing write · co-scaled widths · d ≤ 12 reach ceiling (d = 16 inert at honoured budget) · address block ≠ task features · label-free organization, supervision at the read head · P1 partition (`erosion_partition=True` ships) · the instrument suite (census · G-ADDR · K9 · U telemetry · lifecycle · trash region). Open design questions that travel: the launch head / `d_addr` (the C2W11 bound; the ceiling probe is a free config input) · `k > F` over-launching + soft deflation + keep-the-continuous-coordinate reads · wormholes at large d (C2W9 deferred, trigger banked) · kinetics selectors + two-scale hierarchy (ratified, unbuilt) · §A31.2 (likely reduces to launch-head assertion — theorist).

## 6. C3W1 scope (one fresh lead; the Head spawns; task files per the standing protocol)

1. **`c3-benchmark-scout`** (web-scout, zero worktrees, runs first) — Track A: current baseline numbers/conventions at 26–47 M (Mamba-2 · GDN-2 · TTT/Titans · sliding attention) on enwik8/WT-103/PG-19; PG-19 feasibility at our weight class. Track B: candidate long-horizon multivariate TS venues screened per §3's admissibility rule (incl. what a criterion-4 tripwire run would need per venue); data availability/licensing on CSF3. Deliverable: a cited brief + a shortlist with a per-venue admissibility scorecard.
2. **`c3-csf3-harness`** (engineer, 1 worktree) — the real-data harness on the merged pilot infra: data plumbing for Track A, eval slices (retention/revisit), seeds-as-jobs inside the per-job envelope, resume-first job ladders, the smoke config (local, minutes, ⛔ never a claim venue). Precondition (mechanical): the pilot-ttt merge landed on `main`.
3. **C2 close-out debts — ⛔ NOT C3W1's (Head ruling 2026-08-12, later): the C2W11 Hub scopes and executes ALL of C2's closing tasks and final toy checks** (relay handed to the Head; list in C2-charter Add.16 §A45 as amended). C3W1 only *consumes* two of its outputs: the pilot-ttt merge on `main` (the harness task's mechanical precondition — verify the merge commit exists, not the promise) and the `d_addr` ceiling-probe numbers (an in-block store config input).
4. **CSF3 sequencing:** Advisor adjudicates **runs 1-vs-2 (the ψ ablation — both done, artifacts owed to the Advisor)** → **run 3 = C3's first job** (leak-closure ablation per `PREREG-LeakAblation.md` + the TTT arm per the Hub's default ruling + D5). The `d_addr` ceiling probe (laptop, minutes) rides as a config input for the in-block store.

**Wave tags `[C3Wn]` in `.claude/c3-handover.md` (the NEW light Hub doc — the C2-era `handover_context.md` is archived; leads do NOT append to it).**

---

*Filed by the Head + Advisor, 2026-08-12. Wave verdicts and corrections append as dated addenda below this line. Keep them short.*

---

# C3 ADDENDUM 1 (2026-08-13) — C3W1 venue + budget rulings (Head-ratified) · ⭐ CAMELS is criterion 4's first measured survivor · N-CMAPSS adopted as an APPLICATION venue · the state-byte convention · the run-3 exemption

**Basis: `.claude/outputs/c3-benchmark-scout.md` (accepted) · `.claude/outputs/c3-trackb-tripwire/VERDICT.json` (Advisor re-verified from raw) · the `[C3W1-REVIEW]` escalations. All items Head-ratified 2026-08-13.**

## 1. Venues (final)

- **Track A ADOPTED as scouted:** enwik8 primary · WT-103 secondary · PG-19 internal retention instrument, ⛔ never an external comparison. FineWeb-Edu stays a priced paper-stage option. ⛔ Dyn-eval's published 0.94 bpc is at 277 M — never beside a 40 M number; dyn-eval is an **arm** we run, not a citation.
- ⭐⭐ **CAMELS-US ADOPTED — Track B primary, and criterion 4's FIRST measured survivor** (E = 0.2559 at matched bytes vs S = 0.758; `margin_rel` −0.6625 vs the −0.02 threshold; no fire vs all 14 references; **no fire at 536× budget**). Mechanism as pre-registered: the target depends on **unobserved accumulated storage**. Production loader = a normal engineer task; **`daymet` forcing only** (`maurer` measured defective: tmax == tmin on >99 % of days, 20/20 basins); ⛔ the shipped SAC-SMA output is calibration-period, never quoted against the LSTM; the reference currency is the **median 0.758**, not the mean 0.72; licence cc-by-4.0 (Zenodo 15529996).
- ⭐ **N-CMAPSS DS02 ADOPTED AS AN APPLICATION VENUE (Head ruling — company-research relevance, e.g. HEPA), ⛔ NEVER a primary-claim venue.** Three guardrails, binding forever: **(a)** every N-CMAPSS table PRINTS the matched-bytes exemplar reference row (**7.988 RMSE**, and its tie with the weakest published deep model, FNN 7.89) — disclosure is the defense; claims take capability/application form only, ⛔ never "beats SOTA on RUL"; **(b)** its CLU-native questions are the persistent-store-across-units question (C2W10's re-homed VALUE question), the anytime curve, and interpretable-store probes; **(c)** the CAFE embargo is absolute, both directions — original NASA files only; HEPA comparisons internal-only unless re-derived on corrected labels. ⭐ **Guardrail (a) is a CLAIMS obligation, not a code detail (Head, 2026-08-13): it lives in `claims_matrix.md` as a never-quote row — folded by the extended A8 spoke (§3 below) — with the loader assertion as belt-and-braces, never as the only enforcement.**

## 2. The state-byte budget (the [C3W1-REVIEW] escalation, ruled)

- **Convention: TOTAL state bytes, AS-DEPLOYED dtype** (Hub R1 endorsed; R3's open half closed: ⛔ no dtype normalization — the fp32 store pays its real cost). `dtype_bytes` declared per row, never inferred. ⛔ Never quote a state-byte figure without its convention; ⛔ the scout's single-store 1,966,080 B row is never quotable as "the CLU's state".
- **The ceiling digit binds at the rival-ladder PREREG, not before** — a round number both swap members fit under naturally, no arm sitting exactly on it, others shrink-to-match, occupancy reported. The ladder prereg (digit + pinned rival configs incl. the `flash-linear-attention` 3× trap + the ≥3-seed job plan) **comes to the Advisor before any ladder arm trains**.
- ⭐ **Run 3 is EXEMPT by pre-registration, not re-ruled:** a `preregistered_continuation` exemption, fingerprint-verified bit-identical to run 2 except `erosion_partition`, printed in the ledger. ⛔ Run-3 results are never quoted as budget-compliant ladder rows. Hub R2 endorsed (`d_addr` stays 8; any change is claims-relevant, own prereg).

## 3. Registry fold (extends C2's A8 — one curator pass, two campaign sections, accounting kept separate)

The A8 spoke (C2W11's owed `claims_matrix.md` fold) is **extended** with a C3 section: the N-CMAPSS guardrail-(a) row · the CAMELS caveats (SAC-SMA, maurer, median-currency) · the state-byte convention never-quotes · the dyn-eval category error · the run-3 exemption labelling. ⛔ The matrix stays unsafe for paper-writers until this lands — it is the wave's most urgent zero-worktree item.

## 4. Banked finding (from the C2 close-out, consumed this wave)

**N312:** placement headroom 0.0851 → 0.0375 → 0.0109 → 0.0000 across `d_addr` 4/6/8/12 ⇒ **raising `d_addr` does not open the tier-ii question** — the launch-head problem is a design question, not a dial. `addr_dim` default stays 8.

*Filed by the Advisor, 2026-08-13, Head-ratified same session.*

---

# C3 ADDENDUM 2 (2026-08-13) — CSF3 runs 1-vs-2 ADJUDICATED (the pre-registered ψ ablation): a TIE on ψ, and the deeper shared result — the store is CONTENT-INERT at scale while the memory pathway is not · the toy null-with-mechanism REPRODUCES at 28.5 M

**Advisor, adjudicated from raw (`.claude/outputs/cluformer-pilot/csf3_outs/run{1,2}/pilot_pilot_seed*_S4.json`; config identity verified — the runs differ by exactly `psi_payload_residual False→True`, source `q_star`). Paired seeds {0,1}; run 2 adds seed 2; ⚠ run-1 seed 2 DIED mid-S2 at step 3999 (Aug 8) — resumable post-fingerprint-fix, resume recommended for the n=3 pairing.**

1. **The ψ ablation verdict: TIE at n=2 pairs.** Paired dyn-eval deltas are mixed-sign and ≤ 0.01 bpc (CLU absolute: seed 0 1.8518→1.8611 worse, seed 1 1.8660→1.8639 better; CLU-vs-none margin: 0.0479→0.0386 / 0.0441→0.0462). ⛔ **The ψ-payload-residual hypothesis — one of the pilot's three §5.3 candidate mechanisms — is measured NOT to be the binding constraint at scale.**
2. ⭐⭐ **The shared result of BOTH runs, and it outranks the ablation: the store is CONTENT-INERT at scale.** Live-vs-blank ≤ **0.0013 bpc on 5/5 completed legs** (run 2: −0.0003/−0.0001/+0.0006 — under a grain); self-probe acquisition **at chance 5/5**; the addressing monitor tripped on every leg. Meanwhile the CLU beats the memory-deleted arm by ~0.04 bpc — ⛔ **that margin is NEVER quotable as memory value**: the `none` arm has 2.03 M fewer params (26.53 M vs 28.56 M), blanking the store moves nothing, **a params-matched GRU beats the CLU by 0.014–0.024 on every leg, and echo ≈ CLU**. The 0.04 is pathway machinery, not stored content. Quotable form only with GRU + echo + blank columns beside it.
3. **This is the C2W4 pilot's null-with-mechanism REPRODUCED AT 28.5 M with the ψ fix controlled for** — and it converges with C3W1's G4 (smoke bpc bit-identical across 16× atoms) and C2W11's oracle datum (the store composes when addressed, toy-side). The open question at scale is the flat-curve disjunction: **carries nothing OR cannot be addressed.** ⇒ **the phase-1 store-liveness diagnostic (C3 Add.1-era ruling) is now the ladder's decisive first measurement, not a hygiene item.** ⚠ The C3 arm is NOT the pilot arm (G-B geometry · per-layer calibration · the launch-head design inputs) — the pilot's inert store does not pre-judge it; it does set the null hypothesis.
4. **Bookkeeping:** the TTT arm is NaN on all 5 legs of both runs (the known inner-loop divergence; the fix rides run 3 per PILOT-TTT-RULINGS) ⇒ **the two-sided byte-honest control is a declared NOT-RUN for runs 1–2**; the GRU comparison is params-matched only (state ARITHMETIC_ONLY). Scale traj/point gradient ratios 0.17–2.3 (noted, not interpreted). Monitors fired loudly throughout (7–8/11) — the instruments work at scale. ⛔ Standing: no run-1 or run-2 number is ever the scale verdict alone; run 3's leak-ablation reading (2-vs-3) inherits this addendum as its baseline interpretation.

*Filed by the Advisor, 2026-08-13, after raw re-derivation.* **✅ Head ruled (2026-08-13, later): run-1 seed 2 is NOT resumed — a declared NOT-RUN, permanently; run 1 stays at 2 seeds; and ⛔ RUN 1 IS NEVER REPORTED DIRECTLY — it exists only inside the paired ψ-ablation reading (a strengthening of the standing "no run-1 number is ever the scale verdict" rule). Consequence, on the record: the ψ-ablation TIE is permanently an n=2-pairs reading and carries that power caveat wherever it is cited. Item-2's never-quote (the 0.04-vs-none margin) ratification: pending an explicit Head yes.*

---

# C3 ADDENDUM 3 (2026-08-26) — CSF3 run 3 ADJUDICATED (the leak-closure ablation, runs 2-vs-3): the partition is MECHANICALLY CONFIRMED and costs nothing · the accidental write→φ channel was carrying nothing at scale · ⛔ the store is content-inert for the THIRD consecutive run

**Advisor, adjudicated from raw (`csf3_outs/run3/pilot_pilot_seed{0,1,2}_S4.json`, 3 seeds, all stages S1–S4, `not_run: []`). Basis: `PREREG-LeakAblation.md`'s three registered measurements. ✅ The one-token rule HELD — the run2→run3 flag diff is exactly `memory.erosion_partition: absent → True`, plus three journal-predating `refresh_*` fields sitting at their own defaults (the ratified §A20.4 guard loosening working as designed) ⇒ the ablation is valid.**

1. **LEG 1 — Δbpc: NO SIGNIFICANT HARM; the registered expectation HOLDS.** Paired per-seed (run3 − run2): static `−0.00147 / +0.01198 / +0.01134` ⇒ **+0.00728 ± 0.00877 (2 SE)**; dyn-eval `−0.00124 / +0.01130 / +0.01031` ⇒ **+0.00679 ± 0.00805**. Both straddle zero. ⚠ Stated honestly: the point estimate is **mildly adverse and 2 of 3 seeds are worse** — the claim is "no harm at the 2-SE bar", never "the partition improves bpc". ⛔ **The >2 SE regression that would have re-priced Add.9 §A28.1 upward did NOT fire** ⇒ the accidental write→φ channel was **not teaching φ anything measurable at scale**; the designed replacement stays a registered lever, not an urgent one.
2. ⭐⭐ **LEG 3 — THE PARTITION IS MECHANICALLY CONFIRMED LIVE AT 28.5 M, BITWISE.** Settled-point `grad_phi` **0.6587 → 0.0 exactly**; `grad_store` **0.0986 → 0.0 exactly**; trajectory `grad_store` → **0.0 exactly**, 3/3 seeds. ⇒ **the write→φ placement channel is CLOSED at scale — C2W6's K1 result reproduced 28.5 M parameters up.** ⭐ The read channel stays alive (trajectory `grad_phi` 0.0811 → 0.1491), which is exactly the designed behaviour: the write-side placement path is cut, the read-side is not. ⛔⛔ **NEVER-QUOTE: `ratio_traj_over_point = inf` under the partition.** The denominator is exactly zero by construction, so the ratio is **UNDEFINED, not a measurement** — it may never be read as "the trajectory read is infinitely better than the point read".
3. ⚠ **LEG 2 — depth-trough telemetry NOT CAPTURED.** `store_health` is empty in every pulled artifact and no depth series exists in `monitors_during`; the registered trough statistics live in the cluster-side `[watch/clu_store]` job logs, which were not pulled. ⇒ **a declared NOT-RUN for want of the artifact, never a null.** Closing action if wanted: pull the run-2/run-3 job logs and extract the series; ⛔ nothing in legs 1 or 3 depends on it.
4. ⛔ **THE SHARED RESULT PERSISTS — THIRD CONSECUTIVE RUN.** Store content-inertness: `blank − live` = **+0.00024 / +0.00025 / +0.00066** bpc (run 2: −0.00026 / −0.00014 / +0.00062) — deleting the entire store moves the loss by under a thousandth of a bit, 8 completed legs across three runs, no exception. A params-matched **GRU beats the CLU on 3/3 seeds by +0.0259 mean**; **echo ≈ CLU** (−0.0002 / +0.0015 / +0.0114). ⇒ Add.2 item 2's never-quote (the +0.04-vs-`none` margin is pathway machinery, never memory value) **is re-confirmed and now spans all three runs**.
5. **TTT NaN on 3/3 seeds — PREDICTED, DOCUMENTED, NOT A REGRESSION.** `RUN3-LAUNCH.md` v2 registered this in advance: `ttt_normalized_write` is a `PilotConfig` field and deliberately does **not** travel on this run, because carrying it would create a second differing key and break the one-token identity the exemption verifies. ⇒ **the two-sided byte-honest control is a declared NOT-RUN across all three CSF3 runs**; it first exists on the C3 ladder's TTT leg.

⭐⭐ **The adjudication's one sentence, and it is the input to the pivot: the partition works exactly as designed, costs nothing, and closes a channel that turned out to be carrying nothing — while the store it protects remains routed around by the block for the third run running.** The C2W11 oracle datum (the same store composes at 0.8621 when handed correct addresses) means this is an *integration/addressing* failure, not a broken memory.

*Filed by the Advisor, 2026-08-26, after raw re-derivation. ⚠ Head ratification pending on: leg 3's never-quote; leg 2's NOT-RUN disposition.*
