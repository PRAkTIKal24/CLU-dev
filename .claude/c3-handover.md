# C3 HANDOVER — the light Hub doc (Campaign 3: full CLU on real data)

> **Owned by the C3 wave-lead Hubs.** One fresh lead per wave (standing). Read order for a fresh lead: **(1)** `.claude/advisor-head-c3-charter.md` in full — it is short by design; **(2)** the latest `[C3Wn]` entry below; **(3)** the registries when quoting (`.claude/claims_matrix.md`, `.claude/negative_results.md`). ⛔ The C2-era `handover_context.md` is ARCHIVE — consult it for history, never append to it.
>
> **Keep this file LIGHT.** State snapshot edited in place (it describes NOW, not history); one dated `[C3Wn]` log entry per wave event, newest first; details live in `.claude/outputs/*` and are pointed at, not restated. If an entry needs more than ~30 lines, the material belongs in an output file.

## State snapshot (edit in place; verified, not asserted)

- **Repo:** ✅ **`main @ 0644c48`** — the C3W1 harness merged 2026-08-13 (`[hub] merge c3-csf3-harness`, no-ff, from a temp worktree so the shared checkout and both spokes' base ref were untouched). ⭐ **Merge tree is byte-identical to the tested `f98f939`** (`2ebe5e74…`, verified both sides) ⇒ **the 1781 / 0 result carries to `main` without a re-run**; merge-base was exactly `main`, 0 behind / 8 ahead. ⚠ **`clu-dev @ 60d8be4` is now BEHIND main — a push is owed** (⛔ `clu-dev` only; `origin` verified still frozen at `40c2f31`). Worktrees: **wt1** `c3-run3-budget-exemption` · **wt2** `c3-rival-ladder-prereg` (both live, both based at `f98f939`, now an ancestor of `main`); the shared checkout still sits on the pilot branch, clean. ⚠ **The prereg spoke took the CONCURRENT path** ⇒ ⛔ `chlu/eval/byte_ledger.py` is **read-only to wt2**; wt1 owns it.
- **Charter:** ✅ **C3 ADDENDUM 1 filed by the Advisor 2026-08-13** (venue + budget rulings; CAMELS as criterion 4's first measured survivor; N-CMAPSS as an application venue; the state-byte convention; the run-3 exemption). ⛔ The Hub does not edit the charter.
- ~~**`main = clu-dev = 60d8be4`**~~ (superseded above; the C2 close-out verification that produced it stands). ✅ **The C2 close-out is EXECUTED** — pilot-ttt merge (+3 rulings) · post-merge suite · close entry · curator fold · the `d_addr` probe · `clu-dev` push; **11 of 13 debts verified discharged, 2 residual — see the `[C2-CLOSE-VERIFIED]` entry below.** ⛔ `origin` frozen at `40c2f31` — never push it. Zero worktrees; the shared checkout sits on the (now fully merged) pilot branch — harmless, no debt requires moving it.
- **CSF3:** runs 1–2 DONE (no TTT arm; = the pre-registered ψ ablation, Advisor adjudication owed — artifacts to the Advisor). Run 3 NOT launched (= run 2 + `erosion_partition=True`; the leak-closure ablation; `PREREG-LeakAblation.md` on disk; carries the TTT default ruling + D5). ⭐ **The three pilot rulings the run-3 config needs are now LIVE at `.claude/outputs/c2w11/PILOT-TTT-RULINGS.md`** (`ttt_normalized_write` = default OFF / TTT arm SET · the mechanical `.eqx` precondition · the toy-bit-identity never-quote). Per-job envelope: 2×A100, 4-day. Credits unconstrained; controls unchanged.
- **Repo 2026-08-13 (later): `main @ 3c1dbaa`** — three C3 merges landed (`591d954` G-B **+ the exemption, one merge**; `4a523ca` GDN-2; `3c1dbaa` Mamba-2, **hand-resolved**). All four C3 branches show **0 unmerged**. ⚠ **`clu-dev @ 0644c48` is BEHIND again — push owed** once the suite confirms. ⛔ `origin` frozen `40c2f31`. ✅ **FULL SUITE GREEN ON THE MERGED TREE: 1887 passed / 0 failed** (50:38, exit 0), **HEAD `3c1dbaa` re-verified before AND after**. **Arithmetic exact: 1781 + 106 = 1887**, the 106 being precisely the four merged C3 test files. ⇒ **the hand-resolved `mamba2` merge is verified**, and the push to `clu-dev @ 3c1dbaa` is confirmed sound after the fact.
⚠⚠ **BUT the arithmetic also exposes an unmerged branch: `agent/experiment-engineer/c3-rival-ladder-prereg` is NOT on `main`** (4 commits, **947 lines, purely additive — 3 new files, 0 modifications**): `scripts/c3_geometry_sweep.py`, `scripts/c3_ladder_plan.py`, and ⭐ **`tests/test_c3_geometry_freeze.py` (~32 tests) — which pins the byte-inert-atom-knob finding and the frozen geometry.** Those facts are load-bearing for G-B and are currently **unpinned on `main`**. ⛔ Its prereg deliverable is safe (it lives in gitignored `.claude/`), so nothing is lost — but the code and its guards are missing. **Merge owed; additive-only, so it cannot affect run 3.**
- ⛔⛔ **THE LADDER'S LAST GATE: `.claude/tasks/c3-ladder-amendment-2.md`** (engineer, wt1) — the Advisor reviews it **as one piece, carrying the G-B placement argument**, and **then arms train**. `BUDGET_IS_INTERIM` stays TRUE until that acceptance.
- ~~⛔ **RUN 3 IS NOT YET SUBMITTABLE — two blockers**~~ ✅ **BOTH CLEARED; `RUN3-LAUNCH.md` is at v2** (⛔ v1's `ttt_normalized_write` line was defective — see `[C3W1-MERGE]`). Original entry: **B1** the exemption branch is **unmerged** (a CSF3 pull would get no `--prereg-continuation`); **B2** run 2's script `job_gpu_cluformer.sh` has **no `PREREG_CONT` passthrough** and run 3 **cannot** use the ladder script instead (it narrows `arms`, a `PilotConfig` field ⇒ a second differing key ⇒ the exemption refuses it). **Package filed and otherwise complete: `.claude/outputs/c3-run3-launch/RUN3-LAUNCH.md`** — flags regenerated from run 2's journal rather than transcribed, `PREREG_CONT` on every submission **and** every re-resume, seed-0 sha256 pinned, `.eqx` check 15-way. **Merge the exemption ONCE, with the two-line fix in it.**
- ✅ **`clu-dev/main = 0644c48`** (pushed, `ls-remote`-verified). ⛔ `origin` frozen `40c2f31`. **Zero worktrees** (wt2 reclaimed after verifying its branch ref from the main repo).
- ✅ **`MATCHED_STATE_BYTE_BUDGET = 2_097_152` CONFIRMED correct, no edit** — inside the verified window `[1,597,440 , 3,145,728)`. ⛔ **But `BUDGET_IS_INTERIM=True` still blocks every ladder arm** until the geometry is chosen and the prereg's digit is flipped (one edit).
- ⛔⛔ **THE LADDER IS BLOCKED ON ONE ADVISOR DECISION: G-A / G-B / G-C.** The config-only shrink is **structurally impossible** — `atoms_per_item`/`capacity` are **byte-inert** (the w23 floor `512·√2⁸ = 8192` is what binds, and the pilot ties it exactly). **Hub + spoke both recommend G-B** (8192 atoms in **3 of 12** layers; breaks no design rule; byte/compute/envelope-identical to G-A). ⚠ Two riders: G-B's **3-of-12 placement is a design decision needing its own prereg line**, and under G-B the TTT rival carries our normalisation fix forever (consistent with the standing ruling — `η·n/d = 3.004 ≥ 2` fires it — whereas **G-A's 0.751 would let the rival run natively unmodified**).
- ⛔ **The ladder trains NONE of the six pinned rivals** — charter §2's *"tuned rivals"* clause is **undelivered**; this ladder carries the two-sided swap but not the competitive-baseline half, so **it cannot support the tier-iii primary claim alone.** Advisor: fund the rivals, or declare this phase 1.
- ⭐⭐ **CAMELS-US ADOPTED — the FIRST venue ever to survive criterion 4** (`margin_rel −0.6625` vs −0.02; no fire against any of 14 references; no fire at **536×** budget; licence `cc-by-4.0`). **N-CMAPSS DS02 ADOPTED as an APPLICATION venue, ⛔ never primary-claim**, with three permanent guardrails (the 7.988 reference row in every table · the CAFE embargo · its three CLU-native questions). Both loaders ⏳ **queued behind the ladder start**.
- **Venues (Advisor+Head ruled 2026-08-13):** **Track A ADOPTED** — enwik8 primary · WT-103 secondary · PG-19 internal retention instrument (⛔ never external comparison). **Track B: CAMELS-US primary candidate · N-CMAPSS fallback — ⛔ NEITHER ADOPTED**, both conditional on a measured criterion-4 tripwire. **State-byte budget RULED ≈2 MB**; ⚠ exact figure pending (Hub recommends the **2 MiB = 2,097,152 B** ceiling). **FineWeb-Edu = priced option, paper-stage.** ⛔ MQAR/zoology never primary.
- **Wave:** C3W1 — **SCOPED 2026-08-12** (charter §6). Task files on disk: `.claude/tasks/c3-benchmark-scout.md` (✅ done) · `.claude/tasks/c3-trackb-tripwire.md` (**NEW 2026-08-13, spawnable now — zero worktrees, CPU-only**) · `.claude/tasks/c3-csf3-harness.md` (✅ **RELEASED — precondition MET 2026-08-12, verified 2026-08-13; scout findings FOLDED IN 2026-08-13 (2 traps + the adopted slice convention + the no-default byte-budget knob); spawnable AS AMENDED — ⛔ spawn the amended file, not a remembered version**) · `.claude/outputs/c3-benchmark-scout.md` (✅ **DONE, accepted**) · `.claude/tasks/c2-closeout-debts.md` (relay to the C2W11 Hub, not a C3W1 spoke; ⛔ **A8 still open — the matrix fold is C2W11's last spoke**). Spawn block handed to the Head; ⛔ the Hub launches nothing.
- ✅ **PRECONDITION-MET** (was the blocking fact; ⟲ struck 2026-08-13). `git cat-file -e main:tests/test_ttt_stability_and_d5_wiring.py` **succeeds**, `git log main..agent/experiment-engineer/pilot-ttt-nan-and-d5-wiring` is **empty**, and all four pilot commits (`2469ba5`, `1ed0902`, `171972d`, `7fcef50`) are ancestors of `main` — merge `5656728`, zero conflicts, 5 files / +552 / −8. **A10 delivered:** `.claude/outputs/c2w11/DADDR-CEILING-PROBE.json` exists, regression anchor reproduced at `abs_delta = 0.0`. ⛔ **Both checks were re-run mechanically, not read off a report.**
- **Campaign sequencing (Advisor-ratified 2026-08-12):** relay to the C2W11 lead goes out **now** · the scout spawns **in parallel** (no precondition, ~1.5 d) · the harness spawns **the moment the signal prints**. Nothing idles; A1's latency *is* C3W1's latency.

## Standing discipline (unchanged from C2 — the short list)

Prereg + numeric falsifiers before cells · kill-conditions first · ≥ 3 seeds per paper number · tier control on every claim (system swap / in-block write swap) · launders = diagnostics, never pass conditions · byte ledgers everywhere · NOT-RUNs never nulls · mechanical file preconditions for gated spokes · declared file ownership, ≤ 3 engineer worktrees · HEAD-stable suites (compare HEAD before/after; a green against a stale base is not a green) · spawn lines go to the Head, one copy-able block, one line per spoke · "CLU-former" is a placeholder · wells never named semantically.

## Running log (newest first)

### 2026-08-13 — **[C3W1-MERGE]** C3W1 Hub — **three spokes accepted and MERGED (one hand-resolved conflict); ⛔ my own launch package had a defect and the spoke caught it; Amendment 2 scoped as the last gate before arms train.**

⛔⛔ **THE LAUNCH PACKAGE WAS WRONG AND `c3-gb-landing` CAUGHT IT.** v1's three `sbatch` lines carried `ttt_normalized_write=true` inside `MEM=`. `ttt_normalized_write` is a **`PilotConfig`** field, not `StreamMemoryConfig`, so measured against the **real banked journal** it was (a) **silently dropped** — the TTT fix would not have been on — and (b) still landed in `pilot.memory` as **a second differing key** ⇒ the exemption **refuses the leg**. `SET=` is refused too (`pilot.ttt_normalized_write`). ⭐ **Found by measuring, not reading.** ⭐ **HUB RULING (option A), filed as RUN3-LAUNCH v2: the flag does NOT travel on run 3** — a **scope clarification of PILOT-TTT-RULINGS ruling 1, not an override.** Run 3 is the **leak ablation**; a second token breaks its attribution **regardless of the exemption** (`PREREG-LeakAblation` §4). Ruling 1's flag belongs to the TTT arm's **own** leg — the C3 ladder (fresh G-B geometry, no exemption, `PREREG-C3-LADDER` §6.4 already sets it). ⭐ **Keeping the TTT arm bit-identical to run 2, NaN and all, is exactly what a one-token ablation requires** — run 3 does not measure the TTT arm. ⚠ **Advisor to confirm the scope reading.** ✅ **B1 + B2 both cleared**; `c3-gb-landing` re-verified `RUN3-LAUNCH` §1–§6 line by line and **executed §3's re-derivation against the banked seed-0 journal — it reproduces the MEM/STORE/SET block byte-for-byte.**

✅ **MERGED** (temp worktree; shared checkout never moved): **`c3-gb-landing`** — which **extends the exemption branch, so ONE merge landed both** (B1) · **`c3-rival-gdn2`** (clean) · **`c3-rival-mamba2`** (⚠ **the one branch that was NOT disjoint** — my task file asserted disjointness "by construction" and **the spoke verified it and found it false**, which is why this surfaced at merge instead of as a silent overwrite). **Two conflicts, both additive-adjacent, resolved keeping BOTH sides:** `train_cluformer.py` (G-B renamed the constant to `INTERIM_MATCHED_STATE_BYTE_BUDGET`; kept that name — it is the one used at the call site — plus mamba2's `c3_registry` imports, dropping the now-unused alias import) and `exp_cluformer_pilot.py` (`--prereg-continuation` and `--rival` are independent CLI args). **Ruff clean; both files parse; 106 targeted C3 tests pass.** ⚠ **A hand-resolved merge means the branch greens do NOT carry** — full suite running on the merged tree, result to be recorded here.

⭐ **G-B's best catch:** three store-side instruments **hard-coded layer 0** and under G-B would have **gone silently dark** (`applicable: false`) or raised — the gradient probe, the §7.27 store watch, the monitor pass, the selector readout. **Found by running it, not reading it**, and fixed. That is the failure mode that costs a ladder run.

**Rival findings carried into Amendment 2:** ⛔ GDN-2's `RIVAL_SPECS` row **omits short-convolution state** (+220,320 B) ⇒ **1.0973× OVER** the ceiling under our own total-as-deployed convention (0.9922× under the paper's) · ⛔ `shrink_to_budget`'s solved `n_heads = 6` **has no integer head geometry** (`512/6`), so the pre-registered `2,097,152` was never realizable; deployed is **2,080,800 B** · ⛔ `ArmLedger.dtype_bytes` hardcoded fp32, mis-stating every bf16 row · ⚠ Mamba-2's knob solved on the **24-layer reference** but deployed at **12** ⇒ **0.4949×**.

### 2026-08-13 — **[C3W1-AMEND2]** C3W1 Hub — **Advisor's three rulings scoped as ONE consolidated amendment: `.claude/tasks/c3-ladder-amendment-2.md` (engineer, wt1). ⛔ THE LAST GATE BEFORE ARMS TRAIN.**

**1 ✅ Convention holds, no exceptions** — total-as-deployed **includes auxiliary caches** (conv, normalizer, decay accumulators). GDN-2 **re-shrinks to fit total-including-conv**; its paper-convention figure survives only as a **labelled diagnostic column**. ⭐⭐ **The rider is the substance:** audit **every** rival spec by counting the official implementation's **allocated inference buffers** (the Mamba-2 `allocate_inference_cache` pattern), **pinned by tests** — *"the formula is the documentation, the allocation is the truth."* Applies to **all six rows**, not just the two with live arms; unreachable rows are **NOT OBTAINED + formula-derived, unaudited**, ⛔ never silently kept as if audited.
**2 ✅ The ceiling is a CEILING** (≤ 2,097,152 B, occupancy reported); **deployed bytes under integer-geometry constraints are the VALUES OF RECORD** — **2,080,800 B at 0.992× is COMPLIANT**. ⭐⭐ **New standing rule: pre-register CONSTRAINTS AND SELECTION RULES, never SOLVER OUTPUTS** — a pre-registered solver output is wrong the moment a constraint bites, and it turns a *result* into a *failed prediction*. Stated generally; it outlives this ladder.
**3 ✅ Calibration is per store-bearing layer**, against that layer's own activation statistics, **placement-independent** (⛔ no hard-coded layer index — it must read correctly at 3-of-12, 12-of-12 or any placement); enters as a **design line** (it moves an initialisation ⇒ claims-relevant); **one labelled smoke diagnostic** (own-layer vs layer-0), ⛔ never a claim or a pass condition. **The spoke's flag-don't-patch is noted approvingly and is the standard.**
**4 ✅ ONE PIECE, ONE REVIEW** — Amendment 2 carries the **geometry/placement lines (G-B's 3-of-12 argument, §2.5)** inside its own review, ⛔ not as a fragment assuming §2.5 was already accepted. **Then arms train.**
⭐ **HUB ADDITION, not in the relay — an anti-hobbling defect:** Mamba-2's shrink knob was solved on the **24-layer reference geometry** while the shell deploys **12**, leaving the arm at **0.4949×** of the ceiling with a knob it could have spent. ⛔ **That is hobbling, not thrift** — anti-hobbling has already inverted one C2W10 verdict, and a hobbled *learned* rival is worse than a hobbled classical one because it is the primary claim's control. ⇒ **every rival's knob is re-solved AT THE DEPLOYED GEOMETRY**, occupancies reported, and an arm materially under the ceiling is **a finding, not a saving**. It is a *selection rule*, so ruling 2's standing rule governs it.
⛔ **`BUDGET_IS_INTERIM` stays TRUE and the ladder guard stays live** — flipping it belongs with the Advisor **accepting** this amendment, not with writing it.

### 2026-08-13 — **[C3W1-PHASE2]** C3W1 Hub — **G-B ratified and scoped; ⛔ TWO launch blockers found that would have failed run 3 ON THE CLUSTER; `clu-dev` pushed; phase 2 funded.**

**4 ✅ `clu-dev` PUSHED** — `clu-dev/main = 0644c48` = local `main`, verified by `ls-remote`. ⛔ `origin` re-verified **frozen at `40c2f31`**. The Head can pull on CSF3. **wt2 reclaimed** (prereg spoke's branch ref verified from the main repo first, 0 uncommitted).

**5 ⛔⛔ THE RUN-3 LAUNCH PACKAGE IS FILED — AND IT IS BLOCKED, WITH THE BLOCKERS NAMED.** `.claude/outputs/c3-run3-launch/RUN3-LAUNCH.md`. Writing it surfaced **two defects that would only have appeared on the cluster**:
- **B1 — the exemption branch is UNMERGED.** `main @ 0644c48` is the harness merge only ⇒ a CSF3 pull gets code with **no `--prereg-continuation`**.
- ⛔⛔ **B2 — run 2's job script has NO `PREREG_CONT` passthrough.** Runs 1–2 went through **`job_gpu_cluformer.sh`**; the exemption spoke added the passthrough **only to `job_gpu_c3_seeds.sh`** (`git diff --name-only` confirms `cluformer.sh` untouched on `main` **and** on the branch). ⭐ **And run 3 cannot be re-routed through the ladder script:** it narrows `--arms` per task, `arms` is a `PilotConfig` field ⇒ **a second differing key** ⇒ **the exemption itself refuses it.** The exemption's own strictness proves the routing. **Fix = two lines** mirroring `job_gpu_c3_seeds.sh:84`/`:169`, owned by `c3-gb-landing`, **merged once with the exemption** rather than twice.
⭐ **Design note in the package:** run 2's flags are **regenerated from its journal by a printed command, never transcribed** — the exemption checks bit-identity, so one typo fails the leg. Verified locally against the banked seed-0 journal. `PREREG_CONT` is stated on **every** submission line **and** §6 restates it for **every re-resume**; seed 0 carries the verified `sha256=43e6598d…`; the `.eqx` precondition is a 15-way `OK`/`MISSING` check with `S4.json` backed up first.

**1 ✅ G-B SCOPED → `.claude/tasks/c3-gb-landing.md`** (engineer, **wt1**, ~1 day, **critical path — blocks run 3 AND every ladder job**): `store_layers` on `blocks.py` with the ratified **1,380,864 B / 0.658×** pinned by test · ⚠ **a test that the pilot flag table is provably unchanged** (a `store_layers` default that moves `as_flag_table()` would break run 3's exemption — the same failure mode the exemption spoke found for its own flag) · the §2 two-line passthrough + the launch package re-verified against the branch · the wrong `StateByteBudgetError` remedy text fixed (it names byte-inert knobs) · and **three binding prereg amendments**: the **3-of-12 placement argued as a design decision** (⛔ never a byte-fitting default), **PHASE 1 DECLARED with "this is not the tier-iii claim" stated explicitly**, and ⭐⭐ **the store-liveness diagnostic PRE-REGISTERED on phase 1's first rungs** with numeric falsifiers and a kill condition — the C2 flat-curve disjunction (*carries nothing* OR *cannot be addressed*) resolved **early, at real scale**, reusing C2W11's oracle-addressed-read idea, ⛔ not the toy code and ⛔ not deferred to the end.

**2 ✅ PHASE 2 FUNDED → `.claude/tasks/c3-rival-arms.md`** — ⭐ **one file, spawned THREE times**, each executing the shared contract **plus exactly one** of §A Mamba-2 / §B GDN-2 / §C sliding-window, on its own branch and worktree (disjoint by construction; ownership declared). Contract: **official code as the basis** (the C2W10 SAM-kNN port is the standard — authors' implementation, published defaults, unit-tested equivalence shims) · ⛔ **the FLA 3× trap binding**, per-number `PAPER:`/`OFFICIAL IMPLEMENTATION:` provenance, no library default ever inherited · **entry to the ladder ONLY through the byte ledger**, reproducing `RIVAL_SPECS` to the byte · **shrink-to-match via `shrink_to_budget()`**, never grow, never re-solved by hand · **total bytes as deployed, no dtype normalisation** · ⛔ zero ladder arms trained. ⭐ **Anti-hobbling is the governing discipline** — a rival beating the CLU is *the control working* and is reported unsoftened; one C2W10 verdict was already inverted by a hobbled rival. Per-venue caveats carried: Mamba-2's provenance is **implementation, not paper** (appendix NOT OBTAINED) · GDN-2 is **arXiv:2605.22791**, not v1, and its *shrunk* value hits 2,097,152 by powers-of-two accident (noted so nobody reads it as fitted) · Longformer-small **1.00 bpc is a sanity anchor, ⛔ never a matched baseline** (staged training, eval at 32,256 tokens).

**⚠ Worktree budget:** ≤ 3 engineer worktrees. `c3-gb-landing` takes **wt1**; **two** rival spawns take wt2/wt3; **the third rival waits for wt1** to free when G-B lands (they are file-disjoint, so order does not matter).

### 2026-08-13 — **[C3W1-GEOMETRY]** C3W1 Hub — **both spokes ACCEPTED. Run 3 is UNBLOCKED and submittable. ⛔ The ladder is blocked on ONE Advisor decision: the C3 store geometry (G-A/G-B/G-C) — the shrink everyone assumed is STRUCTURALLY IMPOSSIBLE.**

✅ **Exemption ACCEPTED — run 3 submits now.** ⭐ **One deviation, and it is a catch, not a slip:** the declaration is a **run argument, not a `PilotConfig` field**, because as a field it enters `as_flag_table()` and becomes **a second differing key — breaking the very identity check it is verified by**, and violating `PREREG-LeakAblation` §4 (*"run 3 changes exactly one token"*). The repo already had the pattern (`--d5`/`--slices` are CLI args precisely so they cannot move the resume fingerprint) and the spoke followed it, pinned by its own test. **Nothing is weakened:** still one declaration, verified through the **existing** fingerprint machinery, recorded in `byte_ledger.preregistered_continuation` where an auditor reads it. **14 break-attempt classes all REFUSED** — second key · different key · ⭐ **geometry "shrunk to fit"** · missing/corrupt journal · invalid journal fingerprint (3 shapes) · sha256 mismatch · a final `S3.json` · list/glob/wildcard/derived/non-field (8 param cases) · extra spec key · malformed spec · ⭐ **a forged dict posing as an exemption** · and **no-exemption-over-budget still refused** (original behaviour intact). ⚠ **Run 3's launch line must carry `PREREG_CONT=…` on EVERY submission AND every re-resume** — passthrough is in `job_gpu_c3_seeds.sh`; **the Head/Hub writes the submission line.**

✅ **Prereg ACCEPTED, and it did the right thing by NOT delivering a geometry** — task §2 named "report the conflict and hand back" as a legitimate outcome and it took it. **1,813 passed / 0 failed** (= 1,781 + 32, its own file) at `07b126b` / `main @ 0644c48`; **3 new files, zero modifications to existing tracked code**; ⛔ **zero ladder arms trained.**

### ⛔⛔ F1 — THE STORE CANNOT BE SHRUNK BY CONFIG. The conflict is STRUCTURAL.

⭐ **`atoms_per_item` and `capacity` are BYTE-INERT at `addr_dim = 8`.** `n_atoms` is a `max` including the **w23 dimension-aware floor** `512·√2⁸ =` **8192**, which the pilot's `32 × 256` **ties exactly** ⇒ **the pilot's `atoms_per_item = 256` is decorative** (pinned by 10 tests; `A → 64` and `A → 16` both leave 5,523,456 B unmoved). With `addr_dim` closed (Hub R2 / N312), `n_layers = 12` fixing the 28.56 M param class, and `dim` needing `payload_dim ≤ −5` to fit 8192 atoms under 2 MiB, **the only lever left is the floor's own height.** ⇒ **not a config oversight.**

| | atoms/store-layer | store layers | CLU total | occ. | breaks | TTT `η·n/d` | needs |
|---|---|---|---|---|---|---|---|
| **G-A** | 2048 | 12 | 1,394,688 | 0.665× ✅ | ⛔ **the w23 floor ×4** | **0.751** ✅ | a config value |
| ⭐ **G-B** | 8192 | **3 of 12** | 1,380,864 | 0.658× ✅ | **nothing** | 3.004 ⛔ | a `blocks.py` task + `ttt_normalized_write=True` |
| **G-C** | 4096 | 12 | 2,770,944 | 1.321× ⚠ | floor ×2 **and** the digit | 1.502 ⚠ | config + a re-ruled digit |

⭐ **G-A and G-B are byte-, compute- and envelope-IDENTICAL** (4.18 s/step, 29.7 h worst job, 3.23× headroom) — the store's bytes *and* compute are both **per store-bearing layer**. **G-A starves each cell; G-B uses fewer cells.** ✅ **2 MiB CONFIRMED inside the window `[1,597,440 , 3,145,728)`** under either — no edit needed, closing `c3-csf3-harness` reconciliation item 4.

**▶ HUB RECOMMENDATION: G-B**, with the spoke. *When a question cannot be settled, prefer the option that does not require settling it* — G-A's descent breaks a **pinned** design rule and the write-efficacy curve **cannot** adjudicate it (the pre-registered falsifier **fires on one seed and reverses on another**; median 5.4×, monotone). Two refinements the Hub adds:
- ⭐ **The TTT interaction is NOT a new flip.** `PILOT-TTT-RULINGS` ruling 1 **already** sets `ttt_normalized_write=True` on the TTT arm's leg, justified by the **`η·n/d ≥ 2`** criterion. G-B's 3.004 **fires that criterion** ⇒ consistent with the standing ruling, not a fresh claims decision. ⚠ **But the honest difference:** under **G-A** (0.751) the criterion does **not** fire, so the TTT rival would run in its **native published form with no modification from us**; under G-B every TTT number carries *"with our normalisation fix"* forever. The fix is anti-hobbling (rival **2.12** vs best-before-NaN **4.75**) and byte-neutral, so it is defensible — but it is a sentence a referee will probe, and G-A never has to say it. **That, not the byte count, is the real trade.**
- ⚠ **G-B's layer placement is a DESIGN DECISION, not a packing artifact.** "The CLU as the memory of a streaming block" becomes "in **3 of 12** blocks". Defensible and precedented (hybrid/periodic placement), but it must be **stated and pre-registered as a design choice in its own right**, ⛔ never discovered by a reader as a byte-fitting side-effect.

**⛔ ESCALATION 2 — the ladder does NOT train the six pinned rivals; none is implemented.** ⇒ **Charter §2's "tuned rivals" clause (real Mamba-2 · GDN-2 · sliding attention) is UNDELIVERED by this ladder**, which therefore carries the **two-sided swap** but **not the competitive-baseline half** — and the genuine-win bar requires the primary claim to beat competitive baselines. **This ladder alone cannot support the tier-iii primary claim.** Advisor's call: fund the rival implementations, or scope this ladder explicitly as phase 1 with the rivals as phase 2.

**Smaller, owners needed:** ⛔ **`byte_ledger.py`'s `StateByteBudgetError` remedy text is WRONG** — it tells the operator to shrink `capacity`/`atoms_per_item`, which move **zero bytes** (F1.1); the prereg spoke correctly did **not** edit another spoke's file and left replacement text · **G-B needs a `store_layers` selection on `StreamModel`** (`chlu/core/blocks.py`) — small, unowned · 🔍 pre-existing: `rival_reference_table()` raises below ≈197 kB (mamba2 cannot shrink that far) — harmless at every real budget.

⚠ **Two findings worth carrying:** **G4** — smoke bpc is **bit-identical to 6 d.p.** across a 16× atom range *and* all four iso-byte `capacity` splits while cell state moves 30,208 → 460,288 B ⇒ **the store's read is inert in the loss at smoke scale.** Expected (smoke cannot rank geometries) and the spoke revived the instrument in its declared second arm with the deployed write levers — but this is the C2 **flat-curve disjunction** again (*carries nothing OR cannot be addressed*), and the ladder must resolve it early rather than at the end. **Machine lesson:** two suite runs **died silently under contention** with the concurrent spoke's suite — no traceback, no summary; ⛔ do not read a vanished suite as a pass.

### 2026-08-13 — **[C3W1-RULINGS]** C3W1 Hub — **six Head+Advisor rulings applied; run 3 unblocked BY EXEMPTION; three task files written; the ceiling's admissible window derived and handed to the prereg.**

**1 ✅ Run 3 unblocked by EXEMPTION, ⛔ not by re-ruling the budget.** Run 3 is a pre-registered one-flag ablation whose geometry must not change; the budget was never meant to govern it. → **`.claude/tasks/c3-run3-budget-exemption.md`** (engineer, wt1, ~half-day, **critical path**): `preregistered_continuation=<run-2 journal>` verifying **via the existing `load_journal` fingerprint machinery** (⛔ reuse, never a second comparison — one source of truth about "same leg") that the config is bit-identical to run 2 **except the one registered flag**; **exactly ONE flag, no list/wildcard**; **annotates, never suppresses** — the ledger is still computed and printed with the journal sha256 + old→new value stamped; **exempts the BUDGET check only** (`UnledgeredArmError`, φ-accounting and shared-shell identity stay live and unreachable through it). ⭐ **The deliverable is the seven break-attempt tests** — the narrowness is the feature. Base = the harness branch `f98f939`, so run 3 is not gated on a merge.
**2 ✅ R1/R2/R3 endorsed as made** + new: ⛔ **NO dtype normalisation** — the convention is **total state bytes AS DEPLOYED**; the fp32 store pays its real 2× cost against a bf16 rival, harder-for-us being the defensible direction. Recorded in code (ledger docstring + `BUDGET_PROVENANCE`) so nobody later "fixes" it as a bug.
**3 ✅ The ceiling digit is set at the ladder's prereg, not now.** `MATCHED_STATE_BYTE_BUDGET = 2_097_152` is **INTERIM and binds nothing**; a guard now refuses **any ladder arm** while it is interim (run 3 is not a ladder arm). ⭐ **Hub-derived and handed over: the admissible window is arithmetic, not taste.** For "both swap members fit under, others shrink-to-match": **ceiling ≥ max(CLU_total, TTT_matched_total)** and **ceiling < 3,145,728** (GDN-2, the smallest non-swap rival — above it, rivals *grow*, reversing the ruled direction). **2 MiB sits inside that window only if the frozen C3 CLU total is ≤ ~2 MiB, i.e. a ≈2.63× shrink from pilot.** TTT-Linear (1,597,440) fits under naturally at 0.76× as a *swap member*, not an "other". ⛔ If the frozen geometry cannot fit the window, the prereg **reports the conflict and hands back** — it does not widen the ceiling.
**4 ✅ CAMELS-US ADOPTED** (Track B primary) → production loader funded in **`.claude/tasks/c3-venue-loaders.md`** §1, ⏳ **QUEUED behind the ladder start**. `daymet` **only** (⛔ shipped `maurer` defective: `tmax == tmin` >99 % of days in 20/20 basins, 3 malformed headers) with a **`tmax > tmin` stage-time assertion** so a future product swap cannot reintroduce it · the **SAC-SMA calibration-period caveat** carried in-docstring (0.708 vs Kratzert's out-of-sample 0.603) · **`cc-by-4.0`** recorded · **median NSE 0.758** as the venue currency. ⛔ The tripwire's throwaway loader is **not promoted**.
**5 ✅ N-CMAPSS DS02 ADOPTED as an APPLICATION venue, ⛔ NEVER primary-claim** → same file §2, queued after the Track-A ladder. **Three guardrails travel with it forever**, and two are required **in code, not prose**: **(a)** the matched-bytes reference row (**7.988 RMSE**, tying FNN **7.89**) is printed in **every** N-CMAPSS table — the emitter is structurally incapable of omitting it, same posture as the unledgered-arm error; claims take capability/application form only, ⛔ never *"beats SOTA on RUL"* · **(b)** its CLU-native questions = persistent-store-across-units (C2W10's re-homed VALUE question — a fleet is its natural home) · the anytime curve · interpretable-store probes · **(c)** the **CAFE embargo, absolute**, enforced by a staging check that refuses CAFE-derived artifacts; HEPA comparisons internal-only unless re-derived on corrected labels.
**6 ✅ Sequencing set** → **`.claude/tasks/c3-rival-ladder-prereg.md`** is the wave's next scoping artifact (engineer, wt1): freeze the C3 store geometry **on a measured smoke-scale sweep** (⛔ pilot geometry is not presumed to be C3 geometry) · set the ceiling inside the verified window · pin every arm's config + shrink knob + expected occupancy · pre-register the **CLU/TTT match ratio** (pilot's **1.007×** must survive the shrink) · a **15+-job plan** at a **measured** MFU · numeric predictions with falsifiers, **kill conditions first**. ⛔ **It goes to the Advisor before any ladder arm trains, and trains none itself.** ⚠ **Concurrency note carried in both files:** the prereg and exemption tasks both touch `chlu/eval/byte_ledger.py` — **the exemption task owns it, the prereg consumes it.**

**7 ✅ GUARDRAIL (a) RE-HOMED AS A CLAIMS OBLIGATION** (Head ruling 2026-08-13, relayed to C2W11) — it outlives every task file, so it goes where paper-writers actually look. **The A8 spoke is EXTENDED** into one curator pass with **two clearly-separated campaign sections**: the owed **C2W11 §0.x fold under C2 accounting**, plus a new **C3 venue section** carrying — the **N-CMAPSS guardrail-(a) never-quote** (any N-CMAPSS number without the **7.988** reference row and the **FNN-tie** fact beside it is never quotable; capability/application form only) · the **CAMELS caveats** (SAC-SMA output is calibration-period, ⛔ never vs the LSTM · `maurer` defective, `daymet` only · the currency is the **median 0.758**, not the mean) · the **state-byte convention never-quotes** (⛔ no state-byte figure without its convention **and** dtype; the scout's single-store **1,966,080 B** row is **never** "the CLU's state") · the **dyn-eval 0.94-at-277 M category error** · the **run-3 exemption labelling** (⛔ run-3 results are never budget-compliant ladder rows). ⇒ the matrix returns **paper-safe for both campaigns at once**. **The loader-side assertion stays belt-and-braces, ⛔ never the only enforcement.** Charter ref: **C3 Addendum 1 §1/§3**.

⛔ **Standing, kept visible — the wave's most urgent zero-worktree item:** **A8 (as extended above) is C2W11's last spoke, and `claims_matrix.md` is UNSAFE FOR ANY PAPER-WRITER until it lands** — still the banked **0.2719** with no C2W11 caveat, **zero** occurrences of *"unaskable"*, and now the whole C3 venue set unfolded too. ✅ The Advisor has filed **C3 Addendum 1**; ⛔ the Hub does not touch the charter.

### 2026-08-13 — **[C3W1-REVIEW]** C3W1 Hub — **both spokes DONE and accepted. ⭐⭐ CAMELS SURVIVES criterion 4 — the FIRST venue ever to. ⛔ The ruled 2 MiB budget rests on a counting error and BLOCKS run 3. Three Hub rulings made; two escalations up.**

⭐⭐ **THE WAVE'S SENTENCE: the criterion-4 theorem has its first measured SURVIVOR.** CAMELS-US: exemplar store at budget **E = 0.2559** median NSE vs LSTM-ensemble **S = 0.7580**, `margin_rel = −0.6625` against a **−0.02** threshold — **25× clear**, and it does not fire against **any of 14** references including the weakest published model (VIC-CONUS 0.307, margin −0.0511). ⭐ **Even at 536× the byte budget the local store reaches only 0.2173** — the classical store cannot approach the learned model at *any* size, which is the asymmetry the six prior confirmations never had. Mechanism as pre-registered: the target is a function of **unobserved accumulated storage**, not of the observable forcing window. **`criterion4_cleared = TRUE`; ADOPT-RECOMMENDED, the Advisor's call.** N-CMAPSS: crit-4 cleared and **crit-2 now measurable = PASS**, ⚠ but the byte-matched store **ties the weakest published deep model** (FNN 7.89 vs 7.988, ratio 1.0124 — trips the 2 % rule against that one reference) and **crit-5 stays PARTIAL** ⇒ **fallback only, as ranked**.

✅ **Tripwire spoke accepted** — prereg filed before the harness ran; both C2W10 lessons honoured (raw-and-standardised max consumed; full L ladder, and **over-budget was WORSE than in-budget**, reproducing the "storing more is harmful" law on a second venue). ⭐ **Self-disclosed a defect in its own harness** (omitted ‖q‖² from the squared distance), corrected and quantified it (+0.0028, 0.2531 → 0.2559), and showed no verdict moved. **CAMELS licence RESOLVED: `cc-by-4.0`** (Zenodo 15529996) — the Head-facing blocker is closed. **Scout corrections banked:** `S` is the **median 0.758**, not the mean 0.72 (the median is `dies_if`'s currency, and it is the *highest* number in Kratzert Table 3) · CAMELS is **14.57 GB measured**, N-CMAPSS **15.76 GB** (3× the relayed figure) · ⛔ the shipped **SAC-SMA output is NOT out-of-sample** (0.708 vs Kratzert's split-sample 0.603 — calibration-period fit; never quote it against the LSTM) · ⛔ **the shipped `maurer` forcing product is DEFECTIVE** (tmax == tmin on >99 % of days in 20/20 basins; 3 malformed headers) — **use `daymet`**; owner = any future CAMELS loader, not the current harness.

✅ **Harness spoke accepted** — 8 commits on `agent/experiment-engineer/c3-csf3-harness`, **1781 passed / 0 failed**, HEAD `f98f939` re-verified **before and after** the run, and the baseline **independently re-derived by collect-only at `main @ 60d8be4` = 1744** (corroborating the C2-close number). Byte ledger built **before training**, φ ledgered explicitly on every arm (`phi_accounted`), zero-state arms assert 0 rather than infer it from absence, and both `UnledgeredArmError` and `StateByteBudgetError` **fail loudly**. ⭐ **The content-relabel slice control FAILED on first implementation and the engineer reports it as real, not cosmetic** — the instrument caught its own designer.

### ⛔⛔ THE BUDGET FINDING — the enforcement worked, and it stopped run 3 before an A100-hour was spent

At pilot geometry the CLU arm is **5,523,456 B = 2.63× the ruled 2 MiB**, and the harness now **refuses to train it**. ⭐ **The two-sided swap is internally byte-honest** — CLU 5,523,456 vs TTT-matched 5,483,712 = a **1.007× match**, the property the ruling actually wanted. It is the **budget**, not the **match**, that is violated. **Hub-verified arithmetic, independently:** the ≈2 MB figure paired the scout's **SINGLE-STORE** CLU row (1,966,080 B = 491,520 floats) against **layer-summed** rival totals, and mixed **float32 (ours)** with **bf16 (the rival table)** — a 2× factor on top of a 12× one. **The ruling compared incommensurable objects; it is not the engineer's error and not a defect in the config.** ⛔ The engineer correctly **refused to choose the convention**, because choosing it is choosing the budget.

**▶ HUB RULINGS (3, made now).** **(R1) Enforce on TOTAL state bytes** — it is what actually occupies memory and it is already how the scout's rival rows are summed; per-layer (which would put the CLU at a comfortable 0.22×) is the wrong object and would flatter us. **(R2) `d_addr` STOP ENDORSED — expose, do not flip.** `addr_dim` stays 8 by default and remains a plain flag; flipping the *default* to 12 would make `as_flag_table()` emit it, change the resume fingerprint and **refuse all five banked legs** — precisely the defect the pilot merge repaired — and would silently re-roll the TTT arm's `η·n/d` stability criterion and every state-byte number. Any `d_addr` change is a claims-relevant control change requiring its own prereg. **(R3) `dtype_bytes` stays a declared per-row field, never inferred** (already built); whether to *normalise* dtype across arms rides with the budget escalation.

### 2026-08-13 — **[C3W1-VENUES]** C3W1 Hub — **six Advisor/Head decisions applied; Track A adopted; the ≈2 MB budget RULED; Track-B tripwire spoke scoped (zero worktrees); FineWeb-Edu held as a priced option.**

**1 ✅ Track A adopted as scouted** — enwik8 primary (the only venue with in-class anchors: Longformer-small 41 M **1.00 bpc** · Adaptive-Span 39 M **1.02** · Mega 39 M **1.02** · TXL-12L 41 M **1.06**) · WT-103 secondary · PG-19 as **internal retention instrument only**, ⛔ never an external comparison.
**2 ✅ The state-byte budget is RULED at ≈2 MB** and folded into `c3-csf3-harness.md` §5 as **one named config constant** with enforcement (ledger every arm, mismatch fails loudly) and **occupancy reporting**, not bare compliance. ⚠ **One digit still open, non-blocking:** *"≈2 MB"* is not pre-registerable. Hub recommends **2 MiB = 2,097,152 B as the CEILING** (CLU d=12 **1,966,080 B** = 0.94× · TTT-Linear **1,597,440 B** = 0.76×) over setting the budget equal to our own store's 1,966,080 B — **a ceiling no arm sits exactly on cannot be accused of being tuned to any arm**, and both members of the two-sided swap fit under it naturally. The engineer builds against the constant and defaults to 2,097,152 B if the confirmation has not landed.
**3+4 ✅ Track-B tripwires funded → `.claude/tasks/c3-trackb-tripwire.md`** (results-analyst, **zero worktrees, zero tracked-code edits, CPU-only, hours**). One spoke, both venues — the C2W10 instrument port is the shared cost and porting it twice is waste. ⛔ **It writes no production loader**: productionising a venue we may reject is the waste the kill-condition doctrine forbids; adoption turns the loader into a separate engineer task. Ownership is `.claude/**` only, disjoint from wt1's `chlu/data/**`. Carries: the **prereg-before-harness** rule · ⭐ **both C2W10 lessons that invert verdicts** (anti-hobbling raw-AND-standardised consume-the-max — raw-only would have flipped C2W10's gate to `true`; and the full L-ladder — the CLU's own byte budget was INSECTS' *worst* arm at 59.75 % vs 76.03 % at L=500, storing more was harmful) · CAMELS' **licence-string check as a first-act blocker** · **N-CMAPSS's missing crit-2 rows** (mean-RUL + affine-in-cycle-index, expected strong by construction — that *is* the measurement) · ⛔ the **CAFE embargo, absolute, both directions** · ⛔ **classic C-MAPSS is not a claim venue**.
**5 ✅ FineWeb-Edu held as a PRICED OPTION** (paper-stage decision, not now) — ⛔ no loader built. Hub's earlier "fund it now" recommendation is **withdrawn**; the deferral is made genuinely cheap instead by requiring the registry to be **corpus-generic** (a third stream = one module + one config value, no trainer/iterator/slice/ledger change) and by making the engineer state what such an addition would touch.
**6 ✅ ALREADY DISCHARGED before the relay arrived** — the retention-slice byte-level caveat was folded as **TRAP 1** in `c3-csf3-harness.md` on 2026-08-13, pre-spawn; re-verified this session that the spoke has **no branch, no worktree, no output**, so the instrument is still unwritten and the warning lands ahead of it, not after.

⛔ **Standing:** if a CAMELS tripwire fires it is the **SEVENTH** criterion-4 confirmation and returns to **Head + Advisor, not to a spoke**; the task file says so and stops at the measurement. ⛔ MQAR/zoology synthetics are never a primary (criterion 4's home turf) — diagnostic appendix at most. **The Advisor files the venue addendum in the C3 charter and marks the scorecard adopted; ⛔ the Hub does not touch the charter.**

### 2026-08-13 — **[C3W1-REL]** C3W1 Hub — **scout ACCEPTED; its findings FOLDED into the harness task BEFORE spawn; harness RELEASED. Four decisions up to the Head.**

**Scout accepted** (`.claude/outputs/c3-benchmark-scout.md`, 66 KB · `trackB-scorecard.json`). Compliance re-checked mechanically, not read off the report: **7 rows, ZERO `crit4_tripwire` verdict-field leaks**, two `RECOMMEND-IF-TRIPWIRE-CLEARS` (never a bare RECOMMEND), five REJECTs each with a named cause of death. Hard rules 1–4 honoured; NOT-OBTAINED and NOT-PUBLISHED used where they belong rather than guessed around.

⭐ **The finding: the 26–47 M grid is EMPTY of modern rivals.** Mamba-2 · GDN · GDN-2 · TTT · Titans publish **nothing** on enwik8/WT-103/PG-19 **at any size** — they live on The Pile/Books3/FineWeb-Edu at ≥125 M. The only in-class anchors are pre-2023 attention (Longformer-small 41 M **1.00 bpc** · Adaptive-Span 39 M **1.02** · Mega 39 M **1.02** · TXL-12L 41 M **1.06**). ⇒ **we train all five arms ourselves**, ≈1.5 h (35 % MFU) – 18 h (3 % MFU) per 40 M enwik8 arm on 2×A100. Affordable; the ladder's unit is **≈5 arms × ≥3 seeds = 15+ jobs**. ⛔ **Never-quote: the dyn-eval anchor 0.94 bpc is at 277 M** — beside a 40 M number it is a category error; dyn-eval becomes **an arm**, not a citation. The dividend bar it sets: **−0.05 bpc / 5.1 % rel** (enwik8), **−9 %** (WT-103), Krause et al. 2019.

**⛔ THE WAVE'S BIGGEST RISK — the matched-state-byte budget is a DECISION, not a derivation.** At ≈38 M params the natural state spans **1.60 MB (TTT-Linear) → 100.7 MB (sliding-window @4 k) = 63×**. Pick it wrong and the tier-iii control decides the result before any physics runs. Scout's recommendation (**≈2 MB**: CLU d=12 = 1.966 MB sits at 0.81× TTT-Linear, making the two-sided swap byte-honest and forcing Mamba-2 3.29× / SWA 6.40× / TXL@3800 23.7× to **shrink to match** — the harder, more defensible direction) is **up to the Head/Advisor, NOT the engineer**.

**Folded into `c3-csf3-harness.md` before release** (the spoke had not spawned — the finding was caught in time, not left to go stale): ⚠ **TRAP 1, the byte-level revisit unit** — at vocab 256 distance-to-last-same-symbol degenerates into a frequency count; the unit is the **enclosing whitespace-delimited token**, with a non-degeneracy assertion that fails rather than ships · ⚠ **TRAP 2, rival state config** — `flash-linear-attention` defaults give **3×** the GDN-2 paper's state; pin every rival's config with per-number provenance, ⛔ never inherit a library default and claim byte-matching · ⭐ **ADOPT Sun et al. EMNLP 2021 (arXiv:2109.09115)** for the retention slice (distance-to-last-occurrence + "never in prefix", defined **on PG-19**) and cite **Khandelwal et al. ACL 2018** as the shuffled-control precedent — declare only bin edges / byte-level / all-arms / the control as ours · **the byte budget is a no-default config knob that fails loudly if unset** · reference configs pinned (attn 12 L/d512 = 37.88 M · recurrent 24 L/d512 = 37.75 M) · PG-19's three seam caveats banked (28,752-file inode hazard → one memmap-able uint8 stream + sha256; word-count normaliser from raw text; 50-book val ⇒ per-book spread).

**Track B:** primary **CAMELS-US rainfall–runoff**, fallback **N-CMAPSS DS02** — both `RECOMMEND-IF-TRIPWIRE-CLEARS`, ⛔ **neither adopted**: charter §3 makes the criterion-4 tripwire a **measured** precondition of adoption. Five REJECTs: LTSF (crit 2 — closed-form OLS at the frontier) · TSAD (crit 2 — a random score is SOTA under point-adjust) · traffic (crit 3) · PDE rollout (crit 3) · streaming-drift (crit 4, already fired). ⭐ **Zero survivors was an admissible outcome and no survivor was manufactured.**

⚠ **Consumed A10 as a FINDING, not just a config input:** placement headroom **0.0851 → 0.0375 → 0.0109 → 0.0000** across `d_addr` 4/6/8/12 (N312) ⇒ **raising `d_addr` does not open the tier-ii question.** The harness changes no default and invents no value.

### 2026-08-13 — **[C2-CLOSE-VERIFIED]** C2W11 close-out Hub — **✅ THE C2 CLOSE-OUT IS VERIFIED, NOT ASSERTED: 11 of 13 debts DISCHARGED, the C3W1 precondition is MET, and TWO residuals are named with owners.**

**Method:** every A1–A13 done-check re-run from artifacts and git; ⛔ **no item accepted on the previous Hub's table.**

✅ **A1** both done-signals pass (merge `5656728`; four pilot commits ancestors of `main`) · ✅ **A5 re-derived independently** — collect-only on a clean `main` worktree = **1737**, and the 7-test gap is exactly `test_industrial_loaders.py` (module-level `importorskip("pandas")`, absent from a fresh venv) ⇒ **1737 + 7 = 1744**, and the arithmetic 1726 + 18 checks against the new file's **18** tests · ✅ **A6** struck-and-annotated at **both** sites, dated, no measurement altered · ✅ **A7** footnote filed · ✅ **A9** owner closed (the `§A28.3`→`§A26.3` citation slip stays the **Advisor's**, 7th recurrence) · ✅ **A10** artifact verified, anchor `abs_delta = 0.0`, ⭐ and it is a **finding**: placement headroom **0.0851 → 0.0375 → 0.0109 → 0.0000** across `d_addr` 4/6/8/12 — **raising `d_addr` does not open the tier-ii question** (N312) · ✅ **A11** in-code docstring on `main`, verified **docstring-only** (+25 lines, zero executable change vs the 1744-green commit) · ✅ **A12** `clu-dev/main = 60d8be4` by `ls-remote`, ⛔ `origin` **frozen at `40c2f31`** · ✅ **A13** close entry filed, quoting the re-derived count.

⭐ **A2/A3/A4 RE-HOMED AND THEREBY DISCHARGED.** All three rulings existed **only** in `handover_context.md`, archived the same day it was written — so the run-3 config author would never have read them. They are now live at **`.claude/outputs/c2w11/PILOT-TTT-RULINGS.md`**, restated verbatim in substance with their measured basis (`η·n/d = 3.47` pilot vs 2.31 toy; rival 2.12 vs best-before-NaN 4.75; the five-`.eqx` + `S4.json` spec; the astride-the-boundary 2.08–2.31 evidence) and the §A20.4 guard's live caveat (**`as_flag_table()` registered and NOT done**). ⛔ Nothing re-scored in the move.

⛔ **TWO RESIDUALS, both named:** **(1) A8 is PARTIAL** — `negative_results.md` (N302–N312), `future_work.md` (FW-C2W11-1…12), the ledger and primer 11.30 are all folded, but **`claims_matrix.md` was never touched** (still **v2.15**, last row **CM-39**, **zero** occurrences of *"unaskable"*) ⇒ **a paper-writer loading only the matrix today misses the entire C2W11 never-quote list**, including *"the organizer swap failed"* and any *"K5 verdict"*. **Head-ruled 2026-08-13: the fold stays C2W11's**, spawned as its last spoke. **(2)** the `§A28.3` citation slip is the **Advisor's** and is not this close-out's to fix.

### 2026-08-12 — **[C3W1]** Hub — **wave SCOPED; two spokes written, one HELD on an unmet precondition; the C2 debts relayed, not adopted.** ⟲ **[the HELD status is SUPERSEDED — the precondition was met 2026-08-12 23:43 and verified 2026-08-13; item (4)'s *"A2, still owed"* is likewise discharged. Entry kept verbatim as history.]**

**Scoped (charter §6).** (1) **`c3-benchmark-scout`** (web-scout, zero worktrees, **runs first, no precondition**): Track A = rival numbers/conventions at 26–47 M on enwik8/WT-103/PG-19 with **table-level primary provenance, a comparability verdict per cell, and derived inference state-bytes** (the matched-state-byte control needs them); the **dyn-eval substitute-column convention** and any existing retention/revisit convention pinned; **PG-19 GO/CAVEAT/NO-GO with arithmetic** under 2×A100/4-day. Track B = per-venue admissibility scorecard (`trackB-scorecard.json`) against intervention §6, with ⭐ **criterion 4 SPEC-ONLY — the schema has no crit-4 verdict field by design**; the two known causes of death (crit-2 linear/naive baselines at the LTSF frontier; crit-4 for classic streaming) checked first; CAFE embargo in-line. "No admissible candidate" is a declared **finding**, not a failure.
(2) **`c3-csf3-harness`** (engineer, wt1) — loader registry (enwik8 | WT-103 by config; **PG-19 left as a seam, not built** — its feasibility is the scout's §1.3) · ⭐ **`chlu/eval/text_slices.py`, the real-text retention/revisit instrument — VERIFIED not to exist** (`grep -rln revisit chlu/ tests/` hits only `memory_gym.py`, `exp_bprime_rivals.py` and its test, all toy-side), with a **shuffled-position control** and the slice computed for every arm incl. dyn-eval · seeds-as-jobs + resume-first ladders, **the field-added/default-accepted/non-default-refused regression test** guarding the deliberately loosened §A20.4 check · the local smoke config (⛔ never a claim venue, said so in the file) · **byte ledgers incl. φ emitted structurally — an unledgered arm fails loudly**, because matched-state-bytes is what tier iii's control rests on. Ownership declared and disjoint; ⛔ `chlu/core/blocks.py` and the D5 passthrough are off-limits.
(3) **C2 debts NOT adopted.** Charter §6.3 and Add.16 §A45-as-amended both assign **ALL** C2 closing tasks to the **C2W11 Hub**. Filed `.claude/tasks/c2-closeout-debts.md` as a **relay checklist** (A1–A13 with mechanical done-checks; the **A1 → A5 → A13** chain; **A10 independent and cheap**) plus §B: C3W1 consumes **exactly two** — A1 (mechanical, blocking) and A10 (non-blocking config input, never invented). A numbered override decision went up with the spawn block.
(4) **Run-3 sequencing unchanged** and not the Hub's to move: Advisor adjudicates runs 1-vs-2 (ψ ablation, artifacts owed) → run 3 = leak-closure per `PREREG-LeakAblation.md` (`.claude/outputs/c2w6-anti-erosion/`) + the TTT-arm default ruling (**A2, still owed by C2W11**) + D5.

**⛔ The precondition is UNMET and the harness spoke is HELD** — see the state snapshot. The Hub launched nothing.

**▶ ADVISOR RULINGS on the lead's four questions (2026-08-12, all four closed — ⛔ do not re-litigate).**
(1) **CONFIRM, no override:** the relay stands; **C2 artifacts close under C2 accounting** (a C3 spoke editing another wave's close entry, its spoke's contradiction, or its curator fold would be editing a record it lacks the context for). The conversion offer is **declined**; C3W1 consumes exactly two outputs.
(2) **AGREED — resolved by SEQUENCING the campaigns, not by moving ownership** (recorded in the snapshot).
(3) **YES — A10 pulled forward**, ownership stays C2W11's (toy-side check); deliverable path fixed at `.claude/outputs/c2w11/DADDR-CEILING-PROBE.json`; the harness treats it exactly as scoped — recorded if present, plain flag with **no invented default** if not.
(4) **KEEP THE SEAM** — ⛔ PG-19 is not built unconditionally. This is *"build the kill-condition before the thing it can kill"* applied to a loader: the scout's §1.3 **GO/CAVEAT/NO-GO with arithmetic** *is* the gate. GO → a small same-wave follow-up cell on an already-landed registry seam; NO-GO → no engineer time spent on an unusable venue. The three-line seam is the correct price of the option.

**Files amended under these rulings:** `c2-closeout-debts.md` (A1 = first act + the worktree/checkout warning + the done-signal named as cross-campaign; A10 pulled forward with its path; the sequencing paragraph re-ratified) · `c3-csf3-harness.md` (the probe artifact named, non-blocking behaviour unchanged, ⛔ no reading the number out of a report in place of the artifact).

### 2026-08-12 — **[C3-OPEN]** Head + Advisor — Campaign 3 opened (C2 closed at its Add.16). C3W1 scope = charter §6. No lead spawned yet.
